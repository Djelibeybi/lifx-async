---
phase: 10-land-the-ipv6-thread-branch
plan: 08
type: execute
wave: 7
depends_on: [10-07]
files_modified:
  - src/lifx/network/mdns/transport.py
  - tests/test_network/test_mdns/test_transport.py
  - src/lifx/network/transport.py
  - tests/test_network/test_transport.py
autonomous: true
gap_closure: true
requirements: [IPV6-01, IPV6-04]
tags: [ipv6, transport, cancellation, resource-lifecycle, gap-closure]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Cancelling MdnsTransport.open() while endpoint creation is suspended closes the owned socket, clears _socket/_protocol/_transport, leaves is_open false, propagates CancelledError unchanged, and permits a later successful open (IPV6-04)."
    - "MdnsTransport.open() retains its existing LifxNetworkError wrapping for OSError while every other BaseException is cleaned up and re-raised with its original type."
    - "Cancelling or failing UdpTransport.open() clears _protocol/_transport/_family, leaves is_open false, and permits a later successful open, preserving IPv4 and IPv6 family selection (IPV6-01, D-01, D-02)."
    - "The SPEC R4 concurrency backstop covers Event-controlled cancellation interleaved with a concurrent open and with close racing open, using the real socket ledger to prove no descriptor or phantom-open state survives either ordering."
    - "Every executable source line and branch changed after the same common pre-gap base captured by plan 10-07 is covered by fresh local branch-coverage evidence; missing changed lines or partial changed branches fail the plan."
  artifacts:
    - path: "src/lifx/network/mdns/transport.py"
      provides: "Cancellation-safe owned-socket cleanup in MdnsTransport.open()"
    - path: "tests/test_network/test_mdns/test_transport.py"
      provides: "Real-socket cancellation ledger and reopen regression"
    - path: "src/lifx/network/transport.py"
      provides: "Cancellation/OSError state reset in UdpTransport.open()"
    - path: "tests/test_network/test_transport.py"
      provides: "UdpTransport interruption and reuse regressions"
  key_links:
    - from: "MdnsTransport.open"
      to: "MdnsTransport.is_open"
      via: "Every unsuccessful exit restores the same three-field closed-state invariant"
      pattern: "self._protocol is not None"
    - from: "UdpTransport.open"
      to: "UdpTransport.send"
      via: "_protocol, _transport and _family transition together, so a cancelled endpoint cannot masquerade as sendable"
      pattern: "self._family"
    - from: ".planning/phases/10-land-the-ipv6-thread-branch/10-GAP-BASE.txt"
      to: "scripts/check_patch_coverage.py"
      via: "The immutable plan 10-07 base scopes fresh coverage and prohibition checks over the complete gap execution range"
      pattern: "--base"
  prohibitions:
    - statement: "MUST NOT retune REQUEST_RETRANSMIT_GAPS, ACK_INFLIGHT_LIMIT, ACK_EXPIRY_SECONDS or DISCOVERY_REBROADCAST_GAPS."
      status: resolved
      verification: judgment
    - statement: "MUST NOT add a runtime dependency; pyproject.toml dependencies remains empty."
      status: resolved
      verification: judgment
    - statement: "MUST NOT weaken coverage or tests: add no coverage exemption, delete no test, add no skip, and do not lower the Codecov patch target."
      status: resolved
      verification: judgment
    - statement: "MUST NOT alter or recharacterise the recorded Thread hardware UAT."
      status: resolved
      verification: judgment
---

<objective>
Close the cancellation half of Phase 10 success criterion 4 and the same phantom-open shape in the general UDP transport, so interruption at endpoint creation cannot strand a descriptor or make a reusable transport claim it is already open.

Purpose: make transport lifecycle invariants hold for cancellation, not only the previously tested OSError path, across both socket implementations used by IPv6 control and discovery.
Output: cancellation-safe cleanup and deterministic regressions in the two existing transport modules.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-SPEC.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-CONTEXT.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-VERIFICATION.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-03-SUMMARY.md
@src/lifx/network/mdns/transport.py
@src/lifx/network/transport.py
@tests/test_network/test_mdns/test_transport.py
@tests/test_network/test_transport.py
@scripts/check_patch_coverage.py
@.planning/phases/10-land-the-ipv6-thread-branch/10-GAP-BASE.txt
</context>

## Artifacts this phase produces

This gap plan creates no public symbol, data field, CLI flag, or production/test file. Planning creates `.planning/phases/10-land-the-ipv6-thread-branch/COVERAGE.md`, the reasoned no-external-API declaration required after the deterministic detector's false positive. Execution modifies `src/lifx/network/mdns/transport.py`, `tests/test_network/test_mdns/test_transport.py`, `src/lifx/network/transport.py`, and `tests/test_network/test_transport.py`, while consuming plan 10-07's immutable `10-GAP-BASE.txt` and coverage utility. New test symbols cover single cancellation, concurrent-open cancellation, close-racing-open cancellation, state reset and reuse for both transports. D-24 remains binding: this plan runs no push and no action may target `redding1`. D-25 remains binding: this plan adds ordinary signed gap-fix commits and does not reshape the already-squashed Phase 10 history.

<tasks>

<task type="tracer">
  <name>Task 1: Make mDNS endpoint creation cancellation-safe and reopenable</name>
  <files>src/lifx/network/mdns/transport.py, tests/test_network/test_mdns/test_transport.py</files>
  <read_first>
    - .planning/phases/10-land-the-ipv6-thread-branch/10-VERIFICATION.md (Orchestrator Amendment, Gap 3: cancellation bypasses the OSError cleanup)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-SPEC.md (IPV6-04, AC 9, R4 concurrency backstop and coverage prohibition)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-03-SUMMARY.md (existing real-socket ledger, OSError cleanup and reopen invariants)
    - src/lifx/network/mdns/transport.py (`open`, `close`, `is_open`)
    - tests/test_network/test_mdns/test_transport.py (`_SocketLedger`, `_RecordingSocket`, `TestMdnsTransportOpenFailureIsClean`, `TestMdnsTransportOpenConcurrency`)
  </read_first>
  <behavior>
    - Test 1: cancel an `open()` task suspended inside `create_datagram_endpoint`; `CancelledError` propagates and the real socket ledger reports created equals closed with no live descriptor
    - Test 2: after cancellation, `_socket`, `_protocol` and `_transport` are all None and `is_open` is false
    - Test 3: the same object opens successfully on a subsequent endpoint attempt
    - Test 4: existing bind, setsockopt and endpoint OSError cases still raise `LifxNetworkError` and remain ResourceWarning-clean
    - Test 5: while one Event-blocked open is cancelled, a concurrent open observes a coherent transition and can subsequently establish the endpoint without a leaked descriptor
    - Test 6: close racing an Event-blocked open followed by cancellation leaves the ledger balanced and the object closed, then a later open succeeds
  </behavior>
  <action>
    Build the cancellation tests with `asyncio.Event`-controlled endpoint awaits and the existing real `_RecordingSocket`; do not use timing sleeps or a descriptor-free `MagicMock` as leak evidence. Cover three deterministic schedules: a single cancelled open, cancellation while a second open is interleaved, and close racing the suspended open before cancellation. In every schedule assert exact ledger counts, coherent closed state, unchanged `CancelledError` propagation and successful reuse. Generalise `MdnsTransport.open()`'s unsuccessful-exit cleanup so every `BaseException`, including `CancelledError`, closes the locally owned socket and restores `_socket`, `_protocol` and `_transport` to None. Preserve exception semantics: wrap only `OSError` as the existing `LifxNetworkError`, while cancellation and any other non-OSError are re-raised unchanged after cleanup. Keep the successful ownership transfer unchanged and reconcile it with the existing non-atomic concurrency backstop so the second opener cannot return permanently from transient `_protocol` state. Commit test and implementation together per D-19, signed with GPG and DCO; do not push per D-24 or rewrite the D-25 series.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_network/test_mdns/test_transport.py -q -W error::ResourceWarning</automated>
  </verify>
  <acceptance_criteria>
    - The cancellation regression fails against the current OSError-only handler by showing a live descriptor and phantom-open state, then passes after the fix
    - `CancelledError` is observed by the caller rather than wrapped, logged as an OSError, or swallowed
    - Socket ledger counts balance and no `ResourceWarning` occurs after traceback references are released and garbage collection runs
    - All three internal references are None after cancellation and the same object can open successfully afterwards
    - Event-controlled concurrent-open and close-racing-open cancellation tests balance the real descriptor ledger, preserve `CancelledError`, finish closed and prove later reuse
    - Existing forced-OSError and concurrency tests remain green without weakening assertions
  </acceptance_criteria>
  <done>MdnsTransport.open() has one cleanup invariant for OSError, cancellation and other interrupted exits: no descriptor, no phantom-open state, original exception semantics, and a working retry.</done>
</task>

<task type="auto">
  <name>Task 2: Restore UdpTransport to a reusable closed state after interrupted open</name>
  <files>src/lifx/network/transport.py, tests/test_network/test_transport.py</files>
  <read_first>
    - .planning/phases/10-land-the-ipv6-thread-branch/10-VERIFICATION.md (Gap 3 final paragraph: UdpTransport.open() has the same unfixed state shape)
    - src/lifx/network/transport.py (`UdpTransport.open`, `_endpoint_lost`, `close`, `is_open`, and the `_family` send guard)
    - tests/test_network/test_transport.py (`TestErrorHandling`, `TestEndpointLoss`, `TestSocketFamilySelection`, `TestSendFamilyAssertion`)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-CONTEXT.md (D-01/D-02 shared family selection and the decision to clear `_family` with transport state)
  </read_first>
  <behavior>
    - Test 1: cancel `UdpTransport.open()` while endpoint creation is suspended; cancellation propagates and `_protocol`, `_transport`, `_family` are all None
    - Test 2: after cancellation the same object opens and sends on a matching IPv4 endpoint
    - Test 3: an endpoint-creation OSError raises `LifxNetworkError`, clears all three fields and permits a later open
    - Test 4: IPv6 bind selection and the send-time family mismatch guard retain their existing behaviour
  </behavior>
  <action>
    Add deterministic cancellation and OSError-reuse tests around a mocked event loop endpoint, using events rather than sleeps. Refactor the unsuccessful exit of `UdpTransport.open()` so `_protocol`, `_transport` and `_family` always return to the closed-state invariant; close `_transport` if an endpoint reference was assigned before a later failure. Preserve the existing `LifxNetworkError` wrapping for `OSError` and re-raise cancellation and other non-OSError unchanged. Do not alter `_UdpProtocol.error_received`, `_FATAL_SOCKET_ERRNOS`, `_endpoint_lost`, address-family selection, broadcast options or send-time mismatch behaviour. Commit test and implementation together per D-19, signed with GPG and DCO; do not push per D-24 or rewrite the D-25 history.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_network/test_transport.py::TestErrorHandling tests/test_network/test_transport.py::TestEndpointLoss tests/test_network/test_transport.py::TestSocketFamilySelection tests/test_network/test_transport.py::TestSendFamilyAssertion -q</automated>
  </verify>
  <acceptance_criteria>
    - Cancellation propagates unchanged and leaves `_protocol`, `_transport` and `_family` all None with `is_open` false
    - Endpoint-creation OSError remains wrapped as `LifxNetworkError` and now leaves the same reusable closed state
    - A later `open()` succeeds after each failure mode, and a matching-family send reaches `sendto`
    - IPv4, IPv6 and zoned-link-local family-selection tests remain green
    - Peer errors remain swallowed without endpoint teardown, endpoint death still raises the typed not-open error, and no retry/timing constant changes
  </acceptance_criteria>
  <done>The general UDP transport cannot be wedged into a phantom-open state by cancellation or endpoint-creation failure, and IPv6 family routing plus peer-error semantics remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Security Profile

- `asvs_level: L1` (opportunistic): the task-cancellation and asyncio endpoint trust boundaries are covered.
- `block_threshold: high`: every critical/high threat is mitigated; any unresolved high finding blocks execution.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller/task cancellation -> transport lifecycle | Untrusted timing of task cancellation can interrupt endpoint creation after internal state or an owned descriptor is allocated |
| asyncio endpoint -> library transport state | Endpoint success/failure callbacks and exceptions determine whether the wrapper reports itself open and reusable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-24 | Denial of Service | `MdnsTransport.open()` | high | mitigate | Real-socket cancellation ledger, three-field reset, unchanged cancellation propagation and reopen test (Task 1) |
| T-10-25 | Denial of Service | `UdpTransport.open()` | high | mitigate | Clear protocol/transport/family on every unsuccessful exit and prove reuse after cancellation and OSError (Task 2) |
| T-10-26 | Repudiation | Cleanup test passes without owning a descriptor | medium | mitigate | Reuse `_RecordingSocket` and `ResourceWarning`-as-error for mDNS; deterministic state assertions cover UdpTransport where asyncio owns socket creation (Tasks 1 and 2) |
| T-10-SC | Tampering | Package supply chain | low | accept | No package-manager operation or dependency change is in either task; the zero-runtime-dependency contract is unchanged |
</threat_model>

## ASVS L1 Control Mapping

| Control | Task / verification |
|---------|---------------------|
| Release resources on every exceptional exit | Task 1 real-socket ledger and Task 2 state reset/reuse tests |
| Preserve exception and lifecycle invariants under concurrency | Task 1 Event-controlled concurrent-open and close-racing-open cancellation schedules |
| Verify security controls with automated tests | Focused regressions plus plan 10-07's changed-line/branch gate; unresolved high findings block execution |

<verification>
- `uv run --frozen pytest tests/test_network/test_mdns/test_transport.py tests/test_network/test_transport.py -q -W error::ResourceWarning`
- `uv run ruff check src/lifx/network/mdns/transport.py src/lifx/network/transport.py tests/test_network/test_mdns/test_transport.py tests/test_network/test_transport.py`
- `uv run ruff format --check src/lifx/network/mdns/transport.py src/lifx/network/transport.py tests/test_network/test_mdns/test_transport.py tests/test_network/test_transport.py`
- `uv run pyright`
- `LIFX_REQUIRE_IPV6=1 uv run --frozen pytest tests/test_api/test_ipv6_e2e.py -q`
- `uv run --frozen pytest -q`
- `uv run --frozen pytest tests/test_network/test_mdns/test_transport.py tests/test_network/test_transport.py --cov-report=json:coverage-10-08.json -q -W error::ResourceWarning && uv run scripts/check_patch_coverage.py --base "$(cat .planning/phases/10-land-the-ipv6-thread-branch/10-GAP-BASE.txt)" --coverage coverage-10-08.json --source src/lifx/network/mdns/transport.py --source src/lifx/network/transport.py`
- `uv run --frozen pytest tests/test_scripts/test_check_patch_coverage.py::test_rejects_added_excluded_line tests/test_scripts/test_check_patch_coverage.py::test_rejects_added_coverage_or_skip_marker -q`
- `uv run scripts/check_patch_coverage.py --base "$(cat .planning/phases/10-land-the-ipv6-thread-branch/10-GAP-BASE.txt)" --check-weakening-only`
- `git diff --quiet "$(cat .planning/phases/10-land-the-ipv6-thread-branch/10-GAP-BASE.txt)"..HEAD -- pyproject.toml codecov.yml .planning/phases/10-land-the-ipv6-thread-branch/10-UAT-RESULTS.json src/lifx/const.py src/lifx/animation/flow.py`
- `test -z "$(git diff --diff-filter=D --name-only "$(cat .planning/phases/10-land-the-ipv6-thread-branch/10-GAP-BASE.txt)"..HEAD -- tests)"`
- API-coverage verification passes via `COVERAGE.md`'s reasoned declaration that this local UDP/mDNS work integrates no external API, SDK or service; the detector's “adopts endpoint” signal is a false positive
- Assumption-delta matches on “second” and “optional” are incidental wording from the existing phase scope; transport cancellation changes no core identity assumption
</verification>

<success_criteria>
Both UDP endpoint wrappers survive cancellation and OSError with coherent closed state, preserved exception types, no mDNS descriptor leak, successful reuse, green IPv4/IPv6 behaviour and no Phase 10 scope-fence regression.
</success_criteria>

<output>
Create `.planning/phases/10-land-the-ipv6-thread-branch/10-08-SUMMARY.md` when done.
</output>
