---
phase: 10-land-the-ipv6-thread-branch
plan: 07
type: execute
wave: 6
depends_on: [10-06]
files_modified:
  - src/lifx/network/mdns/discovery.py
  - tests/test_network/test_mdns/test_discovery.py
  - scripts/check_patch_coverage.py
  - tests/test_scripts/test_check_patch_coverage.py
  - .planning/phases/10-land-the-ipv6-thread-branch/10-GAP-BASE.txt
autonomous: true
gap_closure: true
requirements: [IPV6-01, IPV6-02, IPV6-03]
tags: [ipv6, mdns, discovery, resilience, gap-closure]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "A bare IPv6 link-local service record is rejected by the shared address validator without terminating discover_devices_mdns(); later valid records in the same sweep are still yielded (IPV6-01, IPV6-02, D-01, D-02, D-03)."
    - "User-supplied zone-less link-local addresses still raise ValueError at the four public address entry points; discovery resilience does not weaken the validation contract (SPEC R2 adjacency edge, D-04, D-07)."
    - "A follow-up A/AAAA query send failure cannot discard records that _LifxRecordCache.resolve() has already marked resolved; those records are yielded exactly once."
    - "Follow-up query traffic has two independent bounds: no more than 64 distinct targets are attempted and each target receives at most two send attempts, so one transient failure is retryable but persistent failure cannot generate unbounded traffic."
    - "Every executable source line and branch changed after the captured common pre-gap base is covered by fresh local branch-coverage evidence; missing changed lines or partial changed branches fail the plan."
  artifacts:
    - path: "src/lifx/network/mdns/discovery.py"
      provides: "Per-record invalid-address degradation and resolved-record-first delivery ordering"
    - path: "tests/test_network/test_mdns/test_discovery.py"
      provides: "Public-generator and follow-up-send regression coverage"
    - path: "scripts/check_patch_coverage.py"
      provides: "Repository-local changed-line and changed-branch coverage assertion over coverage JSON and a git base"
    - path: ".planning/phases/10-land-the-ipv6-thread-branch/10-GAP-BASE.txt"
      provides: "Immutable common base SHA used by both gap plans for coverage and prohibition diffs"
  key_links:
    - from: "src/lifx/network/mdns/discovery.py::discover_devices_mdns"
      to: "src/lifx/network/mdns/discovery.py::create_device_from_record"
      via: "Per-record ValueError handling preserves the outer async-generator sweep"
      pattern: "create_device_from_record"
    - from: "src/lifx/network/mdns/discovery.py::_LifxRecordCache.resolve"
      to: "src/lifx/network/mdns/discovery.py::discover_lifx_services"
      via: "Resolved records are yielded before unrelated follow-up address-query sends"
      pattern: "record_cache.resolve"
  prohibitions:
    - statement: "MUST NOT retune REQUEST_RETRANSMIT_GAPS, ACK_INFLIGHT_LIMIT, ACK_EXPIRY_SECONDS or DISCOVERY_REBROADCAST_GAPS."
      status: resolved
      verification: judgment
    - statement: "MUST NOT add a runtime dependency; pyproject.toml dependencies remains empty."
      status: resolved
      verification: judgment
    - statement: "MUST NOT weaken coverage or tests: add no coverage exemption, delete no test, add no skip, and do not lower the Codecov patch target."
      status: resolved
      verification: judgment
    - statement: "MUST NOT alter or recharacterise the recorded Thread hardware UAT."
      status: resolved
      verification: judgment
---

<objective>
Close the two mDNS discovery blockers in the Phase 10 verification amendment: one unusable bare link-local record must not terminate the public device generator, and an unrelated follow-up query-send failure must not erase records already resolved by the cache.

Purpose: preserve discovery availability when a Thread border router supplies mixed-quality records or the local network rejects one auxiliary query, while retaining the shared validation contract from D-01 through D-07.
Output: production and regression-test changes in the existing mDNS discovery module only.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-SPEC.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-CONTEXT.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-VERIFICATION.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-03-SUMMARY.md
@src/lifx/network/mdns/discovery.py
@tests/test_network/test_mdns/test_discovery.py
</context>

## Artifacts this phase produces

This gap plan creates no public library symbol, data field or CLI flag. Planning creates `.planning/phases/10-land-the-ipv6-thread-branch/COVERAGE.md`, the reasoned no-external-API declaration required after the deterministic detector's false positive. Execution modifies `src/lifx/network/mdns/discovery.py` and `tests/test_network/test_mdns/test_discovery.py`, and adds the internal verification utility `scripts/check_patch_coverage.py`, its tests, and `10-GAP-BASE.txt`. New test symbols cover public-generator degradation, unrelated constructor failures, resolved-record delivery, bounded follow-up attempts, and the coverage utility. D-24 remains binding: this plan runs no push, and no action may target `redding1`. D-25 remains binding: this plan adds ordinary signed gap-fix commits and does not reshape the already-squashed Phase 10 history.

<tasks>

<task type="tracer">
  <name>Task 1: Keep the public mDNS device sweep alive after a bare link-local record</name>
  <files>src/lifx/network/mdns/discovery.py, tests/test_network/test_mdns/test_discovery.py, .planning/phases/10-land-the-ipv6-thread-branch/10-GAP-BASE.txt</files>
  <read_first>
    - .planning/phases/10-land-the-ipv6-thread-branch/10-VERIFICATION.md (Orchestrator Amendment, Gap 1: reproduced public-generator termination)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-SPEC.md (R2 and Boundaries: retain validation; B3 address ordering remains Phase 11)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-CONTEXT.md (D-01 through D-07: one validator and four strict user-input entry points)
    - src/lifx/network/mdns/discovery.py (`_pick_address`, `create_device_from_record`, `discover_devices_mdns`)
    - tests/test_network/test_mdns/test_discovery.py (`TestCreateDeviceFromRecord`, `TestDiscoverDevicesMdns`)
  </read_first>
  <behavior>
    - Test 1: an async service stream yielding bare `fe80::1` followed by a valid IPv4 record produces the valid device and does not raise from `discover_devices_mdns()`
    - Test 2: direct `create_device_from_record()` for bare `fe80::1` continues to raise the shared validator's zone-identifier `ValueError`
    - Test 3: a relay-only record still filters normally when adjacent to an invalid-address record
    - Test 4: a non-address `ValueError` raised by device construction propagates unchanged rather than being classified as an invalid address
  </behavior>
  <action>
    Before writing a test or production edit, capture `git rev-parse HEAD` as the one full SHA in `10-GAP-BASE.txt`; this is the immutable common pre-gap base consumed by both plans. Then write the failing public-generator regressions. In `discover_devices_mdns()`, validate each service record's selected address with the existing `validate_address()` immediately before device construction, and catch `ValueError` only around that validation call. Emit a structured DEBUG record naming `discover_devices_mdns`, an `invalid_address` action, the serial and address, then continue to the next record. Call `create_device_from_record()` outside that handler so invalid serials, ports and unrelated constructor/programming `ValueError`s still propagate accurately. Do not change `_pick_address()` or introduce address ordering, scope synthesis, interface guessing, or an alternate validator: B3 stays outside Phase 10, and D-01 through D-07 require the shared validator to remain authoritative. Do not catch cancellation or broad runtime failures here. Commit the production change with its tests and base record in the same GPG-signed, DCO-signed commit per D-19. Run no push per D-24, especially none to `redding1`, and do not rewrite the D-25 history.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py::TestDiscoverDevicesMdns -q</automated>
  </verify>
  <acceptance_criteria>
    - The new public-generator test fails before the production change because the first bare link-local record raises, then passes after the change and yields the later valid device
    - `create_device_from_record()` still raises `ValueError` for `fe80::1`; the four user-facing IPV6-02 entry-point tests remain green
    - `_pick_address()` is unchanged, so the Phase 11 B3 boundary is preserved
    - The handler encloses only the shared address-validation call; a constructor `ValueError` unrelated to the address propagates unchanged
    - The invalid record produces a structured DEBUG log without terminating or mislabelling the valid record
  </acceptance_criteria>
  <done>A known-reachable bare link-local advertisement is skipped as one unusable discovery result, while the same sweep continues and yields subsequent controllable devices without weakening IPV6-02 or duplicating IPV6-03.</done>
</task>

<task type="auto">
  <name>Task 2: Deliver resolved records before fallible follow-up address queries</name>
  <files>src/lifx/network/mdns/discovery.py, tests/test_network/test_mdns/test_discovery.py</files>
  <read_first>
    - .planning/phases/10-land-the-ipv6-thread-branch/10-VERIFICATION.md (Orchestrator Amendment, Gap 2: resolved instances consumed before a failed send)
    - src/lifx/network/mdns/discovery.py (`_LifxRecordCache.resolve`, `pending_targets`, the receive-loop ordering around `extracted` and `transport.send`)
    - tests/test_network/test_mdns/test_discovery.py (`TestMdnsFollowUpAddressQueries`, `_fake_transport`, `_receive_script`)
    - .agents/skills/spike-findings-lifx-async/references/discovery.md (keep async generator, first-wins serial dedupe, and bounded discovery traffic)
  </read_first>
  <behavior>
    - Test 1: a packet containing one fully resolved device and one pending target still yields the resolved device when the pending target's address-query send raises `LifxNetworkError`
    - Test 2: the resolved serial is yielded once, despite duplicate packets after the send failure
    - Test 3: one transient send failure is retried once and succeeds; the target receives no further query after success
    - Test 4: repeated duplicate packets for one persistently failing target produce exactly two send attempts, never an unbounded sequence
    - Test 5: packets naming more than 64 distinct unresolved targets cause query traffic for at most the first 64 targets, including targets whose sends fail
  </behavior>
  <action>
    Write the failing mixed-batch and traffic-bound regressions using the existing fake transport. Reorder the receive-loop work so extracted records update the idle deadline, pass serial deduplication, and yield before any unrelated follow-up query is awaited. Isolate each follow-up `transport.send()` failure at the individual target: log a structured DEBUG `address_query_failed` record and continue processing rather than routing it through the packet parse-error handler. Keep `queried_targets` as the successful-send ledger and add a separate per-target attempt ledger: admit at most 64 distinct targets to that ledger, increment before each send, permit no more than two attempts per target, and stop attempting a target once a send succeeds. This makes one transient failure retryable while bounding persistent failures to two sends and total traffic-bearing targets to 64. Retain first-wins serial dedupe. Do not change DNS parsing, cache capacity, retransmission constants, address preference, or public signatures. Commit tests and behaviour together per D-19; do not push per D-24 or reshape the D-25 history.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py::TestMdnsFollowUpAddressQueries tests/test_network/test_mdns/test_discovery.py::TestMdnsSerialDeduplication -q</automated>
  </verify>
  <acceptance_criteria>
    - The reviewer reproduction now yields the already-resolved serial even when the auxiliary address-query send fails
    - A resolved instance remains emitted at most once for the cache lifetime and serial deduplication remains first-wins
    - A transiently failed target can retry once, a persistently failed target receives exactly two attempts, and successful targets remain one-shot
    - The attempted-target ledger, not only the successful-target set, enforces the 64-distinct-target traffic cap
    - The per-target network failure is logged and does not become a packet parse error
    - Existing timeout, idle-deadline, retransmission, cache-bound and serial-dedupe tests remain green
  </acceptance_criteria>
  <done>Fallible auxiliary queries can no longer erase resolved devices, and transient send failures remain bounded and retryable without altering the discovery protocol or its traffic constants.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce changed-line and changed-branch coverage from one pre-gap base</name>
  <files>scripts/check_patch_coverage.py, tests/test_scripts/test_check_patch_coverage.py</files>
  <read_first>
    - .planning/phases/10-land-the-ipv6-thread-branch/10-VERIFICATION.md (Codecov gate decision and the established local diff-plus-coverage measurement)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-SPEC.md (AC 18 and the coverage/test prohibitions)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-01-PLAN.md (coverage JSON/XML is authoritative for missing lines and partial branches)
    - pyproject.toml (pytest already records branch coverage for `lifx`)
  </read_first>
  <behavior>
    - Test 1: added executable lines reported as missing in coverage JSON fail with file and line details
    - Test 2: a missing branch whose source is an added executable line fails with the source/destination arc
    - Test 3: fully executed changed lines and all outgoing arcs pass; changed comments and blank lines do not require execution
    - Test 4: a requested changed source file absent from coverage JSON fails rather than passing vacuously
    - Test 5: an added executable line present in coverage JSON `excluded_lines`, but absent from both `executed_lines` and `missing_lines`, fails with the file and line number
    - Test 6: a full `base..HEAD` weakening-only scan rejects added `# pragma: no cover`, `# pragma: no branch`, `# coverage: ignore`, `@pytest.mark.skip`, `@pytest.mark.skipif`, `pytest.skip(`, `pytest.importorskip(`, `@unittest.skip`, `@unittest.skipIf` and `@unittest.skipUnless`, while unchanged pre-base markers do not fail the scan
  </behavior>
  <action>
    Validate the Task 1 base record as a single full SHA and never rewrite it in plan 10-08. Add a stdlib-only script accepting `--base`, `--coverage` and one or more `--source` paths, plus a `--check-weakening-only` mode that requires only the base. Parse zero-context git diff hunks from the base through the working `HEAD` to derive added line numbers per requested source file. Parse coverage.py JSON's `executed_lines`, `missing_lines`, `excluded_lines`, `executed_branches` and `missing_branches`: treat their line union as the executable-line authority, fail immediately when an added line intersects `excluded_lines`, fail when any other changed executable line is missing, and fail when any missing branch originates on a changed executable line. The weakening-only mode must inspect every added line across the complete base-to-HEAD diff, independently of `--source`, and reject newly introduced line/branch coverage pragmas, coverage-ignore directives, pytest skip/skipif/imperative-skip/importorskip forms, and unittest skip decorators; also fail on any test deletion and on changes to coverage configuration or patch-target files. Match added content only, not diff headers or unchanged context, and report the file, line and matched category. Fail closed when the base is invalid, a requested changed source file has no coverage entry, any required JSON array is absent or malformed, the report lacks branch data, git exits non-zero, or diff/report parsing fails. Print the measured changed executable-line and changed-branch totals on success so the SUMMARY records fresh evidence. Unit-test the parser and every failure mode with temporary git repositories and synthetic coverage JSON, including an executable added line that appears only in `excluded_lines` and parameterised added-line cases for every weakening marker; prove the same marker committed before the base does not fail. Use no new dependency. This utility implements the local evidence accepted in the verification amendment and is used by both gap plans; it does not cite PR #208's vacuous status. Commit these tests and the utility together per D-19, signed with GPG and DCO; do not push per D-24 or rewrite the D-25 series.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_scripts/test_check_patch_coverage.py::test_rejects_added_excluded_line tests/test_scripts/test_check_patch_coverage.py::test_rejects_added_coverage_or_skip_marker -q &amp;&amp; uv run --frozen pytest tests/test_scripts/test_check_patch_coverage.py --cov=scripts.check_patch_coverage --cov-report=json:coverage-checker.json -q &amp;&amp; uv run scripts/check_patch_coverage.py --base "$(cat .planning/phases/10-land-the-ipv6-thread-branch/10-GAP-BASE.txt)" --check-weakening-only &amp;&amp; uv run scripts/check_patch_coverage.py --base "$(cat .planning/phases/10-land-the-ipv6-thread-branch/10-GAP-BASE.txt)" --coverage coverage-checker.json --source scripts/check_patch_coverage.py</automated>
  </verify>
  <acceptance_criteria>
    - `10-GAP-BASE.txt` names the common pre-gap commit and plan 10-08 consumes the same immutable SHA
    - The utility exits non-zero for an added excluded line, a missing changed line, a partial changed branch, absent or malformed line/branch arrays, an absent changed source entry, or an invalid base
    - The weakening-only mode scans every added line and deleted test across the full common-base-to-HEAD range, independent of coverage source selection, and fails on any newly added coverage exemption or test-skip mechanism without penalising an unchanged pre-base marker
    - The utility exits zero only after reporting a non-zero changed executable-line total and the exact changed-branch total for each changed production source file
    - The implementation uses only git, coverage JSON already produced by pytest-cov, and Python's standard library
  </acceptance_criteria>
  <done>Both gap plans have one deterministic local authority that fails on uncovered changed source lines or partial changed branches across the complete execution range.</done>
</task>

</tasks>

<threat_model>
## Security Profile

- `asvs_level: L1` (opportunistic): primary LAN and event-loop trust boundaries are covered.
- `block_threshold: high`: every critical/high threat is mitigated; any unresolved high finding blocks execution.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN mDNS packets -> record cache/public generator | Untrusted service records and address sets determine whether a device object can be constructed and whether the discovery sweep continues |
| Local event loop -> multicast transport send | A network failure during an auxiliary query occurs between cache resolution and user-visible delivery |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-21 | Denial of Service | `discover_devices_mdns()` | high | mitigate | Catch the expected per-record validation failure, keep strict user-input validation, and prove a later valid device is yielded in the same sweep (Task 1) |
| T-10-22 | Denial of Service/Repudiation | `discover_lifx_services()` follow-up sends | high | mitigate | Yield resolved records before auxiliary sends, isolate each send failure, and test exact-once delivery plus bounded retry state (Task 2) |
| T-10-23 | Spoofing | Invalid mDNS address silently accepted | medium | mitigate | Continue using `create_device_from_record()` and the D-01 shared validator; skip the invalid record rather than constructing a device with it (Task 1) |
| T-10-SC | Tampering | Package supply chain | low | accept | No package-manager operation or dependency change is in either task; the zero-runtime-dependency contract is unchanged |
</threat_model>

## ASVS L1 Control Mapping

| Control | Task / verification |
|---------|---------------------|
| Validate untrusted input at the trust boundary | Task 1 scopes the shared validator handler and proves unrelated failures propagate |
| Bound resource consumption | Task 2 enforces two attempts per target and 64 distinct attempted targets |
| Verify security controls with automated tests | Focused regressions plus Task 3's changed-line/branch gate; unresolved high findings block execution |

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | - | IPv6-only devices remain controllable after the landed branch | 10-07, 10-08 | COVERED | Gap plans restore discovery and transport availability without reopening landed capability work |
| REQ | IPV6-01 | IPv6 connect/control/animation path | 10-07, 10-08 | COVERED | Discovery and UDP cancellation blockers close |
| REQ | IPV6-02 | Immediate strict zone-less rejection | 10-07 | COVERED | Strict construction remains; only the discovery sweep degrades per record |
| REQ | IPV6-03 | One address-family implementation | 10-07 | COVERED | No parser or validator is added; existing helper remains authoritative |
| REQ | IPV6-04 | Failed mDNS open leaves no socket | 10-08 | COVERED | Cancellation path added to the prior OSError coverage |
| RESEARCH | - | None in gap-closure mode | - | EXCLUDED | VERIFICATION plus live source is authoritative |
| CONTEXT | D-01..D-23 | Original locked implementation decisions | 10-01..10-06 | COVERED | Historical plans and summaries remain unchanged; D-01..D-07 and D-19 are cited where the gap touches them |
| CONTEXT | D-24 | Push only to origin | 10-07, 10-08 | COVERED | Plans run no push and explicitly prohibit `redding1` |
| CONTEXT | D-25 | Two-commit landed series | 10-07, 10-08 | COVERED | Gap commits do not rewrite the landed history |
| SPEC | R2 adjacency edge | Bare link-local rejected, zoned accepted | 10-07 | COVERED | Public discovery skips the unusable record while user entry points stay strict |
| SPEC | Prohibitions 1-4 | No retuning, dependency, gate weakening or false UAT | 10-07, 10-08 | COVERED | Repeated in must_haves and verification |

<verification>
- `uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py -q`
- `uv run --frozen pytest tests/test_network/test_address.py tests/test_devices/test_base.py tests/test_api/test_api_discovery.py -q`
- `uv run ruff check src/lifx/network/mdns/discovery.py tests/test_network/test_mdns/test_discovery.py`
- `uv run ruff format --check src/lifx/network/mdns/discovery.py tests/test_network/test_mdns/test_discovery.py`
- `uv run pyright`
- `uv run --frozen pytest -q`
- `uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py tests/test_scripts/test_check_patch_coverage.py --cov=scripts.check_patch_coverage --cov-report=json:coverage-10-07.json -q && uv run scripts/check_patch_coverage.py --base "$(cat .planning/phases/10-land-the-ipv6-thread-branch/10-GAP-BASE.txt)" --coverage coverage-10-07.json --source src/lifx/network/mdns/discovery.py --source scripts/check_patch_coverage.py`
- `uv run --frozen pytest tests/test_scripts/test_check_patch_coverage.py::test_rejects_added_excluded_line tests/test_scripts/test_check_patch_coverage.py::test_rejects_added_coverage_or_skip_marker -q`
- `uv run scripts/check_patch_coverage.py --base "$(cat .planning/phases/10-land-the-ipv6-thread-branch/10-GAP-BASE.txt)" --check-weakening-only`
- `git diff --quiet "$(cat .planning/phases/10-land-the-ipv6-thread-branch/10-GAP-BASE.txt)"..HEAD -- pyproject.toml codecov.yml .planning/phases/10-land-the-ipv6-thread-branch/10-UAT-RESULTS.json src/lifx/const.py src/lifx/animation/flow.py`
- `test -z "$(git diff --diff-filter=D --name-only "$(cat .planning/phases/10-land-the-ipv6-thread-branch/10-GAP-BASE.txt)"..HEAD -- tests)"`
- API-coverage verification passes via `COVERAGE.md`'s reasoned declaration that this local UDP/mDNS work integrates no external API, SDK or service; the detector's “adopts endpoint” signal is a false positive
- Assumption-delta matches on “second” and “optional” are incidental wording from the existing phase scope; this gap changes no core identity assumption
</verification>

<success_criteria>
One malformed mDNS address or one failed auxiliary query can no longer terminate or silently empty an otherwise productive discovery sweep; strict address validation, dedupe, traffic bounds, test coverage and Phase 10 scope fences remain intact.
</success_criteria>

<output>
Create `.planning/phases/10-land-the-ipv6-thread-branch/10-07-SUMMARY.md` when done.
</output>
