---
phase: 10-land-the-ipv6-thread-branch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lifx/devices/base.py
  - src/lifx/network/connection.py
  - src/lifx/network/mdns/discovery.py
  - src/lifx/network/mdns/dns.py
  - src/lifx/network/mdns/transport.py
  - src/lifx/network/transport.py
  - src/lifx/animation/animator.py
  - scripts/ipv6_thread_probe.py
  - tests/test_network/test_mdns/test_discovery.py
  - tests/test_network/test_mdns/test_transport.py
  - tests/test_animation/test_animator.py
  - .planning/phases/10-land-the-ipv6-thread-branch/10-COVERAGE-GAPS.md
autonomous: true
requirements: [IPV6-01]
tags: [ipv6, thread, rebase, network, transport]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "The three branch commits (content of b49400b, b88cdb9, 2f884f5) are replayed onto the tip of main as a pure replay: the diff between the pre-rebase head 2f884f5 and the rebased head, restricted to the 11 branch-touched paths, is empty"
    - "Each replayed commit carries a good GPG signature and its original DCO Signed-off-by line (redding1 on the first commit, Avi Miller on the second and third)"
    - "The full test suite passes on the rebased head with no test deleted, skipped or modified to make it pass"
    - "A Light connects to an in-process emulator bound to ::1 and answers get_color() over an AF_INET6 socket — the rebased branch delivers IPv6 control end-to-end (tracer proof for IPV6-01)"
    - "The branch-patch-coverage debt of the rebased branch is MEASURED in this wave, not discovered at merge time: every uncovered line and partial branch in the branch diff is enumerated per file in 10-COVERAGE-GAPS.md with an owning plan (review finding 3)"
  artifacts:
    - "src/lifx/network/mdns/discovery.py — branch rewrite (_LifxRecordCache, _pick_address, PTR retransmit, follow-up A/AAAA) present on the rebased head"
    - "scripts/ipv6_thread_probe.py — 521-line three-stage hardware probe present on the rebased head"
    - "src/lifx/animation/animator.py — frame-socket family follows the device address on the rebased head"
    - ".planning/phases/10-land-the-ipv6-thread-branch/10-COVERAGE-GAPS.md — the measured branch-coverage gap list, one section per file, each gap assigned to plan 10-02 or 10-03"
  key_links:
    - "devices/base.py accepts IPv6 addresses -> connection.py binds the matching wildcard -> transport.py opens the matching socket family (the address-family seam the v2.0 research judged correct)"
  prohibitions:
    - statement: "MUST NOT retune any WiFi-measured reliability constant (REQUEST_RETRANSMIT_GAPS, ACK_INFLIGHT_LIMIT, ACK_EXPIRY_SECONDS, DISCOVERY_REBROADCAST_GAPS) in this phase"
      status: resolved
      verification: judgment
    - statement: "MUST NOT reach the green gate by weakening the checks: no new pragma-no-cover on IPv6 code, no existing test deleted or skipped to make the rebase pass, no lowering of the Codecov patch target"
      status: resolved
      verification: judgment
    - statement: "MUST NOT add a runtime dependency (pyproject.toml dependencies stays [])"
      status: resolved
      verification: judgment
---

<objective>
Rebase the three-commit `feat/ipv6-thread-support` branch (`b49400b`, `b88cdb9`, `2f884f5`) onto `main` as a verifiably pure replay, then prove end-to-end that the rebased branch controls an IPv6-only device against an in-process emulator bound to `::1`.

This is the tracer slice for the whole phase (per D-23 plan 1 and D-18's one-PR shape): every later plan stacks fix commits on this branch, and the `10-PATTERNS.md` line references marked *(branch)* only resolve after this rebase lands the branch content in the working tree. Landing the branch whole — including the `network/mdns/discovery.py` + `network/mdns/dns.py` rewrite and `scripts/ipv6_thread_probe.py` — is locked by the SPEC Boundaries; no behaviour change happens in this plan (D-19: the rebase is a pure replay; fixes come in plans 02–03 with their tests in the same commits).

Purpose: nothing else in the v2.0 milestone is testable until the branch is on a rebased footing (Phase 10 is the critical path).
Output: `feat/ipv6-thread-support` rebased onto the tip of `main`, suite green, IPv6 end-to-end smoke proven, and the branch's patch-coverage debt measured and written down.

<review_response>
Revised 2026-08-27 from `10-REVIEWS.md`. Changes landing in this plan:

- **Finding 3 (HIGH, all three reviewers).** Branch-patch-coverage debt was being discovered in Wave 5, after the PR and UAT sequencing were already in motion. `pyproject.toml:117` already enables `--cov-branch`, so the debt is measurable the moment the rebase lands. Task 2 now measures it here and writes `10-COVERAGE-GAPS.md`; plans 10-02 and 10-03 close the gaps in the files they own; plan 10-06 Task 1 verifies the list is empty rather than discovering its size.
- **Finding 8 (MEDIUM).** The old acceptance criterion required `main..HEAD` to contain exactly three commits, which this plan's own SUMMARY commit made unsatisfiable. It now asserts the three *oldest* commits with `git log --reverse`, which stays true however many commits stack above them.
- **Codex LOW, folded in at no cost:** `git log --show-signature` is a presentation check, so `git verify-commit` on the exact replayed SHAs is used instead; the preflight now fetches and records the `main` / `origin/main` relationship before rewriting history.
- **OpenCode LOW, folded in at no cost:** the tracer smoke run is throwaway, so its actual output (not merely "it passed") must be pasted into `10-01-SUMMARY.md`, since that narrative is the only durable evidence until plan 10-04 turns it into committed tests.
</review_response>
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-SPEC.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-CONTEXT.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols created across Phase 10 (all plans; listed here for source-grounding):
`lifx.network.address` module with `validate_address()`, `family_for()`, `wildcard_for()`; `tests/test_network/test_address.py`; the send-time family assertion in `UdpTransport.send()` and the leak-safe `MdnsTransport.open()`; conftest helpers and fixtures `get_free_port6()`, `_Ipv6EmulatedLifxServer` (test-only `EmulatedLifxServer` subclass whose `start()` adopts a socket configured before bind), `ipv6_available`, `emulator_server_ipv6`, `ipv6_light`; test module `tests/test_api/test_ipv6_e2e.py`; test package `tests/test_scripts/` containing `tests/test_scripts/test_ipv6_thread_probe.py`; env var `LIFX_REQUIRE_IPV6`; CI matrix env wiring in `.github/workflows/ci.yml`; `scripts/ipv6_thread_probe.py` control stage, streaming stage, its `--serial`, `--uat-output` and `--stream` flags, and its new pure helpers `_select_target()`, `_capture_device_state()`, `_restore_device_state()`, `_stage_result()`, `_build_uat_record()`, `_write_uat_record()`; phase artefacts `10-COVERAGE-GAPS.md` (this plan), `10-UAT-RESULTS.json` / `10-EXCEPTION-OVERRIDE.json`. Arriving via this plan's rebase: `_LifxRecordCache`, `_pick_address()`, `build_address_query()`, `_encode_name()` (mdns rewrite), `scripts/ipv6_thread_probe.py` (three stages: records/ports/connect), and the branch's IPv6 family-selection code at `network/transport.py`, `network/connection.py`, `animation/animator.py`, `devices/base.py`.

<tasks>

<!-- type="tracer" is deliberate (tracer-first decomposition): confirmed 2026-08-27 against the executor contract — gsd-core/workflows/execute-plan.md defines type="tracer" as "execute like type=auto (production-quality, real verify, commit)" plus a tracer feedback gate before any expansion task, so no rename to auto is needed. -->
<task type="tracer">
  <name>Task 1: Rebase feat/ipv6-thread-support onto main and prove IPv6 control end-to-end</name>
  <files>src/lifx/devices/base.py, src/lifx/network/connection.py, src/lifx/network/mdns/discovery.py, src/lifx/network/mdns/dns.py, src/lifx/network/mdns/transport.py, src/lifx/network/transport.py, src/lifx/animation/animator.py, scripts/ipv6_thread_probe.py, tests/test_network/test_mdns/test_discovery.py, tests/test_network/test_mdns/test_transport.py, tests/test_animation/test_animator.py</files>
  <read_first>
    - .planning/phases/10-land-the-ipv6-thread-branch/10-SPEC.md (Background + Requirement 8: what the branch changes and why the rebase is expected clean)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-CONTEXT.md (D-18, D-19, D-20: one PR, pure replay, do NOT refresh backup/ipv6-thread-pre-rebase)
    - Output of `git log --oneline main..feat/ipv6-thread-support` and `git show --stat b49400b b88cdb9 2f884f5` (the exact 11-file surface being replayed)
    - tests/conftest.py lines 83-160 and 241-300 (get_free_port, emulator_available, tile_chain_server — the shapes the throwaway smoke script mirrors; do not commit fixture changes in this plan, that is plan 10-04's work)
  </read_first>
  <precondition>Working tree is clean, `git rev-parse feat/ipv6-thread-support` prints 2f884f513415d854f9a2d097d8071d373d610d3f, and `git merge-base main feat/ipv6-thread-support` prints 42c9ad21937c8b787feb71c5caf5c2489289446d.</precondition>
  <action>
    Per D-18 and D-23 plan 1. Steps:

    0. Preflight the remote relationship BEFORE rewriting history (review finding: the old preflight never looked at `origin`). Run `git fetch --prune origin`, then record the output of `git rev-parse main origin/main` and `git merge-base --is-ancestor origin/main main; echo $?` in the summary. Local `main` legitimately carries planning commits that `origin/main` does not, so equality is NOT required; what must hold is that `origin/main` is an ancestor of local `main`. If it is not, `origin/main` has moved independently — STOP and report rather than rebasing onto a divergent base.
    1. `git switch feat/ipv6-thread-support`, then `git rebase main`. The rebase is expected clean: no commit on main since the 42c9ad2 divergence touches src/lifx/network, src/lifx/animation or src/lifx/devices/base.py (verified in CONTEXT). If any conflict appears, STOP and report — a conflict falsifies the phase's premise and must be surfaced, not resolved ad hoc.
    2. Do NOT refresh or touch `backup/ipv6-thread-pre-rebase` (D-20: it is stale at af17071 and the reflog plus the coming PR are the recovery path). Do not push yet; the push and PR happen in plan 10-06.
    3. Prove pure replay: `git diff 2f884f5 HEAD -- src/lifx/network src/lifx/animation src/lifx/devices/base.py scripts/ipv6_thread_probe.py tests/test_network/test_mdns tests/test_animation` must produce no output (main did not touch these paths, so any diff means the replay altered branch content).
    4. Verify signatures and sign-off with a strict per-commit verifier, not a presentation check (review finding): capture the three replayed SHAs with `git rev-list --reverse main..HEAD` and run `git verify-commit <sha>` on each — it exits non-zero on a bad or missing signature, which `git log --show-signature` does not (rebase re-signs as committer with GPG key 27B3A9EA9D847501F05627B166D6066620F03B05). Separately, `git log -3 --format='%h %(trailers:key=Signed-off-by)'` must show the original DCO trailers (redding1 on the replayed b49400b, Avi Miller on the other two). Preserving these is SPEC AC 17's precondition.
    5. Run the full gate locally: `uv run --frozen pytest`, `uv run pyright`, `uv run ruff check .`, `uv run ruff format --check .`. All must pass with zero test deletions, zero new skips, zero source edits — this plan changes no behaviour (D-19). If a branch test fails against main's tip, report the failure; do not patch it here.
    6. Tracer end-to-end proof (throwaway, NOT committed): write a scratch script under the session scratchpad directory that (a) starts an in-process `lifx-emulator-core` server with one colour-capable Light device and `bind_address="::1"` on a free IPv6 port (mirror the construction in tests/conftest.py `emulator_server`/`tile_chain_server`, substituting the bind address; `EmulatedLifxServer` passes bind_address straight into create_datagram_endpoint, so asyncio infers AF_INET6 — verified in SPEC Background), (b) constructs `Light(serial=..., ip="::1", port=...)`, enters `async with`, calls `get_color()` and `set_color()`, and (c) asserts the connection's underlying socket family is `socket.AF_INET6` via the transport's `get_extra_info("socket").family`. Run it with `uv run python`. This proves the rebased branch's devices->connection->transport family seam works end-to-end before any expansion work builds on it. Expect the loopback warning to fire (D-13: leave it alone).
  </action>
  <verify>
    <automated>git diff --quiet 2f884f5 HEAD -- src/lifx/network src/lifx/animation src/lifx/devices/base.py scripts/ipv6_thread_probe.py tests/test_network/test_mdns tests/test_animation && git merge-base --is-ancestor main HEAD && uv run --frozen pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The three OLDEST commits in `main..HEAD` are the replayed branch commits: `git log --reverse main..HEAD --format=%s | head -3` prints the subjects of b49400b, b88cdb9 and 2f884f5 in that order. (Deliberately not an exact commit count: this plan's own documentation commit, and every later plan's fix commits, stack above these three. The old "exactly 3 commits" criterion was unsatisfiable — review finding 8.)
    - The path-restricted `git diff 2f884f5 HEAD` in the verify command exits 0 (pure replay)
    - `git verify-commit` exits 0 for each of the three SHAs in `git rev-list --reverse main..HEAD`; `git log -3 --format=%B` contains `Signed-off-by: redding1` once and `Signed-off-by: Avi Miller <me@dje.li>` twice
    - `git rev-parse origin/main` was recorded and `git merge-base --is-ancestor origin/main main` exited 0 during preflight
    - `uv run --frozen pytest` exits 0; `uv run pyright` exits 0; `uv run ruff check .` exits 0
    - `git diff main HEAD --stat -- tests/` shows only the branch's own test files (test_mdns, test_animation) — no test deleted or skip added
    - The scratch smoke run prints the AF_INET6 family assertion passing and a successful get_color() round trip against ::1, and its verbatim console output is pasted into 10-01-SUMMARY.md (the script is throwaway, so the summary narrative is the only durable evidence until plan 10-04 commits equivalent tests)
    - `git branch --list backup/ipv6-thread-pre-rebase --format=%(objectname)` still prints the pre-existing af17071 object (ref untouched, per D-20)
  </acceptance_criteria>
  <done>feat/ipv6-thread-support sits on the tip of main with the three replayed commits at the base of the stack, signatures and DCO intact, full local gate green, and an IPv6-only Light was controlled end-to-end against an emulator on ::1.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the rebased branch's patch-coverage debt and write the gap list</name>
  <files>.planning/phases/10-land-the-ipv6-thread-branch/10-COVERAGE-GAPS.md</files>
  <read_first>
    - pyproject.toml lines 107-133 (`[tool.pytest.ini_options] addopts`: `--cov=lifx`, `--cov=generate_theme_data`, `--cov-branch`, `--cov-report=xml` are already configured, so branch coverage needs no new flags; and `[tool.coverage.run] omit` plus `[tool.coverage.report] exclude_lines`, which govern what is legitimately unmeasured)
    - codecov.yml lines 6-42 (`coverage.status.patch.default.target: 100%`, and the top-level `flags:` block scoping all five Python flags to `src/lifx/` and `scripts/`)
    - Output of `git diff --stat main HEAD` on the rebased head (the branch diff whose lines the patch gate scores: `mdns/discovery.py` +-328, `mdns/dns.py` +38, `mdns/transport.py` +-64, plus small changes in `animator.py`, `base.py`, `connection.py`, `transport.py`, and the new `scripts/ipv6_thread_probe.py`)
    - Auto-memory `project_codecov_branch_patch` (the gate scores branch partials, not only uncovered lines, so a file at 100% line coverage can still fail)
  </read_first>
  <precondition>Task 1 is complete: the rebase has landed and `uv run --frozen pytest` passes on the rebased head.</precondition>
  <action>
    Measure now, do not fix now (review finding 3: the old plan set deferred this to plan 10-06, where discovering the size of the debt would collide with the PR and UAT sequencing and put real pressure on the "never weaken a check" prohibition).

    1. Run `uv run --frozen pytest` once to produce `coverage.xml` with branch data (addopts already carries `--cov-branch`; add no flags and change no configuration).
    2. Enumerate the files the patch gate will score: `git diff main HEAD --name-only -- 'src/lifx/*' 'scripts/*'`.
    3. For each of those files, extract from the coverage report every line in the branch diff that is uncovered, and every branch that is partially covered (one arm taken, the other not). Cross-reference the diff line ranges with `git diff main HEAD -U0 -- <file>` so only lines the patch gate scores are listed. A convenient per-file view is `uv run --frozen pytest --cov-report=term-missing` output, but the authoritative source is the XML or JSON report because it carries partial-branch data that term-missing summarises loosely.
    4. Write `.planning/phases/10-land-the-ipv6-thread-branch/10-COVERAGE-GAPS.md`: a heading per file, and under it one bullet per gap giving the line number or branch, a one-line description of the untested condition, and an **Owner** naming the plan that will close it. Assign owners by file: `src/lifx/network/mdns/discovery.py`, `src/lifx/network/mdns/dns.py` and `src/lifx/network/mdns/transport.py` are owned by plan 10-03; `src/lifx/devices/base.py`, `src/lifx/api.py`, `src/lifx/network/transport.py`, `src/lifx/network/connection.py` and `src/lifx/animation/animator.py` are owned by plan 10-02; `scripts/ipv6_thread_probe.py` is owned by plan 10-05, which also records the probe's coverage treatment. End the file with a **Totals** line giving the count of uncovered lines and partial branches per owning plan.
    5. If a file has no gaps, say so explicitly under its heading rather than omitting it — an absent heading is indistinguishable from a forgotten file when plan 10-06 verifies the list.
    6. Write no test and edit no source file in this task. Commit `10-COVERAGE-GAPS.md` together with `10-01-SUMMARY.md` in a single documentation commit (`git commit -s`) so the replayed three stay at the base of the stack.
  </action>
  <verify>
    <automated>test -s .planning/phases/10-land-the-ipv6-thread-branch/10-COVERAGE-GAPS.md && grep -q "Owner" .planning/phases/10-land-the-ipv6-thread-branch/10-COVERAGE-GAPS.md && grep -q "Totals" .planning/phases/10-land-the-ipv6-thread-branch/10-COVERAGE-GAPS.md && git diff --quiet main HEAD -- pyproject.toml codecov.yml</automated>
  </verify>
  <acceptance_criteria>
    - `10-COVERAGE-GAPS.md` exists and contains a heading for every path returned by `git diff main HEAD --name-only -- 'src/lifx/*' 'scripts/*'` (verify by comparing the two lists)
    - Every gap bullet names an owning plan (10-02, 10-03 or 10-05); the file ends with a per-plan Totals line
    - `git diff main HEAD --name-only -- pyproject.toml codecov.yml` is empty (measurement changed no configuration, so the gate is untouched)
    - `git diff main HEAD --name-only -- src/ tests/ scripts/` lists only the branch's own eleven replayed paths (this task added no test and no source line)
  </acceptance_criteria>
  <done>The size and shape of the branch's patch-coverage debt is written down and assigned before any wave-2 work starts, so plan 10-06 verifies a closed list instead of discovering an open one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN multicast -> mDNS parser | untrusted mDNS response packets enter `network/mdns/dns.py` / `discovery.py` (arrives with this rebase) |
| LAN unicast -> UDP transport | untrusted device responses enter `network/transport.py` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering/DoS | `_LifxRecordCache` record accumulation | medium | mitigate | Branch already bounds tables (`_MAX_ENTRIES = 1024`, ≤16 AAAA/host, ≤64 follow-up targets — audit finding A3). Verify the bounds survive the rebase (pure-replay diff covers this) |
| T-10-02 | Spoofing | mDNS TXT `id` accepted unvalidated (audit B6) | medium | accept | Explicitly out of scope per SPEC Boundaries — Phase 11 (MDNS-06) owns it; the mDNS path is not yet in any default discovery flow |
| T-10-03 | Information disclosure | probe script logs device addresses/serials | low | accept | Operator-run local diagnostic on a private ULA network |
| T-10-19 | Repudiation (false green under deadline pressure) | patch-coverage debt on the never-scrutinised mDNS rewrite discovered at merge time, when the cheapest route to green is a pragma or a skip | high | mitigate | Measure the debt in Wave 1 and write it down with owners (Task 2), so the work is scheduled into plans 10-02/10-03 rather than improvised against the merge. Directly strengthens T-10-11 and T-10-16 in plans 10-04 and 10-06, whose mitigations assume the gate is never weakened (review finding 3) |
| T-10-SC | Tampering | package installs | low | accept | No package is installed in this phase; `pyproject.toml` `dependencies = []` is unchanged (prohibition 3) |
</threat_model>

<verification>
- Rebased head is a pure replay (path-restricted diff empty) on top of main
- Signatures verified per commit with `git verify-commit`; DCO trailers intact on all three
- Full local gate (pytest, pyright, ruff) green with no weakening
- IPv6 end-to-end smoke passed against an emulator on ::1, output recorded in the summary
- `10-COVERAGE-GAPS.md` enumerates every branch-diff gap with an owning plan
</verification>

<success_criteria>
`feat/ipv6-thread-support` is rebased onto main, verifiably unchanged in content, fully green locally, demonstrably capable of controlling an IPv6-only device, and its patch-coverage debt is measured and assigned — the foundation every remaining plan stacks on.
</success_criteria>

<output>
Create `.planning/phases/10-land-the-ipv6-thread-branch/10-01-SUMMARY.md` when done, committed in the same commit as `10-COVERAGE-GAPS.md` on the feature branch; both merge to main with the PR in plan 10-06.
</output>
