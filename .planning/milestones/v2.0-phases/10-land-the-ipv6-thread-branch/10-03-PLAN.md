---
phase: 10-land-the-ipv6-thread-branch
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - src/lifx/network/transport.py
  - src/lifx/network/mdns/transport.py
  - tests/test_network/test_transport.py
  - tests/test_network/test_mdns/test_transport.py
  - tests/test_network/test_mdns/test_discovery.py
  - tests/test_network/test_mdns/test_dns.py
autonomous: true
requirements: [IPV6-01, IPV6-04]
tags: [ipv6, transport, mdns, error-handling]

estimate:
  tokens: 105000
  raw_tokens: 210000
  tasks: 4
  confidence: med

must_haves:
  truths:
    - "A failed `MdnsTransport.open()` leaves the object logically closed, not merely descriptor-clean: `_socket`, `_protocol` and `_transport` are all cleared, `is_open` is False, and a subsequent `open()` succeeds (SPEC AC 9 read at the level the branch's `is_open` actually reports; review finding 7)"
    - "Every gap that 10-COVERAGE-GAPS.md assigns to plan 10-03 (the mDNS rewrite: `mdns/discovery.py`, `mdns/dns.py`, `mdns/transport.py`) is closed by a test before the PR opens, with no pragma-no-cover added and no test skipped (review finding 3)"
    - "`UdpTransport.send()` raises `LifxNetworkError` in under 100 ms when the destination address family does not match the socket family (SPEC AC 10, audit B1)"
    - "`send()` after endpoint death raises the typed `LifxNetworkError` rather than dereferencing a `None` transport (SPEC R5 concurrency edge, AC 10)"
    - "`EHOSTUNREACH`, `EHOSTDOWN` and `ENETUNREACH` from a peer still route to `error_received` and still do not tear the endpoint down (SPEC AC 11, R5 error-handling edge — the swallow-all contract stays intact)"
    - "`MdnsTransport.open()` closes its socket when `bind()`, `setsockopt()` or `create_datagram_endpoint()` raises, with no `ResourceWarning` emitted (SPEC AC 9, IPV6-04)"
    - statement: "Concurrent MdnsTransport.open() calls, and close() racing a failing open(), leak no descriptor in either interleaving (the `if self._protocol is not None` early return is not atomic)"
      verification: backstop
    - "The module and class docstrings of network/mdns/transport.py describe the actual ephemeral-port RFC 6762 section 6.7 legacy-unicast behaviour and contain none of the three stale group-joining literals (SPEC AC 12, audit B4)"
  artifacts:
    - "src/lifx/network/transport.py — send-time family assertion in UdpTransport.send()"
    - "src/lifx/network/mdns/transport.py — leak-safe open() and honest docstrings"
    - "tests/test_network/test_mdns/test_transport.py — forced-OSError leak tests plus the R4 concurrency backstop test"
  key_links:
    - "UdpTransport.send() -> lifx.network.address.family_for (the assertion consumes plan 10-02's helper — the D-23 helper-before-fixes ordering)"
    - "_FATAL_SOCKET_ERRNOS / _UdpProtocol.error_received / _endpoint_lost — the peer-error vs endpoint-death contract the assertion must not disturb"
  prohibitions:
    - statement: "MUST NOT retune any WiFi-measured reliability constant (REQUEST_RETRANSMIT_GAPS, ACK_INFLIGHT_LIMIT, ACK_EXPIRY_SECONDS, DISCOVERY_REBROADCAST_GAPS) in this phase"
      status: resolved
      verification: judgment
    - statement: "MUST NOT reach the green gate by weakening the checks: no new pragma-no-cover on IPv6 code, no existing test deleted or skipped, no lowering of the Codecov patch target"
      status: resolved
      verification: judgment
---

<objective>
Land the remaining reconciled branch-audit fixes on the rebased branch (D-23 plan 3): the B1 send-time family assertion so a family mismatch fails loudly in milliseconds instead of being swallowed as a `gaierror` timeout, the IPV6-04 socket-leak fix in `MdnsTransport.open()`, and the B4 docstring correction in `network/mdns/transport.py`.

Purpose: an address-shaped error must never behave like a dead device (PITFALLS Pitfall 1/2), and a failed open must never strand a descriptor.
Output: three fix commits each carrying its own tests (D-19), plus the mDNS-rewrite coverage top-up commit.

<review_response>
Revised 2026-08-27 from `10-REVIEWS.md`. Changes landing in this plan:

- **Finding 7 (MEDIUM).** Task 2 previously said only "close the socket". The branch assigns `self._socket` and `self._protocol` *before* awaiting `create_datagram_endpoint()` (`mdns/transport.py:95-105`), and `is_open` returns `self._protocol is not None` (line 227). Closing the raw socket alone therefore leaves a phantom-open transport whose `open()` early-return refuses to rebuild it: descriptor-clean, permanently unusable. Task 2 now requires clearing `_socket`, `_protocol` and `_transport`, and asserts both `is_open is False` and a successful subsequent `open()`.
- **Finding 3 (HIGH, all three reviewers).** The mDNS rewrite (`discovery.py` +-328, `dns.py` +38) has never been scrutinised under the 100% branch-patch gate. Plan 10-01 Task 2 now measures the debt; new Task 4 closes the part of it that lives in mDNS, in Wave 3, before the PR opens. Plan 10-06 Task 1 then verifies rather than discovers.
- **Antigravity MEDIUM, folded into Task 4.** Its concern was that closing mDNS gaps on the fly during the merge plan risks fragile tests for the `_LifxRecordCache` and DNS accumulation logic. Task 4 pre-names the scenarios (partial and truncated packets, table bounds, follow-up target limits, address-selection classification) rather than leaving the executor to invent them under merge pressure.
</review_response>
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-land-the-ipv6-thread-branch/10-SPEC.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-CONTEXT.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-PATTERNS.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-COVERAGE-GAPS.md
@src/lifx/network/transport.py
@src/lifx/network/mdns/transport.py
</context>

## Artifacts this phase produces

New symbols created across Phase 10: `lifx.network.address` module with `validate_address()`, `family_for()`, `wildcard_for()` and the `Device.connect()` address guard (plan 10-02); `tests/test_network/test_address.py` (plan 10-02); the send-time family assertion in `UdpTransport.send()` and the leak-safe, state-clearing `MdnsTransport.open()` (this plan); conftest helpers and fixtures `get_free_port6()`, `_Ipv6EmulatedLifxServer`, `ipv6_available`, `emulator_server_ipv6`, `ipv6_light` (plan 10-04); `tests/test_api/test_ipv6_e2e.py` (plan 10-04); env var `LIFX_REQUIRE_IPV6` (plan 10-04); `tests/test_scripts/test_ipv6_thread_probe.py` and the probe helpers `_select_target()`, `_capture_device_state()`, `_restore_device_state()`, `_stage_result()`, `_build_uat_record()`, `_write_uat_record()` (plan 10-05); `scripts/ipv6_thread_probe.py` control/streaming stages and its `--serial`, `--uat-output`, `--stream` flags (plan 10-05); `10-COVERAGE-GAPS.md` (plan 10-01); `10-UAT-RESULTS.json` / `10-EXCEPTION-OVERRIDE.json` (plan 10-06); rebased branch symbols `_LifxRecordCache`, `_pick_address()`, `build_address_query()` (plan 10-01).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: B1 — send-time family assertion in UdpTransport.send()</name>
  <files>src/lifx/network/transport.py, tests/test_network/test_transport.py</files>
  <read_first>
    - src/lifx/network/transport.py — send() (main shape at lines 389-415: the "Socket not open" LifxNetworkError raise pattern to copy), _FATAL_SOCKET_ERRNOS (line 34), _UdpProtocol.error_received (lines 164-189), _endpoint_lost (line 341), and the module docstring's endpoint-death-vs-peer-error contract
    - tests/test_network/test_transport.py lines 474-560 — the endpoint-death test class whose parameterised errno shape ([EHOSTDOWN, EHOSTUNREACH, ENETUNREACH, ECONNREFUSED] at line 542) the new peer-error regression test copies
    - src/lifx/network/address.py — family_for() (plan 10-02's helper this assertion consumes)
    - CLAUDE.md "UDP endpoint death vs peer errors" gotcha (the contract that must survive)
  </read_first>
  <precondition>Current branch is feat/ipv6-thread-support and `src/lifx/network/address.py` exists (plan 10-02 landed).</precondition>
  <behavior>
    - An AF_INET transport sending to ("::1", 56700) raises LifxNetworkError in under 100 ms, message naming both the destination family and the socket family
    - An AF_INET6 transport sending to ("127.0.0.1", 56700) raises the same way (both directions of the mismatch)
    - A matching-family send behaves exactly as today (happy path unchanged; broadcast "255.255.255.255" on an AF_INET transport still sends)
    - Parameterised over EHOSTUNREACH, EHOSTDOWN, ENETUNREACH: a peer error surfacing via error_received does NOT raise from send(), does NOT tear the endpoint down, and is_open stays True (SPEC AC 11)
    - send() on a dead endpoint (after _endpoint_lost) raises LifxNetworkError "Socket not open", never AttributeError (R5 concurrency edge)
  </behavior>
  <action>
    Write the failing tests first, then add a pre-send family check to `UdpTransport.send()` (B1, per SPEC R5): compare `family_for(address[0])` against the open socket's family and raise `LifxNetworkError` (plain message naming both families, matching the "Socket not open" message style) when they differ. Obtain the socket family by storing it at open time (e.g. `self._family = family` in open()) rather than calling get_extra_info per send — cheaper and None-safe. Placement relative to the existing `is_open`/None check is discretionary (CONTEXT "Claude's Discretion") provided the dead-endpoint typed raise still fires first when the transport is gone.

    The check is a pre-send guard ONLY — do not touch `error_received`, `_FATAL_SOCKET_ERRNOS` or `_endpoint_lost` (the peer-error contract in the module docstring must survive verbatim; PATTERNS "Peer-error vs endpoint-death contract"). family_for() may raise ValueError for a malformed destination string; let that propagate as-is (destinations come from validated Device construction or discovery internals).

    Timing assertions in tests use time.perf_counter around pytest.raises with a 0.1 s bound.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_network/test_transport.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/lifx/network/transport.py send() contains a family comparison consuming family_for and raising LifxNetworkError; `grep -c "family_for" src/lifx/network/transport.py` prints at least 2 (open() from plan 10-02 and send())
    - Both mismatch-direction tests pass with the sub-100 ms assertion
    - The parameterised peer-error test passes for all three errnos with is_open asserted True afterwards
    - `git diff HEAD~1 -- src/lifx/network/transport.py` shows no change to error_received, _FATAL_SOCKET_ERRNOS or _endpoint_lost
    - Full suite exits 0
  </acceptance_criteria>
  <done>A family mismatch fails loudly and typed in milliseconds; genuine peer errors still swallow exactly as the transport contract documents.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IPV6-04 — MdnsTransport.open() leaves no socket behind, plus the R4 concurrency backstop</name>
  <files>src/lifx/network/mdns/transport.py, tests/test_network/test_mdns/test_transport.py</files>
  <read_first>
    - src/lifx/network/mdns/transport.py — open() (lines 48-125 on the rebased head: socket.socket at 71, SO_REUSEADDR at 72, bind at 80, IP_MULTICAST_TTL setsockopt at 91, `self._socket = sock` and `self._protocol = protocol` assigned at 95-105 BEFORE the await, create_datagram_endpoint at 103, the except-OSError-to-LifxNetworkError wrap at 116-125), the `if self._protocol is not None` early-return at the top of open(), the `is_open` property at line 227 (`return self._protocol is not None`), and close() (lines 200-222, which clears `_transport`, `_protocol` and `_socket` together — the same three-field reset the failure path must perform)
    - tests/test_network/test_mdns/test_transport.py (rebased branch version — the existing open/close test shapes)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-SPEC.md Requirement 4 and the R4 backstop row in Edge Coverage
    - The Finding 7 entry in this plan's `<review_response>` (why closing the descriptor alone is not enough)
  </read_first>
  <behavior>
    - Forcing OSError at sock.bind(): open() raises LifxNetworkError AND the created socket is closed
    - Forcing OSError at the setsockopt after bind: same
    - Forcing OSError at create_datagram_endpoint: same
    - After EACH of those three failures: `transport.is_open is False`, and `transport._socket`, `transport._protocol` and `transport._transport` are all None (review finding 7 — the create_datagram_endpoint case is the one that actually regresses today, because `_socket` and `_protocol` are already assigned by then)
    - After EACH of those three failures, removing the forced error and calling `await transport.open()` again succeeds and yields `is_open is True` — the object is reusable, not merely descriptor-clean
    - In all three cases no ResourceWarning is emitted (warnings.catch_warnings with simplefilter("error", ResourceWarning) plus gc.collect() after the failure)
    - Backstop: two concurrent open() calls on one MdnsTransport (asyncio.gather) leave exactly one live endpoint and zero stranded descriptors, in either completion order; close() racing a failing open() also leaks nothing
  </behavior>
  <action>
    Write the failing leak and state tests first (monkeypatch socket.socket to return an instrumented socket whose close() is recorded, forcing OSError at each named step in turn), then fix open() so that any OSError after socket creation both closes the socket and resets the object's logical state before the existing LifxNetworkError re-raise.

    The state reset is the part the review found missing (finding 7): set `self._socket`, `self._protocol` and `self._transport` all back to None on the failure path, mirroring what close() already does. Without it, a create_datagram_endpoint failure leaves `_protocol` set, `is_open` reporting True, and open()'s early-return refusing to ever rebuild the transport — descriptor-clean but permanently dead. Mechanism is discretionary per CONTEXT (try/except with explicit cleanup, or contextlib.ExitStack popped on success); keep the existing except-OSError block with its debug-dict log and `raise LifxNetworkError(f"Failed to open mDNS socket: {e}") from e` as the outer shape. Note create_datagram_endpoint(sock=sock) takes ownership only on success — on failure asyncio may or may not have closed it, so the fix must make close idempotent-safe (calling close() on an already-closed socket is a no-op).

    Then write the R4 backstop test (SPEC edge table carries it into must_haves): concurrent open() calls, and close() racing a failing open(). Use asyncio.Event around the mocked endpoint await so both interleavings are deterministic rather than timing-dependent. If the test exposes a real double-endpoint or leaked-descriptor interleaving, fix it minimally (e.g. an opening-in-progress guard mirroring DeviceConnection._is_opening, or a post-await re-check that closes the loser) — do not restructure the transport. The backstop test must count live descriptors (instrumented sockets created vs closed), not merely assert no exception.

    Coverage top-up for `mdns/transport.py` belongs in this commit: close every gap `10-COVERAGE-GAPS.md` assigns to plan 10-03 for that file. The `discovery.py` and `dns.py` gaps are Task 4's.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_network/test_mdns/test_transport.py -q -W error::ResourceWarning</automated>
  </verify>
  <acceptance_criteria>
    - Three forced-failure tests (bind, setsockopt, create_datagram_endpoint) each assert the instrumented socket's close() was called and pass under -W error::ResourceWarning
    - Each of those three tests also asserts `transport.is_open is False` and that `_socket`, `_protocol` and `_transport` are all None afterwards
    - Each of those three tests also asserts a subsequent unforced `await transport.open()` succeeds and leaves `is_open is True`
    - The concurrency backstop test exists, exercises both interleavings deterministically via asyncio.Event, and asserts created-socket count equals closed-plus-live-endpoint count
    - `uv run --frozen pytest` exits 0
    - No new pragma-no-cover in src/lifx/network/mdns/transport.py (`git diff HEAD~1 -- src/lifx/network/mdns/transport.py | grep -c "pragma: no cover"` prints 0)
  </acceptance_criteria>
  <done>A partway-failed open() strands no descriptor and leaves no phantom-open state in any single-call or concurrent interleaving, and the transport can still be opened afterwards — proven by instrumented-socket tests.</done>
</task>

<task type="auto">
  <name>Task 3: B4 — honest network/mdns/transport.py docstrings</name>
  <files>src/lifx/network/mdns/transport.py</files>
  <read_first>
    - src/lifx/network/mdns/transport.py — the module docstring (lines 1-5) and the MdnsTransport class docstring (lines 22-31), both still describing behaviour that IP-group joining removal made false, plus the RFC 6762 rationale comment already inside open() (near the bind at line 80)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-SPEC.md Requirement 6 (target text and the three banned literals in its acceptance line)
  </read_first>
  <action>
    Rewrite both docstrings to describe the actual behaviour (B4): a UDP transport that binds an ephemeral port so its queries are RFC 6762 section 6.7 legacy-unicast queries, with responders replying directly to that port — reflect the rationale already written in open()'s inline comment into the class docstring. State that queries are sent TO the well-known mDNS IPv4 address (MDNS_ADDRESS/224.0.0.251) while replies arrive unicast. Remove every claim about joining the mDNS group; the three exact literals that must be absent are listed in SPEC Requirement 6's acceptance line and in this task's verify command — do not restate them in the new text. Wording is discretionary per CONTEXT, subject to that negative check. Keep the usage Example block, updated only if it repeats a stale claim. Australian English spelling; recast rather than inserting em dashes.
  </action>
  <verify>
    <automated>uv run python -c "import lifx.network.mdns.transport as m; d=(m.__doc__ or '')+(m.MdnsTransport.__doc__ or ''); banned=['multicast group','membership','IP_ADD_MEMBERSHIP']; hits=[b for b in banned if b in d]; assert not hits, hits; assert '6.7' in d and 'ephemeral' in d.lower()" && uv run --frozen pytest tests/test_network/test_mdns/ -q</automated>
  </verify>
  <acceptance_criteria>
    - The python -c docstring check in verify exits 0 (none of the three banned literals present in either docstring; RFC 6762 section 6.7 and the ephemeral-port rationale present)
    - `uv run ruff check .` and `uv run pyright` exit 0
    - No functional line of the module changed in this commit (`git diff HEAD~1 -- src/lifx/network/mdns/transport.py` touches only docstring/comment lines)
  </acceptance_criteria>
  <done>The mDNS transport's documented behaviour matches its actual behaviour; the stale claims are gone (SPEC AC 12).</done>
</task>

<task type="auto">
  <name>Task 4: Close the mDNS rewrite's branch-coverage debt before the PR opens</name>
  <files>tests/test_network/test_mdns/test_discovery.py, tests/test_network/test_mdns/test_dns.py</files>
  <read_first>
    - .planning/phases/10-land-the-ipv6-thread-branch/10-COVERAGE-GAPS.md — the sections for `src/lifx/network/mdns/discovery.py` and `src/lifx/network/mdns/dns.py`, which name the exact uncovered lines and partial branches this task must close
    - src/lifx/network/mdns/discovery.py on the rebased head — `_LifxRecordCache` (its bounded tables: `_MAX_ENTRIES = 1024`, the per-host AAAA cap, the follow-up target cap), `_pick_address()` and its GUA/ULA/link-local classification, the PTR retransmit loop and the follow-up A/AAAA query path
    - src/lifx/network/mdns/dns.py on the rebased head — the +38 lines the branch added, including `build_address_query()` and `_encode_name()`
    - tests/test_network/test_mdns/test_discovery.py and test_dns.py (current shapes) plus tests/test_network/test_mdns/test_dns_adversarial.py (the malformed-packet test style this repo already uses — extend it rather than inventing a second convention)
    - tests/test_network/test_mdns/conftest.py (existing mDNS fixtures)
  </read_first>
  <precondition>`10-COVERAGE-GAPS.md` exists on the branch with sections assigning mDNS gaps to plan 10-03 (written by plan 10-01 Task 2).</precondition>
  <behavior>
    Scenarios are pre-named here so they are not improvised under merge pressure (Antigravity's MEDIUM concern). Cover at minimum, and only where 10-COVERAGE-GAPS.md actually lists a gap:
    - `_LifxRecordCache` accumulation across multiple packets for one host, including a record arriving before its SRV and after it
    - Truncated and malformed responses: a packet cut mid-record, a name with a bad length prefix, an answer count that overstates the records present
    - Bounds: the `_MAX_ENTRIES` table cap reached, the per-host AAAA cap reached, and the follow-up-target cap reached — each asserted from both sides (one entry below the cap accepted, the entry at the cap rejected or evicted)
    - `_pick_address()` over each classification it distinguishes, including the case where no routable address exists
    - `build_address_query()` / `_encode_name()` round trip against the parser, including a name at the label-length boundary
  </behavior>
  <action>
    Close the mDNS half of the branch's patch-coverage debt now, in Wave 3, so plan 10-06 Task 1 verifies a closed list instead of discovering an open one (review finding 3, raised by all three reviewers).

    Work strictly from `10-COVERAGE-GAPS.md`: for each gap it assigns to plan 10-03 under `discovery.py` and `dns.py`, add a test that exercises the missing line or the untaken branch arm. Extend the existing test modules and their fixtures; add no new test module and no new conftest.

    Change no source file in this task. If closing a gap appears to require a source change, that is a finding, not a licence — stop and report it, because this plan already carries its behaviour changes in Tasks 1 to 3 and a fourth would land untested behaviour in a coverage commit.

    If a listed gap is genuinely unreachable (for example a defensive branch that only a platform the suite cannot reach would take), record it in `10-03-SUMMARY.md` with the reason and leave the line uncovered. Do not add a pragma marker, do not skip a test, do not touch `pyproject.toml` or `codecov.yml`: prohibition 3 names all three as the weakening this gate invites.

    Commit as a single test-only commit (`git commit -s`).
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_network/test_mdns/ -q && uv run --frozen pytest tests/test_network/ --cov=lifx.network.mdns --cov-branch --cov-report=term-missing -q</automated>
  </verify>
  <acceptance_criteria>
    - Every gap listed under `src/lifx/network/mdns/discovery.py` and `src/lifx/network/mdns/dns.py` in 10-COVERAGE-GAPS.md is either covered by a test added in this commit or annotated in 10-03-SUMMARY.md with an unreachability reason
    - The scoped coverage run in verify reports no missing line and no partial branch inside the branch-diff ranges for those two files
    - `git diff HEAD~1 --name-only` lists only files under `tests/` (no source change)
    - `git diff HEAD~1 | grep -c "pragma: no cover"` prints 0, and `git diff main HEAD --name-only -- pyproject.toml codecov.yml` is empty
    - `uv run --frozen pytest` exits 0
  </acceptance_criteria>
  <done>The mDNS rewrite's share of the branch-patch-coverage debt is closed by real tests in Wave 3, with any genuinely unreachable line recorded rather than papered over.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN peers -> UdpTransport.error_received | untrusted network errors from any peer reach the swallow-all handler |
| local process -> mDNS socket lifecycle | descriptor exhaustion is a local-availability concern |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07 | DoS | family-mismatch send swallowed as gaierror -> full retry-schedule stall | medium | mitigate | B1 pre-send family assertion raises LifxNetworkError in <100 ms (Task 1) |
| T-10-08 | DoS | descriptor leak on partway-failed MdnsTransport.open(), amplified by retry loops | medium | mitigate | close-on-failure fix plus instrumented-socket tests including concurrent interleavings (Task 2) |
| T-10-20 | DoS (self-inflicted) | a failed open() leaving `_protocol` set, so `is_open` lies and open()'s early-return refuses to rebuild the transport forever | medium | mitigate | The failure path clears `_socket`, `_protocol` and `_transport` together, mirroring close(); tests assert `is_open is False` and a successful reopen after each forced failure (Task 2, review finding 7) |
| T-10-09 | DoS | over-eager assertion converting peer-unreachable storms into raises, tearing down healthy request flows | high | mitigate | AC 11 regression test parameterised over EHOSTUNREACH/EHOSTDOWN/ENETUNREACH pins the error_received contract; task explicitly forbids touching error_received/_FATAL_SOCKET_ERRNOS (Task 1) |
| T-10-SC | Tampering | package installs | low | accept | No package installed; dependencies = [] unchanged |
</threat_model>

<verification>
- SPEC AC 9, 10, 11, 12 all pass
- A failed open() leaves the transport logically closed and reopenable, not just descriptor-clean
- R4 backstop test present and green under -W error::ResourceWarning
- Every 10-COVERAGE-GAPS.md gap owned by plan 10-03 is closed by a test, or annotated unreachable with a reason; no pragma marker added
- Full suite, pyright, ruff green
</verification>

<success_criteria>
Family mismatches and failed opens fail fast, typed, leak-free and reopenable, while the transport's deliberate peer-error swallowing survives untouched; the mDNS transport's docs tell the truth; and the rewrite's coverage debt is paid before the PR opens rather than against the merge deadline.
</success_criteria>

<output>
Create `.planning/phases/10-land-the-ipv6-thread-branch/10-03-SUMMARY.md` when done (commit on the feature branch).
</output>
