---
phase: 11-mdns-hardening
plan: 12
type: execute
wave: 10
depends_on:
  - 11-10
files_modified:
  - scripts/ipv6_thread_probe.py
  - tests/test_scripts/test_ipv6_thread_probe.py
autonomous: true
gap_closure: true
requirements:
  - MDNS-04
  - MDNS-07
estimate:
  tokens: 16000
  raw_tokens: 32000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "The diagnostic probe retains positive-TTL records for the sweep, honours only D-11's one-second TTL=0 goodbye grace/rescue, and clamps every receive wait to the minimum remaining overall, idle, goodbye-expiry, and next PTR retransmission deadlines (D-08, D-11, D-13, D-14)."
    - "The probe keeps separate case-folded attempt and successful-send ledgers: failed A/AAAA sends are isolated and retried no more than twice, successful sends are never repeated, and no more than 64 distinct SRV targets are attempted (SPEC R4; REVIEW WR-02)."
    - "Probe parity is proven with deterministic fake-clock and fake-transport tests; no live hardware, multicast listener, broadcast change, dependency, or private identifier is involved."
    - "Probe sockets and cache state are released on success, timeout, cancellation, and exception (D-13, D-14)."
  artifacts:
    - path: scripts/ipv6_thread_probe.py
      provides: "Production-parity cache expiry, deadline clamping, and bounded follow-up ledger."
    - path: tests/test_scripts/test_ipv6_thread_probe.py
      provides: "Synthetic goodbye/rescue, four-deadline clamp, simultaneous-event, follow-up ledger, and cleanup tests."
  key_links:
    - from: scripts/ipv6_thread_probe.py::sweep
      to: src/lifx/network/mdns/discovery.py::_LifxRecordCache
      via: "Probe loop calls `expire()`, `next_expiry_delay()`, `pending_targets()`, and `resolve()` in the same clock-cause order as `_discover_lifx_services_sweep`."
    - from: scripts/ipv6_thread_probe.py::sweep
      to: src/lifx/network/mdns/transport.py::MdnsTransport.send
      via: "Separate case-folded `query_attempts` and successful-send ledgers bound and isolate A/AAAA follow-up sends."
---

<objective>
Close the review's probe-parity warnings so the diagnostic surface exercises production mDNS lifetime and follow-up limits rather than presenting a weaker algorithm.

Purpose: Keep validation honest and bounded without reopening the broadcast schedule or requiring hardware.
Output: Production-parity goodbye/retransmission deadline logic, separate bounded follow-up ledgers, and deterministic fake-clock/fake-transport tests.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-mdns-hardening/11-CONTEXT.md
@.planning/phases/11-mdns-hardening/11-RESEARCH.md
@.planning/phases/11-mdns-hardening/11-PATTERNS.md
@.planning/phases/11-mdns-hardening/11-SPEC.md
@.planning/phases/11-mdns-hardening/11-VERIFICATION.md
@.planning/phases/11-mdns-hardening/11-REVIEW.md
@.planning/phases/11-mdns-hardening/11-10-SUMMARY.md
@.agents/skills/spike-findings-lifx-async/references/discovery.md

The spike's 0s/1s/2s/4s schedule applies to broadcast discovery only. This plan does not alter Phase 10 or add that schedule to mDNS.
</context>

## Artifacts this phase produces

- `scripts/ipv6_thread_probe.py` — bounded diagnostic mDNS loop matching production goodbye expiry, PTR retransmission, and follow-up semantics.
- `tests/test_scripts/test_ipv6_thread_probe.py` — fake-clock and fake-transport proof of all four wake-up sources and both follow-up ledgers, using synthetic identifiers and documentation addresses.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Apply production goodbye, retransmission, and receive-deadline semantics in the probe</name>
  <files>scripts/ipv6_thread_probe.py, tests/test_scripts/test_ipv6_thread_probe.py</files>
  <read_first>
    - `scripts/ipv6_thread_probe.py`: `sweep`, its initial PTR send, one- and three-second `retransmit_at` schedule, fixed receive timeout, final `resolve()`, and cleanup path.
    - `src/lifx/network/mdns/discovery.py`: `_discover_lifx_services_sweep` loop ordering, `cache.expire()`, `cache.next_expiry_delay()`, one- and three-second `retransmit_delays`, `IdleDeadline`, `MdnsTransport.send`, and receive-timeout clamping.
    - `tests/test_scripts/test_ipv6_thread_probe.py`: current mDNS cache-reporting and fake transport helpers.
    - `.planning/phases/11-mdns-hardening/11-SPEC.md`: TTL=0, expiry during wait, quiet-window, cancellation, and cleanup edge rows.
  </read_first>
  <behavior>
    - Test `test_probe_goodbye_removes_record_before_report`: a TTL=0 goodbye after a valid chain prevents that device from appearing in probe output only after the one-second grace elapses.
    - Test `test_probe_goodbye_expires_during_receive_wait`: advancing the fake clock to the one-second TTL=0 goodbye deadline removes the record before final resolution; a positive-TTL record without a goodbye remains retained.
    - Test `test_probe_goodbye_rescue_keeps_record_visible`: a matching positive reannouncement before the one-second deadline clears goodbye expiry and keeps the record visible in probe output at report time.
    - Test `test_probe_receive_timeout_is_minimum_of_all_four_deadlines`: each observed receive timeout equals the non-negative minimum remaining overall, idle, next goodbye-expiry, and next PTR retransmission interval.
    - Test `test_probe_ptr_retransmits_at_one_and_three_seconds`: after the initial PTR send, fake-clock wake-ups cause exactly the bounded one- and three-second PTR retransmissions and no fourth PTR send.
    - Test `test_probe_simultaneous_goodbye_expiry_and_ptr_retransmission_are_deterministic`: when goodbye expiry and a PTR retransmission become due together, `expire(now)` runs first, the due retransmission is sent once, and neither event is skipped or duplicated.
    - Test `test_probe_valid_packet_resets_idle_deadline_after_consumer_work`: valid LIFX activity uses production's network-silence semantics.
    - Test `test_probe_closes_transport_on_timeout_cancellation_and_exception`: all terminal paths release the transport and per-call cache.
  </behavior>
  <action>
    Port `_discover_lifx_services_sweep` clock handling into `sweep` while preserving the probe's initial PTR transmission and bounded retransmissions at one and three seconds. At the top of each loop call `cache.expire(now)` before resolution or pending-target decisions, then process a due PTR retransmission. Derive the next `transport.receive()` timeout from the minimum non-negative remaining overall deadline, idle deadline, `cache.next_expiry_delay(now)` for D-11 goodbye grace only, and time until the next PTR retransmission; a zero interval must loop through the due clock causes without inventing a positive wait. When expiry and retransmission are due simultaneously, process goodbye expiry first and the retransmission second, matching production. Positive-TTL records remain retained for the sweep unless a TTL=0 goodbye schedules removal, and a positive reannouncement during the one-second grace clears the pending expiry so the identity remains available to final probe resolution. Reset the idle deadline only from the exact LIFX activity signal produced by Plan 11-10, after packet handling resumes, so the quiet window measures network silence. Resolve only after due goodbye removal. Preserve cancellation, `finally` cleanup, JSON-safe reporting, and the probe's diagnostic-only status. Do not join multicast, add a background listener, invoke broadcast discovery, or use hardware. Commit with a Conventional Commit message using `git commit -S -s`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-11-12-uv-cache uv run --frozen pytest tests/test_scripts/test_ipv6_thread_probe.py -q -W error::ResourceWarning -k 'goodbye or four_deadlines or ptr_retransmits or simultaneous or resets_idle or closes_transport'</automated>
  </verify>
  <acceptance_criteria>
    - Positive-TTL records remain retained; TTL=0 goodbye removal changes probe output only after grace, while a timely positive reannouncement keeps the record visible, per D-11.
    - Receive waits are clamped to the minimum of overall, idle, next goodbye-expiry, and next PTR retransmission deadlines.
    - The initial PTR send plus one- and three-second retransmissions are bounded and simultaneous clock causes execute in deterministic production order.
    - Valid-packet idle reset excludes consumer work and no zero/negative busy loop is introduced.
    - Success, timeout, cancellation, and exception close the transport and discard per-call state.
  </acceptance_criteria>
  <done>The probe uses production-equivalent record lifetime and deadline semantics with deterministic cleanup proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the production 64-target and two-attempt follow-up ledger</name>
  <files>scripts/ipv6_thread_probe.py, tests/test_scripts/test_ipv6_thread_probe.py</files>
  <read_first>
    - `scripts/ipv6_thread_probe.py`: `sweep`, current `queried_targets` handling, `cache.pending_targets()`, and `transport.send(build_address_query(target))` loop.
    - `src/lifx/network/mdns/discovery.py`: `_discover_lifx_services_sweep`, live `query_attempts` and `queried_targets` ledgers, literal 64-target/two-attempt guards, case-folded cache target identities, `MdnsTransport.send`, and `LifxNetworkError` isolation.
    - `tests/test_network/test_mdns/test_discovery.py`: `test_transient_query_failure_retries_once_then_stops`, `test_persistent_query_failure_stops_after_two_attempts`, `test_failed_targets_still_count_towards_sixty_four_cap`, `test_follow_up_queries_stop_at_sixty_four_targets`, and the successful-send dedup test.
    - `.planning/phases/11-mdns-hardening/11-SPEC.md`: requirement 4 successful-send and bounded failure semantics; `.planning/phases/11-mdns-hardening/11-REVIEW.md`: WR-02 probe parity warning.
  </read_first>
  <behavior>
    - Test `test_probe_successful_follow_up_is_sent_once`: repeated pending reports after a successful A/AAAA send never send that case-folded target again.
    - Test `test_probe_failed_follow_up_retries_exactly_twice`: repeated pending reports after `LifxNetworkError` cause two total send attempts for that case-folded target, never a third.
    - Test `test_probe_follow_up_failure_isolated_from_other_targets`: one target's `LifxNetworkError` neither aborts the sweep nor prevents a later target from being sent successfully and recorded in the success ledger.
    - Test `test_probe_follow_up_tracks_target_identity_case_insensitively`: case variants consume one target slot, one attempt count, and one successful-send identity.
    - Test `test_probe_follow_up_sends_64th_target_and_rejects_65th`: the 64th distinct case-folded target is attempted while the 65th and later targets receive no follow-up query, including when earlier sends fail.
    - Test `test_probe_follow_up_uses_srv_target_not_packet_source`: query names come only from cache `pending_targets()`.
    - Test `test_probe_follow_up_completion_stops_retries`: once A/AAAA data resolves a target, no extra attempt is sent.
  </behavior>
  <action>
    Replace the probe's one-shot unbounded set with the same two-ledger semantics already used by `_discover_lifx_services_sweep`: a case-folded `query_attempts` dictionary admits at most 64 distinct targets and increments before each send attempt, while a separate case-folded successful-send set is updated only after `MdnsTransport.send` returns successfully. Iterate `cache.pending_targets()` in deterministic order, skip every target already in the successful set, and permit a failed send to retry until its attempt count reaches the literal production bound of two. Catch `LifxNetworkError` around each address-query send, isolate it to that target, and continue processing other targets and packets; never repeat a successful send. Names must originate only from SRV targets exposed by `pending_targets()`, never packet source metadata. Use the live production literal behaviour and `query_attempts` naming rather than inventing private limit symbols; if maintainers choose to centralise private constants first, do so in an ordered prerequisite within this task and keep them private under D-16. Do not change production behaviour in this plan. Commit with a Conventional Commit message using `git commit -S -s`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-11-12-uv-cache uv run --frozen pytest tests/test_scripts/test_ipv6_thread_probe.py -q -k 'follow_up'</automated>
  </verify>
  <acceptance_criteria>
    - The probe admits at most 64 case-insensitive distinct follow-up targets.
    - A failed target receives at most two A/AAAA send attempts; a successful target receives exactly one and is never repeated.
    - `LifxNetworkError` for one target is isolated, later targets remain eligible, the 64th target is attempted, and the 65th is rejected.
    - Follow-up query names originate only from SRV targets exposed by `pending_targets()`.
    - Tests prove the live literal production bounds or private centralised constants without changing D-16's public/private boundary.
  </acceptance_criteria>
  <done>The diagnostic probe cannot exceed production's 64-target/two-attempt follow-up work budget.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted mDNS response -> diagnostic loop | Records influence waits, retained lifetime, and follow-up work. |
| Cache pending targets -> UDP queries | Attacker-controlled SRV targets can amplify outbound diagnostic traffic. |
| Probe result -> operator output | Diagnostic state must not expose live identifiers in committed evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-12-01 | Denial of service | probe receive loop | high | mitigate | Clamp every wait to overall, idle, goodbye-expiry, and PTR retransmission deadlines; test zero-time and simultaneous-event re-evaluation. |
| T-11-12-02 | Denial of service | probe follow-up ledgers | high | mitigate | Separate attempted from successful case-folded targets, cap admissions at 64, retry failed sends no more than twice, and never repeat success. |
| T-11-12-03 | Tampering | stale/goodbye cache state | medium | mitigate | Retain positive-TTL state, expire only elapsed TTL=0 goodbye grace before resolve/report, and test rescue. |
| T-11-12-04 | Information disclosure | probe evidence | high | mitigate | Use only synthetic test data; do not execute hardware discovery or commit raw probe output. |
| T-11-12-SC | Tampering | package supply chain | low | accept | No package installation or dependency change occurs in this plan. |
</threat_model>

<verification>
- Run `UV_CACHE_DIR=/private/tmp/lifx-async-11-12-uv-cache uv run --frozen pytest tests/test_scripts/test_ipv6_thread_probe.py -q -W error::ResourceWarning`.
- Run `UV_CACHE_DIR=/private/tmp/lifx-async-11-12-uv-cache uv run --frozen ruff check scripts/ipv6_thread_probe.py tests/test_scripts/test_ipv6_thread_probe.py`.
- Run `UV_CACHE_DIR=/private/tmp/lifx-async-11-12-uv-cache uv run --frozen pyright scripts/ipv6_thread_probe.py`.
- Confirm `git verify-commit HEAD` succeeds after each task and a `Signed-off-by:` trailer exists without copying identities into evidence.
</verification>

<success_criteria>
- Review warnings for probe goodbye/retransmission clamping and exact follow-up send semantics are closed with deterministic tests.
- No high threat remains unresolved and every resource terminal path closes cleanly.
- The Phase 10 broadcast schedule and live hardware remain out of scope.
</success_criteria>

## Source coverage audit

| Source item | Coverage |
|-------------|----------|
| GOAL: validated like broadcast while remaining honest | COVERED by production-parity deterministic probe behaviour. |
| REQ MDNS-04 | COVERED by bounded A/AAAA follow-up query behaviour. |
| REQ MDNS-07 | COVERED by diagnostic sweep parity and cleanup tests. |
| REVIEW warnings: goodbye/retransmission clamp and WR-02 64/two ledgers | COVERED by both tasks. |
| CONTEXT D-08/D-09/D-11/D-13/D-14/D-16 | COVERED without public API changes. |
| SPEC resolved edges: goodbye/rescue, expiry during wait, PTR retransmission, simultaneous clock causes, quiet-window, interruption, cleanup, send failure, success deduplication, and target cap | COVERED by named tests. |
| Discovery spike broadcast schedule | EXCLUDED as an explicitly separate Phase 10 path. |
| Hardware validation and live identifiers | EXCLUDED; deterministic mDNS gaps require synthetic evidence only. |

<output>
Create `.planning/phases/11-mdns-hardening/11-12-SUMMARY.md` when done.
</output>
