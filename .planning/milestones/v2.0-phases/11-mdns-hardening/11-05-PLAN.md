---
phase: 11-mdns-hardening
plan: 05
type: execute
wave: 4
depends_on: [11-03]
files_modified:
  - src/lifx/network/mdns/types.py
  - src/lifx/network/mdns/discovery.py
  - src/lifx/network/mdns/__init__.py
  - src/lifx/__init__.py
  - scripts/ipv6_thread_probe.py
  - tests/test_network/test_mdns/test_discovery.py
  - tests/test_api/test_api_discovery.py
  - tests/test_scripts/test_ipv6_thread_probe.py
autonomous: true
requirements: [MDNS-02, MDNS-08]
tags: [mdns, api-cutover, exports, privacy, final-gate]

estimate:
  tokens: 22000
  raw_tokens: 44000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "The service-record class and low-level service generator have leading-underscore names in their defining modules, no compatibility aliases, and no entries in either package `__all__` (D-03)."
    - "The existing `create_device_from_record` name, signature, and mDNS-package export remain intact because D-03 and SPEC AC5 authorise removal of only the record, generator, and any transport enum; the review suggestion to remove a third public symbol is explicitly rejected as an unapproved scope expansion."
    - "`discover_mdns()` and `discover_devices_mdns()` remain supported and return fully typed devices with correct connectivity after the private cutover."
    - "Repository-internal probe and tests import the private names directly from defining modules; script convenience does not recreate a public alias."
    - "The focused mDNS, API, and probe suites plus the full frozen pytest suite pass after the rename, before Plan 11-06 performs the complete phase quality/privacy/coverage gate."
  artifacts:
    - path: "src/lifx/network/mdns/types.py"
      provides: "Final private `_LifxServiceRecord` class with `addresses` and `connectivity`"
      contains: "class _LifxServiceRecord"
    - path: "src/lifx/network/mdns/discovery.py"
      provides: "Final private `_discover_lifx_services()` consumed by supported high-level discovery"
      contains: "async def _discover_lifx_services"
    - path: "src/lifx/network/mdns/__init__.py"
      provides: "Public mDNS export list without low-level record/generator names"
    - path: "src/lifx/__init__.py"
      provides: "Top-level export list without low-level record/generator names"
    - path: "scripts/ipv6_thread_probe.py"
      provides: "Intentional internal private-name consumer without running hardware validation"
    - path: "tests/test_network/test_mdns/test_discovery.py"
      provides: "Runtime export assertions proving the two locked removals and the preserved factory contract"
  key_links:
    - from: "src/lifx/network/mdns/discovery.py::discover_devices_mdns"
      to: "src/lifx/network/mdns/discovery.py::_discover_lifx_services"
      via: "private internal generator call"
      pattern: "_discover_lifx_services"
    - from: "src/lifx/api.py::discover_mdns"
      to: "src/lifx/network/mdns/discovery.py::discover_devices_mdns"
      via: "unchanged supported high-level API"
      pattern: "discover_devices_mdns"
  prohibitions:
    - statement: "MUST NOT bind the mDNS query socket to port 5353 or join the multicast group, including as a fallback for unsolicited reception."
      status: resolved
      verification: "transport source and real-socket regression"
    - statement: "MUST NOT expose `LifxServiceRecord`, `discover_lifx_services()`, `TransportMethod`, or the raw connectivity field as public API, documentation, or examples."
      status: resolved
      verification: "runtime export assertions and active-surface audit"
    - statement: "MUST NOT assert an expansion of the private connectivity abbreviation; only the exact sentinel may be described as Thread and every other case is WiFi."
      status: resolved
      verification: "documentation and active-surface review"
    - statement: "MUST NOT apply cache-flush replacement semantics to legacy-unicast responses."
      status: resolved
      verification: "focused RFC cache regressions"
    - statement: "MUST NOT allow connectivity metadata to influence address selection, routing, connection behaviour, retries, or tuning during Phase 11."
      status: resolved
      verification: "scope audit and behavioural regression suite"
    - statement: "MUST NOT place live serials, IP addresses, hostnames, or raw discovery output in Phase 11 tests, documentation, or evidence."
      status: resolved
      verification: "synthetic fixture review, per-commit staged checks, and immutable-base complete-phase privacy audit"
---

<objective>
Complete the deliberate v2.0 API cutover by internalising the raw service record and generator everywhere while preserving the separately exported record-to-device factory, then migrate repository-owned consumers atomically.

Purpose: implement exactly the breaking change the user authorised, without turning reviewer preference into an additional public API removal.
Output: final private record/generator symbols, cleaned exports, preserved factory, and migrated internal consumers/tests ready for Plan 11-06's phase gate.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mdns-hardening/11-SPEC.md
@.planning/phases/11-mdns-hardening/11-CONTEXT.md
@.planning/phases/11-mdns-hardening/11-RESEARCH.md
@.planning/phases/11-mdns-hardening/11-PATTERNS.md
@.planning/phases/11-mdns-hardening/11-SOURCE-AUDIT.md
@.planning/phases/11-mdns-hardening/11-PHASE-BASE.txt
@.planning/phases/11-mdns-hardening/11-03-SUMMARY.md
@src/lifx/network/mdns/discovery.py
@src/lifx/network/mdns/types.py
@src/lifx/network/mdns/__init__.py
@src/lifx/__init__.py
@scripts/ipv6_thread_probe.py
</context>

## Artifacts this phase produces

Phase 11 produces `Device.connectivity`, `Device._connectivity`,
`Device._set_connectivity()`, `_LifxServiceRecord` with trailing defaulted
`connectivity` and `addresses` fields, `_CachedResourceRecord.expires_at`,
`_validate_txt_id()`, `_connectivity_from_txt()`,
`_LifxRecordCache._MAX_TXT_RRS_PER_OWNER`,
`_LifxRecordCache._MAX_SRV_RRS_PER_OWNER`,
`_LifxRecordCache._pending_expiries`, `_LifxRecordCache.next_expiry_delay()`,
`_LifxRecordCache.expire()`, and `_discover_lifx_services()`, while preserving the
existing public `create_device_from_record()` name and export because D-03 authorises only
the record/generator removal. It also produces the stable
diagnostic reasons `missing_txt_id`, `invalid_txt_id`, `conflicting_txt_id`,
`missing_product_id`, `invalid_product_id`, `invalid_address`, `malformed_packet`,
`rr_identity_limit`, and `unexpected_cache_flush`, plus the phase-local paths `COVERAGE.md` and
`11-SOURCE-AUDIT.md`. It adds no enum, CLI flag, persistence field, schema, dependency, or
external service integration.

<tasks>

<task type="auto">
  <name>Task 1: Internalise only the locked record and generator symbols</name>
  <reversibility rating="one-way">D-03 breaks the documented v1.x low-level import contract with no alias; the user already made this release decision.</reversibility>
  <files>src/lifx/network/mdns/types.py, src/lifx/network/mdns/discovery.py, src/lifx/network/mdns/__init__.py, src/lifx/__init__.py, scripts/ipv6_thread_probe.py</files>
  <read_first>
    - .planning/phases/11-mdns-hardening/11-SPEC.md (AC5, AC23-AC24)
    - .planning/phases/11-mdns-hardening/11-CONTEXT.md (D-03, D-04 and internal consumer list)
    - .planning/phases/11-mdns-hardening/11-RESEARCH.md (Pitfall 8 and runtime-state inventory)
    - .planning/phases/11-mdns-hardening/11-PATTERNS.md (explicit exports and internal script migration)
    - src/lifx/network/mdns/types.py (service-record class and equality/hash methods)
    - src/lifx/network/mdns/discovery.py (generator, preserved record-to-device factory, device generator, examples/docstrings, type annotations)
    - src/lifx/network/mdns/__init__.py (imports, module example, `__all__`, current public factory export to retain)
    - src/lifx/__init__.py (top-level imports and `__all__`)
    - scripts/ipv6_thread_probe.py (record annotations, collection generator, record factory usage)
  </read_first>
  <action>
    Rename the service-record class to `_LifxServiceRecord` and the low-level generator to `_discover_lifx_services` in their defining modules, annotations, docstrings, internal calls, equality checks, and structured logger class names. Remove exactly those old imports, attributes, examples, and `__all__` entries from both package levels; do not add module `__getattr__`, alias assignments, deprecation shims, a transport enum, or a public raw metadata accessor. Preserve `create_device_from_record` under its existing name, signature, defining-module location, and `lifx.network.mdns.__all__` export, and add a runtime regression for that preservation. This explicitly rejects the review suggestion to rename/remove the factory: D-03 and SPEC AC5 enumerate the record, generator, and absent enum, so removing a third existing export would be a new one-way decision the user has not made. Keep `discover_devices_mdns()` and public `discover_mdns()` behaviour unchanged, with the former consuming the private generator and the preserved factory internally. Migrate `scripts/ipv6_thread_probe.py` to direct imports of the private record/generator from their defining modules while retaining its existing factory import, and update its annotations and cache/reporting changes without running the probe or touching hardware/UAT output. Preserve all Phase 10 transport/probe behaviour and do not edit generated protocol/product files or `docs/changelog.md`. Addresses review concern: scopes the deliberate break exactly to the locked symbols instead of extending it on planner authority.
  </action>
  <verify>
    <automated>uv run --frozen python -c "import lifx, lifx.network.mdns as m; removed = ('LifxServiceRecord', 'discover_lifx_services', 'TransportMethod'); assert all(name not in lifx.__all__ and not hasattr(lifx, name) for name in removed); assert all(name not in m.__all__ and not hasattr(m, name) for name in removed); assert 'create_device_from_record' in m.__all__ and callable(m.create_device_from_record)"</automated>
  </verify>
  <acceptance_criteria>
    - Defining modules expose `_LifxServiceRecord` and `_discover_lifx_services` for private internal use; no old-name aliases exist
    - Neither package imports, exports, or aliases the old record/generator names or underscored replacements
    - Supported `discover_mdns()`, `discover_devices_mdns()`, and `create_device_from_record()` names/signatures remain intact; the factory stays exported from `lifx.network.mdns`
    - The internal probe imports private definitions directly and no hardware operation runs
    - No enum, raw public field, compatibility hook, generated-file edit, or changelog edit appears
  </acceptance_criteria>
  <done>The record and generator are private implementation details with exactly the intentional API break, while the unmentioned existing factory remains available.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate tests and prove the scoped public cutover</name>
  <reversibility rating="reversible">Test imports and assertions can be revised locally; the one-way public decision is recorded on Task 1.</reversibility>
  <files>tests/test_network/test_mdns/test_discovery.py, tests/test_api/test_api_discovery.py, tests/test_scripts/test_ipv6_thread_probe.py</files>
  <read_first>
    - .planning/phases/11-mdns-hardening/11-SPEC.md (AC3-AC5 and public API prohibition)
    - .planning/phases/11-mdns-hardening/11-PATTERNS.md (test import/patch-target and export-assertion guidance)
    - tests/test_network/test_mdns/test_discovery.py (record fixtures, generator-local imports, public/high-level tests)
    - tests/test_api/test_api_discovery.py (mDNS patch targets)
    - tests/test_scripts/test_ipv6_thread_probe.py (record fixture and probe patch targets)
    - src/lifx/network/mdns/__init__.py (preserved `create_device_from_record` export)
  </read_first>
  <behavior>
    - Test 1: old record/generator names are absent from package attributes and both `__all__` lists; private replacements are not package-exported; `create_device_from_record` remains present and callable from `lifx.network.mdns`
    - Test 2: direct defining-module private imports work for repository-internal tests and probe fixtures
    - Test 3: public `discover_mdns()` still yields each specialised device with correct connectivity
    - Test 4: renamed private-symbol test classes preserve every old behavioural assertion, and each revised bounds assertion is equivalent or stronger rather than deleted
    - Test 5: the full frozen suite remains green after the source/test import migration
  </behavior>
  <action>
    Update repository tests and patch targets to import `_LifxServiceRecord` and `_discover_lifx_services` directly from their defining modules while continuing to use `create_device_from_record` under its existing name. Rename only `TestLifxServiceRecord` and `TestDiscoverLifxServices` to private-symbol counterparts; preserve `TestCreateDeviceFromRecord` and every test function/behavioural assertion. Confirm Plan 11-02 replaced each old `TestLifxRecordCacheBounds` assertion with an equivalent or stronger explicit bound assertion. Add runtime assertions for absence of the two old names and absence of package-exported private replacements, plus a positive assertion that `create_device_from_record` remains in `lifx.network.mdns.__all__` and callable. Parameterise device factory coverage across plain, infrared, HEV, multizone, matrix, and ceiling products so connectivity propagation is proven for the complete class lattice without changing specialised constructors. Run the focused and full frozen suites in this wave; Plan 11-06 owns the post-documentation structured surface audit, full privacy diff, diff-derived patch coverage, and final quality gates. Addresses review concerns: makes the public-cutover scope executable and non-vacuous while preserving every existing test contract.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_network/test_mdns tests/test_devices/test_base.py tests/test_api/test_api_discovery.py tests/test_scripts/test_ipv6_thread_probe.py -q &amp;&amp; uv run --frozen pytest -q &amp;&amp; uv run --frozen python -c "import lifx, lifx.network.mdns as m; removed = ('LifxServiceRecord', 'discover_lifx_services', 'TransportMethod'); private = ('_LifxServiceRecord', '_discover_lifx_services'); assert all(not hasattr(lifx, name) for name in removed); assert all(not hasattr(m, name) for name in removed); assert all(name not in m.__all__ for name in private); assert 'create_device_from_record' in m.__all__ and callable(m.create_device_from_record)"</automated>
  </verify>
  <acceptance_criteria>
    - Internal test/probe imports and mocks use the two private defining-module symbols and remain green
    - Public export assertions prove old record/generator names and private replacements are unavailable from package surfaces, while the existing factory remains available
    - Every specialised device class receives the same correct connectivity property through the factory
    - The focused and full frozen suites pass without test function/file deletion or hardware execution
  </acceptance_criteria>
  <done>The locked record/generator cutover is complete, the unapproved factory removal is absent, and all migrated repository consumers remain green.</done>
</task>

</tasks>

<threat_model>
## Security Profile

- `asvs_level: L1` (opportunistic)
- `block_threshold: high`; any unresolved critical/high threat blocks plan completion

## Trust Boundaries

| Boundary | Description |
|---|---|
| Internal defining modules -> public package exports | Accidental aliases can recreate removed record/generator contracts, while over-broad removal can break an unmentioned existing factory |
| Private hardware probe -> committed source/tests | Internal consumers require migration without executing or disclosing live hardware state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-18 | Information Disclosure | public exports | high | mitigate | Remove the old record/generator exports and assert old/private names absent at runtime (both tasks) |
| T-11-19 | Tampering | cutover scope | high | mitigate | Direct private imports for the two locked symbols, explicit alias/export absence tests, and a positive regression retaining `create_device_from_record` (both tasks) |
| T-11-20 | Information Disclosure | probe/test evidence | high | mitigate | Never run the probe and use synthetic fixtures only; Plan 11-06 owns the complete immutable-base privacy audit (Task 2) |
| T-11-21 | Denial of Service | migration regression | high | mitigate | Focused plus full frozen suites with no test deletion or skip (Task 2) |
| T-11-SC | Tampering | package supply chain | low | accept | No install is planned and dependencies remain empty; any package change is out of scope and blocks completion |
</threat_model>

## Multi-Source Coverage Audit

`11-SOURCE-AUDIT.md` records complete coverage for the phase goal, MDNS-01 through MDNS-08,
D-01 through D-14, all in-scope research constraints, 28/28 SPEC edge rows, and 6/6 SPEC
prohibition rows. This plan implements D-03 exactly and explicitly rejects the reviewer-proposed
`create_device_from_record` removal because no locked source authorises that additional break.

<verification>
- `uv run --frozen pytest tests/test_network/test_mdns tests/test_devices/test_base.py tests/test_api/test_api_discovery.py tests/test_scripts/test_ipv6_thread_probe.py -q`
- `uv run --frozen pytest -q`
- `uv run --frozen python -c "import lifx.network.mdns as m; assert 'create_device_from_record' in m.__all__ and callable(m.create_device_from_record)"`
- Plan 11-06 owns the complete quality, documentation, privacy, structured-surface, and diff-derived patch-coverage gates after both Wave 4 plans land
</verification>

<success_criteria>
The locked record/generator cutover is complete without aliases, `create_device_from_record` remains available because its removal was not authorised, and all repository consumers pass focused and full frozen tests before the final phase gate.
</success_criteria>

<output>
Create `.planning/phases/11-mdns-hardening/11-05-SUMMARY.md` when done.
</output>
