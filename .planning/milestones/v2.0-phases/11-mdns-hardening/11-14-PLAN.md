---
phase: 11-mdns-hardening
plan: 14
type: execute
wave: 11
depends_on:
  - 11-11
  - 11-12
  - 11-13
files_modified:
  - tests/test_api/test_api_discovery.py
  - .planning/phases/11-mdns-hardening/11-GAP-CLOSURE-EVIDENCE.md
autonomous: true
gap_closure: true
requirements:
  - MDNS-02
estimate:
  tokens: 18000
  raw_tokens: 36000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "The four emulator-backed label-discovery tests fail when a discovered supported product cannot construct a Device; they cannot convert missing construction behaviour into a skip (D-04, D-16)."
    - "Fresh focused mDNS/probe/API tests, the full suite, Ruff lint, Ruff format, Pyright, both documentation builds, branch patch coverage, privacy scanning, GPG signature verification, and DCO verification all pass on the final gap tree."
    - "The final evidence audits all five verifier gaps, all seven review findings, every applicable one of the SPEC's 29 edge rows, all seven SPEC prohibitions, and the complete MDNS-01..MDNS-08 requirement set without relabelling historical results."
    - "Only synthetic identifiers and documentation-range addresses appear in tests or evidence; raw hardware/network output and signer identity remain outside the repository."
  artifacts:
    - path: tests/test_api/test_api_discovery.py
      provides: "Unskippable supported-device construction assertions in four label API paths."
    - path: .planning/phases/11-mdns-hardening/11-GAP-CLOSURE-EVIDENCE.md
      provides: "Fresh commands, actual results, source/edge/prohibition coverage, privacy status, and signed/DCO commit attestation."
  key_links:
    - from: tests/test_api/test_api_discovery.py::TestFindByLabel
      to: src/lifx/network/discovery.py::DiscoveredDevice.create_device
      via: "Supported emulator discovery must return a real device before label lookup proceeds."
    - from: .planning/phases/11-mdns-hardening/11-GAP-BASE.txt
      to: scripts/check_patch_coverage.py
      via: "The immutable pre-gap SHA scopes changed-line and changed-branch coverage to Plans 11-10 through 11-14."
    - from: .planning/phases/11-mdns-hardening/11-GAP-CLOSURE-EVIDENCE.md
      to: .planning/phases/11-mdns-hardening/11-VERIFICATION.md
      via: "Each authoritative missing truth/artifact/link is paired with current command-backed closure evidence."
---

<objective>
Remove the last conditional API-test weakness and produce fresh, privacy-safe, signed evidence that the complete Phase 11 gap-closure tree satisfies the goal, requirements, SPEC edges, prohibitions, verifier gaps, and review findings.

Purpose: Make phase completion depend on current executable evidence rather than historical green counts or silent emulator skips.
Output: Four strict API assertions and one comprehensive gap-closure evidence report.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-mdns-hardening/11-CONTEXT.md
@.planning/phases/11-mdns-hardening/11-RESEARCH.md
@.planning/phases/11-mdns-hardening/11-PATTERNS.md
@.planning/phases/11-mdns-hardening/11-SPEC.md
@.planning/phases/11-mdns-hardening/11-VERIFICATION.md
@.planning/phases/11-mdns-hardening/11-REVIEW.md
@.planning/phases/11-mdns-hardening/11-10-SUMMARY.md
@.planning/phases/11-mdns-hardening/11-11-SUMMARY.md
@.planning/phases/11-mdns-hardening/11-12-SUMMARY.md
@.planning/phases/11-mdns-hardening/11-13-SUMMARY.md
@.planning/phases/11-mdns-hardening/11-GAP-BASE.txt

Evidence must report only commands actually run against the final tree. A missing, skipped, or failed required gate blocks completion and must not be rewritten as success.
</context>

## Artifacts this phase produces

- `tests/test_api/test_api_discovery.py` — four supported-product construction checks that fail rather than skip.
- `.planning/phases/11-mdns-hardening/11-GAP-CLOSURE-EVIDENCE.md` — current, anonymised closure evidence and complete source audit.

<tasks>

<task type="tracer">
  <name>Task 1: Make supported-device construction assertions unskippable</name>
  <files>tests/test_api/test_api_discovery.py</files>
  <read_first>
    - `tests/test_api/test_api_discovery.py`: `TestFindByLabel.test_find_by_label_found`, `test_find_by_label_case_insensitive`, `test_find_by_label_substring_match`, and `test_find_by_label_exact_match`.
    - `src/lifx/network/discovery.py`: `DiscoveredDevice.create_device` supported-product construction and relay/button filtering.
    - `.planning/phases/11-mdns-hardening/11-REVIEW.md`: WARNING for four conditional `Device creation returned None` skips.
    - `.planning/phases/11-mdns-hardening/11-CONTEXT.md`: D-04 and D-16 class/public boundary decisions.
  </read_first>
  <action>
    In exactly the four named emulator-backed label tests, replace the conditional skip after `create_device()` with an explicit non-None assertion carrying a safe static failure message. Retain legitimate emulator fixture availability handling outside these test bodies and retain production relay/button filtering; the known emulator products in these tests are supported lights, so failed construction is a contract failure. Do not broaden this task into broadcast discovery implementation changes. Add no raw emulator identifier to assertion output. Commit with a Conventional Commit message using `git commit -S -s`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-11-14-uv-cache uv run --frozen pytest tests/test_api/test_api_discovery.py::TestFindByLabel::test_find_by_label_found tests/test_api/test_api_discovery.py::TestFindByLabel::test_find_by_label_case_insensitive tests/test_api/test_api_discovery.py::TestFindByLabel::test_find_by_label_substring_match tests/test_api/test_api_discovery.py::TestFindByLabel::test_find_by_label_exact_match -q -W error::ResourceWarning</automated>
  </verify>
  <acceptance_criteria>
    - None of the four named tests contains the conditional `Device creation returned None` skip.
    - Each test asserts successful supported-product construction before entering the device context.
    - Legitimate emulator availability and unsupported relay/button filtering behaviour are unchanged.
    - Failure text is static and contains no serial, MAC, address, hostname, or account value.
  </acceptance_criteria>
  <done>Supported-device construction failures in all four label API paths are test failures, never silent skips.</done>
</task>

<task type="auto">
  <name>Task 2: Run fresh closure gates and record complete source-backed evidence</name>
  <files>.planning/phases/11-mdns-hardening/11-GAP-CLOSURE-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/11-mdns-hardening/11-GAP-BASE.txt`: immutable patch-coverage base SHA.
    - `.planning/phases/11-mdns-hardening/11-VERIFICATION.md`: five authoritative gaps and missing artifacts/links.
    - `.planning/phases/11-mdns-hardening/11-REVIEW.md`: four critical and three warning findings.
    - `.planning/phases/11-mdns-hardening/11-SPEC.md`: 29 Edge Coverage rows, seven Prohibitions, and the MDNS-01..MDNS-08 traceability table.
    - `scripts/check_patch_coverage.py`: base/source/coverage arguments and changed-branch enforcement.
    - `/Users/amiller/.codex/skills/evidence-privacy-audit/scripts/scan_private_candidates.py`: location-only privacy reporting contract.
  </read_first>
  <action>
    Validate `11-GAP-BASE.txt` as one full commit reachable from `HEAD` and keep it unchanged. Run the focused mDNS, probe, and API tests with `ResourceWarning` promoted; then run the entire frozen suite. Produce a fresh branch-coverage JSON in `/private/tmp` with coverage for `lifx` and `scripts.ipv6_thread_probe`, and run `scripts/check_patch_coverage.py` separately for `src/lifx/network/mdns/discovery.py` and `scripts/ipv6_thread_probe.py` using the recorded base. Changed lines and changed branches must both be 100%; never weaken exclusions, tests, coverage configuration, or the checker to make this pass. Run Ruff lint, Ruff format check, Pyright, Zensical build, and llmstxt build.

    Run the privacy scanner across every file changed since the recorded base plus the evidence report, reporting candidate locations/categories only. Inspect the staged diff and all commits in `base..HEAD` for live serials/MACs, non-documentation addresses, local hostnames, account identifiers, and raw discovery output; fix any exposure before continuing and never copy a private mapping or signer identity into the repository. Verify every gap commit with `git verify-commit` and prove the number of commits containing a `Signed-off-by:` trailer equals `git rev-list --count`; report pass/count only.

    Create `11-GAP-CLOSURE-EVIDENCE.md` with timestamp, exact commit/base identifiers, commands, actual exit status/counts, and no claimed pass for skipped/failed work. Include explicit tables mapping: all five verifier gaps; all four critical plus three warning review findings; each MDNS-01..MDNS-08 requirement; each of the SPEC's 29 Edge Coverage rows; and all seven Prohibitions to a current test node, implementation symbol, documentation contract, or justified out-of-phase exclusion already locked by source. Record the assumption delta as `no-change` because `one-second` is a duration. Record that no external API/SDK matrix applies and that the broadcast schedule/hardware paths were not executed. If any required gate, high/critical threat mitigation, source item, edge, or prohibition lacks current evidence, stop without declaring completion. Commit the evidence with a Conventional Commit message using `git commit -S -s`; the executor summary must verify that final commit's signature and DCO trailer too.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-11-14-uv-cache uv run --frozen pytest tests/test_network/test_mdns tests/test_scripts/test_ipv6_thread_probe.py tests/test_api/test_api_discovery.py -q -W error::ResourceWarning &amp;&amp; UV_CACHE_DIR=/private/tmp/lifx-async-11-14-uv-cache uv run --frozen pytest -q -p no:sugar &amp;&amp; UV_CACHE_DIR=/private/tmp/lifx-async-11-14-uv-cache uv run --frozen ruff check . &amp;&amp; UV_CACHE_DIR=/private/tmp/lifx-async-11-14-uv-cache uv run --frozen ruff format --check . &amp;&amp; UV_CACHE_DIR=/private/tmp/lifx-async-11-14-uv-cache uv run --frozen pyright &amp;&amp; UV_CACHE_DIR=/private/tmp/lifx-async-11-14-uv-cache uv run --frozen zensical build &amp;&amp; UV_CACHE_DIR=/private/tmp/lifx-async-11-14-uv-cache uv run --frozen llmstxt-standalone build</automated>
  </verify>
  <acceptance_criteria>
    - Fresh focused and full tests, Ruff lint/format, Pyright, and both documentation builds pass on the final gap tree.
    - Changed-line and changed-branch patch coverage are both 100% for both changed production modules from the immutable pre-gap SHA.
    - Privacy scanning and staged/history inspection find no committed private identifier or raw hardware/network evidence.
    - Every commit after the base is GPG-valid and DCO-signed; evidence records only pass/count, never signer identity.
    - The evidence report covers five verifier gaps, seven review findings, eight requirements, 29 edge rows, and seven prohibitions with no missing row or historical result presented as current.
    - Any failed/skipped gate or unresolved high/critical threat blocks the task rather than being documented as completion.
  </acceptance_criteria>
  <done>Current, privacy-safe, 100%-patch-covered, signed/DCO evidence proves the complete Phase 11 gap closure without scope expansion or silent omissions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Emulator discovery -> API test | A supported device construction failure could be hidden by conditional skipping. |
| Worktree/history -> committed evidence | Live identifiers, signer identity, or stale results could enter a durable artefact. |
| Test/coverage output -> completion claim | A skipped, failed, historical, or weakened gate could be misrepresented as proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-14-01 | Repudiation | conditional API tests | high | mitigate | Replace four construction skips with explicit non-None assertions and run exact nodes. |
| T-11-14-02 | Information disclosure | evidence and Git history | high | mitigate | Scan changed files/staged diff/history with location-only reporting and prohibit raw mappings or signer identity. |
| T-11-14-03 | Tampering | coverage/test gates | high | mitigate | Use immutable base, frozen commands, full branch coverage, and forbid config/checker weakening. |
| T-11-14-04 | Repudiation | commit provenance | high | mitigate | Verify GPG signatures and DCO trailer count across the entire gap range plus final evidence commit. |
| T-11-14-05 | Elevation of privilege | phase completion | high | mitigate | Block completion on any missing source-audit row, failed/skipped gate, or unresolved high/critical threat. |
| T-11-14-SC | Tampering | package supply chain | low | accept | Frozen existing dependencies only; no package installation or lockfile change occurs. |
</threat_model>

<verification>
- Before writing a pass, run branch coverage: `UV_CACHE_DIR=/private/tmp/lifx-async-11-14-uv-cache uv run --frozen pytest -q -p no:sugar --cov=lifx --cov=scripts.ipv6_thread_probe --cov-branch --cov-report=json:/private/tmp/lifx-async-11-gap-coverage.json`.
- Run `UV_CACHE_DIR=/private/tmp/lifx-async-11-14-uv-cache uv run --frozen python scripts/check_patch_coverage.py --base $(tr -d '\n' &lt; .planning/phases/11-mdns-hardening/11-GAP-BASE.txt) --coverage /private/tmp/lifx-async-11-gap-coverage.json --source src/lifx/network/mdns/discovery.py` and repeat for `--source scripts/ipv6_thread_probe.py`.
- Run `UV_CACHE_DIR=/private/tmp/lifx-async-11-14-uv-cache uv run --frozen python /Users/amiller/.codex/skills/evidence-privacy-audit/scripts/scan_private_candidates.py AGENTS.md CLAUDE.md docs/getting-started/quickstart.md src/lifx/network/mdns/types.py src/lifx/network/mdns/discovery.py scripts/ipv6_thread_probe.py tests/test_network/test_mdns/test_discovery.py tests/test_network/test_mdns/test_phase_contract.py tests/test_scripts/test_ipv6_thread_probe.py tests/test_api/test_api_discovery.py .planning/phases/11-mdns-hardening/11-GAP-CLOSURE-EVIDENCE.md`.
- Verify all commits in the immutable-base range using `git verify-commit` with output suppressed; compare the DCO-matching commit count with `git rev-list --count` and record only counts.
- Re-run `git diff --check` and inspect the exact staged file list before the final signed/DCO commit.
</verification>

<success_criteria>
- The final warning and every upstream gap plan are proven on the same current tree.
- All high threats are mitigated; no required gate is skipped or historical.
- Complete GOAL/REQ/RESEARCH/CONTEXT/SPEC/review/verifier coverage is explicit and no deferred or out-of-phase work is introduced.
</success_criteria>

## Source coverage audit

| Source item | Coverage |
|-------------|----------|
| GOAL: mesh-scale correctness, broadcast-grade validation, honest docs | COVERED by the complete Plans 11-10..11-14 gate and evidence matrix. |
| REQ MDNS-02 | COVERED here by strict supported-device construction assertions; MDNS-01 and MDNS-03..08 remain covered by existing plans and the preceding gap plans. |
| VERIFICATION gaps 1-5 | COVERED by Plans 11-10, 11-11, and 11-13; re-proven here. |
| REVIEW critical 1-4 and warnings 1-3 | COVERED by Plans 11-10..11-14; every row is required in evidence. |
| RESEARCH constraints and architectural responsibility map | COVERED: stdlib/async/private boundary retained, source owns runtime, script owns diagnostics, docs own claims. |
| CONTEXT D-01..D-16 | COVERED across the complete Phase 11 plan set and explicitly audited here; no deferred idea is introduced. |
| SPEC 29 Edge Coverage rows and seven Prohibitions | COVERED by mandatory one-row-per-item evidence mapping; a missing row blocks completion. |
| Broadcast schedule, default discovery promotion, live hardware, external API/SDK matrix | EXCLUDED by locked scope and recorded as such rather than implemented or fabricated. |

<output>
Create `.planning/phases/11-mdns-hardening/11-14-SUMMARY.md` when done.
</output>
