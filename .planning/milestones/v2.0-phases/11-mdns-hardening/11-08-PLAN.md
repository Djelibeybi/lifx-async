---
phase: 11-mdns-hardening
plan: 08
type: execute
wave: 7
depends_on: [11-07]
files_modified:
  - .planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md
autonomous: false
requirements: [MDNS-08]
gap_closure: true
tags: [mdns, gap-closure, privacy, history, decision]

estimate:
  tokens: 9000
  raw_tokens: 18000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Before any rewrite decision, the exact repository, branch, HEAD, merge base, affected commit set, unpushed range, upstream state, local/remote refs, tags, and worktrees are re-established read-only."
    - "Every history scan is value-suppressed; the decision record contains commit SHA, repository location, candidate category, reachability, and disposition only."
    - "Local-only status is asserted only after a successful remote-ref refresh and containment check; uncertainty or any shared/pushed reachability prevents the rewrite option from proceeding."
    - "The audit distinguishes inherited baseline candidates from phase-owned additions/removals before the operator explicitly chooses rewrite, no-rewrite-required, or stop; auto-advance cannot choose on their behalf."
    - "The signed authority commits cd0c3ac and f7c7c6f remain exact, and rewrite is unavailable when either commit lies in the affected commit-to-disposition-boundary range."
    - "No pre-existing commit, local branch, worktree, tag, remote repository ref, mapping, or hardware state is mutated; only the audit commit and refreshed remote-tracking observations are allowed."
  artifacts:
    - path: ".planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md"
      provides: "Exact value-suppressed history/ref/worktree reachability audit and operator decision inputs"
      contains: "History reachability audit"
  key_links:
    - from: ".planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md"
      to: "11-09-PLAN.md"
      via: "rewrite_required/local_only plus the explicit rewrite/no-rewrite checkpoint response form Plan 11-09's precondition"
      pattern: "rewrite_required.*(true|false)"
---

<objective>
Establish the exact reachability and sharing status of the value-suppressed Phase 11 history finding, then obtain an explicit blocking-human decision before any current-branch rewrite.

Purpose: distinguish a safe local-only history repair from a shared-history coordination problem without assuming authority from the branch's lack of an upstream.
Output: a committed reachability audit, an explicit operator decision, and `11-08-SUMMARY.md` recording only the selected disposition.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
@$HOME/.codex/skills/evidence-privacy-audit/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/phases/11-mdns-hardening/11-VERIFICATION.md
@.planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md
@.planning/phases/11-mdns-hardening/11-07-SUMMARY.md

<interfaces>
Current diagnostic hints, never rewrite authority:
- Branch: `gsd/phase-11-mdns-hardening`; no upstream was shown at planning time.
- `origin/main..HEAD` was 57 commits at planning time.
- The value-suppressed per-commit patch scan identified an `ipv6` category at commit `70d300d7ca3b00e967213a0c8eedb019cfbf23f9`, while snapshot scans also found candidate categories in the merge base/origin-main STATE; the hit's direction, ownership, and disposition are unresolved until Task 1 classifies them without values.
- No remote branch or tag was locally known to contain that commit, and one worktree was present; every fact must be refreshed before the decision.
</interfaces>
</context>

## Artifacts this phase produces

This plan produces only a location/category/ref reachability audit and an operator decision. It does not rewrite, amend, rebase, reset, push, force-push, delete, garbage-collect, or publish any history; it does not inspect the external private mapping.

<tasks>

<task type="tracer">
  <name>Task 1: Trace the history finding through every local and remote reachability surface</name>
  <precondition>`11-07-SUMMARY.md` exists, its two task commits are signed+DCO, the current branch is `gsd/phase-11-mdns-hardening`, and the worktree/index are clean.</precondition>
  <files>.planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md</files>
  <read_first>
    - $HOME/.codex/skills/evidence-privacy-audit/SKILL.md (current file versus unpushed history and shared-state rules)
    - .planning/phases/11-mdns-hardening/11-07-SUMMARY.md (completed current-file remediation and preserved commits)
    - .planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md (current-file scope; extend append-only)
    - .planning/phases/11-mdns-hardening/11-VERIFICATION.md (immutable phase base and privacy boundary; current SPEC labels that prohibition P7)
  </read_first>
  <action>
    Re-establish the repository root, exact branch, HEAD, `origin/main` tip, merge base, ahead count, upstream presence/absence, worktree list, local branches, remote-tracking branches, tags, and all refs containing each affected commit. Refresh origin refs without merging, rebasing, checking out, pushing, pruning local branches, or changing a worktree, then repeat the containment checks. If remote refresh/authentication fails, record `shared_status: uncertain`; do not infer local-only status from stale refs or the absence of an upstream.

    Scan the confirmed merge-base snapshot, current HEAD snapshot, merge-base-to-HEAD Phase 11 patches, and per-commit STATE snapshots/patches through `scan_private_candidates.py` standard input with commit metadata suppressed. Identify every candidate-bearing snapshot or patch by SHA, location/category, and direction only; do not print or save the matching line/value. Classify hits as inherited baseline, phase-added, phase-removed/modified, clearly synthetic/reserved/public, approved pseudonym requiring operator-only confirmation, live, or unresolved. Do not infer that the patch hit at `70d300d7ca3b00e967213a0c8eedb019cfbf23f9` is the current dirty value merely because both have the `ipv6` category. Check whether every phase-owned affected commit is reachable from a remote-tracking branch, tag, another local branch, another worktree, or a remote ref after refresh. Record the exact earliest phase-owned affected commit (or `none`), parent, current head, commit count, merge base, baseline/phase ownership, and complete ref/worktree disposition in the `History reachability audit` section of `11-PRIVACY-REMEDIATION.md`.

    Record `phase_owned_unresolved_count` and `baseline_unresolved_count` explicitly. Set `rewrite_required: true` only for a live/unresolved phase-owned candidate in reachable unpushed history. Set `rewrite_required: false` only when `phase_owned_unresolved_count: 0`, every phase-owned hit is synthetic/reserved/public, operator-approved pseudonym, or removal-only, and the current file/diff is clean. An inherited live/unresolved baseline candidate makes this plan `shared_status: blocked` because rewriting origin/main/shared ancestry is outside scope. Set `local_only: true` only when a rewrite is required, all remote checks completed successfully, and no affected phase-owned commit is reachable from any remote ref/tag or another branch/worktree. Otherwise set it false/uncertain and name the blocking category without a value.

    Compute the affected rewrite range from the earliest phase-owned affected commit through the pre-disposition HEAD and record `preserved_authority_in_affected_range` for `cd0c3ac` and `f7c7c6f` without changing either object. Set `rewrite_eligible: true` only when `rewrite_required: true`, `local_only: true`, and neither preserved commit is in that range. If either preserved commit is included, set `rewrite_eligible: false` and `escalation_reason: preserved_authority_commit_in_range`; no rewrite option may be offered because descendant re-signing would amend a locked commit. This incompatibility routes only to the explicit stop/escalation decision.

    Record that old objects may remain in local reflogs/object storage after an authorised rewrite and that Plan 11-09 will not expire reflogs or run garbage collection. Before committing, clear the index and stage only `11-PRIVACY-REMEDIATION.md` by exact path. Run the value-suppressed scanner over that staged file and over `git diff --cached --no-ext-diff --binary` through scanner standard input. Exit 2 or any incomplete scan blocks; exit 1 blocks until every location/category hit is classified, and any live or unresolved candidate blocks the commit. Confirm the staged path set contains only the report and run `git diff --cached --check`, then commit with `git commit -S -s`. Attempt cryptographic verification of the new audit commit; invalid/missing signature or DCO blocks. If verification fails only because the execution environment cannot access the verifier/keyring, Task 2 must require approved external verification of the exact audit commit before accepting a disposition. Make no other tracked or Git-history mutation.
  </action>
  <verify>
    <automated>uv run --frozen python -c 'from pathlib import Path; text = Path(".planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md").read_text(encoding="utf-8"); required = ("History reachability audit", "repository:", "branch:", "head_before_rewrite:", "merge_base:", "baseline_candidates:", "baseline_unresolved_count:", "phase_owned_candidates:", "phase_owned_unresolved_count:", "earliest_affected_commit:", "affected_commit_count:", "upstream:", "remote_refresh:", "remote_reachability:", "worktree_reachability:", "rewrite_required:", "local_only:", "rewrite_eligible:", "preserved_authority_in_affected_range:", "category: ipv6"); assert all(token in text for token in required), [token for token in required if token not in text]' &amp;&amp; git diff --check -- .planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The report supplies exact current range/ref/worktree facts and successful/uncertain remote-refresh status
    - Every hit is named only by SHA, file/category, direction, baseline/phase ownership, and reachability disposition
    - A category match alone never equates the dirty finding with a historical candidate
    - `local_only: true` is impossible when remote state is stale, authentication failed, or any other ref/worktree contains the commit
    - `rewrite_eligible: true` is impossible when the affected range contains cd0c3ac or f7c7c6f; that case records the preservation conflict and routes only to stop/escalation
    - No raw value, value hash, private mapping, author email, patch line, packet, or discovery output appears in output/report
    - The report and staged diff receive a complete value-suppressed scan before commit; unresolved/live candidates and scanner exit 2 block
    - No pre-existing history or external state is mutated and the report commit is signed+DCO and cryptographically verified either in-process or through the approved external-verification escalation
  </acceptance_criteria>
  <done>The operator has a current, value-suppressed and fail-closed account of exactly what a rewrite would touch and whether that history is demonstrably local-only.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Decide the audited history disposition</name>
  <read_first>
    - .planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md (exact range, reachability, and local_only status)
  </read_first>
  <decision>Select the history disposition supported by the completed Phase 11 reachability audit.</decision>
  <context>
    A later redaction commit cannot remove a live candidate from an earlier reachable commit. If and only if the report says `rewrite_required: true`, `local_only: true`, `rewrite_eligible: true`, and `preserved_authority_in_affected_range: none`, `rewrite` authorises Plan 11-09 to rewrite the named current-branch range, compare the tree immediately before and after rebase, re-sign every rewritten commit, verify DCO/authorship/count/tree invariants, and delete its temporary non-branch safety ref after success. If the report says `rewrite_required: false` and has zero live/unresolved phase-owned candidates, `no-rewrite` confirms the operator-only pseudonym classifications where needed and authorises Plan 11-09 to preserve every pre-existing commit identity through the disposition boundary while running final current-head gates. Neither option authorises a push, force-push, shared-baseline rewrite, amendment of cd0c3ac or f7c7c6f, another branch/worktree rewrite, reflog expiry, garbage collection, or repository access to the external mapping. A blocked/uncertain/shared-baseline finding, failed cryptographic verification, or an affected range containing either preserved authority commit requires `stop`, which is the explicit escalation path.
  </context>
  <options>
    <option id="rewrite">
      <name>Rewrite eligible local-only history</name>
      <pros>Removes the candidate from the current branch's pushable history while preserving the disposition-boundary tree and signed+DCO policy.</pros>
      <cons>Changes every descendant commit ID in the exact audited range and is forbidden when that range includes either preserved authority commit.</cons>
    </option>
    <option id="no-rewrite">
      <name>Confirm no rewrite required</name>
      <pros>Preserves every commit ID when the audit proves no live or unresolved phase-owned candidate remains and privately confirms any approved pseudonyms.</pros>
      <cons>Cannot be selected for a live/unresolved phase-owned finding or to waive a shared-baseline finding.</cons>
    </option>
    <option id="stop">
      <name>Escalate and stop without rewriting</name>
      <pros>Leaves every commit ID untouched and performs no destructive history operation.</pros>
      <cons>The privacy prohibition remains unresolved and Phase 11 cannot pass or be pushed from this branch.</cons>
    </option>
  </options>
  <verify>
    <automated>uv run --frozen python -c 'from pathlib import Path; text = Path(".planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md").read_text(encoding="utf-8"); assert all(token in text for token in ("rewrite_required:", "local_only:", "rewrite_eligible:", "preserved_authority_in_affected_range:", "remote_refresh:", "phase_owned_candidates:"))'</automated>
  </verify>
  <acceptance_criteria>
    - Auto-advance cannot choose a disposition
    - `rewrite` is accepted only when the report says `rewrite_required: true`, `local_only: true`, `rewrite_eligible: true`, and neither cd0c3ac nor f7c7c6f is in the affected range
    - `no-rewrite` is accepted only when the report says `rewrite_required: false`, no inherited or phase-owned live/unresolved finding blocks the scope, and the operator privately confirms any approved pseudonym provenance
    - The summary records only `rewrite`, `no-rewrite`, or `stop` plus report references; no candidate or mapping value is repeated
    - `stop` is mandatory when preservation and sanitisation conflict, cryptographic verification cannot be established, or sharing state is blocked/uncertain; it halts gaps-only execution before 11-09 and leaves Phase 11 explicitly unresolved
  </acceptance_criteria>
  <resume-signal>Reply exactly `rewrite`, `no-rewrite`, or `stop` as permitted by the committed audit. Do not include candidate values or private mapping details.</resume-signal>
</task>

</tasks>

<threat_model>
## Security Profile

- `asvs_level: L1`
- `block_threshold: high`; all critical/high findings must be mitigated before completion

## Trust Boundaries

| Boundary | Description |
|---|---|
| Local Git graph -> shared-state classification | Missing upstream does not prove a commit was never pushed, tagged, or shared. |
| Value-bearing history -> audit report | Candidate content must be classified without entering output or tracked evidence. |
| Read-only audit -> destructive authority | Diagnostics cannot silently become permission to rewrite commit identities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-35 | Tampering | Range/ref classification | high | mitigate | Task 1 refreshes and records exact merge base, refs, tags, upstream, worktrees, and remote containment; uncertainty fails closed. |
| T-11-36 | Information Disclosure | Historical STATE patches | high | mitigate | All streams pass through the location/category-only helper with commit metadata suppressed; reports never carry values. |
| T-11-37 | Repudiation | Rewrite authority | high | mitigate | A blocking-human checkpoint records the exact selected option against the committed audit; auto-mode cannot decide. |
| T-11-38 | Denial of Service | Broad accidental rewrite | medium | mitigate | The report pins earliest affected commit, parent, head, and count; 11-09 is preconditioned on the exact range and current branch only. |
| T-11-SC | Tampering | Package supply chain | low | accept | No package install or dependency change occurs. |
</threat_model>

## Multi-Source Coverage Audit

GOAL and MDNS-01 through MDNS-07 remain covered by executed Plans 11-01 through 11-06 and the D-15/D-16 recovery in 11-07. This plan claims only MDNS-08's honest evidence boundary. RESEARCH privacy constraints, CONTEXT D-09, and SPEC Prohibition P7 require current, staged, unpushed-history, and shared-state distinctions; all other D-01 through D-16 decisions, 29 Edge Coverage truths, and Prohibitions P1-P6 remain covered and unchanged. No deferred, external-API, or later-phase item is introduced.

<verification>
- Recompute exact local and remote reachability immediately before the checkpoint.
- Record only SHA/location/category/disposition facts in the remediation report.
- Fail closed on stale or unavailable remote state.
- Require the blocking-human `rewrite` response before Plan 11-09 can satisfy its precondition.
</verification>

<success_criteria>
The history finding has an exact, current, value-suppressed reachability classification and no rewrite can begin without an explicit operator decision scoped to that audited local-only range.
</success_criteria>

<output>
Create `.planning/phases/11-mdns-hardening/11-08-SUMMARY.md` when `rewrite` or `no-rewrite` is approved. If `stop` is selected, halt without producing a completion summary that could unblock 11-09.
</output>
