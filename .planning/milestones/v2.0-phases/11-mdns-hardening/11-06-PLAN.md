---
phase: 11-mdns-hardening
plan: 06
type: execute
wave: 5
depends_on: [11-04, 11-05]
files_modified:
  - tests/test_network/test_mdns/test_phase_contract.py
autonomous: true
requirements: [MDNS-01, MDNS-02, MDNS-03, MDNS-04, MDNS-05, MDNS-06, MDNS-07, MDNS-08]
tags: [mdns, final-gate, structured-audit, privacy, coverage]

estimate:
  tokens: 16000
  raw_tokens: 32000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "[explicit R1 boundary] The real transport proof observes a selected port in 1 through 65535, excludes 5353, and receives the directed loopback datagram."
    - "[explicit R2 boundary] Only exact ASCII private value `2` produces public connectivity `\"thread\"`; every other value produces `\"wifi\"` (D-01, D-02)."
    - "[explicit R2 empty] Missing and empty private connectivity metadata produce WiFi without a rejection diagnostic (D-02, D-10)."
    - "[explicit R2 encoding] Whitespace, alternate numeric spelling, negative text, malformed text, and unrecognised values remain WiFi outcomes."
    - "[explicit R3 adjacency] Duplicate/replayed records preserve one retained address and one emission."
    - "[explicit R3 empty] Empty or incomplete packet sequences yield nothing without poisoning later complete input."
    - "[explicit R3 ordering] Complete TXT/SRV/A/AAAA sets within the defensive ceilings resolve to the same identity, address membership, and selected class across packet permutations."
    - "[explicit R3 idempotency] Replay cannot duplicate addresses or re-emit a resolved instance."
    - "[explicit R3 concurrency] Simultaneous discovery calls share no cache, expiry, follow-up, serial-deduplication, emission, or diagnostic state."
    - "[explicit R4 idempotency] Follow-up sends stop at two attempts per hostname and 64 distinct targets per call, and a successful send is never repeated."
    - "[explicit R5 adjacency] Duplicate A/AAAA addresses collapse by unordered membership."
    - "[explicit R5 empty] An instance with no selectable address is not emitted."
    - "[explicit R5 ordering] Selection prefers IPv4, ULA, GUA, then scoped link-local while retaining but not selecting unscoped link-local (D-05, D-06, D-07)."
    - "[explicit R6 adjacency] Live conflicting valid TXT IDs invalidate an instance and recovery occurs only after expiry leaves one valid ID (D-12)."
    - "[explicit R6 empty] Missing or empty TXT ID cannot produce an internal record."
    - "[explicit R6 encoding] Only exact 12-hex unicast serials are accepted and normalised lowercase."
    - "[explicit R6 ordering] Conflicting same-ID TXT construction metadata and same-instance SRV target/port values remain unresolved regardless of packet or lexicographic order; only expiry leaving one consistent value permits resolution."
    - "[explicit R7 adjacency] TTL-zero grace/rescue and count-only cache-flush handling operate on exact live RR identities (D-11, D-14)."
    - "[explicit R7 empty] An empty packet is a no-op."
    - "[explicit R7 ordering] Rescue and expiry derive from current live state, not historical last-wins packet order."
    - "[explicit R7 idempotency] Repeated goodbye, rescue, and cache-flush observations do not change semantic state beyond bounded-key aggregate count magnitude."
    - "[explicit R7 concurrency] Every call starts with empty record, pending-expiry, conflict, emission, follow-up, and diagnostic state."
    - "The low-level record and generator are private without aliases, while `create_device_from_record` remains available because its removal is not authorised by D-03."
    - "A structured contract test proves exact exports, approved documentation phrases, and absence of executable multicast-membership code without broad multiline or prose-negation regexes."
    - "The complete immutable-base Phase 11 diff passes full tests, static checks, documentation builds, privacy review, configuration-weakening checks, and branch-aware coverage derived from every changed `src/lifx/**/*.py` path."
  artifacts:
    - path: "tests/test_network/test_mdns/test_phase_contract.py"
      provides: "Structured public/private export, exact-phrase, AST membership, and supported-factory contract audit"
      contains: "TestPhase11SurfaceContract"
    - path: ".planning/phases/11-mdns-hardening/11-PHASE-BASE.txt"
      provides: "Immutable pre-implementation boundary for complete diff, privacy, and patch-coverage checks"
    - path: ".planning/phases/11-mdns-hardening/COVERAGE.md"
      provides: "Reasoned declaration that this LAN protocol hardening has no external API integration matrix or user setup"
    - path: ".planning/phases/11-mdns-hardening/11-SOURCE-AUDIT.md"
      provides: "Complete ROADMAP/REQUIREMENTS/CONTEXT/RESEARCH/SPEC/REVIEWS/plan coverage reconciliation"
  key_links:
    - from: "tests/test_network/test_mdns/test_phase_contract.py"
      to: "src/lifx/__init__.py and src/lifx/network/mdns/__init__.py"
      via: "structured positive/negative export assertions"
      pattern: "create_device_from_record"
    - from: ".planning/phases/11-mdns-hardening/11-PHASE-BASE.txt"
      to: "scripts/check_patch_coverage.py"
      via: "all changed production Python paths derived from the immutable phase diff"
      pattern: "git diff --name-only"
  prohibitions:
    - statement: "MUST NOT bind the mDNS query socket to port 5353 or join the multicast group, including as a fallback for unsolicited reception."
      status: resolved
      verification: "real-socket test plus AST assertion that the mDNS module contains no membership socket option or join call"
    - statement: "MUST NOT expose `LifxServiceRecord`, `discover_lifx_services()`, `TransportMethod`, or the raw connectivity field as public API, documentation, or examples."
      status: resolved
      verification: "runtime export assertions and exact-token checks over the named public documentation/example files"
    - statement: "MUST NOT assert an expansion of the private connectivity abbreviation; only exact private value `2` may map to Thread and every other case is WiFi."
      status: resolved
      verification: "exact mapping tests plus absence of the standalone private token from public docs/examples"
    - statement: "MUST NOT apply cache-flush replacement semantics to legacy-unicast responses."
      status: resolved
      verification: "focused ordinary-processing plus aggregate-counter regressions"
    - statement: "MUST NOT allow connectivity metadata to influence address selection, routing, connection behaviour, retries, or tuning during Phase 11."
      status: dismissed-per-spec
      verification: "Phase 11 scope tests keep metadata diagnostic-only; this is not a permanent prohibition because Phase 14 may act on measured evidence"
    - statement: "MUST NOT place live serials, IP addresses, hostnames, or raw discovery output in Phase 11 tests, documentation, or evidence."
      status: resolved
      verification: "synthetic fixtures, per-commit staged review, and final immutable-base tracked/untracked human privacy audit"
---

<objective>
Close Phase 11 with a structured, non-vacuous contract audit and a complete immutable-base quality/privacy/coverage gate after the parallel documentation and code cutovers land.

Purpose: prevent fragile text searches, hand-maintained coverage lists, or a final-commit-only review from overstating the phase evidence.
Output: one structured surface-contract test module and complete phase validation evidence.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mdns-hardening/11-SPEC.md
@.planning/phases/11-mdns-hardening/11-CONTEXT.md
@.planning/phases/11-mdns-hardening/11-RESEARCH.md
@.planning/phases/11-mdns-hardening/11-REVIEWS.md
@.planning/phases/11-mdns-hardening/11-SOURCE-AUDIT.md
@.planning/phases/11-mdns-hardening/COVERAGE.md
@.planning/phases/11-mdns-hardening/11-PHASE-BASE.txt
@.planning/phases/11-mdns-hardening/11-04-SUMMARY.md
@.planning/phases/11-mdns-hardening/11-05-SUMMARY.md
@src/lifx/network/mdns/__init__.py
@src/lifx/network/mdns/transport.py
@src/lifx/__init__.py
@docs/user-guide/advanced-usage.md
@examples/discovery_mdns.py
@scripts/check_patch_coverage.py
</context>

## Artifacts this phase produces

Phase 11 produces `Device.connectivity`, `Device._connectivity`,
`Device._set_connectivity()`, `_LifxServiceRecord` with trailing defaulted `connectivity`
and `addresses` fields, `_CachedResourceRecord.expires_at`, `_validate_txt_id()`,
`_connectivity_from_txt()`, `_LifxRecordCache._MAX_TXT_RRS_PER_OWNER`,
`_LifxRecordCache._MAX_SRV_RRS_PER_OWNER`, `_LifxRecordCache._pending_expiries`,
`_LifxRecordCache.next_expiry_delay()`, `_LifxRecordCache.expire()`,
`_discover_lifx_services()`, and `TestPhase11SurfaceContract`. It preserves the existing
public `create_device_from_record()` symbol. It also produces stable diagnostic reasons
`missing_txt_id`, `invalid_txt_id`, `conflicting_txt_id`, `missing_product_id`,
`invalid_product_id`, `invalid_address`, `malformed_packet`, `rr_identity_limit`, and
`unexpected_cache_flush`, plus `11-PHASE-BASE.txt`, `COVERAGE.md`, and
`11-SOURCE-AUDIT.md`. It adds no enum, CLI flag, persistence field, schema, dependency, or
external service integration.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace brittle surface regexes with structured contract assertions</name>
  <reversibility rating="reversible">The structured audit is test-only and can be refined without changing runtime behaviour or public data.</reversibility>
  <files>tests/test_network/test_mdns/test_phase_contract.py</files>
  <read_first>
    - .planning/phases/11-mdns-hardening/11-SPEC.md (AC2, AC5, AC23-AC25 and all prohibitions)
    - .planning/phases/11-mdns-hardening/11-CONTEXT.md (D-01 through D-04 and exact limitation wording)
    - .planning/phases/11-mdns-hardening/11-REVIEWS.md (brittle audit, API scope, and exact-approved-phrase findings)
    - src/lifx/__init__.py and src/lifx/network/mdns/__init__.py (runtime exports)
    - src/lifx/network/mdns/transport.py (private approved phrases and socket operations)
    - docs/api/devices.md, docs/api/network.md, docs/api/index.md, docs/user-guide/advanced-usage.md, examples/discovery_mdns.py (public surface)
    - tests/test_network/test_mdns/test_discovery.py (existing export and cache-flush behaviour tests)
  </read_first>
  <behavior>
    - Test 1: `LifxServiceRecord`, `discover_lifx_services`, and `TransportMethod` are absent from both package surfaces; `_LifxServiceRecord` and `_discover_lifx_services` are not package-exported
    - Test 2: `create_device_from_record` remains callable and present in `lifx.network.mdns.__all__`, proving the review-proposed extra removal did not occur
    - Test 3: the named public docs/examples contain none of the exact removed/private symbol tokens, no standalone private two-letter key, and no transport-method type/expansion
    - Test 4: public guidance contains the exact approved phrases `ephemeral source port`, `legacy-unicast replies`, `does not join the multicast group`, `does not receive unsolicited announcements`, and `mesh scale is proven synthetically`
    - Test 5: the private transport/module guidance contains the exact approved phrases `IPv4 multicast query`, `ephemeral source port`, `legacy-unicast replies`, `does not join the multicast group`, `does not receive unsolicited announcements`, `cache-flush semantics do not apply`, and `cache state is scoped to one discovery call`
    - Test 6: AST inspection of `src/lifx/network/mdns` finds no executable reference to `IP_ADD_MEMBERSHIP`, `IPV6_ADD_MEMBERSHIP`, `IPV6_JOIN_GROUP`, `join_multicast`, or `rejoin_multicast`; prose is not mistaken for code
  </behavior>
  <action>
    Create `TestPhase11SurfaceContract` using ordinary imports, `pathlib`, `ast`, and exact token/phrase assertions. Assert package attributes and `__all__` values directly. Read only the explicit public files listed above and reject exact removed/private identifiers plus a standalone `tm` token; do not scan historical `.planning` or generated `docs/changelog.md`. Assert the exact approved phrases Plan 11-04 is required to write, rather than inferring truth from broad multiline regexes or a negation-word blocklist. Parse Python under `src/lifx/network/mdns` with `ast` and reject exact executable attribute/name identifiers for multicast membership/rejoin operations; do not reject truthful docstrings that say membership is absent. Keep the positive `create_device_from_record` assertion because the review suggestion conflicts with the named D-03/AC5 cutover and was deliberately rejected. Use no shell subprocess in the test.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_network/test_mdns/test_phase_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Export checks distinguish the two authorised removals from the preserved factory
    - Public token checks are path-scoped and exact; historical/generated text is excluded intentionally
    - Limitation checks assert approved phrases rather than guessing whether arbitrary prose is negated
    - Membership/rejoin checks inspect executable AST names and cannot false-fail truthful documentation
    - The test uses only the standard library and existing pytest stack
  </acceptance_criteria>
  <done>The final API and documentation surface is proven by structured positive/negative assertions that reflect the locked scope exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Run the immutable-base phase quality, privacy, and diff-derived coverage gate</name>
  <reversibility rating="reversible">The gate consumes existing artefacts and produces no persistent schema, service, or public API change.</reversibility>
  <files>tests/test_network/test_mdns/test_phase_contract.py</files>
  <read_first>
    - .planning/phases/11-mdns-hardening/11-SPEC.md (AC1-AC26, Edge Coverage, Prohibitions)
    - .planning/phases/11-mdns-hardening/11-SOURCE-AUDIT.md (every source row must remain COVERED)
    - .planning/phases/11-mdns-hardening/COVERAGE.md (reasoned no-external-API declaration)
    - .planning/phases/11-mdns-hardening/11-PHASE-BASE.txt (immutable pre-implementation commit)
    - scripts/check_patch_coverage.py (branch-aware coverage and weakening checks)
    - pyproject.toml, uv.lock, codecov.yml (must remain unchanged by this phase)
  </read_first>
  <action>
    Validate the recorded base without recomputing it. Inspect `git diff "$phase_base" -- .` so committed earlier waves plus current tracked changes are covered, and inspect every intended untracked file from `git status --short` separately because Git diff omits it. Run the focused socket/discovery set with ResourceWarning promoted to error, then the normal full frozen suite, Ruff check/format, strict Pyright, both documentation builds, `git diff --check`, the structured surface test, and the existing coverage-weakening gate. Generate branch-aware coverage JSON at `/tmp/lifx-async-coverage-11.json`; acknowledge that pytest's configured ignored `.coverage`, `coverage.xml`, and `junit.xml` may still be refreshed rather than claiming the `/tmp` report suppresses them. Derive every `--source` argument from `git diff --name-only "$phase_base" -- 'src/lifx/**/*.py'` into a Bash array and fail if no changed production Python path exists; do not use a hand-maintained source list that can miss a deviation. Keep the Phase 10 hardware probe outside global source coverage and rely on its synthetic helper tests. Require no changes to `pyproject.toml`, `uv.lock`, or `codecov.yml`, no deleted test path, and no weakening marker/skip. Keep `COVERAGE.md`'s declaration: the stable Device API is a repository contract, while this dependency-free LAN protocol work integrates no external SDK/service/API and therefore needs no capability matrix or user setup. Record the assumption-delta result as no change: the phrase `one-second` is a time interval, not a singular-to-plural identity-model change, so it creates no contribution decision. If any gate fails, diagnose and fix the implementation/test rather than accepting it.
  </action>
  <verify>
    <automated>bash -c 'set -eu; IFS= read -r phase_base &lt; .planning/phases/11-mdns-hardening/11-PHASE-BASE.txt; case "$phase_base" in ""|*[!0-9a-f]*) exit 1;; esac; test "${#phase_base}" -eq 40; git cat-file -e "${phase_base}^{commit}"; git merge-base --is-ancestor "$phase_base" HEAD; git diff --check "$phase_base" -- .; git diff --quiet "$phase_base" -- pyproject.toml uv.lock codecov.yml; test -z "$(git diff --diff-filter=D --name-only "$phase_base" -- tests)"' &amp;&amp; uv run --frozen pytest tests/test_network/test_mdns tests/test_devices/test_base.py tests/test_api/test_api_discovery.py tests/test_scripts/test_ipv6_thread_probe.py -q -W error::ResourceWarning &amp;&amp; uv run --frozen pytest -q &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright &amp;&amp; uv run zensical build --clean --strict &amp;&amp; uv run llmstxt-standalone build &amp;&amp; uv run --frozen pytest --cov=lifx --cov-branch --cov-report=json:/tmp/lifx-async-coverage-11.json -q &amp;&amp; bash -c '"'"'set -eu; IFS= read -r phase_base &lt; .planning/phases/11-mdns-hardening/11-PHASE-BASE.txt; sources=(); while IFS= read -r source; do test -n "$source" &amp;&amp; sources+=(--source "$source"); done &lt; &lt;(git diff --name-only "$phase_base" -- "src/lifx/**/*.py"); test "${#sources[@]}" -gt 0; uv run scripts/check_patch_coverage.py --base "$phase_base" --coverage /tmp/lifx-async-coverage-11.json "${sources[@]}"; uv run scripts/check_patch_coverage.py --base "$phase_base" --check-weakening-only'"'"'</automated>
    <human-check>Using the validated `phase_base`, inspect `git diff "$phase_base" -- .` and every intended untracked file from `git status --short`. Confirm every serial, MAC, address, hostname, account, mapping, packet, and discovery/probe fragment is synthetic or documentation-safe; sanitise and rerun the complete range audit on any candidate. Retain the separate staged-diff inspection before each signed implementation commit.</human-check>
  </verify>
  <acceptance_criteria>
    - All focused, full, lint, format, type, documentation, diff, structured-surface, and weakening gates pass
    - Patch coverage receives every changed production Python path derived from the phase diff and reaches the existing 100% changed-line/branch target without exemptions
    - No dependency, lock, coverage-configuration, deleted-test, skip, or weakening change appears
    - The complete tracked phase range and all intended untracked files pass privacy review; sensitive diff output is not copied into a tracked artefact
    - `COVERAGE.md` remains a reasoned no-external-API decision, not a fabricated capability matrix
    - The `one-second` detector hit is recorded as a no-change duration phrase, not an identity-model decision
  </acceptance_criteria>
  <done>Every Phase 11 source, edge, prohibition, privacy rule, and review resolution has complete green evidence over the immutable implementation range.</done>
</task>

</tasks>

<threat_model>
## Security Profile

- `asvs_level: L1` (opportunistic)
- `block_threshold: high`; any unresolved critical/high threat blocks plan completion

## Trust Boundaries

| Boundary | Description |
|---|---|
| Implemented source/docs -> public package and published documentation | A stale alias or misleading phrase can recreate an unsupported contract |
| Immutable phase diff -> repository history | Privacy, coverage, dependency, and test weakening must be caught across all waves |
| Coverage source discovery -> patch-coverage decision | Omitting a changed source path can create a false green |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-22 | Tampering | public/private surface audit | high | mitigate | Structured runtime, exact-token/phrase, and AST assertions replace broad regex heuristics (Task 1) |
| T-11-23 | Information Disclosure | complete phase diff | high | mitigate | Immutable-base tracked review plus explicit intended-untracked review and per-commit staged checks (Task 2) |
| T-11-24 | Tampering | patch coverage source set | high | mitigate | Derive every changed production Python source from the phase diff and fail closed on empty/config-weakened input (Task 2) |
| T-11-25 | Repudiation | edge/prohibition evidence | medium | mitigate | Twenty-two explicit edge truths, six prohibition dispositions, structured surface tests, and complete source audit (both tasks) |
| T-11-SC | Tampering | package supply chain | low | accept | No package install/change is planned; dependency and lockfile diffs fail the gate |
</threat_model>

## Multi-Source Coverage Audit

`11-SOURCE-AUDIT.md` records complete coverage for the ROADMAP goal, MDNS-01 through MDNS-08,
D-01 through D-14, all in-scope research constraints, 28/28 SPEC edge rows, 6/6 SPEC
prohibition rows, and every actionable REVIEWS.md finding. No item is missing. The review-proposed
address cap and factory removal are explicitly rejected because they conflict with D-05/AC12 and
the narrower named D-03 cutover respectively; the expiry CPU concern is mitigated without either
scope reduction.

<verification>
- `uv run --frozen pytest tests/test_network/test_mdns/test_phase_contract.py -q`
- `uv run --frozen pytest tests/test_network/test_mdns tests/test_devices/test_base.py tests/test_api/test_api_discovery.py tests/test_scripts/test_ipv6_thread_probe.py -q -W error::ResourceWarning`
- `uv run --frozen pytest -q`
- `uv run ruff check .`
- `uv run ruff format --check .`
- `uv run pyright`
- `uv run zensical build --clean --strict`
- `uv run llmstxt-standalone build`
- `bash -c 'set -eu; IFS= read -r phase_base &lt; .planning/phases/11-mdns-hardening/11-PHASE-BASE.txt; case "$phase_base" in ""|*[!0-9a-f]*) exit 1;; esac; test "${#phase_base}" -eq 40; git cat-file -e "${phase_base}^{commit}"; git merge-base --is-ancestor "$phase_base" HEAD; git diff --check "$phase_base" -- .; git diff --quiet "$phase_base" -- pyproject.toml uv.lock codecov.yml; test -z "$(git diff --diff-filter=D --name-only "$phase_base" -- tests)"'`
- `uv run --frozen pytest --cov=lifx --cov-branch --cov-report=json:/tmp/lifx-async-coverage-11.json -q`
- `bash -c 'set -eu; IFS= read -r phase_base &lt; .planning/phases/11-mdns-hardening/11-PHASE-BASE.txt; sources=(); while IFS= read -r source; do test -n "$source" &amp;&amp; sources+=(--source "$source"); done &lt; &lt;(git diff --name-only "$phase_base" -- "src/lifx/**/*.py"); test "${#sources[@]}" -gt 0; uv run scripts/check_patch_coverage.py --base "$phase_base" --coverage /tmp/lifx-async-coverage-11.json "${sources[@]}"'`
- `bash -c 'set -eu; IFS= read -r phase_base &lt; .planning/phases/11-mdns-hardening/11-PHASE-BASE.txt; uv run scripts/check_patch_coverage.py --base "$phase_base" --check-weakening-only'`
- Inspect `git diff --cached` before every commit, then inspect `git diff "$phase_base" -- .` and every intended untracked file from `git status --short` at the final gate; abort and sanitise any live identifier, private mapping, raw packet, or raw discovery/probe output
- API coverage decision remains `COVERAGE.md`: no external SDK, hosted service, authenticated endpoint, webhook, cloud API, account, secret, dashboard, capability matrix, or user setup applies
- Assumption delta: `one-second` is a duration phrase and does not change singular/plural identity modelling; no contribution decision is required
</verification>

<success_criteria>
Every locked behaviour, explicit edge, prohibition disposition, privacy constraint, and actionable review finding is implemented or explicitly resolved; the authorised raw API is private without aliases; the preserved factory remains public; the Device-level API stays green; the complete phase diff passes privacy review and diff-derived patch coverage; and no Phase 12-14 scope enters the result.
</success_criteria>

<output>
Create `.planning/phases/11-mdns-hardening/11-06-SUMMARY.md` when done.
</output>
