---
phase: 12-ipv6-discovery-plumbing
plan: 05
type: execute
wave: 4
depends_on:
  - 12-04
files_modified:
  - src/lifx/network/transport.py
  - tests/test_network/test_transport.py
autonomous: false
gap_closure: true
requirements:
  - FIND-06
tags: [ipv6, discovery, udp, scope-id, gap-closure]

estimate:
  tokens: 12000
  raw_tokens: 24000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Every `validate_address()`-accepted zoned IPv6 destination reaches `UdpTransport.send()` as `(host_without_zone, port, 0, resolved_scope_id)`, so AC4 is verified at the production boundary rather than only at the public transport double (D-05; FIND-06)."
    - "A numeric zone is converted directly to its integer scope ID, while a named zone is resolved with `socket.if_nametoindex()`; the zone suffix is absent from the host passed to `sendto`."
    - "An unknown named zone or an out-of-range numeric scope raises `LifxNetworkError` before `sendto` and before the targeted discovery timeout can begin; the permanent configuration failure is not reported as `None` or `LifxTimeoutError`."
    - "Unzoned IPv6 retains the canonical `(host, port, 0, 0)` shape required by Windows, and IPv4 send behaviour, family mismatch handling, endpoint ownership, discovery timing, and response validation remain unchanged."
    - "Edge R1 idempotency remains dismissed: targeted discovery and zone conversion are observational, per-call operations with no persistent mutation or response-order promise."
    - "Edge R1 concurrency remains covered by the separate real-endpoint independence, cancellation, close, and reuse tests from 12-03; this local send repair introduces no shared mutable state."
    - "Edge R2 empty/degenerate input remains covered by validation-before-transport tests, while Edge R2 representation is closed by numeric, named, unzoned, compressed, expanded, ULA, GUA, and loopback coverage at the paired public and production boundaries."
    - "Edge R2 concurrency remains delegated to the R1 targeted-discovery lifecycle contract rather than duplicated in the send conversion tests."
    - "Edge R3 repetition and concurrency resolutions remain intact: ordinary CI reruns create no product-level repeat promise, and emulator jobs retain isolated ephemeral ports, owned teardown, and no cross-runner state."
    - "The focused Phase 12 gates, full frozen suite, Ruff format/lint, Pyright, diff/privacy inspection, and current-revision Windows and Ubuntu CI evidence all pass before the gap is closed (D-01 through D-03, D-10, D-11)."
  artifacts:
    - path: "src/lifx/network/transport.py"
      provides: "Scope-preserving canonical IPv6 sockaddr construction and immediate typed zone-resolution failure in `UdpTransport.send()`."
      contains: "if_nametoindex"
    - path: "tests/test_network/test_transport.py"
      provides: "Real `UdpTransport.send()` boundary regressions for numeric, named, unzoned, and unresolvable IPv6 zones."
      contains: "TestSendFamilyAssertion"
  key_links:
    - from: "src/lifx/network/transport.py::UdpTransport.send"
      to: "asyncio.DatagramTransport.sendto"
      via: "AF_INET6 canonical sockaddr `(host_without_zone, port, 0, scope_id)` after numeric conversion or `socket.if_nametoindex()`"
      pattern: "if_nametoindex"
    - from: "tests/test_api/test_api_discovery.py::TestFindByIpAddressGate"
      to: "tests/test_network/test_transport.py::TestSendFamilyAssertion"
      via: "The public representation matrix proves the accepted literal reaches discovery; the transport tests prove the same zoned forms cross the production send conversion with scope intact."
      pattern: "zoned"
    - from: "src/lifx/network/transport.py::UdpTransport.send"
      to: "src/lifx/exceptions.py::LifxNetworkError"
      via: "Permanent zone-resolution/range failures are translated before any datagram send or discovery receive deadline."
      pattern: "LifxNetworkError"
    - from: ".github/workflows/ci.yml::Run targeted IPv6 discovery"
      to: "tests/test_api/test_ipv6_e2e.py::TestIpv6TargetedDiscovery::test_find_by_ip_over_ipv6"
      via: "The existing required Windows/Python 3.10 step supplies current-revision evidence after the gap commit."
      pattern: "Run targeted IPv6 discovery"
  prohibitions:
    - statement: "MUST NOT place live serials, IP addresses, hostnames, interface names, account identifiers, private mappings, or raw discovery output in Phase 12 tests, documentation, commits, or evidence."
      status: resolved
      verification: "Use only loopback/documentation/synthetic literals and patched synthetic zone names; inspect diffs with value-suppressed location/category reporting (SPEC AC11/P1)."
    - statement: "MUST NOT count a pre-existing Windows skip, an allowed failure, an aggregate green status, or a prior revision as the Phase 12 Windows attempt."
      status: resolved
      verification: "Inspect the existing required named step and exact test result on the gap revision; D-11 remains operator-controlled (SPEC AC9/P2)."
---

<objective>
Close the sole Phase 12 verification blocker by preserving IPv6 zone information through the real UDP send conversion, proving numeric and named scopes at `UdpTransport.send()`, and re-establishing local and current-revision cross-runner evidence.

Purpose: make FIND-06 true for the complete validator-accepted zoned-link-local contract instead of only for representations observed above the production send boundary.
Output: a scope-aware canonical IPv6 sockaddr conversion, focused boundary/error regressions, complete local gates, and current-revision CI confirmation.
</objective>

<execution_context>
@~/.codex/gsd-core/workflows/execute-plan.md
@~/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-ipv6-discovery-plumbing/12-CONTEXT.md
@.planning/phases/12-ipv6-discovery-plumbing/12-SPEC.md
@.planning/phases/12-ipv6-discovery-plumbing/12-RESEARCH.md
@.planning/phases/12-ipv6-discovery-plumbing/12-PATTERNS.md
@.planning/phases/12-ipv6-discovery-plumbing/12-VERIFICATION.md
@.planning/phases/12-ipv6-discovery-plumbing/12-REVIEWS.md
@.planning/phases/12-ipv6-discovery-plumbing/12-02-SUMMARY.md
@.planning/phases/12-ipv6-discovery-plumbing/12-04-SUMMARY.md
@.agents/skills/spike-findings-lifx-async/SKILL.md
@.agents/skills/spike-findings-lifx-async/references/discovery.md

The actionable gap is CR-01 only. CR-02, CR-03, WR-01, and WR-02 retain the exact out-of-scope/non-blocking dispositions in `12-VERIFICATION.md`; do not change `discover()`, `find_by_serial()`, `find_by_label()`, unrelated generator ownership, or the existing CI opt-in design.
</context>

## Artifacts this phase produces

- No new public API, public symbol, runtime dependency, generated file, schema, or evidence format.
- `src/lifx/network/transport.py::UdpTransport.send` — modified to produce scope-preserving canonical IPv6 socket addresses and typed immediate zone-resolution errors.
- `tests/test_network/test_transport.py::TestSendFamilyAssertion::test_numeric_zoned_ipv6_destination_uses_scope_id` — new production-boundary numeric-zone regression.
- `tests/test_network/test_transport.py::TestSendFamilyAssertion::test_named_zoned_ipv6_destination_resolves_scope_id` — new production-boundary named-zone regression.
- `tests/test_network/test_transport.py::TestSendFamilyAssertion::test_unknown_named_ipv6_zone_raises_immediately` — new fail-fast error-surface regression.
- `tests/test_network/test_transport.py::TestSendFamilyAssertion::test_out_of_range_numeric_ipv6_zone_raises_immediately` — new numeric range/error-surface regression.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry numeric and named IPv6 zones through the real send boundary</name>
  <reversibility rating="reversible">The change is confined to internal sockaddr conversion behind the unchanged `UdpTransport.send(data, tuple[str, int])` contract and can be reverted with its focused regressions.</reversibility>
  <files>src/lifx/network/transport.py, tests/test_network/test_transport.py</files>
  <read_first>
    - `src/lifx/network/transport.py`: top-level imports and `UdpTransport.send`, especially the existing AF_INET6 four-field conversion and OSError-to-`LifxNetworkError` boundary.
    - `tests/test_network/test_transport.py`: imports, `TestSendFamilyAssertion._open_against_mock_endpoint`, family mismatch tests, and `test_matching_ipv6_destination_uses_canonical_sockaddr`.
    - `src/lifx/network/address.py`: `validate_address` zoned-link-local acceptance and `family_for` handling of scoped IPv6 literals; do not create a competing address validator.
    - `src/lifx/exceptions.py`: documented `LifxNetworkError` network-level error surface.
    - `.planning/phases/12-ipv6-discovery-plumbing/12-VERIFICATION.md`: CR-01 root cause and the failed AC4 truth.
    - `.planning/phases/12-ipv6-discovery-plumbing/12-CONTEXT.md`: D-04 and D-05 real/public representation proof split.
    - `.planning/phases/12-ipv6-discovery-plumbing/12-SPEC.md`: FIND-06 AC2, AC4, AC6, AC10, and AC11.
  </read_first>
  <behavior>
    - Test 1: sending to `fe80::1%7` on an AF_INET6 transport calls `sendto(data, ("fe80::1", port, 0, 7))`; the zone is stripped from the host and no interface-name lookup occurs
    - Test 2: sending to `fe80::1%test0` with `socket.if_nametoindex("test0")` patched to return `11` calls `sendto(data, ("fe80::1", port, 0, 11))` exactly once
    - Test 3: the existing unzoned IPv6 case continues to call `sendto(data, (host, port, 0, 0))`, and the IPv4 case remains a two-field tuple
  </behavior>
  <action>
    Start RED by adding the numeric and named test methods under `TestSendFamilyAssertion`, using its real `UdpTransport.send()` implementation and mocked asyncio datagram endpoint; patch only `socket.if_nametoindex` for the synthetic named zone. Then update the AF_INET6 conversion inside `UdpTransport.send()`: split the optional final `%zone` from the already parsed IPv6 host, keep flowinfo `0`, use scope `0` when no suffix exists, convert a decimal zone directly to an integer scope ID, and resolve a non-decimal zone with `socket.if_nametoindex()`. Pass only `(host_without_zone, port, 0, resolved_scope_id)` to `self._transport.sendto()`. Keep imports at the top, preserve the public two-field `address` type/signature, retain `family_for()` as the sole family decision, and leave IPv4, endpoint state, peer-error routing, discovery timers, retries, and response validation untouched. This is the production boundary required by D-05 and AC4; do not edit generated protocol files or add a dependency.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-12-05-uv-cache uv run --frozen pytest tests/test_network/test_transport.py::TestSendFamilyAssertion -q -W error::ResourceWarning</automated>
  </verify>
  <acceptance_criteria>
    - The new numeric test observes exactly `("fe80::1", 56700, 0, 7)` at the mocked datagram endpoint, not a host containing `%7` and not scope `0`.
    - The new named test proves `socket.if_nametoindex("test0")` is called once and observes exactly `("fe80::1", 56700, 0, 11)` at `sendto`.
    - Numeric zones never call `socket.if_nametoindex`; named zones are never coerced with an ad-hoc interface table.
    - Unzoned IPv6 remains `(host, port, 0, 0)` and IPv4 remains `(host, port)`.
    - `UdpTransport.send()` retains its existing public signature and no other transport/discovery timing or validation code changes.
  </acceptance_criteria>
  <done>A validator-accepted numeric or named zoned IPv6 literal crosses the real production send conversion with its host and scope represented in the native sockaddr fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail invalid zone resolution immediately and run the complete local closure gates</name>
  <reversibility rating="reversible">Typed error translation and its tests stay inside the existing network exception contract and require no public API or persistence migration.</reversibility>
  <files>src/lifx/network/transport.py, tests/test_network/test_transport.py</files>
  <read_first>
    - `src/lifx/network/transport.py`: `UdpTransport.send` after Task 1 and its Raises documentation.
    - `tests/test_network/test_transport.py`: `TestSendFamilyAssertion`, `_FAST_FAILURE_SECONDS`, OSError translation tests, and the new Task 1 scope tests.
    - `tests/test_api/test_api_discovery.py`: `TestFindByIpAddressGate` accepted/invalid representation matrix and split-scope return regression; preserve it as the public half of the proof.
    - `tests/test_api/test_ipv6_e2e.py`: real public IPv4/IPv6 targeted tracer and lifecycle regressions.
    - `tests/test_network/test_discovery_rebroadcast.py`: invariant-bearing retransmission/timing coverage.
    - `tests/test_network/test_discovery_errors.py`: existing discovery validation/error coverage.
    - `src/lifx/exceptions.py`: `LifxNetworkError` documentation.
    - `.planning/phases/12-ipv6-discovery-plumbing/12-VERIFICATION.md`: missing fail-fast zone-resolution and focused/full/current-revision gates.
    - `AGENTS.md`: uv-only execution, top-level imports, privacy, and signed/DCO commit requirements.
  </read_first>
  <behavior>
    - Test 1: a patched `socket.if_nametoindex("missing0")` OSError becomes `LifxNetworkError` containing `zone identifier`, completes inside `_FAST_FAILURE_SECONDS`, and never calls `sendto`
    - Test 2: a decimal scope outside the unsigned 32-bit sockaddr range becomes the same typed immediate failure and never calls `sendto`
    - Test 3: zone failure leaves the already-open endpoint owned and reusable; it does not masquerade as endpoint death or a peer ICMP error
    - Test 4: public representation, real emulator, discovery invariant, complete frozen, Ruff, and Pyright gates remain green
  </behavior>
  <action>
    Start RED with the two named failure tests in `TestSendFamilyAssertion`. In `UdpTransport.send()`, validate a numeric scope before constructing the sockaddr so it fits the platform's unsigned 32-bit `scope_id`; translate an out-of-range value to `LifxNetworkError` immediately. Wrap `socket.if_nametoindex()` resolution failures as `LifxNetworkError` chained from the original `OSError`, with stable text containing `IPv6 zone identifier`; do not catch or relabel asynchronous peer errors, cancellation, or unrelated non-OSError defects. The failure must occur before `sendto`, leave the transport open, and propagate through targeted discovery instead of entering its receive/retry deadline. Update the method's Raises prose to document this existing library error type. Run the focused transport, public address-gate, IPv6 E2E, rebroadcast, and discovery-error suites, then the full frozen suite, Ruff format check, Ruff lint, and Pyright. Run `git diff --check`, inspect the staged paths, and perform privacy review using value-suppressed locations/categories only; test data may use loopback, documentation ranges, and patched synthetic zone names only. Commit implementation and tests atomically with a Conventional Commit message using `git commit -S -s`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-12-05-uv-cache uv run --frozen pytest tests/test_network/test_transport.py tests/test_api/test_api_discovery.py::TestFindByIpAddressGate tests/test_api/test_ipv6_e2e.py tests/test_network/test_discovery_rebroadcast.py tests/test_network/test_discovery_errors.py -q -W error::ResourceWarning &amp;&amp; UV_CACHE_DIR=/private/tmp/lifx-async-12-05-uv-cache uv run --frozen pytest &amp;&amp; UV_CACHE_DIR=/private/tmp/lifx-async-12-05-uv-cache uv run --frozen ruff format --check . &amp;&amp; UV_CACHE_DIR=/private/tmp/lifx-async-12-05-uv-cache uv run --frozen ruff check . &amp;&amp; UV_CACHE_DIR=/private/tmp/lifx-async-12-05-uv-cache uv run --frozen pyright &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Unknown named-zone and out-of-range numeric-zone tests raise `LifxNetworkError` within the existing fast-failure bound, contain `zone identifier`, and assert `sendto` was not called.
    - The failed zone send leaves `transport.is_open` true and does not invoke endpoint-loss handling.
    - Numeric and named success tests, existing unzoned canonical-shape test, family mismatch tests, and IPv4 tests all pass against the real `UdpTransport.send()` method.
    - `TestFindByIpAddressGate` remains the public representation/validation half of AC4; `TestSendFamilyAssertion` supplies the previously missing production-boundary half.
    - Focused Phase 12 tests, full frozen pytest, Ruff format/lint, Pyright, and `git diff --check` pass on the same final local tree.
    - The diff changes only `src/lifx/network/transport.py` and `tests/test_network/test_transport.py`; no dependency, generated file, discovery constant, public signature, schema, mDNS/merged-discovery path, or unrelated API helper changes.
    - Privacy inspection records no live identifier, interface, address, hostname, raw discovery output, or private mapping.
  </acceptance_criteria>
  <done>Zone-resolution failures use the documented typed network error immediately, and the complete local tree proves the repair without weakening any Phase 12 invariant.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Confirm current-revision Windows and Ubuntu IPv6 discovery evidence</name>
  <precondition>The existing Phase 12 source/test pull request contains the signed gap-closure commit and has triggered its existing CI matrix; publishing the commit or retriggering external CI requires the repository owner's normal PR workflow.</precondition>
  <reversibility rating="reversible">This checkpoint reads existing CI evidence and records no new compatibility decision unless the user explicitly exercises D-11.</reversibility>
  <files>.github/workflows/ci.yml, src/lifx/network/transport.py, tests/test_network/test_transport.py</files>
  <read_first>
    - `.github/workflows/ci.yml`: existing `Run targeted IPv6 discovery` Windows/Python 3.10 step and following unchanged full-suite step.
    - `tests/test_api/test_ipv6_e2e.py`: exact tracer node and locked Windows retry marker.
    - `src/lifx/network/transport.py`: final scope-aware send conversion from Tasks 1-2.
    - `tests/test_network/test_transport.py`: final numeric/named/error boundary regressions.
    - `.planning/phases/12-ipv6-discovery-plumbing/12-CONTEXT.md`: D-01 through D-03 and D-10 through D-12.
    - `.planning/phases/12-ipv6-discovery-plumbing/12-SPEC.md`: AC8, AC9, AC11, AC12, and prohibition P2.
    - `.planning/phases/12-ipv6-discovery-plumbing/12-VERIFICATION.md`: required current-revision closure evidence and WR-01's non-blocking disposition.
    - The current source/test PR check summary and exact Windows/Python 3.10 and Ubuntu/Python 3.10 job logs; inspect in place without copying runner identifiers.
  </read_first>
  <what-built>Scope-preserving numeric/named IPv6 datagram targets, immediate typed zone-resolution failure, and the complete local Phase 12 regression gate.</what-built>
  <how-to-verify>Confirm the current gap revision's Windows/Python 3.10 `Run targeted IPv6 discovery` step collected and passed the exact public tracer, then confirm Ubuntu/Python 3.10 ran the IPv6 module in the full suite and the transport boundary tests pass in the matrix rather than being skipped.</how-to-verify>
  <resume-signal>Reply `approved` after the current gap revision satisfies both named job-log checks, or provide the exact failing step and privacy-safe error excerpt for an in-scope repair or D-11 operator decision.</resume-signal>
  <action>
    Use `gh pr checks --watch --fail-fast` and inspect the exact current-revision job logs; do not substitute the earlier Plan 12-04 run, aggregate workflow status, publication, mergeability, a skip, or an allowed failure. Confirm Windows/Python 3.10 reaches the existing required focused step, collects the exact tracer, and passes under the locked pytest retry policy. Confirm Ubuntu/Python 3.10 runs the IPv6 E2E module and the transport tests in its full suite. If an in-scope scope-conversion, test, fixture, or workflow defect fails, fix it within the two-file gap scope unless the evidence proves the existing CI boundary itself is at fault, rerun all local gates, publish the replacement signed/DCO commit through the normal PR workflow, and inspect replacement jobs. If a precise external platform blocker persists, stop for the user's explicit D-11 decision; record D-12 only if the user actually drops Windows. Do not create a success attestation, JUnit artefact, exception file, raw log copy, or evidence containing runner/network identifiers.
  </action>
  <verify>
    <automated>gh pr checks --watch --fail-fast</automated>
    <human-check>The exact current-revision Windows/Python 3.10 focused-step log shows the public tracer collected and passed; the Ubuntu/Python 3.10 full-suite log shows the IPv6 E2E and transport modules executed without skip.</human-check>
  </verify>
  <acceptance_criteria>
    - CI evidence belongs to the exact gap-closure revision containing the final scope repair, not the Plan 12-04 predecessor.
    - The required Windows/Python 3.10 named step collects and passes the public tracer; the exact retry policy remains D-03-compliant.
    - Ubuntu/Python 3.10 full-suite logs show the IPv6 E2E and transport tests executed and passed rather than skipped.
    - Any in-scope replacement commit repeats local focused/full/Ruff/Pyright/privacy gates before new CI evidence is accepted.
    - No skip, allowed failure, aggregate green state, prior-revision log, or merge status substitutes for the named test results.
    - D-11 remains user-controlled and D-12 is recorded only after an explicit drop decision; no exception artefact is created otherwise.
  </acceptance_criteria>
  <done>The exact Phase 12 gap revision has honest required Windows and Ubuntu evidence, or execution is paused at the explicit D-11 operator boundary with no false closure claim.</done>
</task>

</tasks>

<threat_model>
## Security Profile

- `asvs_level: L1`
- `block_threshold: high`; every critical/high threat below must be mitigated before completion

## Trust Boundaries

| Boundary | Description |
|---|---|
| Caller IPv6 literal -> `UdpTransport.send()` | A validated zone string becomes an OS-native interface scope used for local UDP routing. |
| `socket.if_nametoindex()` -> canonical sockaddr | Host interface namespace resolution may fail permanently and must not enter the network retry/timeout path. |
| Test/CI output -> completion claim | A fake boundary, skip, stale revision, or aggregate status could be misreported as AC4/current-run evidence. |
| Local/runner network metadata -> tracked artefacts | Interface names, addresses, hostnames, and raw discovery output must remain outside commits and planning evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-05-01 | Tampering / Denial of Service | `UdpTransport.send()` IPv6 sockaddr conversion | high | mitigate | Strip the zone from the host, resolve numeric/named scopes, pass the scope in field four, and require exact numeric/named boundary tests. |
| T-12-05-02 | Denial of Service | named/numeric zone failure | high | mitigate | Translate resolution/range failures to immediate `LifxNetworkError`, assert no `sendto`, retain endpoint ownership, and enforce the fast-failure bound. |
| T-12-05-03 | Repudiation | Phase 12 closure evidence | high | mitigate | Require focused/full local gates plus exact current-revision Windows and Ubuntu named-test logs; prior/aggregate/skipped evidence cannot close the checkpoint. |
| T-12-05-04 | Information Disclosure | tests, diffs, logs, summaries | high | mitigate | Use only synthetic zone names and loopback/documentation literals; report privacy scan locations/categories without matched values or raw logs. |
| T-12-05-05 | Spoofing / Tampering | UDP discovery responses | medium | mitigate | Preserve and rerun existing source-ID, serial, packet-type, UDP-service, first-wins, and deadline invariant suites without editing their production loop. |
| T-12-05-SC | Tampering | existing Python dependency set | low | accept | No install or lock-file change occurs; every Python command uses the frozen uv environment. |
</threat_model>

<verification>
- Focused production-boundary gate: `UV_CACHE_DIR=/private/tmp/lifx-async-12-05-uv-cache uv run --frozen pytest tests/test_network/test_transport.py::TestSendFamilyAssertion -q -W error::ResourceWarning`.
- Phase regression gate: run `TestFindByIpAddressGate`, the complete IPv6 E2E module, and the unchanged discovery rebroadcast/error modules with `ResourceWarning` promoted.
- Final local gate: `UV_CACHE_DIR=/private/tmp/lifx-async-12-05-uv-cache uv run --frozen pytest`, Ruff format check, Ruff lint, Pyright, and `git diff --check` on the same tree.
- Privacy/integrity gate: inspect only the two declared changed files and staged diff using value-suppressed locations/categories; verify the gap commit with `git verify-commit` and its `Signed-off-by:` trailer without recording signer identity.
- External gate: `gh pr checks --watch --fail-fast`, followed by exact current-revision Windows/Python 3.10 focused-step and Ubuntu/Python 3.10 full-suite log inspection.
</verification>

<success_criteria>
- CR-01 is closed at the real `UdpTransport.send()` boundary for both numeric and named zones, and unknown/range failures are immediate typed network errors.
- FIND-06 and SPEC AC4 are verified end to end by the paired public representation and production sockaddr tests; all other Phase 12 acceptance criteria remain green.
- CR-02, CR-03, WR-01, and WR-02 retain their adjudicated dispositions with no scope expansion.
- Every high threat is mitigated, every required local/current-revision gate is current and non-skipped, and privacy-safe synthetic evidence is preserved.
</success_criteria>

## Source coverage audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Family-aware targeted IPv6 lookup and real cross-runner E2E evidence | 12-05 | COVERED | Repairs the only failed production send link and reruns current-revision evidence. |
| REQ | FIND-06 | `find_by_ip()` resolves a device from an IPv6 literal | 12-05 | COVERED | Zoned validator-accepted literals now retain their required scope at send; ordinary IPv4/IPv6 paths remain regression-gated. |
| RESEARCH | Family/address authority | Keep `family_for()` as the shared family rule and retain the asyncio/stdlib zero-dependency architecture | 12-05 Task 1 | COVERED | Zone-to-sockaddr conversion is local to send and does not create another public validator. |
| RESEARCH | Discovery invariants | Preserve validation, deadlines, retransmission, deduplication, cancellation, and cleanup | 12-05 Task 2 | COVERED | No discovery-loop edit; focused invariant and full suites rerun. |
| RESEARCH | Real boundary evidence | Observe actual production behaviour rather than constructor intent or a fake above the defect | 12-05 Tasks 1-2 | COVERED | `UdpTransport.send()` itself is exercised through its real method. |
| RESEARCH | No retuning/dependency | Retain WiFi-derived constants and zero runtime dependencies | 12-05 Tasks 1-2 | COVERED | No timing, retry, package, generated, or schema file changes. |
| CONTEXT | D-01 | Existing Windows/Python 3.10 source/test PR cell only | 12-05 Task 3 | COVERED | Existing matrix step is reused, not broadened. |
| CONTEXT | D-02 | Focused targeted IPv6 step before unchanged full suite | 12-05 Task 3 | COVERED | Current-revision log inspection reuses the existing required step. |
| CONTEXT | D-03 | Exact Windows retry policy | 12-05 Task 3 | COVERED | The existing tracer marker is preserved and verified. |
| CONTEXT | D-04 | Real public `find_by_ip("::1")` path and actual socket evidence | 12-05 Tasks 2-3 | COVERED | Real tracer reruns locally and in CI; no mocked discovery result. |
| CONTEXT | D-05 | All accepted representations, including zoned link-local | 12-05 Tasks 1-2 | COVERED | Numeric/named scopes are now proven at production send; the complete public matrix stays green. |
| CONTEXT | D-06 | Invalid inputs fail before transport use | 12-05 Task 2 | COVERED | Existing fail-on-use sentinel reruns unchanged. |
| CONTEXT | D-07 | Deterministic post-open cancellation | 12-05 Task 2 | COVERED | Existing event-driven lifecycle module reruns; send repair adds no shared state. |
| CONTEXT | D-08 | Endpoint close and later reuse | 12-05 Task 2 | COVERED | Existing real lifecycle test remains green. |
| CONTEXT | D-09 | Separate concurrency and cancellation tests | 12-05 Task 2 | COVERED | Existing separation remains unchanged and exercised. |
| CONTEXT | D-10 | Named current CI step is evidence | 12-05 Task 3 | COVERED | Exact gap-revision logs required; no success attestation. |
| CONTEXT | D-11 | Windows remains mandatory absent explicit operator drop | 12-05 Task 3 | COVERED | Checkpoint blocks on failure and preserves user authority. |
| CONTEXT | D-12 | Record only an exercised D-11 decision | 12-05 Task 3 | COVERED | No exception artefact is pre-created. |
| SPEC | AC1-AC3 | IPv4 family, IPv6 family/send, and real `::1` typed Device | 12-05 Tasks 2-3 | COVERED | Existing real tests rerun locally/current CI. |
| SPEC | AC4 | Complete accepted/invalid IPv6 representation boundary | 12-05 Tasks 1-2 | COVERED | Failed zoned truth is repaired at production send; public/invalid matrices remain paired evidence. |
| SPEC | AC5-AC7 | Concurrency/cancellation and isolated fixture ownership | 12-05 Task 2 | COVERED | Existing lifecycle and fixture tests rerun; no relevant production edit. |
| SPEC | AC8-AC9 | Supported-runner execution and honest Windows attempt | 12-05 Task 3 | COVERED | Exact current-revision named logs required. |
| SPEC | AC10-AC12 | No retuning, privacy, complete quality gates | 12-05 Tasks 1-3 | COVERED | Two-file change, synthetic data, frozen full/static/current-CI gates. |
| SPEC EDGE | R1 idempotency | Observational per-call operation, no repeat-order promise | 12-05 | COVERED (dismissed) | Lifted into `must_haves.truths`. |
| SPEC EDGE | R1 concurrency | Independent lookup/cancellation/reuse lifecycle | 12-05 Task 2 | COVERED | Existing separate real-endpoint tests rerun. |
| SPEC EDGE | R2 empty/degenerate | Invalid input before transport | 12-05 Task 2 | COVERED | Existing fail-on-construction sentinel reruns. |
| SPEC EDGE | R2 representation | Accepted IPv6 forms and bare/zoned boundary | 12-05 Tasks 1-2 | COVERED | Numeric/named production sockaddr tests close the missing link. |
| SPEC EDGE | R2 concurrency | Delegated to R1 lifecycle contract | 12-05 | COVERED (dismissed) | Lifted into `must_haves.truths`. |
| SPEC EDGE | R3 idempotency | CI rerun hygiene, no product repeat contract | 12-05 | COVERED (dismissed) | Lifted into `must_haves.truths`. |
| SPEC EDGE | R3 concurrency | Ephemeral fixture/teardown/no cross-runner state | 12-05 Tasks 2-3 | COVERED | Existing fixture and independent matrix jobs remain unchanged. |
| SPEC PROHIBITION | P1 | No live identifiers or raw discovery output | 12-05 Tasks 1-3 | COVERED | Synthetic zones/literals and value-suppressed inspection only. |
| SPEC PROHIBITION | P2 | No skip/allowed failure counted as Windows attempt | 12-05 Task 3 | COVERED | Exact current-revision named-step evidence is blocking. |
| VERIFICATION | CR-01 | Zoned IPv6 send loses scope | 12-05 Tasks 1-2 | COVERED | Root cause repaired and tested at `UdpTransport.send()`. |
| VERIFICATION | CR-02, CR-03, WR-01, WR-02 | Adjudicated follow-ups/non-blocking warning | NONE | EXCLUDED | Exact `12-VERIFICATION.md` dispositions retained; no merge/label/ownership/CI-redesign scope added. |
| DETECTOR | External API coverage | Local stdlib UDP sockaddr handling, not an external service API/SDK | 12-05 | EXCLUDED | Run the deterministic detector; create no capability matrix when it remains false. |
| DETECTOR | Schema push | No schema-relevant file | 12-05 | EXCLUDED | No schema task or artefact. |

No GOAL, FIND-06 requirement, applicable research constraint, D-01..D-12 decision, SPEC acceptance criterion, resolved edge, prohibition, or actionable verifier gap is missing. The four adjudicated review follow-ups remain explicitly excluded rather than silently planned.

<output>
Create `.planning/phases/12-ipv6-discovery-plumbing/12-05-SUMMARY.md` when done.
</output>
