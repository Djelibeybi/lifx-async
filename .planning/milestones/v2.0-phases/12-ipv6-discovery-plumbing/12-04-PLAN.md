---
phase: 12-ipv6-discovery-plumbing
plan: 04
type: execute
wave: 3
depends_on: [12-02, 12-03]
files_modified:
  - tests/conftest.py
  - tests/test_api/test_ipv6_e2e.py
  - .github/workflows/ci.yml
autonomous: false
requirements: [FIND-06]
tags: [ipv6, discovery, windows, ci, evidence]

estimate:
  tokens: 19000
  raw_tokens: 38000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "The existing `windows-latest`/Python 3.10 source-or-test PR matrix cell runs one conspicuous focused targeted-IPv6 test step immediately before the unchanged full-suite step (D-01, D-02; AC8, AC9)."
    - "The exact established Windows retry marker is applied to the real public tracer: `retries=2`, `delay=1`, and `condition=sys.platform.startswith(\"win32\")`; a pass after retry is a pass (D-03)."
    - "The focused Windows opt-in makes `emulator_server_ipv6` and its direct consumers (including `ipv6_light`) eligible while preserving the fixture's `ipv6_available` dependency; exact CI node selection, not a false single-consumer fixture claim, keeps the attempt narrow and leaves the general Windows emulator suite disabled (D-01; AC7, AC8)."
    - "Both new emulator-backed test classes, `TestIpv6TargetedDiscovery` and `TestIpv6TargetedDiscoveryLifecycle`, explicitly carry `@pytest.mark.emulator` and therefore inherit the repository's 120-second emulator timeout policy."
    - "The targeted step is required: it has no allowed-failure/continue-on-error path, sets the existing must-not-skip IPv6 guard, and a passing named step is the evidence record without JUnit or a committed success attestation (D-10; AC9)."
    - "The Windows requirement remains mandatory unless the user explicitly drops it after an in-scope fix attempt; a persistent precise external blocker pauses execution for that operator decision (D-11)."
    - "If and only if the D-11 escape is exercised, the executor records the operator's decision in the most convenient existing durable project record instead of inventing a dedicated exception artefact in advance (D-12)."
    - "The focused and full test gates, Ruff format/check, Pyright, and privacy inspection all complete before Phase 12 is considered implemented; the completed `12-SOURCE-AUDIT.md` remains authoritative for the already-resolved API/schema/assumption-delta detector dispositions (AC11, AC12)."
    - "Edge R3 idempotency is dismissed: ordinary CI rerun hygiene creates no new product-level repeat contract."
    - "Edge R3 concurrency is covered: the IPv6 emulator uses an ephemeral port, teardown owns cleanup, tests share no mutable cross-runner state, and the existing matrix runs independent jobs (AC7, AC8)."
  artifacts:
    - path: "tests/conftest.py"
      provides: "Narrow Windows opt-in decision and IPv6 discovery emulator eligibility fixture"
      contains: "targeted_ipv6_emulator_available"
    - path: "tests/test_api/test_ipv6_e2e.py"
      provides: "Eligibility decision tests and exact Windows retry marker on the real targeted tracer"
      contains: "TestIpv6TargetedDiscovery"
    - path: ".github/workflows/ci.yml"
      provides: "Required focused Windows/Python 3.10 targeted IPv6 discovery step before the unchanged full suite"
      contains: "Run targeted IPv6 discovery"
  key_links:
    - from: ".github/workflows/ci.yml::Run targeted IPv6 discovery"
      to: "tests/test_api/test_ipv6_e2e.py::TestIpv6TargetedDiscovery::test_find_by_ip_over_ipv6"
      via: "exact pytest node ID with Windows-only fixture opt-in and must-not-skip guard"
      pattern: "TestIpv6TargetedDiscovery::test_find_by_ip_over_ipv6"
    - from: "tests/conftest.py::targeted_ipv6_emulator_available"
      to: "tests/conftest.py::emulator_server_ipv6"
      via: "dedicated fixture dependency that leaves `emulator_available` unchanged"
      pattern: "targeted_ipv6_emulator_available"
    - from: "GitHub Actions named step"
      to: "D-10/D-11 evidence boundary"
      via: "required step conclusion and log showing the named test actually ran"
      pattern: "Run targeted IPv6 discovery"
  prohibitions:
    - statement: "MUST NOT place live serials, IP addresses, hostnames, account identifiers, or raw discovery output in Phase 12 tests, documentation, commits, or evidence."
      status: resolved
      verification: "AC11 synthetic/loopback fixtures plus value-suppressed staged-diff privacy inspection"
    - statement: "MUST NOT count a pre-existing Windows skip or allowed-failure state as the Phase 12 Windows attempt; the targeted test must actually run, or the precise external blocker and explicit D-11 operator decision must be recorded."
      status: resolved
      verification: "AC9 required named step, no failure allowance, and blocking CI evidence checkpoint"
---

<objective>
Make the real IPv6 targeted tracer runnable in the one locked Windows matrix cell, preserve the established retry policy and full suite, then stop at an honest external CI evidence gate.

Purpose: turn local family-aware correctness into required cross-runner evidence without broadening Windows emulator scope or manufacturing a CI claim locally.
Output: narrow fixture eligibility, exact retry marker, required workflow step, complete local gates, and a blocking source/test-PR CI verification checkpoint.
</objective>

<execution_context>
@~/.codex/gsd-core/workflows/execute-plan.md
@~/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ipv6-discovery-plumbing/12-CONTEXT.md
@.planning/phases/12-ipv6-discovery-plumbing/12-SPEC.md
@.planning/phases/12-ipv6-discovery-plumbing/12-RESEARCH.md
@.planning/phases/12-ipv6-discovery-plumbing/12-PATTERNS.md
@.planning/phases/12-ipv6-discovery-plumbing/12-SOURCE-AUDIT.md
@.planning/phases/12-ipv6-discovery-plumbing/12-02-SUMMARY.md
@.planning/phases/12-ipv6-discovery-plumbing/12-03-SUMMARY.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-04-SUMMARY.md
@tests/conftest.py
@tests/test_api/test_ipv6_e2e.py
@tests/test_network/test_discovery_devices.py
@tests/test_network/test_discovery_rebroadcast.py
@.github/workflows/ci.yml
@pyproject.toml
</context>

## Artifacts this phase produces

This plan produces test-only `targeted_ipv6_emulator_allowed()` and
`targeted_ipv6_emulator_available`, the `LIFX_WINDOWS_IPV6_DISCOVERY` CI opt-in, exact
Windows retry metadata on the already emulator-marked public tracer, and the required
`Run targeted IPv6 discovery` workflow step. It changes `emulator_server_ipv6`'s eligibility
dependency while retaining `ipv6_available`; because `ipv6_light` consumes that server fixture,
the exact focused CI node is the narrowness boundary. It creates no general Windows emulator
support, JUnit evidence, committed success attestation, dependency, public API symbol, or special
D-11 exception artefact.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a narrow Windows eligibility path and exact retry policy</name>
  <reversibility rating="reversible">The opt-in and retry metadata are test-runner policy, scoped to one named fixture/test, and can be removed without data migration.</reversibility>
  <files>tests/conftest.py, tests/test_api/test_ipv6_e2e.py</files>
  <read_first>
    - .planning/phases/12-ipv6-discovery-plumbing/12-CONTEXT.md (D-01 and D-03 exact Windows/retry decisions)
    - .planning/phases/12-ipv6-discovery-plumbing/12-SPEC.md (AC7-AC9, AC12 and Edge R3 concurrency)
    - tests/conftest.py (`emulator_available`, `ipv6_available`, `emulator_server_ipv6`, `--disable-emulator`)
    - tests/test_api/test_ipv6_e2e.py (`TestIpv6TargetedDiscovery`, existing probe decision tests and imports)
    - tests/test_network/test_discovery_devices.py (repository-standard `pytest.mark.flaky` spelling)
    - tests/test_network/test_discovery_rebroadcast.py (same Windows retry policy on discovery behaviour)
    - pyproject.toml (registered markers and test configuration)
  </read_first>
  <behavior>
    - Test 1: Unix remains eligible when the emulator is enabled; `--disable-emulator` remains authoritative on every platform
    - Test 2: Windows is ineligible by default and eligible only when `LIFX_WINDOWS_IPV6_DISCOVERY` is exactly `1`
    - Test 3: the general `emulator_available` fixture remains false on Windows and no unrelated emulator fixture gains the opt-in
    - Test 4: the real IPv6 targeted tracer carries the exact locked Windows-only retry marker
    - Test 5: `emulator_server_ipv6` still depends on `ipv6_available`, and both new emulator-backed classes retain their explicit emulator marker
  </behavior>
  <action>
    Extract a small pure test-only decision helper `targeted_ipv6_emulator_allowed(disable_emulator: bool, platform: str, windows_ci_opt_in: str | None) -> bool` in `tests/conftest.py`. Return false when `--disable-emulator` is set; on Windows return true only for exact opt-in value `1`; on other platforms preserve current eligibility. Add session fixture `targeted_ipv6_emulator_available` that applies this decision and the existing emulator import check. Change `emulator_server_ipv6` to depend on both `targeted_ipv6_emulator_available` and its existing `ipv6_available` fixture; do not remove or bypass the must-not-skip probe. This eligibility change necessarily reaches direct consumers such as `ipv6_light`; keep the Phase 12 Windows attempt narrow by selecting only the exact public tracer node in Task 2, not by claiming the server fixture has only one consumer. Leave `emulator_available`, every IPv4 emulator fixture, and external-emulator behaviour unchanged. Add parameterised pure-decision and fixture-dependency regressions in `tests/test_api/test_ipv6_e2e.py`. Preserve the `@pytest.mark.emulator` class decorators added by Plans 12-01 and 12-03. Import `sys` and decorate only `TestIpv6TargetedDiscovery::test_find_by_ip_over_ipv6` with the exact established marker `pytest.mark.flaky(retries=2, delay=1, condition=sys.platform.startswith("win32"))`; do not add a shell retry or mark the lifecycle/representation/full suite as flaky. This implements D-01 and D-03.
  </action>
  <verify>
    <automated>uv run pytest tests/test_api/test_ipv6_e2e.py -k 'targeted_ipv6_emulator_allowed or test_find_by_ip_over_ipv6' -q &amp;&amp; uv run pytest tests/test_network/test_discovery_devices.py tests/test_network/test_discovery_rebroadcast.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `--disable-emulator` prevents the targeted fixture on every platform
    - Windows eligibility requires exact opt-in `1`; unset, empty, `0`, and other text remain ineligible
    - Unix behaviour and the existing `LIFX_REQUIRE_IPV6` bind probe remain unchanged
    - `emulator_server_ipv6` retains `ipv6_available` as an explicit fixture dependency, so the designated must-not-skip failure path cannot be bypassed
    - General Windows emulator tests stay disabled because `emulator_available` is untouched
    - `TestIpv6TargetedDiscovery` and `TestIpv6TargetedDiscoveryLifecycle` retain explicit `@pytest.mark.emulator` markers; the lifecycle and tracer tests therefore receive the 120-second timeout policy
    - The plan records that `ipv6_light` is another direct `emulator_server_ipv6` consumer and relies on exact CI node selection for Windows narrowness
    - Only the real public targeted tracer carries `retries=2`, `delay=1`, and the exact Windows condition
    - The emulator still binds `::1` on an ephemeral port, asserts `AF_INET6`/`IPV6_V6ONLY`, and stops in fixture teardown
  </acceptance_criteria>
  <done>The designated Windows cell can run the real targeted IPv6 tracer without enabling the broader Windows emulator suite, and the retry policy exactly matches D-03.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the required focused CI step and complete local gates</name>
  <reversibility rating="costly">Changing a required branch check affects the merge gate and runner time, but the workflow edit is version-controlled and reversible.</reversibility>
  <files>.github/workflows/ci.yml, tests/conftest.py, tests/test_api/test_ipv6_e2e.py</files>
  <read_first>
    - .planning/phases/12-ipv6-discovery-plumbing/12-CONTEXT.md (D-01, D-02, D-10, D-11, D-12)
    - .planning/phases/12-ipv6-discovery-plumbing/12-SPEC.md (AC8-AC12 and both prohibitions)
    - .github/workflows/ci.yml (existing source/test matrix, dependency install, `Run unit tests`, and `LIFX_REQUIRE_IPV6` guard)
    - tests/conftest.py (`targeted_ipv6_emulator_available` from Task 1)
    - tests/test_api/test_ipv6_e2e.py (exact tracer node ID and retry marker)
    - .planning/phases/12-ipv6-discovery-plumbing/12-02-SUMMARY.md (representation and validation completion)
    - .planning/phases/12-ipv6-discovery-plumbing/12-03-SUMMARY.md (lifecycle and concurrency completion)
  </read_first>
  <action>
    In the existing `test` matrix, insert a conspicuously named `Run targeted IPv6 discovery` step after `Install dependencies` and immediately before the existing `Run unit tests` step. Gate it to exact `matrix.os == 'windows-latest'` and `matrix.python-version == '3.10'`. Run `uv run --frozen pytest tests/test_api/test_ipv6_e2e.py::TestIpv6TargetedDiscovery::test_find_by_ip_over_ipv6 -q`, set `LIFX_WINDOWS_IPV6_DISCOVERY: '1'` and `LIFX_REQUIRE_IPV6: '1'`, and give the step a bounded timeout. Do not add `continue-on-error`, a workflow retry, a separate job, a new matrix axis, or an all-Windows emulator run. Leave the following full-suite command and its existing Ubuntu/Python 3.10 must-not-skip environment exactly unchanged. Then run the focused Phase 12 tests, the complete frozen suite, Ruff format/check, and Pyright locally. Inspect staged and unpushed Phase 12 ranges with value-suppressed searches: report only file/line/category counts, never matching identifier values. Rely on the completed `12-SOURCE-AUDIT.md`, which already records API coverage as `SKIPPED (detected:false)` and excludes schema and assumption-delta work; do not rerun those planning detectors or create `COVERAGE.md` during execution. This implements D-02 and prepares D-10 evidence without claiming that a local workflow edit has already run in GitHub.
  </action>
  <verify>
    <automated>uv run pytest tests/test_api/test_ipv6_e2e.py tests/test_api/test_api_discovery.py::TestFindByIpAddressGate -q -W error::ResourceWarning &amp;&amp; uv run --frozen pytest &amp;&amp; uv run ruff format --check . &amp;&amp; uv run ruff check . &amp;&amp; uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - The focused step is inside the existing matrix, immediately precedes the unchanged full-suite step, and runs only on Windows/Python 3.10 source-or-test PRs
    - The step selects the exact real public tracer node, enables only the narrow fixture and must-not-skip guard, has a timeout, and has no failure allowance
    - The full-suite command, Ubuntu/Python 3.10 guard, matrix, dependencies, and coverage upload remain unchanged
    - Focused Phase 12 tests, full frozen suite, Ruff format/check, and Pyright pass locally
    - Privacy inspection reports only suppressed locations/categories and finds no committed live infrastructure data
    - The completed source audit's `detected:false` API disposition remains unchanged; execution creates no undeclared `COVERAGE.md`, schema, or assumption-delta artefact
  </acceptance_criteria>
  <done>The repository contains a required, narrow Windows/Python 3.10 IPv6 discovery step and all local implementation gates are green, ready for external CI evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Confirm the source/test PR actually ran the required cross-runner evidence</name>
  <precondition>A source-or-test pull request containing the completed Phase 12 implementation exists and the existing CI test matrix has been triggered; creating or publishing that PR is outside this plan's autonomous authority.</precondition>
  <reversibility rating="reversible">This checkpoint reads CI evidence and records an operator decision only if the required Windows attempt has a persistent external blocker.</reversibility>
  <files>.github/workflows/ci.yml</files>
  <what-built>The completed family-aware discovery implementation, public IPv6 tracer, representation/lifecycle tests, narrow Windows fixture opt-in, and required focused CI step from Tasks 1-2 and Plans 12-01 through 12-03.</what-built>
  <how-to-verify>Open the current source/test PR's `Test (Python 3.10 on windows-latest)` log and confirm `Run targeted IPv6 discovery` collected and passed the exact tracer node rather than skipping; also inspect the Ubuntu/Python 3.10 full-suite log for non-skipped IPv6 execution.</how-to-verify>
  <resume-signal>Reply `approved` after both current-revision logs satisfy D-10, or provide the exact failing step/log excerpt so the executor can make an in-scope fix or request the D-11 operator decision.</resume-signal>
  <read_first>
    - .planning/phases/12-ipv6-discovery-plumbing/12-CONTEXT.md (D-10 evidence, D-11 operator escape, D-12 durable decision record)
    - .planning/phases/12-ipv6-discovery-plumbing/12-SPEC.md (AC8, AC9, AC12 and prohibition P2)
    - .github/workflows/ci.yml (`Run targeted IPv6 discovery` and unchanged full-suite step)
    - The current source/test PR's check summary and Windows/Python 3.10 job log (external evidence; do not copy raw runner network data into the repository)
  </read_first>
  <action>
    Wait for the current source/test PR checks rather than treating local success, workflow syntax, publication, merge, or a previous run as evidence. Confirm the `Test (Python 3.10 on windows-latest)` job reached the named focused step, the exact pytest node was collected and executed, and the required step concluded success; a pass after the locked pytest retry remains a pass. Confirm supported Unix matrix jobs exercised the real IPv6 module through their full suites and the designated Ubuntu/Python 3.10 cell did not skip IPv6. Do not require JUnit or create a committed success attestation. If the Windows step fails, permit in-scope fixes only to the Phase 12 fixture/test/CI boundary and rerun the required job. If a precise external platform blocker persists, stop and ask the user whether to drop the Windows requirement per D-11; neither an existing skip nor allowed failure counts. Only after an explicit drop decision, record the operator's wording and blocker in the most convenient existing durable project record selected at that time, per D-12; do not create a special exception artefact in anticipation.
  </action>
  <verify>
    <automated>gh pr checks --watch --fail-fast</automated>
    <human-check>Inspect the current PR's Windows/Python 3.10 job log: the named focused step must show the exact targeted test ran and passed, not skipped or allowed to fail; also confirm the designated Ubuntu/Python 3.10 full-suite job did not skip the IPv6 module.</human-check>
  </verify>
  <acceptance_criteria>
    - Evidence belongs to the current source/test PR and current Phase 12 revision
    - Windows/Python 3.10 reaches and passes the named required focused step with the exact tracer test actually executed
    - Supported Unix jobs exercise the IPv6 end-to-end module, with Ubuntu/Python 3.10 proving the must-not-skip arm
    - No pre-existing skip, allowed failure, CI publication status, green unrelated job, or merge status is substituted for the required test result
    - Any in-scope CI fix reruns the same required evidence; any D-11 escape is explicit, user-controlled, and durably recorded only after that decision
  </acceptance_criteria>
  <done>The current source/test PR supplies honest cross-runner evidence for the real IPv6 targeted path, or the user has explicitly exercised and durably recorded D-11 after a precise external blocker.</done>
</task>

</tasks>

<threat_model>
## Security Profile

- `asvs_level: L1`
- `block_threshold: high`; unresolved critical/high threats block completion

## Trust Boundaries

| Boundary | Description |
|---|---|
| CI environment variables -> pytest fixture eligibility | A narrow opt-in must not enable unrelated Windows emulator tests |
| Repository workflow -> GitHub-hosted runner evidence | Required external execution must not be confused with local configuration or an allowed failure |
| Runner logs -> committed evidence | Logs may contain environment details and are inspected without copying raw identifiers into the repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-12 | Elevation of Privilege | Windows emulator opt-in scope | high | mitigate | Keep the exact opt-in on a dedicated fixture consumed only by `emulator_server_ipv6`; test the decision matrix (Task 1) |
| T-12-13 | Repudiation | CI success claim | high | mitigate | Require the named current-PR step to execute the exact node and stop at a blocking evidence checkpoint (Tasks 2-3) |
| T-12-14 | Denial of Service | flaky Windows UDP scheduling | medium | mitigate | Apply only the locked pytest retry marker and bounded step timeout; do not add workflow retries or broaden the suite (Tasks 1-2) |
| T-12-15 | Information Disclosure | CI and privacy evidence | high | mitigate | Keep fixtures synthetic, suppress values in inspections, and do not commit logs/raw runner output (Tasks 2-3) |
| T-12-16 | Tampering | required branch gate | high | mitigate | No `continue-on-error`, skip-as-pass, or shell retry; D-11 remains explicit operator authority (Tasks 2-3) |
| T-12-SC | Tampering | package supply chain | low | accept | No install or dependency change is introduced beyond the repository's pinned `uv sync --frozen` workflow |
</threat_model>

## Multi-Source Coverage Audit

The complete audit is in `12-SOURCE-AUDIT.md`. This plan covers FIND-06, D-01 through
D-03, D-10 through D-12, AC7-AC12, both Edge R3 outcomes, and both phase prohibitions.

<verification>
- `uv run pytest tests/test_api/test_ipv6_e2e.py tests/test_api/test_api_discovery.py::TestFindByIpAddressGate -q -W error::ResourceWarning`
- `uv run --frozen pytest`
- `uv run ruff format --check .`
- `uv run ruff check .`
- `uv run pyright`
- On the current source/test PR: `gh pr checks --watch --fail-fast`, followed by inspection of the named Windows/Python 3.10 focused-step log and designated Ubuntu/Python 3.10 full-suite log
</verification>

<success_criteria>
The designated Windows/Python 3.10 runner actually executes and passes the real public IPv6 targeted lookup under the exact retry policy, supported Unix jobs exercise the same real module, local gates are clean, and no skip or failure allowance is misreported as evidence.
</success_criteria>

<output>
Create `.planning/phases/12-ipv6-discovery-plumbing/12-04-SUMMARY.md` when done.
</output>
