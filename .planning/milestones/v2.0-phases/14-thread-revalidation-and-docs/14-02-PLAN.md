---
phase: 14-thread-revalidation-and-docs
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - scripts/measurement_support.py
  - scripts/thread_revalidation.py
  - tests/test_scripts/test_thread_revalidation.py
autonomous: true
requirements: [THREAD-01, THREAD-02, THREAD-03, THREAD-04, THREAD-05]
estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "Per D-01, D-02, D-03, D-04, D-05, D-06, D-07, and D-08, one immutable protocol manifest freezes six alternating paired discovery rounds, their 5.0-to-15.0-second jitter, 100 per-device request trials, 0.5-to-1.5-second request jitter, 60-second staleness polls, three-absence confirmation, three-hour censoring, and both request latency definitions/statistics before collection."
    - "Per D-09 through D-14, the manifest freezes the ascending 1/2/5 FPS ten-seconds-per-rate animation attempt, marks it secondary/non-gating, permits zero useful throughput, and requires restoration/readback. Per D-15 (amended 2026-09-04) closure never depends on this attempt for any class. Per D-16 (amended 2026-09-04), a restoration failure stops before the next device and requires operator-confirmed recovery, then resumes the SAME session with the affected alias re-attempted as a new appended row; evidence collected before the failure remains valid, and only an unrecoverable device stays incomplete, blocking its own class rather than the session."
    - "Per D-17 through D-20, the orchestrator owns explicit init/discovery/requests/animation/staleness/closure/validate modes, one immutable manifest, five append-only journals, external in-memory alias resolution, resumable incomplete work, and deterministic journal-only products."
    - "[Edge THREAD-01 boundary] A session cannot validate unless its manifest declares exactly the six paired rounds required by D-01 and every declared arm has a terminal outcome."
    - "[Edge THREAD-01 adjacency] Coverage uses one uniqueness key per session, source, round and stable alias, so duplicate packets cannot create extra device coverage."
    - "[Edge THREAD-01 empty] Zero-result, failed, timed-out, interrupted and incomplete discovery attempts remain explicit journal outcomes."
    - statement: "[Edge THREAD-01 ordering backstop] Aggregation is keyed by session, source, round and alias, so input row order has no semantic effect."
      verification: backstop
    - "[Edge THREAD-01 precision] Integer source observations and counts remain authoritative; rendered rounding cannot change coverage."
    - "[Edge THREAD-01 idempotency] A rerun receives a distinct session identity, while resume never overwrites prior bytes or repeats a completed round."
    - "[Edge THREAD-01 concurrency] Discovery measurements run serially in a quiesced or explicitly confounded environment and preserve interruption honestly."
  prohibitions:
    - statement: "MUST NOT place raw serials, addresses, hostnames, raw discovery output, packet content, raw exceptions, or the private alias mapping in tracked evidence or generated products."
      status: resolved
      verification: test
    - statement: "MUST NOT discard, overwrite, truncate, or silently exclude failed, empty, interrupted, censored, restoration-failed, or inconvenient observations."
      status: resolved
      verification: test
  artifacts:
    - path: scripts/measurement_support.py
      provides: "Immutable manifest, strict privacy/schema validation, append-only JSONL, exact statistics, and deterministic schedule helpers"
    - path: scripts/thread_revalidation.py
      provides: "Validated schemas and resumable state machine for one manifest, five journals, and three derived products"
    - path: tests/test_scripts/test_thread_revalidation.py
      provides: "Hermetic contract for schedules, uniqueness, append-only resume, terminal outcomes, statistics, provenance, and deterministic generation"
  key_links:
    - from: scripts/thread_revalidation.py
      to: scripts/measurement_support.py
      via: "every init, append, load, resume and generation path uses shared validation before opening output"
      pattern: "manifest|validate|append|resume|generate"
    - from: scripts/thread_revalidation.py
      to: .planning/phases/14-thread-revalidation-and-docs/14-COVERAGE-BASE.txt
      via: "manifest revision and later coverage checks bind to an exact full implementation base/current revision pair"
      pattern: "revision|protocol_version|session"
---

<objective>
Build the immutable, privacy-safe evidence contract that freezes all Phase 14 schedules and makes every terminal outcome resumable and deterministically auditable before physical modes exist.

Purpose: The hardware programme is expensive to repeat and must not be able to change protocol, omit inconvenient results, merge sessions, or leak fleet identity after observations are known.
Output: Complete manifest/journal schemas, exact schedule/statistic functions, state-transition validation, deterministic product generation, and hermetic tests.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-thread-revalidation-and-docs/14-CONTEXT.md
@.planning/phases/14-thread-revalidation-and-docs/14-RESEARCH.md
@.planning/phases/14-thread-revalidation-and-docs/14-PATTERNS.md
@.planning/phases/14-thread-revalidation-and-docs/14-SPEC.md
@.planning/phases/14-thread-revalidation-and-docs/14-REVIEWS.md
@.planning/phases/14-thread-revalidation-and-docs/14-01-SUMMARY.md

<interfaces>
Plan 14-01 supplies the request event/sink and minimal append/reload path. Extend those files rather than replacing their timestamp or ownership contract. The manifest is create-exclusive and immutable; journals are append-only sources of truth; summary, exact-six-class ledger and report are replaceable derived views generated only after all source rows validate.
</interfaces>
</context>

## Artifacts this phase produces

- `SessionManifest`-shaped validated data with protocol version, exact revision, unique session ID, alias-only inventory, confounders, unsigned 64-bit seed, generated discovery/request schedules, fixed animation schedule, staleness cadence/cap, and current constants.
- Separate `14-DISCOVERY.jsonl`, `14-REQUESTS.jsonl`, `14-ANIMATION.jsonl`, `14-STALENESS.jsonl`, and `14-CLOSURE.jsonl` path/row contracts.
- Exact uniqueness keys and terminal outcome enums for every stage, including empty, timeout, interrupted, censored, power-out-of-range, restoration-failed, and zero-throughput states.
- Deterministic `14-SUMMARY.json`, `14-CLASS-LEDGER.json`, and `14-REPORT.md` generators that never parse their own output.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the immutable manifest, privacy gate, and five journal contracts</name>
  <reversibility rating="costly">Once physical rows exist, schema or schedule changes require a new session rather than a migration or rewrite.</reversibility>
  <files>scripts/measurement_support.py, scripts/thread_revalidation.py, tests/test_scripts/test_thread_revalidation.py</files>
  <read_first>
    - .planning/phases/14-thread-revalidation-and-docs/14-CONTEXT.md (D-01 through D-20)
    - .planning/phases/14-thread-revalidation-and-docs/14-SPEC.md (AC-01 through AC-09 and AC-13 through AC-17)
    - .planning/phases/14-thread-revalidation-and-docs/14-REVIEWS.md (oversized-work, censoring, power and provenance findings)
    - scripts/measure_merged_discovery.py (validate-before-open, alias-map and JSONL precedents)
    - tests/test_scripts/test_measure_merged_discovery.py (append-prefix, privacy-before-open and deterministic-order tests)
    - scripts/measurement_support.py (Plan 14-01 request sink ownership)
  </read_first>
  <behavior>
    - `init` creates one manifest exclusively in a new empty session directory; reopening accepts only a byte-semantic exact match and rejects protocol, revision, inventory, confounder, seed, schedule or constant drift.
    - Every journal row carries session ID, protocol version, revision, source/mode, round/trial identity, stable alias when applicable, terminal outcome and confounder classification.
    - Every completed request-journal row additionally carries the Plan 14-01 `thread_connection` observation for the accepted response. A device's radio operates in either Thread or WiFi mode and cannot change without a firmware crossgrade, so this value is invariant per alias per session: it is wire-level confirmation that an alias admitted to the Thread evidence set is genuinely in Thread mode, not a runtime transport measurement.
    - Validation occurs before opening any output; every append preserves all prior bytes; resume selects only manifest-declared missing/incomplete units and never repeats completed mutation work.
    - Failed sessions remain valid historical records but cannot contribute to another session's closure or derived products.
  </behavior>
  <action>Addresses review concerns: split the oversized evidence engine, define session poisoning and make privacy the write boundary. Start RED with manifest create-or-compare, alias-map-outside-repository, forbidden-key/value, append-prefix, duplicate/cross-session and resume tests. Extend `measurement_support.py` with central schema/privacy validation and compact append/load helpers. Implement `thread_revalidation.py init` and schema-only modes for the five D-20 journals. The manifest freezes D-01 through D-20 exactly, including generated values rather than seeds alone, and records that accepted request timestamps are application-observed dequeue timestamps from Plan 14-01, and that `thread_connection` is the device's own frame-address report rather than a client assertion or an address-family inference. Accept raw identities only transiently while mapping to validated aliases; errors, reprs and rows use bounded categories only. Give every planned unit an exact uniqueness key and explicit start/terminal transition. Mark a restoration-failed session non-closing forever without deleting or rewriting its rows. Do not open sockets, load the external map, or create files on module import or a no-subcommand invocation.</action>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_scripts/test_thread_revalidation.py tests/test_scripts/test_measure_merged_discovery.py -q</automated>
    <fails_when>The suite exits non-zero; a manifest can drift; the alias map may be inside the repository; validation happens after output opens; prior bytes change; a completed unit repeats; a terminal failure disappears; cross-session rows validate; or import/default invocation performs I/O.</fails_when>
  </verify>
  <acceptance_criteria>
    - Manifest tests freeze every D-01 through D-20 field and all generated schedules before hardware execution.
    - All five journals reject missing/extra/type-invalid/privacy-unsafe fields before file creation and preserve byte prefixes on append.
    - The request-journal schema requires `thread_connection` on every completed trial and rejects a row that omits it. Validation additionally rejects a session in which one alias reports both values, because radio mode cannot change mid-session: a varying value is a correlation or attribution defect in the measurement path, not an observed network event.
    - Duplicate completed units, cross-session/revision rows, failed-session credit and private-capture ingestion all fail closed.
    - Import and default CLI execution have no network, alias-map or filesystem side effects.
  </acceptance_criteria>
  <done>The complete immutable evidence grammar exists and can resume incomplete work without changing history, touching hardware, or leaking identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement exact schedules, statistics, provenance, and deterministic products</name>
  <files>scripts/measurement_support.py, scripts/thread_revalidation.py, tests/test_scripts/test_thread_revalidation.py</files>
  <read_first>
    - .planning/phases/14-thread-revalidation-and-docs/14-CONTEXT.md (D-01 through D-16 schedule/statistic semantics)
    - .planning/phases/14-thread-revalidation-and-docs/14-SPEC.md (all 43 Edge Coverage rows and five Prohibitions)
    - scripts/thread_revalidation.py (Task 1 schemas and state transitions)
    - tests/test_scripts/test_thread_revalidation.py (Task 1 contract tests)
  </read_first>
  <behavior>
    - One local `random.Random` seeded by the manifest produces stored discovery intervals uniformly bounded 5.0 through 15.0 seconds and request intervals 0.5 through 1.5 seconds without perturbing global random state.
    - Discovery has exactly six paired rounds and alternates which public API runs first; animation is exactly one ascending 1/2/5 FPS schedule for ten seconds per rate; staleness uses absolute poll-start cadence every 60 seconds and a three-hour monotonic cap.
    - Completed request distributions independently report ordinary median, nearest-rank p95 at ceil(0.95*N), and maximum; empty distributions remain undefined and timeouts are counted separately.
    - Derived products stable-sort validated rows and regenerate byte-identically without reading Markdown or another generated output.
  </behavior>
  <action>Implement D-01 through D-16 schedule and statistic helpers in `measurement_support.py` and wire schema-only validation/generation in the orchestrator. Store generated schedules in the manifest and reject any runtime regeneration mismatch. Define provenance enums that keep synthetic/fake mechanics distinct from physical fleet evidence and keep animation integer counts descriptive only. Include exact current animation field names in the animation row contract: `packets_sent`, `total_time_ms`, `gated`, and `acks_outstanding`, plus script-owned offered/failed/interrupted/pre/post-liveness/restoration fields. There is no ACK-received or expiry-count field. Treat `acks_outstanding` only as the ambiguous instantaneous count returned by production; forbid renaming, aggregating or narrating a reduction as acknowledgement delivery because `AckGate.sweep()` also removes expiries. Implement journal-only deterministic generators and all seven THREAD-01 edge truths in frontmatter, plus row-order shuffles, empty distributions, zero-throughput and censored inputs. Do not make a physical completeness claim yet; Plan 14-06 supplies real rows.</action>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_scripts/test_thread_revalidation.py -q</automated>
    <fails_when>The suite exits non-zero; schedules fall outside locked counts/order/ranges; global random state changes; p95 uses interpolation; empty latency becomes zero; output depends on row order or generated Markdown; synthetic provenance is accepted as fleet evidence; or acks_outstanding is interpreted as an ACK/expiry count.</fails_when>
  </verify>
  <acceptance_criteria>
    - Seeded schedule tests assert exact repeatability, bounds, alternation and D-10 ordering.
    - Statistics tests cover odd/even medians, N=1, nearest-rank p95 boundaries, maximum, timeout-only and empty distributions.
    - Animation schema names only fields exposed by current production plus script-owned outcome fields, and a dedicated negative test rejects ACK-delivery narration from outstanding-count changes.
    - Two generations from shuffled equivalent journals are byte-identical and never parse a prior derived product.
  </acceptance_criteria>
  <done>The entire frozen protocol is a deterministic, privacy-safe data contract with exact schedule/statistical semantics and no opportunity to manufacture animation delivery evidence.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high-severity threat blocks plan completion until its named mitigation test passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External alias map / untrusted observations -> journal writer | Private identity and malformed values approach tracked storage. |
| Manifest and append-only journals -> validator/products | Evidence can be drifted, duplicated, omitted, relabelled or joined across sessions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Information disclosure | schema/privacy write boundary | high | mitigate | External-only alias map, recursive key/value rejection and validate-before-open tests cover journals and products. |
| T-14-05 | Tampering / Repudiation | manifest and journals | high | mitigate | Create-exclusive immutable manifest, append-prefix tests, exact uniqueness keys, explicit terminal outcomes and cross-session rejection. |
| T-14-06 | Spoofing | evidence provenance | high | mitigate | Enumerated synthetic versus physical provenance and validators reject synthetic rows as hardware closure evidence. |
| T-14-07 | Denial of service | staleness contract | medium | mitigate | Fixed absolute cadence, one target, monotonic three-hour cap and explicit censored terminal state. |
| T-14-SC | Tampering | package installs | low | accept | No package is installed and runtime dependencies remain empty. |
</threat_model>

<verification>
- Run both focused commands with injected data only.
- Confirm every journal append validates before opening and preserves all prior bytes.
- Regenerate derived products twice from shuffled equivalent rows and compare bytes.
</verification>

<success_criteria>
- D-01 through D-20 are frozen in an executable manifest/journal contract.
- Every discovery outcome and inconvenient terminal state is retained append-only.
- Exact statistics and descriptive animation fields regenerate deterministically with privacy and provenance intact.
</success_criteria>

<output>
Create `.planning/phases/14-thread-revalidation-and-docs/14-02-SUMMARY.md` when done.
</output>
