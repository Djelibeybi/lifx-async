---
phase: 14-thread-revalidation-and-docs
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - scripts/measurement_support.py
  - scripts/measure_merged_discovery.py
  - scripts/ipv6_thread_probe.py
  - tests/test_discovery_observation.py
  - tests/test_scripts/test_measure_merged_discovery.py
  - tests/test_scripts/test_ipv6_thread_probe.py
  - tests/test_scripts/test_thread_revalidation.py
autonomous: true
requirements: [THREAD-01, THREAD-02, THREAD-03, THREAD-04, THREAD-05]
estimate:
  tokens: 42500
  raw_tokens: 85000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Per D-17 and D-19, measurement_support.py becomes the single scripts-layer owner of discovery/request capture contexts, alias/privacy validation, append/load, captured state, restore commands and restore readback; neither measurement script loads a helper from tests."
    - "Per D-14 and D-16, restoration success means commands completed and a fresh class-shaped capture exactly matches the pre-mutation protocol-normalised power, colours, tile/zone layout and applicable effect fields; no visual, object-identity or tolerance comparison can pass."
    - "A captured power level outside 0 or 65535 produces a privacy-safe power_out_of_range preflight terminal outcome before SetPower, animation or restoration mutation; it is not a timeout/network result and the incomplete alias may resume in the same session only after a stable binary power capture."
    - "The Phase 13 measurement CLI and the existing IPv6 Thread probe retain their accepted behaviour and test meaning after shared-helper migration, while raw diagnostic printing remains confined to the private probe adapter and cannot enter the Phase 14 orchestrator's evidence path."
    - "The new hand-written modules are type-checked and measured by explicit focused commands, and 100% changed executable line/branch coverage is checked from the immutable 14-COVERAGE-BASE.txt without weakening global thresholds or adding exclusions."
  prohibitions: []
  artifacts:
    - path: scripts/measurement_support.py
      provides: "Canonical scripts-layer observation, privacy, journal, state capture, restoration and exact comparison helpers"
    - path: scripts/measure_merged_discovery.py
      provides: "Phase 13 measurement tool using direct shared helpers instead of anchored importlib loading from tests"
    - path: scripts/ipv6_thread_probe.py
      provides: "Existing private probe adapted to shared capture/restore helpers without CLI or evidence-semantic drift"
    - path: tests/test_discovery_observation.py
      provides: "Discovery observation tests importing the canonical scripts-layer helper"
    - path: tests/test_scripts/test_ipv6_thread_probe.py
      provides: "Exact class-shaped restore/readback and intermediate-power safety tests"
  key_links:
    - from: scripts/measure_merged_discovery.py
      to: scripts/measurement_support.py
      via: "direct top-of-file imports replace the test-module importlib bridge"
      pattern: "capture_discovery|load_alias|append"
    - from: scripts/ipv6_thread_probe.py
      to: scripts/measurement_support.py
      via: "private probe adapter delegates state capture/restoration/comparison but owns any terminal-only private diagnostic"
      pattern: "CapturedState|restore|readback"
---

<objective>
Consolidate the proven Phase 13/IPv6 probe helpers behind the Phase 14 evidence contract, fix restoration verification, and prove the refactor with explicit type and 100% patch-coverage gates.

Purpose: The review identified both a fragile import-from-tests pattern and an existing restore helper that reports success without proving the state was applied.
Output: One canonical support module, behaviour-preserving migrations, exact restoration/readback semantics, intermediate-power safety, and bounded coverage evidence.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/14-thread-revalidation-and-docs/14-CONTEXT.md
@.planning/phases/14-thread-revalidation-and-docs/14-RESEARCH.md
@.planning/phases/14-thread-revalidation-and-docs/14-PATTERNS.md
@.planning/phases/14-thread-revalidation-and-docs/14-SPEC.md
@.planning/phases/14-thread-revalidation-and-docs/14-REVIEWS.md
@.planning/phases/14-thread-revalidation-and-docs/14-02-SUMMARY.md

<interfaces>
`scripts/measure_merged_discovery.py` currently imports its discovery observation helper from `tests/test_discovery_observation.py` with anchored importlib. `scripts/ipv6_thread_probe.py` owns `CapturedState`, `_capture_device_state()` and `_restore_device_state()`, but restoration returns true after commands without readback. Preserve both CLIs while moving shared value-only mechanics into the Plan 14-02 support module.
</interfaces>
</context>

## Artifacts this phase produces

- Canonical discovery observation event/sink/capture symbols beside the Plan 14-01 request observation symbols in `scripts/measurement_support.py`.
- Shared `CapturedState`, capture, restore-command, recapture and exact protocol-value comparison helpers, returning separate command/readback outcomes.
- `power_out_of_range` preflight outcome and resume rule for intermediate captured power.
- Direct imports in both historical scripts, with their public CLI/output contracts unchanged.
- Explicit Pyright and coverage.py commands plus `scripts/check_patch_coverage.py` enforcement; no global coverage target or threshold weakening.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move discovery observation and privacy helpers out of tests without Phase 13 drift</name>
  <files>scripts/measurement_support.py, scripts/measure_merged_discovery.py, tests/test_discovery_observation.py, tests/test_scripts/test_measure_merged_discovery.py</files>
  <read_first>
    - scripts/measure_merged_discovery.py (`_load_discovery_observation_helper`, alias/privacy and append/load helpers)
    - tests/test_discovery_observation.py (current event, sink and capture implementation)
    - tests/test_scripts/test_measure_merged_discovery.py (existing CLI/privacy/determinism contract)
    - scripts/measurement_support.py (Plan 14-02 canonical support API)
  </read_first>
  <behavior>
    - Discovery event/sink/capture tests retain caller isolation, repr suppression, arrival order and cleanup.
    - The measurement script imports canonical helpers directly at the top and no longer uses importlib, sys.modules mutation, or a tests path.
    - Existing Phase 13 measurement rows, validation, deterministic summary and CLI options remain unchanged.
  </behavior>
  <action>Addresses review concern: observation-sink ownership and historical-tool migration blast radius. Move the discovery observation dataclass, sink and capture context into `measurement_support.py` beside the request equivalents, retaining the established caller-task callback mechanism and value-suppressed representation. Update `measure_merged_discovery.py` and `tests/test_discovery_observation.py` to import the canonical helper directly at the top. Consolidate only alias/privacy and JSONL helpers whose behaviour is already identical; preserve Phase 13 schema, flags, output ordering, messages and emulator/fleet provenance. Do not change production discovery, public APIs or historical JSONL interpretation.</action>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_discovery_observation.py tests/test_scripts/test_measure_merged_discovery.py -q</automated>
    <fails_when>The suite exits non-zero; the measurement script imports from tests or uses importlib for the helper; caller observations cross tasks; repr exposes values; prior Phase 13 rows/CLI change meaning; or privacy validation moves after file creation.</fails_when>
  </verify>
  <acceptance_criteria>
    - No production or scripts file imports a helper from `tests/`.
    - Existing Phase 13 measurement tests pass without fixture or expected-row weakening.
    - Discovery and request sinks have one scripts-layer ownership model and deterministic `finally` cleanup.
  </acceptance_criteria>
  <done>The fragile test-owned observation seam is retired, with Phase 13 behaviour and evidence intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require exact class-shaped restoration readback and fail before intermediate-power mutation</name>
  <files>scripts/measurement_support.py, scripts/ipv6_thread_probe.py, tests/test_scripts/test_ipv6_thread_probe.py, tests/test_scripts/test_thread_revalidation.py</files>
  <read_first>
    - .planning/phases/14-thread-revalidation-and-docs/14-CONTEXT.md (D-05, D-14, D-16)
    - .planning/phases/14-thread-revalidation-and-docs/14-REVIEWS.md (intermediate power and restore comparison findings)
    - scripts/ipv6_thread_probe.py (`CapturedState`, `_capture_device_state()`, `_restore_device_state()` and `stage_target()`)
    - src/lifx/devices/base.py (`get_power()` and binary `set_power()` validation)
    - src/lifx/devices/light.py (Light-specific binary `set_power()` validation)
    - tests/test_scripts/test_ipv6_thread_probe.py (class-shaped and BaseException restoration tests)
  </read_first>
  <behavior>
    - Restore commands and readback verification are separate recorded outcomes; success requires both.
    - Exact comparison covers protocol-normalised integer power, HSBK values, tile/zone ordering and every writable effect field captured by the class shape.
    - A non-binary captured power is recorded as power_out_of_range before mutation; request/animation work for that alias stays incomplete and may resume only after a new stable binary capture in the same healthy session.
    - Ordinary exception, cancellation, KeyboardInterrupt and successful command with mismatched readback all produce honest outcomes and deterministic cleanup.
  </behavior>
  <action>Addresses review concerns: missing restoration policy, intermediate SetPower failure and raw probe diagnostics. Move `CapturedState` plus genuinely shared capture/restore mechanics into `measurement_support.py`. Define comparison as exact equality of protocol-normalised values for power, colour, every tile/zone in order and every captured writable effect field; use no tolerance, visual judgement or object identity. Refactor restoration into command execution followed by a fresh `_capture_device_state()` and comparison, returning bounded command/readback mismatch categories without device identity or raw exception text. Keep the legacy probe's private terminal diagnostic in its adapter only; the shared helper and Phase 14 orchestrator never print or persist it. Before request or animation mutation, require captured power in `{0, 65535}`. For an intermediate value, append `power_out_of_range`, perform no SetPower/frame/restore command, leave the unit incomplete, and permit later same-session resume only after a new capture is binary; never classify it as timeout, retransmission or D-16 restoration failure. Preserve D-16 for an actual post-mutation restoration command/readback failure.</action>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_scripts/test_ipv6_thread_probe.py tests/test_scripts/test_thread_revalidation.py -q</automated>
    <fails_when>The suite exits non-zero; command success alone reports restored; any captured class field is omitted or compared approximately; intermediate power reaches SetPower/Animator/restore; it is counted as a network outcome; a mismatch allows the next target; or shared errors/reprs expose identity.</fails_when>
  </verify>
  <acceptance_criteria>
    - Light, MultiZoneLight and Matrix/Ceiling-shaped fakes prove exact restore write ordering and exact post-write recapture comparison.
    - SET-success/readback-mismatch, ordinary failure, cancellation and KeyboardInterrupt all record separate command/readback outcomes and stop safely.
    - Intermediate power performs zero mutation, remains visible as an incomplete preflight outcome and can resume only after stable binary recapture.
    - Existing probe tests remain green without changing its accepted CLI or private-diagnostic boundary.
  </acceptance_criteria>
  <done>Restoration is evidence-backed rather than acknowledgement-backed, and transient intermediate power cannot corrupt RTT data or trigger an impossible restore.</done>
</task>

<task type="auto">
  <name>Task 3: Prove explicit type and 100 percent changed-executable coverage without global gate drift</name>
  <files>scripts/measurement_support.py, scripts/thread_revalidation.py, scripts/measure_merged_discovery.py, scripts/ipv6_thread_probe.py</files>
  <read_first>
    - .planning/phases/14-thread-revalidation-and-docs/14-COVERAGE-BASE.txt (fixed full SHA)
    - scripts/check_patch_coverage.py (coverage JSON and anti-weakening CLI)
    - pyproject.toml (current Python floor, Pyright and coverage configuration; read-only)
    - .planning/phases/14-thread-revalidation-and-docs/14-REVIEWS.md (100% patch-coverage estimate concern)
  </read_first>
  <action>Address the review's coverage/estimate concern explicitly instead of silently adding two large hardware CLIs to the global default denominator. Run Pyright directly over all four hand-written script modules. Produce branch-aware JSON coverage with explicit `--cov=measurement_support`, `--cov=thread_revalidation`, `--cov=measure_merged_discovery`, `--cov=ipv6_thread_probe`, and `--cov=lifx.network.connection` targets while executing their focused tests. Read the full SHA from `14-COVERAGE-BASE.txt` and run `scripts/check_patch_coverage.py` against every changed executable source; also run its `--check-weakening-only` mode. Add tests until the gate reaches 100% changed lines and branches. Do not add skips, coverage pragmas, exclusions, threshold reductions or a `pyproject.toml` coverage-target change as a pressure valve.</action>
  <verify>
    <automated>uv run --frozen pyright scripts/measurement_support.py scripts/thread_revalidation.py scripts/measure_merged_discovery.py scripts/ipv6_thread_probe.py &amp;&amp; uv run --frozen pytest -o addopts='' --cov=lifx.network.connection --cov=measurement_support --cov=thread_revalidation --cov=measure_merged_discovery --cov=ipv6_thread_probe --cov-branch --cov-report=json:/private/tmp/lifx-phase14-coverage.json tests/test_network/test_connection_retry.py tests/test_discovery_observation.py tests/test_scripts/test_measure_merged_discovery.py tests/test_scripts/test_ipv6_thread_probe.py tests/test_scripts/test_thread_revalidation.py -q &amp;&amp; uv run --frozen scripts/check_patch_coverage.py --base "$(tr -d '\n' &lt; .planning/phases/14-thread-revalidation-and-docs/14-COVERAGE-BASE.txt)" --coverage /private/tmp/lifx-phase14-coverage.json --source src/lifx/network/connection.py --source scripts/measurement_support.py --source scripts/thread_revalidation.py --source scripts/measure_merged_discovery.py --source scripts/ipv6_thread_probe.py &amp;&amp; uv run --frozen scripts/check_patch_coverage.py --base "$(tr -d '\n' &lt; .planning/phases/14-thread-revalidation-and-docs/14-COVERAGE-BASE.txt)" --check-weakening-only</automated>
    <fails_when>Any command exits non-zero; a script has a type error; the base is malformed; a changed executable line/branch is missing or excluded; a skip/pragma/exclusion/threshold change weakens coverage; or a different revision is measured.</fails_when>
  </verify>
  <acceptance_criteria>
    - All hand-written measurement modules pass explicit Python 3.10 Pyright checking.
    - The fixed-base checker reports full changed-line and changed-branch coverage for every changed executable source.
    - Anti-weakening mode reports no new skip, coverage pragma, exclusion or protected-configuration weakening.
    - `pyproject.toml`, dependencies and Python floor remain unchanged.
  </acceptance_criteria>
  <done>The helper migration and restoration correction are fully type-checked and patch-covered without hiding hardware-only branches or weakening repository policy.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high-severity threat blocks plan completion until its named mitigation test passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Historical private probe -> shared evidence helpers | Terminal-only private diagnostics must not become serialisable Phase 14 values. |
| Captured device state -> restore commands -> recaptured state | Acknowledged writes may not equal applied physical state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-08 | Information disclosure | migrated observation and restore helpers | high | mitigate | Shared helpers return bounded categories only; raw private printing stays in the legacy adapter; leak/repr tests run before output. |
| T-14-09 | Tampering / Repudiation | restoration result | high | mitigate | Separate command and exact class-shaped recapture comparison outcomes; a mismatch stops before the next device and requires operator-confirmed recovery before the affected alias is re-attempted, per D-16 (amended 2026-09-04: recovery resumes the same session rather than restarting it). |
| T-14-10 | Denial of service | intermediate power | medium | mitigate | Fail before mutation, record an explicit preflight outcome and allow bounded resume after state stabilises. |
| T-14-SC | Tampering | package installs | low | accept | No package or lockfile change is made. |
</threat_model>

<verification>
- Run all three task commands without hardware.
- Confirm direct imports replace the test-module loader and existing Phase 13/probe tests retain their expectations.
- Confirm exact readback tests cover every class shape and every abnormal exit.
</verification>

<success_criteria>
- Shared helpers have one scripts-layer owner and historical tools do not drift.
- Restore success requires exact post-command readback.
- Intermediate power fails before mutation with a truthful non-network outcome.
- Explicit type, coverage and anti-weakening gates pass from one fixed base.
</success_criteria>

<output>
Create `.planning/phases/14-thread-revalidation-and-docs/14-03-SUMMARY.md` when done.
</output>
