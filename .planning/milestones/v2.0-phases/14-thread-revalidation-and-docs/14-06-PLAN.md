---
phase: 14-thread-revalidation-and-docs
plan: 06
type: execute
wave: 5
depends_on: [14-04]
files_modified:
  - .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-MANIFEST.json
  - .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-DISCOVERY.jsonl
  - .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-REQUESTS.jsonl
  - .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-STALENESS.jsonl
  - .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-ANIMATION.jsonl
  - .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-CLOSURE.jsonl
  - .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-SUMMARY.json
  - .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-CLASS-LEDGER.json
  - .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-REPORT.md
autonomous: false
requirements: [THREAD-01, THREAD-02, THREAD-03, THREAD-04, THREAD-05]
user_setup:
  - service: "Private Thread fleet and external alias map"
    why: "SEED-001 requires fresh measurements from selected physical devices; identity mapping and physical disconnect/recovery cannot be delegated or committed. Correction (amendment): the built CLI (scripts/thread_revalidation.py, Plan 14-04) has no LIFX_PHASE14_EXPECTED_ROSTER consumer and no --expected-roster flag anywhere. The operator-approved expected alias/class roster is a privacy-safe JSON file (aliases and classes only, never a raw serial) passed as `init --inventory <path>` and frozen into the immutable manifest at init time. It is deliberately a SEPARATE file from the alias map: the alias map holds raw serials and must never be committed, while this roster is frozen verbatim into the committed 14-MANIFEST.json, so the two must never be merged."
    env_vars:
      - name: LIFX_PHASE14_ALIAS_MAP
        source: "Operator-controlled absolute path outside this repository to a JSON object mapping raw serial -> privacy-safe alias, consumed by the discover/request/animation/staleness subcommands' --alias-map flag"
      - name: LIFX_PHASE14_SESSION_DIR
        source: "Absolute path to .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE"
    inventory_schema: >-
      `init --inventory` names a JSON file whose top-level value is a list. Each entry is exactly {"alias": str, "device_class": str, "available": bool}.
      alias must match [A-Za-z][A-Za-z0-9-]{0,63} and must NOT be serial-shaped (a raw MAC or 12-hex-digit
      string is rejected outright). device_class must be one of Light, MultiZoneLight, MatrixLight,
      CeilingLight (InfraredLight/HevLight are never inventory entries -- they can only ever close as a
      named_gap, never a roster omission mistaken for one). The roster must name at least one Light,
      MultiZoneLight and CeilingLight alias, plus at least two DISTINCT MatrixLight aliases -- `init`
      refuses to write a manifest at all and emits {"command":"init","ok":false,"reason":"incomplete_expected_roster","detail":"..."}
      when this is violated, so roster completeness is a real, machine-enforced gate at init time.
estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "THREAD-05 adjacency: each available device remains separate even when aliases share one exact public class."
    - "THREAD-05 empty: every exact public class has one non-empty valid closure disposition."
    - "THREAD-05 encoding: exact class names and stable operator-controlled aliases appear while raw identifiers are absent."
    - statement: "THREAD-05 ordering is a backstop: class-ledger row order has no contract meaning because closure is keyed by exact class name."
      verification: backstop
    - "THREAD-05 idempotency: availability changes and reruns create explicit new immutable evidence rather than replacing a prior disposition."
    - "THREAD-05 concurrency: the inventory is frozen per session and every later availability change is an explicit confounder/incomplete outcome."
    - "THREAD-05 roster authority: collection cannot begin until the operator approves and init freezes an expected alias/class roster independent of initial discovery; it includes Light, MultiZoneLight, CeilingLight and both distinct MatrixLight devices."
    - "THREAD-05 gap legality: named gaps are permitted only for InfraredLight and HevLight; every missing expected alias remains incomplete regardless of what initial discovery observed."
  prohibitions:
    - statement: "MUST NOT present interference-confounded observations as authoritative benchmarks, universal Thread limits, regression gates, or sufficient grounds for tuning constants."
      status: resolved
      verification: judgment
  artifacts:
    - path: .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-MANIFEST.json
      provides: "Immutable physical session identity, revision, operator-approved expected alias/class roster, confounders and frozen schedules"
    - path: .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-DISCOVERY.jsonl
      provides: "Six paired physical merged/mDNS discovery rounds"
    - path: .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-REQUESTS.jsonl
      provides: "Per-available-device 100-trial request/retransmission evidence"
    - path: .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-STALENESS.jsonl
      provides: "One selected-device advertisement-staleness experiment with restoration"
    - path: .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-ANIMATION.jsonl
      provides: "One pre-existing, non-gating Candle animation observation, cited as a scope-boundary data point (THREAD-03); not required or extended for closure"
    - path: .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-CLOSURE.jsonl
      provides: "Per-device and exact-six-class closure dispositions"
    - path: .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-SUMMARY.json
      provides: "Deterministic machine-readable result summary"
    - path: .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-CLASS-LEDGER.json
      provides: "Deterministic exact-six-class evidence/gap ledger"
    - path: .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-REPORT.md
      provides: "Human-readable qualified report generated from validated journals"
  key_links:
    - from: .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-MANIFEST.json
      to: .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-CLOSURE.jsonl
      via: "session_id/revision/inventory (the frozen roster) keys and exact per-expected-device completeness, derived by derive_class_ledger_from_roster() at generate/validate-staged time"
      pattern: "session_id|revision|inventory"
    - from: .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-CLOSURE.jsonl
      to: .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-CLASS-LEDGER.json
      via: "deterministic journal-only generation after privacy/provenance/restoration validation"
      pattern: "Light|MultiZoneLight|MatrixLight|CeilingLight|InfraredLight|HevLight"
---

<objective>
Collect the fresh, privacy-safe SEED-001 physical evidence and close each public class without allowing hardware availability or outcomes to distort the contract.

Purpose: The phase goal requires measured current-fleet evidence, but the run must remain operator-gated, non-CI, restoration-safe and incapable of silently closing on censorship, synthetic data or missing hardware.
Output: One immutable physical session, five append-only journals and three deterministic, validated evidence products.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-thread-revalidation-and-docs/14-CONTEXT.md
@.planning/phases/14-thread-revalidation-and-docs/14-SPEC.md
@.planning/phases/14-thread-revalidation-and-docs/14-REVIEWS.md
@.planning/phases/14-thread-revalidation-and-docs/14-04-SUMMARY.md

<interfaces>
All commands use `scripts/thread_revalidation.py` from Plan 14-04, invoked as `uv run --frozen python -m scripts.thread_revalidation <subcommand>` from the repository root. The module form is required: the script imports `scripts.measurement_support`, which resolves only when the repository root is on `sys.path`, and running the file directly puts `scripts/` there instead.

**Amendment correction (verified against `--help` on every subcommand and empirical scratch-session runs):** the shipped parser's exact subcommand surface is `init`, `validate`, `generate`, `validate-staged`, `discover`, `request`, `animation`, `staleness`. There is no `--expected-roster`, `--through-stage`, `--allow-incomplete-animation`, `--require-complete`, `--check-tracked-paths` flag anywhere, and `validate` has no `--alias-map` flag. Confirm the live surface with `uv run --frozen python -m scripts.thread_revalidation <subcommand> --help` before relying on any flag below:

- `init --session-dir --session-id --seed [--protocol-version] [--revision] [--inventory] [--confound ...]` -- creates or idempotently re-verifies the immutable manifest. The operator-approved expected alias/class roster is the JSON file named by `--inventory` (see `user_setup.inventory_schema`). An unreadable or malformed roster file is refused as `{"command":"init","ok":false,"reason":"unreadable_inventory"|"malformed_inventory"}` and writes no manifest. `--revision` defaults to `git rev-parse HEAD` when omitted.
- `validate --session-dir` -- schema-validates all five journals plus the manifest and regenerates `14-SUMMARY.json`/`14-CLASS-LEDGER.json`/`14-REPORT.md` from whatever rows currently exist, INCLUDING a completely empty session.
- `generate --session-dir` -- the real completeness gate. Derives the six-class ledger from the frozen roster and journals via `derive_class_ledger_from_roster()` and writes nothing at all unless every class closes (atomic: all three products or none).
- `validate-staged --evidence-dir <repo-relative-path>` -- inspects the exact nine evidence blobs already in Git's STAGED INDEX (`git show :<path>`, never the working tree) from the CURRENT directory. `--evidence-dir` must be the repository-root-relative evidence directory (e.g. `.planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE`), because it is matched against `git diff --cached --name-only` output, which Git reports relative to the repository root -- NOT the absolute `$LIFX_PHASE14_SESSION_DIR`.
- `discover --session-dir --alias-map [--confound ...]` -- runs ALL SIX paired, order-alternated rounds (12 discover()/discover_mdns() calls total) in one invocation with the manifest's frozen inter-round jitter; resumable (an already-recorded round/source pair is skipped on re-run).
- `request --session-dir --alias-map (--alias <one-alias> | --all) [--confound ...]` -- runs the full 100-trial no-op SetPower series. `--alias` and `--all` are mutually exclusive and exactly one is required. `--alias` targets exactly one alias per invocation (resumable per trial); if the device's power reads non-binary at invocation start, ALL remaining trials for that call are immediately recorded as `power_out_of_range` with no further hardware call -- see Task 1's operator-judgment note below. `--all` runs the same series for every alias in the frozen manifest inventory, sorted, in ONE invocation: since a no-op `SetPower` trial harms no device, a `power_out_of_range` stop on one alias never stops the rest -- iteration continues through the whole roster, and the single printed JSON verdict reports `ok: false` with a per-alias `results` breakdown and an `offending_aliases` list naming exactly which aliases need a genuine re-collection.
- `animation --session-dir --alias-map (--alias <one-alias> | --all) [--confound ...]` -- runs the frozen ascending 1/2/5 FPS observation, using `Animator.for_light`/`for_multizone`/`for_matrix` as appropriate (covers all four available classes uniformly). `--alias` and `--all` are mutually exclusive and exactly one is required. `--alias` targets exactly one alias per invocation. `--all` runs the observation for every alias in the frozen manifest inventory, sorted, in ONE invocation, but HALTS IMMEDIATELY -- never touching another device -- the instant one alias's attempt does not confirm BOTH `restored: true` and `restoration_verified: true` (including when the attempt itself errors before that can be confirmed). The printed JSON verdict names the `poisoned_alias` and lists every un-attempted alias in `not_attempted`; a halt (`ok: false`) means that alias needs operator-confirmed recovery per D-16 (amended 2026-09-04: recovery lets the same session resume, it does not require restarting the whole physical protocol). **This subcommand is not part of Task 2's mandatory flow** -- THREAD-03 is a recorded scope boundary (see 14-CONTEXT.md's amendment), and closure never depends on an animation attempt. It remains shipped and tested for future WiFi validation work (SEED-003).
- `staleness --session-dir --alias-map --alias <one-alias> (--disconnect-ns <int> | --power-off <path> --power-on <path>) [--confound ...]` -- runs the ENTIRE absolute-cadence staleness experiment (up to the real three-hour cap) synchronously in ONE blocking invocation for exactly one alias; not incrementally resumable mid-run (an already-recorded alias is a same-alias no-op that never re-runs a power script). **Post-14-06-hardening correction:** `--disconnect-ns` and the `--power-off`/`--power-on` pair are mutually exclusive and exactly one form is required, enforced by argparse before any I/O. `--power-off`/`--power-on` name operator-supplied executables the CLI itself runs directly (never through a shell, as a literal one-element argv list): the disconnect instant is captured immediately after `--power-off` exits 0, and `--power-on` is attempted once the absence-detection protocol closes. A non-zero exit, a timeout, or a missing/non-executable script is a hard stop with an explicit JSON verdict (`reason: "power_off_failed"` starts nothing and mutates nothing; `reason: "power_on_failed"` still persists the already-closed disposition and says unmistakably that the device is physically dark and needs the operator). Restoration polling after a successful power-on has NO deadline -- it polls both discovery legs until both report present, or the operator's Ctrl-C interrupts it -- and records how long that took as `restoration_duration_s` in the persisted row and the JSON verdict. Live progress (power-off/on results, each restoration poll, and an approaching-cap warning during expiry detection) is written to stderr throughout; stdout still carries exactly the one JSON verdict object.

There is no `closure` CLI subcommand. `append_closure_event()`/`build_closure_event()` are shipped Python functions in `scripts/thread_revalidation.py` with no CLI wiring; Task 2 invokes them directly via `uv run --frozen python -` (stdin script, which resolves `scripts.*` imports from the repository-root cwd exactly like `-m`) to append the InfraredLight/HevLight named-gap rows -- this calls only already-shipped code and adds no new flag or subcommand.

Every subcommand prints EXACTLY ONE JSON verdict object to stdout: `{"command", "ok", "reason", ...command-specific fields}` (never raw identity). Treat this JSON as the source of truth, per the project's "don't rely on `$?` parsing" directive -- exit codes remain a secondary signal. **The single most important correction in this amendment:** `validate`'s `ok: true` means only "every row present is individually schema-valid" -- it is `true` even for a completely empty, freshly-initialised session (verified empirically). `ok` alone NEVER proves collection is complete; every gate below asserts on `validate`'s `counts`/`class_ledger.missing_classes` fields or on `generate`'s `missing_classes`/`classes` fields, never on `ok` alone. `generate` is the only subcommand whose `ok: true` means "this session's six-class evidence is actually complete."

The alias map and the roster (baked into the manifest at init) stay outside version control except for the frozen, privacy-safe manifest itself; raw identities are resolved only in memory. Initial discovery is observation evidence, not inventory authority: an alias appearing in even one physical discovery round is sufficient per-alias discovery evidence for `derive_class_ledger_from_roster()`, but an alias named in the roster that ROSTER-DRIFT (an unexpected alias) or complete absence across the session still blocks that class's closure at `generate` time. Censorship, a missing expected alias and restoration failure are blocking non-closure outcomes, not inconvenient rows to reinterpret.
</interfaces>
</context>

## Artifacts this phase produces

- One immutable `14-MANIFEST.json` bound to the exact implementation revision, protocol version, operator-approved expected alias/class roster (the manifest's `inventory` field), confounders and all generated schedules.
- Five append-only JSONL journals for discovery, requests, staleness, animation and closure. The first four carry `provenance: "physical"` on every row; closure carries physical-provenance evidence only implicitly (it holds exactly two `named_gap` rows for InfraredLight/HevLight, whose schema requires `provenance: null` -- the other four classes close through the roster-derived ledger, not a closure row at all).
- One deterministic JSON summary, exact-six-class JSON ledger and qualified Markdown report.
- No raw capture, alias map, serial, address, hostname, packet body or exception text.

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Run the immutable physical discovery, request and staleness protocol</name>
  <precondition>`LIFX_PHASE14_ALIAS_MAP` names an operator-controlled JSON file outside the repository (raw serial -> alias); the operator has authored a complete roster JSON file (Light, MultiZoneLight, CeilingLight and two distinct MatrixLight aliases, at minimum) outside the repository, ready to pass to `init --inventory <path>`; and, for exactly one selected staleness target, the operator has EITHER prepared a pair of executable power-off/power-on scripts (a smart-plug/relay driver or equivalent) that the CLI itself will run via `staleness --power-off`/`--power-on`, OR can disconnect/reconnect that target manually to supply `--disconnect-ns` instead.</precondition>
  <reversibility rating="costly">A censored staleness result preserves the session but cannot be repaired in place; closure needs an explicit decision. A restoration failure (D-16, amended 2026-09-04) preserves the session and all evidence collected before the failure, halts on the affected alias pending operator-confirmed recovery, and then lets the SAME session resume with that alias re-attempted -- it no longer requires a wholly new session.</reversibility>
  <files>.planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-MANIFEST.json, .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-DISCOVERY.jsonl, .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-REQUESTS.jsonl, .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-STALENESS.jsonl</files>
  <read_first>
    - AGENTS.md (Privacy and Hardware Identifiers)
    - .planning/phases/14-thread-revalidation-and-docs/14-CONTEXT.md (D-01 through D-08, D-16 through D-20)
    - .planning/phases/14-thread-revalidation-and-docs/14-04-SUMMARY.md (frozen CLI and censored escalation)
    - scripts/thread_revalidation.py (`init`, `discover`, `request`, `staleness`, `validate` `--help` -- confirm the live flag surface before using any command below)
  </read_first>
  <action>Only the operator can authorise the external alias mapping, approve the inventory roster and perform the physical disconnect/reconnect. Before any collection, display only the candidate roster's privacy-safe aliases and exact classes and obtain operator confirmation it contains every known available Thread device: Light, MultiZoneLight, CeilingLight and two distinct MatrixLight aliases at minimum; permit no available-class omission (InfraredLight/HevLight are handled in Task 2, never as inventory entries).

Pick a fresh alias-shaped `--session-id` and a fresh `--seed`, record both for this session (reuse identically if resuming), then run `init --session-dir "$LIFX_PHASE14_SESSION_DIR" --session-id "<id>" --seed "<seed>" --inventory "<roster-file-path>"`. This is the real roster-completeness gate: an incomplete roster makes `init` refuse to write any manifest at all and emit `{"command":"init","ok":false,"reason":"incomplete_expected_roster","detail":"..."}` -- confirm `.ok == true` before proceeding. Re-running `init` with identical arguments against an existing manifest is a safe no-op (idempotent verification, not a rewrite).

Run `discover --session-dir "$LIFX_PHASE14_SESSION_DIR" --alias-map "$LIFX_PHASE14_ALIAS_MAP"` once; it drives all six paired, order-alternated rounds internally with the manifest's frozen jitter and is resumable (re-running after an interruption skips already-recorded round/source pairs). An alias observed outside the frozen roster raises `RosterDriftError`, which `main()` reports via `parser.error()` (a plain stderr message and exit code 2, NOT a JSON verdict -- treat any non-zero `discover` exit without a printed JSON object as roster drift and stop for a new operator-approved roster/session).

Before running the series, confirm every roster device's current power is a stable binary value (fully on or fully off, not mid-fade); if any is not, wait for it to settle first. Then invoke `request --session-dir "$LIFX_PHASE14_SESSION_DIR" --alias-map "$LIFX_PHASE14_ALIAS_MAP" --all` once -- it runs the frozen 100-trial series for every alias in the frozen manifest inventory, in sorted order, in one invocation, and a `power_out_of_range` preflight stop on one alias never stops the rest (the no-op `SetPower` trial harms no device). Read the single printed JSON verdict's `.results` array: for any alias whose `.outcomes` is exactly `{"power_out_of_range": 100}`, the CLI has no per-trial recapture -- the entire 100-trial series for that alias was consumed by one stale power read with zero real network trials. `derive_class_ledger_from_roster()` (used by `generate` in Task 3) only counts DISTINCT TRIAL ROWS toward completeness, not their outcome quality, so a fully `power_out_of_range` alias would still let its class close -- this is a genuine gap in the shipped completeness check, not something any available flag can enforce. If this happens, do not proceed with that alias's evidence as meaningful: confirm power is now stable, and note that fixing it requires a fresh session (dup trial numbers cannot be rewritten in place). Re-running `--all` after fixing a stuck device is safe: `.offending_aliases` names exactly which alias(es) still need a genuine re-collection, and every other alias's already-journaled trials are left untouched (resumable per trial, exactly like `--alias`).

Select exactly one staleness alias from the approved roster. Prefer the tool-driven path: invoke `staleness --session-dir "$LIFX_PHASE14_SESSION_DIR" --alias-map "$LIFX_PHASE14_ALIAS_MAP" --alias "<alias>" --power-off "<power-off-script>" --power-on "<power-on-script>"`. The CLI runs `--power-off` itself (as a direct argv list, never a shell) and captures the disconnect instant immediately after it exits 0 -- no manual timestamp capture is needed. (Only if no power scripts are available, fall back to the legacy manual path: disconnect power by hand, capture `time.monotonic_ns()` immediately at that moment -- for example `uv run --frozen python -c "import time; print(time.monotonic_ns())"` run right before disconnecting -- and pass it as `--disconnect-ns` instead. The two forms are mutually exclusive and exactly one is required, enforced before any I/O.) This single invocation blocks synchronously through the real absolute 60-second cadence until confirmed expiry (three consecutive both-legs-absent polls) or the real three-hour censoring cap. Once expiry closes, the CLI itself runs `--power-on` (or, on the legacy path, waits on the operator's manual reconnection) and then polls both discovery legs for restored availability with NO deadline -- watch stderr for live progress (each restoration poll, and the eventual `restored after <n>s` line reporting the measured `restoration_duration_s`) rather than assuming a hang; only the operator's Ctrl-C can interrupt an unbounded restoration wait. A non-zero exit, a timeout, or a missing/non-executable power script is a hard stop with an explicit JSON verdict: a `--power-off` failure starts nothing and mutates nothing; a `--power-on` failure still persists the already-closed disposition and states unmistakably that the device is physically dark and needs manual operator recovery. Preserve every empty/failure/interruption row; do not re-run `staleness` for an alias that already has a recorded row in this session (it is a same-alias no-op that never re-runs a power script, not a retry). If the emitted `.disposition` is `censored`, stop for an explicit user decision with only two choices: (A) authorise a new extended-cap session (a session-manifest amendment, since `STALENESS_CAP_S` is a frozen constant this plan does not change), or (B) keep the censored record and leave THREAD-04/Phase 14 open. Do not silently extend the cap or treat censorship as expiry/gap.

If the `--power-on` script fails (or, on the legacy manual path, the device never becomes visible again and the run must be interrupted), preserve the already-recorded row and every earlier discovery/request/staleness evidence in this session (D-16, amended 2026-09-04: a restoration failure confounds only measurements taken after it, never what was already collected), stop before touching another device, and recover the device under operator control. Recovery does not require a new session: once the device is confirmed recovered, the SAME `--session-id` continues -- the fleet remains ready for later stages, and if the affected alias's own evidence still needs a genuine re-collection, that is a `--session-id` amendment decision for the operator, not an automatic whole-session restart.</action>
  <instructions>Approve the complete privacy-safe inventory roster before `init`, including both MatrixLight devices, authorise the external alias mapping, keep the selected fleet quiesced, and prepare/approve power-off and power-on scripts for the explicitly selected staleness target (or be ready to disconnect/reconnect it manually if no scripts are available). Recover any device manually before continuing. Do not paste raw tool output, mapping contents or power-script source into the task.</instructions>
  <verification>The executor runs the automated JSON assertions below and shows the operator only alias-safe completeness/restoration categories. The operator confirms the selected target is physically available again.</verification>
  <resume-signal>Reply `physical discovery/request/staleness complete` only after the assertions below pass and the target is restored; for censorship choose `authorise amended extended-cap session` or `leave THREAD-04 open`; for restoration failure reply only after manual recovery so the same session can continue.</resume-signal>
  <verify>
    <automated>VALIDATE_OUT=$(uv run --frozen python -m scripts.thread_revalidation validate --session-dir "$LIFX_PHASE14_SESSION_DIR") &amp;&amp; echo "$VALIDATE_OUT" | jq -e --slurpfile manifest "$LIFX_PHASE14_SESSION_DIR/14-MANIFEST.json" '($manifest[0].inventory | length) as $n | .ok == true and .counts.discovery == 12 and .counts.staleness == 1 and .counts.requests == (100 * $n)' &amp;&amp; jq -e --slurpfile manifest "$LIFX_PHASE14_SESSION_DIR/14-MANIFEST.json" '($manifest[0].inventory | map(.alias)) as $expected | ((((.discovery.discover.devices // {}) | keys) + ((.discovery.discover_mdns.devices // {}) | keys)) | unique) as $observed | (($expected - $observed) == []) and ((.staleness | to_entries[0].value.disposition) == "confirmed_expiry") and ((.staleness | to_entries[0].value.restored_available_ns) != null)' "$LIFX_PHASE14_SESSION_DIR/14-SUMMARY.json"</automated>
    <fails_when>Any command exits non-zero or prints no JSON (a silent argparse `parser.error()` -- treat as roster drift/an unresolved alias, per the action above); `validate`'s `counts.discovery` is not exactly 12 (all six paired rounds); `counts.staleness` is not exactly 1; `counts.requests` is not exactly 100 times the manifest's inventory length; any manifest inventory alias is absent from BOTH discovery sources' observed-device keys in `14-SUMMARY.json`; the selected staleness alias's disposition is not `confirmed_expiry` (a `censored`, `interrupted`, or `restored_before_expiry` disposition fails this gate by design -- censorship is handled by the explicit resume-signal choice, never silently passed); or `restored_available_ns` is `null`. Operator-judgment items the CLI's own JSON cannot express: an alias whose `request --all` `.results[].outcomes` entry was exactly `{"power_out_of_range": 100}` (see action) needs a fresh session before its evidence is meaningful, even though this passes the machine count check; `init`'s roster-completeness refusal and `discover`'s roster-drift `parser.error()` are both correctly fail-closed but are NOT JSON verdicts, so their absence-of-a-usable-manifest / absence-of-printed-JSON is itself the signal to stop.</fails_when>
  </verify>
  <acceptance_criteria>
    - One manifest and three journals are append-only, bound to the current full revision and contain only approved aliases/bounded values.
    - The manifest freezes the operator-approved complete expected roster before collection, including Light, MultiZoneLight, CeilingLight and both MatrixLight aliases.
    - All six paired discovery rounds and 100 request-trial rows per expected device are present, whatever their outcome (`completed`, `timeout`, `send_error`, `power_out_of_range`, `cancelled` or `interrupted` -- retransmission itself is internal to a `completed` trial's logical latency, not a separate outcome).
    - A missing expected alias remains incomplete and neither it nor an unexpected alias can be converted into a gap or silently skipped.
    - Staleness has distinct merged-liveness and advertisement observations, exact cadence/confounders, three-pair confirmation and restored availability.
    - Censorship or restoration failure halts closure under the exact escalation above.
  </acceptance_criteria>
  <done>The physical session has complete discovery/request/staleness evidence or has stopped honestly at a named non-closure outcome without risking another device.</done>
</task>

<task type="auto">
  <name>Task 2: Record exact closure dispositions for the two named-gap classes</name>
  <precondition>Task 1 validation passed with confirmed non-censored staleness, restored availability and every alias in the frozen operator-approved expected roster complete through its applicable stage.</precondition>
  <files>.planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-CLOSURE.jsonl</files>
  <read_first>
    - .planning/phases/14-thread-revalidation-and-docs/14-CONTEXT.md (D-09 through D-20, including the 2026-09-04 amendment superseding D-15 and rewriting D-16)
    - .planning/phases/14-thread-revalidation-and-docs/14-SPEC.md (THREAD-03, THREAD-05 and AC-14 through AC-17)
    - scripts/thread_revalidation.py (`validate` `--help`; note there is no `closure` subcommand -- confirm this with `--help` yourself, since Task 2 appends closure rows via a direct Python call to already-shipped functions, not a CLI flag)
    - .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-MANIFEST.json (alias-only frozen inventory; do not hand-edit)
  </read_first>
  <action>Thread animation is a recorded scope boundary, not a per-class measurement (THREAD-03, amended 2026-09-04): `derive_class_ledger_from_roster()` no longer requires an animation attempt for any class, so this task does NOT run `animation --all` or any other animation observation against the Thread fleet. The one animation observation already collected during earlier evidence collection (alias `LIFX-Candle-C-1`, all three of 1/2/5 FPS completed) is retained in `14-ANIMATION.jsonl` as evidence that Thread carries the frames without failing -- and is cited in the report as exactly that, never as evidence that Thread animation is usable. No further alias needs one, and every currently available class (`Light`, `MultiZoneLight`, `MatrixLight`, `CeilingLight`) now closes on discovery and request evidence alone.

There is no `closure` CLI subcommand. Append the two mandatory named-gap dispositions directly via the already-shipped `build_closure_event()`/`append_closure_event()` functions (this calls only existing code through Python's normal import mechanism, not a new flag or subcommand):

```bash
uv run --frozen python - <<'PYEOF'
import os
from datetime import date
from pathlib import Path
from scripts.thread_revalidation import (
    append_closure_event, build_closure_event, load_manifest, CLOSURE_FILENAME,
)

session_dir = Path(os.environ["LIFX_PHASE14_SESSION_DIR"])
manifest = load_manifest(session_dir)
today = date.today().isoformat()
for device_class in ("InfraredLight", "HevLight"):
    row = build_closure_event(
        session_id=manifest["session_id"],
        protocol_version=manifest["protocol_version"],
        revision=manifest["revision"],
        device_class=device_class,
        disposition="named_gap",
        gap_reason=f"No {device_class} device is present in this session physical fleet inventory.",
        gap_recorded_date=today,
    )
    append_closure_event(session_dir / CLOSURE_FILENAME, row)
    print(f"closure appended: {device_class}")
PYEOF
```

Run this exactly once per session (append-only; a duplicate `(session_id, device_class)` row is rejected outright). Do NOT write `evidence_backed` closure rows for Light/MultiZoneLight/MatrixLight/CeilingLight -- `derive_class_ledger_from_roster()` (Task 3) derives those four classes' dispositions directly from the discovery/request journals and deliberately never trusts a caller-supplied closure claim (T-14-20); writing one ourselves would be exactly the kind of unverified claim that derivation exists to override, and it plays no role in `generate`'s real completeness gate. `CLOSURE.jsonl` therefore holds exactly two rows in this CLI's shipped behaviour (InfraredLight, HevLight); the other four classes' closure lives only in the generated `14-CLASS-LEDGER.json` (Task 3). Do not edit prior journals or fabricate evidence for unavailable classes.</action>
  <verify>
    <automated>uv run --frozen python -m scripts.thread_revalidation validate --session-dir "$LIFX_PHASE14_SESSION_DIR" | jq -e '.ok == true and .counts.closure == 2'</automated>
    <fails_when>The command exits non-zero; `counts.closure` is not exactly 2 (InfraredLight and HevLight only -- a stray `evidence_backed` row for an available class is itself a plan violation, not just a count mismatch); a class other than InfraredLight/HevLight receives a `named_gap` row; or an available-class device is labelled a gap.</fails_when>
  </verify>
  <acceptance_criteria>
    - `14-CLOSURE.jsonl` holds exactly two named_gap rows (InfraredLight, HevLight); Light, MultiZoneLight, MatrixLight and CeilingLight carry no closure row and close only through Task 3's roster-derived ledger, never a hand-written claim.
    - No animation observation is run as part of this task; the pre-existing Candle observation in `14-ANIMATION.jsonl` is left untouched and unextended.
  </acceptance_criteria>
  <done>The two named-gap closure dispositions are recorded without benchmarking claims, synthetic substitution, or an animation-attempt dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Generate and verify the qualified evidence products from journals only</name>
  <files>.planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-SUMMARY.json, .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-CLASS-LEDGER.json, .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-REPORT.md</files>
  <read_first>
    - .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-MANIFEST.json (immutable source identity)
    - .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE/14-CLOSURE.jsonl (the two named_gap rows from Task 2)
    - .planning/phases/14-thread-revalidation-and-docs/14-SPEC.md (AC-08, AC-13 through AC-17)
    - scripts/thread_revalidation.py (`generate` and `validate-staged` `--help`, and `derive_class_ledger_from_roster()`/`validate_staged_evidence()`)
    - .planning/phases/13-merged-discovery/13-03-PLAN.md (proven final selectors)
  </read_first>
  <action>Run `generate --session-dir "$LIFX_PHASE14_SESSION_DIR"` -- this IS the real require-complete gate: it derives the six-class ledger from the frozen manifest roster and the five journals via `derive_class_ledger_from_roster()` and writes `14-SUMMARY.json`/`14-CLASS-LEDGER.json`/`14-REPORT.md` atomically ONLY when every class closes (`{"ok":false,"reason":"class_ledger_incomplete","missing_classes":[...]}` and no files touched otherwise). Never hand-edit, parse or preserve an older derived file. State per-device/per-class results and confounders descriptively; `generate_report()` already qualifies all animation/request measurements as this fleet/session's observations and makes no authoritative benchmark, universal Thread limit, regression gate, render/smoothness claim or tuning recommendation -- do not add narrative that overstates this. Run `generate` a SECOND time and require the three product files to be byte-identical to the first run (the shipped function is deterministic and idempotent; a diff indicates a real bug, not expected variance).

Then run the exact proven Phase 13 measurement selector, the focused Phase 14 suites, the full frozen suite, Ruff and Pyright so the hardware-gated phase does not weaken CI. Stage exactly the manifest, five journals and three generated products named in this plan (`git add --` with the nine explicit paths, never a glob). After staging and before commit, run `validate-staged --evidence-dir <repo-relative-evidence-dir>` from the repository root: its `--evidence-dir` value MUST be the repository-relative path `.planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE`, not the absolute `$LIFX_PHASE14_SESSION_DIR` -- it matches against `git diff --cached --name-only`, which Git reports relative to the repository root, and an absolute path here silently reports every one of the nine paths as `missing_evidence_path` (verified empirically). It compares the staged evidence path set against that exact nine-path allowlist and inspects Git INDEX blobs, not worktree files, through the same schema/privacy/provenance/roster/regeneration validators as `generate`. It fails with bounded path/category diagnostics and never echoes a matched identifier or file content. Raw captures and the alias map remain outside the repository.</action>
  <verify>
    <automated>EVIDENCE_DIR=".planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE" &amp;&amp; uv run --frozen python -m scripts.thread_revalidation generate --session-dir "$LIFX_PHASE14_SESSION_DIR" | jq -e '.ok == true and (.missing_classes == []) and ((.classes | keys | length) == 6)' &amp;&amp; sha256sum "$LIFX_PHASE14_SESSION_DIR"/14-SUMMARY.json "$LIFX_PHASE14_SESSION_DIR"/14-CLASS-LEDGER.json "$LIFX_PHASE14_SESSION_DIR"/14-REPORT.md &gt; "$LIFX_PHASE14_SESSION_DIR"/.gen1.sha256 &amp;&amp; uv run --frozen python -m scripts.thread_revalidation generate --session-dir "$LIFX_PHASE14_SESSION_DIR" | jq -e '.ok == true' &amp;&amp; sha256sum "$LIFX_PHASE14_SESSION_DIR"/14-SUMMARY.json "$LIFX_PHASE14_SESSION_DIR"/14-CLASS-LEDGER.json "$LIFX_PHASE14_SESSION_DIR"/14-REPORT.md &gt; "$LIFX_PHASE14_SESSION_DIR"/.gen2.sha256 &amp;&amp; diff "$LIFX_PHASE14_SESSION_DIR"/.gen1.sha256 "$LIFX_PHASE14_SESSION_DIR"/.gen2.sha256 &amp;&amp; rm -f "$LIFX_PHASE14_SESSION_DIR"/.gen1.sha256 "$LIFX_PHASE14_SESSION_DIR"/.gen2.sha256 &amp;&amp; uv run --frozen pytest -o addopts='' tests/test_api/test_api_discovery.py tests/test_network/test_discovery_coordinator.py tests/test_network/test_mdns/test_liveness.py tests/test_devices/test_state_light.py tests/test_scripts/test_measure_merged_discovery.py -q &amp;&amp; uv run --frozen pytest -o addopts='' tests/test_network/test_connection_retry.py tests/test_discovery_observation.py tests/test_scripts/test_measure_merged_discovery.py tests/test_scripts/test_ipv6_thread_probe.py tests/test_scripts/test_thread_revalidation.py tests/test_network/test_mdns/test_phase_contract.py tests/test_repository_guidance.py -q &amp;&amp; uv run --frozen pytest &amp;&amp; uv run --frozen ruff format --check src/lifx tests scripts/measurement_support.py scripts/thread_revalidation.py scripts/measure_merged_discovery.py scripts/ipv6_thread_probe.py examples/discovery_progressive.py &amp;&amp; uv run --frozen ruff check src/lifx tests scripts/measurement_support.py scripts/thread_revalidation.py scripts/measure_merged_discovery.py scripts/ipv6_thread_probe.py examples/discovery_progressive.py &amp;&amp; uv run --frozen pyright &amp;&amp; git add -- "$EVIDENCE_DIR/14-MANIFEST.json" "$EVIDENCE_DIR/14-DISCOVERY.jsonl" "$EVIDENCE_DIR/14-REQUESTS.jsonl" "$EVIDENCE_DIR/14-STALENESS.jsonl" "$EVIDENCE_DIR/14-ANIMATION.jsonl" "$EVIDENCE_DIR/14-CLOSURE.jsonl" "$EVIDENCE_DIR/14-SUMMARY.json" "$EVIDENCE_DIR/14-CLASS-LEDGER.json" "$EVIDENCE_DIR/14-REPORT.md" &amp;&amp; uv run --frozen python -m scripts.thread_revalidation validate-staged --evidence-dir "$EVIDENCE_DIR" | jq -e '.ok == true and (.failures == [])'</automated>
    <fails_when>Any command exits non-zero; `generate`'s first or second call reports `.ok == false` (an incomplete roster-derived ledger -- see `.missing_classes`); the two `generate` runs' product SHA-256 digests differ; `classes | keys | length` is not exactly 6; a focused historical suite, the full suite, Ruff or Pyright fails; `validate-staged` reports any non-empty `.failures` array (each entry's bounded `category` names the exact defect: `missing_evidence_path`/`unexpected_staged_path` means the wrong nine paths were staged -- check `--evidence-dir` is the repo-relative path, not the absolute session dir; `schema_validation_failed` means a journal or the manifest is malformed; `incomplete_expected_roster` means the roster itself regressed; `six_class_ledger_incomplete` means the roster-derived ledger is not actually complete despite `generate` having reported success, which would itself indicate a real bug; `summary_not_regenerated_from_journals`/`class_ledger_not_regenerated_from_journals`/`report_not_regenerated_from_journals` means a staged product does not match fresh regeneration from the staged journals, i.e. a stale or hand-edited product was staged); or `validate-staged` prints no JSON (git not on PATH, or not run from the repository root, per `_run_git`'s reliance on the current process's cwd for `git diff --cached`).</fails_when>
  </verify>
  <acceptance_criteria>
    - Summary, ledger and report are byte-deterministic journal-only derivations (`generate` run twice produces byte-identical products) and `generate` reports `.ok == true` with zero `missing_classes` before staging.
    - After exact-path staging and before commit, `validate-staged --evidence-dir .planning/phases/14-thread-revalidation-and-docs/14-EVIDENCE` (repo-relative, run from repository root) reports `.ok == true` and an empty `.failures` array against the nine Git index blobs, with identity-free diagnostics.
    - The exact six classes and every expected alias/gap/confounder/outcome, including both MatrixLight devices, are represented without raw identifiers; only InfraredLight and HevLight are `named_gap`, and both come from Task 2's two closure rows (no `evidence_backed` closure row was ever written for the other four classes -- they close through roster derivation alone).
    - Evidence language is descriptive and fleet/session-qualified, with no animation delivery or universal performance inference.
    - Historical Phase 13 selector, all Phase 14 focused suites, full pytest, Ruff and Pyright pass.
  </acceptance_criteria>
  <done>SEED-001 is closed by fresh, exact-class, privacy-safe and correctly qualified evidence, with hardware remaining outside CI.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | Item | Status | Executable coverage |
|--------|------|--------|---------------------|
| GOAL | Revalidate v1.1 wire-reliability findings over Thread from this fleet, per class, and ship consumer guidance/corrections without hardware blocking CI or another phase | COVERED | Plans 14-01 through 14-04 build and prove the measurement path; Plan 14-05 ships docs independently in Wave 1; Plan 14-06 alone is operator-gated. |
| REQ | THREAD-01 | COVERED | Plans 14-02, 14-04 Task 1 and 14-06 Task 1. |
| REQ | THREAD-02 | COVERED | Plans 14-01, 14-02, 14-04 Task 1 and 14-06 Task 1. |
| REQ | THREAD-03 | COVERED | Recorded as a scope boundary (amended 2026-09-04): Plans 14-02, 14-03, 14-04 built and proved the observation mechanics used for the one pre-existing Candle observation; 14-06 Task 2 cites it without running any further animation attempt. |
| REQ | THREAD-04 | COVERED | Plans 14-02, 14-04 Task 2 and 14-06 Task 1, including censorship escalation. |
| REQ | THREAD-05 | COVERED | Plans 14-02, 14-04 Task 3 and 14-06 Tasks 2-3. |
| REQ | DOCS-04 | COVERED | Plan 14-05 Tasks 1-2. |
| REQ | DOCS-05 | COVERED | Plan 14-05 Task 2. |
| REQ | DOCS-06 | COVERED | Plan 14-05 Tasks 2-3. |
| RESEARCH | Operator correction: WiFi animation findings do not transfer to Thread | COVERED | Plans 14-02/14-04 freeze a non-gating current-behaviour observation; Plan 14-06 rejects cross-transport/performance claims. |
| RESEARCH | Immutable manifest, five append-only stages and journal-only derived products | COVERED | Plan 14-02 Tasks 1-2 and Plan 14-06 Tasks 1-3. |
| RESEARCH | Private observer at request-correlation owner seam | COVERED | Plan 14-01 Tasks 1-2. |
| RESEARCH | Read-only current animation behaviour at script level | COVERED | Plan 14-04 Task 2; no animation production file is modified. |
| RESEARCH | Fail-closed mutation with verified restoration | COVERED | Plan 14-03 Task 2, Plan 14-04 Tasks 2-3, Plan 14-06 Tasks 1-2. |
| RESEARCH | Frozen discovery/request/staleness schedules and one fixed animation observation | COVERED | Plan 14-02 Task 2 and Plan 14-04 Tasks 1-2. |
| RESEARCH | One executable documentation source | COVERED | Plan 14-05 Tasks 1-2 with strict snippet paths. |
| RESEARCH | Pitfalls: logical latency versus ACK RTT; command success versus restored state; resume duplication; staleness ambiguity | COVERED | Plans 14-01, 14-03 and 14-04 contain exact clocks, readback, uniqueness and per-leg semantics. |
| RESEARCH | Pitfalls: type/coverage escape and hardware leakage into CI | COVERED | Plans 14-01/14-03 use a fixed patch base and explicit gates; Plan 14-06 is the only hardware checkpoint. |
| RESEARCH | Zero runtime dependencies, Python 3.10 and standard-library asyncio | COVERED | All plans forbid dependency/Python-floor changes; Plans 14-03/14-05 verify Pyright and Python 3.10 guidance. |
| CONTEXT | D-01 six paired discovery rounds | COVERED | Plan 14-02 manifest and Plan 14-04 Task 1. |
| CONTEXT | D-02 alternating source order and bounded jitter | COVERED | Plan 14-02 manifest and Plan 14-04 Task 1. |
| CONTEXT | D-03 100 attempts per available Thread device including failures | COVERED | Plan 14-04 Task 1 and Plan 14-06 Task 1. |
| CONTEXT | D-04 60-second dual-source polls, three absences, three-hour censor | COVERED | Plan 14-04 Task 2 and Plan 14-06 Task 1 with explicit censored escalation. |
| CONTEXT | D-05 no-op captured-power SetPower with restoration | COVERED | Plans 14-03 Task 2 and 14-04 Task 1. |
| CONTEXT | D-06 stored 0.5-to-1.5-second request gaps | COVERED | Plans 14-02/14-04 Task 1. |
| CONTEXT | D-07 logical completion and winning-sequence ACK RTT | COVERED | Plan 14-01 tracer and Plan 14-04 Task 1. |
| CONTEXT | D-08 ordinary median, nearest-rank p95, maximum, undefined timeout latency | COVERED | Plans 14-02/14-04 Task 1. |
| CONTEXT | D-09 secondary non-gating animation observation | COVERED | Plans 14-04 Task 2 and 14-06 Task 2. |
| CONTEXT | D-10 one ascending 1/2/5 FPS ten-second observation | COVERED | Plans 14-02/14-04 Task 2. |
| CONTEXT | D-11 no WiFi-derived concurrent query workload | COVERED | Plan 14-04 Task 2. |
| CONTEXT | D-12 transport-side counters, zero result accepted | COVERED | Plans 14-02/14-04 Task 2 and 14-06 Task 2. |
| CONTEXT | D-13 no production animation instrumentation/tuning | COVERED | Plan 14-04 modifies only scripts/support/tests and regression-tests this boundary. |
| CONTEXT | D-14 fresh Animator, capture and restore/readback on every exit | COVERED | Plans 14-03 Task 2, 14-04 Task 2 and 14-06 Task 2. |
| CONTEXT | D-15 (superseded 2026-09-04) closure never depends on an animation attempt for any class | COVERED | `derive_class_ledger_from_roster()` takes no animation input; 14-06 Task 3's `generate` closes every available class on discovery/request evidence alone. |
| CONTEXT | D-16 (amended 2026-09-04) failed restoration halts and requires operator-confirmed recovery, then resumes the SAME session rather than restarting under a new identity | COVERED | Plans 14-02, 14-03 and Plan 14-06 Task 1's staleness restoration escalation. |
| CONTEXT | D-17 dedicated orchestrator and shared helper reuse | COVERED | Plans 14-01 through 14-04. |
| CONTEXT | D-18 immutable manifest, independent append/resume | COVERED | Plans 14-02 and 14-06. |
| CONTEXT | D-19 external in-memory alias resolution and safe write boundary | COVERED | Plans 14-02/14-03 and 14-06. |
| CONTEXT | D-20 one manifest, five journals, deterministic three products | COVERED | Plans 14-02 and 14-06. |
| CONTEXT | D-21 canonical discovery guide, moved UDP material and concise API/advanced pages | COVERED | Plan 14-05 Tasks 1-2. |
| CONTEXT | D-22 full consumer journey | COVERED | Plan 14-05 Task 1. |
| CONTEXT | D-23 one executable progressive snippet source | COVERED | Plan 14-05 Tasks 1-2. |
| CONTEXT | D-24 AGENTS canonical, CLAUDE import-only, Python 3.10 tests | COVERED | Plan 14-05 Task 3 explicitly resolves the conflicting phase contract. |
| SPEC | All 43 Edge Coverage rows | COVERED | 14-02 truths contain 7 THREAD-01 rows; 14-04 contains 18 THREAD-02/03/04 rows; 14-05 contains 12 DOCS rows; 14-06 contains 6 THREAD-05 rows. Every dismissed row is a flat `verification: backstop` object. |
| SPEC | Five must-NOT prohibitions | COVERED | Raw-identity and discard/overwrite prohibitions are in 14-02; broad mutation and synthetic-as-fleet prohibitions are in 14-04; benchmark/tuning prohibition is in 14-06. |
| REVIEW | CLAUDE.md reduction conflicts with phase contract | COVERED | Plan 14-05 Task 3 removes CLAUDE from duplicate-query paths and adds an import/de-duplication contract. |
| REVIEW | Accepted-ACK capture point and ns/float clock mismatch | COVERED | Plan 14-01 Task 1 samples at dequeue with monotonic_ns while production scheduling stays float monotonic. |
| REVIEW | discover() versus discover_mdns() staleness and absent-pair ambiguity | COVERED | Plan 14-04 Task 2 gives each leg distinct semantics and confirms only three both-absent pairs. |
| REVIEW | Censored staleness had no escalation path | COVERED | Plan 14-04 tests and Plan 14-06 Task 1 expose exactly amend-and-new-session or leave open. |
| REVIEW | Animation ACK/expiry statistics do not exist; outstanding reductions are ambiguous | COVERED | Plans 14-02/14-04 name only exposed fields and prohibit ACK-delivery interpretation. |
| REVIEW | Intermediate captured power can raise before sending | COVERED | Plan 14-03 Task 2 creates `power_out_of_range` preflight and same-session stable recapture. |
| REVIEW | New scripts face 100 percent patch coverage and estimate pressure | COVERED | Six-plan split plus fixed 14-COVERAGE-BASE and explicit Plan 14-03 type/coverage gates. |
| REVIEW | Docs were unnecessarily behind the hardware checkpoint | COVERED | Plan 14-05 is independent Wave 1. |
| REVIEW | Observation-sink ownership/test import anti-pattern | COVERED | Plans 14-01/14-03 assign support/production ownership and remove importlib-from-tests. |
| REVIEW | Snippet path checking too weak | COVERED | Plan 14-05 Task 2 configures `check_paths: true`, root base path and real docs builds. |
| REVIEW | troubleshooting.md retains Python 3.10-incompatible TaskGroup advice | COVERED | Plan 14-05 Task 2 includes troubleshooting in scope. |
| REVIEW | Restoration comparison and probe raw diagnostic boundary unspecified | COVERED | Plan 14-03 Task 2 freezes exact protocol comparison and keeps terminal diagnostics out of evidence. |
| REVIEW | Inventory changes, synchronous send_frame and staging privacy needed explicit treatment | COVERED | Plans 14-04/14-06 specify all three mechanically. |
| REVIEW | Initial discovery could omit known available devices and legalise false gaps | COVERED | Plan 14-04 freezes and validates an independent operator-approved expected roster; Plan 14-06 requires evidence for Light, MultiZoneLight, CeilingLight and both MatrixLight aliases, permits gaps only for InfraredLight/HevLight, and leaves every missing expected alias incomplete. |
| REVIEW | Privacy validation did not inspect exact post-staging index contents | COVERED | Plan 14-04 implements a bounded-output exact-allowlist staged-index validator; Plan 14-06 stages the nine evidence paths explicitly and runs it before commit. |
| REVIEW | Stale API-surface intel reports zero symbols | REJECTED AS EVIDENCE | The plan does not infer absence; source files and existing tests define interfaces, as required by the task hint. |
| SCHEMA | No schema files in phase scope | COVERED | No schema migration or fabricated schema plan is present. |
| API | No external API integration in this internal-library phase | COVERED | No external API coverage artefact or fabricated integration is present. |

Audit result: 0 missing items, 0 deferred locked decisions, 0 silent review drops. Deferred ideas from CONTEXT remain out of scope.

<threat_model>
Security posture: OWASP ASVS Level 1; every high-severity threat blocks plan completion until its named mitigation test passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External alias map/private fleet -> tracked evidence | Live identity and infrastructure data must be transformed before any file opens. |
| Operator physical mutation -> session progression | State must be exactly restored and read back before another target. |
| Physical journals -> published evidence products | Poor, interrupted or confounded results must remain visible without acquiring universal authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-18 | Information disclosure | physical evidence paths | high | mitigate | External mapping, pre-open validation, bounded values, tracked-path privacy scan and exact post-staging Git-index inspection block on any leak without echoing matched content. |
| T-14-19 | Tampering / Repudiation | session and derived products | high | mitigate | Exact revision, create-exclusive manifest, append-only journals, journal-only deterministic regeneration and byte comparison. |
| T-14-20 | Spoofing | synthetic or unmapped evidence | high | mitigate | Unique approved alias mapping plus provenance validator; synthetic/fake rows cannot satisfy closure. |
| T-14-21 | Denial of service / Elevation of privilege | physical mutation | high | mitigate | One selected alias, exact restoration/readback, halt-before-the-next-device, and operator-confirmed recovery before the same session resumes (D-16, amended 2026-09-04: recovery no longer requires restarting the whole session under a new identity). |
| T-14-SC | Tampering | package installs | low | accept | No package or lockfile change is made. |
</threat_model>

<verification>
- Complete the blocking operator checkpoint (Task 1) without raw captures entering the repository.
- Run the final command exactly; high-severity privacy, provenance, immutability and restoration threats are blocking.
- Confirm the audit remains 0 missing after execution and generated evidence contains all six exact classes.
</verification>

<success_criteria>
- Fresh SEED-001 evidence exists per available device and exact class, or unavailable classes have honest named gaps.
- THREAD-04 has confirmed non-censored advertisement expiry and restored starting availability.
- Products are deterministic, privacy-safe and explicitly limited to this fleet/session.
- Hardware work never runs in CI and does not gate the independent documentation plan.
</success_criteria>

<output>
Create `.planning/phases/14-thread-revalidation-and-docs/14-06-SUMMARY.md` when done.
</output>
