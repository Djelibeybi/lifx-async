---
phase: 13-merged-discovery
plan: 04
type: execute
wave: 3
depends_on: [13-02, 13-03]
files_modified:
  - src/lifx/api.py
  - tests/conftest.py
  - tests/test_api/test_api_discovery.py
autonomous: true
requirements: [FIND-01, FIND-03, FIND-04, FIND-09, FIND-10]
estimate:
  tokens: 16000
  raw_tokens: 32000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "[Edge R1 adjacency] When UDP and verified mDNS return the same normalised serial, the literal first fully valid result wins once."
    - "[Edge R1 empty] An empty UDP leg and empty mDNS leg yield no device."
    - "[Edge R1 ordering] Default discovery streams results as validation completes with no fixed transport priority."
    - "[Edge R1 repetition] Completed default calls discard source and verification state and start both legs afresh."
    - "[Edge R1 concurrency] Concurrent legs, subscribers, candidate probes, failures, and cancellation preserve deterministic ownership and cleanup."
    - "[Edge R3 repetition] Every default discovery call attempts mDNS afresh after prior success, empty completion, or expected failure."
    - "[Edge R3 concurrency] An expected mDNS failure cannot cancel, shorten, delay, or alter the surviving shared UDP schedule."
    - "D-01/D-02 failures caught inside the live mDNS sweep reach the merger exactly once as typed bounded events; the merged DEBUG record contains no raw exception or endpoint text, and source-specific `discover_mdns()` keeps its established no-injection behaviour."
    - "Every existing public discover()/find_by_serial() test is hermetic after the default gains mDNS: ambient multicast is replaced by an empty private source unless the test explicitly injects a synthetic source."
    - "The UDP pump preserves the established one-at-a-time DiscoveredDevice construction cadence while draining the shared raw stream; it does not fan 73 device-construction requests out concurrently."
    - "One caller-origin overall wall deadline begins when discover() starts and bounds both-leg replay, verification/construction, merge queue processing, and yield; an accepted pre-deadline record that finishes construction after the deadline is not yielded, while the shared UDP producer retains its own origin wire deadline."
    - "The measured caller's explicit observation sink crosses the UDP coordinator boundary through Plan 13-02, so a deterministic late subscriber records exactly one UDP event per accepted response and a same-call dual-source measurement contains both udp and mdns without unrelated concurrent discovery pollution."
  prohibitions:
    - statement: "MUST NOT make dual-source discovery the only public enumeration mode, remove discover_mdns(), add a transport selector, or let entry-point selection change device routing, retry, tuning, or connectivity semantics."
      status: resolved
      verification: test
  artifacts:
    - path: src/lifx/api.py
      provides: "Python 3.10-compatible dual-source default stream with explicit failure disposition and cleanup"
      exports: [discover, discover_udp, discover_mdns]
    - path: tests/test_api/test_api_discovery.py
      provides: "First-valid merge, source-mode, live mDNS open/receive catch degradation, exactly-once diagnostics, fresh-call, cancellation, and early-close tests"
  key_links:
    - from: src/lifx/api.py
      to: src/lifx/network/discovery.py
      via: "UDP pump owns discover_devices_shared() and receives subscriber-specific devices"
      pattern: "discover_devices_shared|aclosing"
    - from: src/lifx/api.py
      to: src/lifx/network/mdns/discovery.py
      via: "mDNS pump owns _discover_verified_devices_mdns(), receives typed sweep failures emitted by the actual open/receive catch plus typed candidate failures through one sink, and emits only directly verified candidates"
      pattern: "_discover_verified_devices_mdns|_MdnsSweepFailure|_MdnsCandidateFailure|failure_sink|aclosing"
    - from: src/lifx/api.py
      to: scripts/measure_merged_discovery.py
      via: "private context-local source/winner observations are emitted before and after dedup during the exact discover() call consumed by the harness"
      pattern: "_DiscoveryObservation|accepted|winner|duplicate"
---

> **Path amendment (Plan 13-07):** Paths below describe this plan's original
> execution checkout. Current canonical locations and compatibility boundaries
> are mapped in [\`13-PATH-AMENDMENT.md\`](13-PATH-AMENDMENT.md).

<objective>
Promote `discover()` to a concurrent UDP plus verified-mDNS stream while retaining explicit source-specific enumeration and isolating only the expected mDNS failures selected by D-01.

Purpose: Thread-only devices become visible by default without making dual-source discovery mandatory, imposing hidden transport priority, weakening the existing UDP contract, or leaking resources on Python 3.10.
Output: A typed two-leg merge state machine, privacy-safe absorbed-failure diagnostics, fail-fast unexpected-error handling, and complete public source-mode tests.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/13-merged-discovery/13-01-SUMMARY.md
@.planning/phases/13-merged-discovery/13-02-SUMMARY.md
@.planning/phases/13-merged-discovery/13-03-SUMMARY.md
@.planning/phases/13-merged-discovery/13-CONTEXT.md
@.planning/phases/13-merged-discovery/13-SPEC.md
@.planning/phases/13-merged-discovery/13-RESEARCH.md
@.planning/phases/13-merged-discovery/13-PATTERNS.md

<interfaces>
`discover_devices_shared()` yields UDP `DiscoveredDevice` objects constructed with the caller's request settings from one process-wide active raw sweep.

`_discover_verified_devices_mdns()` yields only current-call directly answering devices, with D-06 StateColor adoption complete before a light is emitted.

`_discover_verified_devices_mdns(failure_sink=...)` forwards private `_MdnsSweepFailure(stage, reason, error_type)` events emitted at `_discover_lifx_services_sweep()`'s actual transport-open/receive catch and reports every absorbed candidate drop at its actual catch/rejection site as one `_MdnsCandidateFailure(stage, reason, error_type)`. With the sink present, the mDNS layer suppresses its legacy raw exception-text log; with no sink, `discover_mdns()` retains existing standalone behaviour. The verifier performs no duplicate absorbed-failure logging. Plan 13-01's context-local observation scope captures raw source identities only in memory, and Plan 13-03 emits the mDNS raw-record observations needed for FIND-08.

Expected sweep exceptions are concrete sink-delivered `LifxNetworkError` events from open/send/receive/follow-up stages; ordinary receive `LifxTimeoutError` remains normal completion and has no synthetic failure reason. Expected candidate exceptions are `LifxConnectionError`, `LifxTimeoutError`, `LifxProtocolError`, `LifxUnsupportedCommandError`, `LifxUnsupportedDeviceError`, and explicit response/identity mismatch. `LifxUnsupportedDeviceError` drops only that advertised unsupported candidate. `asyncio.CancelledError` propagates after cleanup; all other exceptions fail fast.
</interfaces>
</context>

## Artifacts this phase produces

- Private frozen dataclass `_DiscoveryEvent` with fields `kind`, `source`, `device`, `error_type`, `stage`, and `reason` in `src/lifx/api.py`.
- Private coroutines `_pump_udp_discovery()` and `_pump_mdns_discovery()` and cleanup helper `_cancel_and_reap()` in `src/lifx/api.py`.
- Extension of the private observation contract so the merged pumps emit pre-dedup `accepted` events and the merger emits one `winner` or `duplicate` disposition per source event; no public callable gains an observer argument.
- Stable private absorbed-diagnostic reason values `sweep_open_network`, `sweep_send_network`, `sweep_receive_network`, `sweep_address_followup_network`, `candidate_connect`, `candidate_timeout`, `candidate_protocol`, `candidate_unsupported`, `candidate_identity`, and `candidate_response`; no unreachable exceptional-timeout reason is created.
- Modified public async generator `discover()` as the dual-source default; existing `discover_udp()` and `discover_mdns()` remain source-specific.
- Test fixture `_empty_mdns_for_public_discovery` in `tests/conftest.py`, active by default for public discovery tests with an explicit opt-in/injected-source marker for mDNS/merged cases.

## Review feedback dispositions

- The Antigravity `asyncio.TaskGroup` finding is rejected as factually inapplicable. This plan continues to require explicit Python 3.10 `asyncio.create_task()` pumps and manual cancellation/reaping; no plan text is changed toward TaskGroup.
- Resolve observation ownership by path without duplication: direct `discover_devices()` passes its caller sink into `_discover_with_packet()`, while the coordinator producer runs sink-free and Plan 13-02 emits once when each accepted raw record is delivered to that subscriber. The shared facade, `discover_udp()`, and this merged pump emit no additional event.
- Carry that ownership explicitly across the coordinator: Plan 13-02 transfers the caller-owned observation sink with each subscription. Reject the uncited claim that the mDNS caller-loop `ContextVar` alone solves UDP coordinator-boundary observation; the worker thread cannot rely on ambient propagation.
- Make ambient-mDNS isolation a suite contract before dual-source behaviour lands. The autouse fixture is synchronous so it sets and resets the private mDNS override `ContextVar` in the pytest test's own inherited context; ambient `MdnsTransport`/socket spies remain active as a backstop. Tests that need mDNS opt in to a synthetic private source; all other public discovery tests use the empty source and cannot touch the developer LAN.
- Accept the caller-origin overall wall deadline: it bounds replay, sequential UDP construction, mDNS verification, merge processing, and yield without extending the producer-origin shared UDP wire deadline.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Merge UDP and verified mDNS by first valid serial</name>
  <files>src/lifx/api.py, tests/conftest.py, tests/test_api/test_api_discovery.py</files>
  <read_first>
    - src/lifx/api.py
    - tests/test_api/test_api_discovery.py
    - tests/conftest.py
    - src/lifx/network/discovery.py
    - src/lifx/network/mdns/discovery.py
    - src/lifx/network/discovery_observation.py
    - src/lifx/protocol/models.py
    - .planning/phases/13-merged-discovery/13-01-SUMMARY.md
    - .planning/phases/13-merged-discovery/13-02-SUMMARY.md
    - .planning/phases/13-merged-discovery/13-03-SUMMARY.md
    - .planning/phases/13-merged-discovery/13-SPEC.md
  </read_first>
  <behavior>
    - `discover()` starts UDP and mDNS pumps concurrently under one caller timeout, yields UDP-only and verified-mDNS-only devices, and ends only after both legs end.
    - Serial dedup uses `Serial.from_string(...).to_string()` after each result is fully valid; whichever eligible result is enqueued first wins with no source priority.
    - A leg completing empty does not end the other; reversing controlled completion order reverses the retained duplicate instance.
    - Repeat and concurrent calls have fresh per-call mDNS work and share only a compatible active UDP sweep.
    - `discover_udp()` and `discover_mdns()` retain exact source-only participation and device settings.
    - One context-local observation scope sees each UDP and verified-mDNS source contribution exactly once before cross-leg dedup plus the resulting merged winner/duplicate disposition from this same `discover()` invocation; reversing controlled completion reverses the winner event without changing the alias/source set.
    - A deterministic late shared-UDP subscriber receives prefix and suffix observations in its own caller sink, and a same-call dual-source test proves the merged evidence set contains both `udp` and `mdns` while a simultaneous unrelated discovery call contributes nothing.
    - Public discovery tests default to an empty injected mDNS source and never construct `MdnsTransport`; merged/mDNS cases explicitly inject their synthetic source and remain independent of ambient LAN advertisements.
    - The empty-mDNS autouse fixture is synchronous, sets the override token in the test's own context, resets it in `finally`, and retains independent ambient transport/socket spies.
    - The UDP pump awaits each existing `_create_discovered_device()` call before consuming the next raw shared response, preserving serial device-construction cadence while source pumps still run concurrently.
    - A UDP `create_device()` path that returns `None` for an unsupported product is skipped without creating an mDNS candidate-failure event or a merged source-contribution event.
    - The caller-owned overall wall deadline is checked before and after construction/verification and before merge yield; no result crosses it even when its raw response arrived earlier.
  </behavior>
  <action>Implement a Python 3.10-compatible per-call event state machine in `discover()`. Capture one caller-origin monotonic overall wall deadline and the caller-owned observation sink at generator execution. Create both source generators, own each with `aclosing()`, start explicit `asyncio.create_task()` pumps, and consume an unbounded caller-loop event queue until both `leg_done` events arrive or the caller deadline is reached. Do not use `asyncio.TaskGroup`; that Antigravity claim was factually incorrect and would violate the Python 3.10 floor. The UDP pump passes the explicit sink and caller deadline through `discover_devices_shared()`, consumes it sequentially, and awaits each existing `_create_discovered_device()` before taking the next item, preserving the current one-at-a-time device-construction cadence instead of issuing an unbounded fleet-wide burst. Check the caller deadline before and after device construction and again before merge yield; discard and clean up a record whose construction crosses the deadline. Preserve the shared UDP producer's earlier origin wire deadline without extending it. Serial-normalise only a fully constructed pre-deadline UDP device or directly verified mDNS device. Emit no second UDP raw event in the pump: Plan 13-02's explicit subscription delivery already produces the sole caller-scoped UDP observation. Emit one matching verified-mDNS accepted event, then one merged winner/duplicate disposition at the cross-leg decision. Add `_empty_mdns_for_public_discovery` as a normal synchronous autouse fixture in `tests/conftest.py`; set the private mDNS override ContextVar token before yielding control to the test, reset it in `finally`, and retain separate spies that fail if ambient `MdnsTransport` construction or socket access occurs. Tests exercising merged or mDNS behaviour opt in with an explicit marker/helper and inject synthetic records. Add deterministic event/barrier tests for a late UDP subscriber, dual-source `udp`+`mdns` evidence, deadline-crossing construction, and an unrelated concurrent call that cannot pollute the measured sink. The private override remains unexported and the real runtime no-override path is unchanged. Preserve D-06 ordering, keep source-specific APIs exact, and add no selector.</action>
  <reversibility rating="costly">Changing the default public discovery source set affects every caller's timing and result population, but FIND-01/FIND-09 and the accepted measurement requirement lock this behaviour.</reversibility>
  <acceptance_criteria>
    - Controlled UDP-only, mDNS-only, mixed, empty, one-leg-empty, and dual-advertised cases pass.
    - Duplicate winner changes when valid completion order reverses, while the unique serial set does not.
    - Observer assertions for dual-advertised synthetic aliases show both contributing sources and exactly one winner/one duplicate; reversing source completion reverses only their dispositions, and call spies prove no extra discovery invocation occurs.
    - A late-subscriber test proves prefix and suffix UDP observations appear exactly once in the measured caller's sink; the merged observation set includes `udp` and `mdns`, and unrelated concurrent discovery contributes zero observations.
    - Results stream before the slower leg completes and each normalised serial appears once.
    - Exact signatures/exports preserve all three enumerators and show no source-selector parameter.
    - `find_by_ip()` and `find_by_label()` signatures/source behaviour remain unchanged.
    - Existing empty/not-found/timeout public tests fail if any ambient `MdnsTransport` is constructed; explicit merged tests prove their synthetic source is the only mDNS input.
    - Fixture tests prove the synchronous autouse fixture sets the ContextVar in the test's own context, resets it after the test, and leaves transport/socket spies enabled.
    - A 73-record UDP fake observes maximum one in-flight `_create_discovered_device()` call while the mDNS pump remains independently productive.
    - A synthetic unsupported UDP product returning `None` is absent from merged source accounting and cannot be mislabelled as an absorbed mDNS candidate failure.
    - A record accepted before the caller deadline but constructed after it is not yielded; cleanup completes and the active producer's wire deadline is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_api/test_api_discovery.py -q -k 'discover and not find_by_serial'</automated>
    <fails_when>non-zero exit, or pytest reports "0 passed" in the summary line</fails_when>
  </verify>
  <done>Default discovery concurrently streams first-valid unique devices from both legs, while explicit UDP-only and mDNS-only APIs remain unchanged and independently usable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Isolate expected mDNS failures and fail fast on every other error</name>
  <files>src/lifx/api.py, tests/test_api/test_api_discovery.py</files>
  <read_first>
    - src/lifx/api.py
    - tests/test_api/test_api_discovery.py
    - src/lifx/exceptions.py
    - src/lifx/network/mdns/discovery.py
    - src/lifx/network/connection.py
    - .planning/phases/13-merged-discovery/13-CONTEXT.md
    - .planning/phases/13-merged-discovery/13-SPEC.md
  </read_first>
  <behavior>
    - D-01 sweep-level typed network failures end only mDNS; candidate-level connection/timeout/protocol/unsupported-command/unsupported-device/identity/response failures drop only that candidate.
    - A sweep failure caught before transport open, during receive before a result, or during receive after a valid partial result reaches the merger from the Plan 13-03 typed sink exactly once; the pump does not wait for an exception that the generator has already absorbed.
    - Each absorbed failure emits exactly one DEBUG dictionary containing stable stage/reason and exception class name, without exception text or network/device/packet/TXT data.
    - Injected programming/invariant errors cancel and await all UDP, mDNS, probe, and generator work, then propagate the original error promptly.
    - Caller cancellation and consumer `aclose()` synchronously reap all owned work, preserve cancellation, and are never logged as unavailability.
    - UDP sends/results/deadline remain identical to UDP-only when mDNS fails before open, during receipt, or after a valid partial result.
  </behavior>
  <action>Pass one synchronous `failure_sink` into `_discover_verified_devices_mdns()` and convert both Plan 13-03 typed sweep/candidate values into exactly one caller-loop absorbed event. Consume the event emitted by each live conditional open/send/receive/follow-up catch; do not infer failure from normal exhaustion or recreate it at the outer pump. Keep an outer D-01 allowlist only for a genuinely escaping expected failure before the typed contract is active, and prove an event cannot take both routes. Treat `LifxUnsupportedDeviceError` as `candidate_unsupported`, drop only that candidate, and permit a later valid candidate/UDP leg to yield; do not route it to D-03 fail-fast. Ordinary receive timeout stays normal leg progress/completion and emits no failure category. Emit one structured DEBUG dictionary per D-02 containing only stage, reason, and error class. Preserve `discover_mdns()` as the no-sink direct delegate so newly added open/initial/retransmit catches re-raise and existing receive/follow-up catches retain their source-specific absorption/logging. For D-03, route non-allowlisted errors to a fatal event, immediately cancel every sibling/source/probe task, await them with `asyncio.gather(..., return_exceptions=True)`, close both generators, then re-raise the original error without waiting for the discovery deadline. For D-04, perform cancellation-resistant cleanup then re-raise cancellation without an availability diagnostic. Exercise the public merged path through actual mDNS open, initial/retransmit send, receive before/after a record, and follow-up seams; assert exactly one event/DEBUG, partial preservation where applicable, fresh later work, ambient-test hermeticity, and complete cleanup. Compare UDP bytes/destinations/send schedule/results/deadline with `discover_udp()`.</action>
  <acceptance_criteria>
    - Every expected sweep/candidate class has an isolated test and every injected unexpected `RuntimeError` propagates promptly after observed cleanup.
    - Before-open, receive-before-result, and receive-after-partial tests patch the actual `MdnsTransport` seam under `_discover_lifx_services_sweep()` rather than replacing the outer verified generator; each produces one merger event and one DEBUG record, and the partial-result case preserves its already verified device.
    - Each candidate drop produces exactly one sink event and exactly one DEBUG record while the sweep continues to a later valid candidate; no drop is silent and no event is logged twice.
    - Absorbed DEBUG records contain only stable category values and exception class; exception messages, targets, destinations, and synthetic identifiers are absent from `repr(caplog.records)`, including lower-level `MdnsTransport` records on the merged sink path.
    - Caller cancellation produces `CancelledError`, no availability log, no pending tasks, and closed source generators/connections/subscription.
    - A later call creates fresh mDNS work after success, empty completion, and each absorbed failure.
    - UDP-only and degraded-default schedule/result/deadline observations are equal.
    - Direct `discover_mdns()` no-sink tests retain the established standalone log/propagation contract and do not use the merged diagnostic injection.
    - An unsupported relay/button-only product advertisement produces one `candidate_unsupported` DEBUG event, no Echo request, no fatal merge error, and does not prevent a later supported candidate or UDP result.
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_api/test_api_discovery.py -q -k 'discover or failure or cancellation'</automated>
    <fails_when>non-zero exit, or pytest reports "0 passed" in the summary line</fails_when>
  </verify>
  <done>D-01 through D-04 distinguish availability from defects and cancellation exactly, with productive UDP degradation, privacy-safe diagnostics, and no leaked task, generator, connection, subscription, or socket.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Two independently fallible network legs to one public stream | Expected mDNS unavailability must not suppress UDP; unexpected defects must not become silent omissions. |
| Failure objects to structured DEBUG logs | Exception content may carry live network data and must not cross the logging boundary. |
| Process-wide UDP coordinator to measured caller | Caller observation ownership and deadline must cross explicitly; ambient context and unrelated discovery must not affect evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-13 | Spoofing | Cross-leg serial merge | high | mitigate | Normalise validated serials only after UDP validation or direct mDNS liveness; first fully valid queue event wins once. |
| T-13-14 | Denial of Service | mDNS availability failure and ambient test I/O | high | mitigate | Explicit expected-failure allowlist terminates only mDNS; an empty default test source prevents ambient multicast/private-value leakage, and schedule-equivalence tests prove UDP remains productive. |
| T-13-15 | Repudiation | Unexpected programming error | high | mitigate | Non-allowlisted errors fail fast after total cleanup and preserve the original exception rather than silently degrading. |
| T-13-16 | Information Disclosure | Absorbed failure diagnostics | high | mitigate | Route the live sweep catch's typed event exactly once, suppress its raw-text log only on the merged sink path, log stable stage/reason/type at the merger, and assert injected messages, addresses, identities, TXT, packets, and payloads are absent while standalone no-sink compatibility remains explicit. |
| T-13-16A | Information Disclosure | Merged observation evidence | high | mitigate | Carry the caller sink explicitly through the UDP subscription, scope mDNS/merge observations to the same call, and prove concurrent unrelated discovery contributes no evidence. |
| T-13-16B | Denial of Service | Replay and device construction | medium | mitigate | Enforce one caller-origin overall wall deadline before/after construction and before yield, without extending the producer-origin wire deadline. |
</threat_model>

<verification>
- Public merge/source-mode and failure-boundary tests pass, including actual mDNS transport open/receive failures before and after a partial result.
- Plan 13-01 invariant and Plan 13-02 coordinator suites remain green.
- Existing mDNS lifecycle tests remain green.
- Ruff and Pyright pass for API and tests.
</verification>

<success_criteria>
- `discover()` is concurrent, streaming, serial-first-valid, and fresh per call.
- Expected mDNS unavailability degrades to unchanged shared UDP behaviour; unexpected defects and cancellation never do.
- Explicit `discover_udp()` and `discover_mdns()` remain public and source-specific.
- Every owned async resource is reaped synchronously on exhaustion, early close, failure, and cancellation.
</success_criteria>

<output>
Create `.planning/phases/13-merged-discovery/13-04-SUMMARY.md` when done.
</output>
