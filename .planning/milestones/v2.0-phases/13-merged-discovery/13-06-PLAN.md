---
phase: 13-merged-discovery
plan: 06
type: execute
wave: 5
depends_on: [13-05]
files_modified:
  - .gitignore
  - .github/workflows/ci.yml
  - scripts/measure_merged_discovery.py
  - tests/test_scripts/test_measure_merged_discovery.py
  - .planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl
autonomous: false
requirements: [FIND-01, FIND-02, FIND-03, FIND-04, FIND-05, FIND-07, FIND-08, FIND-09, FIND-10]
user_setup:
  - service: "Physical LIFX fleet"
    why: "FIND-07 requires at least six paired fleet rounds and FIND-08 may require eligible firmware evidence that CI/emulator cannot supply."
    operator_actions:
      - "Create the ignored mode-0600 handoff `.planning/local/phase-13-merged-discovery/fleet-session.json` containing only the private alias-map path outside the repository plus truthful quiescence/confound categories; never include mapping contents or live values."
      - "Quiesce competing pollers where operationally possible and select the truthful quiescence/confound categories in that durable handoff."
estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "A focused completed-merge test invokes the exact emulator-mode `discover()` call and proves both legs are hermetic: UDP uses the owned loopback/dynamic port, mDNS uses the private context-local synthetic source and real loopback liveness, both sources contribute to the result, and no ambient multicast destination or socket is opened."
    - "Every emulator row is collected only after the OS-bound port is written back to `server.port` and every existing synthetic device's StateService state, a later `add_device()` inherits that verified non-zero port, and a real response proves the advertised port equals the bound endpoint."
    - "Emulator evidence is labelled `synthetic_mdns_lower_bound` and `single_round`; it is a hermetic lower bound rather than representative fleet behaviour, while physical fleet evidence is labelled representative."
    - "Canonical FIND-07 baseline rows identify `implementation_path: direct_udp`, while merged rows identify `implementation_path: merged_dual`, so coordinator cost is included in rather than held constant across both arms."
    - "The CI workflow and completed-merge test are locally verified and committed with GPG signature and DCO sign-off before any exact-revision CI run is triggered; the designated measurement checks out the immutable PR head separately, proves its `HEAD` equals that SHA before measurement, writes that verified SHA into both rows, and only a matching downloaded artefact may be appended."
    - "The Task 1 commit contains at least one source-changing path covered by the pull_request CI path filters, so the measured exact-head commit actually triggers ordinary CI; merge-ref tests remain separate from the immutable-head measurement checkout."
    - "The operator preparation checkpoint writes a durable ignored mode-0600 handoff containing only the external alias-map path, truthful fleet-quiescence state, categorical confounds, and readiness; mapping contents/live values are never committed, and all GitHub, CI, artefact, collection, and validation work remains executor-owned."
  prohibitions: []
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Named Ubuntu/Python 3.10 exact-PR-head paired emulator measurement boundary and pinned JSONL artefact upload while ordinary matrix tests retain merge-ref semantics"
    - path: scripts/measure_merged_discovery.py
      provides: "Explicit revision input used to stamp both CI measurement rows with the already-verified immutable checkout SHA"
    - path: tests/test_scripts/test_measure_merged_discovery.py
      provides: "Focused completed-merge proof for exact-call dual-source contribution, ambient-network exclusion, dynamic endpoint injection, and total success/failure/cancellation teardown"
    - path: .planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl
      provides: "Append-only direct-UDP baseline and merged-dual emulator/fleet arm records, including hermetic source contribution and FIND-08 disposition"
    - path: .planning/local/phase-13-merged-discovery/fleet-session.json
      provides: "Ignored mode-0600 cross-executor handoff containing the external alias-map path and categorical fleet readiness only; never tracked"
  key_links:
    - from: tests/test_scripts/test_measure_merged_discovery.py
      to: src/lifx/api.py
      via: "the Plan 13-06 test drives the completed merged arm through the measurement harness into the real discover() API with the script-owned loopback server and private mDNS source, before CI evidence is collected"
      pattern: "discover|paired|_override_mdns_service_source|MdnsTransport|127.0.0.1|port=0"
    - from: .github/workflows/ci.yml
      to: scripts/measure_merged_discovery.py
      via: "after the ordinary matrix test has used the root merge-ref checkout, the pull-request-only Ubuntu/Python 3.10 measurement boundary checks out `github.event.pull_request.head.sha` into a separate path, proves that checkout's `git rev-parse HEAD` equals the immutable event SHA, passes that verified value as the row revision, validates both rows against it, and only then uploads the raw JSONL using the workflow's existing pinned action"
      pattern: "pull_request.head.sha|git rev-parse HEAD|--revision|measure_merged_discovery|phase13-emulator-measurements"
    - from: "Task 1 signed commit"
      to: "Task 2 pull-request CI run"
      via: "the executor captures committed HEAD, pushes that branch, selects the pull_request run whose headSha equals HEAD, waits for success, and downloads the named artefact before validation and append"
      pattern: "git verify-commit|Signed-off-by|headSha|gh run watch|gh run download"
    - from: scripts/measure_merged_discovery.py
      to: src/lifx/network/mdns/discovery.py
      via: "emulator merged mode enters the private ContextVar source override around one discover() iteration; the normal sweep cannot construct MdnsTransport, while candidate liveness still reaches the owned loopback server"
      pattern: "_override_mdns_service_source|_discover_lifx_services|_discover_verified_devices_mdns"
    - from: scripts/measure_merged_discovery.py
      to: src/lifx/network/discovery_observation.py
      via: "each arm captures source contributions, cross-source duplicates, and raw FIND-08 identity observations from that exact timed call before alias-only serialisation"
      pattern: "_capture_discovery_observations|sources|winner|duplicate"
---

> **Path amendment (Plan 13-07):** Paths below describe this plan's original
> execution checkout. Current canonical locations and compatibility boundaries
> are mapped in [\`13-PATH-AMENDMENT.md\`](13-PATH-AMENDMENT.md).

> **Evidence-path amendment (2026-08-31):** The operator explicitly removed all
> Phase 13-only measurement steps from `.github/workflows/ci.yml`. That decision
> supersedes this plan's original nested-checkout, CI-produced artefact, and
> artefact-download requirements. For the final implementation revision,
> ordinary pull-request CI and the 100.00% Codecov patch gate remain the remote
> exact-head proof. The hermetic paired emulator measurement is instead run by
> the executor locally, after those gates pass, and both rows are stamped with
> that already-verified immutable revision. The rows must be described as local
> executor evidence and never as CI-produced or downloaded from an artefact.
> Physical-fleet collection remains deferred to Plan 13-07.

<objective>
Prepare Phase 13 evidence collection by landing and locally validating the CI measurement path, consuming exact immutable-PR-head CI evidence automatically, and pausing only for private fleet preparation.

Purpose: FIND-07 requires CI evidence from code GitHub has actually executed, while the operator-only alias and quiescence facts must be available before physical collection begins.
Output: A signed and DCO-bearing implementation commit, a successful exact-head pull-request CI run, two validated immutable CI rows appended to canonical JSONL, and a manual-only fleet-readiness checkpoint.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/13-merged-discovery/13-01-SUMMARY.md
@.planning/phases/13-merged-discovery/13-02-SUMMARY.md
@.planning/phases/13-merged-discovery/13-03-SUMMARY.md
@.planning/phases/13-merged-discovery/13-04-SUMMARY.md
@.planning/phases/13-merged-discovery/13-05-SUMMARY.md
@.planning/phases/13-merged-discovery/13-ENTRY-GATE.md
@.planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl
@.planning/phases/13-merged-discovery/13-CONTEXT.md
@.planning/phases/13-merged-discovery/13-SPEC.md
@.planning/phases/13-merged-discovery/13-RESEARCH.md
@.planning/phases/13-merged-discovery/13-PATTERNS.md

<interfaces>
`scripts/measure_merged_discovery.py` accepts the three D-14 modes, validates every row before append, loads a private alias map only from outside the repository, and regenerates the summary from JSONL using `--validate-only --summary`.

For `--environment emulator`, the script itself owns an in-process synthetic `EmulatedLifxServer`, awaits its bind to `127.0.0.1` on an OS-assigned port, writes that actual non-zero port back to `server.port` and every managed device's `state.port`, proves a device added after bind inherits that port, asserts a real StateService response advertises the same port, injects that exact endpoint into both sequential arms, and enters Plan 13-01's private `_override_mdns_service_source()` around the exact merged `discover()` call with a matching synthetic `_LifxServiceRecord`. `_discover_lifx_services()` must consume that source without constructing `MdnsTransport`; Plan 13-03's liveness verifier still sends the real correlated GetColor request to the owned loopback server. CI does not need and must not assume a separately started emulator or ambient mDNS service. Injected source, ContextVar token, liveness connection, and server all complete cancellation-resistant teardown.

The private observation scope surrounds each exact measured API call. UDP acceptance, raw mDNS TXT identity, verified source contribution, and merge winner/duplicate events are associated through the external alias mapping in memory. No auxiliary discovery sweep is permitted for source reconstruction or FIND-08 comparison, and no raw identity reaches JSONL or logs.

The established CI test matrix runs Ubuntu, macOS, and Windows for source/test pull requests. Reuse the existing pinned `actions/upload-artifact` commit; do not introduce an unpinned action or package install.
</interfaces>
</context>

## Artifacts this phase produces

- New named CI step `Run paired merged-discovery emulator measurement` in the Ubuntu/Python 3.10 matrix cell and uploaded artefact name `phase13-emulator-measurements`.
- `.gitignore` entry for `.planning/local/phase-13-merged-discovery/`; the manual checkpoint creates `fleet-session.json` there with mode 0600 so Plan 13-07 can consume it in a later executor session without tracking private values.
- Extended `tests/test_scripts/test_measure_merged_discovery.py` coverage of the completed merged `discover()` call, including dual-source contribution, dynamic loopback/port injection, ambient mDNS exclusion, and success/failure/cancellation teardown.
- Immutable JSONL rows whose required fields are `schema_version`, `kind`, `scenario_id`, `pair_id`, `round`, `arm`, `implementation_path`, `environment`, `revision`, `quiescence`, `confounds`, `elapsed_ns`, `first_result_ns`, `unique_count`, and per-alias `sources`; FIND-08 disposition rows carry only alias, eligibility category, and match/gap disposition.
- `.planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl` is extended only by the two validated rows downloaded from the exact immutable pull-request-head run.

## Review Feedback Addressed

| Concern | Disposition in executable plans |
|---------|---------------------------------|
| Ambient mDNS test hermeticity | Plans 13-04/13-05 install an empty default private source for public tests and require explicit synthetic opt-in; Plan 13-06 spies on all ambient transport/socket/destination paths. |
| Emulator dynamic-port advertisement | Plans 13-01/13-06 set `server.port` and every existing device state after bind, prove later `add_device()` inherits the bound port, and assert real StateService advertisement equals it. |
| Sole UDP accepted observation | Plans 13-01/13-02/13-04 use an explicit caller-owned sink across the coordinator boundary, emit exactly once per accepted raw response for that caller including replay, and isolate unrelated concurrent calls. |
| Consumer pacing change | Plan 13-02 documents/tests coordinator-driven pacing, slow consumers, and producer-origin deadlines. |
| Conditional missing mDNS catches | Plan 13-03 adds open/initial/retransmit catches only for the sink path and preserves no-sink propagation; existing receive/follow-up standalone behaviour remains exact. |
| Echo reachability and unsupported devices | Plan 13-03 records Echo as synthetic future coverage only; `LifxUnsupportedDeviceError` is candidate-local and Plan 13-04 proves it cannot fail the merge. |
| Shared deadline origin | Plans 13-02/13-04/13-05 preserve the producer-origin wire window and add a caller-origin overall wall deadline that bounds replay, construction, and yield without starting a fresh compatible sweep. |
| FIND-07 baseline integrity | Plan 13-01 keeps canonical baseline on direct `discover_devices()` and records `implementation_path`; merged rows include coordinator plus mDNS cost. |
| Invalid serial behaviour | Plan 13-05 intentionally tests ASCII-space stripping for valid input while malformed normalisation returns the established `None` and starts no network work. |
| Coordinator shutdown/fork/abandonment | Plan 13-02 adds bounded atexit shutdown, child reset, collected-subscriber detach, explicit Linux/fork-only guards, and deterministic subprocess coverage without sleeps, fixed interfaces, or signal handlers. |
| UDP construction fan-out | Plan 13-04 preserves one-at-a-time UDP device construction while source pumps remain concurrent. |
| Private measurement seams | Plan 13-01 retains them as unexported caller-context hermeticity seams that never bypass direct liveness and cannot change unrelated calls. |
| Direct `discover_devices()` / `find_by_label()` bypasses | Explicitly accepted as outside FIND-10's named default/UDP-only enumeration boundary and AC9 requires `find_by_label()` source behaviour unchanged. |
| Permanent CI measurement cost | Accepted as one Ubuntu/Python 3.10 step because FIND-07 requires current-revision emulator evidence; all other matrix cells incur no extra measurement run. |
| Nested immutable-head environment cost | Accepted: the isolated head checkout runs its own `uv sync --frozen`; the lockfile cache mitigates download cost, and exact-head dependency isolation is part of the evidence boundary. |
| Cross-session alias handoff | Plan 13-06 creates one ignored mode-0600 readiness file at a fixed local path; Plan 13-07 reads it directly, and neither mapping contents nor live values enter Git. |
| Evidence labels and variance | Emulator rows are `synthetic_mdns_lower_bound`/`single_round`; fleet rows are representative, and fleet baseline-count variance is reported categorically as advisory rather than a pass/fail threshold. |
| CI timeout and source-changing head | The measurement step has explicit `timeout-minutes`; Task 2 rejects a measured head without Task 1 source/test/workflow paths covered by pull_request CI filters. |
| Probe cap claim | The cap of 16 is documented as a reasoned D-07 safety choice, not a measured optimum. |

## Review Feedback Rejected

- Antigravity's TaskGroup claim is factually incorrect: Plan 13-04 explicitly uses Python 3.10 `create_task()` plus manual cleanup.
- Antigravity's `_resolve_targets()` claim is factually incorrect: that identifier does not appear in the submitted plans; Plan 13-03 names the real private record/sweep seams.
- Antigravity's same-loop-only redesign is rejected because it violates locked D-10/D-11 cross-loop process-wide sharing.
- No `sweep_timeout` diagnostic is planned because ordinary receive timeout is reachable only as normal retransmission/deadline progress, not an exceptional failure branch.
- Claude's unsigned-commit claim is rejected: host verification proves the latest eight commits have `G` status with required full key fingerprint `27B3A9EA9D847501F05627B166D6066620F03B05`. The existing signed+DCO gate remains mandatory.
- Claude's fresh-sweep remaining-window threshold is rejected because it conflicts with locked D-10 compatible-caller sharing unless the user changes CONTEXT.md.
- Antigravity's uncited claim that the caller-loop mDNS ContextVar solves UDP coordinator-boundary observation is rejected; it does not cross the process-wide coordinator thread/loop, so Plans 13-01/13-02 carry the caller sink explicitly.

<tasks>

<task type="auto">
  <name>Task 1: Add and locally validate the paired emulator CI path</name>
  <files>.gitignore, .github/workflows/ci.yml, scripts/measure_merged_discovery.py, tests/test_scripts/test_measure_merged_discovery.py</files>
  <read_first>
    - .gitignore
    - .github/workflows/ci.yml
    - .planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl
    - scripts/measure_merged_discovery.py
    - tests/test_scripts/test_measure_merged_discovery.py
    - .planning/phases/13-merged-discovery/13-ENTRY-GATE.md
    - .planning/phases/13-merged-discovery/13-SPEC.md
    - AGENTS.md
  </read_first>
  <action>Because this plan runs after Plan 13-05, extend `tests/test_scripts/test_measure_merged_discovery.py` with the completed-merge proof that Plan 13-01 could not execute while `discover()` was still UDP-only. Add `.planning/local/phase-13-merged-discovery/` to `.gitignore` for the later durable manual handoff. Drive the measurement harness through one exact `discover()` invocation rather than replacing that API with a fake: start the script-owned async emulator, await its `127.0.0.1`/`port=0` bind, capture the actual non-zero port, set both `server.port` and every existing `device.state.port` to it, add a device after bind and prove its state inherits the same value, and issue/assert a real StateService response advertising that non-zero port before measurement. Inject the endpoint into both arms and supply one matching synthetic `_LifxServiceRecord` only for the merged arm. Assert the known synthetic alias has both UDP and mDNS contribution from that exact caller-scoped observation sink after the real correlated GetColor liveness exchange; run unrelated synthetic discovery concurrently and prove it adds no evidence. Label emulator rows `synthetic_mdns_lower_bound` and `single_round`. Assert the baseline row uses `implementation_path: direct_udp` through the pre-share `discover_devices()` seam and the merged row uses `implementation_path: merged_dual`, so the coordinator is included in the measured public delta. Spy on `MdnsTransport` construction, ambient socket creation, and fixed multicast sends so any ambient path fails. Exercise normal completion, injected source failure, and cancellation; observe generator finalisation, ContextVar reset, liveness connection closure, and emulator shutdown before return/re-raise. Add an explicit `--revision` argument to the measurement script that accepts one 40-character lowercase commit SHA and stamps every row produced by that invocation with exactly that value. Add validate-only `--expected-revision` support that requires the requested immutable SHA on exactly the selected pair and rejects absent, mixed, malformed, or duplicate revision evidence; cover both arguments with positive and malformed/mismatch tests. In `.github/workflows/ci.yml`, preserve the existing root `actions/checkout` and `Run unit tests` steps unchanged so all ordinary matrix tests continue to execute GitHub's pull-request merge ref. Only after those tests, and only when `github.event_name == 'pull_request'`, `matrix.os == 'ubuntu-latest'`, and `matrix.python-version == '3.10'`, use the same pinned `actions/checkout` revision a second time with `ref: ${{ github.event.pull_request.head.sha }}`, `persist-credentials: false`, and `path: phase13-head`; that nested checkout is used solely for Phase 13 measurement. Give the following shell assertion step `id: phase13-head-revision`, `working-directory: phase13-head`, and `EXPECTED_SHA: ${{ github.event.pull_request.head.sha }}`; capture `VERIFIED_SHA` once from `git rev-parse HEAD`, require exact equality before dependency sync or measurement, then write `sha=$VERIFIED_SHA` to `GITHUB_OUTPUT`. In the subsequent measurement step, keep `working-directory: phase13-head`, set explicit `timeout-minutes: 5`, and bind `VERIFIED_SHA: ${{ steps.phase13-head-revision.outputs.sha }}` in `env`; run `uv sync --frozen`, one sequential paired emulator invocation via `uv run --frozen` with `--revision "$VERIFIED_SHA"`, and validate the runner-temporary JSONL via `uv run --frozen` with `--expected-revision "$VERIFIED_SHA"`. Before upload, independently require both row revisions to equal `VERIFIED_SHA`; upload `phase13-emulator-measurements` only after those checks using the existing immutable upload-action pin. Require baseline `sources: ["udp"]` and merged `sources` containing both categories from the one exact call. Store only categorical `owned_loopback_dynamic`, never the endpoint. Extend workflow-structure tests to prove the ordinary root checkout remains ref-less, the exact-head checkout is separate and pull-request-only, its immutable `ref` feeds the pre-measurement `git rev-parse HEAD` equality assertion and step output, the measurement consumes that output through `--revision`, exact-revision validation precedes upload, the measurement step has the explicit timeout, and ordinary merge-ref tests remain in place. Run the focused completed-merge test and local JSONL validator, but do not trigger GitHub Actions, download an artefact, or append CI rows in this task. On acceptance, the executor must create the Conventional Commit through the repository/GSD task-commit protocol with `git commit -S -s`; the commit must include at least one Task 1 source/test/workflow path matched by the pull_request CI path filters. Task 2 must not begin until this signed, DCO-bearing source-changing commit is `HEAD`. The raised estimate permits multiple executor commits if the executor needs them, but this mandatory measured task boundary remains exact.</action>
  <reversibility rating="reversible">The CI evidence step and artefact are repository-internal and removable after the contract changes; raw evidence remains append-only with corrective rows rather than rewrites.</reversibility>
  <acceptance_criteria>
    - `tests/test_scripts/test_measure_merged_discovery.py` exercises the completed merged `discover()` call itself and passes before evidence collection; it does not satisfy the proof by faking or bypassing `discover()`.
    - The local focused test produces two schema-valid rows containing the known synthetic alias and a positive unique count; baseline sources equal `["udp"]`, while merged sources contain both `udp` and `mdns` from the one exact call after loopback liveness.
    - Before either arm, the OS-bound port, `server.port`, every existing device `state.port`, a later-added device's state port, and the observed StateService advertised port are asserted equal and non-zero; any regression fails the focused test and CI step.
    - The baseline row is `implementation_path: direct_udp` and the merged row is `implementation_path: merged_dual`; validation rejects a shared-UDP baseline masquerading as the pre-change arm.
    - CI tests fail if emulator mode constructs `MdnsTransport`, opens/sends to the fixed multicast destination, or obtains any result from an unrelated network; success, injected failure, and cancellation all prove source/ContextVar/connection/server teardown.
    - Emulator rows are explicitly labelled synthetic-mDNS lower-bound and single-round; tests reject representative-fleet wording.
    - Workflow-structure tests prove the ordinary matrix checkout remains ref-less and continues to own the normal tests, while a separate pull-request-only Ubuntu/Python 3.10 checkout uses `ref: ${{ github.event.pull_request.head.sha }}`, asserts its `git rev-parse HEAD` equals that event SHA before measurement, passes the single verified value through `--revision`, validates both row revisions, and uploads the named artefact only afterwards with the workflow's existing immutable action pin.
    - This task leaves canonical `13-MEASUREMENTS.jsonl` unchanged; no CI row is accepted from a local run or an uncommitted checkout.
    - The executor's task commit is the mandatory boundary: it is a Conventional Commit, verifies with the required full GPG fingerprint, contains a `Signed-off-by` trailer and a pull_request-filter-matched source/test/workflow path, and precedes every Task 2 GitHub action.
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest tests/test_scripts/test_measure_merged_discovery.py -q &amp;&amp; uv run --frozen scripts/measure_merged_discovery.py --validate-only --output .planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl</automated>
    <fails_when>non-zero exit, pytest reports "0 passed" in the summary line, or validation rejects the immutable measurement rows</fails_when>
  </verify>
  <done>The completed-merge test and workflow-structure test pass locally; ordinary tests retain merge-ref semantics, the designated measurement has a tested immutable-head checkout/assert/stamp/validate/upload chain, and canonical evidence still contains no row derived from these uncommitted changes.</done>
</task>

<task type="auto">
  <name>Task 2: Consume the signed commit's immutable-head CI evidence</name>
  <files>.planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl</files>
  <read_first>
    - .planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl
    - scripts/measure_merged_discovery.py
    - .planning/phases/13-merged-discovery/13-ENTRY-GATE.md
    - .agents/skills/spike-findings-lifx-async/SKILL.md
    - .agents/skills/spike-findings-lifx-async/references/discovery.md
    - .planning/phases/13-merged-discovery/13-CONTEXT.md
    - .planning/phases/13-merged-discovery/13-SPEC.md
    - AGENTS.md
  </read_first>
  <action>Consume the mandatory Task 1 commit boundary before any remote action. Require a clean worktree, capture `EXPECTED_SHA` from `git rev-parse HEAD`, verify that exact object with `git verify-commit --raw`, require `VALIDSIG 27B3A9EA9D847501F05627B166D6066620F03B05`, its Conventional Commit subject, and at least one `Signed-off-by` trailer. Reject the historical unsigned-commit claim; host evidence confirms the latest eight commits are `G`, but this exact object still must pass the gate. Use `git diff-tree --no-commit-id --name-only -r "$EXPECTED_SHA"` to require Task 1's workflow/script/test changes and at least one path matched by the pull_request CI filters, so ordinary CI is triggered for the measured head. Push that exact phase-branch HEAD to origin. Resolve the pull request for the branch; if none exists, create a draft pull request targeting the repository default branch so the normal `pull_request` CI trigger runs the committed workflow. Never use `workflow_dispatch`, because this repository's manual workflow path can reach release/deploy jobs and is not the evidence boundary. Select only the `ci.yml` pull-request run whose `headSha` equals `EXPECTED_SHA`, record its run ID, wait with `gh run watch` until terminal, and require success. Download only `phase13-emulator-measurements` with `gh run download` into a newly created ignored temporary directory. Re-query the run and require its immutable head SHA still equals `EXPECTED_SHA`. Before touching canonical JSONL, run the measurement validator through `uv run --frozen` on the downloaded file with `--expected-revision "$EXPECTED_SHA"` and independently require exactly two rows: one `direct_udp` baseline with `sources: ["udp"]` and one `merged_dual` row with both `udp` and `mdns`; require matching pair/scenario metadata, integer fields, privacy schema, `synthetic_mdns_lower_bound`, and `single_round`. Record the canonical file's byte length and digest, append those validated artefact rows exactly once, then prove the previous byte prefix and digest are unchanged and rerun canonical validation with the same expected revision. Keep downloaded raw output and run logs out of tracked files and terminal summaries. The executor owns signature/DCO verification, push, PR creation/identification, exact-run selection, wait, download, validation, and append; any GitHub authentication failure becomes a dynamic auth gate rather than pre-planned manual work.</action>
  <acceptance_criteria>
    - The CI pair comes from the pull-request run whose `headSha` equals the signed Task 1 commit; that run's designated measurement separately checked out and asserted `HEAD` equal to the immutable PR head SHA before stamping both rows with it, the run succeeds, and both downloaded rows validate before append.
    - The canonical JSONL's pre-append byte prefix is unchanged, the two CI rows are appended exactly once, and neither a local substitute nor a workflow run for another revision is accepted.
    - The ordinary merge-ref test coverage remains part of the selected successful run; only the isolated measurement checkout uses the immutable PR-head revision.
    - The exact Task 1 commit contains at least one source/test/workflow path covered by pull_request CI filters; a documentation-only head is rejected before push/run selection.
    - No manual workflow dispatch, operator-run CI action, copied artefact content, unverified row, or second append is accepted.
  </acceptance_criteria>
  <verify>
    <automated>EXPECTED_SHA=$(git rev-parse HEAD) &amp;&amp; git verify-commit --raw "$EXPECTED_SHA" 2&gt;&amp;1 | grep -F 'VALIDSIG 27B3A9EA9D847501F05627B166D6066620F03B05' &amp;&amp; test "$(git show -s --format=%B "$EXPECTED_SHA" | grep -c '^Signed-off-by: ')" -ge 1 &amp;&amp; git diff-tree --no-commit-id --name-only -r "$EXPECTED_SHA" | grep -Eq '^(src/|tests/|scripts/|\.github/workflows/ci\.yml$)' &amp;&amp; uv run --frozen scripts/measure_merged_discovery.py --validate-only --expected-revision "$EXPECTED_SHA" --output .planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl</automated>
    <fails_when>the commit signature/DCO check fails, validation rejects the exact revision, the CI pair is absent or duplicated, or the canonical prefix changed</fails_when>
  </verify>
  <done>The immutable raw evidence contains exactly one validated emulator pair from the signed Task 1 commit's successful ordinary pull-request CI run, with both rows stamped by the workflow's independently asserted PR-head SHA.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Prepare the private fleet evidence session</name>
  <read_first>
    - .gitignore
    - .planning/phases/13-merged-discovery/13-CONTEXT.md
    - .planning/phases/13-merged-discovery/13-SPEC.md
  </read_first>
  <action>Create the fixed ignored handoff `.planning/local/phase-13-merged-discovery/fleet-session.json` with filesystem mode 0600. Store only `alias_map_path` pointing to the operator-controlled mapping outside this repository, the truthful `quiesced`, `not_quiesced`, or `unknown` category, stable categorical confounds, and readiness state. Do not embed mapping contents, aliases-to-live-value pairs, serials, addresses, hostnames, raw discovery output, or credentials. Confirm `git check-ignore` recognises the handoff and that the external mapping is readable locally. This manual-only file is the durable cross-executor-session input consumed by Plan 13-07; do not rely on an ephemeral shell environment variable or paste the private path into task chat.</action>
  <instructions>
    1. Confirm `.planning/local/phase-13-merged-discovery/` is ignored, create `fleet-session.json`, and set mode 0600.
    2. Set only its `alias_map_path` to the private mapping outside this checkout; never copy mapping contents into the handoff.
    3. Quiesce competing pollers where operationally possible, choose the truthful quiescence category, and add only stable categorical confounds plus readiness.
    4. Reply `ready` with the category and confound names only; do not reveal the path, run the measurement script, or copy evidence manually.
  </instructions>
  <acceptance_criteria>
    - The fixed handoff exists, is ignored by Git, has mode 0600, and remains available across executor sessions.
    - Its external alias-map path is readable, but neither the handoff nor task reply contains mapping contents or live identifiers.
    - Quiescence and confounds use only the allowed stable categories, and readiness is explicit.
  </acceptance_criteria>
  <verify>
    <human-check>The operator confirms the ignored mode-0600 handoff and readable external mapping are available, the fleet state has been checked, and the quiescence/confound categories are ready for executor use.</human-check>
  </verify>
  <verification>The operator confirms the ignored mode-0600 handoff and readable external mapping are available, the fleet state has been checked, and the quiescence/confound categories are ready for executor use.</verification>
  <resume-signal>Reply `ready` with the quiescence category and categorical confounds.</resume-signal>
  <done>Plan 13-07 can read one durable ignored readiness handoff in a later executor session without any mapping content or live identifier entering Git or chat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner artefact to canonical JSONL | The downloaded artefact is untrusted until revision, schema, pairing, integer, and privacy validation pass. |
| Completed-merge focused test and hermetic emulator context to merged discovery | The test must exercise the exact `discover()` call; a private context-local record source and owned dynamic loopback server must prevent all ambient mDNS socket/destination access while preserving real liveness verification and both source contributions. |
| Operator-only fleet preparation to the next automated plan | Only an ignored mode-0600 local handoff with an external path reference, quiescence category, and categorical confounds crosses executor sessions; mapping contents and live identifiers remain private. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-22 | Tampering | Downloaded CI measurement | high | mitigate | Preserve ordinary merge-ref tests, isolate the measurement in a second checkout pinned to the immutable PR head SHA, assert checkout `HEAD` equality before measurement, stamp and validate both rows with that one verified value before pinned-action upload, then repeat exact-SHA artefact checks before append. |
| T-13-25 | Information Disclosure/Denial of Service | Emulator merged-discovery source routing | high | mitigate | Make Plan 13-06 own the completed-merge test file; require the exact focused pytest command before evidence collection, ContextVar-scoped synthetic records, dynamic endpoint injection, negative `MdnsTransport`/ambient-socket/fixed-destination send spies, dual-source alias proof from one exact `discover()` call, and success/failure/cancellation teardown of source, connection, and server. |
| T-13-26 | Information Disclosure | Manual preparation checkpoint | high | mitigate | Use a fixed ignored mode-0600 local handoff, request only the external path reference and categorical fleet state, and explicitly prohibit mapping contents, live identifiers, raw discovery output, or path disclosure in chat/Git. |
</threat_model>

<verification>
- `uv run --frozen pytest tests/test_scripts/test_measure_merged_discovery.py -q` passes against the completed merged `discover()` implementation before CI evidence collection.
- The Task 1 commit verifies as GPG-signed and DCO-bearing before any push or CI selection.
- The exact successful pull-request run's immutable head SHA equals the Task 1 commit, and its two downloaded rows validate against that SHA before append.
- The workflow-structure test proves the ordinary matrix checkout remains ref-less while the isolated pull-request-only measurement checkout pins `github.event.pull_request.head.sha`, asserts `git rev-parse HEAD` equality before measurement, passes that verified SHA as both row revisions, validates them, and only then uploads.
- The exact emulator merged call records both sources for the owned alias while spies prove no ambient `MdnsTransport` destination/socket opens and every owned resource closes on success, failure, and cancellation.
- The human-action checkpoint creates only the ignored mode-0600 durable handoff and contains no executor-run collection or evidence automation.
</verification>

<success_criteria>
- Task 1 completes through the executor's real signed/DCO task-commit boundary before Task 2 starts.
- Task 2 performs every remote and artefact operation automatically against the exact immutable PR-head SHA and appends only validated CI rows.
- Task 3 pauses solely for the durable ignored alias-path/fleet-state handoff required by Plan 13-07.
</success_criteria>

<output>
Create `.planning/phases/13-merged-discovery/13-06-SUMMARY.md` when done.
</output>
