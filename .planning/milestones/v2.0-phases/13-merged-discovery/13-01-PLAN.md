---
phase: 13-merged-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lifx/api.py
  - src/lifx/__init__.py
  - src/lifx/network/discovery.py
  - src/lifx/network/discovery_observation.py
  - src/lifx/network/mdns/discovery.py
  - tests/test_api/test_api_discovery.py
  - scripts/measure_merged_discovery.py
  - tests/test_scripts/test_measure_merged_discovery.py
  - .planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl
  - .planning/phases/13-merged-discovery/13-ENTRY-GATE.md
autonomous: true
requirements: [FIND-02, FIND-07, FIND-08, FIND-09]
estimate:
  tokens: 24000
  raw_tokens: 48000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "[Edge R2 adjacency] The public baseline pins duplicate UDP responses and the first accepted serial winner before merged behaviour is introduced."
    - "[Edge R2 empty] The public baseline pins an empty discovery sweep yielding no device."
    - "[Edge R2 ordering] The public baseline preserves the first accepted record for a repeated serial."
    - "[Edge R2 repetition] The unchanged invariant assertions pass before merge implementation and remain reusable after it."
    - "[Edge R2 concurrency] Early close of public discovery synchronously closes the owned UDP generator before merge implementation."
    - "The measurement baseline always exercises the direct pre-share UDP path, so the final FIND-07 delta includes rather than hides the coordinator's contribution."
    - "An emulator bound with port zero writes the verified non-zero bound port to both `server.port` and every existing device state's StateService port before either arm runs, and a later `add_device()` retains that port."
    - "The observation scope creates a caller-owned sink that direct discovery receives explicitly; later shared discovery must carry that sink across its coordinator boundary instead of relying on ambient ContextVar propagation."
  prohibitions:
    - statement: "MUST NOT alter WiFi-measured discovery, request-retry, bandwidth, or animation constants in Phase 13."
      status: resolved
      verification: judgment
  artifacts:
    - path: src/lifx/api.py
      provides: "Public UDP-only discover_udp() baseline while discover() still delegates to UDP"
      exports: [discover, discover_udp, discover_mdns]
    - path: tests/test_api/test_api_discovery.py
      provides: "Pre-merge public discovery invariant suite"
    - path: scripts/measure_merged_discovery.py
      provides: "Mode-driven append-only paired measurement and validation harness with owned embedded-emulator lifecycle"
    - path: src/lifx/network/discovery_observation.py
      provides: "Private caller-owned source/identity observation sink captured only for the exact measured call"
    - path: src/lifx/network/discovery.py
      provides: "Explicit observation-sink input for direct UDP acceptance after validation/deduplication; shared delivery must pass the caller sink explicitly"
    - path: src/lifx/network/mdns/discovery.py
      provides: "Private context-local service-record source override that keeps emulator-mode merged discovery off the ambient mDNS destination"
    - path: .planning/phases/13-merged-discovery/13-ENTRY-GATE.md
      provides: "Immutable record that baseline invariants and harness passed before merged production code"
  key_links:
    - from: src/lifx/api.py
      to: src/lifx/network/discovery.py
      via: "discover_udp() owns discover_devices() with aclosing and discover() delegates to discover_udp()"
      pattern: "discover_udp|discover_devices|aclosing"
    - from: scripts/measure_merged_discovery.py
      to: src/lifx/api.py
      via: "baseline arm calls the private direct discover_devices() path matching the pre-merge discover() implementation, while merged arm calls discover() once inside a private observation scope"
      pattern: "discover_devices|discover|_capture_discovery_observations"
    - from: scripts/measure_merged_discovery.py
      to: lifx_emulator.EmulatedLifxServer
      via: "--environment emulator owns one loopback server on an OS-assigned port, injects that address/port into both arms, and supplies the merged arm's synthetic mDNS record from the same owned device"
      pattern: "EmulatedLifxServer|_override_mdns_service_source|127.0.0.1|port=0"
    - from: scripts/measure_merged_discovery.py
      to: src/lifx/network/mdns/discovery.py
      via: "the exact emulator-mode discover() call runs inside a private ContextVar source scope, so _discover_lifx_services() consumes the synthetic record generator without constructing MdnsTransport"
      pattern: "_override_mdns_service_source|_discover_lifx_services|ContextVar"
---

> **Path amendment (Plan 13-07):** Paths below describe this plan's original
> execution checkout. Current canonical locations and compatibility boundaries
> are mapped in [\`13-PATH-AMENDMENT.md\`](13-PATH-AMENDMENT.md).

<objective>
Establish the Phase 13 entry gate before any merged-discovery implementation: preserve today's UDP behaviour behind an explicit public `discover_udp()`, pin the public discovery invariants, and run the complete measurement harness in `baseline-only` mode.

Purpose: FIND-02 and FIND-07 require executable pre-change evidence rather than retrospective tests. This tracer is the production-quality baseline slice mandated before later waves may change `discover()`.
Output: Public UDP-only enumeration, unchanged baseline `discover()`, invariant tests, append-only measurement tooling, a synthetic baseline row, and `13-ENTRY-GATE.md`.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-merged-discovery/13-CONTEXT.md
@.planning/phases/13-merged-discovery/13-SPEC.md
@.planning/phases/13-merged-discovery/13-RESEARCH.md
@.planning/phases/13-merged-discovery/13-PATTERNS.md
@.agents/skills/spike-findings-lifx-async/references/discovery.md

<interfaces>
Current `src/lifx/api.py` owns public async generators `discover()` and `discover_mdns()` and wraps delegated generators with `contextlib.aclosing`. Current `discover()` delegates only to `discover_devices()`.

Current `src/lifx/network/discovery.py` exposes `discover_devices(timeout, broadcast_address, port, max_response_time, idle_timeout_multiplier, device_timeout, max_retries)` and preserves overall/idle deadlines, validation, first-wins serial deduplication, and endpoint cleanup.

Use `Serial.from_string(value).to_string()` as the single normalised 12-hex identity boundary. Python 3.10 is the floor; `asyncio.TaskGroup` is unavailable.

The measurement script runs with development dependencies under `uv run --frozen`. For `--environment emulator` it must create and own an in-process `lifx_emulator.EmulatedLifxServer`, bind only to `127.0.0.1` with `port=0`, read and validate the assigned non-zero port from the started datagram transport, write that port to `server.port` and every managed emulator device's `state.port`, add a later device and prove its advertised port cannot regress to zero, assert StateService advertises the verified port, pass that exact loopback address/port to the UDP parameters, and enter `_override_mdns_service_source()` around the exact merged `discover()` call with one matching synthetic `_LifxServiceRecord`. The override is private and context-local, causes `_discover_lifx_services()` to bypass `MdnsTransport`, and resets after the call; normal calls and public signatures remain unchanged. Pytest's session fixture is an analogue only; the CLI must not depend on fixture ownership.
</interfaces>
</context>

## Artifacts this phase produces

- Public async generator `lifx.api.discover_udp()` with the exact existing `discover()` parameters and defaults, exported as `lifx.discover_udp` and listed in both `__all__` declarations.
- Measurement functions `_build_measurement_row()`, `_append_measurement_row()`, `_load_measurements()`, `_validate_measurements()`, `_render_measurement_summary()`, `_eligible_find08_firmware()`, `main_async()`, and `main()` in `scripts/measure_merged_discovery.py`.
- Private frozen `_DiscoveryObservation`, caller-owned `_DiscoveryObservationSink`, context-local `_capture_discovery_observations()`, `_current_discovery_observation_sink()`, and value-suppressed emission helpers in `src/lifx/network/discovery_observation.py`; the ContextVar selects the sink only on the caller loop, while direct/shared producers receive the selected sink explicitly. None are public or exported.
- Private `_MdnsServiceSourceOverride` contract plus `_override_mdns_service_source()` context manager in `src/lifx/network/mdns/discovery.py`; a `ContextVar` scopes one async record-source factory to the exact caller context, and `_discover_lifx_services()` owns the injected generator with `aclosing()` instead of opening `MdnsTransport`.
- Script-private `_EmbeddedMeasurementEmulator` asynchronous context manager that owns one synthetic colour device/server, returns its validated loopback address/dynamic port and matching synthetic `_LifxServiceRecord`, and finishes injected-source and server shutdown before exit on success, error, or cancellation.
- CLI flags `--mode` (`baseline-only`, `merged-only`, `paired`), `--validate-only`, `--environment` (`emulator`, `fleet`), `--rounds`, `--timeout`, `--max-response-time`, `--idle-timeout-multiplier`, `--quiescence` (`quiesced`, `not_quiesced`, `unknown`), repeatable `--confound`, `--alias-map`, `--output`, and `--summary`.
- New files `scripts/measure_merged_discovery.py`, `tests/test_scripts/test_measure_merged_discovery.py`, `.planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl`, and `.planning/phases/13-merged-discovery/13-ENTRY-GATE.md`.

## Scope sanity disposition

Retain the public invariant tracer and measurement/evidence harness in one entry-gate plan. The
10-file frontmatter is the aggregate of three ordered task commits: Task 1 touches exactly five
files, Task 2 touches three, and Task 3 touches two; no task exceeds the planner's five-file split
threshold. The ordering is indivisible for FIND-02/FIND-07 and SPEC AC1: the observation/public-API
seams must exist before the harness can run, and the immutable gate record must name their committed
broadcast-only revision before Plan 13-02 changes UDP enumeration. Splitting these tasks into
separate plans would add no parallel execution path because every later Phase 13 plan must remain
blocked until all three entry-gate commits exist. Review found Task 2 materially heavier than its
earlier projection, so the calibrated estimate is raised to 24,000 tokens (48,000 raw at the
measured 0.5 factor); the executor may keep the required task order while using multiple signed,
DCO-bearing commits inside the plan if context pressure would otherwise compress implementation.
The estimate remains well
inside the 100,000-token smart-zone budget.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the broadcast-only public baseline through discover_udp()</name>
  <files>src/lifx/api.py, src/lifx/__init__.py, src/lifx/network/discovery.py, src/lifx/network/discovery_observation.py, tests/test_api/test_api_discovery.py</files>
  <read_first>
    - src/lifx/api.py
    - src/lifx/__init__.py
    - src/lifx/network/discovery.py
    - src/lifx/network/discovery_observation.py (create)
    - tests/test_api/test_api_discovery.py
    - tests/test_network/test_discovery_rebroadcast.py
    - tests/test_network/test_discovery_errors.py
    - .planning/phases/13-merged-discovery/13-SPEC.md
    - .planning/phases/13-merged-discovery/13-CONTEXT.md
  </read_first>
  <behavior>
    - `discover_udp()` has the current `discover()` signature/defaults and streams the same devices in the same order.
    - Before merged code exists, `discover()` delegates to `discover_udp()` and remains broadcast-only.
    - Empty, overall-timeout, consumer-resume idle-reset, first-wins duplicate, source-validation, serial-validation, exhaustion, early-close, cancellation, and error-cleanup cases are pinned through the public API.
    - `discover`, `discover_udp`, and `discover_mdns` are importable from both `lifx.api` and top-level `lifx`; no source-selector parameter is introduced.
    - `_capture_discovery_observations()` owns a sink in the caller context; direct `discover_devices()` captures it on that caller loop and passes it explicitly to `_discover_with_packet()`.
    - `_discover_with_packet()` emits exactly one accepted observation to the explicitly supplied sink after validation/deduplication and before its raw yield; a call with no sink remains inert.
  </behavior>
  <action>Add public `discover_udp()` by moving the current UDP-only body of `discover()` without changing its parameters, defaults, `discover_devices()` arguments, construction isolation, or `aclosing()` ownership. Keep `discover()` as a thin UDP-only delegate during this entry-gate task. Export `discover_udp` from `lifx.api` and `lifx` beside the two existing enumerators. Create the private value-suppressed `_DiscoveryObservationSink` and context-local `_capture_discovery_observations()` contract. The ContextVar is only a caller-loop selector: `discover_devices()` must capture the current sink at call entry and pass it explicitly into `_discover_with_packet()`, which emits exactly one UDP accepted event after all established validation and first-wins checks and immediately before its accepted raw yield. This explicit parameter is the later coordinator hand-off contract; no executor may assume ambient ContextVar propagation across an OS-thread/event-loop boundary. Add focused public tests that exercise inherited lower-level source/serial validation rather than mocking validation away, and freeze the overall deadline, consumer-body exclusion from the idle window, first-wins serial result, fresh post-completion state, synchronous generator cleanup, call-scoped isolation from an unrelated concurrent discovery, and exactly-one accepted-event rule required by FIND-02. This is the only production change allowed before the gate: an explicit name and private observation of existing behaviour per FIND-09, with no merged mDNS leg, tuning change, or new dependency.</action>
  <reversibility rating="costly">The new public name becomes a compatibility promise, but FIND-09 and the locked SPEC have already selected it; do not add a decision checkpoint that reopens the choice.</reversibility>
  <acceptance_criteria>
    - `inspect.signature(discover_udp)` equals the pre-change `discover()` signature and defaults.
    - Public baseline tests fail if source validation, serial validation, first-wins deduplication, overall timeout, resume-aware idle timeout, fresh completed-call state, or early-close cleanup is removed.
    - Top-level and module-level public surface assertions include all three enumeration names and reject a source-selector parameter.
    - No discovery/retry/bandwidth/animation constant changes in this task's diff.
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_api/test_api_discovery.py tests/test_network/test_discovery_rebroadcast.py tests/test_network/test_discovery_errors.py -q</automated>
    <fails_when>non-zero exit, or pytest reports "0 passed" in the summary line</fails_when>
  </verify>
  <done>The new UDP-only public entry point and unchanged public baseline pass the complete pre-merge invariant selection, and their delegate generators are closed on every terminal path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the append-only paired measurement and identity-evidence harness</name>
  <files>src/lifx/network/mdns/discovery.py, scripts/measure_merged_discovery.py, tests/test_scripts/test_measure_merged_discovery.py</files>
  <read_first>
    - scripts/measure_merged_discovery.py (create)
    - tests/test_scripts/test_measure_merged_discovery.py (create)
    - src/lifx/network/discovery_observation.py (create)
    - src/lifx/network/discovery.py
    - src/lifx/network/mdns/discovery.py
    - src/lifx/api.py
    - tests/conftest.py
    - scripts/ipv6_thread_probe.py
    - src/lifx/theme/schema.py
    - src/lifx/api.py
    - src/lifx/protocol/models.py
    - .planning/spikes/CONVENTIONS.md
    - .planning/phases/13-merged-discovery/13-CONTEXT.md
    - .planning/phases/13-merged-discovery/13-SPEC.md
  </read_first>
  <behavior>
    - `baseline-only` calls the direct `discover_devices()` path that pre-merge `discover()` used, `merged-only` calls `discover()`, and `paired` runs direct baseline then merged sequentially for every round; each row records `implementation_path` so direct and shared paths cannot be conflated.
    - `--environment emulator` starts one real in-process synthetic LIFX device on `127.0.0.1` and an OS-assigned non-zero UDP port, waits until its endpoint is bound, re-stamps every managed device's StateService port to that exact value, asserts the advertisement equals the endpoint, injects that UDP address/port into every selected arm, and scopes a matching synthetic mDNS record source around the exact merged `discover()` call.
    - While the private mDNS source scope is active, `_discover_lifx_services()` consumes and closes only that injected async generator; no `MdnsTransport`, ambient multicast destination, or ambient mDNS socket is constructed, and the scope resets on success, failure, and cancellation.
    - The measured API call runs once inside a private caller-owned observation scope; its sink is captured explicitly for that call, source events and duplicate contributions are converted immediately to aliases, and unrelated concurrent discovery cannot enter the row.
    - `_discover_with_packet()` emits exactly one private UDP `accepted` observation after all source/packet/service/address/serial validation and first-wins deduplication and immediately before yielding the accepted raw response when an explicit sink is supplied; direct baseline uses that parameter, while Plan 13-02 must carry the same caller sink explicitly through shared replay/fan-out without wrapper duplication.
    - One validated compact JSON object is appended per arm and round without changing existing bytes; rows retain integer monotonic nanoseconds/counts, nullable first-result nanoseconds, scenario/pair/round/arm/environment/revision metadata, quiescence, categorical confounds, aliases, and contributing source categories.
    - Validation rejects missing/duplicate arms, incomparable scenario metadata, forbidden identifier fields or values, malformed aliases, and a summary not reproducible from JSONL; row order is not semantic.
    - Firmware eligibility uses integer boundaries 3.70 through 3.99 inclusive, normalised `Serial` identities, duplicate collapse, and an explicit non-gating no-eligible-population disposition.
  </behavior>
  <action>Create the one measurement script per D-13 through D-16 using `time.monotonic_ns()`, strict JSONL validation-before-append, and deterministic summary generation. Consume the caller-owned observation contract created in Task 1: events carry source, stage, raw identity with a value-suppressed representation, optional integer firmware components, and connectivity only in memory; the scope resets in `finally`, is not exported, changes no public signature, and lets the script reconstruct accepted sources, cross-source duplicates, and winner order from the exact timed call. Capture the sink once at that call boundary and ensure unrelated concurrent discovery has a different or absent sink. Assert that direct `_discover_with_packet()` emits one UDP `accepted` observation after every established validation and deduplication check and immediately before the accepted `DiscoveryResponse` yield when given that sink. Plan 13-02 owns the explicit shared-subscriber delivery contract; no shared wrapper may rely on ambient propagation or emit more than once for a response delivered to the measured subscriber. Run the baseline arm through direct `discover_devices()` even after Plan 13-02 changes public `discover_udp()` to shared enumeration; record `implementation_path: direct_udp` versus `merged_dual` so FIND-07 measures the full public change including coordinator cost. In `src/lifx/network/mdns/discovery.py`, define private `_MdnsServiceSourceOverride` and `_override_mdns_service_source()` backed by a `ContextVar` token. This retained private hermetic-test/evidence seam is not exported, cannot change routing outside its caller context, and never bypasses the Phase 13 direct-response liveness gate. When an override is present, `_discover_lifx_services()` owns that source factory's async generator with `aclosing()` and never instantiates `MdnsTransport`; with no override, retain the exact normal mDNS sweep and standalone behaviour. Use an external `--alias-map` only to resolve live values in memory; reject mapping paths inside the repository and never serialise raw keys, raw values, addresses, hostnames, TXT content, packets, or exception text. Implement the exact three D-14 modes and sequential paired order plus short-window `--timeout`, `--max-response-time`, and `--idle-timeout-multiplier` passthroughs for focused tests; fleet/CI evidence uses documented defaults. For `--environment emulator`, create one synthetic colour light and `EmulatedLifxServer` inside a script-owned asynchronous context, bind to `127.0.0.1` with `port=0`, await `start()`, obtain and validate the bound port from `transport.get_extra_info("sockname")`, assign it to `server.port` and every existing device from the injected manager, add a second synthetic device after binding, and prove both devices advertise the same verified non-zero port so later `add_device()` cannot regress. Assert a real StateService reply advertises that value before either arm. Build one matching synthetic `_LifxServiceRecord`, pass the endpoint to both UDP parameter sets, and scope the override only around the exact merged iteration. Mark real-emulator tests `pytest.mark.emulator` (or an explicit equivalent timeout) so the repository's 120-second budget applies. Close the injected generator, reset its ContextVar, and stop the server through cancellation-resistant `finally` cleanup while preserving the original error or cancellation. Require at least six complete fleet pairs and current-revision emulator paired evidence independently, qualify non-clean runs per D-15, and retain FIND-08 transient identity comparison without another sweep. The operator script may use the audited development dependency `lifx-emulator-core`; keep runtime `dependencies = []` unchanged.</action>
  <acceptance_criteria>
    - Appending a row leaves every prior byte and line unchanged.
    - Tests cover all three modes, baseline-before-merged order, missing/duplicate/incomparable arms, exact integer storage, nullable first result only for empty output, deterministic regeneration, confound labelling, and privacy rejection before file open.
    - A real-emulator baseline test discovers the script-owned synthetic alias with `sources: ["udp"]`; a private-source contract test feeds the matching synthetic `_LifxServiceRecord` through `_discover_lifx_services()` in the same emulator context without constructing `MdnsTransport`.
    - The emulator test asserts the OS-bound endpoint port, `server.port`, every managed device `state.port`, a device added after bind, and the observed StateService advertised ports are the same non-zero integer before measurement.
    - Direct-versus-shared baseline tests prove final paired rows retain `implementation_path: direct_udp`; a separate focused observation may report shared UDP cost, but it cannot replace or contaminate the canonical FIND-07 baseline arm.
    - Observation spies prove exactly one UDP accepted event per accepted raw response delivered to the exact measured caller through direct discovery; an unrelated concurrent discovery contributes zero events, and Plan 13-02 repeats the rule for shared and late subscribers.
    - Transport spies prove the private emulator source scope never constructs `MdnsTransport`, opens an ambient mDNS socket, or sends to the fixed multicast destination; the bound dynamic UDP port is passed to both enumerator parameter sets without being serialised.
    - Success, injected source failure, and caller cancellation tests observe injected-generator finalisation, ContextVar reset, and server shutdown before control returns; Plan 13-06 repeats this proof through the completed merged `discover()` and its liveness connection.
    - Observation tests reconstruct `udp`, `mdns`, and dual-source aliases plus winner/duplicate order from one scope; reversed source completion reverses the recorded winner without changing the alias/source set, and no extra source-specific call is made.
    - Tests cover firmware tuples `(3, 69)`, `(3, 70)`, `(3, 99)`, `(4, 0)`, separator/case normalisation, duplicate observations, match/mismatch, and empty eligible population.
    - The harness reports observed deltas only and defines no slowdown ceiling or new network/retry constant.
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_scripts/test_measure_merged_discovery.py -q</automated>
    <fails_when>non-zero exit, or pytest reports "0 passed" in the summary line</fails_when>
  </verify>
  <done>The complete before/after harness is executable on the broadcast-only tree, owns a real synthetic emulator and context-local mDNS source when selected, cannot touch ambient discovery destinations, captures per-source observations from the exact measured call, and remains append-only and privacy-safe with paired/FIND-08 validation ready for post-merge evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Record the passing pre-merge entry gate</name>
  <files>.planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl, .planning/phases/13-merged-discovery/13-ENTRY-GATE.md</files>
  <read_first>
    - .planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl (create)
    - .planning/phases/13-merged-discovery/13-ENTRY-GATE.md (create)
    - scripts/measure_merged_discovery.py
    - tests/test_api/test_api_discovery.py
    - tests/test_network/test_discovery_rebroadcast.py
    - tests/test_network/test_discovery_errors.py
    - .planning/phases/13-merged-discovery/13-SPEC.md
  </read_first>
  <action>Before any merged-discovery commit, run the focused public/lower-level invariant selection and run `measure_merged_discovery.py` in `baseline-only` mode against its script-owned embedded emulator. Assert in process that the API received the bound dynamic loopback port, then append only the known synthetic alias, positive unique count, `udp` source contribution, and categorical `owned_loopback_dynamic` target marker to the canonical JSONL; never serialise the numeric endpoint. Create `13-ENTRY-GATE.md` recording the exact pre-merge revision, commands, aggregate outcomes, categorical emulator ownership/teardown proof, and the fact that `discover()` still delegated to UDP only; do not copy raw discovery output or identifiers. Include a value-suppressed diff check proving the established rebroadcast, idle, timeout, request-retry, bandwidth, and animation constants are unchanged. Later plans depend on this plan so commit ancestry makes the before/after ordering falsifiable.</action>
  <acceptance_criteria>
    - `13-ENTRY-GATE.md` names a revision before any merged-discovery implementation and records passing public invariants, lower-level invariants, measurement tests, and one executable baseline-only emulator run.
    - The JSONL contains a schema-valid baseline row with synthetic aliases and no live network or hardware value.
    - The baseline row proves at least one known synthetic device answered through the injected loopback/dynamic-port target using only a categorical target marker; an empty or ambient-network row cannot satisfy the gate and no numeric endpoint is tracked.
    - Git history places the entry-gate commit before every Plan 13-02 and later production commit.
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_api/test_api_discovery.py tests/test_network/test_discovery_rebroadcast.py tests/test_network/test_discovery_errors.py tests/test_scripts/test_measure_merged_discovery.py -q &amp;&amp; uv run --frozen scripts/measure_merged_discovery.py --mode baseline-only --environment emulator --rounds 1 --quiescence quiesced --output .planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl</automated>
    <fails_when>non-zero exit, pytest reports "0 passed" in the summary line, or the measurement command writes no validated baseline row</fails_when>
  </verify>
  <done>The executable baseline gate is committed before merge work, with immutable privacy-safe evidence that the pre-merge contract and harness passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UDP datagrams to discovery parser | Untrusted local-network packets cross established source, packet, service, and serial validation before acceptance. |
| Private emulator source scope to mDNS record stream | Only the script-owned synthetic record may enter emulator-mode merged discovery; ambient multicast I/O must remain unreachable. |
| Live fleet observations to tracked JSONL | Private identifiers are resolved through an external operator mapping and only stable aliases/categories may cross into repository evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Spoofing/Tampering | Public UDP discovery baseline | high | mitigate | Block completion on focused tests that exercise established source-ID, packet-type, service-port, serial, and first-wins validation through `discover_udp()`. |
| T-13-02 | Information Disclosure | Measurement writer | high | mitigate | Validate every row before append, reject raw identifier/address/hostname/packet fields and values, require external alias mapping, and run value-suppressed privacy tests. |
| T-13-03 | Repudiation | Before/after evidence | medium | mitigate | Store immutable arm/round rows with scenario, pair, revision, environment, quiescence, and confound metadata; generate summaries solely from raw JSONL. |
| T-13-04 | Denial of Service | Embedded measurement server | medium | mitigate | Bind only loopback on an OS-assigned port, inject that endpoint into each sequential arm, and block completion on success/failure/cancellation teardown tests that observe server transport closure. |
| T-13-04A | Information Disclosure/Denial of Service | Emulator mDNS source selection | high | mitigate | Scope a private synthetic record source around the exact merged call, bypass `MdnsTransport`, and block completion on negative transport-open/send spies plus source/ContextVar/connection/server teardown tests. |
</threat_model>

<verification>
- The pre-merge public and lower-level discovery invariant suites pass before later plans begin.
- `uv run --frozen pytest -o addopts='' tests/test_scripts/test_measure_merged_discovery.py -q` passes.
- Ruff format/lint and Pyright pass for every modified Python module.
- `13-ENTRY-GATE.md` and its baseline row contain only synthetic aliases/categories and record no live identifiers.
</verification>

<success_criteria>
- `discover_udp()` is the exact source-specific name for existing UDP enumeration, while pre-merge `discover()` remains behaviourally unchanged.
- FIND-02's public contract and inherited validation/deadline/lifecycle properties are executable before merged code.
- The complete D-13 through D-16 harness runs `baseline-only` on the pre-merge revision and preserves append-only raw evidence.
- No established tuning constant or runtime dependency changes.
</success_criteria>

<output>
Create `.planning/phases/13-merged-discovery/13-01-SUMMARY.md` when done.
</output>
