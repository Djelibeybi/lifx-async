---
quick_id: 260830-ea6
phase: quick-260830-ea6
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - pyproject.toml
  - tests/conftest.py
  - tests/test_api/test_ipv6_e2e.py
  - tests/test_network/test_discovery_devices.py
  - tests/test_network/test_discovery_errors.py
  - tests/test_network/test_discovery_rebroadcast.py
  - tests/test_network/test_mdns/test_discovery.py
must_haves:
  truths:
    - The suite retries an eligible transient network failure exactly once with no delay.
    - Ordinary assertion failures are not retried suite-wide.
    - Existing Windows emulator flaky markers retry their known assertion-shaped flakes without losing coverage for the already-approved network exceptions.
    - The synthetic mDNS packet-permutation test no longer depends on completing six permutations inside a 0.1-second wall-clock window.
  artifacts:
    - pyproject.toml
    - tests/conftest.py
    - tests/test_network/test_mdns/test_discovery.py
  key_links:
    - pyproject.toml retry defaults are constrained by tests/conftest.py::pytest_set_filtered_exceptions.
    - Windows-only flaky markers override the global exception filter with an explicit bounded exception tuple.
---

<objective>
Activate the existing pytest-retry policy and close the two gaps that currently allow cross-platform matrix flakes through: no configured global retry count, and assertion-shaped failures excluded even from explicitly flaky Windows emulator tests.

Purpose: Reduce transient CI noise while preserving deterministic assertion failures as first-attempt failures, and remove the recent synthetic mDNS test's avoidable scheduler dependency.
Output: A one-retry global policy, explicit Windows emulator retry exceptions, and deterministic packet-permutation coverage.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@pyproject.toml
@tests/conftest.py
@tests/test_api/test_ipv6_e2e.py
@tests/test_network/test_discovery_devices.py
@tests/test_network/test_discovery_errors.py
@tests/test_network/test_discovery_rebroadcast.py
@tests/test_network/test_mdns/test_discovery.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate one filtered suite-wide retry</name>
  <files>pyproject.toml</files>
  <action>Add pytest-retry's `retries = 1` and `retry_delay = 0` ini options under `[tool.pytest.ini_options]`. Keep the existing `pytest_set_filtered_exceptions()` hook authoritative, so the global marker automatically added by pytest-retry remains limited to `LifxTimeoutError` and `LifxConnectionError`; do not add `AssertionError`, broad `Exception`, or an unrestricted retry option to the global policy. Leave `retry_outcome` at the plugin default so retried tests remain visible in pytest output rather than being reported as ordinary first-attempt passes.</action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-uv-cache uv run --frozen python -c 'import pytest; config = pytest.Config.fromdictargs({}, []); assert config.getini("retries") == "1"; assert config.getini("retry_delay") == "0"'</automated>
  </verify>
  <done>Pytest loads one retry with zero delay from repository configuration, while the existing exception hook continues to exclude ordinary assertion failures globally.</done>
</task>

<task type="auto">
  <name>Task 2: Make the existing Windows flaky markers retry their actual failure shape</name>
  <files>tests/conftest.py, tests/test_api/test_ipv6_e2e.py, tests/test_network/test_discovery_devices.py, tests/test_network/test_discovery_errors.py, tests/test_network/test_discovery_rebroadcast.py</files>
  <action>Define named immutable exception tuples in `tests/conftest.py`: one for the globally retryable network exceptions and one Windows emulator tuple that adds `AssertionError`. Return the network-only tuple from `pytest_set_filtered_exceptions()` without widening the global policy. Import the Windows emulator tuple into each file containing one of the five existing `@pytest.mark.flaky` declarations and add it through the marker's `only_on` argument. Retain each marker's current Windows condition, retry count, and delay. Include both network exception classes and `AssertionError` in the explicit tuple because an individual `only_on` override replaces, rather than augments, pytest-retry's global filter.</action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-uv-cache uv run --frozen pytest --collect-only -q tests/test_api/test_ipv6_e2e.py tests/test_network/test_discovery_devices.py tests/test_network/test_discovery_errors.py tests/test_network/test_discovery_rebroadcast.py</automated>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-uv-cache uv run --frozen ruff check tests/conftest.py tests/test_api/test_ipv6_e2e.py tests/test_network/test_discovery_devices.py tests/test_network/test_discovery_errors.py tests/test_network/test_discovery_rebroadcast.py</automated>
  </verify>
  <done>All five pre-existing Windows flaky markers explicitly retry assertion-shaped emulator flakes plus the two approved network exception types, while non-marked assertions still fail immediately.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove wall-clock timing from the mDNS packet-permutation regression</name>
  <files>tests/test_network/test_mdns/test_discovery.py</files>
  <behavior>
    - Every TXT, SRV, and address packet ordering, including the empty packet and replay, yields exactly one equal service record.
    - Exhausting the scripted packets ends discovery through a controlled fake deadline rather than elapsed wall time.
    - The test needs no flaky marker and no globally retryable AssertionError.
  </behavior>
  <action>Refactor `test_generator_packet_permutations_yield_one_equal_record()` to use the file's existing `_fake_deadline()` seam. For each permutation, create a fresh deadline and scripted receiver; after the receiver has delivered the finite sequence, set that deadline's overall-expired state and raise `LifxTimeoutError` so the generator exits on its next loop check. Patch `IdleDeadline` to return that per-permutation deadline. Preserve all packet-order, empty-packet, replay, record-count, type, and equality assertions. Do not solve this synthetic test by increasing its real timeout or adding `@pytest.mark.flaky`: its inputs are in-memory and should complete independently of CI scheduler load.</action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-uv-cache uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py::TestMdnsFollowUpAddressQueries::test_generator_packet_permutations_yield_one_equal_record -q</automated>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-uv-cache uv run --frozen ruff check tests/test_network/test_mdns/test_discovery.py</automated>
  </verify>
  <done>The packet-permutation test deterministically exercises every existing case without a 0.1-second scheduling race or retry marker.</done>
</task>

</tasks>

<verification>
Run the targeted pytest and Ruff commands from all three tasks. Then run `UV_CACHE_DIR=/private/tmp/lifx-async-uv-cache uv run --frozen pytest` as the final repository gate; inspect the retry report if any test required a second attempt, and treat any final failure as a real failure rather than repeatedly rerunning the suite.
</verification>

<success_criteria>
- Repository pytest configuration enables exactly one zero-delay global retry.
- The global exception filter remains exactly the two approved transient network exception types.
- Every existing Windows flaky marker has an explicit exception override that includes assertion failures and preserves network failures.
- The recent mDNS packet-permutation failure is eliminated through deterministic test control, not hidden by retrying all assertions.
- Targeted tests, Ruff, and the full frozen suite pass.
</success_criteria>

<output>
Create `.planning/quick/260830-ea6-adjust-the-pytest-retry-configuration-as/260830-ea6-SUMMARY.md` when done.
</output>
