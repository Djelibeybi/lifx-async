---
phase: 08-hardware-fidelity-validation
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .claude/theme-capture/README.md
  - .planning/phases/08-hardware-fidelity-validation/tests/test_documentation_counts.py
autonomous: true
requirements: [FIDELITY-01]

estimate:
  tokens: 9000
  raw_tokens: 18000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Active planning/capture documentation distinguishes 26 raw exactly-16-colour records, including excluded sport theme Carlton, from exactly 25 shipped non-sport lifx-app themes; the stale 21 count is removed (SPEC R1 boundary; D-20)."
    - "Mechanical exact-set equality with the 25 shipped source slugs fails for any missing or extra ceiling row (SPEC R1 empty; D-20)."
    - "Ceiling determination identity is the shipped ASCII slug, not an emoji-bearing raw display name (SPEC R1 encoding; D-20)."
    - "The eventual evidence table is specified as sorted by ASCII slug for deterministic comparison (SPEC R1 ordering; D-20)."
    - "Ceiling membership is literal committed palette length 16 at uint16-normalised precision; no rounding or device inference selects rows (SPEC R1 precision; D-20)."
    - "Carlton is documented only as the raw sport record explaining 26 versus 25, never as a shipped Phase 8 target."
    - "All active count claims continue to state that a 16-entry device readback cannot establish a longer true source palette."
  artifacts:
    - ".planning/PROJECT.md — corrected Phase 8 ceiling scope at all three active claims"
    - ".planning/REQUIREMENTS.md — corrected FIDELITY-01 wording"
    - ".planning/ROADMAP.md — corrected Phase 8 success criterion from 26 to mechanically verified 25 shipped targets"
    - ".claude/theme-capture/README.md — raw 26 versus shipped 25 caveat replacing stale 21"
    - ".planning/phases/08-hardware-fidelity-validation/tests/test_documentation_counts.py — mechanical count and active-claim regression tests"
  key_links:
    - "data/themes.jsonl disposition+palette length filter -> 25 shipped slugs -> all active Phase 8 count claims"
    - ".claude/theme-capture/themes.jsonl raw palette length filter -> 26 records -> Carlton sport exclusion explanation"
    - "Documentation regression test -> PROJECT/REQUIREMENTS/ROADMAP/capture README; quoted historical/spec/pattern-map text is deliberately outside the scan"
---

<objective>
Correct the stale Phase 8 source counts and pin them mechanically. Active project,
requirements, roadmap and capture prose will say exactly: 26 raw exactly-16-colour
capture records; Carlton is an excluded sport record; therefore the shipped non-sport
`lifx-app` determination set has 25 rows. The protocol limitation remains explicit.

Purpose: remove the stale 26-shipped/21-raw ambiguity before official evidence is reviewed.
Output: four corrected active documents and a phase-local regression test derived from both
JSONL sources.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/08-hardware-fidelity-validation/08-SPEC.md
@.planning/phases/08-hardware-fidelity-validation/08-CONTEXT.md
@.planning/phases/08-hardware-fidelity-validation/08-RESEARCH.md
@.planning/phases/08-hardware-fidelity-validation/08-REVIEWS.md
@.claude/theme-capture/README.md
@data/themes.jsonl
@.claude/theme-capture/themes.jsonl
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 03 share)

- New file `.planning/phases/08-hardware-fidelity-validation/tests/test_documentation_counts.py`.
- Test functions `test_ceiling_inventory_is_25_shipped_from_26_raw()` and
  `test_active_phase_8_documents_use_correct_ceiling_counts()`.
- Corrected count paragraphs in `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`,
  `.planning/ROADMAP.md`, and `.claude/theme-capture/README.md`.
- No new public Python symbols, CLI flags, dataclasses or schema fields.

<planning_decisions>
- Plan 08-03 intentionally follows 08-02 in Wave 3. It is the only execution plan that edits
  `.planning/ROADMAP.md`, which avoids the review-identified Wave-1 coordination hazard with the
  tracer and keeps ROADMAP mutation out of a parallel worktree wave.
- The active-document scanner deliberately enumerates PROJECT, REQUIREMENTS, ROADMAP and the
  capture README. The review's broader-scanner suggestion is rejected for this phase because SPEC
  and PATTERNS intentionally quote the stale 26/21 defect; a repository-wide negative grep would
  make its own authoritative problem statement fail.
</planning_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Correct PROJECT, REQUIREMENTS and ROADMAP to 25 shipped targets</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/PROJECT.md (Phase 8 scope, current ceiling limitation and Active requirement sites)
    - .planning/REQUIREMENTS.md (FIDELITY-01 exact wording)
    - .planning/ROADMAP.md (Phase 8 success criterion 3; retain plan list written by planning)
    - .planning/phases/08-hardware-fidelity-validation/08-SPEC.md (authoritative 25/26/21 resolution and FIDELITY-01 acceptance)
    - data/themes.jsonl (mechanical shipped source; do not modify)
    - .claude/theme-capture/themes.jsonl (mechanical raw source; do not modify)
  </read_first>
  <reversibility rating="reversible">These are scoped factual prose corrections backed by committed data and can be reverted without code or schema effects.</reversibility>
  <action>
    Implement the SPEC R1 boundary resolution and D-20 in Australian English. Update every active
    Phase 8 claim in `.planning/PROJECT.md` that currently describes 26 as the shipped target set.
    State that the raw capture has 26 exactly-16-colour records, one is Carlton in the excluded
    AUSSIE RULES sport category, and the shipped non-sport `lifx-app` set therefore has 25. Preserve
    the protocol statement: a device readback cannot distinguish a true length of 16 from clipping.

    Rewrite FIDELITY-01 in `.planning/REQUIREMENTS.md` to bind exactly 25 shipped themes selected by
    `disposition == "lifx-app"` and palette length 16, note that all 10 Art Series themes are within
    that set, and explain the 26th raw record is excluded Carlton. Do not mark the requirement
    complete.

    Replace Phase 8 success criterion 3 in `.planning/ROADMAP.md` with the exact 25 shipped non-sport
    determination outcome and the 26-raw/Carlton distinction. Preserve the Phase 8 goal,
    requirements, dependency, hardware notes and plan list. Do not edit the SPEC/PATTERNS quoted
    stale-site examples; they are authoritative historical/problem descriptions, not active claims.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache uv run --frozen python -c "import json; from pathlib import Path; shipped=[json.loads(line) for line in Path('data/themes.jsonl').read_text().splitlines() if line]; raw=[json.loads(line) for line in Path('.claude/theme-capture/themes.jsonl').read_text().splitlines() if line]; s=sorted(r['slug'] for r in shipped if r['disposition']=='lifx-app' and len(r['colors'])==16); rr=[r for r in raw if len(r['colors'])==16]; assert len(s)==25 and len(rr)==26; assert [r['name'] for r in rr if r['name'].startswith('Carlton')]==['Carlton 🔵']; docs='\n'.join(Path(p).read_text() for p in ('.planning/PROJECT.md','.planning/REQUIREMENTS.md','.planning/ROADMAP.md')); assert '25 shipped' in docs and 'Carlton' in docs; print('25 shipped / 26 raw / Carlton excluded')"</automated>
  </verify>
  <done>All three active planning documents identify 25 shipped targets, explain the one-record raw difference with Carlton, retain the protocol ceiling limitation, and leave FIDELITY-01 pending.</done>
  <acceptance_criteria>
    - The automated command exits 0 and prints `25 shipped / 26 raw / Carlton excluded`.
    - `.planning/ROADMAP.md` Phase 8 criterion 3 says `25 shipped non-sport` and names the raw 26/Carlton distinction.
    - `.planning/REQUIREMENTS.md` FIDELITY-01 remains unchecked and defines membership from disposition plus literal length 16.
    - `.planning/PROJECT.md` no longer presents 26 as the shipped Phase 8 set at any active site.
    - No JSONL or generated theme data file is modified.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct the capture caveat and add a mechanical active-document regression</name>
  <files>.claude/theme-capture/README.md, .planning/phases/08-hardware-fidelity-validation/tests/test_documentation_counts.py</files>
  <read_first>
    - .claude/theme-capture/README.md (Caveats section and its stale 21 count)
    - .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md (Task 1 corrected claims the regression will scan)
    - .planning/phases/08-hardware-fidelity-validation/08-SPEC.md (exact active-document acceptance and exclusions)
    - data/themes.jsonl and .claude/theme-capture/themes.jsonl (both mechanical count sources)
    - .planning/phases/08-hardware-fidelity-validation/tests/test_documentation_counts.py (create RED tests here before changing README)
  </read_first>
  <behavior>
    - "The raw capture count is 26, the shipped lifx-app count is 25 and the raw-only sport member is Carlton."
    - "The capture README states all three facts and retains that device readback cannot reveal a 17th colour."
    - "PROJECT, REQUIREMENTS and ROADMAP contain no active claim that 26 or 21 shipped non-sport themes are at the ceiling."
    - "SPEC and PATTERNS are excluded from stale-claim scanning because their matching text documents the defect to be corrected."
  </behavior>
  <reversibility rating="reversible">The README correction and phase-local regression test are data-derived and isolated.</reversibility>
  <action>
    Replace the capture README's stale Caveats count with a precise paragraph: the raw capture has
    26 exactly-16-colour records; Carlton belongs to excluded AUSSIE RULES; the shipped non-sport
    `lifx-app` set has 25. Keep the warning that the wire carries at most 16 palette entries and
    therefore cannot prove a true source length or show clipping.

    Create the phase-local test module. The first test must parse both JSONL files, derive both counts,
    assert exact shipped set length 25, raw length 26, and raw-only excluded record Carlton. The second
    test must inspect only the four active documents modified by this plan and require the correct
    distinctions while rejecting stale active claims. Do not hardcode the 25 slugs. Keep the test out
    of ordinary package/runtime code; execute it explicitly as Phase 8 support verification.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache uv run --frozen pytest -o addopts='' .planning/phases/08-hardware-fidelity-validation/tests/test_documentation_counts.py -q</automated>
  </verify>
  <done>The README distinguishes raw and shipped sets, and two positive pytest tests mechanically bind all active count claims to committed data.</done>
  <acceptance_criteria>
    - The focused command exits 0 and reports exactly 2 passed tests.
    - The test derives 25 and 26 from the two JSONL inputs and identifies Carlton without a parallel slug list.
    - The README names 26 raw, 25 shipped and Carlton, and still says a device cannot expose a longer palette.
    - The active-document test deliberately enumerates only PROJECT, REQUIREMENTS, ROADMAP and the capture README.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw capture/shipped data -> active planning claims | Two related datasets differ by an excluded sport record and can be conflated. |
| Historical problem descriptions -> regression scanner | SPEC/PATTERNS legitimately quote stale claims and must not be mistaken for active truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-14 | Tampering | count correction | medium | mitigate | Derive 25/26/Carlton mechanically from both committed JSONL files and assert all active documents. |
| T-08-15 | Repudiation | FIDELITY-01 wording | medium | mitigate | Name filter predicates and the raw-only excluded record in REQUIREMENTS/ROADMAP, not only an unexplained number. |
| T-08-16 | Information disclosure | capture README | low | accept | The README contains public theme/category facts only; no LAN, device, tablet or account identity is added. |
| T-08-17 | Denial of service | stale scanner false positive | low | mitigate | Scope the regression to the four active documents and explicitly exclude SPEC/PATTERNS problem quotations. |
</threat_model>

<verification>
- The two focused documentation tests pass with exactly two collected tests.
- `git diff -- data/themes.jsonl src/lifx/theme/data.py` is empty.
- Repository search over the four active documents finds the correct 25-shipped/26-raw distinction and no stale active 21 claim.
</verification>

<success_criteria>
- ROADMAP success criterion 3 is corrected exactly as required by the phase brief.
- Requirements/project/capture prose agree with the mechanically verified shipped and raw data.
- The correction is protected by a deterministic, positive-count test.
</success_criteria>

<output>
Create `.planning/phases/08-hardware-fidelity-validation/08-03-SUMMARY.md` when done.
</output>
