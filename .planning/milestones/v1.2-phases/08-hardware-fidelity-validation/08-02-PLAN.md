---
phase: 08-hardware-fidelity-validation
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - .planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py
  - .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py
autonomous: true
requirements: [FIDELITY-01, FIDELITY-02, FIDELITY-03]

estimate:
  tokens: 42000
  raw_tokens: 84000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "The fixed samples straddle the wire boundary: cheerful resolves to 5 shipped colours and mondrian to exactly 16 before any cycle begins (SPEC R2 boundary; D-13, D-14)."
    - "Palette equality is an unordered multiset implemented only by Theme.palette_equals(): equal colours remain separate occurrences and duplicate counts must match (SPEC R2 adjacency and ordering; D-05)."
    - "Every comparison uses exact protocol uint16 HSBK tuples with no source-float or visual tolerance (SPEC R2 precision; D-05)."
    - "For each role, app cycles alternate Mondrian 1, Cheerful 1, Mondrian 2, Cheerful 2, Mondrian 3, Cheerful 3 from an operator-attested hidden Cheerful configuration; library cycles remain grouped Cheerful 1–3 then Mondrian 1–3. Every theme/source still has exactly three cycles and repetition cannot change or shorten the verdict (SPEC R2 idempotency; D-06/D-13)."
    - "2026-08-16 final supersession: each scheduled app cycle requires a fresh explicit `--attest-role` claim, scoped to role/run/binding digest and unable to cross the role transition. The Morph configuration hierarchy cannot prove selected label, group or count, but independently proves its in-panel controls. Automated production taps are limited to the exact enabled theme-button entry, category, exact theme and Save."
    - "2026-08-16 resume-evidence correction: mode-0600 checkpoints preserve each completed cycle's deterministic uint16 palette observations, explicit nullable stable palette, match and failure data. Resume rejects legacy partial data, non-prefix/duplicate/out-of-order keys and a mismatched next key rather than losing source evidence at the non-Tile checkpoint."
    - "2026-08-16 activation/restoration correction: Save alone does not start Morph. After each fresh static-OFF baseline, the first pending app cycle for an attested role taps the exact in-panel play button once and requires stable LAN MORPH/non-empty-palette proof before theme-button/category/theme/Save. Restoration writes once, then uses bounded stable full-state rereads at the configured poll interval; the failed preliminary live run remains private and non-finalisable."
    - "2026-08-16 mutation-boundary correction: only the durable initial checkpoint after both contacts, metadata/preflight and complete OFF snapshots permits activation or either cycle callback. Earlier failure is redacted incomplete exit 2 with best-effort closes only and no restoration verdict; after that boundary both captured roles restore on every terminal path, with restoration or close failure taking exit 3 precedence. The `fead749fc8d240c7bb637caf45df2708` run is pre-boundary, non-finalisable and has no mutation evidence."
    - "2026-08-16 alternating-picker correction: because LIFX cannot select the picker’s current theme and Morph UI cannot expose it, every invocation reaching an app key requires matching `--attest-role` plus `--attest-initial-theme cheerful`. This private claim is not UI evidence. Play precedes the first Mondrian Save, each later app Save alternates theme, and each key still performs exactly one picker selection and Save. A library-only resume may proceed until its next app key; legacy grouped checkpoints/provenance fail. Private run `1a5bdd47875c48cfa65d1c44f4934935` activated Morph and completed one Cheerful Save before its second picker failed on a current-Cheerful reselection; it is non-finalisable and yields no official evidence."
    - "Cycles are sequential, and interruption or an incomplete fixed cycle set cannot pass (SPEC R2 concurrency; D-06, D-11)."
    - "The non-Tile slot rejects product 55 and accepts only one responding real indoor non-Tile matrix product after live capability validation (SPEC R3 boundary; D-15, D-16)."
    - "Cross-product equality remains duplicate-sensitive, unordered and exact at uint16 HSBK precision (SPEC R3 adjacency, ordering and precision; D-05, D-06)."
    - "No responding approved non-Tile product produces human_needed/incomplete, never an empty success, Tile substitute or emulator pass (SPEC R3 empty; D-09, D-16)."
    - "All three app and all three library cycles for both themes run on the same recorded non-Tile product (SPEC R3 idempotency; D-06, D-11, D-16)."
    - "The selected device is tested sequentially and restored on interruption while unrelated devices remain untouched (SPEC R3 concurrency; D-15, D-17, D-18)."
    - "Two consecutive identical unordered readbacks establish stability; reset, timeout and every transitional read remain explicit evidence (D-05)."
    - "Because set_effect() is unacknowledged, a prior/transitional first read is retained and stability polling—not a fixed sleep or single read—establishes both library application and restoration completion (D-05, D-17)."
    - "A stable mismatch never truncates later cycles, changes palette data or suppresses restoration; it yields exit 1 and a failed official criterion (D-06, D-09)."
    - "Resume starts at the next unfinished cycle only when runner revision, app version, catalogue, private device identities/firmware, themes, ordering and timeouts exactly match (D-11)."
    - "Ceiling is preferred and Luna is the fallback, but either must pass the same live non-Tile matrix capability checks before mutation (D-16)."
    - "Every device snapshot contains power, base colour, complete effect settings in reported milliseconds, full matrix pixels and Ceiling component state where applicable; any missing pre-read or any running effect aborts before mutation so the static OFF pixel snapshot is exactly restorable (D-17)."
    - "Effect restoration converts reported milliseconds to setter seconds exactly, including a truthy sub-millisecond value whose setter conversion produces zero rather than the setter's falsy default-speed branch (D-17)."
    - "Official JSON is the authority and Markdown is derived from it; only a designated complete or definitively failed, restored run can be finalised (D-19)."
    - "The 25 ceiling determinations are derived by filtering data/themes.jsonl for disposition lifx-app and palette length 16, sorted by slug, without a second slug list or CSV (D-20)."
    - "Official finalisation uses a strict allowlist and validates exact slugs, cycles, comparisons, target class and restoration before writing either official file (D-21)."
  artifacts:
    - ".planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py — complete two-device runner, resume state machine and fail-closed finaliser"
    - ".planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py — branch-complete pure/injected tests for schedules, restoration, resume, schema, privacy and rendering"
  key_links:
    - "OFFICIAL_THEME_SLUGS x device roles x app/library x cycle indices -> deterministic 24-cycle schedule; checkpoint stores the next unfinished key"
    - "Each app schedule key -> current home hierarchy -> unique private app-label target -> verified configured group-control count `1` plus FX -> verified MORPH surface -> fixed category/exact theme -> current Save -> LAN stability polls"
    - "MatrixLight/CeilingLight snapshot APIs -> private RestorationSnapshot -> restore-and-reread verification -> terminal status"
    - "Private complete/failed run -> build_public_results() allowlist -> validate_public_results() -> JSON authority -> render_uat_markdown()"
    - "data/themes.jsonl -> derive_ceiling_determinations() -> exact sorted 25-row public table"
  prohibitions:
    - statement: "MUST NOT call undocumented LIFX theme endpoints or use identifiable cloud credentials to obtain a source palette"
      status: resolved
      verification: judgment
    - statement: "MUST NOT claim a true source length from a 16-entry device readback or claim fidelity beyond the products, firmware and cycles actually evidenced"
      status: resolved
      verification: judgment
    - statement: "MUST NOT commit a local IP address, device serial/MAC value or identifiable household label in validation evidence"
      status: resolved
      verification: test
    - statement: "MUST NOT normalise away a mismatch or modify captured/shipped theme data during validation"
      status: resolved
      verification: test
    - statement: "MUST NOT address, power or alter an unrelated fleet device or any Exterior device"
      status: resolved
      verification: test
    - statement: "MUST NOT leave a selected device in the test state after success, failure or interruption"
      status: resolved
      verification: test
    - statement: "MUST NOT substitute emulator or Tile evidence for the required real non-Tile product"
      status: resolved
      verification: test
---

<objective>
Expand the proven tracer into the complete, resumable hardware schedule and implement the
fail-closed official-evidence finaliser. The runner will execute the locked 24 real-hardware
cycles, restore capability-complete state on every path, and project only sanitised public
facts into the Phase 8 JSON/Markdown contracts.

Purpose: make FIDELITY-01..03 measurable without allowing incomplete, mismatched, private,
Tile-only or unrestored runs to appear successful.
Output: complete runner/finaliser behaviour and focused automated tests; Plan 08-04 performs
the real run and materialises official evidence.
</objective>

## 2026-08-16 execution supersession

Production app cycles are guided, passive operator actions. The runner must print the exact
role/theme request and use only read-only LAN MORPH observations to record the result. It must not
tap, scroll, select, save, play or recover any LIFX app surface. This supersedes the prior picker
automation after repeated zero-cycle failures proved slower than the prior day's 130+ manual
theme mappings. The locked 24-cycle order and evidence cardinality remain unchanged.

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-hardware-fidelity-validation/08-SPEC.md
@.planning/phases/08-hardware-fidelity-validation/08-CONTEXT.md
@.planning/phases/08-hardware-fidelity-validation/08-RESEARCH.md
@.planning/phases/08-hardware-fidelity-validation/08-PATTERNS.md
@.planning/phases/08-hardware-fidelity-validation/08-REVIEWS.md
@.planning/phases/08-hardware-fidelity-validation/08-01-SUMMARY.md
@.agents/skills/spike-findings-lifx-async/SKILL.md
@CLAUDE.md

<interfaces>
- Use the plan-01 `RunnerSettings`, `ThemeSpec`, `TargetBinding`, `PaletteObservation`, `CycleResult`, redacted ADB adapter and stable-readback contract directly.
- `MatrixLight.get_all_tile_colors()` returns one complete colour list per reported tile; `set_matrix_colors(tile_index, colors)` restores one tile.
- `MatrixLight.get_effect()` exposes speed in protocol milliseconds while `set_effect()` accepts seconds and treats falsy zero as its 3-second default; the runner must own and test the exact boundary conversion.
- `CeilingLight.get_uplight_color()` and `get_downlight_colors()` supplement the generic full-matrix snapshot.
- `Device.connect(host, serial)` plus live `DeviceVersion.product` and `get_product(product_id)` establish class/model/product metadata.
</interfaces>
</context>

## Artifacts this phase produces (plan 02 share)

- Dataclass `RunProvenance` with fields `runner_revision`, `app_version`,
  `catalogue_fingerprint`, `target_fingerprints`, `firmware_by_role`, `theme_records_sha256`,
  `schedule_sha256`, and `effective_settings`.
- Dataclass `EffectSnapshot` with fields `effect_type`, `speed_ms`, `duration`, `palette`,
  `sky_type`, `cloud_saturation_min`, and `cloud_saturation_max`.
- Dataclass `RestorationSnapshot` with private fields `power`, `base_colour`,
  `effect`, `chain`, `tile_colours`, `uplight_colour`, and `downlight_colours`.
- Dataclass `RestorationResult` with public fields `device_role`, `snapshot_complete`,
  `attempted`, `verified`, and `failure`.
- Dataclass `PublicDeviceRecord` with fields `role`, `device_class`, `model`, `product_id`,
  and `host_firmware`.
- Protocols/adapters `AdbAdapter`, `DeviceAdapter`, `CheckpointStore`, `ClockAdapter`,
  `FileSystemAdapter` and `EvidenceWriter`; every subprocess, clock/sleep, LAN call and filesystem
  operation in the runner crosses one of these injectable boundaries.
- Functions `build_cycle_schedule()`, `run_preflight()`, `catalogue_fingerprint()`,
  `build_provenance()`, `load_checkpoint()`, `write_checkpoint()`, `validate_resume()`,
  `run_official_cycles()`, `capture_snapshot()`, `restore_snapshot()`,
  `effect_speed_seconds_for_restore()`, `verify_restoration()`,
  `derive_ceiling_determinations()`, `build_public_results()`,
  `validate_public_results()`, `render_uat_markdown()`, and `write_official_evidence()`.
- CLI flags `--run`, `--resume RUN_ID`, `--finalise`, and `--validate-evidence PATH`.
  `--run` and `--resume` are mutually exclusive; `--finalise` is allowed only after the
  chosen run is complete or definitively failed and restoration is verified.
- Private checkpoint schema fields `run_id`, `provenance`, `next_cycle`, `cycles`,
  `snapshots`, `restorations`, `events_path`, `diagnostics_path`, `terminal_status`, and
  `finalisable`. The checkpoint remains mode 0600 and git-ignored.
- Public JSON root fields `schema_version`, `phase`, `run_id`, `runner_revision`,
  `app_version`, `catalogue_fingerprint`, `commands`, `themes`, `devices`, `cycles`,
  `ceiling_determinations`, `restorations`, `outcome`, and `completed_at_utc`.
- Public theme fields `slug`, `display_name`, `category`, `palette_count`,
  `record_sha256`; device fields `role`, `device_class`, `model`, `product_id`,
  `host_firmware`; cycle fields `device_role`, `theme_slug`, `source`, `cycle_index`,
  `stable_palette`, `poll_count`, `matches_expected`, `failure`; determination fields
  `slug`, `shipped_palette_length`, `determination`, `true_length`, `evidence_kind`,
  `evidence_citation`; restoration fields `device_role`, `snapshot_complete`, `verified`,
  `failure`.
- Permitted `outcome` values `pass`, `mismatch`, `incomplete`, `human_needed`, and
  `restoration_failure`; permitted determination values `device-ceiling-unresolvable` and
  `cited-source-length`.
- Output contracts `.planning/phases/08-hardware-fidelity-validation/08-UAT-RESULTS.json`
  and `.planning/phases/08-hardware-fidelity-validation/08-UAT.md`, materialised only by
  Plan 08-04 from an actual designated run.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand to the exact 24-cycle schedule with provenance-gated resume</name>
  <files>.planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py, .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py</files>
  <read_first>
    - .planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py (Plan 08-01 production tracer and CLI)
    - .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py (Plan 08-01 injected adapters and branch patterns)
    - .planning/phases/08-hardware-fidelity-validation/08-CONTEXT.md (D-03 through D-06, D-09, D-11, D-13 through D-16, D-18)
    - .planning/phases/08-hardware-fidelity-validation/08-SPEC.md (FIDELITY-02/03 cycle counts and real non-Tile acceptance)
    - data/themes.jsonl (derive both frozen theme records)
    - src/lifx/products/registry.py (product 55 exclusion, current Ceiling/Luna capability metadata)
  </read_first>
  <behavior>
    - "The schedule is exactly 24 unique ordered keys: per role, six alternating app keys starting Mondrian from a privately attested Cheerful configuration, then six grouped library keys."
    - "For each of the 12 app keys, the injected runner records home, unique exact bound app-label target selection, group-control count-one/FX verification, MORPH selection, MORPH-surface verification, derived category, exact fixed display name and current Save before the first LAN read."
    - "For either role, two exact matching device tiles or no exact matching tile exhaust exactly two fresh-hierarchy retries and stop before tap, Save, effect mutation or palette readback."
    - "A stable mismatch at cycle 1 is retained and cycles 2..24 still execute."
    - "Resume skips only completed keys and refuses any provenance change named in D-11."
    - "Product 55, an emulator marker, Exterior, non-matrix class or a changed app/LAN label fails the non-Tile preflight before mutation."
    - "No responding approved non-Tile target returns outcome human_needed and exit 2; no synthetic or source-Tile cycle is inserted."
  </behavior>
  <reversibility rating="reversible">The schedule and checkpoint schema are confined to an unshipped phase-local harness.</reversibility>
  <action>
    Expand the Plan 08-01 tracer per D-03 through D-06, D-09, D-11, D-13 through D-16
    and D-18. `build_cycle_schedule()` must return the literal logical order: role
    `source-tile` then `non-tile-matrix`; within each role app keys `mondrian` 1, `cheerful` 1,
    `mondrian` 2, `cheerful` 2, `mondrian` 3, `cheerful` 3; then library keys `cheerful` 1–3
    followed by `mondrian` 1–3. Before an invocation reaches an app key, require matching
    `--attest-role` and `--attest-initial-theme cheerful`; this is an operator claim because the
    current picker theme is not observable in Morph UI. For every app key,
    issue the declared `lifx:/home` VIEW deep link without force-stop, retry fresh dumps until Home
    proves each bound label or exact configured group card; a raw `Select lights` hierarchy is never
    Home even when it contains the bound label. Repeat that reset/proof before every scheduled app
    key; use the scheduled role's private
    `TargetBinding`, whose host+serial+live-LAN-label equality is frozen in D-18 provenance. From
    the immediately current hierarchy require exactly one device tile whose full semantic label
    equals `binding.app_label`; if absent, expand only accessibility-ID suffix
    `ax_device_list_group_card_button_<binding.app_group>`, then require exactly one right detail
    panel whose raw resource-ID basename is `detail_panel` and exactly one raw-text configured
    group heading centred within it. Before target selection, resolve only raw `Lights` as the
    known zero-selection marker inside that panel; a positive ASCII decimal count is an opaque
    pre-existing selection and fails closed. Tap only its unique smallest-area clickable container
    (never a global tab or unrelated numeric control), then require raw-text
    `Select lights` from a fresh hierarchy,
    then require that same unique label with at most five fresh hierarchy dumps: when absent, require exactly one current
    `android.widget.ScrollView` whose `scrollable` value is `true`, derive a lower-to-upper-quartile
    swipe from its current bounds, and fail closed on no progress, ambiguity or exhaustion; tap
    it exactly once; dump again and require the configured in-panel group heading and raw selector count exactly
    `1`. If exactly one raw `Select lights` label remains, tap only one unique clickable
    `ax_device_control_close_button` whose centre is inside and bounds horizontally overlap that
    detail panel and lie above the selector; then freshly require selector absence and exactly one
    FX control on the same group-control surface. If it auto-closes, require that direct
    selector-absent count-`1`/FX proof. A missing, duplicate or out-of-region close control fails
    without a second target tap, generic close control or Android Back; otherwise tap FX, then require MORPH;
    dump again and verify the MORPH
    surface before resolving the category and exact display name derived from the fixed theme
    record (`Moods`/`Cheerful`, then `Art Series`/`Mondrian`), reading the latter from JSON source
    key `name`. Match category headings by applying `derive_slug()` to both sides so app emoji and
    case do not alter `moods`/`art_series`; keep display-name matching exact and unique. Use the
    Plan 08-01 bounded semantic-scroll routine for every picker traversal, including Mondrian; do
    not retain a prior cell coordinate or blind-prefetch count. Then resolve the current
    `radio_theme`/`radio_currentcolors` state and the current `save_button`. Tap Save and only then
    begin LAN readback. The app order alternates from the hidden Cheerful baseline; the library
    order remains Cheerful then Mondrian, with no theme override.

    Resolve each step afresh rather than retaining coordinates. If any device label, device
    surface, MORPH control/surface, category, exact theme or Save control is absent or ambiguous,
    perform exactly two fresh-hierarchy lookup retries, write the semantic failure and diagnostics
    only to the restrictive private trace/checkpoint, then stop incomplete and restore. Ambiguous
    device labels must fail before any UI tap, Save, LAN/effect mutation or palette readback. Record every
    successful transition privately; public cycles and official evidence retain only role, theme
    slug, source and measured results. Inject the production state machine in tests and assert this
    ordered navigation prefix independently for all 12 app schedule keys, not merely the tracer.
    Library cycles call the existing MORPH setter. That setter is unacknowledged, so both sources
    use the same bounded stable-readback function, retain prior/transitional reads, and never use a
    fixed completion sleep or single read as authority.

    Continue all 24 scheduled keys after stable mismatches and record each mismatch/failure.
    Stop early only for preflight, semantic-control exhaustion, interruption or restoration-safety
    faults; none of those may produce pass. Live-validate source role as product 55/Tile. For the
    non-Tile role prefer the explicitly bound Ceiling and allow a bound Luna fallback, but in both
    cases require a real `MatrixLight` that is not product 55, is not marked emulator, is indoor,
    matches its app label and reports model/product/firmware. Do not use fleet discovery.
    Candle is not an accepted fallback: D-16 is the later locked refinement of SPEC's broader
    example list. A Candle binding or a site with only Candle available returns `human_needed`
    rather than silently broadening the device contract.

    Write a mode-0600 checkpoint atomically after every observation/cycle/state transition. Freeze
    every D-11 provenance dimension before first mutation. `--resume RUN_ID` must recompute and
    compare the entire provenance, refuse a named mismatch without altering the checkpoint, preserve
    every completed cycle, and begin at the first missing ordered key. Catalogue drift at any point
    invalidates the run. A missing/unresponsive non-Tile target sets `human_needed`, exits 2 and leaves
    FIDELITY-03 unsatisfied.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache uv run --frozen pytest -o addopts='' .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py -q -k "schedule or cycle or navigation or semantic or scroll or category or ambiguous or resume or provenance or non_tile or human_needed"</automated>
  </verify>
  <done>The runner produces exactly 24 deterministic cycle keys, proves the full unique-device-to-MORPH-to-theme-to-Save sequence before every app readback, preserves all outcomes, resumes only the same experiment, and cannot turn UI ambiguity or missing/Tile/emulated/Exterior secondary hardware into evidence.</done>
  <acceptance_criteria>
    - A schedule test asserts 24 unique tuples and the exact role/theme/source/index order.
    - A parameterised run over all 12 app keys asserts exact private-label device selection and verified device/MORPH surfaces precede derived category, exact cheerful/mondrian display name, current Save and first LAN read on every cycle; all 12 library keys bypass Android navigation and use MORPH setter/readback.
    - Zero-match and duplicate-exact-label fixtures for both roles exhaust the two locked retries and, after the required read-only LAN identity check, assert no tap, Save, device/effect mutation or palette readback; private trace receives the semantic failure while public result projection contains no label or raw transition.
    - A first-cycle mismatch fixture still records 24 results and terminal outcome `mismatch`.
    - Parameterised resume tests independently change every D-11 provenance field and assert refusal before any adapter mutation call.
    - Parameterised non-Tile tests reject product 55, emulator, Exterior, label mismatch and non-matrix; the no-response case asserts `human_needed` and exit 2.
    - A Candle-only fixture is rejected as `human_needed` per locked D-16; tests do not re-admit it from SPEC's earlier example list.
    - The focused pytest command exits 0 with a positive selected-test count.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture and verify capability-complete restoration on every exit</name>
  <files>.planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py, .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py</files>
  <read_first>
    - .planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py (Task 1 schedule/checkpoint state machine)
    - .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py (Task 1 tests and injected devices)
    - .planning/phases/08-hardware-fidelity-validation/08-CONTEXT.md (D-07, D-10, D-17 and D-18)
    - src/lifx/devices/light.py (get_color(), get_power(), set_color(), set_power())
    - src/lifx/devices/matrix.py (MatrixEffect fields, get_all_tile_colors(), set_matrix_colors(), get_effect(), set_effect())
    - src/lifx/devices/ceiling.py (Ceiling state, get/set uplight/downlight methods and component power semantics)
  </read_first>
  <behavior>
    - "Any missing power/base colour/effect/chain/pixel read prevents the first mutation; Ceiling also requires complete uplight and downlight reads."
    - "A pre-state effect other than FirmwareEffect.OFF fails preflight before mutation because moving pixels cannot supply the exact stable restoration baseline required by D-17."
    - "Effect speed 3000 ms restores through set_effect(speed=3.0); OFF speed 0 restores through a truthy sub-millisecond seconds value that rounds to protocol 0 and cannot select set_effect()'s falsy 3000 ms default."
    - "Success, mismatch, semantic failure, timeout, cancellation and KeyboardInterrupt all attempt device and Android restoration."
    - "Restoration first establishes stable OFF effect settings, then rewrites every captured tile including Ceiling component zones, restores base colour and power last, and re-reads every snapshot field plus Ceiling component projections."
    - "The unacknowledged effect restore retains transitional reads and requires two consecutive equal complete effect snapshots; no fixed sleep or one-read acknowledgement is accepted."
    - "A restore write or verification mismatch records restoration_failure and exit 3; it cannot be finalised as pass/mismatch."
    - "An app-label/LAN-label mismatch causes no snapshot mutation and no device command."
  </behavior>
  <reversibility rating="costly">This task mutates real device state when executed; complete snapshots, finally restoration and readback verification bound the recovery cost.</reversibility>
  <action>
    Implement D-07, D-10, D-17 and D-18 as the outer run lifecycle. Before the first app or
    library mutation, read and privately persist for both selected devices: power, the full
    `get_color()` triple's base colour, complete `MatrixEffect` configuration, reported chain,
    every colour of every tile, and for Ceiling its uplight colour and every downlight colour.
    Reject a partial snapshot before mutation. Store private identifiers only in the ignored
    checkpoint and log role names.

    `MatrixEffect.speed` from `get_effect()` is milliseconds; record it as `speed_ms`. Before any
    mutation, require `effect_type is FirmwareEffect.OFF` on both roles after two consecutive equal
    complete effect/pixel reads. A running MORPH/FLAME/SKY pre-state is a fail-closed incomplete
    preflight, because its changing pixels cannot be captured or verified as the exact baseline
    D-17 requires. The operator may place the selected lights into static/OFF effect state and
    rerun preflight; the runner must never downgrade moving pixels to diagnostic-only restoration.

    Wrap all cycles in cancellation-aware `try/finally`. Restore the captured OFF effect first
    through `set_effect()` so a later effect cannot overwrite pixels. Convert a non-zero snapshot
    with `speed=snapshot.speed_ms / 1000.0`. For `speed_ms == 0`, pass a named truthy positive value
    below 0.0005 seconds whose public setter conversion deterministically rounds to protocol zero;
    never pass falsy `0.0`, which selects the setter's 3000 ms default. Permit this zero workaround
    only for an OFF snapshot and test the actual `round(speed * 1000) if speed else 3000` boundary.
    Because the setter sends without acknowledgement, poll and retain complete effect readbacks
    until two consecutive settings equal the snapshot; do not sleep and assume completion.

    After OFF is stable, restore every captured tile with `set_matrix_colors()`, including the
    Ceiling uplight/downlight zones represented by its full tile, then restore base colour and
    original power last. Re-read and compare power, base colour, exact OFF effect/settings, full
    pixels, and the Ceiling component getters at protocol precision. Use public device APIs rather
    than hand-built packets. Restore Android keep-awake in an outer `finally` even if device restore
    raises.

    A restore exception, timeout or readback mismatch sets `restoration_failure`, exits 3, retains
    diagnostics/checkpoint and forbids official pass/mismatch finalisation. Tests must inject a
    fault at each restore stage and prove later restoration attempts, device close and Android
    restoration still happen. No best-effort success flag is allowed.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache uv run --frozen pytest -o addopts='' .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py -q -k "snapshot or static or speed or restore or cancellation or interrupt or ceiling"</automated>
  </verify>
  <done>Both roles have complete pre-mutation snapshots and exact post-run verification; every exit restores both devices and Android, while any restoration defect is a distinct terminal failure.</done>
  <acceptance_criteria>
    - Snapshot tests assert zero mutation adapter calls after each independently missing required read.
    - Snapshot tests reject MORPH, FLAME and SKY pre-state before mutation, and accept an exactly stable OFF/static baseline with power either on or off.
    - Unit tests assert 3000 ms becomes 3.0 seconds and OFF 0 ms uses a truthy sub-millisecond value that the real setter expression converts back to 0 rather than 3000.
    - Restoration tests cover Tile OFF-effect/pixels/base/power and Ceiling OFF-effect/pixels/uplight/downlight projections/base/power, with effect before pixels and power last.
    - Transitional restore-read fixtures prove unacknowledged set_effect completion comes only from two consecutive equal effect snapshots and no fixed sleep.
    - Success, mismatch, timeout, cancellation and interrupt fixtures all assert device close and Android prior-value restoration.
    - Every injected restore fault produces outcome `restoration_failure`, exit 3 and `finalisable == false`.
    - The focused pytest command exits 0 with positive selected-test count.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build fail-closed public JSON and derived Markdown finalisation</name>
  <files>.planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py, .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py</files>
  <read_first>
    - .planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py (Tasks 1-2 checkpoint, cycle and restoration contracts)
    - .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py (Tasks 1-2 fixture factories)
    - .planning/phases/08-hardware-fidelity-validation/08-CONTEXT.md (D-10, D-12, D-19, D-20 and D-21)
    - .planning/phases/08-hardware-fidelity-validation/08-SPEC.md (R1 table, replayable evidence acceptance and all seven prohibitions)
    - data/themes.jsonl (mechanical ceiling inventory; read-only and hash before/after)
    - src/lifx/const.py (MAX_PALETTE_COLORS=16 citation source)
    - .planning/milestones/v1.1-phases/03-retry-schedule-reshape/03-UAT-RESULTS.json (stable machine-readable evidence shape analogue)
    - .planning/phases/06-generated-theme-library/06-UAT.md (human review surface analogue)
  </read_first>
  <behavior>
    - "Mechanical ceiling derivation returns the exact sorted 25 shipped lifx-app slugs and excludes raw sport record Carlton."
    - "Every default determination states device-ceiling-unresolvable with shipped length 16 and cites the shipped data plus MAX_PALETTE_COLORS; a cited-source-length requires a legitimate citation and integer true_length >=16."
    - "Unknown public keys, IP/serial/MAC/household label/adb serial/account/token patterns, wrong cycle cardinality/order, missing mismatch, wrong non-Tile class/product, source hash change or unrestored role make validation fail before either official write."
    - "Markdown is a pure rendering of already validated JSON and contains the same outcome, device metadata, 24 cycles, 25 rows and restoration verdicts."
    - "A complete restored mismatch run is finalisable as failed evidence; incomplete, human_needed and restoration_failure are never rendered as pass."
    - "Every ADB subprocess, LAN call, clock/sleep, checkpoint/file operation and atomic evidence write is reached through an injected adapter, so the complete production runner—not an extracted subset—can achieve the SPEC-required 100% statement and branch gate without hardware."
  </behavior>
  <reversibility rating="reversible">The schema and renderer are phase-local and official files are atomically regenerated from a designated private run.</reversibility>
  <action>
    Implement D-10, D-12 and D-19 through D-21. Treat 100% statement and branch coverage of the
    entire phase runner as an architecture constraint from the first RED test, not a late cleanup.
    Route every subprocess, device/network call, monotonic clock/sleep, checkpoint/trace/
    diagnostic filesystem operation, temporary-file replace and official evidence write through
    the named injected adapter protocols in the artefact section. Production adapters perform the
    real I/O; deterministic fakes must execute every production branch including exceptions,
    cancellation and cleanup. Do not move untested branches to another module or weaken the gate.

    Derive the ceiling rows at finalisation by
    reading `data/themes.jsonl`, filtering `disposition == "lifx-app"` and `len(colors) == 16`,
    sorting by ASCII slug and requiring exactly 25 rows. Do not embed the 25 slugs in code or a
    fixture. The default permitted determination is `device-ceiling-unresolvable`, with
    `shipped_palette_length=16`, `true_length=null`, and citations naming `data/themes.jsonl` and
    `src/lifx/const.py:MAX_PALETTE_COLORS`. Only a legitimately accessible, explicitly cited
    non-device source may use `cited-source-length`; no endpoint or credential call is in scope.

    Construct the public record from an explicit field allowlist using the schema in the artefact
    section; never redact a private dict in place. Validate root/nested keys and scalar types,
    exact theme hashes/order, exact 24-cycle keys, each stable palette and comparison, mismatch
    retention, real non-Tile metadata, exact 25-slug equality, terminal-status consistency and both
    restoration verdicts. Record safe app version and catalogue digest, but reject tablet serial,
    account identity, token/cookie, host/IP, serial/MAC, app-visible household labels, raw hierarchy,
    screenshot paths and local diagnostic contents. Hash `data/themes.jsonl` and generated theme data
    before and after the run/finalisation; any change rejects finalisation.

    `write_official_evidence()` may run only for a designated complete or definitively mismatched run
    with verified restoration. Validate fully in memory, render Markdown solely from the validated
    JSON object, write both files to temporary siblings with mode 0644, reparse/revalidate both, then
    atomically replace the official pair together; on any failure leave neither partial new file.
    `human_needed`, incomplete and restoration failure remain private/checkpoint outcomes and cannot
    claim pass. After a full pass only, remove the private raw trace/screenshots/hierarchies while
    retaining the restrictive target file; on any non-pass retain diagnostics and print the private
    run directory.

    Add negative fixtures in pytest temporary directories for each SPEC test-tier prohibition and
    assert fail-closed rejection before writes. Judgment-tier tests assert the report wording never
    derives true length from a device and never broadens claims beyond public product/firmware/cycles.
  </action>
  <verify>
    <automated>COVERAGE_FILE=/tmp/lifx-phase8.coverage UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache uv run --frozen coverage erase &amp;&amp; COVERAGE_FILE=/tmp/lifx-phase8.coverage UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache uv run --frozen coverage run --branch --include='.planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py' -m pytest -o addopts='' .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py -q &amp;&amp; COVERAGE_FILE=/tmp/lifx-phase8.coverage UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache uv run --frozen coverage report --include='.planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py' --show-missing --fail-under=100</automated>
  </verify>
  <done>The finaliser mechanically derives all 25 rows, rejects incomplete/private/false evidence, preserves failed measurements, and can atomically create JSON authority plus Markdown review only from a restored designated run.</done>
  <acceptance_criteria>
    - A mechanical test prints `25 ceiling determinations` and exact set equality against the data-derived selection; Carlton is absent without a hardcoded 25-slug fixture.
    - Negative tests cover every unexpected-key level and each private identifier/address/account/raw-diagnostic pattern in both JSON and rendered Markdown.
    - Negative tests cover dropped/replaced mismatch, modified theme-source hash, Tile/emulator secondary, missing cycle, wrong order, unrestored device and partial pair write.
    - A validated synthetic pass and a validated synthetic mismatch each round-trip JSON -> Markdown; only the pass renders Phase outcome `pass`.
    - The coverage command runs the complete phase-local support suite, exits 0 with a positive pytest count, and reports 100% statement and branch coverage for `uat_theme_fidelity.py` with no missing branches.
    - Coverage fixtures exercise every production adapter success/failure boundary; no real ADB, LAN or private filesystem state is required for the 100% gate.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Private checkpoint -> resume engine | Mutable local state controls which real-hardware cycles are skipped or rerun. |
| Runner -> Tile/Ceiling/Luna | Commands alter complete matrix/effect/power/component state on real indoor hardware. |
| Private run -> public evidence | Identifiers, account/UI diagnostics and incomplete observations must not cross the publication boundary. |
| Public evidence -> verifier | A malformed or selectively normalised record could make failed or partial UAT appear successful. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-07 | Tampering | resume checkpoint | high | mitigate | Exact D-11 provenance equality, atomic mode-0600 writes and tests changing each dimension independently. |
| T-08-08 | Tampering | device restoration | high | mitigate | Pre-mutation complete snapshots, finally restoration, exact readback verification and terminal exit 3 on any defect. |
| T-08-09 | Information disclosure | public projection | high | mitigate | Build from allowlisted safe fields, identifier/content-pattern scans and negative privacy fixtures before atomic writes. |
| T-08-10 | Repudiation | mismatch/cycle record | high | mitigate | Append-only observations, exact 24-key completeness/order, mismatch retention and terminal-status consistency checks. |
| T-08-11 | Spoofing | non-Tile evidence | high | mitigate | Live typed MatrixLight, product !=55, non-emulator product metadata, private/app identity agreement and human_needed on absence. |
| T-08-12 | Information disclosure | Android app identity/session | medium | mitigate | Record version and catalogue digest only; reject account/token/cookie/tablet identifiers and raw hierarchy/screenshot material. |
| T-08-13 | Denial of service | bounded polls/UI retries | medium | mitigate | Explicit recorded deadlines, two semantic retries, sequential cycles and cleanup in finally. |
</threat_model>

<verification>
- **2026-08-16 execution supersession:** A post-failure `--role-only non-tile-matrix`
  reconciliation run may preflight or run Luna from a fresh static-OFF baseline using
  `--attest-role non-tile-matrix` (and `--attest-initial-theme cheerful` for `--run`). It is
  never resumeable or finalisable, never contacts Tile, and preserves only a private 12-key
  checkpoint/result for later human reconciliation. Its role-complete exit cannot satisfy the
  official 24-cycle evidence contract.
- **2026-08-16 non-Tile settling supersession:** Guided `non-tile-matrix` app observations wait
  the recorded default 5.0 seconds after a fresh complete non-empty MORPH trigger, discard that
  trigger, then begin a new stability window. Two equivalent complete non-empty post-settle
  palettes must still differ from the prior completed app palette; OFF, incomplete, unchanged or
  unstable post-settle reads fail closed. The operator-action timeout covers trigger detection
  only, the settle interval cannot consume the post-settle stability timeout, and the Tile gets no
  added wait. The finite non-negative `--non-tile-settle-duration` is provenance/private-trace
  material. The earlier one-cycle Luna-only Mondrian observation predates this rule and remains
  private, non-finalisable, provisional and non-resumable.
- All phase-local tests pass with a positive collected count; hardware adapters remain injected in these tests.
- `UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache uv run --frozen ruff check .planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py` exits 0.
- `UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache uv run --frozen ruff format --check .planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py` exits 0.
- `UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache uv run --frozen pyright .planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py` reports 0 errors under the repository's standard Pyright configuration.
- Task 3's `coverage run --branch` plus `coverage report --fail-under=100` command exits 0,
  reports 100% statement and branch coverage scoped to `uat_theme_fidelity.py`, and has a
  positive pytest count. This proves support code only; Plan 08-04 real-hardware UAT remains
  the independent authority for FIDELITY-02/03.
</verification>

<success_criteria>
- The complete runner can execute, resume and truthfully classify all 24 locked cycles.
- Every selected device and Android global setting is restored and verified on every exit.
- Public finalisation is schema-allowlisted, privacy-safe, cycle-complete, mismatch-preserving and atomic.
- The 25-row determination set is generated only from the shipped data source.
</success_criteria>

<output>
Create `.planning/phases/08-hardware-fidelity-validation/08-02-SUMMARY.md` when done.
</output>
