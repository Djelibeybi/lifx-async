---
phase: 08-hardware-fidelity-validation
plan: 04
type: execute
wave: 4
depends_on: [08-02, 08-03]
files_modified:
  - .planning/phases/08-hardware-fidelity-validation/08-UAT-RESULTS.json
  - .planning/phases/08-hardware-fidelity-validation/08-UAT.md
autonomous: false
requirements: [FIDELITY-01, FIDELITY-02, FIDELITY-03]
user_setup:
  - service: local-real-hardware
    why: "The app/device fidelity claims require the approved Android tablet, source Tile and one indoor non-Tile matrix product; no emulator or alternate Tile can substitute."
    dashboard_config:
      - task: "Create .planning/local/phase-08-theme-fidelity/targets.json with exactly source-tile and non-tile-matrix bindings plus indoor_confirmed=true and quiesced_confirmed=true; chmod the directory 0700 and file 0600."
        location: "Local ignored Phase 8 private root"
      - task: "Connect and authorise exactly one unlocked Android tablet with the signed-in com.lifx.lifx app."
        location: "USB/ADB and LIFX app"
      - task: "Quiesce Home Assistant, alternate LIFX app sessions and other pollers for both selected lights for the duration of the run."
        location: "Local home-automation environment"
      - task: "Before preflight, place both approved lights in static state with FirmwareEffect.OFF; a running effect is rejected before mutation because exact pixel restoration cannot be established."
        location: "LIFX app or authorised local control"

estimate:
  tokens: 12000
  raw_tokens: 24000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "A private mode-0600 target file, one authorised unlocked tablet and operator-confirmed quiescence exist before the hardware run; the runner itself still revalidates all preflight facts (D-03, D-12, D-15, D-18)."
    - "For each role, official app evidence alternates Mondrian 1, Cheerful 1, Mondrian 2, Cheerful 2, Mondrian 3, Cheerful 3 after a private Cheerful starting-theme attestation; library evidence remains grouped Cheerful 1–3 then Mondrian 1–3. Every poll/failure remains local and every stable result is represented publicly (D-01, D-02, D-05, D-06, D-13, D-14)."
    - "The same fixed sequence runs on one live-validated indoor Ceiling or Luna non-Tile matrix product; no response leaves FIDELITY-03 human_needed rather than passed (D-06, D-15, D-16)."
    - "A Candle-only site remains human_needed: D-16's locked Ceiling/Luna choice is not broadened from SPEC's earlier example list."
    - "2026-08-16 final user-approved supersession: this is once-off UAT, not recurring hardware validation. `--attest-role` is the operator's explicit configured-target/role claim, separately required for source preflight and every role's app work. The Morph configuration hierarchy cannot prove selected label, parent group or count; it only proves detail-panel in-panel `Morph`/`Effect`/configuration-scroll/enabled-theme-button semantics. Preflight and app work never Home/group/selector/close/effect navigate or select a target. Preflight does not choose a theme or Save; an app cycle enters through the exact theme button, then chooses category, exact theme and Save. The private record separately retains operator-attested role/run/binding digest/timestamp and UI Morph booleans; exact LAN identity bridges the claim. Loss or ambiguity hard-stops to a manual reset/reposition checkpoint; source cannot authorise the non-Tile transition."
    - "2026-08-16 read-only contact clarification: after the observed exhausted single preflight connection but successful isolated fresh contact, only the initial bound-device adapter may make one fully fresh second connection after an expected contact failure and partial close. Metadata, snapshots, app Save, palette reads and every mutation remain one-shot; a second failure is redacted incomplete. This phase-local containment does not alter library retry policy."
    - "2026-08-16 private observability clarification: the mode-0600 private trace records only fixed preflight-stage role/stage/status facts for attestation, contact/fresh retry, metadata, snapshot and completion or failure. It contains no target identity, exception text or raw hierarchy, and public stderr/evidence is unchanged."
    - "2026-08-16 MatrixLight integration correction: a healthy source snapshot established `get_device_chain()` as the current chain API. The phase-local snapshot helper uses it; no preflight, restoration or mutation contract changes."
    - "2026-08-16 resume-evidence correction: the private mode-0600 checkpoint persists full CycleResult palette observations, explicit nullable stable palette, match and failure evidence. Resume accepts only a strict ordered 24-key prefix and matching next key, so the mandatory source-to-non-Tile manual checkpoint cannot erase source evidence; legacy partial checkpoints fail closed."
    - "2026-08-16 activation/restoration correction: Save alone did not activate the stopped Tile Morph effect. After the static OFF baseline, each attested role's first pending app cycle taps the exact in-panel play button once and requires stable read-only LAN MORPH/non-empty-palette proof before theme selection. Restoration writes only once and waits through bounded stable full snapshots at the configured poll interval; the failed preliminary run stays private, non-finalisable and absent from official evidence."
    - "2026-08-16 mutation-boundary correction: a run becomes mutation-capable only after both contacts, metadata/preflight, complete static snapshots and durable initial private checkpoint. Pre-boundary contact, metadata, snapshot or checkpoint failure is redacted incomplete exit 2 with best-effort closes and no restoration claim; post-boundary every terminal path restores both captured roles and restoration/close faults take exit 3 precedence. `fead749fc8d240c7bb637caf45df2708` is a private pre-boundary failure with no mutation evidence."
    - "2026-08-16 alternating-picker correction: before a run or resume reaches any app key, the operator manually sets the scheduled role’s static OFF Morph configuration to Cheerful and supplies matching `--attest-role` and `--attest-initial-theme cheerful`. The claim is private and separate from the observed Morph controls; missing/wrong claims stop before play or Save. App Saves then alternate Mondrian/Cheerful, avoiding current-theme reselection; library-only resumes continue only until the next app checkpoint. The changed schedule hash rejects old grouped checkpoints. `1a5bdd47875c48cfa65d1c44f4934935` is a private non-finalisable failed run: activation passed, one Cheerful Save completed, then a second picker failed because Cheerful was current and could not be reselected."
    - "2026-08-16 picker/restoration correction: the Android picker has no category switch. After the exact theme button, the runner observes headings only, finds the exact theme in fresh hierarchy dumps and, for off-screen Mondrian, bounded-scrolls exactly one current scrollable picker using its fresh bounds; it never taps a category control and stops on ambiguous, stale, repeated or exhausted state. `12dc7a6b8e994077b664bd2b71999ce3` is private, non-finalisable and has zero completed cycles because the old runner searched for a category control. `MatrixLight.set_color()` is device-wide, so restoration writes base colour before exact per-tile pixels and verifies stable exact pixels; static Tile geometry is compared but accelerometer telemetry is excluded as non-restorable. Private checkpoint snapshots are audit-only, never an out-of-process restoration mechanism."
    - "The app is not force-stopped; lookup retries, catalogue fingerprints, Android keep-awake lifecycle, recorded effective waits and distinct terminal exits remain active during the real run (D-03, D-04, D-07, D-09)."
    - "An interrupted run resumes only under exact provenance and never loses a completed cycle or raw local observation (D-10, D-11)."
    - "Both devices must present stable FirmwareEffect.OFF pre-state before mutation, then have complete pre-state captured and exact effect-speed/pixel/device/Android restoration verified after success, mismatch, failure or interruption (D-17)."
    - "Committed device identity is limited to source-tile/non-tile-matrix plus model, product ID and firmware; LAN addresses, serial/MAC, household labels, tablet/account identity and raw UI diagnostics stay private (D-10, D-12, D-18)."
    - "08-UAT-RESULTS.json is the schema-validated authority and 08-UAT.md is derived solely from it after the designated complete or definitively failed restored run (D-19, D-21)."
    - "The official ceiling table has exact set equality with the 25 sorted shipped lifx-app length-16 slugs and a cited permitted determination on every row (D-20, D-21)."
    - "If all 24 readbacks match, both restorations verify and the schema audit passes, outcome is pass; any stable mismatch, missing cycle, secondary-hardware absence, privacy fault or restoration fault cannot pass."
  artifacts:
    - ".planning/phases/08-hardware-fidelity-validation/08-UAT-RESULTS.json — authoritative sanitised machine evidence from the designated real-hardware run"
    - ".planning/phases/08-hardware-fidelity-validation/08-UAT.md — human review rendering derived from the JSON authority"
    - ".planning/local/phase-08-theme-fidelity/targets.json — private operator input, git-ignored/mode 0600, never committed"
    - ".planning/local/phase-08-theme-fidelity/<run-id>/checkpoint.json and trace.jsonl — private resumable evidence, git-ignored/mode 0600, never committed"
    - ".planning/local/phase-08-theme-fidelity/<run-id>/diagnostics/ — private screenshots/hierarchies on failure only, git-ignored, never committed"
  key_links:
    - "Approved private bindings -> preflight identity/capability checks -> exactly two real selected devices"
    - "Android semantic Save and library set_effect(MORPH) -> get_effect stable readbacks -> exact shipped-palette comparisons"
    - "Private completed/failed checkpoint -> strict public projection -> authoritative JSON -> derived Markdown"
    - "Device snapshots -> restoration commands -> post-restore readbacks -> official restoration verdicts"
---

<objective>
Execute the automatic Phase 8 preflight and complete real-hardware run, then commit only the
sanitised, schema-validated evidence pair. This plan is the sole authority for FIDELITY-02/03
hardware truth; the prior automated tests prove support-code behaviour but cannot substitute.

Purpose: demonstrate app/library MORPH fidelity on the source Tile and one indoor non-Tile
matrix product, answer the 25 ceiling rows truthfully, and prove complete restoration.
Output: `08-UAT-RESULTS.json` and derived `08-UAT.md`, or an explicit human_needed/failed
state with private diagnostics and no false passing evidence.
</objective>

## 2026-08-16 execution supersession

The real run no longer automates picker navigation. It retains only input-free Android/app/version
and Morph-config preflight. For every app key, it presents an operator action checkpoint and waits
for a fresh stable LAN MORPH palette; no Android tap, swipe, theme/category/theme-row/Save/play or
UI recovery is permitted in the mutation lifecycle. This decision followed repeated zero-cycle
picker failures, while the prior day's manual workflow mapped more than 130 themes faster.

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-hardware-fidelity-validation/08-SPEC.md
@.planning/phases/08-hardware-fidelity-validation/08-CONTEXT.md
@.planning/phases/08-hardware-fidelity-validation/08-REVIEWS.md
@.planning/phases/08-hardware-fidelity-validation/08-01-SUMMARY.md
@.planning/phases/08-hardware-fidelity-validation/08-02-SUMMARY.md
@.planning/phases/08-hardware-fidelity-validation/08-03-SUMMARY.md
@.planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py
@.agents/skills/spike-findings-lifx-async/SKILL.md
@.agents/skills/spike-findings-lifx-async/references/concurrency-and-keepalive.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 04 share)

- New committed file `.planning/phases/08-hardware-fidelity-validation/08-UAT-RESULTS.json`
  containing the public schema defined in Plan 08-02.
- New committed file `.planning/phases/08-hardware-fidelity-validation/08-UAT.md`, rendered
  exclusively from the validated JSON authority.
- Runtime-only private target, checkpoint, JSONL trace and diagnostic paths listed in
  `must_haves.artifacts`; all are already covered by Plan 08-01's narrow ignore rule and
  are never staged or committed.
- No new Python symbols, CLI flags, dataclasses or schema fields; this plan executes and
  materialises Plan 08-02's contracts.

<tasks>

<task type="auto">
  <name>Task 1: Run the non-mutating live preflight and classify hardware availability</name>
  <files>.planning/local/phase-08-theme-fidelity/targets.json, .planning/local/phase-08-theme-fidelity/&lt;run-id&gt;/checkpoint.json</files>
  <read_first>
    - .planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py (authoritative CLI defaults, preflight checks, status codes and private paths)
    - .planning/phases/08-hardware-fidelity-validation/08-CONTEXT.md (D-03, D-07, D-13 through D-18)
    - .planning/phases/08-hardware-fidelity-validation/08-SPEC.md (real-hardware/no-substitute and human_needed contract)
    - .planning/local/phase-08-theme-fidelity/targets.json (private operator binding; never quote its values into logs, plans, summaries or commits)
    - .planning/local/phase-08-theme-fidelity/&lt;run-id&gt;/checkpoint.json if preflight already created it (private; preserve rather than overwrite an existing run)
    - .agents/skills/spike-findings-lifx-async/SKILL.md (quiescence and real-hardware constraints)
  </read_first>
  <precondition>The private target file exists with restrictive permissions and quiescence/indoor confirmations, both targets have been placed in static FirmwareEffect.OFF state, and the approved unlocked tablet is attached; target responsiveness is deliberately not assumed because preflight must classify missing non-Tile hardware as human_needed.</precondition>
  <reversibility rating="reversible">Preflight reads state, fingerprints the app/catalogue and temporarily restores Android keep-awake; it does not mutate either light.</reversibility>
  <action>
    First verify `git status --short` and preserve all existing user work. Assert the private root is
    ignored with `git check-ignore`; never stage it. Confirm pollers are quiesced as recorded in the
    private target file. Run the runner with `--preflight-only` and the documented default waits,
    using `UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache`. Do not discover the fleet or add a host/serial
    argument.

    Require one fresh selector-free Home hierarchy to prove both approved bindings through their
    exact labels or configured group-card accessibility-ID suffixes without tapping either card or
    selector. Then capture private current hierarchy reconnaissance only for the source Tile: the
    approved selected group-control surface with raw count `1`, FX tab (accessibility-ID suffix
    `ax_device_control_effects_tab`), MORPH surface and picker; prove the new semantic navigation
    chain, normalised category/exact unique theme rules, current Save control and the bounded scroll
    that reaches Mondrian. If the source exact device label is hidden, expand only its configured private group-card accessibility-ID
    suffix `ax_device_list_group_card_button_<app_group>`, then require exactly one right detail
    panel whose raw resource-ID basename is `detail_panel` and one raw-text configured group
    heading centred within it. Resolve the stateful selector only from raw `Lights`; a positive
    ASCII decimal selected-light count fails closed before any selector or target tap. Tap only its
    unique smallest-area clickable container; require raw-text `Select lights` from a fresh hierarchy, and re-resolve that exact label from
    fresh hierarchies. While it is absent, require exactly one current `android.widget.ScrollView` with `scrollable="true"`, use
    only its current bounds for a bounded lower-to-upper-quartile swipe, and fail closed on no
    progress, ambiguity or exhaustion; never enumerate or tap unrelated groups or the global
    content-description tab. Do not
    treat the old capture script's theme/Save coordinates or its assumed already-open MORPH screen
    as selectors. After the one exact target tap, require raw count `1`; if exactly one raw
    `Select lights` remains, tap only a unique clickable `ax_device_control_close_button` with
    centre inside and horizontal overlap with that detail panel and bounds above the selector, then
    freshly require selector absence plus unique FX. If the selector auto-closes, accept only the
    direct selector-absent count-`1`/FX proof. Missing, duplicate or out-of-region close controls
    fail closed without target retry, generic close or Android Back.

    Do not tap Save during source-only reconnaissance, so it does not change either light. The
    successful target selection may nevertheless persist in the app as raw count `1`. Before Task
    2, stop at a blocking operator checkpoint: manually reset that source group selector to raw
    `Lights` and dismiss any selector surface. The runner must not automate Back, a generic close
    control or an additional target tap to perform this reset.

    Require the runner to print redacted positive facts for exactly one authorised tablet, app
    version, stable catalogue fingerprint, cheerful/Moods and mondrian/Art Series, source Tile
    product 55, both stable OFF/static restoration baselines, and one responding live Ceiling or
    Luna non-Tile matrix role with resolved Python device class, model/product/firmware. Checkpoint
    values stay private. If the bound device resolves as Candle, explicitly print the public
    D-16 restriction and classify `human_needed`; do not re-admit Candle because the earlier SPEC
    listed it as an example. Confirm Android keep-awake equals its prior value after
    the command. If the approved non-Tile role does not respond or fails live capability validation,
    retain private diagnostics, report `human_needed` and exit 2; stop this plan without writing a
    Tile/emulator substitute or passing official evidence.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache uv run --frozen python .planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py --preflight-only</automated>
  </verify>
  <done>Preflight reports both approved physical roles ready without mutating lights and restores Android state, or truthfully stops with human_needed/incomplete before any official pass claim.</done>
  <acceptance_criteria>
    - A ready run exits 0 and prints `PREFLIGHT PASS`, both public roles, cheerful, mondrian, app version and catalogue digest without private identifiers.
    - Ready output names the resolved `CeilingLight` or Luna matrix class and confirms both restoration baselines have `FirmwareEffect.OFF`; a running effect or Candle stops incomplete/human_needed before mutation.
    - Private reconnaissance demonstrates unique current home/device/FX-tab/MORPH/picker/Save semantics and bounded Mondrian traversal; no committed or console record contains raw hierarchy text or device labels.
    - `git check-ignore .planning/local/phase-08-theme-fidelity/targets.json` exits 0; `git status --short` does not list any private file.
    - A missing/unresponsive secondary fixture or live target exits 2, records outcome `human_needed`, and creates neither official file.
    - Android `stay_on_while_plugged_in` after preflight equals the preflight checkpoint's captured prior value.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Execute all hardware cycles, restore state and finalise official evidence</name>
  <files>.planning/phases/08-hardware-fidelity-validation/08-UAT-RESULTS.json, .planning/phases/08-hardware-fidelity-validation/08-UAT.md, .planning/local/phase-08-theme-fidelity/&lt;run-id&gt;/checkpoint.json, .planning/local/phase-08-theme-fidelity/&lt;run-id&gt;/trace.jsonl</files>
  <read_first>
    - .planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py (run/resume/finalise/validate CLI contracts)
    - .planning/phases/08-hardware-fidelity-validation/08-CONTEXT.md (all D-01 through D-21; this task materialises every decision)
    - .planning/phases/08-hardware-fidelity-validation/08-SPEC.md (all acceptance criteria, edge truths and prohibitions)
    - .planning/phases/08-hardware-fidelity-validation/08-UAT-RESULTS.json and 08-UAT.md if either already exists (never overwrite prior authority until the new pair validates atomically)
    - .planning/local/phase-08-theme-fidelity/&lt;run-id&gt;/checkpoint.json if Task 1 or a prior interrupted run produced one (private; inspect provenance locally and never quote identifiers)
    - .planning/local/phase-08-theme-fidelity/&lt;run-id&gt;/trace.jsonl if it exists (private append-only observations; preserve on resume/failure)
    - data/themes.jsonl and src/lifx/const.py (mechanical 25-row inventory and ceiling citation; read-only)
  </read_first>
  <precondition>Task 1 returned PREFLIGHT PASS for both public roles, the source app selector was manually reset to raw Lights and dismissed after source-only reconnaissance, and the selected devices remain quiesced; if the preflight state has changed, rerun Task 1 rather than bypassing it.</precondition>
  <reversibility rating="costly">The automatic run changes two real lights; the pre-state snapshot, verified finally restoration and distinct restoration-failure status are mandatory recovery controls.</reversibility>
  <action>
    Inspect the private root for an interrupted run. If exactly one incomplete run passes the full
    D-11 provenance comparison, execute `--resume RUN_ID --finalise`; otherwise execute
    `--run --finalise`. Use the documented default waits unless the live preflight proved a recorded
    override necessary; never change themes, ordering, cycle counts or targets. A resume mismatch
    must stop rather than create a fresh mixed dataset under the same run ID.

    Allow the automatic runner to finish all 24 sequential cycles even after stable palette
    mismatches. Do not pipe output through `head` or otherwise close stdout early. Treat exit 1 as a
    completed failed measurement requiring gap planning, not permission to rerun away the mismatch.
    Treat exit 2 as incomplete/human_needed and exit 3 as restoration failure; neither may yield a
    passing official pair. Confirm both devices and Android are restored from direct readbacks before
    finalisation. The library and restore `set_effect()` calls are unacknowledged: accept completion
    only from bounded two-consecutive stable readbacks, retain every transitional read privately,
    and do not insert an operator sleep or accept one read as acknowledgement. Confirm restored
    effect speed is byte-equivalent in protocol milliseconds, including a zero-speed OFF snapshot,
    and confirm the full static pixel/Ceiling component baseline after the restored OFF effect is
    stable.

    After the run, invoke `--validate-evidence` on `08-UAT-RESULTS.json`. Parse the JSON independently
    to assert 24 unique cycles, 12 per role, exact app/library/theme/index cardinalities, exact 25-row
    ceiling-set equality derived from `data/themes.jsonl`, non-Tile product !=55, both restoration
    verdicts true and no private identifier/address/raw-diagnostic fields. Require `outcome == "pass"`
    for phase success. Verify `08-UAT.md` was derived from the authority and contains the same 24/25
    records and device/restoration summary. On a full pass, confirm raw screenshots/hierarchies/trace
    were deleted as D-10 requires; on failure confirm they remain private and print the diagnostic
    directory. Never modify `data/themes.jsonl` or generated theme data in response to the result.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache uv run --frozen python .planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py --validate-evidence .planning/phases/08-hardware-fidelity-validation/08-UAT-RESULTS.json && UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache uv run --frozen python -c "import json; from pathlib import Path; r=json.loads(Path('.planning/phases/08-hardware-fidelity-validation/08-UAT-RESULTS.json').read_text()); assert r['outcome']=='pass'; assert len(r['cycles'])==24 and len(r['ceiling_determinations'])==25; assert {d['role'] for d in r['devices']}=={'source-tile','non-tile-matrix'}; assert all(x['verified'] for x in r['restorations']); md=Path('.planning/phases/08-hardware-fidelity-validation/08-UAT.md').read_text(); assert '24' in md and '25' in md and 'source-tile' in md and 'non-tile-matrix' in md; print('Phase 8 hardware evidence PASS: 24 cycles, 25 determinations, 2 restorations')"</automated>
  </verify>
  <done>The authoritative JSON and derived Markdown exist only after the designated real run, prove all 24 exact comparisons and 25 ceiling determinations with two verified restorations, and contain no private identity; otherwise the phase truthfully remains mismatch, human_needed or restoration failure.</done>
  <acceptance_criteria>
    - The automated command exits 0 and prints `Phase 8 hardware evidence PASS: 24 cycles, 25 determinations, 2 restorations` only for a true passing run.
    - JSON contains 24 unique required cycle keys, all stable palettes/comparisons, 25 exact data-derived determinations, safe model/product/firmware metadata and two verified restorations.
    - Markdown carries the same outcome/cardinalities and is reproducible byte-for-byte from the JSON authority.
    - Privacy/schema validation rejects IP, serial/MAC, household/app label, adb/tablet/account identity, token/cookie and raw diagnostic content.
    - `git diff -- data/themes.jsonl src/lifx/theme/data.py` is empty.
    - Focused tests, ruff, format and pyright from Plans 08-01/02 remain green with positive test counts; emulator/injected tests are reported separately from the hardware UAT.
  </acceptance_criteria>
</task>

</tasks>

## Cross-AI Review Incorporation

| Review concern | Disposition | Plan contract |
|---|---|---|
| Hardware plan marked autonomous | INCORPORATED | 08-04 is `autonomous: false` and retains explicit `user_setup`. |
| `set_effect()` is unacknowledged | INCORPORATED | 08-01/02/04 retain transitional reads and use bounded two-equal polling, never a fixed completion sleep. |
| Duplicate comparison helper could drift | INCORPORATED | 08-01/02 use `Theme.palette_equals()` as the sole equality seam; adapter code only normalises `None` to an empty identity-less Theme. |
| Whole-runner 100% branch coverage is demanding | INCORPORATED WITHOUT SCOPE REDUCTION | The SPEC gate remains 100% statement and branch coverage for `uat_theme_fidelity.py`; every I/O boundary is injectable and all production branches run through fakes. |
| `get_effect().palette` can be `None` | INCORPORATED | 08-01 tests explicit empty normalisation without `Theme([])` default-white behaviour. |
| Effect speed milliseconds/seconds mismatch and zero trap | INCORPORATED | 08-02 stores `speed_ms`, converts non-zero values by `/ 1000.0`, uses a tested truthy sub-millisecond value for OFF zero, and verifies protocol readback. |
| Mondrian is off-screen | INCORPORATED | 08-01 adds bounded fresh-hierarchy scrolling and an injected tracer that reaches entry 16; 08-02 applies it to all app cycles. |
| App category has emoji/uppercase | INCORPORATED | Category headings use `derive_slug()` on both sides; display names remain exact and unique. |
| Capture script has no semantic home/device/FX-tab/MORPH chain | INCORPORATED | 08-01 treats it only as a partial ADB/XML/swipe analogue and builds the full new chain from private current-hierarchy reconnaissance. |
| Existing ADB wrapper swallows failures | INCORPORATED | New `adb()` raises a redacted typed error on non-zero or stderr, except routine stderr progress from a successful fixed-argv `pull`; command failure is not a semantic miss. |
| Running-effect pixels cannot be exactly restored | INCORPORATED FAIL-CLOSED | 08-02/04 require stable `FirmwareEffect.OFF` before mutation, then restore OFF before pixels and verify the complete static baseline. D-17 is not weakened. |
| Phase tests cannot import the runner | INCORPORATED | 08-01 creates phase `tests/conftest.py` before collection; test modules keep imports at the top. |
| Focused pytest inherits root-writing addopts | INCORPORATED | Every focused Phase 8 pytest command uses `-o addopts=''`; coverage/JUnit files stay out of the worktree. |
| Pyright mode called strict | INCORPORATED | Plans describe and run the repository's standard Pyright configuration explicitly against the runner. |
| Theme source key misnamed | INCORPORATED | `load_theme_specs()` reads JSON key `name` into internal/public `display_name`. |
| ROADMAP edit in parallel Wave 1 | INCORPORATED | 08-03 now depends on 08-02 in Wave 3; it is the sole execution-plan ROADMAP writer, and 08-04 moves to Wave 4. |
| Device navigation/duplicate-label coverage | INCORPORATED | 08-01/02 preserve exact approved-label selection, current surface verification, and zero/multiple-match failure before mutation for every app cycle. |
| Re-admit Candle from SPEC example | REJECTED | Locked D-16 is later and non-negotiable: only Ceiling then Luna are accepted; Candle-only availability is `human_needed`. |
| Broaden stale-count document scanner | REJECTED FOR THIS PHASE | 08-03 enumerates the four active claims because SPEC/PATTERNS intentionally quote the historical defect; mechanical 25/26 source derivation still gates those active files. |

## Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Shipped palettes match app MORPH on more than the capture product and the 16-colour question has evidenced answers | 08-01, 08-02, 08-04 | COVERED | Tracer, full runner/finaliser and live execution form one path. |
| REQ | FIDELITY-01 | Exactly 25 shipped ceiling determinations and corrected counts | 08-02, 08-03, 08-04 | COVERED | Mechanical data filter, active-doc correction and official table. |
| REQ | FIDELITY-02 | Three app and three library cycles for cheerful/mondrian on source Tile | 08-01, 08-02, 08-04 | COVERED | Real hardware remains the Plan 08-04 authority. |
| REQ | FIDELITY-03 | Same comparison on one real indoor non-Tile matrix product | 08-02, 08-04 | COVERED | Missing secondary hardware yields human_needed. |
| RESEARCH | stack | Standard library, existing lifx-async and ADB; no new package | 08-01, 08-02 | COVERED | Package legitimacy audit is not applicable. |
| RESEARCH | architecture | Thin I/O orchestration plus pure validators and two private/public projections | 08-01, 08-02 | COVERED | Every ADB/LAN/clock/filesystem edge is injected so the whole runner reaches the retained 100% branch gate; UAT stays independent. |
| RESEARCH | restoration | Fail-closed state machine and complete Ceiling-aware restoration | 08-02, 08-04 | COVERED | Stable OFF/static baseline before mutation; effect-speed-safe restore before pixels; exit 3 on any restore defect. |
| RESEARCH | reset observation | Capture-era reset observation is not a canonical reusable signature | 08-01, 08-04 | COVERED | Superseded 2026-08-16: no private input or guessed constant; stable unexpected palettes are retained mismatches. |
| RESEARCH | evidence | Strict allowlist, exact 25/24 cardinalities and JSON authority -> Markdown | 08-02, 08-04 | COVERED | Negative fixtures and live finalisation. |
| CONTEXT | D-01 | Automated UIAutomator app path from home, semantic controls, Save, no force-stop | 08-01, 08-02, 08-04 | COVERED | |
| CONTEXT | D-02 | Category plus exact display-name semantic lookup | 08-01, 08-02 | COVERED | Emoji/case-normalised category, exact unique name and bounded fresh-hierarchy scrolling. |
| CONTEXT | D-03 | Complete tablet/app/device/theme/version/catalogue preflight | 08-01, 08-02, 08-04 | COVERED | |
| CONTEXT | D-04 | Exactly two semantic retries then diagnostics/restore/stop | 08-01, 08-02 | COVERED | |
| CONTEXT | D-05 | Two identical unordered reads, retain every poll, timeout fails and stable unexpected palettes are mismatches | 08-01, 08-02 | COVERED | |
| CONTEXT | D-06 | Three app then three library cycles, continue after mismatch | 08-02, 08-04 | COVERED | |
| CONTEXT | D-07 | Capture/restore Android keep-awake | 08-01, 08-02, 08-04 | COVERED | |
| CONTEXT | D-08 | Phase-local redacted runner and private JSONL | 08-01 | COVERED | |
| CONTEXT | D-09 | Recorded waits and distinct exit statuses | 08-01, 08-02 | COVERED | |
| CONTEXT | D-10 | Raw diagnostics private, cleanup only after pass | 08-01, 08-02, 08-04 | COVERED | |
| CONTEXT | D-11 | Exact-provenance resume at next unfinished cycle | 08-02, 08-04 | COVERED | |
| CONTEXT | D-12 | Restrictive private connection/checkpoint identity and public roles only | 08-01, 08-02 | COVERED | |
| CONTEXT | D-13 | Fixed samples; app alternates from attested Cheerful, library remains Cheerful then Mondrian | 08-01, 08-02 | COVERED | |
| CONTEXT | D-14 | Only slugs fixed; metadata/palette/hash derived from shipped JSONL | 08-01, 08-02 | COVERED | |
| CONTEXT | D-15 | Explicit two-target private file; no fleet discovery | 08-01, 08-02, 08-04 | COVERED | |
| CONTEXT | D-16 | Ceiling preferred, Luna fallback, live non-Tile validation | 08-02, 08-04 | COVERED | Candle re-admission is explicitly rejected; absence remains human_needed. |
| CONTEXT | D-17 | Capability-complete pre-state and verified restoration | 08-02, 08-04 | COVERED | Stable OFF precondition preserves exact pixels; milliseconds/seconds and zero speed are tested. |
| CONTEXT | D-18 | Host/serial/app-label identity binding stays private | 08-01, 08-02, 08-04 | COVERED | |
| CONTEXT | D-19 | JSON authority and derived Markdown from designated run | 08-02, 08-04 | COVERED | |
| CONTEXT | D-20 | Mechanical sorted 25-row table, no parallel inventory | 08-02, 08-03, 08-04 | COVERED | |
| CONTEXT | D-21 | Fail-closed allowlist and completeness before official writes | 08-02, 08-04 | COVERED | |

SPEC R1 adjacency and R2 empty rows are dismissed with explicit reasons in `08-SPEC.md`;
they are not unresolved or backstop obligations. All 18 covered SPEC edge rows are lifted as
plain-string truths across Plans 08-02 and 08-03. The SPEC contains no `🧪 backstop` row and no
unresolved edge. All seven resolved prohibitions are lifted under Plan 08-02
`must_haves.prohibitions`; none is silently dropped. The SPEC's out-of-scope static-pixel,
visual, broad-catalogue/product, Exterior, endpoint, Phase 9 tooling and palette-rewrite items
remain excluded from every task.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator/private setup -> automatic runner | Human-selected physical targets and quiescence assertion authorise mutation. |
| Android app -> real device -> LAN readback | The two independent source paths meet at firmware-reported MORPH palette state. |
| Automatic runner -> household lights | A failure can leave visible lights or Ceiling components changed. |
| Private run -> committed Git evidence | Household, network, tablet/account and raw UI material must not cross. |
| Official evidence -> phase verdict | Partial, mismatched or unrestored measurements must not be labelled pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-18 | Spoofing | physical target/app label | high | mitigate | Private host+serial+label binding, live product/class/firmware check and semantic app-label match before mutation. |
| T-08-19 | Tampering | real light state | high | mitigate | Complete dual-device snapshots, sequential commands, finally restoration and exact post-restore readbacks. |
| T-08-20 | Information disclosure | official JSON/Markdown | high | mitigate | Strict public allowlist, identifier/content scans and final validate-evidence gate before staging. |
| T-08-21 | Repudiation | UAT cycle outcomes | high | mitigate | Fixed 24-key schedule, every mismatch retained, private append-only trace and JSON authority with exact terminal outcome. |
| T-08-22 | Spoofing | secondary product | high | mitigate | Product !=55, typed real non-Tile MatrixLight, no emulator marker, safe product metadata and human_needed on absence. |
| T-08-23 | Denial of service | missing app/hardware | medium | mitigate | Non-mutating preflight, bounded waits/retries, exit 2 classification and no unrelated discovery. |
| T-08-24 | Information disclosure | app/tablet session | medium | mitigate | Record app version/catalogue digest only; no account/credential/tablet ID, screenshot or hierarchy in committed output. |
</threat_model>

<verification>
- **2026-08-16 execution supersession:** The accepted Tile theme observations and unresolved
  Tile restoration are private, non-finalisable material. Luna proceeds only via the fresh
  role-only workflow; its 12-key private artefact must never be merged with Tile observations
  into official 24-cycle evidence without later human reconciliation.
- **2026-08-16 non-Tile settling supersession:** Luna and Ceiling guided app observations use a
  recorded default 5.0-second post-trigger settle interval before a fresh stability window; the
  trigger cannot be retained as the cycle result, the operator-action and post-settle stability
  budgets remain separate, and the source Tile has no extra wait. The previous one-cycle Luna-only
  Mondrian observation predates this rule and is private, non-finalisable, provisional and never
  resumable. The real run must use a fresh non-Tile baseline and this new contract.
- Preflight positively identifies exactly one tablet, two fixed themes and both approved physical device roles without mutation.
- The real run produces all 24 required stable comparisons or an honest non-pass outcome; unit/emulator tests are not counted as hardware evidence.
- Schema validation and an independent cardinality/set audit pass before the official pair is accepted.
- Both official files contain safe metadata only and both direct restoration verdicts are true.
- `UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache uv run --frozen pytest` passes with a positive test count after focused Phase 8 gates.
- Ruff, format and the repository's standard Pyright configuration are clean; Phase 8 support code has 100% statement and branch coverage independently of UAT.
</verification>

<success_criteria>
- All 12 source-Tile and 12 non-Tile readbacks match their fixed shipped palettes exactly.
- Exactly 25 mechanically selected ceiling determinations are committed with cited permitted findings.
- No private identifier, address, account/session or raw UI diagnostic enters Git evidence.
- Both physical devices and Android keep-awake are restored and directly verified.
- Missing secondary hardware remains human_needed; mismatch/restoration/schema faults remain failed and feed gap planning.
</success_criteria>

<output>
Create `.planning/phases/08-hardware-fidelity-validation/08-04-SUMMARY.md` when done.
</output>
