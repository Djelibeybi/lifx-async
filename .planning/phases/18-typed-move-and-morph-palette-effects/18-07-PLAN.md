---
phase: 18-typed-move-and-morph-palette-effects
plan: 07
type: execute
wave: 3
depends_on: ["18-06"]
files_modified:
  - docs/user-guide/effects.md
  - tests/test_devices/test_multizone_move.py
  - docs/migration/effect-api-changes.md
  - docs/api/devices.md
  - AGENTS.md
autonomous: true
requirements: [EFFECT-01, EFFECT-02]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "docs/user-guide/effects.md has a \"Firmware Move effect\" section whose python examples a test extracts and executes verbatim, in document order, against an emulator strip, leaving get_effect() reporting MOVE; every block is self-contained (imports what it uses and relies on no name an earlier block defined) (the Move part of D-11, R7; review L294@976b22a)"
    - "The migration guide's Move content and the docs/api/devices.md multizone example show the shipped typed API, and the Summary of Removals no longer points readers at a set_effect() keyword form that does not exist (R7)"
    - "The migration guide states that the MultiZoneEffect.direction setter now accepts a Direction member or its case-insensitive name and raises ValueError for a bare integer it used to store unchecked, with Direction(value) as the fix (D-09 as clarified; review L240@976b22a)"
    - "Neither src/lifx nor docs/ contains a keyword-first set_move_effect call or a set_effect( call whose first keyword is effect_type=FirmwareEffect.MOVE, including forms split across lines: both scans are Python regexes with \\s* after the opening parenthesis and around =, because ruff format never reflows Markdown in docs/ (R7; review L139@976b22a, L395@976b22a)"
    - "No multizone example calls set_effect( with an effect_type= keyword, so the invalid multizone set_effect(effect_type=FirmwareEffect.OFF) form in docs/api/devices.md is gone too; the scan is scoped to the multizone regions because that keyword form is valid on MatrixLight (review L226@92a657d)"
    - "docs/user-guide/effects.md describes the Move palette as the amended 18-SPEC R9 does: a generated or caller-supplied palette is passed to apply_theme(), which shuffles it and blends between its colours across the zones, and a caller-supplied palette skips the get_all_color_zones() colour read (review L225@92a657d, L112@976b22a)"
    - "uv run zensical build --clean --strict succeeds and llmstxt-standalone validate passes (R7)"
    - "AGENTS.md's component_state.py line names the firmware-effect palette rule (derive_effect_palette(), validate_effect_palette()) shared by Morph and Move"
    - "Every added prose line is Australian English with no em dash, no example overrides a library default, and the generated changelog is untouched"
    - "The phase's changed library lines are fully line- and branch-covered by the default suite"
    - "The phase diff adds no library line that mentions connectivity or Thread in any letter case, with no exemption for any import (the pre-split `+import threading` carve-out is gone): the Animator Thread guard and its documentation moved to Phase 20, so nothing in this phase may begin it (review L293@976b22a)"
  artifacts:
    - path: "docs/user-guide/effects.md"
      provides: "The Firmware Move effect section"
      contains: "## Firmware Move effect"
    - path: "docs/migration/effect-api-changes.md"
      provides: "The Move section rewritten to the shipped typed API"
      contains: "set_move_effect(Direction.FORWARD"
    - path: "AGENTS.md"
      provides: "The component_state.py description widened to the firmware-effect palette rule"
      contains: "derive_effect_palette()"
    - path: "tests/test_devices/test_multizone_move.py"
      provides: "The verbatim execution of the user-guide Move example"
      contains: "Firmware Move effect"
  key_links:
    - from: "tests/test_devices/test_multizone_move.py"
      to: "docs/user-guide/effects.md"
      via: "the test reads the section under the exact heading and executes its python blocks verbatim"
      pattern: "## Firmware Move effect"
  prohibitions:
    - statement: "MUST NOT add any connectivity- or Thread-dependent library behaviour in this phase; after the split the Animator Thread guard is Phase 20's (phase-wide scan of every added line under src/lifx)"
      status: resolved
      verification: test
    - statement: "MUST NOT edit the release-generated changelog"
      status: resolved
      verification: test
---

<objective>
Document the typed Move API and the Move and Morph palette behaviour where callers look for them,
fix every stale Move example, and run the phase-wide gates.

Purpose: EFFECT-01 R7 and the Move part of D-11, plus the EFFECT-02 palette behaviour. Two
published examples describe a typed Move API that did not exist, and `docs/api/devices.md` shows
two multizone `set_effect()` calls that `MultiZoneLight.set_effect()` does not accept.

Output: the user-guide Move section and its executed example, the migration guide rewrite, the
API reference fixes, the AGENTS.md `component_state.py` line, and the phase-wide R7, docs-build,
prose, split-boundary and coverage gates.

**What moved to Phase 20 (decision scope after the split).** D-11 also named two Thread notes (a
"Thread devices" note under "When to Use Animation" in `docs/user-guide/animation.md` and a
matching note under "Conductor" in `docs/user-guide/effects.md`); they moved to Phase 20 with the
Animator Thread guard, as did D-01 to D-08 and every exception, connectivity and Animator
documentation change the earlier version of this plan carried. This plan implements only the Move
part of D-11 and must not add any of that content.

**Examples use existing defaults only.** No example passes a timeout or other non-default argument
it does not need (project rule: examples never override library defaults).

**Scope note.** Existing em dashes in these files belong to Phase 21's documentation prose sweep
and are left alone; the gate here checks only lines this phase adds.

**Canonical artefacts reconciled before execution.** The round 2 review pass (planning, not this
plan's executor) edited `18-SPEC.md` and `18-CONTEXT.md` so they describe Phase 18 after the
ANIM-05 split: R1 to R3 and R8 and every Thread boundary, constraint, checklist item, edge row
and prohibition row are marked moved to Phase 20, and R6 now permits plan 08's import-only
hoist. This plan's ledger lists every edit. An executor or verifier reading the SPEC now measures
only R4 to R7, R9 and R10.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-typed-move-and-morph-palette-effects/18-SPEC.md
@.planning/phases/18-typed-move-and-morph-palette-effects/18-CONTEXT.md
@.planning/phases/18-typed-move-and-morph-palette-effects/18-06-SUMMARY.md
@AGENTS.md

<interfaces>
Shipped by plans 03, 04 and 06:
- `MultiZoneEffect.move(direction, speed, duration=0)`;
  `MultiZoneLight.set_move_effect(direction, speed, duration=0, palette=None)`.
- `MatrixLight.set_effect(FirmwareEffect.MORPH)` with no palette derives one from the device.
- `derive_effect_palette()` and `validate_effect_palette()` in `src/lifx/devices/component_state.py`.

Stale content located by the planner (line anchors re-checked against this branch on 2026-09-23):
- `docs/migration/effect-api-changes.md`: keyword-first `set_move_effect` calls in "Before" blocks at
  :45, :78, :130; `set_effect(` with an `effect_type=FirmwareEffect.MOVE` first keyword at :53
  (split across lines), :84, :142 and :236 (the Summary of Removals line).
- `docs/api/devices.md` :622-662: the MultiZone Control example calls `set_effect(` with
  `effect_type=FirmwareEffect.MOVE` (split across lines, :647) and
  `set_effect(effect_type=FirmwareEffect.OFF)` (:660), neither of which `MultiZoneLight.set_effect()`
  accepts; :319-321 is the `### MultiZoneEffect` lead-in.
- `AGENTS.md` :266 (`component_state.py` description).
- `docs/user-guide/effects.md` :892, the `## Next Steps` heading the new section goes before.

From tests/test_devices/test_multizone_move.py (plans 03 and 06): `_run_example(source,
namespace)`, `_received_packets(...)`, `TestDocumentedMoveExamples`.
</interfaces>
</context>

## Artifacts this phase produces

Produced by this plan:

- `docs/user-guide/effects.md`: new `## Firmware Move effect` section.
- `docs/migration/effect-api-changes.md`, `docs/api/devices.md`, `AGENTS.md`: updated as described
  in the tasks.
- `tests/test_devices/test_multizone_move.py`: `_markdown_section_examples(path, heading) ->
  list[str]` and `TestDocumentedMoveExamples.test_user_guide_move_example_runs_verbatim`.

No library source file changes in this plan.

Changed by the round 2 review-incorporation pass on 2026-09-23 (planning, not by this plan's
executor): `.planning/phases/18-typed-move-and-morph-palette-effects/18-SPEC.md` and
`18-CONTEXT.md`, as listed in this plan's Round 3 ledger. The executor does not edit either.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end documented Move example: the user-guide section executed verbatim against the emulator</name>
  <files>docs/user-guide/effects.md, tests/test_devices/test_multizone_move.py</files>
  <read_first>
    - `docs/user-guide/effects.md` :1-60 (tone and example style) and :860-898 (the end of the
      file and `## Next Steps`).
    - `src/lifx/devices/multizone.py` `set_move_effect()` and `MultiZoneEffect.move()` docstrings
      from plans 03 and 06: the user guide must agree with them.
    - `tests/test_devices/test_multizone_move.py` `_run_example` and `TestDocumentedMoveExamples`.
    - `.planning/phases/18-typed-move-and-morph-palette-effects/18-CONTEXT.md` D-11 (its Move
      part only) and D-13 to D-21.
  </read_first>
  <action>
**The Move section (the Move part of D-11).** Add a `## Firmware Move effect` section to
`docs/user-guide/effects.md`, directly before `## Next Steps`. Explain in two or three short
paragraphs that Move runs on the strip's own firmware (the library sends one packet; there is no
Conductor and no Animator involved), that it rotates the colours already on the strip, and that
`set_move_effect()` therefore, when every zone shows one colour, generates a three-colour palette
and passes it to `apply_theme()` first; leaves a multi-colour strip as it is; and passes a
`palette=` the caller supplies to `apply_theme()` unchanged, without first reading the strip's
colours (it skips `get_all_color_zones()`; review L112@976b22a). Say
plainly that `apply_theme()` shuffles the palette and blends between its colours across the
zones, so the strip shows a blend of the palette rather than each colour in the order given. This
wording follows the amended 18-SPEC R9 acceptance (92a657d), which settles the palette behaviour
review L225@92a657d said the docs were waiting on. Give one or more
fenced `python` examples, each runnable as written given a connected `MultiZoneLight` bound to
`light` (say so in the lead-in sentence), importing from `lifx` what it uses: the one-call typed
form `await light.set_move_effect(Direction.FORWARD, 5.0)`, and the builder form using
`MultiZoneEffect.move(...)` with `await light.set_effect(effect)`, noting that the builder path
never paints. Use whole-second speeds and no non-default argument beyond what each example is
showing. Make every block self-contained: it imports what it uses and relies on no name an
earlier block defined, so a future block added in any position cannot break the others; the
test still runs them in document order against one strip (review L294@976b22a). Leave
`### Conductor` and every other existing section untouched.

**The executed example.** In `tests/test_devices/test_multizone_move.py` add
`_markdown_section_examples(path: Path, heading: str) -> list[str]`, which reads the file, finds the
line exactly equal to `heading`, takes the text up to the next line starting with `## `, and returns
every fenced `python` block in it (asserting there is at least one). Add
`TestDocumentedMoveExamples.test_user_guide_move_example_runs_verbatim`: for each block from
`docs/user-guide/effects.md` under `## Firmware Move effect`, run `_run_example(block, {"light":
strip})` against the `d073d5000005` emulator strip (`emulator_devices[4]`, opened with
`async with`) in document order, giving each block a fresh namespace holding only `light` so a
block that leans on an earlier block's names fails, then assert `get_effect()` reports MOVE. The
test docstring states that the blocks run in document order and must each be self-contained. No
packet capture is needed here, so no barrier applies. Resolve the docs path from the repository
root relative to the test file.

Australian English; no em dashes in added text. Commit with a Conventional Commit, no phase-number
scope, `git commit -S -s`.
  </action>
  <verify>
    <automated>uv run --frozen pytest "tests/test_devices/test_multizone_move.py::TestDocumentedMoveExamples" -q -rs --no-cov -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or the summary line contains "failed", "error", "skipped" or "no tests ran"</fails_when>
    <automated>bash -c 'F=docs/user-guide/effects.md; grep -qx "## Firmware Move effect" "$F" || { echo MOVE-SECTION-MISSING; exit 1; }; N=$(awk "/^## Firmware Move effect\$/{f=1;next} /^## /{f=0} f" "$F" | grep -c "^\`\`\`python" || true); echo "move-section-python-blocks=$N"; test "$N" -ge 1 || { echo MOVE-SECTION-HAS-NO-EXAMPLE; exit 1; }; echo EFFECTS-GUIDE-UPDATED'</automated>
    <fails_when>non-zero exit, or the output contains "MOVE-SECTION-MISSING" or "MOVE-SECTION-HAS-NO-EXAMPLE", or the printed move-section-python-blocks value is 0, or the line "EFFECTS-GUIDE-UPDATED" is absent</fails_when>
    <automated>uv run --frozen pytest "tests/test_devices/test_multizone_move.py::TestDocumentedMoveExamples::test_user_guide_move_example_runs_verbatim" -q -rs --no-cov -p no:cacheprovider</automated>
    <fails_when>non-zero exit (pytest exits 4 with "not found" when the node ID does not exist), or the summary line is not "1 passed"</fails_when>
    <automated>bash -c 'B=$(git merge-base HEAD main) || exit 1; G=$(git diff "$B" -- docs/user-guide/effects.md) || exit 1; N=$(printf "%s\n" "$G" | grep -cE "^-[^-]" || true); echo "removed-lines-in-effects-guide=$N"; test "$N" -eq 0'</automated>
    <fails_when>non-zero exit, or the printed removed-lines-in-effects-guide value is anything other than 0 (the Move section is purely additive, so no existing line may change)</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/user-guide/effects.md` contains the exact heading line `## Firmware Move effect`,
      directly before `## Next Steps`, with at least one fenced `python` block.
    - `test_user_guide_move_example_runs_verbatim` executes every python block in that section
      unmodified and passes against the emulator.
    - `### Conductor` and every other pre-existing section of `docs/user-guide/effects.md` are
      unchanged.
  </acceptance_criteria>
  <done>The user guide shows how to start Move with typed arguments, and the example it shows
  demonstrably runs.</done>
</task>

<task type="auto">
  <name>Task 2: The migration guide and the devices API reference page</name>
  <files>docs/migration/effect-api-changes.md, docs/api/devices.md</files>
  <read_first>
    - `docs/migration/effect-api-changes.md` in full.
    - `docs/api/devices.md` :295-330 and :618-662.
    - `.planning/phases/18-typed-move-and-morph-palette-effects/18-SPEC.md` R7.
  </read_first>
  <action>
**Migration guide (R7).** Rewrite every Move passage in `docs/migration/effect-api-changes.md` to
the shipped API. "Before" material that showed the pre-4.3.0 keyword-first Move method becomes one
prose sentence describing it (speed and an integer direction, 0 reversed and 1 forward, passed as
keywords), with no code call, because the same method name now exists with a different signature
and a code sample would read as current. "After" and "New Code" blocks show
`await light.set_move_effect(Direction.FORWARD, 5.0)` and the builder form
`MultiZoneEffect.move(Direction.FORWARD, 5.0)` passed to `set_effect()`; the unified-naming
section's multizone line uses the typed method; the Summary of Removals line for the old
`set_move_effect()` says the keyword form was removed in 4.3.0 and the typed
`set_move_effect(direction, speed, duration=0, palette=None)` replaces it. In the Move section,
add one or two sentences on the `MultiZoneEffect.direction` setter: it now accepts a `Direction`
member or its case-insensitive name (`"forward"`, `"reversed"`), and a bare integer, which it
used to store unchecked, now raises `ValueError`; wrap such a value as `Direction(value)`. Use
that literal `Direction(value)` in the fix so the gate below can find it (per D-09 as clarified
on 2026-09-23 and the amended R6; review L240@976b22a). Keep the matrix, enum and
application-request sections as they are.

**API reference.** In `docs/api/devices.md`: in the MultiZone Control example, replace the Move call
with `await light.set_move_effect(Direction.FORWARD, 5.0)` and the stop call with
`await light.stop_effect()` (the old calls pass keywords `MultiZoneLight.set_effect()` does not
accept), keeping the imports consistent with what the example uses; in the `### MultiZoneEffect`
lead-in, mention `MultiZoneEffect.move()` and `MultiZoneLight.set_move_effect()`. Leave the
`### Connectivity` section and every section outside MultiZone as they are.

Australian English; no em dashes in added text. Commit with a Conventional Commit, no phase-number
scope, `git commit -S -s`.
  </action>
  <verify>
    <automated>uv run --frozen python -c "import pathlib, re; pats = [re.compile(r'set_effect\(\s*effect_type\s*=\s*FirmwareEffect\.MOVE'), re.compile(r'set_move_effect\(\s*speed\s*=')]; files = [p for root in ('src/lifx', 'docs') for p in pathlib.Path(root).rglob('*') if p.suffix in ('.py', '.md')]; hits = sorted({(str(p), q.pattern) for p in files for q in pats if q.search(p.read_text(encoding='utf-8'))}); print('scanned=%d stale-move-examples=%r' % (len(files), hits)); assert len(files) > 100 and not hits"</automated>
    <fails_when>non-zero exit, or the printed stale-move-examples list is non-empty, or the scanned count is 100 or fewer (a wrong path would scan nothing). Both patterns tolerate a newline or spaces after the opening parenthesis and around `=` (Python's `\s` matches newlines), so a call split across lines in Markdown, which `ruff format` never touches, is caught; the planner confirmed the scan finds today's four stale sites before this plan runs</fails_when>
    <automated>bash -c 'grep -qF "set_move_effect(Direction.FORWARD" docs/migration/effect-api-changes.md || { echo MIGRATION-NOT-TYPED; exit 1; }; grep -qF "Direction(value)" docs/migration/effect-api-changes.md || { echo MIGRATION-SETTER-NARROWING-UNDOCUMENTED; exit 1; }; grep -qF "set_move_effect(Direction.FORWARD" docs/api/devices.md || { echo DEVICES-EXAMPLE-NOT-TYPED; exit 1; }; grep -qF "MultiZoneEffect.move(" docs/api/devices.md || { echo MULTIZONE-EFFECT-LEAD-IN-NOT-UPDATED; exit 1; }; echo REFERENCE-PAGES-UPDATED'</automated>
    <fails_when>non-zero exit, or the output contains "MIGRATION-NOT-TYPED", "MIGRATION-SETTER-NARROWING-UNDOCUMENTED", "DEVICES-EXAMPLE-NOT-TYPED" or "MULTIZONE-EFFECT-LEAD-IN-NOT-UPDATED", or the line "REFERENCE-PAGES-UPDATED" is absent</fails_when>
    <automated>uv run --frozen python -c "import pathlib, re; kw = re.compile(r'set_effect\(\s*effect_type\s*='); regions = [('docs/api/devices.md', '### MultiZone Control', '### '), ('docs/user-guide/effects.md', '## Firmware Move effect', '## ')]; texts = {p: pathlib.Path(p).read_text(encoding='utf-8').splitlines() for p, _, _ in regions}; spans = [(p, texts[p].index(h), s) for p, h, s in regions]; bodies = [(p, chr(10).join(texts[p][i:next((j for j in range(i + 1, len(texts[p])) if texts[p][j].startswith(s)), len(texts[p]))])) for p, i, s in spans]; [print('%s: %d chars scanned' % (p, len(b))) for p, b in bodies]; hits = [p for p, b in bodies if kw.search(b) or len(b) < 200]; print('multizone-keyword-set_effect=%r' % hits); assert not hits"</automated>
    <fails_when>non-zero exit (including a ValueError when either heading is missing), or the printed multizone-keyword-set_effect list is non-empty; a region under 200 characters counts as a hit, so a wrong heading cannot pass by scanning nothing. This covers both the MOVE and the OFF keyword form (the latter is valid only on MatrixLight, so the scan is scoped to the multizone regions rather than the whole tree)</fails_when>
    <automated>bash -c 'B=$(git merge-base HEAD main) || exit 1; G=$(git diff --name-only "$B" -- docs) || exit 1; X=$(printf "%s\n" "$G" | grep -vxE "docs/user-guide/effects.md|docs/migration/effect-api-changes.md|docs/api/devices.md" | grep . || true); test -z "$X" || { echo "UNPLANNED-DOCS-CHANGED:"; printf "%s\n" "$X"; exit 1; }; echo DOCS-CHANGES-WITHIN-PLAN'</automated>
    <fails_when>non-zero exit, or the output contains "UNPLANNED-DOCS-CHANGED:", or the line "DOCS-CHANGES-WITHIN-PLAN" is absent (a Thread note, exception page or connectivity edit under docs/ belongs to Phase 20 and fails this gate)</fails_when>
  </verify>
  <acceptance_criteria>
    - The multi-line-aware scan of `src/lifx` and `docs/` finds no stale Move example: neither
      `set_move_effect\(\s*speed\s*=` nor `set_effect\(\s*effect_type\s*=\s*FirmwareEffect\.MOVE`
      matches anywhere.
    - The migration guide's Move section documents the `direction` setter change and shows
      `Direction(value)` as the fix.
    - Neither the `### MultiZone Control` section of `docs/api/devices.md` nor the
      `## Firmware Move effect` section of `docs/user-guide/effects.md` calls `set_effect(` with an
      `effect_type=` keyword, so the invalid multizone OFF form is gone as well as the MOVE form.
    - The migration guide and the `docs/api/devices.md` MultiZone Control example show
      `set_move_effect(Direction.FORWARD, 5.0)`, and the `### MultiZoneEffect` lead-in names
      `MultiZoneEffect.move()`.
    - The only files changed under `docs/` in the phase diff are `docs/user-guide/effects.md`,
      `docs/migration/effect-api-changes.md` and `docs/api/devices.md`.
  </acceptance_criteria>
  <done>No published page shows a Move API that does not exist, and the reference pages point
  callers at the typed Move builder and method.</done>
</task>

<task type="auto">
  <name>Task 3: Repository guidance and the phase-wide gates</name>
  <files>AGENTS.md</files>
  <read_first>
    - `AGENTS.md` :255-270 (the Device Layer list, `component_state.py` at :266).
    - `tests/test_repository_guidance.py` :1-60 (the AGENTS.md contract tests that must keep
      passing).
    - `.github/check_patch_coverage.py` :223-290 (`check_coverage`).
  </read_first>
  <action>
**AGENTS.md.** Widen the `component_state.py` description at :266 to include the firmware-effect
palette rule (`derive_effect_palette()`, `validate_effect_palette()`) shared by Morph and Move,
keeping the existing items. Change nothing else in AGENTS.md: the Animation Layer list, the
Exception Hierarchy sentence and the state-caching bullets are Phase 20's to touch if the Thread
guard needs them.

**Gates.** Run the strict docs build and llms.txt validation, the added-lines prose check, the
untouched-changelog check, the split-boundary scan, and the phase-wide patch coverage over every
library file this phase changed (`multizone.py`, `matrix.py`, `component_state.py`). The
split-boundary scan lists every added line under `src/lifx` that mentions connectivity or Thread in
any letter case, with no exemption for any line, imports included; this phase adds none, so any
hit is a defect to remove from this phase (it belongs to Phase 20), not a line to classify. The
pre-split exemption for an added `import threading` line is deleted (review L293@976b22a):
`component_state.py` already imports `threading`, and plan 04's additions to its import block
(`from collections.abc import Sequence`, the `lifx.const` names) leave that line where it is, so
nothing in this phase re-adds it.

Commit with a Conventional Commit, no phase-number scope, `git commit -S -s`.
  </action>
  <verify>
    <automated>uv run --frozen zensical build --clean --strict && uv run --frozen llmstxt-standalone validate</automated>
    <fails_when>non-zero exit from either command, or the build output contains "Aborted" or an "ERROR" line</fails_when>
    <automated>bash -c 'E=$(printf "\342\200\224"); B=$(git merge-base HEAD main) || exit 1; G=$(git diff "$B" -U0 -- docs AGENTS.md src/lifx tests) || exit 1; N=$(printf "%s\n" "$G" | grep -E "^\+" | grep -vE "^\+\+\+" | grep -c "$E" || true); echo "added-lines-with-em-dash=$N"; test "$N" -eq 0'</automated>
    <fails_when>non-zero exit, or the printed added-lines-with-em-dash value is anything other than 0</fails_when>
    <automated>bash -c 'B=$(git merge-base HEAD main) || exit 1; G=$(git diff --name-only "$B") || exit 1; C=$(printf "%s\n" "$G" | grep -cx "docs/changelog.md" || true); echo "changelog-touched=$C"; test "$C" -eq 0'</automated>
    <fails_when>non-zero exit, or the printed changelog-touched value is anything other than 0</fails_when>
    <automated>bash -c 'B=$(git merge-base HEAD main) || exit 1; A=$(git diff "$B" -U0 -- src/lifx) || exit 1; H=$(printf "%s\n" "$A" | grep -E "^\+" | grep -vE "^\+\+\+" | grep -iE "connectivity|thread" || true); test -z "$H" || { echo "SPLIT-BOUNDARY-BREACHED:"; printf "%s\n" "$H"; exit 1; }; F=$(git diff --name-only "$B" -- src/lifx) || exit 1; X=$(printf "%s\n" "$F" | grep -vxE "src/lifx/devices/(multizone|matrix|component_state)\.py" | grep . || true); test -z "$X" || { echo "UNPLANNED-LIBRARY-FILE-CHANGED:"; printf "%s\n" "$X"; exit 1; }; echo NO-THREAD-OR-CONNECTIVITY-IN-PHASE-18'</automated>
    <fails_when>non-zero exit, or the output contains "SPLIT-BOUNDARY-BREACHED:" or "UNPLANNED-LIBRARY-FILE-CHANGED:", or the line "NO-THREAD-OR-CONNECTIVITY-IN-PHASE-18" is absent</fails_when>
    <automated>mkdir -p .cache/phase-18 && uv run --frozen pytest -p no:cacheprovider --cov-report=json:.cache/phase-18/cov-18-07.json -q && uv run --frozen python .github/check_patch_coverage.py --base "$(git merge-base HEAD main)" --coverage .cache/phase-18/cov-18-07.json --source src/lifx/devices/multizone.py --source src/lifx/devices/matrix.py --source src/lifx/devices/component_state.py</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL:", or the pytest summary line contains "failed" or "error"</fails_when>
    <automated>bash -c 'grep -E "^ *- \`component_state\.py\`" AGENTS.md | grep -qF "derive_effect_palette()" || { echo AGENTS-COMPONENT-STATE-NOT-WIDENED; exit 1; }; echo AGENTS-COMPONENT-STATE-WIDENED'</automated>
    <fails_when>non-zero exit, or the output contains "AGENTS-COMPONENT-STATE-NOT-WIDENED", or the line "AGENTS-COMPONENT-STATE-WIDENED" is absent</fails_when>
    <automated>uv run --frozen pytest tests/test_repository_guidance.py -q --no-cov -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or the summary line contains "failed", "error" or "no tests ran"</fails_when>
    <automated>uv run --frozen ruff check . && uv run --frozen ruff format --check . && uv run --frozen pyright</automated>
    <fails_when>non-zero exit from any of the three, or pyright's summary reports 1 or more errors</fails_when>
  </verify>
  <acceptance_criteria>
    - The strict docs build and llms.txt validation both exit 0.
    - No added line in `docs`, `AGENTS.md`, `src/lifx` or `tests` contains an em dash.
    - The release-generated changelog is not in the phase diff.
    - No added line under `src/lifx` mentions connectivity or Thread, and no filter exempts any
      added line (an import included) from that scan before the case-insensitive match; the only
      library files the phase changed are `multizone.py`, `matrix.py` and `component_state.py`.
    - AGENTS.md's `component_state.py` line names `derive_effect_palette()` and
      `validate_effect_palette()`, and `tests/test_repository_guidance.py` passes.
    - The patch-coverage checker prints `PASS` for all three changed library files and a
      `PASS total` line; the full default suite passes.
  </acceptance_criteria>
  <done>The typed Move API and the palette behaviour are documented wherever callers read about
  them, the docs build cleanly, nothing from the Thread guard leaked into this phase, and the whole
  phase meets the 100% branch patch coverage bar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Published docs to callers | Callers copy examples into production code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-21 | Tampering | Documented examples drifting from the shipped API | medium | mitigate | The user-guide Move section and both docstring examples are executed verbatim against the emulator; a multi-line-aware scan forbids the stale forms |
| T-18-22 | Information disclosure | Addresses in documentation | low | mitigate | Examples operate on a `light` already connected or use the existing documentation address; no live identifier is added |
| T-18-23 | Tampering | Thread-guard behaviour leaking into Phase 18 after the split | low | mitigate | The split-boundary gate fails on any added `src/lifx` line mentioning connectivity or Thread and on any changed library file outside the three planned ones; the docs gate fails on any changed page outside the three planned ones |
</threat_model>

<!-- plan-criteria-allow: R4 - every git call in these gates is captured by command substitution and followed by || exit 1, so a git failure fails the gate before any pipe reads its output -->

<verification>
- `uv run --frozen zensical build --clean --strict` and `uv run --frozen llmstxt-standalone validate` pass.
- The full default suite passes and the phase-wide patch-coverage check passes for all three changed library files.
- The split-boundary scan finds no added connectivity or Thread line under `src/lifx`.
</verification>

<success_criteria>
The typed Move API appears in the published documentation with an example that runs, the Move and
Morph palette behaviour is described the way the amended SPEC states it, and no page shows an API
that does not exist.
</success_criteria>

## Review Dispositions Ledger

### Round 1 — 92a657d

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| Documentation of the explicit palette behaviour depends on an unresolved HIGH finding in 18-06; finalise the palette wording only after R9 is reconciled (L225@92a657d, L233@92a657d) | MEDIUM | Resolved upstream by the maintainer's adjudication (L526@92a657d): Plan 07, Task 1 describes the palette as the amended R9 does (passed to `apply_theme()`, which shuffles and blends it); must_haves truth. The Conductor half of this concern moved to Phase 20 (see Round 2) |
| The stale-call scan targets Move only and would not catch the invalid multizone `set_effect(effect_type=FirmwareEffect.OFF)` form at `docs/api/devices.md:660` (L226@92a657d, L232@92a657d) | LOW | Plan 07, Task 2: a region-scoped verify fails on any `set_effect(effect_type=` keyword call in the `### MultiZone Control` section and the new `## Firmware Move effect` section, covering both MOVE and OFF; it is region-scoped because the OFF keyword form is valid on `MatrixLight` (`docs/api/devices.md` Tile Control) |
| Direct `Animator(...)` construction should be described as an escape hatch that does not inspect connectivity (L227@92a657d, L234@92a657d) | LOW | Moved to Phase 20 with the Animator Thread guard's documentation |
| Add the Move docs only through the existing transitive dependency on the Move method plan; the ordering is otherwise sound (L231@92a657d) | LOW | No change needed: Plan 07 depends on 18-06, which depends on 18-04 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| A catch-and-degrade snippet for the Thread refusal in the effects guide (Antigravity, L419@92a657d) | Moved to Phase 20 with the Animator Thread guard's documentation |

### Round 2: phase split, 2026-09-23

| Change | Reason |
|--------|--------|
| Wave 4 to wave 3; `depends_on` is plan 06 only; requirements EFFECT-01 and EFFECT-02 | Moved to Phase 20: the Animator factory guard, the Conductor exclusion and their requirement. Plan 06 is wave 2 |
| Moved to Phase 20: the "Thread devices" note in `docs/user-guide/animation.md`, the Conductor Thread note in `docs/user-guide/effects.md`, the exception page and AGENTS.md exception-list entries for the new Thread exception, the AGENTS.md Animation Layer guard entry, and the connectivity sentences in `docs/api/devices.md`, `docs/user-guide/advanced-usage.md` and `src/lifx/devices/base.py` | They describe the Thread guard, which Phase 18 no longer ships. `files_modified` drops the four files that only carried them |
| The phase-wide connectivity listing becomes a split-boundary gate that fails on any added connectivity or Thread line under `src/lifx`; patch coverage covers `multizone.py`, `matrix.py` and `component_state.py` only | The judgment listing existed to classify guard lines; this phase has none |
| New gates: the Move section is purely additive, the only changed docs are the three planned pages, and AGENTS.md's `component_state.py` line is widened | Enforces the reduced scope and the retained D-11 Move part |
| The em-dash scope note now names Phase 21 | The Documentation Prose Sweep was renumbered from 20 to 21 in the split |

### Round 3: cross-AI review round 2, 2026-09-23 (976b22a)

This plan is the home for phase-wide artefact changes, so the 18-SPEC.md and 18-CONTEXT.md edits
made during this pass are recorded here, after the two tables.

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 18-SPEC.md and 18-CONTEXT.md still describe the pre-split phase (title, "10 locked", R1 to R3 and R8, the Thread goal, boundaries, constraints, edges, prohibitions and checklist; CONTEXT's domain, `spec_lock`, the emulator pin bump and the emulator Thread prerequisite), so an executor following the plans and a verifier following the SPEC would disagree (Codex, L140@976b22a, L193@976b22a to L202@976b22a; verified by the orchestrator, L462@976b22a) | HIGH | Both files edited in place during this planning pass under the orchestrator's authorisation, minimally and auditably: every Thread item is marked "Moved to Phase 20 (ANIM-05) on 2026-09-23" rather than deleted, numbering is kept, and the full text stays in git history and in `../20-animator-thread-guard/prior-plans/README.md`. Full edit list below. Plan 07's objective and artefact list note the reconciliation |
| The stale `set_move_effect(speed=` scan is a substring test, so a call split across lines passes, contradicting this plan's own truth (Codex, L139@976b22a, L144@976b22a; Antigravity, L395@976b22a; consensus L454@976b22a, divergent view L496@976b22a) | MEDIUM | Plan 07, Task 2: both stale-Move scans are now Python regexes, `set_move_effect\(\s*speed\s*=` and `set_effect\(\s*effect_type\s*=\s*FirmwareEffect\.MOVE`; the region-scoped keyword scan also tolerates spaces around `=`. Python's `\s` matches newlines, so the existing `effect_type` regex already tolerated a line break after the parenthesis; the `\s*=` widening is new. The planner ran the new scan against today's tree and it reports all four known stale sites, so it is not vacuous. Plan 06's `src/lifx` scan gets the same regex. `ruff format` never touches Markdown, so the gap was real in `docs/` |
| The split-boundary gate whitelists an added `import threading` line, a pre-split carve-out that holes the gate (OpenCode, L293@976b22a, L297@976b22a; consensus L478@976b22a) | MEDIUM | Plan 07, Task 3: the exclusion is deleted from the verify command; the action explains why no Phase 18 change re-adds that line (`component_state.py` already imports it and plan 04's import additions leave it in place); must_haves truth and acceptance criterion updated |
| The migration guide should record that the `direction` setter no longer accepts a bare integer (OpenCode, L240@976b22a, L243@976b22a; consensus L481@976b22a) | LOW | Plan 07, Task 2: the Move section gains the sentence with `Direction(value)` as the fix, and a new grep fails with `MIGRATION-SETTER-NARROWING-UNDOCUMENTED` without it; must_haves truth. Plan 03 chose documenting over an integer path (see its Round 3 ledger) |
| User-guide Move examples run in order against one strip; state whether they are order-independent (OpenCode, L294@976b22a) | LOW | Plan 07, Task 1: every block must be self-contained; the test gives each block a fresh namespace holding only `light` and its docstring states the document-order rule; must_haves truth |
| "Without reading the zones" should say the explicit palette skips `get_all_color_zones()` (Codex, L112@976b22a) | LOW | Plan 07, Task 1 action and must_haves truth reworded |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| The removed-lines gate on `docs/user-guide/effects.md` could trip on a blank-line adjustment next to the new heading (OpenCode, L295@976b22a) | No change: the gate counts only `^-[^-]`, which a removed blank line (a bare `-`) never matches, so blank-line adjustments already pass |
| Use AST parsing rather than regex for the stale-example audit (Antigravity, L398@976b22a) | Rejected: most stale sites are in Markdown, which Python's `ast` cannot parse; the multi-line regexes above cover the reviewed gap |

### 18-SPEC.md and 18-CONTEXT.md edits made in this pass

| File | Section | Edit |
|------|---------|------|
| 18-SPEC.md | Title | "Phase 18: Typed Move and Morph Palette Effects Specification" |
| 18-SPEC.md | Requirements header | "6 locked for Phase 18: R4 to R7, R9 and R10"; R1 to R3 and R8 moved, numbering kept; pointers to the Phase 20 README and to commit 976b22a, the last to carry the full pre-split text (`git log -p` finds it after a rebase) |
| 18-SPEC.md | Goal | Animator and Conductor sentence removed; a note says it moved with ANIM-05 |
| 18-SPEC.md | Background | The three ANIM-05 paragraphs replaced by a one-line moved stub; the EFFECT-01 paragraph unchanged |
| 18-SPEC.md | R1, R2, R3, R8 | Bodies replaced by a one-line "Moved to Phase 20 (ANIM-05) on 2026-09-23" stub each, titles kept |
| 18-SPEC.md | R6 Target | "`direction` property unchanged" becomes "`direction` getter unchanged", plus a dated clause that the setter follows D-09 and now rejects a bare integer (consequential to finding 7, since R6 was already being amended) |
| 18-SPEC.md | R6 Acceptance | "pass unmodified" becomes "assertions, fixtures and node IDs unchanged", permitting only the D-12 import-only hoist proven by a diff gate (finding 2) |
| 18-SPEC.md | R7 Target | The Animator factory and Conductor docstring clause marked moved |
| 18-SPEC.md | Boundaries | The two Thread in-scope bullets and four Thread out-of-scope bullets marked moved; the docs bullet drops the Thread-default docs; one summary out-of-scope bullet for ANIM-05 added |
| 18-SPEC.md | Constraints | Coverage examples drop guard and opt-in; the emulator Thread-device clause and the `enable_thread` signature constraint marked moved; `conductor.py` dropped from the file set |
| 18-SPEC.md | Acceptance Criteria | 15 Thread items become plain "Moved to Phase 20" bullets with no checkbox; the R6 item reworded to match the amended R6; 16 Phase 18 checkboxes remain |
| 18-SPEC.md | Edge Coverage | Header "15/15 applicable Phase 18 edges"; the 10 R1, R2, R3 and R8 rows marked "moved to Phase 20"; a stray blank line that split the table in two removed |
| 18-SPEC.md | Prohibitions | Header "3/3 applicable Phase 18 prohibitions"; the 6 ANIM-05 rows marked "moved to Phase 20" with short labels |
| 18-SPEC.md | Before the Interview Log | One note that the Ambiguity Report and Interview Log are the 2026-09-09 record and their Thread rows moved |
| 18-CONTEXT.md | Title | "Phase 18: Typed Move and Morph Palette Effects - Context" |
| 18-CONTEXT.md | `<domain>` | Rewritten to the Move and Morph palette scope plus the D-12 hoist; a second paragraph says the Thread guard and its files moved to Phase 20 |
| 18-CONTEXT.md | `spec_lock` | "6 requirements are locked for Phase 18"; the in-scope and out-of-scope lists mirror the amended SPEC |
| 18-CONTEXT.md | D-09 | A dated clarification: backward compatible for the annotated `Direction` type, a bare `int` now raises `ValueError`, documented in the migration guide; the decision itself unchanged (consequential to finding 7) |
| 18-CONTEXT.md | Claude's Discretion | The guard-helper and Thread test-layout bullets marked moved |
| 18-CONTEXT.md | Canonical References | SPEC counts updated (16 criteria, 15 edges, 3 prohibitions); "Origin of the guard" marked moved; the pin-bump clause removed ("Phase 18 bumps no dev-dependency"); the emulator Thread prerequisite marked moved and no longer a prerequisite |
| 18-CONTEXT.md | `<code_context>`, `<specifics>` | One note that the Thread entries serve ANIM-05 and moved; three Thread specifics tagged moved |

Not edited, flagged to the orchestrator instead: `18-PATTERNS.md` still carries the pre-split
title and Thread-guard analogues; 18-SPEC R9's "with no zone read" wording is left as locked
(plan 06 states how it reads it); and `../20-animator-thread-guard/prior-plans/README.md` still
says the ANIM-05 requirements "still live in" `18-SPEC.md`, which after this pass holds only
stubs, so that pointer should name commit 976b22a or `git log -p`.

<output>
Create `.planning/phases/18-typed-move-and-morph-palette-effects/18-07-SUMMARY.md` when done
</output>
