---
phase: 18-typed-move-and-morph-palette-effects
plan: 08
type: execute
wave: 3
depends_on: ["18-03", "18-06"]
files_modified:
  - tests/test_devices/test_multizone.py
  - tests/test_animation/test_animator.py
  - pyproject.toml
autonomous: true
requirements: [EFFECT-01]
coupling_justified: ["18-07: both run repo-wide ruff, pyright and full-suite gates; this plan only tightens PLC0415 on two test paths 18-07 never edits, and 18-07's only test file is already under the rule, so either order passes"]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Every function-local import in tests/test_animation/** and tests/test_devices/test_multizone.py is at module scope, both per-file-ignore entries for those paths are deleted from pyproject.toml, and uv run ruff check . exits 0 (D-12, carrying Phase 16 D-05)"
    - "PLC0415 is proven active on both paths after the deletion: a ruff stdin probe containing a function-local import, linted under each path's filename, is reported as PLC0415 with exit status 1 (the planner confirmed the probe exits 0 under an ignored path and 1 under an unignored one)"
    - "No test was lost or added: the sorted node-ID set collected with pytest --collect-only -qq for the two paths after the sweep is exactly equal to the set captured before any edit (an import-only sweep adds no test), both captures fail closed on a pytest or grep failure through set -o pipefail, and the default suite passes"
    - "Both swept test files differ from the merge-base only inside import statements: an ast- and difflib-backed diff gate maps every changed non-blank line, in the merge-base version and in the working tree, to the line span of an Import or ImportFrom node (so a removed indented function-local import, including every line of a parenthesised multi-line one, counts as import-only) and fails on any other added or removed line, comments included. This is the evidence the amended R6 names for tests/test_devices/test_multizone.py (cross-AI review round 2, L167@976b22a, L174@976b22a)"
    - "ruff check --fix and ruff format run only on the owned paths (tests/test_devices/test_multizone.py in Task 1, tests/test_animation in Task 2), never on the repository root, and every commit in the phase that touches a swept path or pyproject.toml touches nothing outside tests/test_devices/test_multizone.py, tests/test_animation/ and pyproject.toml (review L168@976b22a, L173@976b22a)"
    - "The pyproject.toml handoff comment states the new count of unswept paths and keeps Phase 19's sentence intact"
    - "The sweep runs after plans 03 and 06 recorded that tests/test_devices/test_multizone.py was byte-identical to the merge-base, and the amended R6 permits exactly this import-only change afterwards; plans 03 and 06's rerunnable R6 gates accept the post-sweep file only because this plan has deleted its PLC0415 ignore and the change is import-only. No Phase 18 plan edits tests/test_animation/ before this sweep"
  artifacts:
    - path: "pyproject.toml"
      provides: "The two Phase 18 PLC0415 per-file-ignore entries (tests/test_animation/** and tests/test_devices/test_multizone.py) absent, whether or not Phase 19 has already removed its own tests/test_theme/** entry; every other entry unchanged"
      contains: "\"src/**\" = [\"PLC0415\"]"
    - path: "tests/test_animation/test_animator.py"
      provides: "All imports at module scope"
      contains: "import inspect"
    - path: "tests/test_devices/test_multizone.py"
      provides: "All imports at module scope"
      contains: "from lifx.protocol.protocol_types import ("
  key_links:
    - from: "pyproject.toml [tool.ruff.lint.per-file-ignores]"
      to: "tests/test_animation/** and tests/test_devices/test_multizone.py"
      via: "no entry exempts them, so the repository-wide PLC0415 selection applies"
      pattern: "PLC0415"
  prohibitions:
    - statement: "MUST NOT keep an inline import by adding a per-line suppression comment, and MUST NOT narrow the rule with an exclude; every site is hoisted"
      status: resolved
      verification: test
    - statement: "MUST NOT delete, skip or de-collect any test while hoisting"
      status: resolved
      verification: test
    - statement: "MUST NOT change any non-import line of a swept test file (assertion, fixture, test body, docstring or comment) and MUST NOT let a ruff auto-fix or format run touch any file outside the owned paths"
      status: resolved
      verification: test
---

<objective>
Close the PLC0415 handoff Phase 16 D-05 gave this phase: hoist every function-local import in this
phase's two test paths to module scope and delete their two per-file-ignore lines.

Purpose: D-12. Phase 16 enabled `PLC0415` repository-wide and left one visible, deletable line per
unswept path, owned by the phase that edits those files. This phase owns
`tests/test_animation/**` and `tests/test_devices/test_multizone.py`.

Output: the hoisted imports (measured by the planner: 18 sites in
`tests/test_animation/test_animator.py`, 2 in `tests/test_devices/test_multizone.py`, none elsewhere
under `tests/test_animation/`) and the two deleted lines, with the handoff comment updated.

**Why this is the last wave.** Plans 03 and 06 each prove that
`tests/test_devices/test_multizone.py` is byte-identical to the merge-base while its `PLC0415`
ignore is still in `pyproject.toml`. This sweep changes that file for the first time, after that
evidence is recorded, and changes only import placement. R6 was amended on 2026-09-23 (cross-AI
review round 2, L167@976b22a) to say exactly that: the existing tests pass with assertions,
fixtures and node IDs unchanged, and the only permitted edit is this import-only hoist, proven by
a diff gate. That gate lives here and in plans 03 and 06, and every copy can be rerun at phase
end. No Phase 18 plan touches `tests/test_animation/` before this sweep.

**Why the animation tree is still swept here after the split.** The roadmap keeps this hoist in
Phase 18 because it touches `tests/test_animation/` and `tests/test_devices/test_multizone.py`,
not Thread code, and D-12 assigns both paths to this phase. The "existing `tests/test_animation/`
suite passes unmodified" evidence moved to Phase 20 with the Animator Thread guard; Phase 20
depends on Phase 18, so it measures that evidence against a tree that already contains this
import-only hoist.

**Phase 19 runs in parallel and edits the same comment block** to delete its own
`tests/test_theme/**` entry. If that change has already landed when this plan runs, keep it and
merge the comment text rather than restoring anything.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-typed-move-and-morph-palette-effects/18-CONTEXT.md
@.planning/phases/16-mdns-correctness-docs-and-test-hygiene/16-LEARNINGS.md
@AGENTS.md
@pyproject.toml

<interfaces>
pyproject.toml :84-104, `[tool.ruff.lint.per-file-ignores]`: a comment at :90-97 naming the
unswept paths and their owning phases, then the entries `"src/**"`, `".planning/**"`,
`"tests/test_animation/**"`, `"tests/test_theme/**"`, `"tests/test_devices/test_multizone.py"`,
`".agents/skills/**"`, `".claude/skills/**"`, each `= ["PLC0415"]`. `[tool.ruff.lint] select`
includes `"PLC0415"`.

Planner measurement (`ruff check --isolated --select PLC0415`): 18 violations in
`tests/test_animation/test_animator.py` (imports of `inspect`, `struct`,
`lifx.animation.packets.HEADER_SIZE`, `lifx.animation`, `lifx.devices.matrix.MatrixLight`,
`lifx.devices.multizone.MultiZoneLight` and `lifx.devices.mirror.MirrorLight` inside test
functions, several of them repeated) and 2 in `tests/test_devices/test_multizone.py` (:835 and
:873, `from lifx.protocol.protocol_types import (...)` inside `TestMultiZoneEffect`); re-measured
on this branch on 2026-09-23, 20 in total. No Phase 18 plan creates a module under
`tests/test_animation/`; plans 03 and 06 add `tests/test_devices/test_multizone_move.py`, which is
not under a per-file ignore and uses module-scope imports from the start.

Phase 16 lessons (16-LEARNINGS.md): a per-line suppression in the colon form is silently invalid;
every ruff gate must assert its exact exit status; `ruff check` exits 0 on a path that does not
exist; a node-ID set comparison, not a count, is what catches a de-collected test, and it must fail
closed when its baseline is missing.

Collection output under the locked pytest (9.1.1, measured at review commit 92a657d, addressing
review L255@92a657d): `pytest --collect-only -q` prints a collection tree with no `::` node-ID
lines at all (0 for `tests/test_devices/test_multizone.py`), so a `-q | grep "::"` baseline is
empty and fails its own gate before the sweep starts. `pytest --collect-only -qq` prints one node
ID per line followed by a `N tests collected` summary line (63 node IDs for
`tests/test_devices/test_multizone.py`; 263 for both paths together, re-measured on this branch on
2026-09-23; no Phase 18 plan adds a test to either path). Every collection command in this plan therefore
uses `-qq`, runs through `uv run --frozen` so it always uses the locked pytest, and runs under
`set -o pipefail` so a pytest collection error or an empty `grep` fails the command instead of
writing an empty-but-passing file.
</interfaces>
</context>

## Artifacts this phase produces

Produced by this plan:

- `pyproject.toml`: the `tests/test_animation/**` and `tests/test_devices/test_multizone.py`
  PLC0415 per-file-ignore entries removed; the handoff comment updated.
- `tests/test_animation/test_animator.py`, `tests/test_devices/test_multizone.py`: imports hoisted.
- `.cache/phase-18/nodeids-before.txt` and `.cache/phase-18/nodeids-after.txt` (gitignored
  evidence files, not committed).
- No new tooling file: the import-only diff gate and the owned-paths confinement check are
  inline verify commands, so nothing is added under `.planning/scripts/` or `tests/`.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end sweep of one path: test_multizone.py hoisted, its ignore line deleted, ruff clean and no test lost</name>
  <files>tests/test_devices/test_multizone.py, pyproject.toml</files>
  <read_first>
    - `pyproject.toml` :84-104.
    - `tests/test_devices/test_multizone.py` :1-30 (the module import block) and :829-900 (the two
      inline imports in `TestMultiZoneEffect`).
    - `.planning/phases/16-mdns-correctness-docs-and-test-hygiene/16-LEARNINGS.md`, the
      "Lessons" and "Patterns" sections.
  </read_first>
  <action>
**Baseline first, before any edit.** Capture the sorted node IDs of both paths with exactly this
command, run from the repository root before touching any file:
`bash -c 'set -o pipefail; git diff --quiet HEAD -- tests/test_animation tests/test_devices/test_multizone.py pyproject.toml || { echo BASELINE-TREE-NOT-CLEAN; exit 1; }; mkdir -p .cache/phase-18 || exit 1; uv run --frozen pytest tests/test_animation tests/test_devices/test_multizone.py --collect-only -qq --no-cov -p no:cacheprovider | grep "::" | sort > .cache/phase-18/nodeids-before.txt; S=$?; echo "baseline-collect-exit=$S"; test "$S" -eq 0'`.
It refuses to run once either path or `pyproject.toml` differs from `HEAD`, so a baseline can
never be taken from an already-swept tree, and `pipefail` makes a pytest collection error (exit 2
or 5) or a `grep` that matched nothing exit non-zero. If it prints anything other than
`baseline-collect-exit=0`, stop and report rather than editing. Use `-qq`, never `-q`: under the
locked pytest 9.1.1, `-q` prints no node IDs (review L255@92a657d). Record the baseline line count
in the summary.

**Hoist.** In `tests/test_devices/test_multizone.py` move the names imported inside the two test
methods into the existing module-scope `from lifx.protocol.protocol_types import (...)` block
(merging, not duplicating), and delete the two function-local import statements. Settle ordering
with `uv run --frozen ruff check --fix tests/test_devices/test_multizone.py` and
`uv run --frozen ruff format tests/test_devices/test_multizone.py`, naming that one file; never
run either against `.` or any other path, so an auto-fix cannot reach work outside this plan
(review L168@976b22a). Change nothing else in the file: no assertion, fixture, test body,
docstring or comment edit. The `# Mock StateEffect response` comments above the two inline
imports stay where they are; they still describe the code that follows. Removing a blank line
left behind is fine. The import-only diff gate below fails on any other change.

**Delete the entry.** Remove the `tests/test_devices/test_multizone.py` line from
`[tool.ruff.lint.per-file-ignores]`. Leave every other entry, including
`tests/test_animation/**`, for Task 2.

Stage only the two owned files, and commit with a Conventional Commit that names both changes
(for example `test: hoist multizone test imports and drop their PLC0415 exemption`, per review
L312@976b22a), no phase-number scope, `git commit -S -s`.
  </action>
  <verify>
    <automated>bash -c 'test -s .cache/phase-18/nodeids-before.txt || { echo BASELINE-MISSING; exit 1; }; N=$(wc -l < .cache/phase-18/nodeids-before.txt); echo "baseline-nodeids=$N"; test "$N" -gt 200 || exit 1; B=$(grep -vc "::" .cache/phase-18/nodeids-before.txt || true); echo "non-nodeid-lines=$B"; test "$B" -eq 0'</automated>
    <fails_when>non-zero exit, or the output contains "BASELINE-MISSING", or the printed baseline-nodeids value is 200 or fewer (a -q capture yields 0), or non-nodeid-lines is anything other than 0</fails_when>
    <automated>bash -c 'uv run --frozen ruff check .; S=$?; echo "ruff-exit=$S"; test "$S" -eq 0'</automated>
    <fails_when>the printed ruff-exit is anything other than 0</fails_when>
    <automated>bash -c 'grep -qF "\"tests/test_devices/test_multizone.py\" = [\"PLC0415\"]" pyproject.toml && { echo MULTIZONE-IGNORE-STILL-PRESENT; exit 1; }; echo MULTIZONE-IGNORE-DELETED'</automated>
    <fails_when>non-zero exit, or the output contains "MULTIZONE-IGNORE-STILL-PRESENT", or the line "MULTIZONE-IGNORE-DELETED" is absent</fails_when>
    <automated>uv run --frozen pytest tests/test_devices/test_multizone.py -q --no-cov -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or the summary line contains "failed", "error" or "no tests ran"</fails_when>
    <automated>uv run --frozen python -c "import ast, difflib, subprocess as sp; run = lambda *a: sp.run(list(a), check=True, capture_output=True, text=True).stdout; base = run('git', 'merge-base', 'HEAD', 'main').strip(); paths = run('git', 'diff', '--name-only', base, '--', 'tests/test_animation', 'tests/test_devices/test_multizone.py').split(); rng = lambda lines: {i for n in ast.walk(ast.parse(chr(10).join(lines))) if isinstance(n, (ast.Import, ast.ImportFrom)) for i in range(n.lineno - 1, n.end_lineno)}; pairs = [(p, run('git', 'show', base + ':' + p).splitlines(), open(p, encoding='utf-8').read().splitlines()) for p in paths]; bad = [(p, s, k + 1, t[k]) for p, o, n in pairs for ro, rn in [(rng(o), rng(n))] for tag, i1, i2, j1, j2 in difflib.SequenceMatcher(None, o, n, autojunk=False).get_opcodes() if tag in ('replace', 'delete', 'insert') for s, t, lo, hi, r in (('-', o, i1, i2, ro), ('+', n, j1, j2, rn)) for k in range(lo, hi) if t[k].strip() and k not in r]; print('swept-files=%r non-import-changes=%d' % (paths, len(bad))); [print('NON-IMPORT-CHANGE: %s %s%d: %s' % b) for b in bad]; assert not bad; print('SWEEP-DIFF-IMPORT-ONLY')"</automated>
    <fails_when>non-zero exit, or any line beginning "NON-IMPORT-CHANGE:", or the line "SWEEP-DIFF-IMPORT-ONLY" is absent, or the printed swept-files list does not include tests/test_devices/test_multizone.py</fails_when>
  </verify>
  <acceptance_criteria>
    - `.cache/phase-18/nodeids-before.txt` exists with more than 200 node IDs, every line a
      `::` node ID, captured with `--collect-only -qq` under `set -o pipefail` before any edit
      (the capture printed `baseline-collect-exit=0`).
    - `ruff check .` exits 0 with the multizone entry deleted.
    - `tests/test_devices/test_multizone.py` passes, and the import-only diff gate prints
      `SWEEP-DIFF-IMPORT-ONLY` with that file in `swept-files` and no `NON-IMPORT-CHANGE:` line.
    - The ruff fix and format commands named only `tests/test_devices/test_multizone.py`, and the
      commit stages only that file and `pyproject.toml`.
  </acceptance_criteria>
  <done>The multizone test module is under the repository-wide PLC0415 rule with no exemption and
  no behaviour change.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the animation test tree, delete its entry, prove the rule is live and no test was lost</name>
  <files>tests/test_animation/test_animator.py, pyproject.toml</files>
  <read_first>
    - `tests/test_animation/test_animator.py` :1-20 and every function containing a local import
      (list them with `uv run --frozen ruff check --isolated --select PLC0415 --output-format concise tests/test_animation`).
    - `pyproject.toml` :84-104 after Task 1.
  </read_first>
  <action>
**Hoist.** Move every function-local import in `tests/test_animation/test_animator.py` to the
module import block, deleting any that duplicate a name already imported at module scope, then
settle ordering with `uv run --frozen ruff check --fix tests/test_animation` and
`uv run --frozen ruff format tests/test_animation`, naming that path only; never run either
against `.` or any other path (review L168@976b22a). Change nothing else: no assertion, fixture,
test body, docstring or comment edit. Comments that sat above an inline import, such as the one
before the `struct` import in the frame-duration test, stay where they are. If any other module
under `tests/test_animation/` reports a PLC0415 site, hoist it the same way. No suppression
comment and no exclude: every site is hoisted. The import-only diff gate below fails on any
change outside an import statement in either swept file.

**Delete the entry and update the comment.** Remove the `tests/test_animation/**` line from
`[tool.ruff.lint.per-file-ignores]`. Rewrite the comment above the entries so the count of unswept
paths matches what remains and the sentence about this phase's two paths is gone; keep Phase 19's
sentence about `tests/test_theme/**` and the sentences about `src/**`, `.planning/**` and the two
archived skill harnesses intact.

**Prove it.** Capture `.cache/phase-18/nodeids-after.txt` with the same `--collect-only -qq`
command under `set -o pipefail` and require the two sorted files to be exactly equal: an
import-only sweep neither removes nor adds a test, so any difference in either direction is a
defect to explain and fix, not to accept. Run the positive and negative controls, the import-only
diff gate, the owned-paths confinement check and the full suite as the verify block does. Stage
only files under `tests/test_animation/` and `pyproject.toml`, and commit with a Conventional
Commit, no phase-number scope, `git commit -S -s`.
  </action>
  <verify>
    <automated>bash -c 'uv run --frozen ruff check .; S=$?; echo "ruff-exit=$S"; test "$S" -eq 0'</automated>
    <fails_when>the printed ruff-exit is anything other than 0</fails_when>
    <automated>bash -c 'grep -qF "\"tests/test_animation/**\" = [\"PLC0415\"]" pyproject.toml && { echo ANIMATION-IGNORE-STILL-PRESENT; exit 1; }; grep -qF "\"tests/test_theme/**\"" pyproject.toml || echo "NOTE: Phase 19 has already removed its entry"; echo ANIMATION-IGNORE-DELETED'</automated>
    <fails_when>non-zero exit, or the output contains "ANIMATION-IGNORE-STILL-PRESENT", or the line "ANIMATION-IGNORE-DELETED" is absent</fails_when>
    <automated>bash -c 'P=$(printf "def probe():\n    import os\n    return os\n"); for f in tests/test_animation/_plc0415_probe.py tests/test_devices/test_multizone.py; do O=$(printf "%s\n" "$P" | uv run --frozen ruff check --stdin-filename "$f" -); S=$?; echo "$f probe-exit=$S"; test "$S" -eq 1 || { echo "RULE-NOT-ACTIVE: $f"; exit 1; }; printf "%s\n" "$O" | grep -q PLC0415 || { echo "RULE-NOT-REPORTED: $f"; exit 1; }; done; echo RULE-ACTIVE-ON-BOTH-PATHS'</automated>
    <fails_when>non-zero exit, or any line beginning "RULE-NOT-ACTIVE:" or "RULE-NOT-REPORTED:", or the line "RULE-ACTIVE-ON-BOTH-PATHS" is absent</fails_when>
    <automated>bash -c 'N=$(grep -rn "noqa: PLC0415" tests || true); test -z "$N" || { echo "SUPPRESSION-ADDED:"; printf "%s\n" "$N"; exit 1; }; echo NO-SUPPRESSIONS'</automated>
    <fails_when>non-zero exit, or the output contains "SUPPRESSION-ADDED:", or the line "NO-SUPPRESSIONS" is absent</fails_when>
    <automated>bash -c 'set -o pipefail; test -s .cache/phase-18/nodeids-before.txt || { echo BASELINE-MISSING; exit 1; }; uv run --frozen pytest tests/test_animation tests/test_devices/test_multizone.py --collect-only -qq --no-cov -p no:cacheprovider | grep "::" | sort > .cache/phase-18/nodeids-after.txt || { echo "AFTER-COLLECTION-FAILED"; exit 1; }; D=$(diff .cache/phase-18/nodeids-before.txt .cache/phase-18/nodeids-after.txt); test -z "$D" || { echo "NODEID-SET-CHANGED:"; printf "%s\n" "$D"; exit 1; }; echo "before=$(wc -l < .cache/phase-18/nodeids-before.txt) after=$(wc -l < .cache/phase-18/nodeids-after.txt) NODEID-SETS-EQUAL"'</automated>
    <fails_when>non-zero exit, or the output contains "BASELINE-MISSING", "AFTER-COLLECTION-FAILED" or "NODEID-SET-CHANGED:", or the line containing "NODEID-SETS-EQUAL" is absent, or its before and after counts differ</fails_when>
    <automated>uv run --frozen python -c "import ast, difflib, subprocess as sp; run = lambda *a: sp.run(list(a), check=True, capture_output=True, text=True).stdout; base = run('git', 'merge-base', 'HEAD', 'main').strip(); paths = run('git', 'diff', '--name-only', base, '--', 'tests/test_animation', 'tests/test_devices/test_multizone.py').split(); rng = lambda lines: {i for n in ast.walk(ast.parse(chr(10).join(lines))) if isinstance(n, (ast.Import, ast.ImportFrom)) for i in range(n.lineno - 1, n.end_lineno)}; pairs = [(p, run('git', 'show', base + ':' + p).splitlines(), open(p, encoding='utf-8').read().splitlines()) for p in paths]; bad = [(p, s, k + 1, t[k]) for p, o, n in pairs for ro, rn in [(rng(o), rng(n))] for tag, i1, i2, j1, j2 in difflib.SequenceMatcher(None, o, n, autojunk=False).get_opcodes() if tag in ('replace', 'delete', 'insert') for s, t, lo, hi, r in (('-', o, i1, i2, ro), ('+', n, j1, j2, rn)) for k in range(lo, hi) if t[k].strip() and k not in r]; print('swept-files=%r non-import-changes=%d' % (paths, len(bad))); [print('NON-IMPORT-CHANGE: %s %s%d: %s' % b) for b in bad]; assert not bad; print('SWEEP-DIFF-IMPORT-ONLY')"</automated>
    <fails_when>non-zero exit, or any line beginning "NON-IMPORT-CHANGE:", or the line "SWEEP-DIFF-IMPORT-ONLY" is absent, or the printed swept-files list does not include both tests/test_animation/test_animator.py and tests/test_devices/test_multizone.py</fails_when>
    <automated>bash -c 'B=$(git merge-base HEAD main) || exit 1; L=$(git log --format=%h "$B"..HEAD -- tests/test_animation tests/test_devices/test_multizone.py pyproject.toml) || exit 1; test -n "$L" || { echo NO-SWEEP-COMMITS; exit 1; }; for c in $L; do F=$(git diff-tree --no-commit-id --name-only -r "$c") || exit 1; X=$(printf "%s\n" "$F" | grep -vxE "tests/test_devices/test_multizone\.py|tests/test_animation/.+|pyproject\.toml" | grep . || true); test -z "$X" || { echo "SWEEP-COMMIT-TOUCHES-UNOWNED-PATH: $c"; printf "%s\n" "$X"; exit 1; }; done; echo "sweep-commits=$(printf "%s\n" "$L" | grep -c .) SWEEP-CONFINED-TO-OWNED-PATHS"'</automated>
    <fails_when>non-zero exit, or the output contains "NO-SWEEP-COMMITS" or "SWEEP-COMMIT-TOUCHES-UNOWNED-PATH:", or the line containing "SWEEP-CONFINED-TO-OWNED-PATHS" is absent (every phase commit that touches a swept path or pyproject.toml must touch nothing else, which a repository-wide ruff auto-fix or format would break)</fails_when>
    <automated>uv run --frozen pytest -p no:cacheprovider --no-cov -q</automated>
    <fails_when>non-zero exit, or the summary line contains "failed" or "error"</fails_when>
    <automated>uv run --frozen ruff format --check . && uv run --frozen pyright</automated>
    <fails_when>non-zero exit from either, or pyright's summary reports 1 or more errors</fails_when>
  </verify>
  <acceptance_criteria>
    - `ruff check .` exits 0 with neither Phase 18 entry present.
    - The stdin probe is reported as PLC0415 (exit 1) under both path names.
    - No `tests/` line carries a PLC0415 suppression.
    - The before and after node-ID files, both captured with `--collect-only -qq` under
      `set -o pipefail`, are identical (`diff` prints nothing): no test lost and none added. The
      default suite passes.
    - The import-only diff gate prints `SWEEP-DIFF-IMPORT-ONLY` with both swept files listed and
      no `NON-IMPORT-CHANGE:` line, so every assertion, fixture, test body, docstring and comment
      is unchanged (the amended R6 evidence).
    - Every phase commit that touches a swept path or `pyproject.toml` touches nothing else
      (`SWEEP-CONFINED-TO-OWNED-PATHS`), and ruff's fix and format ran on the owned paths only.
    - The handoff comment's path count matches the remaining entries and still names Phase 19's path
      (unless Phase 19 already removed it).
  </acceptance_criteria>
  <done>Both of this phase's test paths are under the repository-wide PLC0415 rule, the handoff is
  closed, and no test was lost or changed in behaviour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lint configuration to CI | Per-file-ignores decide which files the rule guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-24 | Repudiation | Tests silently de-collected by a mechanical sweep | medium | mitigate | Exact-equality node-ID comparison (`--collect-only -qq`, `set -o pipefail`) against a baseline captured from a clean tree before any edit, failing closed when the baseline is missing, empty, or produced by a failed collection |
| T-18-25 | Tampering | A vacuous lint gate | medium | mitigate | Every ruff invocation asserts its exit status; a stdin probe proves the rule fires under both path names |
| T-18-26 | Tampering | Collateral edits to Phase 19's comment and entry | low | mitigate | Only this phase's two entries are removed; Phase 19's sentence and entry are preserved or its own removal is kept |
| T-18-27 | Tampering | A non-import edit, or a repository-wide ruff auto-fix, riding along with the import sweep | medium | mitigate | An ast- and difflib-backed gate fails on any changed line outside an import statement in either swept file; ruff fix and format are named on the owned paths only; a history check fails when any phase commit touching a swept path or `pyproject.toml` also touches another file |
</threat_model>

<!-- plan-criteria-allow: R4 - every shell git call in these gates is captured by command substitution and followed by || exit 1, and the Python gates run git through subprocess with check=True, so a git failure fails the gate before any pipe reads its output -->

<verification>
- `uv run --frozen ruff check .` exits 0 and the default suite passes.
- The before and after node-ID sets for the two paths, each collected with `--collect-only -qq`
  under `set -o pipefail`, are exactly equal.
- Both swept test files differ from the merge-base only inside import statements, and every
  sweep commit is confined to the owned paths.
</verification>

<success_criteria>
The PLC0415 handoff Phase 16 recorded for this phase is closed: no exemption remains for its test
paths and the suite is unchanged in behaviour.
</success_criteria>

## Review Dispositions Ledger

### Round 1 — 92a657d

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| Baseline node-ID command uses `--collect-only -q`, which prints no node IDs under pytest 9.1.1, so Task 1 fails its own `test -s` / `>200` gate (L255@92a657d, verified by the maintainer, L537@92a657d) | HIGH | Plan 08, Task 1: the baseline command in the action now uses `--collect-only -qq` (re-measured at 263 node IDs for both paths, unchanged after the split); the Task 1 verify also rejects any non-`::` line in the baseline file |
| The same broken command is repeated for the after-state comparison (L256@92a657d) | HIGH | Plan 08, Task 2: the after-state verify uses `--collect-only -qq` |
| Add `set -o pipefail` or assert the pytest exit status separately (L262@92a657d) | HIGH | Plan 08, Tasks 1 and 2: both captures run under `set -o pipefail`; Task 1 prints and asserts `baseline-collect-exit=0` and refuses to capture from a tree where either path or `pyproject.toml` differs from `HEAD`; Task 2 fails with `AFTER-COLLECTION-FAILED` |
| Require exact equality of the before and after node-ID sets (L263@92a657d) | HIGH | Plan 08, Task 2: `comm -23` (lost tests only) replaced with `diff` of the two sorted files, failing with `NODEID-SET-CHANGED:` on any difference; must_haves truth, acceptance criteria, `<verification>` and T-18-24 updated to exact equality |
| Collection-output assumptions must be tested against the locked environment, since `pyproject.toml` admits pytest 8.4.2 and later (L257@92a657d) | LOW | Plan 08, both tasks: every collection runs through `uv run --frozen` (the locked pytest 9.1.1), and the `>200` count, the non-`::` line check and `pipefail` fail closed if a future pytest changes the `-qq` format |
| Hoisting inline imports could introduce circular imports (Antigravity, L432@92a657d) | LOW | Plan 08, Task 2: already covered; the full default suite and the collection comparison both run after the hoist, and an import cycle fails collection |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (none) | Every 18-08 finding is addressed above |

### Round 2: phase split, 2026-09-23

| Change | Reason |
|--------|--------|
| Wave 4 to wave 3; `depends_on` is plans 03 and 06; requirements EFFECT-01 only | Moved to Phase 20: the Animator factory guard plan and its requirement. That plan was a predecessor only because it proved the animation tests unmodified; plans 03 and 06 still prove `tests/test_devices/test_multizone.py` unmodified |
| The hoist of `tests/test_animation/**` stays in this plan | The roadmap keeps it here (it is not Thread code) and D-12 assigns both paths to Phase 18; Phase 20 measures its own "animation tests unmodified" evidence against the post-hoist tree |
| PLC0415 sites (18 plus 2) and node-ID counts (263, 63) re-measured on this branch | No surviving plan adds a module or a test under either path |

### Round 3: cross-AI review round 2, 2026-09-23 (976b22a)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| Locked R6 says `tests/test_devices/test_multizone.py` passes "unmodified", but this plan edits it; a final-diff verifier will see a modification (Codex, L167@976b22a, L172@976b22a, L174@976b22a; consensus L473@976b22a) | MEDIUM | 18-SPEC R6 amended in place (see plan 07's Round 3 ledger) to permit the D-12 import-only hoist with assertions, fixtures and node IDs unchanged. Plan 08, Tasks 1 and 2: a new diff gate parses the merge-base and working-tree versions with `ast`, maps every changed non-blank line to an `Import`/`ImportFrom` node's line span (so a removed indented or parenthesised multi-line function-local import counts as import-only) and fails on any other change, comments included. The planner prototyped it in a scratch repository: a full simulated hoist of both files passes, and a single changed assertion fails with four `NON-IMPORT-CHANGE:` lines. Plans 03 and 06 run the same gate on `test_multizone.py`, and their byte-identical check now applies only while its `PLC0415` ignore is present, so every copy can be rerun at phase end. New must_haves truth, prohibition, T-18-27 and `<verification>` line |
| `ruff check --fix` / `ruff format` were not scoped to the owned files; a repository-wide auto-fix could alter unrelated work (Codex, L168@976b22a, L173@976b22a; consensus L476@976b22a) | LOW | Plan 08: Task 1 names `tests/test_devices/test_multizone.py` and Task 2 names `tests/test_animation` on both commands, never `.`; each task stages only its owned files; a new Task 2 history check fails when any phase commit touching a swept path or `pyproject.toml` touches anything else. must_haves truth and acceptance criteria |
| The pyproject artefact names Phase 19's `tests/test_theme/**` line as required content, false if Phase 19 lands first (OpenCode, L311@976b22a, L314@976b22a; consensus L487@976b22a) | LOW | Plan 08 artefact: `provides` now says the two Phase 18 entries are absent whether or not Phase 19 has removed its own, and `contains` anchors on the `"src/**"` entry, which stays in either order |
| Task 1's example commit message says only "hoist" although the commit also edits `pyproject.toml` (OpenCode, L312@976b22a) | LOW | Plan 08, Task 1: the example message names both changes |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| The `grep -vc "::"` check on the baseline always yields 0 because the capture already filters to `::` lines (Antigravity, L417@976b22a) | No change: the reviewer endorsed it as an invariant assertion; it guards against a future edit that drops the filter |

<output>
Create `.planning/phases/18-typed-move-and-morph-palette-effects/18-08-SUMMARY.md` when done
</output>
