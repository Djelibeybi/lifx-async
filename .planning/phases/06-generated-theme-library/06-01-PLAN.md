---
phase: 06-generated-theme-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/themes.jsonl
  - src/lifx/theme/generator.py
  - src/lifx/theme/data.py
  - src/lifx/theme/theme.py
  - src/lifx/theme/library.py
  - tests/test_theme/test_theme.py
  - tests/test_theme/test_theme_generator.py
  - pyproject.toml
  - codecov.yml
autonomous: true
requirements: [THEME-04, COMPAT-01, META-01, META-02, COMPAT-03]
estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A record whose slug is empty aborts generation with an error naming the offending display name (THEME-01 encoding edge, D-09)"
    - "Two records resolving to the same slug abort generation with an error naming both display names (THEME-01 adjacency edge)"
    - "A record with zero colours aborts generation with an error naming the offending display name (THEME-02 empty edge)"
    - "A record with a missing field, a wrong container type, a string numeric or a boolean colour value aborts generation with a controlled error naming the offending record and its JSONL line number — range checks alone do not establish integer-ness because bool is an int in Python (THEME-02/THEME-04 input validation)"
    - "Every key — primary slug and alias alike — passes one canonical-key validation: non-empty, str.isascii(), lowercase and str.isidentifier(); a primary slug additionally equals the D-09 derivation of its stored display name; an alias such as bad-alias, an empty alias or a Unicode alias aborts generation (THEME-01, D-09)"
    - "The generator writes atomically — emit to a uniquely named temp file created with tempfile.mkstemp in the target directory, format the temp file, rename over the target, and unconditionally remove the temp file on any failure path — so an interrupted or concurrent run leaves src/lifx/theme/data.py unchanged and leaves no stray temp file (THEME-04 concurrency edge, D-05)"
    - "Every emitted palette is canonically sorted by its normalised (hue, saturation, brightness, kelvin) tuple with duplicates preserved (D-24); two consecutive generator runs over the same input produce byte-identical output (THEME-02 ordering edge)"
    - "Duplicate colours within a palette are preserved exactly — multiset, not set (THEME-02 adjacency edge)"
    - "A caller-constructed Theme has None for slug, name and category, and Theme([...]) / Theme() behave exactly as before (META-01 empty edge, D-16, D-17)"
    - "ThemeLibrary.get('forrest') and ThemeLibrary.get('forest') return equal palettes and both report display name 'Forrest' and slug 'forrest' — the alias carries the target's identity (COMPAT-03, D-14)"
    - "Every pre-v1.2 theme name resolves at every commit in this plan — the generated dict is consulted first and the hand-written table remains as fallback until Plan 06-02 completes the import (COMPAT-01 intermediate-state guarantee)"
    - "Theme.__eq__ compares palettes as unordered multisets at uint16 precision, ignores identity, and Theme is unhashable (D-19, D-20)"
    - "Re-running uv run python -m lifx.theme.generator over the unchanged seed produces byte-identical output (THEME-04, convention-only check)"
    - "Regeneration reads only the committed data file — no network, no device (THEME-04 prohibition)"
  artifacts:
    - path: "data/themes.jsonl"
      provides: "Committed normalised theme data file (seed: 4 records covering the three record shapes), D-02/D-03"
    - path: "src/lifx/theme/generator.py"
      provides: "Theme data module generator with record-schema and canonical-key validation aborts, canonical palette sort (D-24) and atomic formatted write via a unique temp file"
      contains: "def main"
    - path: "src/lifx/theme/data.py"
      provides: "Generated theme data module: ThemeRecord dataclass + THEMES flat dict"
      contains: "DO NOT EDIT"
    - path: "src/lifx/theme/theme.py"
      provides: "Theme identity kwargs (slug/name/category) and palette-only multiset __eq__"
    - path: "src/lifx/theme/library.py"
      provides: "Transitional generated-first lookup with hand-written-table fallback"
    - path: "tests/test_theme/test_theme_generator.py"
      provides: "Generator validation, alias-expansion, round-trip and atomic-write tests"
    - path: "pyproject.toml"
      provides: "Coverage omit entries for the generator and the generated module (D-21, D-22)"
    - path: "codecov.yml"
      provides: "Codecov ignore entries for the generator and the generated module, matching the existing protocol/products generator entries (D-21, D-22)"
  key_links:
    - from: "src/lifx/theme/generator.py"
      to: "data/themes.jsonl"
      via: "reads the committed JSONL from the repo data directory — no download step"
      pattern: "themes\\.jsonl"
    - from: "src/lifx/theme/library.py"
      to: "src/lifx/theme/data.py"
      via: "imports THEMES for generated-first lookup"
      pattern: "from lifx.theme.data import"
    - from: "src/lifx/theme/data.py"
      to: "src/lifx/color.py"
      via: "HSBK literals at full float precision, round-trip-exact at uint16"
      pattern: "from lifx.color import HSBK"
    - from: "src/lifx/theme/library.py"
      to: "src/lifx/theme/theme.py"
      via: "get() constructs Theme over a fresh list with slug/name/category attached"
      pattern: "slug=record\\.slug"
  prohibitions:
    - statement: "MUST NOT ship a palette value that no capture record supports — no invented themes, no interpolated colours, no tidied palettes"
      status: resolved
      verification: judgment
    - statement: "MUST NOT reference the undocumented LIFX theme endpoints from shipped code; regeneration MUST NOT require a network or a device"
      status: resolved
      verification: judgment
    - statement: "MUST NOT claim more fidelity than was measured — a single product (Tile, 55), palette order lost to shuffling, palettes capped at the protocol's 16-colour ceiling"
      status: resolved
      verification: judgment
---

<objective>
Prove the whole generated-theme pipeline end to end on a thin slice: four normalised JSONL
records travel data file → generator → generated data module → `ThemeLibrary.get()`
returning app-accurate palette, display name and category, while every pre-v1.2 theme name
keeps resolving through a temporary fallback to the existing hand-written table.

This is the tracer for Phase 6 (THEME-04's foundational position): the generator, the data
module split (D-01), the record schema (D-02), the atomic formatted write (D-05), the flat
dict with alias keys (D-04, D-13, D-14) and the Theme identity fields (D-16–D-18) are all
production-quality from this plan onwards — Plan 06-02 only expands the record count and
removes the fallback. Also lands `Theme.__eq__`/unhashability (D-19, D-20), the canonical
palette sort (D-24 — the generator sorts every palette by its normalised
`(hue, saturation, brightness, kelvin)` tuple, duplicates preserved) and the
coverage-omission precedent (D-21, D-22).

Purpose: catch a wrong record schema, a broken emission format or a broken lookup path
after one commit, not after the 166-record import.
Output: `data/themes.jsonl` (seed), `src/lifx/theme/generator.py`,
`src/lifx/theme/data.py` (generated), identity-carrying `Theme`, transitional
`ThemeLibrary`, generator tests, coverage config.

SPEC edge coverage is 22/22 resolved with 0 unresolved — no assumptions are carried into
this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-generated-theme-library/06-SPEC.md
@.planning/phases/06-generated-theme-library/06-CONTEXT.md
@.planning/phases/06-generated-theme-library/06-PATTERNS.md
@.claude/theme-capture/README.md
@src/lifx/products/generator.py
@src/lifx/products/registry.py
</context>

## Artifacts this phase produces

Phase 6 as a whole (Plans 06-01 and 06-02) creates these symbols and files:

- `data/themes.jsonl` — committed normalised data file, repo data directory outside the
  package (new directory; the wheel already excludes it via `packages = ["src/lifx"]`)
- `src/lifx/theme/generator.py` — new module: `load_theme_records()`,
  `validate_records()`, `validate_key()` (canonical-key predicate applied to slugs AND
  aliases), `derive_slug()` (the D-09 derivation restated for stored ASCII names),
  `canonical_palette()` (D-24 stable sort by stored uint16 tuple), `emit_data_module()`,
  `format_generated_files()`, `main()`; runnable as
  `uv run python -m lifx.theme.generator`
- `src/lifx/theme/data.py` — generated module: `ThemeRecord` frozen dataclass
  (`slug: str`, `name: str`, `category: str`, `colors: tuple[HSBK, ...]`) and
  `THEMES: dict[str, ThemeRecord]` (168 keys at phase end)
- `Theme.slug`, `Theme.name`, `Theme.category` — new optional keyword-only identity
  attributes on `lifx.theme.theme.Theme` (default `None`)
- `Theme.__eq__` — palette-only unordered-multiset equality; `Theme` becomes unhashable
- Rewritten `ThemeLibrary.get()` (fresh list, identity attach, shortened `KeyError`),
  `ThemeLibrary.get_available_themes()` (168 sorted names at phase end)
- `tests/test_theme/test_theme_generator.py` — new test module
- `pyproject.toml` — two new `[tool.coverage.run] omit` entries
- No new names join `lifx.theme.__all__`; `src/lifx/theme/__init__.py` is unchanged

## Review revisions (2026-08-14, cross-AI review — 06-REVIEWS.md)

Findings incorporated into this plan's executable content:

| # | Finding (severity) | Disposition here |
|---|--------------------|------------------|
| 2 | `cheerful` tracer assertion vacuous — the pre-v1.2 table has **5** colours, not 7, so `len == 5` cannot discriminate generated-first from fallback (HIGH, confirmed) | Task 1 verify now compares `Counter(as_tuple)` multisets: equal to the seed record AND unequal to `_THEMES['cheerful']`; the false "table has 7" statement is corrected |
| 3 | Fixed temp filename `data_generated_tmp.py` races concurrent runs (MEDIUM, Codex+OpenCode) | Task 1 `main()` uses `tempfile.mkstemp(dir=OUTPUT_PATH.parent, suffix=".py")`, closes the handle before Ruff, removes the temp file in a `finally` even if Ruff or validation raises (Antigravity's cleanup point); Task 3 tests it |
| 4 | `str.isidentifier()` accepts Unicode; aliases only collision-checked, never validated (MEDIUM, Codex+OpenCode) | One `validate_key()` applied to primary slugs AND aliases: non-empty, `isascii()`, lowercase, `isidentifier()`; primary slugs additionally equal `derive_slug(name)`; same invariants re-asserted at emit time as defence in depth |
| 5 | Record-schema validation underspecified — `bool` passes integer range checks (MEDIUM, Codex) | `validate_records()` now checks exact field sets, container types, `type(v) is int` (rejects bools and string numerics), and every error names the record and its JSONL line number |
| D-24 | Palette order vs positional consumers (HIGH — operator decision) | `canonical_palette()` sorts every palette by its stored uint16 `(hue, saturation, brightness, kelvin)` tuple, stable, duplicates preserved, before emission; tracer verify asserts every emitted palette is sorted |
| 11 | `Theme.shuffled()`/`random()` drop identity, undermining D-18 (LOW, OpenCode) | Documented in the `Theme` class docstring as a known deferred gap — visible, not silent |
| 9 | COMPAT-01 missing from this plan's `requirements` (LOW, Codex) | Added — this plan implements the COMPAT-01 intermediate guarantee |
| 8 | 100% branch coverage claim unverified (MEDIUM, OpenCode) | Verified 2026-08-14: the gate is real — pyproject `addopts` carries `--cov-branch --cov-report=xml` and `codecov.yml` sets `coverage.status.patch.default.target: 100%`; pyproject has no `fail_under` (the gate is Codecov's PR patch status, not pytest). Task 3 also extends `codecov.yml` `ignore` to match the existing generator entries |

Rejected with rationale:

- **Antigravity's digit-leading slug relaxation** (allow `^[a-z0-9_]+$`, e.g. a future
  "80s Pop"): rejected. `06-SPEC.md:67` mandates identifier keys, and the orchestrator
  verified no captured theme name slugs to a digit-leading key. `isidentifier()` stays.
  The hypothetical is noted here as a future risk only: a digit-leading app theme would
  abort generation loudly, which is the correct failure mode until the SPEC changes.

<tasks>

<task type="tracer">
  <name>Task 1: One theme end to end — JSONL record → generator → data.py → ThemeLibrary.get()</name>
  <reversibility rating="costly">D-02: the JSONL record shape defined here is what the Phase 9 capture, analysis and regeneration tooling will read and write; changing it later touches all three together.</reversibility>
  <files>data/themes.jsonl, src/lifx/theme/generator.py, src/lifx/theme/data.py, src/lifx/theme/theme.py, src/lifx/theme/library.py</files>
  <read_first>
    - src/lifx/theme/library.py (current `_THEMES` table lines 40–405 and the API lines 407–560 — the `get()` being extended and the mutation bug being fixed)
    - src/lifx/theme/theme.py (constructor being extended with identity kwargs)
    - src/lifx/products/generator.py (the analog: `format_generated_files()` lines 20–55 to copy near-verbatim; emission style lines 106–195; `main()` lines 500–536 — note its download step and its format-in-place ordering are both deliberately NOT copied)
    - src/lifx/products/registry.py (lines 1–10: the DO-NOT-EDIT header format to adapt; `ProductInfo` dataclass-in-generated-module precedent)
    - src/lifx/color.py (`HSBK.from_protocol`, `to_protocol`, `as_tuple` — the existing uint16 encoding; `LightHsbk` import from `lifx.protocol.protocol_types` at line 41)
    - .claude/theme-capture/themes.jsonl (raw record shape; source of the seed records)
    - .claude/theme-capture/tools/analyse_themes.py (lines 46–57: the `slug()` function — the canonical D-09 derivation that produced the measured "138 distinct slugs, all isidentifier" facts; its final step collapses every run of non-alphanumeric characters to one underscore and strips leading/trailing underscores)
    - .planning/phases/06-generated-theme-library/06-CONTEXT.md (D-01–D-18 and the D-24 amendment — the locked decisions this task implements)
  </read_first>
  <action>
    Build the thinnest full path through every layer this phase touches. Per D-01 the
    generated module is `src/lifx/theme/data.py` (name at planner discretion, now fixed)
    and the hand-written `ThemeLibrary` stays in `library.py`; the generator never touches
    API code.

    **1. Seed `data/themes.jsonl`** (new repo directory `data/` at the repo root, outside
    `src/lifx` per D-03 — no packaging change needed, `packages = ["src/lifx"]` already
    excludes it). Four records, one JSON object per line, sorted by slug, covering the
    three record shapes:
    - `cheerful` — plain app record (category Moods, 5 colours) whose slug is shared with
      the pre-v1.2 library; proves generated-first precedence over the fallback (the app
      palette differs from the old 7-colour palette, which is the sanctioned THEME-03
      resync, not a break)
    - `evening` — pre-v1.2 orphan record, category `Library`, carrying the exact
      pre-v1.2 palette from `_THEMES["evening"]` (D-12; the existing pinned test for
      evening must keep passing)
    - `forrest` — app record (category Nature, 9 colours) carrying
      `"aliases": ["forest"]` (D-04; display name 'Forrest 🌳' ships emoji-stripped as
      `Forrest` per D-06)
    - `mondrian` — app-only record (category Art Series, 16 colours); proves a
      brand-new theme resolves

    Derive the seed records MECHANICALLY with a throwaway script in the session
    scratchpad (never retype palette values — the phase prohibition forbids shipping any
    value no capture record supports): read the raw capture, select the three app
    records by name ('Cheerful', 'Forrest 🌳', 'Mondrian'), read
    `ThemeLibrary._THEMES["evening"]` for the orphan, then normalise. Record schema
    (D-02 — pure JSONL, no header record, every field on the record, fixed key order):
    `slug` (str, pre-resolved), `name` (str, emoji-stripped display name), `category`
    (str, Title Case per D-07/D-10), `aliases` (list of str, present only when
    non-empty), `colors` (list of objects with integer fields `hue`, `saturation`,
    `brightness` in 0–65535 and `kelvin` in 1500–9000 — values uint16-normalised at rest
    via `HSBK.to_protocol()`/`as_tuple()`). Per D-24, store each record's colours
    canonically sorted by their `(hue, saturation, brightness, kelvin)` uint16 tuple,
    duplicates preserved — captured order is a shuffle accident, not data. Name/category
    stripping and slug derivation follow the single D-09 pipeline as implemented by
    `slug()` in `.claude/theme-capture/tools/analyse_themes.py:46-57` (reuse it
    mechanically, never re-implement by eye): strip emoji → NFKD → drop non-ASCII →
    trim; then for slugs lowercase and collapse every run of non-alphanumeric characters
    to a single underscore, stripping leading/trailing underscores — NOT a bare
    whitespace underscore-join, which would leave punctuation from names like
    `Valentine's` or `Dance/Pop` in the slug and fail `isidentifier()`; for categories
    Title Case. The four seed names carry no punctuation, but the rule is stated once
    here because Plan 06-02 feeds it 138 names, eight of which are punctuated. Drop the
    capture's `index` and `suspect_unchanged` fields (zero records carry
    suspect_unchanged true) and do not retain the raw app string (D-08 — it survives in
    the committed capture).

    **2. Write `src/lifx/theme/generator.py`** — stdlib-plus-lifx only, zero new
    dependencies. Structure, following `products/generator.py` with the deliberate D-05
    difference:
    - Module constants: `DATA_FILE = Path(__file__).resolve().parents[3] / "data" / "themes.jsonl"`,
      `OUTPUT_PATH = Path(__file__).parent / "data.py"`.
    - `load_theme_records(path)` — parse JSONL; a malformed line aborts with a
      RuntimeError naming the line number. Return each record with its line number
      attached (e.g. `(line_number, record)` pairs) so every later validation error can
      name the offending line.
    - `validate_key(key)` — ONE canonical-key predicate applied to primary slugs AND
      aliases (review finding: aliases were previously only collision-checked, so
      `bad-alias`, an empty alias or a Unicode alias could reach `THEMES`): a key must be
      a non-empty `str`, pass `str.isascii()` (`str.isidentifier()` alone accepts Unicode
      identifiers like `café`, which 06-SPEC.md:67's deterministic-ASCII rule forbids),
      equal its own `.lower()`, and pass `str.isidentifier()`. Do NOT relax to allow
      digit-leading keys — considered and rejected, see the review-revisions table.
    - `derive_slug(name)` — the D-09 derivation restated for stored (already
      emoji-stripped, ASCII) names: lowercase, collapse every run of non-alphanumeric
      characters to one underscore, strip leading/trailing underscores — the same final
      step as `analyse_themes.py::slug()`.
    - `canonical_palette(colors)` — D-24: return the palette sorted (stable) by the
      stored uint16 `(hue, saturation, brightness, kelvin)` tuple, duplicates preserved.
      Sorting a multiset preserves it, so THEME-02's comparison standard is unaffected.
    - `validate_records(records)` — abort with RuntimeError naming the offending display
      name (or slug) AND its JSONL line number when: a record is missing a required
      field, carries an unexpected field, or has a wrong container type (record not a
      JSON object, `colors` not a list of objects, `aliases` present but not a list of
      strings); a colour object is missing a field, carries an extra field, or holds a
      value where `type(v) is int` is false — this rejects booleans (in Python `bool`
      is an `int`, so range checks alone cannot establish integer-ness) and string
      numerics; a slug or alias fails `validate_key()`; a primary slug does not equal
      `derive_slug(record.name)`; two records (or a record and an alias, or two aliases)
      resolve to the same key — name BOTH display names; a record has zero colours; a
      colour field is out of range (hue/saturation/brightness outside 0–65535, kelvin
      not 0 and outside 1500–9000 — kelvin 0 is `KELVIN_SATURATED`, a legitimate wire
      value this repo never clamps or rejects on an inbound path).
    - `emit_data_module(records)` — return the module source as a string:
      DO-NOT-EDIT docstring adapted from `registry.py` naming `data/themes.jsonl` as the
      source and `lifx/theme/generator.py` as the generator, documenting that palette
      order is canonical — sorted by the normalised (hue, saturation, brightness,
      kelvin) tuple per D-24, because the app shuffles order on every application and
      captured order is an accident — and recording the D-09 slug-derivation rule as
      provenance; then
      `from __future__ import annotations`, `from dataclasses import dataclass`,
      `from lifx.color import HSBK`; a frozen `@dataclass class ThemeRecord` with fields
      `slug: str`, `name: str`, `category: str`, `colors: tuple[HSBK, ...]`;
      `THEMES: dict[str, ThemeRecord]` with one entry per record, keys sorted by slug;
      after the dict literal, one assignment line per alias in deterministic (sorted)
      order binding the alias key to the target's entry (e.g. the forest key is assigned
      the forrest entry — same frozen record object, so the alias carries the target's
      identity per D-13/D-14). Emit each record's colours through `canonical_palette()`
      (D-24) — the seed file is already stored sorted, but the generator enforces the
      order regardless so a hand-edited or future data file cannot ship an uncanonical
      palette. Emit every string through `repr()`. As defence in depth, re-assert at
      emit time that every key passes `validate_key()` and every name and category
      passes `str.isascii()` (review finding: emit-time asserts back the validator).
      Convert each stored uint16 colour to user-facing floats by constructing
      `HSBK.from_protocol(LightHsbk(...))` at generation time and emitting
      `HSBK(hue=..., saturation=..., brightness=..., kelvin=...)` with full-precision
      `repr()` floats; assert per colour that the emitted value's `as_tuple()` equals the
      stored uint16 tuple and abort on mismatch (guards the round-trip). Preserve
      duplicate colours exactly — multiset, never a set.
    - `format_generated_files(*paths)` — copy from `products/generator.py` lines 20–55
      near-verbatim (ruff format + ruff check --fix via `subprocess.run` with fixed argv
      `[sys.executable, "-m", "ruff", ...]`, no shell, RuntimeError on failure).
    - `main()` — load → validate → emit → write to a UNIQUELY named temp file from
      `tempfile.mkstemp(dir=OUTPUT_PATH.parent, suffix=".py")` (same directory so
      `Path.replace()` stays a same-filesystem atomic rename; a real `.py` suffix so
      ruff accepts it; a unique name so two concurrent generator runs cannot race on a
      shared temp path — review finding: the previously proposed fixed name
      `data_generated_tmp.py` did not deliver the concurrent-run truth). Close the
      mkstemp file handle before Ruff runs, then `format_generated_files(temp)` →
      `temp.replace(OUTPUT_PATH)`; unconditionally remove the temp file in a `finally`
      block (`unlink(missing_ok=True)` — a no-op after a successful rename) so even a
      Ruff crash or a validation error mid-write leaves no temp file behind. This is
      D-05's stricter ordering: the committed file is only ever replaced by finished,
      formatted output, and an interrupted run leaves it unchanged. Unlike the products
      generator there is no fetch step of any kind — the generator reads only the
      committed local data file (SPEC prohibition; regeneration works offline).
    - `if __name__ == "__main__": main()` so it runs as
      `uv run python -m lifx.theme.generator`.

    **3. Run the generator** to produce `src/lifx/theme/data.py` and commit the output
    (the generated module, not the data file, is the shipped artefact per D-03).

    **4. Extend `Theme.__init__`** (theme.py) per D-16/D-17: signature becomes
    `def __init__(self, colors: list[HSBK] | None = None, *, slug: str | None = None, name: str | None = None, category: str | None = None)`
    — keyword-only, all defaulting to None, assigned to `self.slug`, `self.name`,
    `self.category`. Positional callers and `Theme()` are untouched. Update the class
    docstring Attributes section. Slug and display name are both reachable so code can
    round-trip a theme back to its key without reversing the derivation (D-18). Leave
    `shuffled()`, `random()`, `add_color()` and `__repr__` unchanged — identity
    propagation through copies is out of scope — but per the review, make the gap
    visible rather than silent: note in the class docstring that `shuffled()` and
    `random()` return identity-less copies (slug, name and category do not propagate),
    a known deferred limitation of D-18's round-trip guarantee. Equality lands in
    Task 2, not here.

    **5. Transitional lookup in `ThemeLibrary`** (library.py): add
    `from lifx.theme.data import THEMES` at module top. `get()` lowercases as today,
    then: if the name is in `THEMES`, return
    `Theme(list(record.colors), slug=record.slug, name=record.name, category=record.category)`
    — a fresh list, fixing the shared-list mutation leak for the generated path; else if
    in `cls._THEMES`, return `Theme(list(cls._THEMES[normalized_name]))` — also a fresh
    list, fixing the verified live bug for the fallback path too. Keep the existing
    KeyError message wording for now (the shortened message and its test change land
    together in Plan 06-02). `get_available_themes()` stays exactly
    `sorted(cls._THEMES.keys())` during the transition — the existing tests pin the
    57-name count, and a seed key may gain resolution before it is advertised; nothing
    vanishes. Plan 06-02 switches the listing to the generated dict and completes D-15
    (listing matches exactly what get() accepts). `get_by_category()` and `get_theme()`
    are untouched.
    The hand-written `_THEMES` table stays in place — the no-key-vanishes guarantee
    (COMPAT-01) binds this commit, and the fallback preserves all 57 names.
  </action>
  <verify>
    <automated>uv run python -m lifx.theme.generator && H1=$(shasum src/lifx/theme/data.py | cut -d' ' -f1) && uv run python -m lifx.theme.generator && H2=$(shasum src/lifx/theme/data.py | cut -d' ' -f1) && [ "$H1" = "$H2" ] && uv run python -c "
import json
from collections import Counter
from lifx.theme import ThemeLibrary as L
m = L.get('mondrian'); assert (m.slug, m.name, m.category) == ('mondrian', 'Mondrian', 'Art Series'), m.slug
f1, f2 = L.get('forest'), L.get('forrest')
assert Counter(c.as_tuple() for c in f1) == Counter(c.as_tuple() for c in f2)
assert f1.name == 'Forrest' and f1.slug == 'forrest' and f1.category == 'Nature'
e = L.get('evening'); assert e.category == 'Library' and len(e) == 3
a = L.get('autumn'); assert a.slug is None and a.name is None and a.category is None
seed = {json.loads(l)['slug']: json.loads(l) for l in open('data/themes.jsonl')}
exp = Counter((c['hue'], c['saturation'], c['brightness'], c['kelvin']) for c in seed['cheerful']['colors'])
c = L.get('cheerful'); got = Counter(x.as_tuple() for x in c)
old = Counter(x.as_tuple() for x in L._THEMES['cheerful'])
assert c.name == 'Cheerful' and got == exp, 'cheerful does not match the seed record'
assert got != old, 'fallback won: generated-first precedence is broken'
for s in seed:
    pal = [x.as_tuple() for x in L.get(s)]
    assert pal == sorted(pal), s + ': palette not canonically sorted (D-24)'
listing = L.get_available_themes(); assert len(listing) == 57 and 'autumn' in listing and 'sports' in listing
t = L.get('evening'); n = len(t); t.add_color(t[0]); assert len(L.get('evening')) == n
print('tracer ok')" && uv run pytest tests/test_theme/ -q && uv run ruff check . && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - `data/themes.jsonl` exists with exactly 4 lines, each valid JSON; `LC_ALL=C grep -c '[^ -~]' data/themes.jsonl` finds 0 lines (pure ASCII)
    - `src/lifx/theme/data.py` exists, starts with a docstring containing `DO NOT EDIT`, contains `class ThemeRecord` and `THEMES: dict[str, ThemeRecord]`, and contains a `forest` key assignment bound to the `forrest` entry
    - `uv run python -m lifx.theme.generator` exits 0 twice in a row with byte-identical `data.py` (shasum comparison above)
    - `ThemeLibrary.get('mondrian')` returns slug `mondrian`, name `Mondrian`, category `Art Series` (new app theme resolves)
    - `ThemeLibrary.get('forest')` and `get('forrest')` return multiset-equal palettes, both with name `Forrest` (D-14)
    - `ThemeLibrary.get('autumn')` still resolves via the fallback with all three identity fields None (D-17; COMPAT-01 intermediate state)
    - `ThemeLibrary.get('cheerful')` returns a palette multiset-equal (Counter over `as_tuple()`) to the seed record AND multiset-unequal to `_THEMES['cheerful']` — the pre-v1.2 table also has 5 colours (library.py:64-70; the plan previously claimed 7, which was wrong), so a length check cannot discriminate; only the Counter pair proves generated-first precedence over the fallback
    - Every seed theme's palette is emitted canonically sorted by its uint16 `(hue, saturation, brightness, kelvin)` tuple (D-24)
    - Mutating a returned Theme via `add_color` does not change the next `get()` of the same name (fresh-list fix, both paths)
    - `uv run pytest tests/test_theme/ -q` exits 0 with no test file modified in this task
    - `uv run ruff check .` exits 0 and `uv run pyright` reports 0 errors
    - `! grep -q 'urlopen\|http' src/lifx/theme/generator.py` succeeds (no network path in the generator)
  </acceptance_criteria>
  <done>The four seed records travel data file → generator → generated module → `ThemeLibrary.get()` returning app-accurate palette, display name and category; all 57 pre-v1.2 names still resolve; the generator validates, formats a temp file and renames atomically; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Theme equality — palette-only multiset, deliberately unhashable</name>
  <reversibility rating="costly">D-19: equality semantics are observable by every caller of Theme and by the test suite; changing them later is a coordinated behaviour change, not a refactor.</reversibility>
  <files>src/lifx/theme/theme.py, tests/test_theme/test_theme.py</files>
  <read_first>
    - src/lifx/theme/theme.py (as left by Task 1 — identity kwargs already present)
    - src/lifx/color.py lines 282–295 (`HSBK.__eq__`/`__hash__` — the documentation style to copy and the uint16-granularity hash that makes Counter work)
    - tests/test_theme/test_theme.py (existing test classes and style to extend)
    - .planning/phases/06-generated-theme-library/06-CONTEXT.md (D-19, D-20 and the deferred-ideas note rejecting immutability)
  </read_first>
  <behavior>
    - Two Themes with the same colours in different orders compare equal (the app shuffles palette order; order is never compared)
    - Two Themes differing only in duplicate counts of a colour compare unequal (multiset, not set)
    - Identity is ignored: a library theme and a caller-constructed Theme with the same palette but no identity compare equal — `love` and `romance` (genuinely identical palettes in the pre-v1.2 table) compare equal
    - Comparing a Theme with a non-Theme returns NotImplemented (so `theme == 3` is False without raising)
    - `hash(theme)` raises TypeError — defining `__eq__` without `__hash__` makes Theme unhashable, the locked outcome for a mutable object whose palette can change via `add_color()` (D-20)
    - Caller-constructed `Theme([HSBK(...)])` still iterates, indexes, len()s and add_color()s exactly as before; identity fields default to None (D-17)
  </behavior>
  <action>
    Write the failing tests first (the behaviour list above), then implement
    `Theme.__eq__` in theme.py: return NotImplemented unless `other` is a Theme, else
    compare `collections.Counter(self.colors) == Counter(other.colors)` — HSBK is
    hashable at uint16 granularity so this gives multiset equality at protocol precision
    with no new comparison code. Copy the documentation style of `HSBK.__eq__`
    (docstring explaining WHY order and identity are excluded: the app shuffles palette
    order on every application, and identity-bearing and caller-built themes with the
    same palette are the same theme). Do NOT define `__hash__` — Python setting it to
    None is the locked outcome (D-20); do not hand-write a hash over the colour tuple.
    `from collections import Counter` goes at the top of the file with the other imports.
    Add tests for identity kwargs while here: kwargs stored, defaults None, positional
    construction unchanged. Ensure both `__eq__` branches (Theme and non-Theme) and both
    outcomes (equal/unequal) are exercised — the CI gate on this hand-written file is
    Codecov's PR patch status at target 100% over branch-measured data (verified
    2026-08-14: pyproject `addopts` carries `--cov-branch --cov-report=xml` and
    codecov.yml sets `coverage.status.patch.default.target: 100%`; D-21).
  </action>
  <verify>
    <automated>uv run pytest tests/test_theme/test_theme.py -q && uv run pytest tests/ -q -x --ignore=tests/test_effects && uv run ruff check . && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - `src/lifx/theme/theme.py` contains `def __eq__` and does NOT contain `def __hash__`
    - Tests assert each behaviour listed above: shuffled-order equality, duplicate-count inequality, identity-ignored equality, `hash(Theme())` raising TypeError, and `Theme() == object()` evaluating False without raising
    - `uv run python -c "from lifx.theme import Theme; print(Theme.__hash__)"` prints `None`
    - `uv run pytest tests/test_theme/test_theme.py -q` exits 0
    - `uv run pyright` reports 0 errors and `uv run ruff check .` exits 0
  </acceptance_criteria>
  <done>Theme equality is palette-only unordered-multiset at uint16 precision, Theme is unhashable, existing Theme behaviour is untouched, and every new branch is covered by tests.</done>
</task>

<task type="auto">
  <name>Task 3: Generator hardening tests and coverage configuration</name>
  <files>tests/test_theme/test_theme_generator.py, pyproject.toml, codecov.yml, src/lifx/theme/generator.py</files>
  <read_first>
    - src/lifx/theme/generator.py (as written by Task 1 — the functions under test)
    - tests/test_products/test_product_generator.py (the analog: structure, fixture style, section-divider comments, direct function imports, tmp_path usage)
    - pyproject.toml lines 130–136 (the `[tool.coverage.run] omit` block being extended)
    - codecov.yml (the `ignore:` list already carrying protocol/generator.py, protocol_types.py and products/generator.py — the precedent being extended)
    - .planning/phases/06-generated-theme-library/06-CONTEXT.md (D-21, D-22, D-23, D-24 — what is and is not tested)
  </read_first>
  <action>
    Create `tests/test_theme/test_theme_generator.py` modelled on
    `test_product_generator.py`: module docstring listing coverage areas, plain pytest
    fixtures returning literal JSONL record dicts (a minimal valid record set and
    malformed variants), tests importing the generator functions directly. All tests run
    against fixture data in `tmp_path` — never against the committed `data/themes.jsonl`
    (D-23: the data file is the record of what should exist; no drift check, no count
    assertions against real data). Cover:
    - a record with an empty slug aborts with a RuntimeError whose message contains the
      offending display name
    - a slug failing `str.isidentifier()` (e.g. `1bad` or `bad-slug`) aborts naming the
      display name
    - a slug passing `str.isidentifier()` but failing `str.isascii()` (e.g. `café`)
      aborts — the canonical-key check closes the Unicode-identifier loophole
    - an alias failing `validate_key()` aborts: a hyphenated alias, an empty alias and a
      Unicode alias each abort naming the carrying record (review finding: aliases were
      previously only collision-checked)
    - a primary slug that does not equal `derive_slug(name)` aborts naming the record
    - schema aborts name the record AND its JSONL line number: a record missing a
      required field; a colour object missing a field; a colour value that is a string
      numeric; a colour value that is a bool (must abort even though `True` passes an
      integer range check); `colors` not a list
    - two records with the same slug abort with a message containing BOTH display names;
      an alias colliding with an existing slug likewise aborts
    - a record with an empty colours list aborts naming the display name
    - a colour with an out-of-range field aborts; kelvin 0 does NOT abort
      (`KELVIN_SATURATED` is a legitimate wire value)
    - canonical palette order (D-24): a fixture record whose colours arrive unsorted is
      emitted sorted by its uint16 `(hue, saturation, brightness, kelvin)` tuple, with a
      duplicated colour surviving in both positions
    - alias expansion: the emitted module binds the alias key to the target's entry so
      both keys share one record carrying the target's slug/name/category (D-13, D-14)
    - uint16 round-trip: for boundary values (0, 65535 and a mid value), the emitted
      HSBK literal's `as_tuple()` equals the stored uint16 tuple
    - duplicate colours in a record survive into the emitted module exactly (multiset)
    - deterministic output: `emit_data_module()` called twice over the same records
      returns identical strings
    - atomic write: patch the format step to raise, run `main()` against tmp_path
      copies, assert the output target is byte-for-byte unchanged and the output
      directory holds no stray file afterwards (glob the directory — the mkstemp name is
      unique per run, so assert on directory contents rather than one fixed temp name;
      this also proves the `finally` cleanup fires when Ruff raises)
    - the emitted module source starts with a docstring containing the do-not-edit
      warning and names both the data file and the generator
    Fix any generator defects these tests expose (this task owns generator.py for that
    purpose). Then extend `[tool.coverage.run] omit` in pyproject.toml with
    `src/lifx/theme/generator.py` and `src/lifx/theme/data.py` (D-21, D-22 — the exact
    precedent: generators are omitted from coverage but still carry a test file; the
    hand-written library.py/theme.py changes stay fully covered). Remove the stray blank
    line inside the existing omit list while editing it. Extend `codecov.yml`'s
    `ignore:` list with the same two paths — the discovered precedent spans both files
    (protocol/generator.py, protocol_types.py and products/generator.py appear in both
    the pyproject omit list and the codecov ignore list), and codecov's PR patch status
    is the actual CI gate (review finding 8, verified 2026-08-14).
  </action>
  <verify>
    <automated>uv run pytest tests/test_theme/test_theme_generator.py -q && uv run pytest tests/test_theme/ -q && grep -c 'src/lifx/theme/generator.py' pyproject.toml | grep -q '^1$' && grep -c 'src/lifx/theme/data.py' pyproject.toml | grep -q '^1$' && grep -c 'src/lifx/theme/generator.py' codecov.yml | grep -q '^1$' && grep -c 'src/lifx/theme/data.py' codecov.yml | grep -q '^1$' && uv run ruff check . && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_theme/test_theme_generator.py` exists and `uv run pytest tests/test_theme/test_theme_generator.py -q` exits 0 with at least 14 tests collected
    - Empty-slug, non-identifier-slug, non-ASCII-slug, invalid-alias, derivation-mismatch, schema (missing field / bool / string numeric / wrong container, each naming record and line number), duplicate-slug, alias-collision, zero-colour and out-of-range aborts each verified with message-content assertions
    - A test proves a bool colour value aborts — the discriminating case for integer-ness
    - A test proves canonical palette ordering (D-24) with a duplicate colour preserved
    - Atomic-write test proves an interrupted run leaves the target unchanged and the output directory free of stray files
    - pyproject.toml `[tool.coverage.run] omit` and codecov.yml `ignore:` each list both `src/lifx/theme/generator.py` and `src/lifx/theme/data.py` exactly once
    - No test in the new file opens the committed `data/themes.jsonl`
    - `uv run pytest tests/test_theme/ -q` exits 0; `uv run ruff check .` and `uv run pyright` are clean
  </acceptance_criteria>
  <done>The generator's schema and canonical-key validation aborts, alias expansion, canonical palette ordering, round-trip exactness, determinism and unique-temp atomic write are pinned by tests over fixture data; the generator and generated module are excluded from coverage in both pyproject.toml and codecov.yml per the products/protocol precedent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/themes.jsonl → data.py | A committed data file becomes executable Python via the generator — trusted-but-validated input crossing into shipped code |
| generator → subprocess | The generator shells out to ruff over the temp file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | emit_data_module() string fields | medium | mitigate | Every string reaches the generated source only through repr(); every key — slug AND alias — must pass the canonical-key check (non-empty, ASCII, lowercase, isidentifier); colour fields validated as genuine ints (`type(v) is int`, bools rejected) in range; malformed JSON or a schema violation aborts naming the record and line; emit-time asserts back the validator — a hostile record cannot inject statements into data.py |
| T-06-02 | Tampering | format_generated_files() subprocess | low | mitigate | Fixed argv [sys.executable, -m, ruff, ...] with no shell and Path-derived arguments, copied from the audited products generator |
| T-06-03 | Tampering (supply chain) | regeneration inputs | medium | mitigate | Generator reads only the committed local data file — no network fetch, no device access (SPEC prohibition, gated by acceptance criteria); this phase installs zero new packages, so no package-legitimacy audit applies |
| T-06-04 | Repudiation | data.py vs data file drift | low | accept | D-23: operator declined a drift gate knowingly; regeneration idempotence is verified by hand (double-run hash check in this plan) and drift after a stale regeneration ships silently — accepted |
</threat_model>

<verification>
- `uv run python -m lifx.theme.generator` run twice produces byte-identical `src/lifx/theme/data.py`
- All 57 pre-v1.2 names resolve at every commit (fallback in place); the four seed themes resolve with correct identity
- `ThemeLibrary.get('cheerful')` discriminates generated-first from fallback by Counter equality/inequality, not length
- Every emitted palette is canonically sorted by its uint16 tuple (D-24)
- `uv run pytest tests/test_theme/ -q`, `uv run ruff check .`, `uv run pyright` all clean at every task boundary
- Theme equality/unhashability behaviour pinned by tests; generator aborts pinned by tests over fixtures
</verification>

<success_criteria>
- The full pipeline (data file → generator → generated module → ThemeLibrary.get with identity) works end to end for the seed and is committed, production-quality
- Theme carries optional identity (D-16–D-18), palette-only multiset equality (D-19) and is unhashable (D-20)
- Generator schema/canonical-key validation aborts, canonical palette ordering (D-24), unique-temp atomic formatted write (D-05) and coverage omissions (D-21, D-22 — pyproject and codecov.yml) are in place
- No pre-v1.2 key stops resolving in any commit of this plan
</success_criteria>

<output>
Create `.planning/phases/06-generated-theme-library/06-01-SUMMARY.md` when done.
</output>
