---
phase: 06-generated-theme-library
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - data/themes.jsonl
  - src/lifx/theme/data.py
  - src/lifx/theme/library.py
  - tests/test_theme/test_library.py
  - tests/test_api/test_api_apply_theme.py
  - tests/test_effects/test_spin.py
  - tests/test_effects/test_rule_trio.py
autonomous: true
requirements: [THEME-01, THEME-02, THEME-03, COMPAT-01, COMPAT-03, META-01, META-02]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "All 138 non-sport app slugs resolve via ThemeLibrary.get() (THEME-01)"
    - "Every shipped app palette equals its captured palette as a uint16-normalised unordered multiset, duplicates included — verified mechanically against the raw capture during the data build and again after the cutover (THEME-02)"
    - "The 25 differing shared slugs return the app palette; kelvin 8000 appears among soothing's colours where the pre-v1.2 palette was uniformly 3500 (THEME-03)"
    - "The 2 already-identical shared slugs are carried without diff — only the 25 differing shared slugs change (THEME-03 unclassified edge)"
    - "All 57 pre-v1.2 keys resolve, checked against a literal fixture list not derived from the new library (COMPAT-01 empty edge)"
    - "aurora_borealis/aurora and forest/forrest each return equal palettes, and each alias reports the target's display name, slug and category (COMPAT-03, D-14; empty edge — both rename-map entries asserted)"
    - "Unknown slug raises KeyError carrying the requested name and a pointer to get_available_themes(), the full listing dropped (THEME-01 empty edge)"
    - "get_available_themes() stays sorted by slug and lists exactly the keys get() accepts — 168 names, everything resolvable (THEME-01 ordering edge, D-15)"
    - "get() keeps lowercasing its input; every shipped key is already ASCII lowercase (COMPAT-01 encoding edge)"
    - "Resolution is by key and order-independent (COMPAT-01 ordering edge)"
    - "An app slug colliding with a carried-forward orphan resolves app-wins with the orphan dropped loudly by the conversion; verified zero live instances (COMPAT-01 adjacency edge)"
    - "christmas resolves to the Holidays record; the identical Archives twin is dropped, recorded in phase docs only (META-02 unclassified edge)"
    - "Emoji are stripped from display names and categories, slugs derive from the stripped name via the single D-09 pipeline, and no shipped display name or category contains a non-ASCII character (META-01 encoding edge, D-06)"
    - "ThemeLibrary.get('forrest') exposes display name 'Forrest'; ThemeLibrary.get('christmas') exposes category 'Holidays' (META-01/META-02 acceptance)"
    - "Every one of the 138 app slugs exposes one of the 8 app categories; each of the 28 orphan pre-v1.2-only keys exposes Library, while the 2 rename keys (forest, aurora_borealis) expose their target's category per D-14 (META-02, D-10)"
    - "Every key in get_available_themes() satisfies str.isidentifier() (THEME-01 acceptance)"
    - "Mutating a returned Theme does not change what the next get() of the same slug returns (mutation-leak fix)"
    - "No captured palette is empty (minimum observed: 2 colours); a zero-colour record aborts generation (THEME-02 empty edge)"
    - "No key ending in the retired legacy suffix exists anywhere in the library (COMPAT-02 retired — replacement criterion for the moot edges, D-11)"
    - "Re-running the generator over unchanged inputs leaves git status clean — verified once by hand, convention only, matching packets.py/registry.py (THEME-04 acceptance)"
    - "get_by_category() still returns themes for its existing category names, textually untouched — Phase 7 owns META-04"
  artifacts:
    - path: "data/themes.jsonl"
      provides: "The full committed data file: 166 records (138 app + 28 Library orphans), aliases on the aurora and forrest records, uint16-normalised, pure ASCII, sorted by slug"
    - path: "src/lifx/theme/data.py"
      provides: "Regenerated module carrying all 168 resolvable keys"
      contains: "DO NOT EDIT"
    - path: "src/lifx/theme/library.py"
      provides: "Hand-written ThemeLibrary over the generated dict: fresh-list get() with identity, shortened KeyError, 168-name listing; hand-written _THEMES literal deleted"
    - path: "tests/test_theme/test_library.py"
      provides: "COMPAT-01 literal 57-key fixture, rename-pair, soothing-kelvin, mutation-leak, KeyError-message, identifier/ASCII/category sweeps"
  key_links:
    - from: "src/lifx/theme/library.py"
      to: "src/lifx/theme/data.py"
      via: "single-source lookup — _THEMES becomes an alias of the generated THEMES dict so get_by_category() survives untouched"
      pattern: "_THEMES.*=.*THEMES"
    - from: "data/themes.jsonl"
      to: ".claude/theme-capture/themes.jsonl"
      via: "mechanical conversion verified multiset-equal at uint16 precision — no hand-typed palette values"
      pattern: "themes\\.jsonl"
    - from: "tests/test_theme/test_library.py"
      to: "src/lifx/theme/library.py"
      via: "literal pre-v1.2 key fixture resolving every historical name"
      pattern: "PRE_V12_KEYS"
  prohibitions:
    - statement: "MUST NOT ship a palette value that no capture record supports — no invented themes, no interpolated colours, no tidied palettes"
      status: resolved
      verification: judgment
    - statement: "MUST NOT reference the undocumented LIFX theme endpoints from shipped code; regeneration MUST NOT require a network or a device"
      status: resolved
      verification: judgment
    - statement: "MUST NOT claim more fidelity than was measured — a single product (Tile, 55), palette order lost to shuffling, palettes capped at the protocol's 16-colour ceiling"
      status: resolved
      verification: judgment
---

<objective>
Expand the proven Plan 06-01 pipeline from 4 seed records to the full library: build the
complete `data/themes.jsonl` (166 records — 138 app themes plus 28 pre-v1.2 orphans, with
rename aliases on the `aurora` and `forrest` records per D-04), regenerate
`src/lifx/theme/data.py` to its final 168-key form, cut `ThemeLibrary` over to the
generated dict alone (deleting the 366-line hand-written `_THEMES` table and the
transitional fallback), shorten `get()`'s `KeyError` per SPEC, and pin the phase's
compatibility and metadata guarantees with tests.

Purpose: this completes THEME-01/02/03, COMPAT-01/03 and META-01/02 — every non-sport app
theme resolves by ASCII slug with app-accurate colours and app metadata, and no name that
resolved before v1.2 breaks.
Output: full data file, final generated module, rewritten hand-written `library.py`,
acceptance tests.

SPEC edge coverage is 22/22 resolved with 0 unresolved — no assumptions are carried into
this plan. The four COMPAT-02 edges are moot (requirement retired, D-11); their
replacement criterion — no legacy-suffixed key exists — is carried in must_haves.truths.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-generated-theme-library/06-SPEC.md
@.planning/phases/06-generated-theme-library/06-CONTEXT.md
@.planning/phases/06-generated-theme-library/06-PATTERNS.md
@.planning/phases/06-generated-theme-library/06-01-SUMMARY.md
@.claude/theme-capture/README.md
</context>

## Artifacts this phase produces

Phase 6 as a whole (Plans 06-01 and 06-02) creates these symbols and files:

- `data/themes.jsonl` — committed normalised data file (final form in this plan: 166
  records, 168 resolvable names)
- `src/lifx/theme/generator.py` — generator module (built in 06-01):
  `load_theme_records()`, `validate_records()`, `emit_data_module()`,
  `format_generated_files()`, `main()`
- `src/lifx/theme/data.py` — generated module: `ThemeRecord` frozen dataclass and
  `THEMES: dict[str, ThemeRecord]` (168 keys after this plan)
- `Theme.slug`, `Theme.name`, `Theme.category` identity attributes; `Theme.__eq__`;
  `Theme` unhashable (built in 06-01)
- Rewritten `ThemeLibrary.get()` / `get_available_themes()`; `get_by_category()` and
  `get_theme()` textually untouched
- `tests/test_theme/test_theme_generator.py` (built in 06-01) and the acceptance tests
  in `tests/test_theme/test_library.py` (this plan)
- `pyproject.toml` coverage omit entries (built in 06-01)
- No new names join `lifx.theme.__all__`; `src/lifx/theme/__init__.py` is unchanged

<tasks>

<task type="auto">
  <name>Task 1: Build the full committed data file — 166 records from capture plus orphans</name>
  <reversibility rating="one-way">D-06: emoji-stripped display names and categories are public API values once released — restoring emoji later changes what every caller's output, logs and docs render. The operator locked this door in discuss-phase (06-CONTEXT.md D-06), so no checkpoint re-asks it; the rating is kept for the record.</reversibility>
  <files>data/themes.jsonl</files>
  <read_first>
    - .claude/theme-capture/themes.jsonl (the 179-record raw capture — sole source for app palettes)
    - .claude/theme-capture/README.md (capture method and the four caveats)
    - src/lifx/theme/library.py (the `_THEMES` table, still present until Task 2 — sole source for the 28 orphan palettes)
    - data/themes.jsonl (the 4-record seed from Plan 06-01 — the schema being expanded; this task replaces its contents wholesale)
    - src/lifx/theme/generator.py (the validation rules the file must satisfy)
    - .planning/phases/06-generated-theme-library/06-CONTEXT.md (D-02, D-04, D-06–D-10, D-12 and the measured facts block)
  </read_first>
  <action>
    Rebuild `data/themes.jsonl` in full with a throwaway conversion script in the session
    scratchpad (NOT committed — Phase 9 owns resync tooling; record the script's logic in
    the SUMMARY). Never retype a palette value: every colour comes mechanically from the
    raw capture or from `ThemeLibrary._THEMES`.

    **App records (139 → 138):** drop the 40 records in the three sport categories
    (names contain AUSSIE RULES, LEAGUE, UNION — exactly 40 lines); collapse `christmas`
    by keeping the 'Christmas 🎄' HOLIDAYS record (capture index 78) and dropping the
    'Christmas 🎅' ARCHIVES twin (capture index 133) — the palettes are identical; the
    drop is recorded here and in the SUMMARY, nowhere in running code (SPEC edge,
    META-02). For each remaining record: display name = strip emoji via NFKD → drop
    non-ASCII → collapse internal whitespace → trim (D-06, D-08 — the raw app string is
    not carried forward; it survives in the committed capture); category = same strip
    applied to the category heading, then Title Case (D-07), yielding exactly the 8
    names `Moods`, `Art Series`, `Music`, `Nature`, `Space`, `Play`, `Holidays`,
    `Archives` (D-10); slug = the stripped display name lowercased and
    underscore-joined — one pipeline, stated once (D-09); colours = each captured float
    value normalised through the protocol's uint16 encoding (construct an HSBK from the
    captured floats, then take `as_tuple()`) and stored as integer fields (D-02),
    duplicates preserved exactly. Drop the capture's `index` and `suspect_unchanged`
    fields (all 179 records carry suspect_unchanged false — verified 2026-08-14).

    **Rename aliases (D-04):** add `"aliases": ["aurora_borealis"]` to the `aurora`
    record and `"aliases": ["forest"]` to the `forrest` record. Verified: neither old
    key exists independently in the app set, so no conflict.

    **Orphan records (28):** the 30 pre-v1.2 keys with no app counterpart, minus the two
    rename keys above, are exactly these 28 — carry each unchanged (D-12) from
    `_THEMES` with category `Library` (D-10): arctic, autumn, bias_lighting,
    cherry_blossom, cyberpunk, deep_sea, desert, epic, evening, fire, focusing, galaxy,
    holly, hygge, intense, love, neon, proud, pumpkin, relaxing, santa, serene,
    shamrock, sports, spring, tropical, vaporwave, water. Palettes uint16-normalise
    through the same `as_tuple()` path (identical at protocol precision, the phase's
    comparison standard). Display name for a Library record is derived from its key —
    underscores to spaces, Title Case (e.g. cherry_blossom becomes Cherry Blossom) — a
    discretionary choice recorded here: the app has no name for these, and the derived
    name is ASCII and distinct from the slug as META-01 requires.

    **Collision guard (SPEC COMPAT-01 adjacency edge):** before writing, assert no
    orphan key collides with an app slug; if one ever does, the app record wins and the
    script must fail loudly naming the dropped orphan rather than silently keeping
    either. Zero live instances exist (the 27 shared slugs are handled as app records;
    the 28 orphans are disjoint by construction).

    **File form (D-02):** one JSON object per line, records sorted by slug, fixed key
    order slug → name → category → aliases (only when present) → colors, pure ASCII
    (json.dumps default ensure_ascii), no header record, no top-level metadata.

    **Embedded verification (one-off, results recorded in the SUMMARY):** the script
    must assert — 166 records total; 138 with an app category and 28 with Library;
    aliases exactly {aurora: [aurora_borealis], forrest: [forest]}; every slug passes
    str.isidentifier() and is unique including aliases (168 resolvable names); every
    app record's colour multiset equals its raw capture record's colours after uint16
    normalisation (Counter over (hue, saturation, brightness, kelvin) tuples,
    duplicates included); every Library record's colour multiset equals its `_THEMES`
    entry via as_tuple(); the 27 shared slugs are all present as app records (blissful,
    calaveras, cheerful, christmas, coral_reef, dream, earth, energizing, exciting,
    fantasy, gentle, halloween, hanukkah, independence, kwanzaa, mellow, party,
    peaceful, powerful, romance, soothing, spacey, stardust, thanksgiving, tranquil,
    warming, zombie — measured 2026-08-14); no record has zero colours (minimum
    observed in the capture: 2).
  </action>
  <verify>
    <automated>uv run python -c "
import json
from collections import Counter
recs = [json.loads(l) for l in open('data/themes.jsonl')]
assert len(recs) == 166, len(recs)
cats = Counter(r['category'] for r in recs)
assert cats['Library'] == 28 and sum(cats.values()) - cats['Library'] == 138, cats
assert set(cats) == {'Moods', 'Art Series', 'Music', 'Nature', 'Space', 'Play', 'Holidays', 'Archives', 'Library'}, set(cats)
aliases = {r['slug']: r['aliases'] for r in recs if 'aliases' in r}
assert aliases == {'aurora': ['aurora_borealis'], 'forrest': ['forest']}, aliases
keys = [r['slug'] for r in recs] + [a for r in recs for a in r.get('aliases', [])]
assert len(keys) == 168 and len(set(keys)) == 168
assert all(k.isidentifier() for k in keys)
assert [r['slug'] for r in recs] == sorted(r['slug'] for r in recs)
assert all(len(r['colors']) >= 2 or r['category'] == 'Library' for r in recs)
assert all(r['colors'] for r in recs)
assert all(0 <= c[f] <= 65535 for r in recs for c in r['colors'] for f in ('hue', 'saturation', 'brightness'))
line = open('data/themes.jsonl', 'rb').read()
assert all(b < 128 for b in line), 'non-ASCII byte in data file'
print('data file ok:', len(recs), 'records,', len(keys), 'resolvable names')"</automated>
  </verify>
  <acceptance_criteria>
    - `data/themes.jsonl` holds exactly 166 lines of valid JSON, sorted by slug, pure ASCII bytes
    - Category values are exactly the 9 names Moods, Art Series, Music, Nature, Space, Play, Holidays, Archives, Library; Library count is 28
    - `aurora` carries alias `aurora_borealis` and `forrest` carries alias `forest`; no other record has an aliases field
    - 168 resolvable names, all unique, all passing `str.isidentifier()`
    - Exactly one `christmas` record, category `Holidays`
    - The conversion script's multiset verification against the raw capture (138 app records) and against `_THEMES` (28 orphans) passed — output recorded in the SUMMARY
    - The conversion script and its collision guard live in the scratchpad, not the repo
  </acceptance_criteria>
  <done>The complete normalised data file is committed: 166 records, 168 resolvable names, every palette mechanically verified multiset-equal to its source at uint16 precision, emoji stripped everywhere, sorted and deterministic.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate and cut ThemeLibrary over to the generated dict</name>
  <reversibility rating="one-way">D-11: once released, callers depend on the shared slugs meaning the app palettes; reintroducing the pre-v1.2 values later needs a new naming scheme and a migration note. The operator retired COMPAT-02 and locked this in discuss-phase (06-CONTEXT.md D-11), so no checkpoint re-asks it; the rating is kept for the record.</reversibility>
  <files>src/lifx/theme/data.py, src/lifx/theme/library.py, tests/test_theme/test_library.py, tests/test_api/test_api_apply_theme.py, tests/test_effects/test_spin.py</files>
  <read_first>
    - src/lifx/theme/library.py (the transitional get() from Plan 06-01, the `_THEMES` literal to delete, `get_by_category()` lines to leave textually untouched, module docstring to rewrite)
    - src/lifx/theme/data.py (regenerated output — read after regeneration to confirm shape)
    - tests/test_theme/test_library.py (the tests that pin pre-v1.2 palettes and counts — the exact breakage inventory below)
    - .planning/phases/06-generated-theme-library/06-PATTERNS.md ("Tests that will break and must change" section)
    - .planning/phases/06-generated-theme-library/06-CONTEXT.md (D-11, D-13, D-15, D-23)
  </read_first>
  <action>
    **Regenerate:** run `uv run python -m lifx.theme.generator` over the full data file;
    `src/lifx/theme/data.py` now carries all 168 keys (166 records + 2 alias
    assignments). Commit the regenerated module — it, not the data file, is the shipped
    artefact (D-03).

    **Cut library.py over:** delete the hand-written `_THEMES` dict literal (currently
    lines 40–405) and the transitional fallback branch in `get()`. Import `THEMES` and
    `ThemeRecord` from `lifx.theme.data`. Keep a class attribute
    `_THEMES: dict[str, ThemeRecord] = THEMES` on `ThemeLibrary` so
    `get_by_category()` — whose membership filter reads `name in cls._THEMES` — remains
    textually untouched and working over the resynced palettes (Phase 7 owns META-04;
    do not edit that method). `get()` becomes: lowercase the input (unchanged house
    style), single lookup in `THEMES` (every key is a real entry in the flat dict, no
    alias indirection, D-13), on miss raise
    KeyError with exactly this message: Theme '{name}' not found. Use
    ThemeLibrary.get_available_themes() to list the available themes. — the name plus a
    pointer, the full listing dropped (SPEC edge, THEME-01 empty); on hit return
    `Theme(list(record.colors), slug=record.slug, name=record.name, category=record.category)`
    over a fresh list. `get_available_themes()` returns `sorted(THEMES)` — all 168
    names, exactly what get() accepts (D-15). `get_theme()` stays untouched. Rewrite the
    module docstring: the library is generated from `data/themes.jsonl` by
    `lifx.theme.generator`, synced from the LIFX app via hardware capture on 2026-08-14,
    168 resolvable names; drop the "60+ curated" wording and the aiolifx-themes
    reference; note palette order carries no meaning. Keep Australian English in all
    prose.

    **Fix the known test breakage** (per D-23, replace palette/count pins with
    behavioural assertions — the data file is the record of what should exist):
    - `test_get_nonexistent_theme`: assert the new message — the theme name and the
      words `get_available_themes` present; drop the old listing assertion
    - `test_list_returns_sorted_list` / `test_list_count`: drop the `== 57` count pins;
      keep sortedness and non-emptiness
    - `test_get_specific_themes`: drop the per-theme colour-count pins; assert each name
      resolves to a Theme with at least one colour and correct identity fields present
    - `test_christmas_theme_colors` / `test_halloween_theme_colors`: the app palettes
      differ — replace hue-content pins with identity assertions (christmas → category
      Holidays; halloween resolves with a non-empty palette)
    - `test_evening_theme_values`: keep — evening is an orphan carried unchanged (D-12)
      and its tolerant ranges survive uint16 normalisation
    - `test_all_themes_are_valid`: the app data reaches kelvin 1500 (measured range
      1500–9000), so relax the lower bound from 2500 to 1500 (MIN_KELVIN); keep the
      other bounds
    - `test_get_existing_theme` / `test_get_theme_basic`: drop the `len == 3` pin if
      present against a resynced theme (evening keeps 3 colours, so these may pass
      unchanged — verify rather than assume)
    Then run the FULL suite and fix any remaining breakage outside test_theme — expected
    candidates: `tests/test_api/test_api_apply_theme.py` (uses spacey, whose app palette
    has 2 colours where the old had 4) and `tests/test_effects/test_spin.py` (uses
    exciting, an orphan — likely unaffected). Apply the same rule: behavioural
    assertions, no new palette or count pins. The commit for this task must leave the
    whole suite green.

    **One-off THEME-02/THEME-03 verification (not a committed test, D-23):** run a
    scratchpad check that for every app record in `data/themes.jsonl`,
    `Counter(c.as_tuple() for c in ThemeLibrary.get(slug).colors)` equals the multiset
    of stored uint16 tuples, and that `soothing` now contains kelvin 8000. Record the
    result in the SUMMARY. This is the acceptance for "shipped palette equals captured
    palette" executed end to end through the public API.
  </action>
  <verify>
    <automated>uv run python -m lifx.theme.generator && H1=$(shasum src/lifx/theme/data.py | cut -d' ' -f1) && uv run python -m lifx.theme.generator && H2=$(shasum src/lifx/theme/data.py | cut -d' ' -f1) && [ "$H1" = "$H2" ] && uv run python -c "
import json
from collections import Counter
from lifx.theme import ThemeLibrary as L
recs = [json.loads(l) for l in open('data/themes.jsonl')]
names = L.get_available_themes()
assert len(names) == 168 and names == sorted(names)
for r in recs:
    t = L.get(r['slug'])
    stored = Counter((c['hue'], c['saturation'], c['brightness'], c['kelvin']) for c in r['colors'])
    assert Counter(c.as_tuple() for c in t.colors) == stored, r['slug']
    assert (t.slug, t.name, t.category) == (r['slug'], r['name'], r['category']), r['slug']
assert 8000 in {c.kelvin for c in L.get('soothing')}
assert L.get('christmas').category == 'Holidays'
assert L.get('forrest').name == 'Forrest'
print('cutover ok: 168 names, all palettes multiset-exact')" && uv run pytest tests/ -q && uv run ruff check . && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - `src/lifx/theme/library.py` no longer contains a hand-written HSBK dict literal; `grep -c 'HSBK(' src/lifx/theme/library.py` reports 0
    - `library.py` contains `_THEMES: dict[str, ThemeRecord] = THEMES` and `get_by_category` has no diff in this task
    - `ThemeLibrary.get_available_themes()` returns 168 sorted names and every one of them resolves through `get()`
    - Every app slug's palette is multiset-equal at uint16 precision to its data-file record, end to end through the public API (verify script above)
    - `ThemeLibrary.get('nonexistent')` raises KeyError containing `nonexistent` and `get_available_themes`, and NOT containing a theme listing
    - `8000 in {c.kelvin for c in ThemeLibrary.get('soothing')}` is True (THEME-03; pre-v1.2 was uniformly 3500)
    - Double-run of the generator is byte-identical (hash comparison in the verify); after this task's commit, one further regeneration leaves `git status --porcelain src/lifx/theme/data.py` empty — the THEME-04 convention check, performed once and recorded in the SUMMARY
    - `uv run pytest tests/ -q` exits 0 (full suite, not just test_theme), `uv run ruff check .` exits 0, `uv run pyright` reports 0 errors
  </acceptance_criteria>
  <done>ThemeLibrary serves all 168 names from the generated module alone, palettes are app-accurate at uint16 precision, the KeyError is short, the mutation leak is fixed on the only remaining path, the full test suite is green, and regeneration idempotence is verified and recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin the compatibility and metadata guarantees</name>
  <files>tests/test_theme/test_library.py, tests/test_effects/test_rule_trio.py</files>
  <read_first>
    - tests/test_theme/test_library.py (as left by Task 2 — the file being extended; existing class-per-method structure to follow)
    - .planning/phases/06-generated-theme-library/06-SPEC.md (Acceptance Criteria and Edge Coverage — the list being pinned)
    - .planning/phases/06-generated-theme-library/06-CONTEXT.md (D-13–D-15, D-23 — what may and may not be pinned)
  </read_first>
  <behavior>
    - Every pre-v1.2 key resolves: parametrised over a LITERAL module-level fixture tuple (SPEC: captured as a literal, not read from the new library, so an empty or incorrect derivation cannot vacuously pass)
    - Rename pairs resolve both ways with the target's identity: get('aurora_borealis') equals get('aurora') as palettes and reports slug 'aurora', name 'Aurora', category 'Nature'; get('forest') equals get('forrest') and reports slug 'forrest', name 'Forrest', category 'Nature' (COMPAT-03, D-14)
    - soothing contains kelvin 8000 (THEME-03, SPEC-mandated pin)
    - Mutating a returned Theme (add_color) does not change the next get() of the same slug — compare lengths across two gets, no literal count
    - KeyError for an unknown name contains the name and a pointer to get_available_themes, without the full theme listing
    - Every key in get_available_themes() passes str.isidentifier(); every listed key resolves through get() (D-15)
    - Every theme's display name and category are pure ASCII; name and category are non-None and distinct from the slug for every library theme (META-01)
    - Every theme's category is one of Moods, Art Series, Music, Nature, Space, Play, Holidays, Archives, Library; per-category counts are NOT asserted (D-23)
    - No key ends with the retired legacy suffix (COMPAT-02 retirement criterion)
    - get() stays case-insensitive for a new app slug (MONDRIAN resolves)
    - Theme equality integration: two consecutive gets of the same slug compare equal via the palette-only __eq__
  </behavior>
  <action>
    Extend `tests/test_theme/test_library.py` with the behaviours above, following the
    existing class-per-method style. Add the module-level literal fixture exactly as
    measured from the pre-v1.2 library on 2026-08-14 — PRE_V12_KEYS, a tuple of these
    57 strings: arctic, aurora_borealis, autumn, bias_lighting, blissful, calaveras,
    cheerful, cherry_blossom, christmas, coral_reef, cyberpunk, deep_sea, desert, dream,
    earth, energizing, epic, evening, exciting, fantasy, fire, focusing, forest, galaxy,
    gentle, halloween, hanukkah, holly, hygge, independence, intense, kwanzaa, love,
    mellow, neon, party, peaceful, powerful, proud, pumpkin, relaxing, romance, santa,
    serene, shamrock, soothing, spacey, sports, spring, stardust, thanksgiving,
    tranquil, tropical, vaporwave, warming, water, zombie. Parametrise the COMPAT-01
    test over it with pytest.mark.parametrize. Use collections.Counter over
    HSBK.as_tuple() for all palette comparisons (unordered multiset at protocol
    precision — order is never compared). Per D-23 do NOT add: a literal list of the new
    168 slugs, any count assertion against the new data (no len == 168, no per-category
    counts, no per-theme colour counts), or any comparison between data/themes.jsonl
    and data.py. The dynamic sweeps (identifier, ASCII, category membership,
    listing-resolves) iterate over get_available_themes() at runtime, which is allowed —
    they pin invariants, not contents. While the suite is open, glance at
    `tests/test_effects/test_rule_trio.py` (default theme 'exciting', an orphan) and
    correct it only if Task 2's sweep left a genuine failure; otherwise leave it
    untouched. Confirm branch coverage on the two hand-written files this phase changed
    (library.py, theme.py) is complete — CI enforces 100 percent branch patch coverage
    and checks branch partials, not just lines.
  </action>
  <verify>
    <automated>set -o pipefail && uv run pytest tests/ -q && uv run pytest --cov=lifx.theme --cov-branch --cov-report=term-missing tests/ -q 2>&1 | awk '/src\/lifx\/theme\/(library|theme)\.py/ {if ($NF != "100%") {print "COVERAGE GAP:", $0; bad=1}} END {exit bad}'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_theme/test_library.py` contains a module-level `PRE_V12_KEYS` tuple with exactly 57 entries matching the list above, and a parametrised test resolving each
    - Rename-pair tests assert both multiset palette equality and target identity for both pairs (four names total)
    - A test asserts `8000 in {c.kelvin for c in ThemeLibrary.get('soothing')}`
    - A test proves add_color on a returned Theme leaves the next get() unchanged
    - Sweep tests over get_available_themes() cover: isidentifier, every-name-resolves, ASCII-only name and category, category in the 9-value set, no legacy-suffixed key
    - No new test pins a count or literal slug list of the new data (D-23) — the only literal list is PRE_V12_KEYS and the 9 category names
    - `uv run pytest tests/ -q` exits 0; coverage on src/lifx/theme/library.py and src/lifx/theme/theme.py shows 100% with branch measurement enabled
  </acceptance_criteria>
  <done>COMPAT-01, COMPAT-03, THEME-03's kelvin pin, the mutation-leak fix, the shortened KeyError, and the META-01/META-02 ASCII and category guarantees are all pinned by committed tests; the full suite is green with complete branch coverage on the hand-written theme files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw capture → data/themes.jsonl | Hardware-captured JSONL is converted into the committed data file — the conversion is the integrity-critical step (values must trace to a capture record or the pre-v1.2 table) |
| data/themes.jsonl → data.py | The committed data file becomes executable Python via the 06-01 generator (validated: repr-escaped strings, identifier-checked slugs, range-checked colours) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | conversion script output | medium | mitigate | Every palette mechanically derived and verified multiset-equal at uint16 precision against its source (capture for app records, _THEMES for orphans) before commit; no hand-typed values; collision guard fails loudly |
| T-06-06 | Tampering (supply chain) | regeneration inputs | medium | mitigate | Regeneration reads only the committed local data file — no network, no device, no undocumented LIFX endpoints (SPEC prohibition); zero packages installed in this phase, so no package-legitimacy audit applies |
| T-06-07 | Repudiation | data.py vs data file drift | low | accept | D-23: no drift gate by operator decision; idempotence verified once by hand and recorded in the SUMMARY; a short or stale regeneration would ship silently — accepted knowingly |
| T-06-08 | Information disclosure | committed data file | low | accept | The data file carries only public theme names and colour values already shipped in the LIFX app; the raw app strings with emoji stay in .claude/theme-capture (not packaged, not published) |
</threat_model>

<verification>
- `data/themes.jsonl`: 166 records, 168 resolvable names, pure ASCII, sorted, multiset-verified against both sources
- `ThemeLibrary` serves all 168 names from the generated module alone; palettes multiset-exact at uint16 precision end to end through the public API
- All 57 pre-v1.2 keys resolve against a literal fixture; both rename pairs resolve both ways with target identity
- Full suite green (`uv run pytest tests/ -q`), ruff clean, pyright clean, branch coverage complete on library.py and theme.py
- Generator double-run byte-identical over the final inputs (THEME-04 convention check, recorded)
</verification>

<success_criteria>
- Phase goal met: every non-sport app theme resolves by ASCII slug with app-accurate colours and app metadata, generated from the capture, and no name that resolved before v1.2 breaks
- THEME-01/02/03, COMPAT-01/03, META-01/02 all demonstrably satisfied; THEME-04's regeneration convention verified by hand once
- COMPAT-02's retirement is visible: no legacy-suffixed keys, no Legacy category, pre-v1.2 palettes of redefined themes recoverable only from git history
</success_criteria>

<output>
Create `.planning/phases/06-generated-theme-library/06-02-SUMMARY.md` when done.
</output>
