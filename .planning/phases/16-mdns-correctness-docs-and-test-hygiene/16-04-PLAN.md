---
phase: 16-mdns-correctness-docs-and-test-hygiene
plan: 04
type: execute
wave: 3
depends_on:
  - 16-01
  - 16-02
files_modified:
  - pyproject.toml
  - tests/conftest.py
  - tests/test_packaging.py
  - tests/test_api/test_api_apply_theme.py
  - tests/test_api/test_api_discovery.py
  - tests/test_api/test_api_organization.py
  - tests/test_devices/test_base.py
  - tests/test_devices/test_ceiling.py
  - tests/test_devices/test_light.py
  - tests/test_devices/test_matrix.py
  - tests/test_devices/test_state_ceiling.py
  - tests/test_devices/test_state_hev.py
  - tests/test_devices/test_state_infrared.py
  - tests/test_devices/test_state_light.py
  - tests/test_devices/test_state_management.py
  - tests/test_effects/test_conductor.py
  - tests/test_effects/test_integration.py
  - tests/test_effects/test_plasma2d.py
  - tests/test_effects/test_registry.py
  - tests/test_network/test_concurrent_requests.py
  - tests/test_network/test_connection.py
  - tests/test_network/test_connection_retry.py
  - tests/test_network/test_discovery_devices.py
  - tests/test_network/test_discovery_errors.py
  - tests/test_network/test_mdns/test_discovery.py
  - tests/test_network/test_mdns/test_dns.py
  - tests/test_network/test_mdns/test_transport.py
  - tests/test_protocol/test_protocol_generator.py
autonomous: true
requirements:
  - TEST-01

estimate:
  tokens: 60000
  raw_tokens: 120000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Exactly three paths are excluded from the sweep and left untouched: `tests/test_animation/`, `tests/test_devices/test_multizone.py` and `tests/test_theme/`. `tests/conftest.py` and `tests/test_packaging.py` sit at the repository test root and are in scope."
    - "No file with zero function-local imports is modified by the sweep: every file in the sweep commit carried at least one `PLC0415` violation in the parent commit."
    - "An import whose relocation would change observable behaviour stays function-local and carries an adjacent comment naming the circular-import or patching reason."
    - "`uv run --frozen ruff check --select PLC0415 tests/`, excluding the three Phase 18 and Phase 19 paths, reports zero violations, down from 166 across 27 files."
    - "`uv run --frozen ruff check .`, `uv run --frozen ruff format --check .` and `uv run --frozen pytest` all pass after the sweep and after the configuration change."
    - statement: "A targeted single-file run of a changed file that imports `measurement_support` passes, proving the hoisted import does not depend on the tooling conftest beside `.planning/scripts/tests/`, which such a run never loads because naming a path suppresses `testpaths`. `tests/conftest.py` IS loaded by that run, because pytest loads a conftest for any collection under `tests/`, and its `sys.path` insert at :50-52 is relied on deliberately; the backstop proves the relocation adds no dependency beyond that one. Stated this way after the cross-AI plan review found the earlier wording claimed independence from the conftest the run actually loads. The concrete file is `tests/test_network/test_connection_retry.py`, whose function-local `import measurement_support` at :1224 hoists to module scope."
      verification: backstop
  artifacts:
    - pyproject.toml
    - tests/test_network/test_mdns/test_discovery.py
    - tests/test_network/test_connection_retry.py
    - tests/conftest.py
  key_links:
    - "D-08 sequencing: the 27-file sweep is ONE commit and the ruff configuration change is a SEPARATE, LATER commit. Sweep first means no commit in history has `PLC0415` enabled with violations outstanding, so every commit stays green under `ruff check .` and a bisect never lands on a red lint state."
    - "Each of the per-file-ignore entries is a visible, deletable line: Phase 18 deletes `tests/test_animation/**` and `tests/test_devices/test_multizone.py` when it sweeps its files, Phase 19 deletes `tests/test_theme/**`, and `src/**` plus `.planning/**` remain for a future phase (16-CONTEXT.md `<deferred>`). That handoff is why D-05 chose per-file-ignores over selecting `PLC0415` on `tests/` alone."
    - "D-06's retained-import mark is `# noqa: PLC0415 - <reason>`. The colon form suggested in 16-PATTERNS.md is REJECTED by ruff (`Invalid # noqa directive ... expected code to consist of uppercase letters followed by digits only`) and leaves the violation unsuppressed; verified against the pinned ruff in this tree."
  prohibitions:
    - statement: "MUST NOT delete, skip, xfail or weaken any test, and MUST NOT add a `noqa` or per-file-ignore, to make the import sweep pass."
      status: resolved
      verification: "test plus source assertion, bounded by D-07. PERMITTED: a `noqa: PLC0415` that carries a named circular-import or patching reason; a per-file-ignore covering a path this phase is forbidden to edit. PROHIBITED: a bare `noqa` with no reason; any per-file-ignore covering a file that is in scope for the sweep; any broadening of an existing ignore to swallow an in-scope file. The permitted set is enumerable and a verifier can list it: SEVEN per-file-ignore entries (`src/**`, `.planning/**`, `tests/test_animation/**`, `tests/test_theme/**`, `tests/test_devices/test_multizone.py`, `.agents/skills/**`, `.claude/skills/**` — see the flagged D-05 extension in this plan), plus however many reason-bearing `noqa: PLC0415` comments the sweep proves necessary. Checkable: no collected pytest node ID present before the sweep is absent after it, compared as sets; no new `skip`, `xfail` or deleted test appears in the sweep diff; every `noqa: PLC0415` under `tests/` is followed by ` - ` and prose. Step 3a REMOVES a version skip from `tests/test_packaging.py` rather than adding one, which moves in the strengthening direction this prohibition protects and gains Python 3.10 coverage for that test."
---

<!-- planner-discipline-allow: noqa: PLC0415 -->
<!-- planner-discipline-allow: noqa: PLC0415 - -->
<!-- planner-discipline-allow: PLC0415 -->

<objective>
Close TEST-01: function-local imports move to module scope across `tests/`, excluding Phase 18
and Phase 19 territory, and a standing ruff rule prevents the regression recurring.

Purpose: `test_discovery.py` already imports two symbols from
`lifx.network.discovery.mdns.discovery` at module scope while importing two more from the
identical module inside 50 functions. No circular-import barrier exists for those 50; the
pattern is habit, not constraint. `PLC0415` is not in ruff's `select` list, so nothing enforces
the project rule that all imports go at the top of the file.

Output: 166 function-local imports across 27 files hoisted in one mechanical commit, then a
separate commit enabling `PLC0415` with per-file-ignores that leave each excluded path a
visible line the owning phase deletes when it sweeps its own files.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-mdns-correctness-docs-and-test-hygiene/16-SPEC.md
@.planning/phases/16-mdns-correctness-docs-and-test-hygiene/16-CONTEXT.md
@.planning/phases/16-mdns-correctness-docs-and-test-hygiene/16-PATTERNS.md
@AGENTS.md
@pyproject.toml
</context>

## Artifacts this phase produces

Full phase-level list. Ownership marked per plan.

| Artifact | Owner |
|----------|-------|
| `_LifxRecordCache._owner_is_unusable()` — extracted guard predicate | plan 16-01 |
| `_LifxRecordCache._selected_address_for_normalised()` — private delegate | plan 16-01 |
| `TestLifxRecordCacheOwnerNormalisation` test class and its methods | plan 16-01 |
| `tests/test_docs_language.py` module and its test functions | plan 16-02 |
| New approved-phrase entries in `test_phase_contract.py` (both lists) | plan 16-02 |
| New `AGENTS.md` derived-not-cached caching category bullet | plan 16-03 |
| `PLC0415` entry in `[tool.ruff.lint] select` | **this plan** |
| Per-file-ignore keys for `PLC0415`: `src/**`, `.planning/**`, `tests/test_animation/**`, `tests/test_theme/**`, `tests/test_devices/test_multizone.py`, `.agents/skills/**`, `.claude/skills/**` | **this plan** |
| Reason-bearing `# noqa: PLC0415 - <reason>` marks on every retained function-local import under `tests/` | **this plan** |

## FLAGGED: D-05's enumerated five ignore entries are incomplete (seven are required)

D-05 enumerates five per-file-ignore entries. Measured against the working tree with the pinned
ruff, five is not enough to satisfy SPEC requirement 4's acceptance criterion that
`uv run ruff check .` passes.

`ruff check --select PLC0415 .` reports 322 violations: 189 under `tests/`, 82 under `src/`,
49 under `.planning/` and **2 elsewhere**. Those two are:

- `.agents/skills/spike-findings-lifx-async/sources/001-modem-sleep-keepalive/probe.py:374`
- `.claude/skills/spike-findings-lifx-async/sources/001-modem-sleep-keepalive/probe.py:374`

Both are git-tracked (`git ls-files` returns each). They are archived spike harnesses,
preserved verbatim as evidence sources; editing them defeats their purpose and any edit would
have to be mirrored across the two copies. Ruff has no `exclude` list, and pre-commit excludes
only `^\.planning/phases/.*-EVENTS\.jsonl$` and `.planning/scripts/tests/`, so a newly selected
rule reaches them.

**Resolution:** add two further per-file-ignore entries, `.agents/skills/**` and
`.claude/skills/**`, for the same reason D-07 already permits: a per-file-ignore covering a
path this phase is forbidden to edit. This extends D-05's enumerated **count** from five to
seven; it does not change D-05's **rationale**, its chosen mechanism, or its cross-phase
handoff property, and it adds no ignore over any file that is in scope for the sweep.

Verified empirically against this tree: with the seven entries and `PLC0415` selected,
`ruff check --no-cache .` reports exactly 166 `PLC0415` violations, which is precisely the
in-scope sweep set and nothing else. With only the five D-05 entries it reports 168, the extra
two being the archived probes.

**Operator decision point:** if the preferred resolution is instead to sweep the two archived
probe files (breaking their verbatim-preservation property) or to introduce a ruff `exclude`
list, say so and this plan's Task 2 changes accordingly. Absent that, Task 2 lands seven
entries.

## Issue closure has an owner: the phase ship step, not any plan commit

`16-SPEC.md` lists *"Closing issues #213, #215, #216 and #217 through `Closes` lines in the merge
commit"* as in scope, and the SPEC interview log records the decision as *"closed by the merge
commit through `Closes` lines in the PR body"*. The cross-AI plan review pointed out that no plan
in this phase produces a merge commit: all four commit directly to the phase branch, and none of
their Conventional Commit messages may carry a `Closes` trailer without closing the issue at the
wrong moment, before the phase has shipped.

**Owner: the phase ship step**, which opens the pull request and whose squash or merge commit is
the only merge commit this phase produces. This plan carries the obligation because it is the
last requirement to land and its summary is where the phase's configuration state is recorded.
The obligation is: `16-04-SUMMARY.md` lists the four issue numbers against the requirement each
one closes, so whoever writes the PR body has the mapping in front of them rather than having to
reconstruct it from `.planning/REQUIREMENTS.md`.

| Issue | Requirement | Landed by |
|-------|-------------|-----------|
| #213 | MDNS-09 | plan 16-01 |
| #215 | DOCS-08 | plan 16-02 |
| #216 | DOCS-07 | plan 16-03 |
| #217 | TEST-01 | plan 16-04 |

No plan commit carries a `Closes` line. That is deliberate, not an omission: a direct commit
carrying one would close the issue when the commit lands rather than when the phase merges, and
the four issues are only closed once all four requirements have shipped together.

## Ruff cache caveat (measurement hygiene)

Ruff's result cache is not reliably invalidated by a `--config` override during measurement,
and a stale entry can report violations that the new configuration suppresses. Every decisive
`PLC0415` measurement below passes `--no-cache`. A real `pyproject.toml` edit does change the
configuration hash, so ordinary developer and CI runs need no flag.

<tasks>

<task type="auto">
  <name>Task 1: Hoist 166 function-local imports across 27 files in one commit</name>
  <reversibility rating="reversible">D-08 makes the sweep a single mechanical commit with no
  behavioural intent, so it reverts cleanly. Matches the operator's rating in
  16-CONTEXT.md.</reversibility>
  <files>tests/conftest.py, tests/test_packaging.py, tests/test_api/, tests/test_devices/, tests/test_effects/, tests/test_network/, tests/test_protocol/</files>
  <read_first>
    - The authoritative inventory, produced first by running
      `uv run --frozen ruff check --no-cache --select PLC0415 tests/ --exclude tests/test_animation --exclude tests/test_theme --exclude tests/test_devices/test_multizone.py --output-format=concise`.
      Ruff's own rule is authoritative, not a hand grep: the spec-phase figures came from a
      grep that counted docstring prose lines as imports and missed files ruff does flag
      (16-CONTEXT.md `<spec_lock>`).
    - `tests/test_network/test_mdns/test_discovery.py` — the module-scope import block at the
      top and the 51 flagged sites. It already imports `_discover_lifx_services_sweep` and
      `_LifxRecordCache` from `lifx.network.discovery.mdns.discovery` at module scope while
      importing `_discover_lifx_services` and `discover_devices_mdns` from the identical module
      inside 50 functions. That is the precedent proving no barrier exists for those 50.
    - `tests/test_network/test_connection_retry.py:18-35` and `:1170-1245` — the module-scope
      block already contains `from measurement_support import (...)` at :27, and the four
      flagged sites are `import inspect` (:1173), `from lifx.const import
      REQUEST_RETRANSMIT_GAPS` (:1175), `import measurement_support` (:1224, used at :1227 as
      `measurement_support.asyncio` for a patch target, so it needs the module object, not a
      late import) and `from lifx.network.connection import
      _REQUEST_OBSERVER_TASK_ATTRIBUTE` (:1238). This is the backstop file.
    - `tests/conftest.py:40-58` and `:1005-1020` — the `sys.path` insert of
      `.planning/scripts` at :50-52 (which runs at conftest import time, before any test module
      in the tree is imported) and the single flagged site, `from types import SimpleNamespace`
      inside a fixture at :1015.
    - `tests/test_packaging.py:11-31` — the whole import block and
      `test_pyproject_declares_no_runtime_dependencies`. The flagged site is the function-local
      `import tomllib` at :27, which sits behind a `sys.version_info < (3, 11)` skip at :24-26
      because `tomllib` is 3.11+. Step 3a below resolves it explicitly; this is not an
      executor judgement call.
    - `pyproject.toml:57` — `tomli>=2.0.1 ; python_full_version < '3.11'`, already a
      development dependency, which is what makes Step 3a a no-new-dependency change.
    - `pyproject.toml`'s `[tool.pyright]` `include` list — it names `src`,
      `scripts/generate_theme_data.py`, four `.planning/scripts` files and two `.github` files.
      `tests/` is absent, so pyright does not type-check `tests/test_packaging.py` and the
      compatibility import raises no pyright question on a Python where `tomli` is not
      installed.
    - `AGENTS.md` — the project rule that all imports go at the top of the file, the pytest
      opt-in-flag model, and the two `sys.path` inserts and why they exist.
    - `.planning/phases/16-mdns-correctness-docs-and-test-hygiene/16-CONTEXT.md` — D-06, D-07
      and D-08.
    - `.planning/phases/16-mdns-correctness-docs-and-test-hygiene/16-SPEC.md` — requirement 4,
      its acceptance criteria and prohibition P6.
  </read_first>
  <action>
This whole sweep is ONE commit (D-08). Per-directory and per-requirement splits were declined:
the change is mechanical with no behavioural intent, and a bisect landing on all 27 files at
once is acceptable. Do not touch `pyproject.toml` in this commit; the configuration change is
Task 2's separate, later commit.

**Step 1, take the inventory and the collection baseline. Do this BEFORE editing anything.**

Run the ruff command named in `read_first` and record the file list and the per-file counts.
Save that inventory: the plan summary must report it. Expect 166 violations across 27 files, led
by `tests/test_network/test_mdns/test_discovery.py` (51),
`tests/test_network/test_connection.py` (19), `tests/test_network/test_discovery_errors.py`
(12) and `tests/test_network/test_concurrent_requests.py` (11). If the measured numbers differ
because plan 16-01 or plan 16-02 added a site, use the measured numbers and record the delta.

Then save the pre-sweep collected node-ID set, which the no-reduction gate compares against.
Run the same collect-only invocation the gate uses, `uv run --frozen pytest --collect-only -q -q
--no-cov -p no:cacheprovider`, filter its output to the lines matching the node-ID pattern
`^[^ ]+\.py::`, pass them through `sort -u`, and redirect the result to
`"${TMPDIR:-/tmp}/lifx-phase16-nodeids-before.txt"`. The gate reads that exact path, so use it
verbatim.

Write it outside the repository tree so it never reaches a commit and never perturbs a
`git status` or file-disjointness gate. The tree collects 4256 node IDs today, so a run
producing far fewer means the baseline itself is wrong and must be retaken rather than used.
The gate fails closed if the file is missing, so this step cannot be skipped silently.

**Why a set and not a floor.** This plan previously gated on a collected count at or above
2400. The cross-AI plan review measured the live tree at 4256, so that floor would have accepted
losing more than 1800 tests. A set comparison fails on the first node ID that disappears,
which is the property the gate is supposed to have.

**Step 2, hoist.** For each flagged import, move it into the module's top-level import block.
Where the module already imports from the same source at module scope, merge the names into
that existing statement rather than adding a second one. Apply the `I` (isort) ordering and the
formatter over the SWEPT FILES ONLY, by passing the Step 1 inventory file list to
`ruff check --fix` and `ruff format`, so the resulting ordering matches the repository's
configured style rather than a hand-guessed one. Do not use the repository-wide `ruff check .
--fix` or `ruff format .` here: both reviewers noted that an unrelated formatting drift
anywhere in the tree would then be pulled into this commit, entangling it with a change that is
supposed to be mechanically attributable. Run the repository-wide forms as CHECK-ONLY commands
afterwards, which the verify block already does.

**Step 3, judge the exceptions before hoisting them.** A function-local import stays local only
where relocating it changes observable behaviour. The cases to look for:
  - the import is the patch target itself, or the test patches an attribute of the importing
    module's namespace and the late binding is what makes the patch effective;
  - the import must not run at collection time (an optional-dependency or packaging probe, or
    an import that raises unless a fixture has already run);
  - the import breaks a genuine circular import;
  - the import follows an environment or `sys.path` mutation performed inside the test body.
Note that `tests/conftest.py`'s `.planning/scripts` insert at :50-52 runs at conftest import
time, which is before any test module under `tests/` is imported, so a module-scope
`import measurement_support` in a test module under `tests/` resolves. That is not an exception
case; it is the backstop this plan proves.

State the backstop's conclusion accurately. Pytest loads `tests/conftest.py` for ANY collection
under `tests/`, including a targeted single-file run, so that run does not prove independence
from `tests/conftest.py` and is not meant to. What it proves is independence from the OTHER
conftest, the one beside `.planning/scripts/tests/`, which is an initial-argument conftest
reached only through `testpaths` and which a run naming an explicit path never loads. The
relocation must therefore survive on `tests/conftest.py`'s insert alone. The cross-AI plan
review found this plan's earlier wording had the conclusion reversed; do not restore it in the
summary or in any comment.

**Step 3a, resolve `tests/test_packaging.py` with the existing `tomli` dependency.** This one
file is called out by name because it is the only in-scope site where neither hoisting nor
retaining is available under the rules as written, and the cross-AI plan review verified the
bind: hoisting a bare `tomllib` breaks Python 3.10 collection outright, while retaining the
import needs a `noqa` whose reason is neither a circular import nor a patching constraint, which
is outside D-07's permitted set. Do not resolve it by widening D-07 and do not add a per-file
ignore for it; it is in scope for the sweep, so an ignore there is prohibited.

The resolution is an improvement rather than a workaround. `tomli>=2.0.1` under a
`python_full_version < '3.11'` marker is already a development dependency at pyproject.toml:57,
so the backport is present exactly on the Pythons that lack the standard-library module. Do
this:

  1. Delete the two-line version skip at the top of
     `test_pyproject_declares_no_runtime_dependencies`.
  2. Delete the function-local `import tomllib`.
  3. Add a module-scope compatibility block, placed after the existing module-scope import block
     and separated from it by a blank line so ruff's isort pass leaves both alone: try to import
     the standard-library TOML reader, and on `ModuleNotFoundError` import the backport under
     the same local name. Give the `except` clause a short comment naming Python 3.10 as the
     reason.
  4. Remove the two imports that the deleted skip leaves unused, `sys` at :13 and `pytest` at
     :17. Neither is referenced anywhere else in the file, and leaving either behind fails
     `ruff check .` on `F401`, which would look like the sweep breaking the tree.

This adds no dependency, adds no `noqa`, and GAINS Python 3.10 coverage for a test that
currently skips there. It removes a skip rather than adding one, so SPEC prohibition P6 is
satisfied in the strengthening direction. Collection is unchanged, because the skip was a
runtime call inside the test body rather than a collection-time marker, so the node-ID baseline
from Step 1 stays comparable.

**Step 4, mark every retained import (D-06).** Write the mark as
`# noqa: PLC0415 - <named circular-import or patching reason>`. Use a space, a hyphen and a
space after the code. Do NOT use the colon form: ruff rejects `# noqa: PLC0415: reason` with
`Invalid # noqa directive ... expected code to consist of uppercase letters followed by digits
only` and leaves the violation unsuppressed. Verified against the pinned ruff in this tree; the
form shown in 16-PATTERNS.md is wrong on this point. A bare `# noqa: PLC0415` with no reason is
prohibited (D-06, SPEC prohibition P6).

**Step 5, respect the file-disjointness property.** Do not touch `tests/test_animation/`,
`tests/test_devices/test_multizone.py` or `tests/test_theme/`. Those are Phase 18 and Phase 19
territory and the v2.1 roadmap's locked property is that Phases 16, 18 and 19 are file-disjoint
and can run in parallel. Do not modify a file that carried zero flagged imports.

**Step 6, prohibitions.** Do not delete, skip, xfail or weaken any test to make the sweep pass.
The collected pytest count must not fall.

Commit with a Conventional Commit message and no GSD phase or plan number as the scope, using
`git commit -S -s`.
  </action>
  <acceptance_criteria>
    - `uv run --frozen ruff check --no-cache --select PLC0415 tests/` excluding the three
      Phase 18 and Phase 19 paths reports zero violations, AND that ruff invocation is asserted
      to have exited 0. A captured-output check that never inspects the exit status passes when
      ruff fails to run at all, which the cross-AI plan review reproduced against this plan's
      earlier gate using an unwritable `uv` cache.
    - Every ruff invocation in this plan asserts its exit status: 0 where no diagnostic is
      expected, exactly 1 where a diagnostic is required, and never an unchecked `|| true`
      swallowing a tool failure as success.
    - `tests/test_packaging.py` carries a module-scope TOML-reader compatibility import and no
      version skip, imports neither `sys` nor `pytest`, and defines the same two test functions
      as the parent commit.
    - Every file in the sweep commit is a `.py` file under `tests/` that carried at least one
      `PLC0415` violation in the parent commit, so no zero-violation file was modified.
    - No file under `tests/test_animation/`, `tests/test_theme/` or
      `tests/test_devices/test_multizone.py` appears in the sweep commit.
    - `pyproject.toml` does NOT appear in the sweep commit (D-08 keeps the configuration change
      separate).
    - Every `noqa: PLC0415` under `tests/` is followed by ` - ` and at least one word of prose;
      no bare mark exists, and `ruff check` emits no `Invalid # noqa directive` warning.
    - `uv run --frozen pytest tests/test_network/test_connection_retry.py -q` passes as a
      targeted single-file run. That is the backstop, and its conclusion is that the hoisted
      import survives without the tooling conftest beside `.planning/scripts/tests/`, which such
      a run never loads. `tests/conftest.py` is loaded by that run and its insert is relied on.
    - No collected pytest node ID present before the sweep is absent after it, compared as sets
      of node IDs rather than as a count or a floor.
    - `uv run --frozen ruff check .`, `uv run --frozen ruff format --check .` and
      `uv run --frozen pyright` all pass.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'O=$(uv run --frozen ruff check --no-cache --select PLC0415 tests/ --exclude tests/test_animation --exclude tests/test_theme --exclude tests/test_devices/test_multizone.py --output-format=concise 2>&1); S=$?; echo "ruff-exit=$S"; test "$S" -eq 0 || { echo "RUFF-DID-NOT-SUCCEED"; printf "%s\n" "$O" | tail -20; exit 1; }; N=$(printf "%s\n" "$O" | grep -c PLC0415 || true); echo "in-scope-plc0415=$N"; printf "%s\n" "$O" | grep -i "invalid" && { echo INVALID-NOQA-DIRECTIVE; exit 1; }; test "$N" -eq 0 || { echo IN-SCOPE-VIOLATIONS-REMAIN; exit 1; }; echo SWEEP-COMPLETE'</automated>
    <fails_when>non-zero exit, or the printed `ruff-exit=` value is anything other than 0, or the output contains "RUFF-DID-NOT-SUCCEED" or "INVALID-NOQA-DIRECTIVE", or the printed `in-scope-plc0415=` value is anything other than 0, or the line "SWEEP-COMPLETE" is absent</fails_when>
    <automated>bash -c 'FAIL=0; D=$(mktemp -d); F=$(git show --name-only --pretty=format: HEAD | grep . || true); test -n "$F" || { echo NO-FILES-IN-SWEEP-COMMIT; exit 1; }; for f in $F; do case "$f" in tests/test_animation/*|tests/test_theme/*|tests/test_devices/test_multizone.py) echo "EXCLUDED-PATH-TOUCHED: $f"; FAIL=1;; pyproject.toml) echo "CONFIG-IN-SWEEP-COMMIT: $f"; FAIL=1;; tests/*.py|tests/*/*.py|tests/*/*/*.py) ;; *) echo "UNEXPECTED-FILE-IN-SWEEP: $f"; FAIL=1;; esac; done; test "$FAIL" -eq 0 || { rm -rf "$D"; exit 1; }; git archive -o "$D/base.tar" HEAD~1 -- $F || { echo NEW-FILE-IN-SWEEP-OR-ARCHIVE-FAILED; rm -rf "$D"; exit 1; }; tar -xf "$D/base.tar" -C "$D" || { echo BASELINE-EXTRACT-FAILED; rm -rf "$D"; exit 1; }; rm -f "$D/base.tar"; O=$(uv run --frozen ruff check --no-cache --select PLC0415 --output-format=concise "$D" 2>&1); S=$?; echo "baseline-ruff-exit=$S"; test "$S" -le 1 || { echo "RUFF-ERRORED-ON-BASELINE"; printf "%s\n" "$O" | tail -20; rm -rf "$D"; exit 1; }; V=$(printf "%s\n" "$O" | grep PLC0415 | sed "s|^$D/||" | cut -d: -f1 | sort -u); echo "baseline-violating-files=$(printf "%s\n" "$V" | grep -c . || true)"; for f in $F; do printf "%s\n" "$V" | grep -qx "$f" || { echo "ZERO-VIOLATION-FILE-MODIFIED: $f"; FAIL=1; }; done; rm -rf "$D"; test "$FAIL" -eq 0 || exit 1; echo SWEEP-SCOPE-OK'</automated>
    <fails_when>non-zero exit, or the output contains any of "NO-FILES-IN-SWEEP-COMMIT", "EXCLUDED-PATH-TOUCHED:", "CONFIG-IN-SWEEP-COMMIT:", "UNEXPECTED-FILE-IN-SWEEP:", "NEW-FILE-IN-SWEEP-OR-ARCHIVE-FAILED", "BASELINE-EXTRACT-FAILED", "RUFF-ERRORED-ON-BASELINE" or "ZERO-VIOLATION-FILE-MODIFIED:", or the printed `baseline-ruff-exit=` value is 2 or higher, or the printed `baseline-violating-files=` value is 0, or the line "SWEEP-SCOPE-OK" is absent. The parent-commit content is reconstructed with one `git archive` and measured with ONE ruff invocation over the extracted tree, rather than one `uv run ruff` per file: the earlier per-file form paid interpreter startup 27-plus times and was slow enough on CI to discourage running the gate. `git archive` is also what makes a file that did not exist in the parent commit fail loudly instead of being silently skipped.</fails_when>
    <automated>bash -c 'B=$(grep -rn "noqa: PLC0415" tests/ | grep -v "noqa: PLC0415 - " || true); test -z "$B" || { echo "UNREASONED-NOQA:"; printf "%s\n" "$B"; exit 1; }; echo ALL-RETAINED-IMPORTS-CARRY-A-REASON'</automated>
    <fails_when>non-zero exit, or the output contains "UNREASONED-NOQA:", or the line "ALL-RETAINED-IMPORTS-CARRY-A-REASON" is absent</fails_when>
    <automated>bash -c 'F=tests/test_packaging.py; grep -q "pytest.skip" "$F" && { echo VERSION-SKIP-REMAINS; exit 1; }; grep -qE "^[[:space:]]+import tomllib" "$F" && { echo FUNCTION-LOCAL-TOMLLIB-REMAINS; exit 1; }; grep -qE "^import sys$|^import pytest$" "$F" && { echo UNUSED-IMPORT-REMAINS; exit 1; }; grep -qE "^try:" "$F" || { echo NO-COMPATIBILITY-IMPORT-BLOCK; exit 1; }; grep -qE "^except ModuleNotFoundError" "$F" || { echo NO-MODULENOTFOUND-FALLBACK; exit 1; }; grep -q "noqa" "$F" && { echo SUPPRESSION-USED-INSTEAD; exit 1; }; N=$(grep -c "^def test_" "$F" || true); echo "packaging-tests=$N"; test "$N" -eq 2 || { echo PACKAGING-TEST-COUNT-CHANGED; exit 1; }; echo PACKAGING-COMPAT-IMPORT-OK'</automated>
    <fails_when>non-zero exit, or the output contains "VERSION-SKIP-REMAINS", "FUNCTION-LOCAL-TOMLLIB-REMAINS", "UNUSED-IMPORT-REMAINS", "NO-COMPATIBILITY-IMPORT-BLOCK", "NO-MODULENOTFOUND-FALLBACK", "SUPPRESSION-USED-INSTEAD" or "PACKAGING-TEST-COUNT-CHANGED", or the printed `packaging-tests=` value is anything other than 2, or the line "PACKAGING-COMPAT-IMPORT-OK" is absent</fails_when>
    <automated>uv run --frozen pytest tests/test_packaging.py -q --no-cov -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or the output contains "no tests ran" or "skipped", or the pytest summary line contains "failed" or "error", or the summary reports fewer than 2 tests passed</fails_when>
    <automated>uv run --frozen pytest tests/test_network/test_connection_retry.py -q --no-cov -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or the output contains "no tests ran", or the pytest summary line contains "failed" or "error", or the output contains "ModuleNotFoundError: No module named 'measurement_support'"</fails_when>
    <automated>bash -c 'B="${TMPDIR:-/tmp}/lifx-phase16-nodeids-before.txt"; A="${TMPDIR:-/tmp}/lifx-phase16-nodeids-after.txt"; test -s "$B" || { echo "BASELINE-MISSING: $B"; exit 1; }; O=$(uv run --frozen pytest --collect-only -q -q --no-cov -p no:cacheprovider); S=$?; test "$S" -eq 0 || { echo "COLLECT-EXIT=$S"; printf "%s\n" "$O" | tail -20; exit 1; }; printf "%s\n" "$O" | grep -E "^[^ ]+\.py::" | sort -u > "$A"; sort -u "$B" -o "$B"; echo "baseline-nodeids=$(grep -c . "$B") after-nodeids=$(grep -c . "$A")"; L=$(comm -23 "$B" "$A"); test -z "$L" || { echo "NODE-IDS-LOST:"; printf "%s\n" "$L" | head -20; exit 1; }; echo NO-NODE-ID-LOST'</automated>
    <fails_when>non-zero exit, or the output contains "BASELINE-MISSING:", "COLLECT-EXIT=" or "NODE-IDS-LOST:", or the line "NO-NODE-ID-LOST" is absent. A count comparison is deliberately not used: the tree collects 4256 node IDs, so any floor low enough to be safe is high enough to accept losing hundreds of tests. The gate also fails closed when the Step 1 baseline was never written.</fails_when>
    <automated>uv run --frozen pytest -q</automated>
    <fails_when>non-zero exit, or the pytest summary line contains "failed" or "error", or the summary reports 0 tests collected</fails_when>
    <automated>uv run --frozen ruff check . && uv run --frozen ruff format --check . && uv run --frozen pyright</automated>
    <fails_when>non-zero exit, or any line beginning "Would reformat:", or a pyright summary reporting 1 or more errors</fails_when>
  </verify>
  <done>
    Zero in-scope `PLC0415` violations remain under `tests/`, down from 166 across 27 files.
    Every retained function-local import carries a reason-bearing mark in the form ruff accepts.
    No excluded path and no zero-violation file was modified, `pyproject.toml` is untouched, the
    targeted single-file backstop run passes, and the whole suite, ruff, ruff format and pyright
    are green. One commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enable PLC0415 with the per-file-ignores that hand off to Phases 18 and 19</name>
  <reversibility rating="costly">D-05 is rated `costly` by the operator and this plan adopts
  that rating. Undoing means removing the `select` entry, all seven per-file-ignore entries and
  every reason-bearing `noqa` comment D-06 introduced, in one change. It also creates a
  cross-phase obligation: Phase 18 deletes `tests/test_animation/**` and
  `tests/test_devices/test_multizone.py`, and Phase 19 deletes `tests/test_theme/**`, when they
  sweep their own files. Applying the reference criteria independently agrees with `costly`
  rather than `one-way`: the change is a configuration edit plus comment removals, with no data
  migration, no published artefact and no external contract, so no `checkpoint:decision` is
  inserted. The flagged D-05 count extension is called out above and does not change this
  rating.</reversibility>
  <files>pyproject.toml</files>
  <read_first>
    - `pyproject.toml:80-89` — the `[tool.ruff.lint]` `select` and `ignore` keys and the five
      existing `[tool.ruff.lint.per-file-ignores]` entries, whose glob style (path key mapped
      to a list of rule codes, with brace expansion where two paths share a rule) is the style
      to follow.
    - `.planning/phases/16-mdns-correctness-docs-and-test-hygiene/16-CONTEXT.md` — D-05 with its
      full rationale, and D-07's permitted-versus-prohibited split.
    - The FLAGGED section at the top of this plan — the measured reason seven entries are needed
      rather than five, and the operator decision point.
    - `.planning/ROADMAP.md` — the v2.1 phase order and which phase owns which excluded path,
      so the handoff comment names the right phases.
    - `.pre-commit-config.yaml` — confirm the ruff hooks run over the same tree, so the new rule
      is enforced on commit as well as in CI.
  </read_first>
  <action>
This is a SEPARATE, LATER commit from Task 1 (D-08). The order is deliberate: with the sweep
already landed, enabling the rule leaves `ruff check .` green at this commit and at every
commit before it. Enabling the rule first would leave one commit in history with 166
outstanding violations, which a bisect could land on.

**Add the rule.** In `[tool.ruff.lint]` at pyproject.toml:80-81, add `"PLC0415"` to the `select`
list so it reads `["E", "F", "I", "N", "W", "UP", "PLC0415"]`. Leave `ignore = []` as it is.

**Add the per-file-ignore entries.** In `[tool.ruff.lint.per-file-ignores]`, after the five
existing `E501` entries, add seven keys each mapped to `["PLC0415"]`:
  - `"src/**"` — 82 violations, largely legitimate circular-import breakers such as the six
    device-class imports inside `_create_device_from_record` and the `discover_devices_mdns`
    import in `api.py`. A future phase lifts this the way Phases 18 and 19 lift theirs.
  - `".planning/**"` — 49 violations in operator tooling, outside the measured tree.
  - `"tests/test_animation/**"` — Phase 18 territory. Phase 18 deletes this line when it sweeps.
  - `"tests/test_theme/**"` — Phase 19 territory. Phase 19 deletes this line when it sweeps.
  - `"tests/test_devices/test_multizone.py"` — Phase 18 territory. Phase 18 deletes this line.
  - `".agents/skills/**"` and `".claude/skills/**"` — archived spike harnesses preserved
    verbatim as evidence sources, two violations in one duplicated file. See the FLAGGED
    section above.

**Add a comment above the new entries** recording why each excluded path is a visible,
deletable line rather than a silent exclusion, and naming which phase deletes which line
(Phase 18: `tests/test_animation/**` and `tests/test_devices/test_multizone.py`; Phase 19:
`tests/test_theme/**`). That handoff is the reason D-05 chose per-file-ignores over selecting
`PLC0415` on `tests/` alone. Keep the comment in Australian English with no em dashes. The
comment is free to name the rule code: the configuration gate counts ENTRIES rather than raw
string occurrences, so prose in a comment cannot move the count. See the note below.

**Note on the configuration gate.** This plan's earlier gate asserted that the rule code appeared
in `pyproject.toml` exactly eight times, one for `select` and seven for the ignores. The
cross-AI plan review found that gate self-contradicting: the handoff comment this same task
mandates naturally names the rule, producing a ninth occurrence and failing the gate on correct
work. Relaxing the comparison to a lower bound was rejected, because it would stop detecting a
dropped entry, which is the one thing the gate exists to detect. The gate now counts the
`select` line carrying the code and the per-file-ignore lines mapping a path key to a list
containing it, asserting exactly one and exactly seven, and separately requires each of the seven
path keys by name. Dropping any entry still fails; comment prose is invisible to it.

Add no ignore over any file that is in scope for the sweep, and do not broaden any existing
entry to swallow an in-scope file (D-07, SPEC prohibition P6).

Commit with a Conventional Commit message and no GSD phase or plan number as the scope, using
`git commit -S -s`.
  </action>
  <acceptance_criteria>
    - `pyproject.toml` `[tool.ruff.lint] select` contains `PLC0415`.
    - `[tool.ruff.lint.per-file-ignores]` contains exactly seven keys mapping to a list
      containing `PLC0415`: `src/**`, `.planning/**`, `tests/test_animation/**`,
      `tests/test_theme/**`, `tests/test_devices/test_multizone.py`, `.agents/skills/**` and
      `.claude/skills/**`. No eighth key, and no key naming a file in scope for the sweep.
    - The configuration gate counts entries rather than raw occurrences of the rule code, so it
      agrees with the mandated handoff comment instead of contradicting it, and still fails if
      any one of the seven entries or the `select` entry is dropped.
    - The five pre-existing `E501` per-file-ignore entries are unchanged, asserted as a count of
      five rather than as the presence of one.
    - `uv run --frozen ruff check --no-cache .` exits 0 with no `PLC0415` line and no
      `Invalid # noqa directive` warning.
    - The rule is genuinely enforced, not vacuously configured: a function-local import in a
      synthetic file named under `tests/` is reported as `PLC0415` by the configured `select`
      with no `--select` flag.
    - Each of the seven ignores genuinely applies: a function-local import in a synthetic file
      named under each ignored path is NOT reported.
    - This commit changes `pyproject.toml` and nothing else.
    - `uv run --frozen pytest -q`, `uv run --frozen ruff format --check .` and
      `uv run --frozen pyright` all pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen ruff check --no-cache .</automated>
    <fails_when>non-zero exit, or any line matching "PLC0415" in the output, or any line matching "Invalid `# noqa` directive"</fails_when>
    <automated>bash -c 'T=$(mktemp -t plcprobe.XXXXXX.py); printf "def f():\n    import os\n    return os\n" > "$T"; O=$(uv run --frozen ruff check --no-cache --stdin-filename tests/test_gate_probe.py --output-format=concise - < "$T" 2>&1); S=$?; rm -f "$T"; echo "positive-probe-exit=$S"; test "$S" -eq 1 || { echo POSITIVE-PROBE-EXIT-NOT-1; printf "%s\n" "$O" | tail -20; exit 1; }; printf "%s\n" "$O" | grep -q PLC0415 || { echo RULE-NOT-ENFORCED-ON-TESTS; printf "%s\n" "$O" | tail -20; exit 1; }; echo RULE-ENFORCED-ON-TESTS'</automated>
    <fails_when>non-zero exit, or the printed `positive-probe-exit=` value is anything other than 1, or the output contains "POSITIVE-PROBE-EXIT-NOT-1" or "RULE-NOT-ENFORCED-ON-TESTS", or the line "RULE-ENFORCED-ON-TESTS" is absent. Ruff exits 1 when it reports a diagnostic and 2 when it fails to run, so requiring exactly 1 distinguishes an enforced rule from a broken invocation, which a bare grep for the rule code cannot.</fails_when>
    <automated>bash -c 'T=$(mktemp -t plcprobe.XXXXXX.py); printf "def f():\n    import os\n    return os\n" > "$T"; FAIL=0; for p in src/lifx/probe_gate.py .planning/scripts/probe_gate.py tests/test_animation/test_probe_gate.py tests/test_theme/test_probe_gate.py tests/test_devices/test_multizone.py .agents/skills/probe_gate.py .claude/skills/probe_gate.py; do O=$(uv run --frozen ruff check --no-cache --stdin-filename "$p" --output-format=concise - < "$T" 2>&1); S=$?; test "$S" -eq 0 || { echo "IGNORED-PATH-PROBE-EXIT: $p exit=$S"; printf "%s\n" "$O" | tail -5; FAIL=1; continue; }; printf "%s\n" "$O" | grep -q PLC0415 && { echo "IGNORE-NOT-APPLIED: $p"; FAIL=1; }; done; rm -f "$T"; test "$FAIL" -eq 0 || exit 1; echo ALL-SEVEN-IGNORES-APPLIED'</automated>
    <fails_when>non-zero exit, or any line beginning "IGNORED-PATH-PROBE-EXIT:" or "IGNORE-NOT-APPLIED:", or the line "ALL-SEVEN-IGNORES-APPLIED" is absent. Each probe now requires ruff to exit 0, so an ignore that appears to hold only because ruff failed to run is caught, which the earlier `|| true` form could not do.</fails_when>
    <automated>bash -c 'L=$(git show --name-only --pretty=format: HEAD | grep -c . || true); F=$(git show --name-only --pretty=format: HEAD); printf "%s\n" "$F"; echo "files-in-config-commit=$L"; test "$L" -eq 1 || { echo WRONG-FILE-COUNT-IN-CONFIG-COMMIT; exit 1; }; printf "%s\n" "$F" | grep -qx pyproject.toml || { echo CONFIG-COMMIT-DOES-NOT-TOUCH-PYPROJECT; exit 1; }; echo CONFIG-COMMIT-SHAPE-OK'</automated>
    <fails_when>non-zero exit, or the printed `files-in-config-commit=` value is anything other than 1, or the output contains "WRONG-FILE-COUNT-IN-CONFIG-COMMIT" or "CONFIG-COMMIT-DOES-NOT-TOUCH-PYPROJECT", or the line "CONFIG-COMMIT-SHAPE-OK" is absent</fails_when>
    <automated>bash -c 'S=$(grep -cE "^select = .*\"PLC0415\"" pyproject.toml || true); echo "plc0415-select-lines=$S"; test "$S" -eq 1 || { echo WRONG-SELECT-ENTRY-COUNT; exit 1; }; I=$(grep -cE "^\"[^\"]+\" = \[[^]]*\"PLC0415\"" pyproject.toml || true); echo "plc0415-ignore-entries=$I"; test "$I" -eq 7 || { echo WRONG-IGNORE-ENTRY-COUNT; exit 1; }; for k in "src/\*\*" ".planning/\*\*" "tests/test_animation/\*\*" "tests/test_theme/\*\*" "tests/test_devices/test_multizone.py" ".agents/skills/\*\*" ".claude/skills/\*\*"; do C=$(grep -cE "^\"$k\" = \[[^]]*\"PLC0415\"" pyproject.toml || true); test "$C" -eq 1 || { echo "IGNORE-KEY-NOT-EXACTLY-ONCE: $k count=$C"; exit 1; }; done; E=$(grep -c "\"E501\"" pyproject.toml || true); echo "e501-entries=$E"; test "$E" -eq 5 || { echo E501-IGNORES-CHANGED; exit 1; }; echo CONFIG-SHAPE-OK'</automated>
    <fails_when>non-zero exit, or the printed `plc0415-select-lines=` value is other than 1, or the printed `plc0415-ignore-entries=` value is other than 7, or the printed `e501-entries=` value is other than 5, or the output contains "WRONG-SELECT-ENTRY-COUNT", "WRONG-IGNORE-ENTRY-COUNT", "IGNORE-KEY-NOT-EXACTLY-ONCE:" or "E501-IGNORES-CHANGED", or the line "CONFIG-SHAPE-OK" is absent. Counts entries rather than raw occurrences of the rule code, so the mandated handoff comment cannot move the total, while dropping any single entry still fails.</fails_when>
    <automated>uv run --frozen pytest -q</automated>
    <fails_when>non-zero exit, or the pytest summary line contains "failed" or "error", or the summary reports 0 tests collected</fails_when>
    <automated>uv run --frozen ruff format --check . && uv run --frozen pyright</automated>
    <fails_when>non-zero exit, or any line beginning "Would reformat:", or a pyright summary reporting 1 or more errors</fails_when>
  </verify>
  <done>
    `PLC0415` is in ruff's `select` list, seven per-file-ignore entries scope it, and both
    directions are proven: the rule fires on a synthetic in-scope test path and is suppressed on
    each of the seven ignored paths. The whole repository passes `ruff check .`. The
    configuration change is its own commit, after the sweep, so no commit in history has the
    rule enabled with violations outstanding.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository lint configuration -> every future contribution | A rule that is configured but not enforced, or enforced but silently ignored over an in-scope path, gives false assurance to every later change. |
| Test-collection order -> module-scope imports | Hoisting an import changes when it runs relative to conftest setup, `sys.path` mutation and patching. A relocation that silently changes what a test exercises is a weakened test wearing a green tick. |
| Excluded paths -> Phases 18 and 19 | The per-file-ignore entries are the mechanism preserving the locked v2.1 file-disjointness property. Touching an excluded path, or hiding the exclusion, breaks a cross-phase guarantee. |

## STRIDE Threat Register

ASVS level 1, blocking on `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-16 | Tampering | Tests silently weakened to make the sweep pass | high | mitigate | SPEC prohibition P6, bounded by D-07. Three gates: the collected node-ID SET taken before the sweep is compared against the set taken after, and the gate fails on the first node ID that disappears, so a deleted or de-collected test fails; every `noqa: PLC0415` under `tests/` must carry ` - ` plus prose, so a bare suppression fails; and the sweep-scope gate rejects any per-file-ignore over an in-scope file because `pyproject.toml` may not appear in the sweep commit at all. The set comparison replaces an earlier 2400 floor that the cross-AI plan review measured against a tree collecting 4256, where it would have accepted losing more than 1800 tests. It also fails closed when the Step 1 baseline is missing, so skipping the baseline is not a way past it. |
| T-16-17 | Tampering | A vacuously configured rule | high | mitigate | A `select` entry that never fires would satisfy `ruff check .` while enforcing nothing, which is the same failure class Phase 15's vacuous-coverage-gate guard was built for. Mitigated by a positive-control probe (a synthetic function-local import named under `tests/` must be reported by the configured `select` with no `--select` flag) and a negative-control probe over all seven ignored paths. |
| T-16-21 | Tampering | A gate that reports success when its tool never ran | high | mitigate | The class the cross-AI plan review found live in this plan, and the reason it is recorded as a threat rather than fixed silently: a gate capturing ruff's output without inspecting its exit status reports zero violations when ruff fails to start, which was reproduced against the decisive sweep gate using an unwritable `uv` cache. Two rules now bind every gate in this plan. Every ruff invocation captures its status and asserts an exact expected value: 0 where no diagnostic is expected, 1 where a diagnostic is required, and at most 1 where either is acceptable, so ruff's exit 2 error status can never read as success. And no `test` that decides a gate may sit mid-chain without an explicit failure branch, because a bare `test` followed by any other command has its status discarded by the trailing command. Both patterns existed in this plan's earlier gates and are removed throughout. |
| T-16-18 | Tampering | Behaviour-changing import relocation | high | mitigate | An import that is the patch target, or that must not run at collection time, changes what the test exercises when hoisted. Mitigated by the Step 3 judgement list, by Step 3a's named resolution for the one file where neither hoisting nor retaining was available, by the reason-bearing retention convention, and empirically by the full suite plus the targeted single-file backstop run of `tests/test_network/test_connection_retry.py`, which is the file whose `measurement_support` import is most sensitive to collection order. The backstop's conclusion, stated correctly: a targeted run loads `tests/conftest.py` and relies on its `sys.path` insert, and what it proves is that the hoisted import needs nothing beyond that, in particular not the tooling conftest beside `.planning/scripts/tests/` that only a `testpaths` run loads. |
| T-16-19 | Repudiation | An invisible exclusion | medium | mitigate | D-05 chose per-file-ignores precisely so each excluded path is a visible, deletable line with a named owning phase. The configuration comment names Phase 18 and Phase 19 against their lines, and the FLAGGED section records why the count is seven rather than five rather than letting two entries appear unexplained. |
| T-16-20 | Denial of service | n/a | low | accept | Mechanical import relocation and a lint configuration entry. No runtime path, no network behaviour and no resource budget is touched. |
| T-16-SC | Tampering | Package-manager installs | low | accept | No install task exists in this phase: no `uv add`, no `pip`, no `npm`, no `cargo`. `PLC0415` ships with the ruff already pinned in `uv.lock`, so no new package enters the dependency graph and the package-legitimacy gate has nothing to audit. `uv run --frozen` is used throughout, which refuses to resolve or add a dependency. |
</threat_model>

<verification>
- `uv run --frozen ruff check --no-cache .` exits 0 with no `PLC0415` line, which is
  requirement 4's decisive command expressed as a ruff invocation rather than a hand-counted
  total (16-CONTEXT.md `<spec_lock>`).
- `uv run --frozen ruff format --check .`, `uv run --frozen pyright` and
  `uv run --frozen pytest` all pass.
- Every ruff invocation in this plan captures and asserts its exit status, and no gate leaves a
  deciding `test` mid-chain where the trailing command discards its status. Both patterns were
  present in the plan the cross-AI review read, and both produced gates that could not fail.
- No collected pytest node ID disappears between the pre-sweep baseline and the post-sweep
  collection. The gate fails closed when the baseline is missing, so it cannot be bypassed by
  skipping Step 1.
- `tests/test_packaging.py` runs its manifest assertion on Python 3.10 as well, because Step 3a
  replaced its version skip with a module-scope compatibility import over the `tomli` dev
  dependency already declared at pyproject.toml:57. No dependency is added and no `noqa` is
  introduced.
- The four issues this phase closes are recorded in `16-04-SUMMARY.md` with their requirement
  mapping, for the ship step's PR body. No plan commit carries a `Closes` line.
- `git diff --name-only` across this plan's two commits lists exactly the swept test files plus
  `pyproject.toml`, and no file under `tests/test_animation/`, `tests/test_theme/` or
  `tests/test_devices/test_multizone.py`.
- Commit shape matches D-08: the sweep is one commit containing no `pyproject.toml` change, and
  the configuration change is a separate later commit containing only `pyproject.toml`.
- Codecov: this plan changes no measured line, so the patch gate scores nothing from it.
  Phase 15's vacuous-gate guard distinguishes that legitimate case from a report that
  classifies nothing.
</verification>

<success_criteria>
- Roadmap success criterion 4 is TRUE: the mDNS discovery tests import at module scope, ruff
  and the complete mDNS discovery suite pass, and any retained function-local import carries a
  named circular-import or patching reason rather than being left unexplained.
- D-05, D-06, D-07 and D-08 are implemented as recorded, with the single flagged extension to
  D-05's entry count surfaced above rather than applied silently.
- The v2.1 file-disjointness property between Phases 16, 18 and 19 is preserved, and each
  excluded path is a visible line its owning phase deletes.
</success_criteria>

<output>
Create `.planning/phases/16-mdns-correctness-docs-and-test-hygiene/16-04-SUMMARY.md` when done.
Record in it: the pre-sweep ruff inventory (file list and per-file counts); the pre-sweep and
post-sweep collected node-ID counts and the fact that no node ID was lost; the list of retained
function-local imports with their stated reasons; the `tests/test_packaging.py` resolution and
that it gained Python 3.10 coverage rather than needing a suppression; whether the seven-entry
per-file-ignore list was accepted or changed by the operator; and the issue-to-requirement
mapping for #213, #215, #216 and #217 that the ship step's PR body needs.
</output>
