---
phase: 17-fleet-diagnostics-and-the-staleness-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/scripts/ipv6_thread_probe.py
  - .planning/scripts/tests/test_ipv6_thread_probe.py
autonomous: true
requirements: [MDNS-10]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "An SRV target holding cached address records that yield no usable selection is reported as a distinct state from an SRV target with no cached address record at all (AC-01, AC-02)"
    - "The refused-record report names how many address records were seen for the target and classifies each one (AC-03)"
    - "The view's target field carries the normalised owner, so a trailing-dot SRV target is reported as the key the cache itself uses and the probe does not depend on every downstream lookup normalising for it (AC-04)"
    - "An instance whose cached records were refused but which has a usable packet-source fallback states that a fallback existed and was not used (AC-05)"
    - "A file-wide grep of the probe source for the removed link-local counter name or the old zone-scope predicate name returns nothing (AC-06)"
    - "The IPv4-preferred, fallback-source and routable-preferred WHY branches still print, and their tooling tests pass (AC-07)"
    - "A malformed middle instance is marked as malformed, all three instances are still returned in order, and report_records() returns normally over the same cache (AC-08)"
    - "A TXT payload parsing as TxtData but carrying no id, p or fw key prints ? for the missing values without raising (AC-09)"
    - "Instances after the malformed one print unchanged and in the same order (AC-08, ordering edge)"
    - "Each malformed field is named once and the instance's remaining well-formed fields still print (AC-08, adjacency edge)"
    - "The two conditions a malformed wire payload can still produce are rendered rather than raised: a TXT payload that did not parse prints a diagnostic naming the malformed field and the loop continues to the remaining instances, and an instance with no usable chosen address renders through the non-resolving states of R1's five-state model instead of terminating the run, which is what lets report_records() narrow a local instead of asserting (SPEC R3 Target clause as amended by A6)"
    - "The third condition, a non-list AAAA value, ceases to exist rather than becoming a runtime branch: _InstanceView.aaaa is typed tuple[str, ...] and built inside _instance_view() from the cache's own values, so no runtime value can be non-list, and that elimination is recorded in 17-01-SUMMARY.md's ## Eliminated by construction section, which amended R3 requires in place of a diagnostic (SPEC amendment A6)"
  artifacts:
    - path: ".planning/scripts/ipv6_thread_probe.py"
      provides: "Typed per-instance view, five-state address selection reporting, defensive TXT handling, renamed routable-scope predicate"
      contains: "_InstanceView"
    - path: ".planning/scripts/tests/test_ipv6_thread_probe.py"
      provides: "TestSyntheticCacheReporting extensions covering the refused, absent, normalised-owner, unused-fallback and malformed cases, plus the report_records smoke test"
      contains: "test_instance_view_reports_refused_when_cached_addresses_yield_no_selection"
  key_links:
    - from: ".planning/scripts/ipv6_thread_probe.py"
      to: "src/lifx/network/discovery/mdns/discovery.py"
      via: "_instance_view() derives target through _normalise_dns_name() once, so the view's target field is the key records_for() itself computes rather than a .lower()ed near-miss"
      pattern: "_normalise_dns_name"
    - from: ".planning/scripts/ipv6_thread_probe.py"
      to: ".planning/scripts/tests/test_ipv6_thread_probe.py"
      via: "TestSyntheticCacheReporting asserts on _InstanceView attributes rather than dict subscripts"
      pattern: "view\\.selection"
  prohibitions:
    - statement: "MUST NOT modify any file under .planning/milestones/v2.0-phases/"
      status: resolved
      verification: test
---

<objective>
Fix the three `report_records()` defects in the IPv6 Thread probe so the diagnostic reports
which of two distinct non-resolving address states it actually observed, stops reporting a
quantity that cannot be non-zero, and survives a partially assembled instance.

Purpose: MDNS-10. A diagnostic tool that collapses two different failures into one message
cannot tell an operator which one the fleet is in, and a bare assert ends the whole run at the
first instance it was run to diagnose.

Output: `.planning/scripts/ipv6_thread_probe.py` carrying a typed `_InstanceView` structure
(D-05), five explicit selection states (D-01), the record count and per-record classification
on the refused message (D-02), a single owner normalisation site (D-04), the split summary
counts (D-03), the malformed-TXT marker (D-06, D-07), and the dead link-local counter, its
warning and the old predicate name gone (R2). Plus the tooling tests in
`.planning/scripts/tests/test_ipv6_thread_probe.py` (D-08).

**Sequencing (D-20), stage one of three.** D-20 splits this phase on the hardware boundary into
a no-hardware stage, a hardware stage and a stage that needs the hardware stage's numbers. This
plan is the first half of D-20's no-hardware stage. D-20's three stages map onto five execution
waves rather than three, because two pairs of plans cannot run concurrently: this plan and
`17-02-PLAN.md` both modify `.planning/scripts/ipv6_thread_probe.py` and
`.planning/scripts/tests/test_ipv6_thread_probe.py`, and `17-03-PLAN.md` and `17-04-PLAN.md`
share a git index and an evidence directory, which D-21 as amended records is not made safe by
file-disjointness alone. Both splits are serialisation within one D-20 stage, not extra stages.
Nothing here touches hardware.
</objective>

<assumption_delta_decision>
The deterministic assumption-delta detector returned `detected: true` with a single
`chosen`-family signal. It is a false positive, already adjudicated by the orchestrator: the
match is a literal substring inside a counter identifier this phase deletes, not a
derived-to-chosen transition. No core assumption loses a monopoly in this phase.

- **Primary noun:** unchanged.
- **Decision:** `no-change`. The signal is a lexical match on a counter identifier this phase
  deletes; no generalization occurred.
</assumption_delta_decision>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/17-fleet-diagnostics-and-the-staleness-control/17-SPEC.md
@.planning/phases/17-fleet-diagnostics-and-the-staleness-control/17-CONTEXT.md
@.planning/phases/17-fleet-diagnostics-and-the-staleness-control/17-RESEARCH.md
@.planning/phases/17-fleet-diagnostics-and-the-staleness-control/17-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols and paths created by this plan. They do not exist yet, so drift verification must
not flag them as unacknowledged.

| Kind | Name | File |
|------|------|------|
| dataclass | `_InstanceView` | `.planning/scripts/ipv6_thread_probe.py` |
| type alias | `_Selection` | `.planning/scripts/ipv6_thread_probe.py` |
| function | `_non_resolving_lines` | `.planning/scripts/ipv6_thread_probe.py` |
| function | `_has_routable_scope` (rename of the existing zone-scope predicate) | `.planning/scripts/ipv6_thread_probe.py` |
| test | `test_instance_view_reports_refused_when_cached_addresses_yield_no_selection` | `.planning/scripts/tests/test_ipv6_thread_probe.py` |
| test | `test_instance_view_reports_absent_when_no_address_record_is_cached` | `.planning/scripts/tests/test_ipv6_thread_probe.py` |
| test | `test_instance_view_keys_addresses_and_selection_on_the_same_normalised_owner` | `.planning/scripts/tests/test_ipv6_thread_probe.py` |
| test | `test_instance_view_names_an_unused_fallback_when_cached_addresses_are_refused` | `.planning/scripts/tests/test_ipv6_thread_probe.py` |
| test | `test_instance_view_marks_a_malformed_txt_and_keeps_the_remaining_instances` | `.planning/scripts/tests/test_ipv6_thread_probe.py` |
| test | `test_non_resolving_lines_marks_a_missing_chosen_address` | `.planning/scripts/tests/test_ipv6_thread_probe.py` |
| test | `test_report_records_completes_over_a_malformed_middle_instance` | `.planning/scripts/tests/test_ipv6_thread_probe.py` |
| document section | `## Red phase` in `17-01-SUMMARY.md` | `.planning/phases/17-fleet-diagnostics-and-the-staleness-control/17-01-SUMMARY.md` |
| document section | `## Eliminated by construction` in `17-01-SUMMARY.md` | `.planning/phases/17-fleet-diagnostics-and-the-staleness-control/17-01-SUMMARY.md` |

Symbols the rest of the phase adds are listed in `17-02-PLAN.md` through `17-05-PLAN.md`.

The two identifiers Task 3 deletes are named literally in that task's action body, because the
executor cannot remove a symbol it is not allowed to name. Their negative greps are exempted
here:

<!-- planner-discipline-allow: linklocal_chosen -->
<!-- planner-discipline-allow: is_reachable_choice -->

## Review findings addressed in this plan

Pass 1 of `17-REVIEWS.md` raised three findings against this plan and all three were
incorporated. Pass 2 raised four more and pass 3 raised two, all listed below, newest first. All
are incorporated.

### Pass 3

| Finding | Reviewer | Disposition |
|---------|----------|-------------|
| The plan removes the AAAA list type check while locked R3 and this plan's own `must_haves` still require a diagnostic for it, so execution cannot honestly mark both satisfied | Codex (MEDIUM), verified by the orchestrator | **Resolved at the SPEC level, and this plan now matches it.** SPEC amendment A6 splits R3's three conditions by whether they can still occur: the unparsed TXT payload and the instance with no usable chosen address are handled at run time, and the non-list AAAA condition is eliminated by construction once `_InstanceView.aaaa` is `tuple[str, ...]` built inside `_instance_view()`. Amended R3 requires that elimination to be **recorded** rather than evidenced by a branch, so this plan's `must_haves` now state the two run-time conditions and the one eliminated one separately, Task 3 writes the elimination into `17-01-SUMMARY.md` under `## Eliminated by construction`, and a gate reads that section. The engineering is unchanged: re-adding the AAAA check as a runtime branch would create exactly the dead code R2 deletes three lines away |
| Task 1's fourth verify gate floors the whole-file passed count at 132, which is the pre-change count, where 134 should pass once Task 1's two tests exist | OpenCode (LOW), uncontested | **Incorporated.** The floor is 134. It is redundant with gate 1, which requires at least three passes inside `TestSyntheticCacheReporting`, so nothing slipped through the weaker floor; it is raised rather than deleted because it is the only gate in this task that sees the whole file, and a new test that breaks an unrelated class shows up here first |

### Pass 2

| Finding | Reviewer | Disposition |
|---------|----------|-------------|
| `_InstanceView.selection` and `_InstanceView.chosen` are typed independently, so pyright cannot infer that `chosen` is non-null from `selection == "selected"`. The plan's claim that "there is nothing left to narrow" is therefore not necessarily true | Codex (MEDIUM) | **Incorporated.** The claim is withdrawn and replaced with a mechanism. Task 1 binds `chosen = view.chosen` to a local and tests that local for `None`, which is narrowing pyright performs unconditionally, and moves the three non-resolving renderings into `_non_resolving_lines()`. The state a dataclass cannot forbid, a `selected` or `fallback` view carrying a null `chosen`, is rendered as R3's malformed-field marker rather than asserted, which is what SPEC R3's Target clause asks for: each of the three conditions prints a diagnostic line naming the malformed field |
| Task 1's third test asserts exact message text through `capsys`, re-coupling the suite to wording amendment A1 deliberately freed, and D-08 allows exactly one `report_records()` smoke test that asserts no message text | OpenCode (LOW/MEDIUM) | **Incorporated by removal.** `test_report_records_prints_both_non_resolving_states` is dropped. D-08 is honoured literally: the only `report_records()` test in this plan is the malformed-instance smoke test, which asserts no message text. The two states are distinguished at the view level by tests 1 and 2, which is what A1 moved them there for, and the two message literals stay pinned by this plan's own source-grep acceptance criteria, which bind the executor without binding the suite forever |
| The success criteria require `17-01-SUMMARY.md` to record four named pre-change failures, but the verification section checks only code, tests, lint and protected paths, so the plan can pass without the promised red-phase record | Codex (MEDIUM) | **Incorporated.** Task 1 now creates `17-01-SUMMARY.md` with its `## Red phase` section as its first act, each later task appends to it, and Task 3 carries a runnable gate that reads the file and fails when the heading or any of the four recorded test names is absent. The criterion and the gate now claim the same thing |
| The two state tests written pre-change fail with `AttributeError`, not a failing assertion, so a criterion asking for "the failing assertion" overstates what is recorded | OpenCode (LOW) | **Incorporated by narrowing.** Every red-phase criterion now asks for "the recorded pre-change failure", and the actions say which shape to expect for each test: `AttributeError` for the three view-level tests, which read an attribute the pre-change dict does not have, and `AssertionError` raised from inside `report_records()` for the smoke test |
| Line drift in `read_first`: `TargetNotFound` and `TargetOutcome` are at 643-668, not 656-676, and `SweepResult` is at 197 | OpenCode (LOW) | **Incorporated.** Corrected in Task 1's `read_first` |

### Pass 1

| Finding | Reviewer | Disposition |
|---------|----------|-------------|
| Task 2's "two different lookup keys" rationale is materially false: `records_for()` normalises its owner at `src/lifx/network/discovery/mdns/discovery.py:330`, and `addresses_for()` reaches it through `_addresses_in_order()`, so both probe lookups already key identically today | OpenCode, confirmed against source during review aggregation and contradicting Codex's "genuine asymmetry" strength claim | **Incorporated.** Task 2's action now states the true mechanism. The edit and the test are unchanged; only the rationale and the acceptance wording change. `17-RESEARCH.md` Pitfall 4 and `17-CONTEXT.md` D-04 carry the same false rationale and are deliberately left unedited, because a decision record is a record of what was decided; the correction lives here, where the executor reads it |
| `_InstanceView.selection` is unrestricted `str` despite exactly five enumerated states | Codex (LOW) | **Incorporated.** Task 1 types it `Literal["selected", "fallback", "refused", "absent", "no-target"]`, which pyright already checks for this file |
| Several acceptance criteria claim tests fail against the parent source, but no red-phase command is ever run | Codex (LOW) | **Incorporated by narrowing.** A red-phase command cannot be one of this task's verify steps, because by the time verification runs the source is already changed and reconstructing the pre-change probe fights the tooling conftest's own `sys.path` insert. Instead the red phase becomes an ordered, recorded step in each task's action, and the acceptance criteria now assert the recorded transcript rather than an unrun claim |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end typed instance view carrying five explicit selection states</name>
  <precondition>Phase 16's MDNS-09 shared guard is present on this branch: `grep -q "_owner_is_unusable" src/lifx/network/discovery/mdns/discovery.py` succeeds</precondition>
  <reversibility rating="costly">D-05: every consumer of the view, including the existing TestSyntheticCacheReporting assertions, moves from dict subscripts to attribute access in this one change.</reversibility>
  <files>.planning/scripts/ipv6_thread_probe.py, .planning/scripts/tests/test_ipv6_thread_probe.py</files>
  <read_first>
    - `.planning/scripts/ipv6_thread_probe.py` lines 172-195 (`classify_address` and the zone-scope predicate), 197-206 (`SweepResult`), 355-403 (`_instance_view`), 406-513 (`report_records`), 643-668 (`TargetNotFound` and `TargetOutcome`, the in-file dataclass precedent to copy; the earlier "656-676" citation was line-drifted and is corrected here)
    - `.planning/scripts/tests/test_ipv6_thread_probe.py` lines 131-160 (`_cache_chain`) and 1039-1077 (`TestSyntheticCacheReporting` and its one dict-subscript consumer)
    - `src/lifx/network/discovery/mdns/discovery.py` lines 344-400 (`_addresses_in_order`, `addresses_for`, `_owner_is_unusable`, `selected_address_for`) and lines 775-843 (`resolve`, which reaches the packet-source fallback only when there is no SRV endpoint)
    - `.planning/phases/17-fleet-diagnostics-and-the-staleness-control/17-PATTERNS.md`, the section on `_instance_view()` and `report_records()`
  </read_first>
  <behavior>
    - A cache holding TXT plus SRV plus one unscoped `fe80::` AAAA for the SRV target yields a view whose selection is the refused state, whose chosen is None, and whose address_records holds that one address.
    - A cache holding TXT plus SRV and no A or AAAA record for the SRV target yields a view whose selection is the absent state, whose chosen is None, and whose address_records is empty.
    - A cache holding TXT plus SRV plus a usable A record yields the selected state and a chosen equal to that A record.
    - _non_resolving_lines() returns a different CHOSEN line for a refused view than for an absent view, and a third for a no-target view, so the three states are distinguishable without any test asserting on stdout.
  </behavior>
  <action>
Introduce a module-level `@dataclass(frozen=True) class _InstanceView` in
`.planning/scripts/ipv6_thread_probe.py`, placed beside the existing `TargetNotFound` and
`TargetOutcome` dataclasses and following their one-line-docstring-with-rationale convention.
Fields, in this order and with these exact names and types: `instance: str`,
`txt: TxtData | None`, `txt_count: int`, `srv: SrvData | None`, `srv_count: int`,
`target: str | None`, `a: str | None`, `aaaa: tuple[str, ...]`,
`address_records: tuple[str, ...]`, `addresses: frozenset[str]`, `chosen: str | None`,
`fallback: str | None`, `selection: _Selection`.

Declare `_Selection` as a module-level type alias immediately above the dataclass,
`_Selection = Literal["selected", "fallback", "refused", "absent", "no-target"]`, importing
`Literal` from `typing` in the existing module-scope import block. An unrestricted `str` would
admit an invalid sixth state and give pyright nothing to check exhaustively, which is the
opposite of what D-05 buys by typing the structure at all. The probe is inside pyright's
explicit include list at `pyproject.toml:122-131`, so the alias is machine-checked rather than
decorative, and a dispatch arm added later for a state that does not exist fails the type gate
instead of shipping as dead code.

Change `_instance_view()`'s return annotation from `list[tuple[str, dict[str, object]]]` to
`list[_InstanceView]` and build one `_InstanceView` per TXT owner. The instance name becomes a
field rather than the first half of a tuple. `address_records` is
`tuple(a_values) + tuple(aaaa_values)`, which reproduces the library's own A-then-AAAA
first-learned order from `_addresses_in_order()`. `aaaa` becomes `tuple(aaaa_values)`. Keep `a`
as the first A value or None.

Compute `selection` and `chosen` together inside `_instance_view()`, per D-01 and D-06, using
only the cache's public surface. There are exactly five states and no others, spelled as the
string literals `"selected"`, `"fallback"`, `"refused"`, `"absent"` and `"no-target"`. Derive
them so: when `target` is None and a packet-source fallback exists for the instance, the state
is `"fallback"` with `chosen` set to that fallback; when `target` is None and no fallback
exists, the state is `"no-target"` with `chosen` None; when `target` is set and
`cache.selected_address_for(target)` returns a value, the state is `"selected"` and `chosen` is
that value; when `target` is set, the selection is None and `address_records` is non-empty, the
state is `"refused"` and `chosen` is None; otherwise the state is `"absent"` and `chosen` is
None.

That derivation deliberately stops the probe using the packet-source fallback for an instance
that does have an SRV record. It matches `resolve()` at
`src/lifx/network/discovery/mdns/discovery.py:816-825`, which reaches
`self._fallback_ip_by_instance` only in the `elif allow_fallback` arm, taken when
`srv_endpoint is None`. The previous code printed the fallback with the reason "no SRV record"
for an instance that did have one. Keep the `fallback` field populated in every state so the
renderer can name an unused fallback.

Rewrite `report_records()`'s per-instance loop to iterate `for view in views:` and read every
field through attributes instead of subscripts. Keep every existing header, SRV, A and AAAA print
statement byte-identical.

Bind the chosen address to a local before testing it: `chosen = view.chosen`, then
`if chosen is None:` print the non-resolving rendering and continue, otherwise fall into the
existing classification and WHY chain with `chosen` in hand. Bind to a local rather than testing
`view.chosen` in place, and dispatch the resolving arm on that local rather than on
`view.selection`. A frozen dataclass gives pyright no relationship between two independently typed
fields, so nothing lets it infer a non-null `chosen` from a `selection` value; a local bound once
and tested for `None` is narrowing it performs unconditionally. This is the mechanism that
replaces the removed type assertion, and it is why no `cast` and no re-assertion is needed.

Add a module-level pure renderer `_non_resolving_lines(view: _InstanceView) -> list[str]`,
returning the lines to print when `chosen` is None. It is a separate function so the tooling tests
can assert the three states are distinguishable without capturing stdout, which is what SPEC
amendment A1 moved these assertions off stdout for. Its arms, per D-02:

- refused: a CHOSEN line reading `none - N cached address record(s) for the SRV target, none usable`, with N being `len(view.address_records)`, followed by one indented line per record in `view.address_records`, in the same address-then-`[classification]` shape the AAAA block already uses, with the classification coming from `classify_address()`.
- absent: a CHOSEN line reading `none - no address record cached for the SRV target`.
- no-target: the existing `none - no SRV record and no fallback source` line, unchanged.
- any remaining selection value, which is a `selected` or `fallback` view carrying a null chosen: a CHOSEN line reading `none - chosen address missing for a resolved selection state`. This is R3's **second** condition as SPEC amendment A6 states it, an instance with no usable chosen address rendering through the non-resolving states of R1's five-state model rather than terminating the run, and it is what lets the caller narrow a local instead of asserting. `_instance_view()` does not construct that combination, so it will not appear on a real run; it is written as a marker rather than deleted because deleting it would put the narrowing back on a claim pyright cannot check, and it is written as a print rather than an assert because ending the run at one malformed instance is the defect R3 exists to remove.

The WHY branch chain keeps its three surviving reasons unchanged in this task:
`A record present; IPv4 preferred over IPv6`, `no SRV record; fell back to the response
packet's source`, and `routable {classification} preferred over link-local`. Leave the fourth
branch, its counter and the warning line alone here; Task 3 removes them.

Migrate the one existing consumer in `.planning/scripts/tests/test_ipv6_thread_probe.py`,
`test_instance_view_retains_unordered_advertised_addresses` around line 1070, from the
tuple-unpack plus dict-subscript form to `[view] = probe._instance_view(cache)` with
`view.instance`, `view.addresses`, `view.chosen` and `view.fallback`. Its asserted values do
not change.

Order the work red first, and record the red phase. Create
`.planning/phases/17-fleet-diagnostics-and-the-staleness-control/17-01-SUMMARY.md` now, holding
only a `## Red phase` heading, and append to that section as each task's red run happens. Creating
the file in this task rather than at the end of the plan is deliberate: Task 3 carries an
automated gate that reads it, and a record written only after every gate has run is a record no
gate can check. Complete the rest of the SUMMARY at the end of the plan as usual.

Write the two new tests before touching `_instance_view()`, run
`uv run --frozen pytest ".planning/scripts/tests/test_ipv6_thread_probe.py::TestSyntheticCacheReporting" -q --no-cov -p no:cacheprovider`,
and paste the resulting failure summary line and the recorded pre-change failure for each of the
two state tests under the `## Red phase` heading, naming each test. Expect an `AttributeError`
rather than a failing assertion: both tests read `view.selection` on what is still a dict before
the change, so the recorded failure is a traceback, not an assertion diff. Record what actually
happened. That transcript is what AC-01 and AC-02 mean by "fails against the pre-change source";
without it the claim is unevidenced, because no verification step run after the change can observe
the state before it.

Add two tests to `TestSyntheticCacheReporting`, built from the same real
`_LifxRecordCache().add_packet()` idiom that `_cache_chain()` and the existing test already
use, per D-08. Name them exactly
`test_instance_view_reports_refused_when_cached_addresses_yield_no_selection` and
`test_instance_view_reports_absent_when_no_address_record_is_cached`. The refused fixture
advertises TXT, SRV and one AAAA record whose address is the unscoped link-local literal `fe80::20`
for the SRV target; `_pick_address()` refuses it through `_is_usable_mdns_address()`
(`src/lifx/network/discovery/mdns/discovery.py:201-227`), and that refusal happens at selection
rather than at ingest, which is what makes the fixture constructible. The absent fixture
advertises TXT and SRV only, with no address record for the target. Have each test also assert
that `probe._non_resolving_lines(view)` returns a first line differing from the other state's,
which is how the two renderings are proven distinct without asserting either literal.

Do not add a second `report_records()` test here. D-08 allows exactly one, the malformed-instance
smoke test Task 3 adds, and it asserts no message text. SPEC amendment A1 moved the state
assertions to the view level precisely to leave the wording free to change, so a stdout capture
pinning either new CHOSEN literal into the suite would undo the amendment. The two literals are
still pinned for this phase by this task's source-grep acceptance criteria, which bind the
executor without binding the suite forever.
  </action>
  <verify>
    <automated>uv run --frozen pytest ".planning/scripts/tests/test_ipv6_thread_probe.py::TestSyntheticCacheReporting" -q --no-cov -p no:cacheprovider</automated>
    <fails_when>non-zero exit, a "failed" or "error" count other than 0 in the summary line, or fewer than 3 tests reported as passed (the one pre-existing test plus this task's two)</fails_when>
    <automated>uv run --frozen python -c "import sys; sys.path.insert(0, '.planning/scripts'); import ipv6_thread_probe as p; V=p._InstanceView; import dataclasses; f={x.name for x in dataclasses.fields(V)}; print('view-fields=%r' % sorted(f)); assert f.issuperset({'selection','chosen','address_records','fallback','target'}), 'VIEW-FIELDS-MISSING'; mk=lambda sel, ch: V(instance='i', txt=None, txt_count=0, srv=None, srv_count=0, target='t', a=None, aaaa=(), address_records=('fe80::20',), addresses=frozenset(), chosen=ch, fallback=None, selection=sel); heads={s: p._non_resolving_lines(mk(s, None))[0] for s in ('refused', 'absent', 'no-target')}; print('non-resolving-heads=%r' % heads); assert len(set(heads.values())) == 3, 'NON-RESOLVING-STATES-NOT-DISTINCT'; stray=p._non_resolving_lines(mk('selected', None)); print('missing-chosen-lines=%r' % stray); assert stray and 'chosen' in stray[0], 'MISSING-CHOSEN-NOT-MARKED'; print('NON-RESOLVING-RENDERER-OK')"</automated>
    <fails_when>non-zero exit, any of the assertion messages VIEW-FIELDS-MISSING, NON-RESOLVING-STATES-NOT-DISTINCT or MISSING-CHOSEN-NOT-MARKED in the traceback, or the absence of the final line NON-RESOLVING-RENDERER-OK. This drives the shipped renderer over four hand-built views rather than restating its contract in prose: it is the gate that can fail if the refused and absent renderings are ever collapsed back into one message, which is R1 itself, and if the null-chosen state is left unmarked</fails_when>
    <automated>uv run --frozen pyright</automated>
    <fails_when>non-zero exit, or any output line containing "- error:"; the probe sits inside pyright's explicit include list at pyproject.toml:122-131, so a stale dict subscript, a wrong field type, or a chosen address read without narrowing a local surfaces here</fails_when>
    <automated>uv run --frozen pytest .planning/scripts/tests/test_ipv6_thread_probe.py -q --no-cov -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or a passed count below 134, which is the 132 this file reported before the change plus this task's two new tests. The floor was 132 until pass 3, which OpenCode noted is the pre-change count and therefore weaker than intended; nothing slipped through it, because gate 1 already requires at least three passes inside `TestSyntheticCacheReporting`, but this is the only gate in the task that sees the whole file</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "class _InstanceView" .planning/scripts/ipv6_thread_probe.py` returns 1
    - `grep -c "list\[_InstanceView\]" .planning/scripts/ipv6_thread_probe.py` returns at least 1
    - `grep -c 'view\["' .planning/scripts/ipv6_thread_probe.py` returns 0
    - `grep -c 'view\["' .planning/scripts/tests/test_ipv6_thread_probe.py` returns 0
    - The probe source contains all five selection literals: selected, fallback, refused, absent, no-target
    - The probe source contains the string `cached address record(s) for the SRV target, none usable`
    - The probe source contains the string `no address record cached for the SRV target`
    - The probe source still contains the string `no SRV record and no fallback source`
    - `grep -c "_Selection = Literal\[" .planning/scripts/ipv6_thread_probe.py` returns 1, and no non-comment line declares `selection: str`
    - `grep -c "def _non_resolving_lines" .planning/scripts/ipv6_thread_probe.py` returns 1
    - Driven directly, `_non_resolving_lines()` returns three distinct first lines for the refused, absent and no-target states, and marks `chosen` for a view whose selection is resolved but whose chosen is null
    - `17-01-SUMMARY.md` exists and carries a `## Red phase` section holding the pytest summary line and the recorded pre-change failure for `test_instance_view_reports_refused_when_cached_addresses_yield_no_selection` and `test_instance_view_reports_absent_when_no_address_record_is_cached`, both run before `_instance_view()` was changed (AC-01, AC-02). Task 3's gate is what checks this; the criterion is stated here because this task is where the record is written
    - Exactly one `report_records()` test exists in this plan and it is Task 3's smoke test, so D-08's one-smoke-test rule and SPEC amendment A1 are both honoured
    - `uv run --frozen pyright` reports 0 errors
  </acceptance_criteria>
  <done>`_instance_view()` returns a typed structure carrying five explicit selection states, `_non_resolving_lines()` renders the refused and absent states with different text, the chosen address is narrowed through a local rather than an assertion, the existing consumer test asserts through attributes, and pyright is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Single owner-normalisation site, the unused-fallback statement, and the split summary counts</name>
  <files>.planning/scripts/ipv6_thread_probe.py, .planning/scripts/tests/test_ipv6_thread_probe.py</files>
  <read_first>
    - `.planning/scripts/ipv6_thread_probe.py` lines 88-95 (the import tuple from `lifx.network.discovery.mdns.discovery`), plus the `_instance_view()` and `report_records()` Task 1 rewrote
    - `src/lifx/network/discovery/mdns/discovery.py` lines 144-158 (`_normalise_dns_name`, idempotent since `f060a90`), 329-334 (`records_for`, which normalises its owner argument), 344-356 (`_addresses_in_order` and `addresses_for`, which reach `records_for` and therefore inherit that normalisation), 376-390 (`selected_address_for`, which normalises explicitly before its guards)
    - `.planning/phases/17-fleet-diagnostics-and-the-staleness-control/17-CONTEXT.md` decisions D-03 and D-04. **D-04's stated reason is wrong and this plan's Review-findings table records the correction; the decision itself stands.**
    - `.planning/phases/17-fleet-diagnostics-and-the-staleness-control/17-RESEARCH.md`, Common Pitfalls, Pitfall 4. **Pitfall 4 repeats the same wrong reason; read the action below instead.**
  </read_first>
  <behavior>
    - A cache whose SRV target is advertised with a trailing dot yields a view whose target field carries no trailing dot and is casefolded, and whose addresses and chosen are both populated and agree.
    - A view in the refused state whose instance also has a packet-source fallback reports that a fallback exists and is not used.
    - The summary block reports a cached-but-unusable count and an awaiting-records count derived from the per-instance views.
  </behavior>
  <action>
Per D-04, add `_normalise_dns_name` to the existing import tuple from
`lifx.network.discovery.mdns.discovery` at `.planning/scripts/ipv6_thread_probe.py:90-94`,
alongside `_create_device_from_record`, `_discover_lifx_services` and `_LifxRecordCache`. That
tuple is already this file's established pattern for consuming library-private discovery
symbols, so no new import style is introduced.

Replace the `.lower()` call on the SRV target at what was line 374 with
`_normalise_dns_name(srv.target)`, so the owner is normalised once where `target` is derived.

**What this change does and does not fix.** It does not repair a divergence between the two
cache lookups: there is none. `addresses_for()` reaches `_addresses_in_order()`, which calls
`records_for()`, and `records_for()` normalises its own argument at
`src/lifx/network/discovery/mdns/discovery.py:330`
(`self._records_by_owner.get(_normalise_dns_name(owner))`), exactly as `selected_address_for()`
does at line 376. `add_packet()` also normalises at ingest. Both probe lookups therefore already
key identically today, whatever spelling the probe hands them, and any plan text or research note
claiming a trailing-dot target reached "two different lookup keys" is describing a library that
does not exist. What the change actually fixes is narrower and still worth doing:

1. `_InstanceView.target` currently carries the raw `.lower()`ed SRV target, trailing dot and
   all, so the field the view publishes is not the key the cache computes from it. Every reader
   of `view.target` (the renderer, the tests, any later consumer that queries the cache with it)
   sees a near-miss of the real key.
2. `str.lower()` and `_normalise_dns_name()` are not the same operation. The helper casefolds
   and strips every trailing dot; `.lower()` does neither reliably for non-ASCII. Today that
   divergence is absorbed downstream by `records_for()`, which means the probe's correctness
   currently depends on every lookup it uses continuing to normalise on its behalf. Normalising
   once at derivation removes that dependency instead of relying on it.

Do not describe this change to a reader as closing a lookup-key seam. It is defence in depth on
a field the probe publishes, and the honest framing is what keeps the next reader from
mis-modelling the cache.

Per D-02 and AC-05, extend the refused and absent arms of `_non_resolving_lines()` with a
statement about an unused fallback. When `view.fallback` is not None in either of those two
states, append a line reading `packet-source address present and not used while an SRV record
exists`, using the same indented-label shape as the surrounding block. The library reaches its
packet-source fallback only when there is no SRV endpoint, so naming the fallback as present
and unused is the truthful form of AC-05's second clause, and the probe must not silently
discard it.

Per D-03, replace the summary block's pending-target dump with two counts derived from the
per-instance views, so the summary and the per-instance view describe the same set. Delete the
`result.cache.pending_targets()` call inside `report_records()`, the summary line labelled
`still-unresolved SRV targets` and the loop that lists those targets. In their place print two
lines carrying counts accumulated over the per-instance loop: the number of views in the
refused state, labelled `cached but no usable address`, and the number in the absent state,
labelled `awaiting address records`. Keep the three existing summary lines, the A record count,
the AAAA record count and the resolved count, unchanged. `pending_targets()` stays in use by
the sweep loop's follow-up query logic; only its use inside `report_records()` is removed.

Add two tests to `TestSyntheticCacheReporting`, named exactly
`test_instance_view_keys_addresses_and_selection_on_the_same_normalised_owner` and
`test_instance_view_names_an_unused_fallback_when_cached_addresses_are_refused`. The first
builds a cache whose SRV target value carries a trailing dot while its A record owner is the
bare form, then asserts the view's target equals `_normalise_dns_name(srv.target)` exactly
(no trailing dot, casefolded), that `view.addresses` is non-empty, and that `view.chosen`
equals the advertised address. Only the first of those three assertions fails against the
pre-change source; the other two pass either way, because `records_for()` normalises for the
probe today. State that in the test's docstring so nobody later reads a green run of the last
two assertions as proof the seam was real. Run the test before making the change, and record its
pre-change failure under `## Red phase` in `17-01-SUMMARY.md` alongside Task 1's, naming the test.
Expect an `AttributeError` on the `view.target` read rather than an assertion diff, for the same
reason Task 1's two tests do: the pre-change view is a dict. The second
reuses Task 1's refused fixture, passes a packet-source
address to `add_packet()` so the instance has a fallback recorded, and asserts `view.selection`
is the refused state, `view.chosen` is None, and `view.fallback` is that packet-source address.
  </action>
  <verify>
    <automated>uv run --frozen pytest ".planning/scripts/tests/test_ipv6_thread_probe.py::TestSyntheticCacheReporting" -q --no-cov -p no:cacheprovider</automated>
    <fails_when>non-zero exit, a "failed" or "error" count other than 0 in the summary line, or fewer than 5 tests reported as passed (the one pre-existing test, Task 1's two and this task's two)</fails_when>
    <automated>bash -c 'F=.planning/scripts/ipv6_thread_probe.py; grep -qF "_normalise_dns_name" "$F" || { echo "MISSING-NORMALISER"; exit 1; }; N=$(grep -vE "^[[:space:]]*#" "$F" | grep -c "srv.target.lower()" || true); echo "lower-call-sites=$N"; test "$N" -eq 0'</automated>
    <fails_when>non-zero exit, the line MISSING-NORMALISER in the output, or a printed lower-call-sites value other than 0</fails_when>
    <automated>bash -c 'F=.planning/scripts/ipv6_thread_probe.py; for s in "packet-source address present and not used while an SRV record exists" "cached but no usable address" "awaiting address records"; do grep -qF "$s" "$F" || { echo "MISSING: $s"; exit 1; }; done; N=$(grep -c "pending_targets" "$F" || true); echo "pending-target-references=$N"; test "$N" -eq 1'</automated>
    <fails_when>non-zero exit, any MISSING: line in the output, or a printed pending-target-references value other than 1 (the sweep loop's follow-up query logic being the sole survivor)</fails_when>
    <automated>uv run --frozen pyright</automated>
    <fails_when>non-zero exit, or any output line containing "- error:"</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/scripts/ipv6_thread_probe.py` imports `_normalise_dns_name` from `lifx.network.discovery.mdns.discovery`
    - No non-comment line in the probe calls `.lower()` on the SRV target
    - The probe source contains `packet-source address present and not used while an SRV record exists`
    - The probe source contains the summary labels `cached but no usable address` and `awaiting address records`
    - `grep -c "pending_targets" .planning/scripts/ipv6_thread_probe.py` returns 1
    - `test_instance_view_keys_addresses_and_selection_on_the_same_normalised_owner` passes, and `17-01-SUMMARY.md`'s `## Red phase` section records its pre-change failure on the `view.target` read specifically, not on the addresses or chosen assertions (AC-04). Task 3's gate checks the record's presence and the named test; the "on the target read specifically" part is the executor's own accurate transcription and is not machine-checked
    - The task's action text and the new test's docstring both state that both cache lookups already key identically through `records_for()`, and neither claims the change repairs a lookup-key divergence
    - `uv run --frozen pyright` reports 0 errors
  </acceptance_criteria>
  <done>The view's target field is the normalised owner rather than a lower-cased near-miss, the refused and absent renderings name an unused packet-source fallback, and the summary reports the two per-instance counts instead of the cache's separate pending set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove the unreachable link-local counter and warning, rename the zone-scope predicate, and make a malformed instance survivable</name>
  <files>.planning/scripts/ipv6_thread_probe.py, .planning/scripts/tests/test_ipv6_thread_probe.py</files>
  <read_first>
    - `.planning/scripts/ipv6_thread_probe.py` lines 172-195 (`classify_address` and the zone-scope predicate) and every call site of that predicate, at lines 500, 598 and 695
    - `.planning/scripts/tests/test_ipv6_thread_probe.py` lines 1078-1130 (`TestSelectTarget`, including `test_returns_not_found_for_a_zoneless_link_local_address`, which must stay green and unedited)
    - `.planning/phases/17-fleet-diagnostics-and-the-staleness-control/17-RESEARCH.md`, Common Pitfalls, Pitfall 3 (three call sites, not one) and Pitfall 5 (the missing-TXT-key case is already handled and must not be re-fixed)
    - `.planning/phases/17-fleet-diagnostics-and-the-staleness-control/17-CONTEXT.md` decisions D-06 and D-07
  </read_first>
  <behavior>
    - After the change, no line of the probe source contains either the removed counter name or the old predicate name.
    - _select_target() still returns a TargetNotFound whose reason contains "no zone ID" for a zoneless fe80::1 record, now through the renamed predicate.
    - A cache holding three TXT owners where the middle one has a TXT record whose parsed payload is not a TxtData yields three views in sorted order, the middle one carrying txt None, and report_records() over that cache returns without raising.
  </behavior>
  <action>
Delete the summary counter named `linklocal_chosen`: its declaration at what was line 428, its
increment at what was line 494, and its summary line at what was line 508, which is labelled
`chose a bare link-local addr`. Delete the WHY branch that sets the reason `ONLY link-local
AAAA records available`, which is the branch that incremented it. Delete the two-line warning at
what was lines 500-501, which calls the zone-scope predicate and prints that a link-local
address without a zone ID is not connectable. Both are unreachable: `_pick_address()`
(`src/lifx/network/discovery/mdns/discovery.py:215`) filters through
`_is_usable_mdns_address()` (line 210), which rejects an unscoped link-local address, and DNS
AAAA wire data carries no zone ID, so the chosen address in the selected state can never be an
unscoped link-local literal.

Rename the module-level predicate `is_reachable_choice`, declared at line 189, to
`_has_routable_scope`. Preserve its body and its behaviour exactly. Update the two surviving
call sites, in `stage_connect()` at what was line 598 and in `_select_target()` at what was line
695, to the new name. Do not delete the function and do not inline it: both surviving call sites
classify a resolved `_LifxServiceRecord.ip`, which can still be a packet-source fallback address
and is not proven unreachable by anything Phase 16 landed, and the `_select_target()` site is
directly covered by `TestSelectTarget::test_returns_not_found_for_a_zoneless_link_local_address`,
which must remain green and unedited. The rename is what satisfies AC-06's literal grep, which
demands the old name vanish from the file rather than the function vanish from the program.

Per D-06 and D-07, replace the bare TXT type assertion at what was line 436 with rendering that
marks the malformed field. `_instance_view()` already yields a None TXT value for a TXT owner
with no parseable payload, because it filters through `isinstance(record.parsed_data, TxtData)`,
so the assembling side already carries the marking and `report_records()` only renders it. When
`view.txt` is None, print a single line reading `TXT      : payload did not parse; serial,
product and firmware unavailable` in place of the three serial, product and firmware lines, then
continue into that instance's SRV, A, AAAA and CHOSEN lines exactly as for a well-formed one.
The malformed field is named once, the instance's remaining well-formed fields still print, and
the loop moves on to the next instance. Never skip a malformed instance: dropping the instance
the probe was run to diagnose is the inverse of what R3 asks for.

The other two bare assertions are already gone by the time this task runs, but they were disposed
of differently and the difference matters.

The AAAA list type check at what was line 437 is **eliminated by construction, and the elimination
is recorded**. `_InstanceView.aaaa` is typed `tuple[str, ...]` and is built inside
`_instance_view()` from the cache's own values, so no runtime value can be non-list: there is no
optional value and nothing to narrow. Do not reintroduce it as a runtime branch, and do not write a
diagnostic for it. Re-adding it would create exactly the dead code R2 deletes three lines away, and
`17-CONTEXT.md` D-05 records that converting an unreachable guard into a branch was already
rejected on that ground.

Because nothing at run time will ever demonstrate this disposition, SPEC amendment A6 requires it to
be **recorded** instead. Append a `## Eliminated by construction` section to
`.planning/phases/17-fleet-diagnostics-and-the-staleness-control/17-01-SUMMARY.md`, beside the
`## Red phase` section Task 1 created, naming the removed assertion, naming the field `aaaa` and its
`tuple[str, ...]` type, naming `_instance_view()` as the single construction site, and stating that
no runtime branch replaces it and why. Three lines of real content or more; the gate below reads
that section and fails when it is absent, when any of those three terms is missing from it, or when
it is a bare heading. This is the only place amended R3's third disposition can be evidenced, so a
section written after every gate has run is a record no gate can check, exactly as with the red
phase.

The chosen address type check at what was line 485 is **replaced, not removed**, and the earlier
draft of this plan was wrong to claim otherwise. It asserted that "there is nothing left to
narrow" because `selection` decides which arm reads `chosen`. A frozen dataclass carries no such
relationship: `selection` and `chosen` are independently typed fields, and pyright will not infer
a non-null `chosen` from a `selection` value, so that claim was a promise about a checker that
does not hold. Task 1 replaced it with two mechanisms that do hold: `report_records()` binds
`chosen = view.chosen` to a local and tests that local, which is narrowing pyright performs
unconditionally; and `_non_resolving_lines()` renders a `selected` or `fallback` view carrying a
null chosen as a marker line naming `chosen` as the malformed field. That is R3's **second**
condition as amended by A6, an instance with no usable chosen address rendering through R1's
non-resolving states rather than terminating the run. Verify it here rather than re-litigating it:
the test below drives that arm directly.

Do not touch the three TXT pair reads at what were lines 439-441, which already supply a `?`
default for a missing key. AC-09's missing-key case is satisfied by that existing code and needs
no change.

Add three tests. In `TestSyntheticCacheReporting`, add
`test_instance_view_marks_a_malformed_txt_and_keeps_the_remaining_instances`, which feeds
`cache.add_packet()` three TXT owners whose instance names sort into a known order, giving the
middle owner a TXT-type `DnsResourceRecord` whose `parsed_data` is not a `TxtData`, and asserts
that `_instance_view()` returns three views in sorted instance order, that the middle view's
`txt` is None, and that the third view's fields are exactly what the well-formed chain produces.

Add `test_non_resolving_lines_marks_a_missing_chosen_address`, which constructs an `_InstanceView`
directly with `selection` set to the selected state and `chosen` set to None, calls
`probe._non_resolving_lines(view)`, and asserts the returned lines are non-empty and that the
first one names `chosen`. This is a view-level test, not a `report_records()` test, so it does not
consume D-08's single smoke-test allowance, and it makes the arm that lets pyright narrow a real
tested path rather than an assertion of intent.

Add `test_report_records_completes_over_a_malformed_middle_instance`, the one smoke test D-08
allows, which calls `probe.report_records()` over a `SweepResult` built on the malformed-TXT cache
and asserts only that the call returned. It asserts no message text, because SPEC amendment A1
moved the state assertions to the view level.

Write that smoke test first and run it before removing the TXT assertion, then record its
pre-change failure (an `AssertionError` raised from inside `report_records()`) under the
`## Red phase` heading in `17-01-SUMMARY.md`, naming the test. R3's whole claim is that the run
used to end at the first malformed instance, and the recorded red output is the only place that
claim is evidenced. By the end of this task the `## Red phase` section names all four tests, which
is what the gate below reads.
  </action>
  <verify>
    <automated>bash -c 'F=.planning/scripts/ipv6_thread_probe.py; H=$(grep -n "linklocal_chosen\|is_reachable_choice" "$F" || true); test -z "$H" || { echo "DEAD-NAMES-STILL-PRESENT:"; printf "%s\n" "$H"; exit 1; }; echo AC-06-SATISFIED'</automated>
    <fails_when>non-zero exit, or the output block DEAD-NAMES-STILL-PRESENT: followed by matching lines. This gate is deliberately unfiltered and covers comments too, because AC-06 requires the names to vanish from the file entirely rather than only from executable lines</fails_when>
    <automated>bash -c 'F=.planning/scripts/ipv6_thread_probe.py; for s in "def _has_routable_scope(" "A record present; IPv4 preferred over IPv6" "fell back to the response packet" "preferred over link-local" "payload did not parse"; do grep -qF "$s" "$F" || { echo "MISSING: $s"; exit 1; }; done; N=$(grep -c "_has_routable_scope" "$F" || true); echo "routable-scope-references=$N"; test "$N" -eq 3'</automated>
    <fails_when>non-zero exit, any MISSING: line in the output, or a printed routable-scope-references value other than 3 (one definition plus the two surviving call sites)</fails_when>
    <automated>uv run --frozen pytest .planning/scripts/tests/test_ipv6_thread_probe.py -q --no-cov -p no:cacheprovider</automated>
    <fails_when>non-zero exit, a "failed" or "error" count other than 0 in the summary line, or a passed count below 139 (the pre-change 132 plus this plan's 7 new tests)</fails_when>
    <automated>uv run python -c "import pathlib,re,sys; p=pathlib.Path('.planning/phases/17-fleet-diagnostics-and-the-staleness-control/17-01-SUMMARY.md'); t=p.read_text(encoding='utf-8') if p.exists() else ''; sec=re.search(r'^## Red phase\b(.*?)(?=^## |\Z)', t, re.M|re.S); print('red-phase-section-found=%s' % bool(sec)); body=sec.group(1) if sec else ''; names=('test_instance_view_reports_refused_when_cached_addresses_yield_no_selection','test_instance_view_reports_absent_when_no_address_record_is_cached','test_instance_view_keys_addresses_and_selection_on_the_same_normalised_owner','test_report_records_completes_over_a_malformed_middle_instance'); missing=[n for n in names if n not in body]; print('red-phase-tests-missing=%r' % missing); n=len([l for l in body.splitlines() if l.strip()]); print('red-phase-body-lines=%d' % n); sys.exit(0 if (sec and not missing and n >= 8) else 1)"</automated>
    <fails_when>non-zero exit, a printed red-phase-section-found value of False, a non-empty red-phase-tests-missing list, or a red-phase-body-lines value below 8. This is the gate Codex's finding asked for: the success criteria promise a recorded pre-change failure for four named tests, and before this gate existed the verification section checked only code, tests, lint and protected paths, so the plan could pass with no record at all. The line-count floor stops a section holding four bare test names and no transcript</fails_when>
    <automated>uv run python -c "import pathlib,re,sys; p=pathlib.Path('.planning/phases/17-fleet-diagnostics-and-the-staleness-control/17-01-SUMMARY.md'); t=p.read_text(encoding='utf-8') if p.exists() else ''; sec=re.search(r'^## Eliminated by construction\b(.*?)(?=^## |\Z)', t, re.M|re.S); print('elimination-section-found=%s' % bool(sec)); body=sec.group(1) if sec else ''; terms=('aaaa', 'tuple[str, ...]', '_instance_view'); missing=[x for x in terms if x not in body]; print('elimination-terms-missing=%r' % missing); n=len([l for l in body.splitlines() if l.strip()]); print('elimination-body-lines=%d' % n); sys.exit(0 if (sec and not missing and n >= 3) else 1)"</automated>
    <fails_when>non-zero exit, a printed elimination-section-found value of False, a non-empty elimination-terms-missing list, or an elimination-body-lines value below 3. Amended R3 requires the non-list AAAA condition to be recorded as eliminated by construction rather than evidenced by a runtime branch, and a record no gate reads is a promise rather than a deliverable. The three required terms are the field, its type and its single construction site, which together are the elimination argument; a section that names none of them is prose about something else</fails_when>
    <automated>bash -c 'F=.planning/scripts/ipv6_thread_probe.py; N=$(grep -vE "^[[:space:]]*#" "$F" | grep -c "aaaa: tuple\[str, ...\]" || true); echo "typed-aaaa-declarations=$N"; test "$N" -eq 1'</automated>
    <fails_when>non-zero exit, or a printed typed-aaaa-declarations value other than 1. This is the construction half of the same claim: the record above asserts the AAAA condition cannot arise, and this asserts the type that makes that true is actually declared on the shipped dataclass. Without it the record could be accurate prose about a field typed `list[str] | None`</fails_when>
    <automated>uv run --frozen pyright</automated>
    <fails_when>non-zero exit, or any output line containing "- error:"</fails_when>
    <automated>uv run --frozen ruff check .</automated>
    <fails_when>non-zero exit, or any line matching the ruff diagnostic shape path:line:col: CODE in the output</fails_when>
    <automated>uv run --frozen ruff format --check .</automated>
    <fails_when>non-zero exit, or any line containing "would be reformatted"</fails_when>
    <automated>bash -c 'C=$(git diff --name-only main...HEAD -- .planning/milestones/v2.0-phases/ || true); W=$(git status --porcelain -- .planning/milestones/v2.0-phases/ || true); H=$(printf "%s\n%s\n" "$C" "$W" | grep -v "^[[:space:]]*$" || true); test -z "$H" || { echo "V2.0-EVIDENCE-MODIFIED:"; printf "%s\n" "$H"; exit 1; }; echo V2.0-EVIDENCE-UNTOUCHED'</automated>
    <fails_when>non-zero exit, or the output block V2.0-EVIDENCE-MODIFIED: followed by one or more paths (SPEC prohibition P6). The working-tree half is Codex's LOW finding: `main...HEAD` alone sees only committed history, so a staged or unstaged edit sitting in the tree at the moment before commit would pass a branch-diff-only check</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n 'linklocal_chosen\|is_reachable_choice' .planning/scripts/ipv6_thread_probe.py` returns nothing (AC-06)
    - `grep -c "_has_routable_scope" .planning/scripts/ipv6_thread_probe.py` returns 3
    - `TestSelectTarget::test_returns_not_found_for_a_zoneless_link_local_address` passes and its source is unchanged in the diff for this task
    - The probe source contains all three surviving WHY reasons: the IPv4-preferred text, the fallback-source text, and the routable-preferred text (AC-07)
    - The probe source contains `payload did not parse; serial, product and firmware unavailable`
    - `grep -c "assert isinstance" .planning/scripts/ipv6_thread_probe.py` returns 0; the file's one remaining `assert` is the `--uat-output` invariant narrowing at line 1271 inside `main_async()`, which this phase does not touch
    - The probe source still contains three TXT pair reads that supply a `?` default (AC-09, satisfied by existing code and not re-fixed)
    - `test_non_resolving_lines_marks_a_missing_chosen_address` passes, so the arm that lets `report_records()` narrow a local instead of asserting is a tested path rather than a claim. That arm is R3's second condition as amended by A6, an instance with no usable chosen address rendering rather than terminating the run
    - `17-01-SUMMARY.md`'s `## Red phase` section exists, names all four red-phase tests including `test_report_records_completes_over_a_malformed_middle_instance`, and holds at least eight non-blank lines of recorded output; the gate above checks exactly that and nothing more
    - `17-01-SUMMARY.md` carries an `## Eliminated by construction` section naming `aaaa`, `tuple[str, ...]` and `_instance_view`, holding at least three non-blank lines, and the probe declares exactly one `aaaa: tuple[str, ...]` field. Together these are amended R3's third disposition: the non-list AAAA condition is recorded as eliminated rather than evidenced by a branch, and the type that eliminates it is proven present. No diagnostic exists for that condition and none is required
    - `uv run --frozen pytest .planning/scripts/tests/test_ipv6_thread_probe.py -q` reports 0 failures
    - Neither `git diff --name-only main...HEAD -- .planning/milestones/v2.0-phases/` nor `git status --porcelain -- .planning/milestones/v2.0-phases/` produces output
  </acceptance_criteria>
  <done>The dead counter, its WHY branch, its summary line and the unreachable warning are gone; the zone-scope predicate is renamed with both live call sites updated and their test still green; a malformed TXT payload is marked once and the run completes over every instance; and the AAAA condition's elimination by construction is recorded where a gate reads it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mDNS wire data to probe cache | Untrusted DNS resource records from the local network reach `_LifxRecordCache` through `parse_dns_response()`; this plan changes how the probe *reports* on that cache, never how it parses |
| probe stdout to operator terminal | Raw device identifiers reach the operator's terminal by design; no artefact is committed from this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Denial of Service | `report_records()` over a partially assembled instance | medium | mitigate | Task 3 replaces the bare TXT assertion with a rendered marker, so one malformed instance no longer ends the whole diagnostic run. This is R3 itself, verified by `test_report_records_completes_over_a_malformed_middle_instance` |
| T-17-02 | Information Disclosure | Probe stdout carrying live serials, mDNS instance names, SRV hostnames and addresses | high | accept (this plan only) | No artefact from this plan is committed, and raw output to the operator's own terminal is the probe's purpose. `17-02-PLAN.md` adds the opt-in redaction that mitigates this for the one run whose output is committed, and `17-03-PLAN.md` gates that commit behind an operator staged-diff inspection |
| T-17-03 | Tampering | Accidental edit to Phase 14's committed Thread evidence while adding this phase's | low | mitigate | Task 3's final verify greps the branch diff for any path under `.planning/milestones/v2.0-phases/` and fails on a match (SPEC prohibition P6) |

**Package legitimacy:** not applicable. This plan installs no package in any ecosystem;
`17-RESEARCH.md`'s Package Legitimacy Audit records both scripts' PEP 723 headers as unchanged,
listing only `lifx-async`.

**ASVS level 1, block on high.** T-17-02 is `high` and is accepted only within this plan's
scope, where nothing is committed. It is mitigated in `17-02-PLAN.md` and gated in
`17-03-PLAN.md` and `17-04-PLAN.md` before any artefact reaches the repository.
</threat_model>

<verification>
- `uv run --frozen pytest .planning/scripts/tests/test_ipv6_thread_probe.py -q` reports 0 failures with at least 139 tests passed
- `uv run --frozen pytest -q` reports 0 failures, confirming the library suite is unaffected by a change confined to operator tooling
- `uv run --frozen pyright` reports 0 errors
- `uv run --frozen ruff check .` and `uv run --frozen ruff format --check .` both pass
- `grep -n 'linklocal_chosen\|is_reachable_choice' .planning/scripts/ipv6_thread_probe.py` returns nothing
- `17-01-SUMMARY.md` carries a `## Red phase` section naming all four red-phase tests, checked by Task 3's gate rather than asserted in prose
- `17-01-SUMMARY.md` carries an `## Eliminated by construction` section recording the AAAA disposition amended R3 requires to be recorded rather than branched, checked by its own gate alongside the typed-field gate
- No file under `.planning/milestones/v2.0-phases/` appears in `git diff --name-only main...HEAD` or in `git status --porcelain`
</verification>

<success_criteria>
- The probe reports the refused state and the absent state with two different messages, and the refused message carries the record count and each record's classification
- The view's `target` field carries the normalised owner the cache itself computes, and the plan text describing that change makes no claim about a lookup-key divergence, because there is none
- The dead counter, its WHY branch, its summary line, the unreachable warning and the old predicate name are all absent from the probe source, with both live call sites of the renamed predicate updated and their existing test unedited and green
- A malformed TXT payload marks one field, prints the rest of the instance, and lets the run complete; a chosen address missing from a resolved selection state is marked the same way rather than asserted, which is what lets pyright narrow through a local. Those are amended R3's two run-time conditions. Its third, the non-list AAAA value, is eliminated by construction and recorded in `17-01-SUMMARY.md` rather than re-armed as a branch, which is what SPEC amendment A6 requires and what makes both this plan and locked R3 honestly satisfiable at once
- Seven new tooling tests exist and pass, exactly one of which calls `report_records()`, and `17-01-SUMMARY.md` carries a `## Red phase` section naming the two R1 state tests, the normalised-owner test and the R3 smoke test with their recorded pre-change failures. Task 3's gate proves the section exists, names all four, and is not empty; it does not judge the transcript's accuracy, which is the executor's own record
</success_criteria>

<output>
`.planning/phases/17-fleet-diagnostics-and-the-staleness-control/17-01-SUMMARY.md` is created by
Task 1 holding only its `## Red phase` section, appended to by Tasks 2 and 3, and completed at the
end of the plan. Task 3 adds the `## Eliminated by construction` section. Task 3's gates read both
sections, so both must exist before Task 3's verification runs; a SUMMARY written only at the end is
one no gate can check.

Commit with Conventional Commit messages carrying no GSD phase or plan number as scope, signed
and signed off (`git commit -S -s`). Suggested scopes: `fix(probe)` for the reporting fixes,
`test(probe)` for the tooling tests.
</output>
