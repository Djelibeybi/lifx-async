---
phase: 13-merged-discovery
plan: 05
type: execute
wave: 4
depends_on: [13-04]
files_modified:
  - src/lifx/api.py
  - tests/test_api/test_api_discovery.py
autonomous: true
requirements: [FIND-05, FIND-09]
estimate:
  tokens: 12000
  raw_tokens: 24000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "[Edge R5 repetition] Every serial lookup starts a fresh two-source race and repeated calls retain no winner or failure state."
    - "[Edge R5 concurrency] UDP-first, mDNS-first, simultaneous matches, no-match survival, expected failure, cancellation, and loser reaping return only after both legs are closed."
    - "Invalid serial text preserves the existing public result: find_by_serial() returns None before starting either source rather than exposing Serial.from_string()'s ValueError."
    - "ASCII spaces are intentionally stripped by Serial.from_string() before validation, so a spaced valid serial matches normally; all other malformed inputs still return None before either source or any network work starts."
    - "When lookup joins a compatible active UDP sweep, it receives replay under the producer's original wire deadline and its own caller-origin overall wall deadline; tests expose the possibly shorter remaining window, and no construction or result crosses the caller deadline."
    - "All public serial-lookup tests remain mDNS-hermetic through the Plan 13-04 empty-source fixture unless they explicitly inject a synthetic mDNS candidate."
  prohibitions: []
  artifacts:
    - path: src/lifx/api.py
      provides: "Dual-source first-valid find_by_serial() race with pre-return loser teardown"
      exports: [find_by_serial]
    - path: tests/test_api/test_api_discovery.py
      provides: "Complete serial-race ordering, no-match, failure, repetition, concurrency, and cancellation matrix"
  key_links:
    - from: src/lifx/api.py
      to: src/lifx/network/discovery.py
      via: "UDP match pump subscribes to the process-wide raw/shared discovery path and normalises the requested serial once"
      pattern: "find_by_serial|discover_devices_shared|Serial.from_string"
    - from: src/lifx/api.py
      to: src/lifx/network/mdns/discovery.py
      via: "mDNS match pump consumes only directly verified candidates and shares the merged failure policy"
      pattern: "find_by_serial|_discover_verified_devices_mdns"
---

> **Path amendment (Plan 13-07):** Paths below describe this plan's original
> execution checkout. Current canonical locations and compatibility boundaries
> are mapped in [\`13-PATH-AMENDMENT.md\`](13-PATH-AMENDMENT.md).

<objective>
Make `find_by_serial()` race shared UDP and verified mDNS discovery, returning the first valid exact match only after the losing work is cancelled and reaped.

Purpose: FIND-05 covers both WiFi devices without mDNS advertisements and Thread devices without an IPv4 broadcast path, without exposing a selector or leaking tasks/sockets behind an early return.
Output: A dual-source serial race and exhaustive deterministic public-API lifecycle tests.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/13-merged-discovery/13-02-SUMMARY.md
@.planning/phases/13-merged-discovery/13-03-SUMMARY.md
@.planning/phases/13-merged-discovery/13-04-SUMMARY.md
@.planning/phases/13-merged-discovery/13-CONTEXT.md
@.planning/phases/13-merged-discovery/13-SPEC.md
@.planning/phases/13-merged-discovery/13-RESEARCH.md
@.planning/phases/13-merged-discovery/13-PATTERNS.md

<interfaces>
Reuse the `_DiscoveryEvent`, source pumps, `_cancel_and_reap()`, D-01 failure allowlist, and D-04 cancellation behaviour created by Plan 13-04.

Normalise valid caller input once with `Serial.from_string(serial).to_string()`, but catch its `ValueError` and return `None` before source creation to preserve the current invalid-input contract. A no-match `leg_done` from one source is not a lookup result while the other leg can still produce an exact verified match.
</interfaces>
</context>

## Artifacts this phase produces

- Private coroutine `_race_serial_sources()` in `src/lifx/api.py`.
- Modified public coroutine `find_by_serial()` retaining its existing signature and return type while changing source participation to UDP plus verified mDNS.
- Serial-race fake/barrier fixtures in `tests/test_api/test_api_discovery.py`; no new public selector, enum, or transport type.

## Review feedback dispositions

- Preserve invalid serial behaviour: malformed text returns `None` and performs no network work. Phase 13 does not turn it into a new public `ValueError` contract.
- Make existing `Serial.from_string()` ASCII-space stripping intentional and covered: valid spaced text normalises and races normally, while empty, separator-only, non-hex, wrong-length, and otherwise malformed text returns `None` with no network work.
- Preserve D-09/D-10 producer-origin sharing for lookup when a compatible UDP sweep is already active. A late lookup gets replay plus the remaining producer window under its own caller wall deadline; no construction/yield may cross that deadline.
- Reject a fresh-sweep remaining-window threshold because it conflicts with locked D-10 compatible-caller sharing unless the user changes CONTEXT.md.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Race exact serial matches without treating leg completion as a result</name>
  <files>src/lifx/api.py, tests/test_api/test_api_discovery.py</files>
  <read_first>
    - src/lifx/api.py
    - tests/test_api/test_api_discovery.py
    - src/lifx/protocol/models.py
    - src/lifx/network/discovery.py
    - src/lifx/network/mdns/discovery.py
    - .planning/phases/13-merged-discovery/13-04-SUMMARY.md
    - .planning/phases/13-merged-discovery/13-SPEC.md
  </read_first>
  <behavior>
    - UDP and verified mDNS begin concurrently under the existing caller timeout; either source can win first.
    - One source finishing empty or with an expected mDNS failure does not end the lookup while the other can match.
    - Simultaneous controlled matches use literal event completion order with no source preference.
    - Before constructing/returning the winner, the loser task is cancelled and awaited and both generators report closure.
    - Both no-match returns `None` only after both legs finish; input/source selectors are absent.
    - Invalid serial text returns `None` synchronously before either UDP subscription or mDNS source starts.
    - A valid serial containing ASCII spaces is intentionally normalised by `Serial.from_string()` and can match from either source; malformed inputs still return `None` with zero network activity.
    - Public tests use the default empty mDNS override and fail on any ambient `MdnsTransport`; source-winning cases opt in to a synthetic private record stream.
  </behavior>
  <action>Refactor `find_by_serial()` to normalise the requested serial once. Preserve and document `Serial.from_string()`'s intentional ASCII-space stripping, so a synthetically spaced valid serial reaches the exact-match race. Catch `ValueError` for all malformed forms and return `None` before creating a task, generator, subscription, socket, or connection. For valid input, capture one caller-origin overall wall deadline, create a shared-UDP match pump and verified-mDNS match pump, and consume the same typed event discipline as `discover()`. Enqueue only an exact fully valid match or leg terminal/fatal event. Continue after a no-match leg completion. When joining an active compatible UDP sweep, retain Plan 13-02's producer-origin wire deadline and replay contract while checking the caller deadline before/after construction and before return; document/test that the lookup may receive less than a fresh full timeout. Do not start a fresh sweep based on a remaining-window threshold because locked D-10 requires compatible active callers to share. On a winner, cancel the loser, await every task with the common cleanup helper, close both generators/subscription/temporary connection, and only then return the device if the caller deadline still permits it. Reuse D-01 expected mDNS isolation including candidate-local `LifxUnsupportedDeviceError`, D-03 fail-fast errors, and D-04 cancellation propagation. Keep public tests under the ambient-empty fixture unless they explicitly inject synthetic mDNS, preserve the exact signature/return type, and add no source selector per FIND-09.</action>
  <reversibility rating="costly">Dual-source lookup changes observable timing/source participation but is locked by FIND-05; the public signature remains stable.</reversibility>
  <acceptance_criteria>
    - UDP-first, mDNS-first, one-leg no-match, expected mDNS failure, both no-match, and deterministic simultaneous-match tests pass.
    - The winner is not returned until observed loser generator/connection/subscription closure completes.
    - Unexpected errors propagate after both legs are reaped; expected mDNS errors still permit UDP success.
    - Public signature tests show no selector and no changes to `find_by_ip()` or `find_by_label()`.
    - Invalid empty, separator-only, non-hex, and wrong-length serial strings return `None`, invoke neither source, and emit no failure diagnostic.
    - A valid synthetic serial with ASCII spaces is normalised and can match; malformed forms remain `None`/no-network.
    - Replay or construction crossing the caller-origin wall deadline yields no device and leaves both legs reaped.
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_api/test_api_discovery.py -q -k 'find_by_serial'</automated>
    <fails_when>non-zero exit, or pytest reports "0 passed" in the summary line</fails_when>
  </verify>
  <done>`find_by_serial()` returns the literal first valid exact match from either source and never leaves losing work behind.</done>
</task>

<task type="auto">
  <name>Task 2: Prove repeated, concurrent, cancelled, and early-return serial races are leak-free</name>
  <files>tests/test_api/test_api_discovery.py</files>
  <read_first>
    - tests/test_api/test_api_discovery.py
    - src/lifx/api.py
    - tests/test_network/test_discovery_coordinator.py
    - tests/test_network/test_mdns/test_liveness.py
    - .planning/phases/13-merged-discovery/13-CONTEXT.md
    - .planning/phases/13-merged-discovery/13-SPEC.md
  </read_first>
  <action>Add deterministic event/barrier tests for repeated calls after each winner/failure/no-match outcome, concurrent lookups for the same and different serials, caller cancellation while both legs are blocked, cancellation immediately after one match is queued, construction crossing the caller deadline, and construction failure after loser teardown. Include the intentional ASCII-space normalisation case and malformed/no-network matrix. Retain actual owned generator/connection/subscription observations so cleanup is asserted, not inferred. Avoid fixed sleeps, polling, hard-coded interfaces, or macOS-only loop assumptions. Ensure no unretrieved exception warning, pending task, coordinator thread, endpoint, or temporary connection survives the public return/cancellation boundary.</action>
  <acceptance_criteria>
    - Repeated calls create fresh mDNS work and either join only an active compatible UDP sweep or start a fresh completed one.
    - A lookup joining near producer expiry receives accepted replay only while both producer and caller deadlines permit it; construction cannot cross the caller deadline, and a post-terminal lookup starts a fresh full sweep.
    - Concurrent lookups cannot exchange matches or close each other's generators.
    - Cancellation propagates only after both source `finally` blocks and every connection/subscription close signal.
    - Construction failure occurs after loser teardown and leaves no warning or background work.
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_api/test_api_discovery.py -q -k 'find_by_serial or cancellation or concurrent'</automated>
    <fails_when>non-zero exit, or pytest reports "0 passed" in the summary line</fails_when>
  </verify>
  <done>The complete FIND-05 lifecycle matrix proves first-hit lookup remains isolated, fresh, and resource-clean under repeated and concurrent callers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller serial string to network match | Untrusted input is normalised/validated once before comparison with validated UDP or directly verified mDNS identities. |
| Winning event to public return | Background loser work must be fully reaped before ownership crosses back to the caller. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-17 | Spoofing | Serial lookup input/match | high | mitigate | Reject malformed serial text with the established `None` result before network work, then accept only exact normalised matches from validated UDP or directly correlated/liveness-verified mDNS candidates. |
| T-13-18 | Denial of Service | Losing lookup leg | high | mitigate | Cancel, await, and close both generators/subscriptions/connections before construction or return; cover every early-return/cancellation path. |
| T-13-19 | Tampering | Concurrent result routing | medium | mitigate | Use a per-call event queue and exact serial comparison; deterministic concurrent tests prove no cross-call result exchange. |
| T-13-20 | Repudiation | Expected versus unexpected failure | low | accept | Reuse the tested merged-discovery disposition and original-error propagation; no new diagnostic surface is added. |
</threat_model>

<verification>
- All focused serial-race tests pass.
- Full public API discovery tests pass with `ResourceWarning` promoted to error where the repository pattern supports it.
- Coordinator and liveness lifecycle suites remain green.
- Ruff and Pyright pass.
</verification>

<success_criteria>
- Either source can win `find_by_serial()` and one empty/expected-failed source cannot mask the other.
- No public source selector is introduced.
- Return, `None`, error, early close, and cancellation occur only after complete loser/source cleanup.
- Repeated and concurrent calls share no per-call winner/failure state.
</success_criteria>

<output>
Create `.planning/phases/13-merged-discovery/13-05-SUMMARY.md` when done.
</output>
