---
phase: 13-merged-discovery
plan: 07
type: execute
wave: 6
depends_on: [13-06]
files_modified:
  - .planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl
  - .planning/phases/13-merged-discovery/13-MEASUREMENT-SUMMARY.md
  - .planning/phases/13-merged-discovery/13-PATH-AMENDMENT.md
  - src/lifx/network/discovery/__init__.py
  - src/lifx/network/discovery/udp.py
  - src/lifx/network/discovery/coordinator.py
  - src/lifx/network/discovery/mdns/
  - src/lifx/network/mdns/
  - tests/test_discovery_observation.py
  - tests/test_network/test_discovery_imports.py
autonomous: true
requirements: [FIND-01, FIND-02, FIND-03, FIND-04, FIND-05, FIND-07, FIND-08, FIND-09, FIND-10]
estimate:
  tokens: 14000
  raw_tokens: 28000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "[Edge R6 boundary] Evidence includes at least six fleet pairs and at least one exact implementation-revision emulator CI pair, while observed timing has no pass/fail regression threshold."
    - "[Edge R6 adjacency] Every comparison joins baseline and merged arms with equal scenario, pair, environment, revision, quiescence, and confound metadata."
    - "[Edge R6 empty] Missing fleet access is reported explicitly and never substitutes emulator evidence for the required fleet comparison."
    - statement: "[Edge R6 ordering backstop] Raw measurement row order has no semantic meaning because validation keys observations by scenario, pair, arm, and round."
      verification: backstop
    - "[Edge R6 precision] Raw monotonic elapsed values and result counts remain integers and summaries do not round away source observations."
    - "[Edge R6 repetition] Each repeated arm/round is appended as a distinct immutable row and earlier rows are never overwritten."
    - "[Edge R6 concurrency] Baseline and merged arms execute sequentially so the compared observations do not interfere on the network."
    - "[Edge R7 boundary] FIND-08 eligibility includes firmware 3.70 and 3.99 and excludes 3.69 and 4.0 using integer components."
    - "[Edge R7 adjacency] Each available eligible WiFi device is compared once after identity normalisation and duplicate collapse."
    - "[Edge R7 empty] No eligible physical device produces a named non-gating population gap rather than substitute confirmation."
    - "[Edge R7 representation] TXT identity and broadcast serial comparison uses case-insensitive normalised 12-hex identities."
    - statement: "[Edge R7 ordering backstop] FIND-08 evidence row order has no semantic effect because each observation is keyed by a privacy-safe device alias."
      verification: backstop
    - "[Edge R7 precision] Firmware eligibility compares integer major/minor components and never treats a version as a decimal."
    - "The fleet collection reads the ignored mode-0600 handoff created at Plan 13-06's manual-only checkpoint, resolves its external alias map and exact quiescence/confound categories across executor sessions, and allows no mapping value or live identifier into a tracked file."
    - "The summary labels emulator evidence as synthetic-mDNS lower-bound and single-round, labels physical fleet evidence representative, and reports fleet baseline-count variance as advisory categorical observation with no pass/fail threshold."
    - "Final closure depends transitively on every implementation plan and directly on the exact-head CI evidence plus manual fleet preparation completed by Plan 13-06."
  prohibitions:
    - statement: "MUST NOT place live serials, MAC addresses, IP addresses, hostnames, account identifiers, private mappings, or raw discovery output in Phase 13 tests, logs, measurements, documentation, commits, or evidence."
      status: resolved
      verification: test
    - statement: "MUST NOT mark FIND-08 confirmed using emulator data, Thread firmware, ineligible WiFi firmware, or an inferred identity relationship."
      status: resolved
      verification: test
  artifacts:
    - path: .planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl
      provides: "Append-only direct-UDP baseline and merged-dual CI/fleet arm records with aliases, source contribution, confound qualification, and FIND-08 disposition"
    - path: .planning/phases/13-merged-discovery/13-MEASUREMENT-SUMMARY.md
      provides: "Deterministically regenerated observed timing/result/source delta and evidence qualification"
    - path: .planning/local/phase-13-merged-discovery/fleet-session.json
      provides: "Ignored mode-0600 input handoff from Plan 13-06; read-only here and never tracked"
  key_links:
    - from: .planning/phases/13-merged-discovery/13-06-SUMMARY.md
      to: .planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl
      via: "the final plan begins only after the signed exact-head CI pair and operator fleet-readiness checkpoint are complete"
      pattern: "13-06|fleet-session.json|alias_map_path|quiescence|confounds"
    - from: scripts/measure_merged_discovery.py
      to: .planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl
      via: "one executor-run paired fleet invocation validates each alias-only row before append and preserves the canonical byte prefix"
      pattern: "paired|fleet|rounds|alias-map|validate"
    - from: .planning/phases/13-merged-discovery/13-MEASUREMENT-SUMMARY.md
      to: .planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl
      via: "summary is regenerated only by --validate-only --summary from immutable rows"
      pattern: "MEASUREMENTS.jsonl|MEASUREMENT-SUMMARY"
---

<objective>
Collect the prepared physical-fleet evidence automatically, finalise the pseudonymised append-only record and derived report, and close all remaining Phase 13 privacy, cross-platform, source-coverage, and quality gates.

Purpose: FIND-07 requires at least six paired physical-fleet rounds, and FIND-08 permits only eligible physical WiFi evidence or the exact named non-gating population gap.
Output: Validated immutable fleet rows, an honest FIND-08 disposition, a deterministic measurement summary, and complete final verification after all implementation and CI evidence work.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/13-merged-discovery/13-01-SUMMARY.md
@.planning/phases/13-merged-discovery/13-02-SUMMARY.md
@.planning/phases/13-merged-discovery/13-03-SUMMARY.md
@.planning/phases/13-merged-discovery/13-04-SUMMARY.md
@.planning/phases/13-merged-discovery/13-05-SUMMARY.md
@.planning/phases/13-merged-discovery/13-06-SUMMARY.md
@.planning/phases/13-merged-discovery/13-ENTRY-GATE.md
@.planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl
@.planning/phases/13-merged-discovery/13-CONTEXT.md
@.planning/phases/13-merged-discovery/13-SPEC.md
@.planning/phases/13-merged-discovery/13-RESEARCH.md
@.planning/phases/13-merged-discovery/13-PATTERNS.md
@.planning/phases/13-merged-discovery/13-REVIEWS.md
@.agents/skills/spike-findings-lifx-async/SKILL.md
@.agents/skills/spike-findings-lifx-async/references/discovery.md

<interfaces>
Plan 13-06 leaves canonical JSONL containing the unchanged entry-gate baseline plus exactly one validated emulator pair from the signed implementation commit's successful pull-request CI run. Its final checkpoint creates only the ignored mode-0600 `.planning/local/phase-13-merged-discovery/fleet-session.json` with an external `alias_map_path`, one truthful quiescence category, categorical confounds, and readiness; it never stores mapping contents or live values.

`scripts/measure_merged_discovery.py` owns all collection. The D-14 `paired` mode executes direct baseline then merged sequentially in one invocation, wraps each exact timed call in the private observation scope, validates every row before append, and resolves raw identities to stable aliases in memory through the external mapping.

The canonical JSONL is source data. `13-MEASUREMENT-SUMMARY.md` is derived deterministically and cannot upgrade confounded, incomplete, ineligible, or missing evidence.
</interfaces>
</context>

## Implementation path amendment

The user-selected Plan 13-07 repair consolidates all discovery implementation
under `src/lifx/network/discovery/` before fleet collection. The authoritative
old-to-new mapping and the historical-artifact interpretation rule are recorded
in `13-PATH-AMENDMENT.md`. Observation models, value-suppressed representation,
`ContextVar`, and capture state move entirely to `tests/test_discovery_observation.py`;
installed source retains only inert private callable plumbing and never imports
the repository test helper. This amendment does not authorise fleet collection
or access to the private handoff during the repair.

## Artifacts this phase produces

- At least twelve appended fleet measurement rows forming six complete sequential baseline/merged pairs, with integer timing/count fields, alias-only source contribution, and truthful quiescence/confound metadata.
- Eligible firmware 3.70 through 3.99 WiFi identity dispositions when that physical population exists, otherwise the exact `no_eligible_find08_population` non-gating gap.
- `.planning/phases/13-merged-discovery/13-MEASUREMENT-SUMMARY.md`, generated solely from validated immutable JSONL.
- Summary qualification fields that call emulator evidence `synthetic_mdns_lower_bound` and `single_round`, physical fleet evidence `representative`, and fleet baseline-count variance `stable_baseline_counts` or `variable_baseline_counts` as advisory only.

<tasks>

<task type="auto">
  <name>Task 1: Collect and validate six privacy-safe paired fleet rounds</name>
  <precondition>Plan 13-06 completed its manual-only checkpoint: the ignored mode-0600 `.planning/local/phase-13-merged-discovery/fleet-session.json` exists, its `alias_map_path` resolves outside the repository, and it contains the truthful quiescence category, categorical confounds, and readiness.</precondition>
  <files>.planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl</files>
  <read_first>
    - .planning/phases/13-merged-discovery/13-06-SUMMARY.md
    - .planning/local/phase-13-merged-discovery/fleet-session.json
    - .planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl
    - scripts/measure_merged_discovery.py
    - tests/test_scripts/test_measure_merged_discovery.py
    - .agents/skills/spike-findings-lifx-async/references/discovery.md
    - .planning/phases/13-merged-discovery/13-CONTEXT.md
    - .planning/phases/13-merged-discovery/13-SPEC.md
    - AGENTS.md
  </read_first>
  <action>Assert that `.planning/local/phase-13-merged-discovery/fleet-session.json` is mode 0600, ignored, unstaged, ready, and contains only the allowed keys. Resolve its `alias_map_path` to a readable file outside the repository; use its quiescence category and categorical confounds without inferring a cleaner state. Record the canonical JSONL byte length and digest, then run one executor-owned `uv run --frozen scripts/measure_merged_discovery.py --mode paired --environment fleet --rounds 6` invocation with the external alias-map path, truthful `--quiescence`, repeatable `--confound` values, and canonical output path. Each pair must execute direct baseline then merged sequentially and be labelled representative fleet evidence. For every arm, use only the one exact timed API call's caller-scoped private observations to build per-alias source contribution, overlap, and winner disposition; do not start follow-up discovery sweeps, and prove unrelated concurrent discovery cannot enter the observation set. For FIND-08, associate raw mDNS TXT identity and accepted UDP broadcast identity transiently through the external mapping for the same alias, admit only WiFi firmware major 3 with integer minor 70 through 99, compare normalised identities in memory, and serialise only alias plus match/mismatch disposition. If no eligible physical device exists, append the exact named non-gating `no_eligible_find08_population` gap. Compute only the categorical advisory baseline-count variance label `stable_baseline_counts` or `variable_baseline_counts`; do not add a pass/fail variance threshold. Validate each row before append, rerun whole-file validation after collection, prove the previous byte prefix and digest are unchanged, and inspect the value-suppressed diff. Keep the handoff, mapping, live identifiers, addresses, hostnames, raw packets, raw discovery output, and exception text out of tracked files and reported output.</action>
  <acceptance_criteria>
    - At least six complete fleet pair IDs each contain exactly one direct-UDP baseline and one merged-dual arm in sequential order with comparable scenario metadata.
    - Every physical result is represented only by a stable operator alias and contributing source categories; no private mapping value or live identifier is tracked.
    - Dual-advertised aliases reconstruct both sources and either first-winner order from one timed call without an auxiliary discovery sweep.
    - Non-quiesced or unknown evidence is retained and labelled confounded rather than presented as clean.
    - Fleet evidence is labelled representative, and baseline-count variance is reported only as the advisory stable/variable category without affecting pass/fail.
    - FIND-08 contains only eligible WiFi evidence or the exact named non-gating population gap; emulator, Thread, ineligible firmware, and inferred identity never confirm it.
    - The pre-collection canonical JSONL prefix remains byte-identical and every new row validates before and after append.
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen scripts/measure_merged_discovery.py --validate-only --output .planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl</automated>
    <fails_when>validation reports fewer than six complete fleet pairs, an incomplete or incomparable pair, a malformed row, a privacy violation, an invalid FIND-08 disposition, or a changed canonical prefix</fails_when>
  </verify>
  <done>The immutable raw evidence contains the validated exact-head CI pair, at least six complete privacy-safe fleet pairs, and an honest eligible-firmware disposition with truthful confound qualification.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the summary and close privacy, cross-platform, and full quality gates</name>
  <files>.planning/phases/13-merged-discovery/13-MEASUREMENT-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-merged-discovery/13-MEASUREMENT-SUMMARY.md (create)
    - .planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl
    - scripts/measure_merged_discovery.py
    - tests/test_scripts/test_measure_merged_discovery.py
    - .github/workflows/ci.yml
    - .planning/phases/13-merged-discovery/13-06-SUMMARY.md
    - .planning/phases/13-merged-discovery/13-SPEC.md
    - AGENTS.md
  </read_first>
  <action>Run `uv run --frozen scripts/measure_merged_discovery.py --validate-only --summary` to regenerate the human-readable report solely from JSONL. Include raw/count deltas, time-to-first/completion observations, unique counts, source contribution/overlap, pair completeness, exact measured implementation revision, emulator CI qualification, fleet round count, quiescence/confound labels, advisory stable/variable baseline-count category, and FIND-08 match/mismatch/gap disposition. Label emulator evidence `synthetic_mdns_lower_bound` and `single_round`, never representative; label physical fleet evidence representative. Do not invent a performance ceiling, variance threshold, or upgrade confounded, missing, or ineligible evidence. State that the mDNS liveness cap of 16 is reasoned under D-07 rather than measured. Regenerate twice and require byte identity. Re-run the focused Phase 13 suites, the complete frozen suite, Ruff format/check, and Pyright using `uv run --frozen` exclusively. Inspect the successful exact implementation-revision pull-request run recorded by Plan 13-06 to prove Ubuntu, macOS, and Windows ordinary merge-ref jobs collected and passed the tests while the Ubuntu/Python 3.10 isolated measurement used the immutable PR-head SHA with its explicit timeout. Perform the repository's value-suppressed staged-diff privacy audit over source, tests, scripts, JSONL, summary, logs, and planning artefacts. Exercise the configured GSD gates: retain the existing `COVERAGE.md` determination that the detector's internal Python-callable signals are not an external vendor API integration; record `assumption_delta` as false; confirm the schema-path scan finds no ORM/schema migration; and verify every plan retains an ASVS L1 threat model with dispositions and executable high-severity mitigations. Fail closure for any live identifier candidate, private mapping, changed WiFi-derived constant, runtime dependency change, untracked task/socket/thread/endpoint, unresolved high-severity threat, missing requirement/decision/SPEC row, or accepted review finding absent from executable coverage.</action>
  <acceptance_criteria>
    - Regenerating the summary twice is byte-identical and every claim traces to immutable row keys.
    - The summary labels confounded evidence and reports observed deltas/advisory fleet variance without a threshold; emulator is synthetic-mDNS lower-bound/single-round and fleet is representative.
    - Focused and full local quality gates pass, and the exact implementation-revision Ubuntu/macOS/Windows jobs collected and passed the new tests with the named Ubuntu/Python 3.10 measurement.
    - Value-suppressed privacy audit reports zero live or unresolved identifier candidates; no private mapping or raw output is tracked.
    - Final diff contains no discovery/request/bandwidth/animation constant retuning and no runtime dependency change.
    - All nine requirements, all sixteen decisions, all thirty-six SPEC edge rows, all five prohibitions, accepted/rejected review dispositions, and every declared high-severity mitigation remain covered without widening scope.
    - API-coverage false-positive disposition, no assumption delta, no schema push, and ASVS L1 coverage are recorded and pass their configured gates.
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen scripts/measure_merged_discovery.py --validate-only --output .planning/phases/13-merged-discovery/13-MEASUREMENTS.jsonl --summary .planning/phases/13-merged-discovery/13-MEASUREMENT-SUMMARY.md &amp;&amp; uv run --frozen pytest -o addopts='' tests/test_api/test_api_discovery.py tests/test_network/test_discovery_coordinator.py tests/test_network/test_mdns/test_liveness.py tests/test_devices/test_state_light.py tests/test_scripts/test_measure_merged_discovery.py -q &amp;&amp; uv run --frozen pytest &amp;&amp; uv run --frozen ruff format --check src/lifx tests scripts/measure_merged_discovery.py &amp;&amp; uv run --frozen ruff check src/lifx tests scripts/measure_merged_discovery.py &amp;&amp; uv run --frozen pyright</automated>
    <fails_when>non-zero exit, either pytest invocation reports "0 passed", the summary cannot be regenerated byte-identically, formatting or lint changes are required, Pyright reports an error, or the final source/privacy/threat audit has a gap</fails_when>
  </verify>
  <done>The deterministic summary and raw evidence satisfy FIND-07/FIND-08 honestly, every Phase 13 requirement and locked decision remains covered, every high-severity/privacy gate is closed, and current cross-platform/full-suite evidence is green.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| ROADMAP | GOAL | Default concurrent merged discovery with explicit source APIs, shared UDP, dual-source serial lookup, and measured invariant preservation | 01-07 | COVERED | Entry gate precedes implementation; exact-head CI and fleet finalisation are sequential. |
| REQUIREMENTS | FIND-01 | Concurrent default UDP+mDNS first-valid serial merge | 03,04 | COVERED | Verified mDNS candidates only. |
| REQUIREMENTS | FIND-02 | Existing timeout, idle-reset, dedup, source/serial validation contract | 01,02,06,07 | COVERED | Pre-change, exact-head CI, and final gates. |
| REQUIREMENTS | FIND-03 | Expected mDNS failure degrades to UDP on Python 3.10 | 04 | COVERED | Explicit allowlist, no TaskGroup. |
| REQUIREMENTS | FIND-04 | Direct mDNS liveness before default yield | 03,04 | COVERED | Product-directed correlated probe. |
| REQUIREMENTS | FIND-05 | Dual-source serial race and loser cleanup | 05 | COVERED | Full lifecycle matrix. |
| REQUIREMENTS | FIND-07 | Paired fleet and emulator CI timing/result delta | 01,06,07 | COVERED | Direct-UDP versus merged-dual, exact-head CI pair, and at least six fleet pairs. |
| REQUIREMENTS | FIND-08 | Eligible firmware 3.70-3.99 WiFi identity evidence | 01,07 | COVERED | Non-gating named gap allowed only for absent eligible population. |
| REQUIREMENTS | FIND-09 | Public discover_udp/discover_mdns plus merged discover; no selectors | 01,04,05 | COVERED | Exact signatures and source participation. |
| REQUIREMENTS | FIND-10 | Process-wide compatible active UDP sharing and replay | 02,04 | COVERED | Raw accepted-response fan-out only. |
| CONTEXT | D-01 | Stage-aware expected failure allowlist | 03,04 | COVERED | Live sweep catches emit typed failures; candidate stages remain separate. |
| CONTEXT | D-02 | One privacy-safe DEBUG event per absorbed failure | 03,04 | COVERED | Live catch suppresses raw text on the injected path; merger logs stage/reason/type exactly once. |
| CONTEXT | D-03 | Unexpected errors fail fast after total cleanup | 04,05 | COVERED | Original error preserved. |
| CONTEXT | D-04 | Cancellation propagates after deterministic cleanup | 04,05 | COVERED | Never availability. |
| CONTEXT | D-05 | Product-directed GetColor/Echo liveness | 03 | COVERED | StateUnhandled rejects light. |
| CONTEXT | D-06 | Seed StateColor label/colour/power without stale getters | 03,04 | COVERED | Private snapshot adopted before merge eligibility. |
| CONTEXT | D-07 | Bounded liveness concurrency under original deadline | 03 | COVERED | Planner selects testable private cap 16 as a reasoned safety bound, not a measured optimum. |
| CONTEXT | D-08 | Existing request timeout/retries, no new schedule | 03 | COVERED | Remaining deadline caps requests. |
| CONTEXT | D-09 | Append-only active log and independent cursors | 02 | COVERED | Prefix then suffix. |
| CONTEXT | D-10 | Process-wide cross-loop compatible sharing | 02 | COVERED | Costly locked decision. |
| CONTEXT | D-11 | Lazy internal coordinator loop/thread | 02 | COVERED | Async transport remains unchanged. |
| CONTEXT | D-12 | Confirmed non-last and last detach semantics | 02 | COVERED | Last waits producer/endpoint close. |
| CONTEXT | D-13 | Append-only paired JSONL and derived summary | 01,06,07 | COVERED | Raw rows immutable; summary derived. |
| CONTEXT | D-14 | One script with baseline-only, merged-only, paired | 01,06,07 | COVERED | Paired is sequential. |
| CONTEXT | D-15 | Quiescence/confound categories retained and qualified | 06,07 | COVERED | Operator supplies categories; executor collects and labels. |
| CONTEXT | D-16 | External alias mapping and per-alias source contribution | 06,07 | COVERED | Raw mapping never tracked. |
| RESEARCH | ARCH-UDP | Share validated raw DiscoveryResponse, not DiscoveredDevice | 02 | COVERED | Subscriber settings applied after fan-out. |
| RESEARCH | ARCH-PY310 | Explicit task supervision and thread-safe loop bridges | 02,04,05 | COVERED | No Python 3.11 primitive. |
| RESEARCH | ARCH-MDNS | Invocation-local records, bounded current-call liveness, and typed live sweep-failure delivery | 03,04 | COVERED | No shared mDNS cache; conditional sink contract retained. |
| RESEARCH | ARCH-STATE | D-06 pre-state snapshot/adoption seam | 03 | COVERED | No partial LightState. |
| RESEARCH | ARCH-RACE | Serial lookup races matches, not leg completion | 05 | COVERED | Winner returned after loser cleanup. |
| RESEARCH | ARCH-EVIDENCE | Sequential paired append-only measurement | 01,06,07 | COVERED | Raw nanoseconds and counts. |
| RESEARCH | RESOLVED-STATE | Private immutable StateColor discovery snapshot | 03 | COVERED | No public cache semantics. |
| RESEARCH | RESOLVED-CAP | Deterministic liveness concurrency cap of 16 | 03,07 | COVERED | Cap/deadline tests; wording remains reasoned-not-measured. |
| RESEARCH | RESOLVED-FIND08 | Operator-gated eligible hardware or named non-gating gap | 06,07 | COVERED | Exact population-gap route; no substitute evidence. |
| RESEARCH | RESOLVED-EMULATOR-MDNS | Context-local synthetic mDNS source for exact emulator discover call | 01,06 | COVERED | No public selector or ambient MdnsTransport; real loopback liveness retained. |
| RESEARCH | RESOLVED-SWEEP-DIAGNOSTIC | Live sweep catches emit bounded typed failures | 03,04 | COVERED | Exactly-once merger DEBUG; standalone compatibility retained. |
| RESEARCH | SECURITY | Spoofing, amplification, abandonment, and leakage mitigations | 01-07 | COVERED | ASVS L1 threat registers block high severity. |
| RESEARCH | PACKAGE | No package installation; dependencies remain empty | 01-07 | COVERED | Package legitimacy audit is not applicable. |
| PATTERNS | API | aclosing ownership and exact public export lists | 01,04,05 | COVERED | Existing source-specific mDNS remains direct. |
| PATTERNS | UDP | `_discover_with_packet()` remains sole producer | 02 | COVERED | Validation/timing not duplicated. |
| PATTERNS | COORDINATOR | Thread-owned loop, caller-loop queues, idempotent detach | 02 | COVERED | Independently tested. |
| PATTERNS | LIVENESS | Shared classifier, request machinery, current GetColor coverage and synthetic exact Echo payload | 03 | COVERED | Unsupported classification is candidate-local. |
| PATTERNS | LIGHT | One StateColor decoder/adoption helper | 03 | COVERED | Fresh getters preserved. |
| PATTERNS | MEASUREMENT | Build/validate/append, strict schema, deterministic summary | 01,06,07 | COVERED | Existing probe/schema patterns reused. |
| PATTERNS | PORTABILITY | Events/barriers, real OS threads, current CI matrix | 02,05,06,07 | COVERED | No sleeps or macOS interface literals. |
| SPEC | R1 | Default merged discovery | 03,04 | COVERED | 5/5 edge rows lifted into truths. |
| SPEC | R2 | Pre-merge compatibility gate | 01 | COVERED | 5/5 edge rows lifted into truths. |
| SPEC | R3 | mDNS failure isolation | 04 | COVERED | 2/2 edge rows lifted into truths. |
| SPEC | R4 | mDNS liveness before yield | 03 | COVERED | 2/2 edge rows lifted into truths. |
| SPEC | R5 | Dual-source serial lookup | 05 | COVERED | 2/2 edge rows lifted into truths. |
| SPEC | R6 | Measured timing/result delta | 01,06,07 | COVERED | 7/7 edge rows lifted; dismissed ordering row is a backstop object. |
| SPEC | R7 | Eligible firmware identity evidence | 01,07 | COVERED | 6/6 edge rows lifted; dismissed ordering row is a backstop object. |
| SPEC | R8 | Explicit source-specific public APIs | 01,02,04,05 | COVERED | 2/2 edge rows lifted into truths. |
| SPEC | R9 | Active UDP single-flight | 02 | COVERED | 5/5 edge rows lifted into truths. |
| SPEC | EDGES | All 36 Edge Coverage rows | 01-07 | COVERED | 34 explicit string truths plus 2 flat backstop objects; zero unresolved assumptions. |
| SPEC | PROHIBITIONS | All 5 must-NOT rows | 01,02,04,07 | COVERED | Sibling `must_haves.prohibitions`, never truths. |
| REVIEWS | ACCEPTED | Caller sink crossing and exact-call isolation; caller wall deadline; synchronous empty-mDNS fixture plus ambient spies; server/device/later-add port repair; durable ignored alias handoff; evidence labels/variance; frozen uv and CI timeout; intentional serial-space parsing; capacity estimates; deterministic guarded lifecycle tests; source-changing exact-head CI; unparsable firmware; reasoned cap wording | 01-07 | COVERED | Every current actionable finding is executable in a task, acceptance criterion, verify gate, or final audit. |
| REVIEWS | REJECTED | TaskGroup claim, `_resolve_targets()` claim, same-loop redesign, invented sweep-timeout diagnostic, unsigned-commit claim, fresh-sweep remaining-window threshold, and ambient ContextVar crossing claim | 02-06 | COVERED | Rejections retain evidence, full signing fingerprint, and D-10/explicit-sink rationale without altering locked scope. |
| DETECTOR | API-COVERAGE | Internal Python callable signals are not an external vendor API/SDK/service integration | 01,07 | COVERED | Existing `COVERAGE.md` records the reasoned no-external-integration result and Task 2 seals the configured gate. |
| DETECTOR | ASSUMPTION-DELTA | No unresolved assumption delta detected | 07 | COVERED | Detector result is false; final audit rechecks rather than inventing work. |
| DETECTOR | SCHEMA-PUSH | No ORM/schema/migration path is changed | 07 | COVERED | Schema-path scan is empty; no schema push task is warranted. |
| DETECTOR | SECURITY | Security enforcement at OWASP ASVS L1 | 01-07 | COVERED | Every plan retains a STRIDE register with severity, disposition, and executable mitigation. |

### Explicit scope exclusions (not gaps)

- Completed/negative/TTL caches, shared mDNS state, source-selector parameters, an mDNS-specific `find_by_label()` path, Home Assistant changes, tuning changes, Thread commissioning/border-router management, IPv6 multicast mDNS/rejoin, Phase 14 device/performance evidence, runtime dependencies, transport threading, and Python 3.11-only primitives are excluded by the locked SPEC and are absent from every task. `find_by_label()` retains its direct targeted GetLabel sweep and remains explicitly outside FIND-10 sharing per AC9.
- Phase 11/12 hardened behaviour is consumed, not reopened; fixes are limited to regressions directly caused or exposed by Phase 13 execution.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Physical fleet/private mapping to tracked evidence | Only stable aliases, categorical sources/confounds, and aggregate timings/counts may cross into the repository. |
| Ignored readiness handoff to executor collection | The local file may contain a private external path and must stay mode 0600, ignored, untracked, and absent from reports. |
| Immutable JSONL to derived summary | Generated claims must trace to complete validated pairs and may not upgrade confounded, absent, or ineligible evidence. |
| Final repository diff to published Phase 13 evidence | Live identifiers, raw output, private mapping data, tuning changes, and unresolved high-severity threats must be rejected before closure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-21 | Information Disclosure | Fleet evidence | high | mitigate | Mode-0600 ignored handoff, external alias map, exact-call observation scoping, validate-before-append, forbidden-key/value scan, value-suppressed staged audit, and no raw logs/output in repository. |
| T-13-23 | Repudiation | Confounded comparison | medium | mitigate | Require the operator-supplied quiescence enum and categorical confounds per row; validator and summary visibly qualify non-clean pairs. |
| T-13-24 | Spoofing | FIND-08 confirmation | medium | mitigate | Exact integer firmware eligibility and normalised physical WiFi identity comparison; absent population records the named gap rather than confirmation. |
| T-13-27 | Tampering | Derived measurement report | high | mitigate | Regenerate solely from validated append-only JSONL, require byte-identical repeated generation, and trace every claim to immutable row keys. |
</threat_model>

<verification>
- At least six comparable fleet pairs and the exact implementation-revision emulator CI pair validate in canonical JSONL.
- `13-MEASUREMENT-SUMMARY.md` regenerates byte-identically solely from immutable rows.
- The complete frozen pytest suite, focused Phase 13 suites, Ruff format/lint, and Pyright pass.
- The Plan 13-06 exact-head pull-request run proves ordinary Ubuntu/macOS/Windows merge-ref test coverage and the isolated Ubuntu/Python 3.10 measurement boundary.
- Value-suppressed privacy/diff inspection finds no raw identifiers, mapping values, raw output, constant retuning, dependency change, or unclosed high-severity threat.
- The source audit covers all nine requirements, sixteen D-NN decisions, thirty-six SPEC edge rows, five prohibitions, research constraints, pattern assignments, and review dispositions.
</verification>

<success_criteria>
- Timing/result/source deltas are measured, raw, paired, repeatable, and honestly qualified rather than converted into a threshold.
- FIND-08 is closed only by eligible physical WiFi evidence or recorded as the allowed named non-gating population gap.
- All thirty-six SPEC edge rows and all five prohibitions have exactly one lifted must-have entry across the plan set.
- All ROADMAP, requirement, decision, research, pattern, SPEC, review, security, privacy, and cross-platform sources audit as covered with no silent widening.
</success_criteria>

<output>
Create `.planning/phases/13-merged-discovery/13-07-SUMMARY.md` when done.
</output>
