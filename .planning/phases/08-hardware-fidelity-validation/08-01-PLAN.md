---
phase: 08-hardware-fidelity-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py
  - .planning/phases/08-hardware-fidelity-validation/tests/conftest.py
  - .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py
autonomous: true
requirements: [FIDELITY-02]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "2026-08-16 final supersession: explicit `--attest-role` claims the configured target and role because the Morph configuration hierarchy cannot prove selected label, parent group or count. Preflight and production app work independently require a fresh in-panel Morph configuration proof, then may only tap the exact enabled theme-button entry, category, exact theme and Save; the private run-role binding digest plus exact LAN identity bridge the operator claim without public identifiers."
    - "Every app cycle starts from home, selects exactly one tile whose semantic label equals the private app label already bound to the live LAN target, then either semantically closes the still-open selected-target selector once or proves it auto-closed before verifying the configured group-control surface with raw selected count `1` and FX. It then opens and verifies MORPH, normalises emoji/case only for category-heading comparison, resolves one exact theme display name, taps the current Save control, and only then begins LAN readback; the transitions stay private and any absent or ambiguous semantic step fails closed after two lookup retries (D-01, D-02, D-04, D-18)."
    - "Theme traversal uses bounded fresh-hierarchy semantic scrolling that can reach Mondrian; neither a recorded grid position nor a coordinate-only control is authoritative (D-02, D-13)."
    - "Before mutation, preflight proves exactly one authorised tablet, the signed-in and unlocked LIFX app, both approved target labels, both fixed themes, app version, and a stable catalogue fingerprint (D-03)."
    - "A missing or ambiguous device label, device surface, MORPH control/surface, category, theme or Save control receives exactly two lookup retries, then records redacted local diagnostics, restores state, and stops (D-01, D-04)."
    - "The runner captures the prior Android keep-awake value, temporarily writes bitmask 7, and restores the prior value on every exit path (D-07)."
    - "The phase-local runner prints redacted progress and writes detailed events only beneath the ignored private root; it is not Phase 9 resync tooling (D-08, D-10)."
    - "CLI defaults and overrides are explicit and recorded, with exit 0=pass, 1=validation mismatch, 2=incomplete/preflight or human_needed, and 3=restoration failure (D-09)."
    - "Private target bindings are read from a mode-0600 local file; committed output uses only source-tile and non-tile-matrix roles (D-12, D-15). The 2026-08-16 correction supersedes the earlier reset-signature assumption."
    - "The runner hardcodes only cheerful then mondrian; category, display name, expected palette, and the record hash are derived from data/themes.jsonl (D-13, D-14)."
    - "Each target binding carries host, serial, app label, app group, indoor confirmation and a quiescence assertion; LAN and app labels must identify the same physical light before mutation (D-15, D-18)."
  artifacts:
    - ".planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py — phase-local Python runner and tracer contracts"
    - ".planning/phases/08-hardware-fidelity-validation/tests/conftest.py — phase-local import path bootstrap for the runner"
    - ".planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py — dependency-free tracer, semantic-selection, privacy and CLI tests"
    - ".gitignore — narrow ignore for .planning/local/phase-08-theme-fidelity/"
  key_links:
    - "data/themes.jsonl record['name'] -> load_theme_specs() -> ThemeSpec.display_name for cheerful and mondrian; no copied palette or metadata table exists"
    - "UIAutomator hierarchy -> exact private app-label device tile -> verified device surface -> verified MORPH surface -> derive_slug-normalised category heading + exact unique name + bounded semantic scroll + current Save match -> adb fixed-argv tap -> MatrixLight.get_effect()"
    - "TargetBinding host+serial+app_label -> Device.connect(ip, serial) -> live label/product validation before mutation"
    - "Captured RestorationSnapshot -> tracer finally block -> verified restore attempt before any terminal status"
---

<objective>
Build the production tracer and private execution boundary for Phase 8. The tracer proves one
`cheerful` source-Tile path across Android semantic selection, Save, LAN MORPH readback,
library MORPH, exact multiset comparison, checkpointing and restoration. It is deliberately
one path; Plan 08-02 expands the proven path to the locked two-theme/two-device schedule.

Purpose: expose UI, LAN, identity, privacy and restoration integration failures before the
complete hardware run is assembled.
Output: the phase-local runner skeleton, focused tests, and a narrow ignore rule for all
private target/checkpoint/diagnostic material.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-hardware-fidelity-validation/08-SPEC.md
@.planning/phases/08-hardware-fidelity-validation/08-CONTEXT.md
@.planning/phases/08-hardware-fidelity-validation/08-RESEARCH.md
@.planning/phases/08-hardware-fidelity-validation/08-PATTERNS.md
@.planning/phases/08-hardware-fidelity-validation/08-REVIEWS.md
@.agents/skills/spike-findings-lifx-async/SKILL.md
@.agents/skills/spike-findings-lifx-async/references/concurrency-and-keepalive.md
@CLAUDE.md

<interfaces>
- `Device.connect(ip: str, serial: str | None, ...)` returns the live typed device; entering its async context initialises state and guarantees close.
- `MatrixLight.get_effect() -> MatrixEffect` slices the reported palette to `palette_count`.
- `MatrixLight.set_effect(FirmwareEffect.MORPH, speed: float, duration: int, palette: list[HSBK])` is the authorised library MORPH path; it sends an unacknowledged packet, so only bounded `get_effect()` stability polling establishes completion.
- `ThemeLibrary.get(slug)` supplies the shipped `Theme`; `Theme.palette_equals()` defines duplicate-sensitive unordered uint16 equality.
- `.claude/theme-capture/tools/sweep_themes.py` supplies only a partial fixed-argv ADB/XML/swipe analogue. Its theme/Save taps are coordinates, it swallows subprocess failures, and it has no home/device/MORPH navigation; the semantic chain in this plan is new and must be established from current private hierarchies.
</interfaces>
</context>

## Artifacts this phase produces (plan 01 share)

- New file `.planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py`.
- New file `.planning/phases/08-hardware-fidelity-validation/tests/conftest.py` that inserts the phase directory into `sys.path` before test collection so the test module keeps all imports at the top.
- New file `.planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py`.
- New `.gitignore` entry `/.planning/local/phase-08-theme-fidelity/`.
- Constants `OFFICIAL_THEME_SLUGS = ("cheerful", "mondrian")`, `PRIVATE_ROOT`,
  `SEMANTIC_RETRIES = 2`, `ANDROID_KEEP_AWAKE_MASK = 7`, `EXIT_PASS = 0`,
  `EXIT_MISMATCH = 1`, `EXIT_INCOMPLETE = 2`, and `EXIT_RESTORATION_FAILURE = 3`.
- Dataclass `RunnerSettings` with fields `ui_wait_timeout`, `stability_timeout`,
  `poll_interval`, `max_theme_scrolls`, `targets_path`, and `private_root`.
- Dataclass `ThemeSpec` with fields `slug`, `display_name`, `category`, `expected_palette`,
  and `record_sha256`.
- Dataclass `TargetBinding` with private fields `role`, `host`, `serial`, `app_label`,
  `app_group`, `indoor_confirmed`, and `quiesced_confirmed`.
- Dataclasses `PaletteObservation` (`monotonic_offset`, `palette`, `is_reset`) and
  `CycleResult` (`device_role`, `theme_slug`, `source`, `cycle_index`, `observations`,
  `stable_palette`, `matches_expected`, `failure`).
- Functions `load_theme_specs()`, `theme_from_readback()`, `normalise_category_heading()`,
  `adb()`, `dump_ui_hierarchy()`, `run_hierarchy_reconnaissance()`,
  `find_semantic_control()`, `scroll_to_semantic_theme()`, `poll_stable_palette()`,
  `run_tracer_cycle()`, `build_parser()`, and `main()`.
- CLI flags `--targets`, `--private-root`, `--ui-wait-timeout`, `--stability-timeout`,
  `--poll-interval`, `--max-theme-scrolls`, and `--preflight-only`. There is no theme,
  host or discovery flag.
- Private target-file schema fields `schema_version`, `source-tile`, `non-tile-matrix`, plus
  each binding's `host`, `serial`, `app_label`, `app_group`,
  `indoor_confirmed`, and `quiesced_confirmed`. This file is runtime input, mode 0600,
  git-ignored, and never committed.

<planning_decisions>
- Mandatory discovery is Level 1 current-source verification: the repository contains Matrix MORPH,
  product registry, state and theme-equality seams, but the review verified that the capture script
  has only coordinate theme/Save taps and no home/device/MORPH semantic chain. Task 1 therefore
  performs private current-hierarchy reconnaissance before navigation; no dependency is added.
- The deterministic assumption-delta scan returned `detected: false`; no identity-model
  transition is recorded.
- No supported ORM schema path occurs in the ROADMAP, CONTEXT, RESEARCH or planned files;
  schema push is not applicable.
- **Superseded 2026-08-16:** The capture-era reset-palette observation is not a canonical
  reusable signature. The active contract requires neither an operator-provided value nor a
  guessed constant; two stable unexpected readbacks are retained as mismatch evidence.
</planning_decisions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace cheerful from app Save and library MORPH through source-Tile restoration</name>
  <files>.planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py, .planning/phases/08-hardware-fidelity-validation/tests/conftest.py, .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py</files>
  <read_first>
    - .planning/phases/08-hardware-fidelity-validation/08-CONTEXT.md (D-01, D-02, D-05, D-08, D-13, D-14, D-15 and D-18 are the tracer contract)
    - .planning/phases/08-hardware-fidelity-validation/08-SPEC.md (FIDELITY-02, real-hardware boundary, exact multiset constraint, static-pixel exclusion)
    - data/themes.jsonl (the only shipped metadata/palette source; derive cheerful and mondrian rather than copying them)
    - .claude/theme-capture/tools/sweep_themes.py (partial ADB/XML/swipe analogue only; its theme/Save taps are coordinates, subprocess wrapper ignores return codes/stderr, and it has no home/device/MORPH navigation)
    - src/lifx/devices/base.py (Device.connect typed factory and async-context state initialisation)
    - src/lifx/devices/matrix.py (MatrixLight.get_effect(), set_effect(), get_all_tile_colors() and set_matrix_colors())
    - src/lifx/theme/theme.py, src/lifx/theme/library.py and src/lifx/theme/slug.py (Theme.palette_equals(), shipped lookup and derive_slug category normalisation)
    - .planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py (create this file, then reread the new contract before GREEN implementation)
    - .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py (create RED tests here before production code)
  </read_first>
  <behavior>
    - "RED: all expected/readback and consecutive-read comparisons call Theme.palette_equals(); reordered equal HSBK tuples match, a duplicate-count change does not, and get_effect().palette=None normalises to an empty readback rather than a default-white Theme."
    - "RED: the app tracer records home -> exactly one source-tile tile labelled with the private approved app label -> verified current device surface -> MORPH control -> verified current MORPH surface -> Moods -> exact Cheerful -> current save_button -> first LAN read, in that order; these semantic transitions appear only in the private trace/checkpoint projection."
    - "RED: a current hierarchy containing '🎨 ART SERIES' matches shipped category 'Art Series' through derive_slug normalisation, while Mondrian remains an exact case-sensitive display-name match read from record['name']."
    - "RED: a fake picker with Mondrian beyond the first viewport is reached within the configured semantic-scroll bound using a fresh hierarchy after every swipe; exhaustion fails closed, and no recorded grid coordinate satisfies the lookup."
    - "RED: private reconnaissance captures home, exact device, verified device surface, MORPH and picker hierarchy shapes; zero/multiple candidates at any stage fail before the next tap."
    - "RED: adb() raises a redacted typed error on non-zero return code or any stderr, rather than treating command failure as a missing semantic control."
    - "RED: zero or multiple exact matches for the private approved app label trigger exactly two fresh-hierarchy lookup retries, then fail before any UI tap or device mutation; substring and adjacent-label matches cannot satisfy lookup."
    - "RED: one injected end-to-end tracer records the full app navigation and Save, two stable LAN reads, library MORPH, two stable LAN reads, both comparisons, and a verified restoration attempt in that order."
    - "RED: the tracer invokes MatrixLight.set_effect(FirmwareEffect.MORPH, palette=ThemeLibrary.get('cheerful').colors); no static canvas/pixel theme path can satisfy the fake-device assertion."
    - "RED: a mismatch remains in CycleResult and does not suppress restoration."
  </behavior>
  <reversibility rating="reversible">All production changes are phase-local internal tooling with no public package API or data migration.</reversibility>
  <action>
    Implement the production tracer per D-01, D-02, D-05, D-08, D-13, D-14, D-15 and
    D-18 after the RED tests exist. Keep imports at the top and support Python 3.10-3.14.
    Create the phase test `conftest.py` with the single phase-directory `sys.path` insertion needed
    before collection, so `test_uat_theme_fidelity.py` imports the runner normally with its imports
    at the top; do not mutate `sys.path` after imports inside the test module.

    Load exactly `cheerful` and `mondrian` from `data/themes.jsonl`, require disposition
    `lifx-app`, read the display value from source key `name` (there is no `display_name` source
    key), normalise colours through the repository HSBK representation, and freeze a canonical
    SHA-256 of each resolved record. `OFFICIAL_THEME_SLUGS` is the sole fixed theme list. For the
    live tracer execute only cheerful on role `source-tile`; the injected tracer suite must also
    exercise bounded traversal to off-screen Mondrian. First establish D-18's
    private identity binding through targeted `Device.connect(binding.host, binding.serial)`,
    requiring the live LAN label to equal `binding.app_label` and the live product to be
    Tile/product 55 before any mutation. Then perform D-01 without a force-stop: issue the
    declared `lifx:/home` VIEW deep link with fixed arguments, retry fresh dumps until Home proves
    the bound label or exact configured group card; reject any raw `Select lights` hierarchy even
    if it contains a bound label. Preflight proves both bindings from this one selector-free Home
    surface, then reconnoitres the source Tile path only. Where that source group must open its target selector,
    require only raw `Lights` as the structural zero-selection marker; a positive count fails closed
    before any selector or target tap. Then require exactly one device tile whose full semantic
    label equals `binding.app_label`; tap its freshly resolved centre; dump again and require the
    configured in-panel group heading and raw selector count exactly `1`. If exactly one raw
    `Select lights` label remains, tap only one unique clickable
    `ax_device_control_close_button` whose centre is inside and bounds horizontally overlap that
    detail panel and lie above the selector; then freshly require selector absence and exactly one
    FX control on the same group-control surface. If it already auto-closed, require that direct
    selector-absent count-`1`/FX proof. Any missing, duplicate or out-of-region close control fails
    without a second target tap, generic close control or Android Back; then tap FX, require MORPH; dump again and verify the
    current surface is MORPH before resolving Moods, exactly
    Cheerful, and the current `save_button`; tap Save; only then begin LAN stability polling.
    Resolve every tap from the immediately current hierarchy. After an initial absent or ambiguous
    result for any device label, surface, MORPH control, category/theme cell or Save control, make
    exactly two fresh-hierarchy lookup retries, then fail closed with exit 2 and restoration. A
    device-label ambiguity must fail before any UI tap or effect/device mutation.

    The existing capture script is not a semantic-selector implementation: its `THEME_BUTTON` and
    `SAVE_BUTTON` are coordinates and it assumes the app is already parked on MORPH. Implement the
    complete home -> exact approved device -> verified selected group-control surface -> FX ->
    verified MORPH surface -> category -> theme -> Save chain as new current-hierarchy automation. Before the
    first authoritative navigation, run private hierarchy reconnaissance for the home, device,
    MORPH and picker surfaces; require one exact candidate at each transition and store hierarchy
    shapes only below the private run directory. `adb()` must use fixed argv/no shell, enforce a
    timeout, and raise a redacted typed failure on a non-zero return code or stderr from any
    command other than a successful `pull` transfer, whose routine progress is allowed. Such
    failures do not consume semantic-control retries.

    Category headings are the one normalised selector: apply the existing `derive_slug()` rule to
    both the shipped category and current heading so `Art Series` matches `🎨 ART SERIES` and case
    differences. Display-name selection remains exact and unique; the tests must pin that both
    fixed records' `name` values are emoji-free. Implement `scroll_to_semantic_theme()` with a
    recorded default bound of 20 swipes, a fresh dump after every swipe, end-of-list detection and
    no blind prefetch/grid-position authority. Its fake-hierarchy tracer must reach picker entry 16
    Mondrian and prove bound exhaustion stops incomplete without a theme or Save tap.

    Append the successful home, device selection, device-surface verification, MORPH selection,
    MORPH-surface verification, category, exact theme and Save transitions to the restrictive
    private trace/checkpoint only. Public `CycleResult`, official evidence and console output may
    contain only the public role and theme slug, never the private app label or raw hierarchy.

    For both the app and library sources, retain every poll observation and declare stability
    only after two consecutive palettes compare equal through `Theme.palette_equals()`. This named
    method is the only equality seam: do not add `palette_key()`, `palettes_match()`, a `Counter`
    helper or set/frozenset comparison. `theme_from_readback()` may only adapt the observed
    `list[HSBK] | None` to an identity-less Theme; explicitly normalise `None` to an empty colour
    list without triggering `Theme([])`'s default-white constructor behaviour. Timeout fails
    the cycle and stable unexpected palettes remain mismatch evidence. The library source must call
    `MatrixLight.set_effect(FirmwareEffect.MORPH, palette=theme.colors)`; the static canvas/pixel
    application route is outside this phase. `set_effect()` uses unacknowledged `send_packet()`, so
    the first read may still expose the prior effect. The bounded two-consecutive `get_effect()`
    rule is the sole completion authority for library and app paths: retain transitional reads,
    never insert a fixed completion sleep, and never fail from one transitional read. Compare the
    stable readback by calling the frozen shipped Theme's `palette_equals()`.

    Introduce narrow injected ADB/device/checkpoint adapters only so the exact production state
    machine can run under tests. The injected end-to-end test must traverse real orchestration,
    not call pure helpers in isolation. Always close the device and attempt tracer snapshot
    restoration in `finally`; retain a mismatch as data rather than ending before cleanup.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache uv run --frozen pytest -o addopts='' .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py -q -k "tracer or palette or semantic or scroll or category or adb"</automated>
  </verify>
  <done>The injected production tracer proves the unique private-label device selection and verified MORPH navigation precede the cheerful theme and Save before LAN readback, then completes the library path, preserves duplicate counts, and verifies restoration after match, mismatch and semantic failure.</done>
  <acceptance_criteria>
    - `uat_theme_fidelity.py` contains `OFFICIAL_THEME_SLUGS = ("cheerful", "mondrian")`, `FirmwareEffect.MORPH`, `poll_stable_palette`, and `run_tracer_cycle`.
    - The focused command exits 0 with at least one selected tracer test, one reorder-equality test, one duplicate-count mismatch test and one restoration-after-mismatch test reported by pytest.
    - A spy around `Theme.palette_equals()` proves it is the only equality seam for expected/readback and stability comparisons; `get_effect().palette=None` becomes an empty observation and never a default-white Theme.
    - Fake current hierarchies prove `Art Series` matches `🎨 ART SERIES`, exact `Mondrian` is reached beyond the first viewport within the bound, and wrong-case/duplicate theme names or exhausted scroll bounds fail before Save.
    - ADB fixtures prove non-zero return code and any stderr raise a redacted command failure without being misclassified as a semantic miss.
    - The end-to-end fake asserts home, exact unique source-tile app-label selection, semantic device-surface verification, MORPH selection, MORPH-surface verification, Moods/Cheerful and current Save all precede the first LAN read, followed by two stable reads, library MORPH, two stable reads and restoration in deterministic order.
    - A hierarchy fixture containing two device tiles with the exact approved app label performs the two locked lookup retries, emits private diagnostics, and after the required read-only LAN identity check asserts zero tap, Save, LAN-set/effect-set or palette-readback calls before returning incomplete.
    - Private-transition projection tests assert the device label and raw semantic transition payload cannot enter public CycleResult/evidence fields or redacted console output.
    - No public library source or `data/themes.jsonl` is modified.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce fail-closed preflight, Android lifecycle and private diagnostics</name>
  <files>.gitignore, .planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py, .planning/phases/08-hardware-fidelity-validation/tests/conftest.py, .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py</files>
  <read_first>
    - .gitignore (Local storage section; add one narrow Phase 8 rule)
    - .planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py (Task 1 tracer contracts)
    - .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py (Task 1 tests; extend rather than replace)
    - .planning/phases/08-hardware-fidelity-validation/08-CONTEXT.md (D-03, D-04, D-07 through D-10, D-12, D-15 and D-18)
    - .claude/theme-capture/README.md (Save timing and device-only capture caveats)
    - .claude/theme-capture/tools/sweep_themes.py (subprocess safety annotations and hierarchy diagnostics)
  </read_first>
  <behavior>
    - "A target file with extra keys or roles, missing indoor/quiesced confirmation, invalid serial or permissive mode fails before ADB or LAN mutation."
    - "Exactly one adb device in state device passes; zero, two or unauthorised/offline entries exit incomplete."
    - "App version, unlocked state, both labels, both fixed semantic themes and catalogue fingerprint are required before mutation; a second fingerprint differs => incomplete."
    - "A semantic miss retries exactly twice, emits local screenshot/hierarchy diagnostics with mode 0600, restores keep-awake, and exits 2."
    - "Logs and exceptions expose only source-tile/non-tile-matrix, product metadata and opaque run ID, never host, serial, app label, adb serial or raw hierarchy text."
  </behavior>
  <reversibility rating="reversible">The ignore rule and phase-local validation contracts can be changed without touching shipped APIs or official evidence.</reversibility>
  <action>
    Implement D-03, D-04, D-07 through D-10, D-12, D-15 and D-18. Add the exact ignore
    rule `/.planning/local/phase-08-theme-fidelity/`; do not ignore the phase directory or
    official UAT paths. Require the private root to be mode 0700 and target/checkpoint/trace/
    diagnostic files to be mode 0600. The private target JSON accepts exactly the two role keys
    and the fields listed in the artefact section.
    It must reject Exterior or `indoor_confirmed != true`, missing quiescence confirmation,
    unexpected roles, malformed serials and unexpected keys before calling ADB or LAN code.

    Build `--preflight-only` and the explicit timeout flags with defaults 10.0 seconds for UI
    waits, 15.0 seconds for stability and 0.5 seconds for polling. Record every effective value
    in private provenance. Preflight must require exactly one authorised `adb devices` entry,
    `com.lifx.lifx` installed, an unlocked screen, app home visible and signed-in as proven by both
    approved labels, both fixed category/name pairs resolvable through normalised-category/
    exact-name bounded traversal, and a catalogue fingerprint that is
    unchanged at the end of preflight. Record only app version/version code and the catalogue digest;
    do not read, store or print an account identity or credential.

    Read `settings get global stay_on_while_plugged_in`, set it to bitmask 7 only after capturing
    the prior value, and restore the exact prior value in the outermost `finally`. Run fixed-argv
    subprocesses with no shell. Retry a missing expected theme or `save_button` exactly twice. On
    exhaustion, write screenshot and hierarchy only to the run's private diagnostics directory,
    print the opaque run ID/private directory without contents, attempt all applicable restoration,
    and exit 2. Delete raw diagnostics and trace only after a complete pass; retain them on any
    failure. All console progress and exception text must go through a redacting role-based formatter.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache uv run --frozen pytest -o addopts='' .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py -q -k "preflight or target or adb or awake or diagnostic or redact"</automated>
  </verify>
  <done>Preflight is non-mutating and fail closed; private artefacts are narrowly ignored and restrictive; semantic misses, ADB ambiguity and provenance drift cannot leak identifiers or leave Android keep-awake changed.</done>
  <acceptance_criteria>
    - `.gitignore` contains exactly one `/.planning/local/phase-08-theme-fidelity/` entry and continues to track `.planning/phases/08-hardware-fidelity-validation/`.
    - The focused command exits 0 with positive selected-test count and covers permissive mode, extra target, Exterior, ADB 0/1/2-device states, retry exhaustion, catalogue drift, redaction and keep-awake restoration.
    - `--help` lists the seven plan-01 flags and no theme, host, serial or fleet-discovery option.
    - Exit-status tests assert 0, 1, 2 and 3 are distinct constants with the meanings locked above.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Private target/checkpoint -> runner | LAN addresses, serials and app labels cross from local operator data into code. |
| Android/UIAutomator -> runner | App hierarchy, package state and semantic labels are untrusted mutable UI input. |
| Runner -> selected LAN devices | The runner can change real lights and must address exactly two approved indoor targets. |
| Runner -> local filesystem/console | Raw hierarchy, screenshots, events and exceptions can disclose household or tablet identifiers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information disclosure | target/checkpoint/diagnostics | high | mitigate | Narrow git-ignore, mode 0700/0600 enforcement, public-role logging and tests that inject host/serial/label/adb identifiers. |
| T-08-02 | Spoofing | semantic device/theme selection | high | mitigate | Exact category+display-name and app-label matches, one authorised tablet, app/catalogue fingerprint and two bounded retries per D-02..D-04. |
| T-08-03 | Tampering | LAN target selection | high | mitigate | Exactly two private bindings, live host+serial+label/product agreement, indoor/quiesced assertions and no discovery path per D-15/D-18. |
| T-08-04 | Tampering | Android global setting | medium | mitigate | Capture prior keep-awake value and restore it from the outermost finally on every exit, covered by injected failure tests. |
| T-08-05 | Elevation of privilege | adb subprocess | medium | mitigate | Fixed argv, no shell, fixed package/settings commands, bounded timeout and no credential/session export. |
| T-08-06 | Repudiation | tracer event ordering | medium | mitigate | Append-only private events with monotonic offsets and explicit app/save/read/library/restore states. |
</threat_model>

<verification>
- Focused phase tests pass with positive selected-test counts.
- `UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache uv run --frozen ruff check .planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py .planning/phases/08-hardware-fidelity-validation/tests/test_uat_theme_fidelity.py` exits 0.
- `UV_CACHE_DIR=/tmp/lifx-phase8-uv-cache uv run --frozen pyright .planning/phases/08-hardware-fidelity-validation/uat_theme_fidelity.py` exits 0.
- Unit/injected results demonstrate the execution contract only; they do not satisfy FIDELITY-02 hardware truth.
</verification>

<success_criteria>
- One production tracer path is executable through every app/LAN/private-state layer and tested end to end.
- Preflight and private-storage failures occur before mutation and cannot leak committed identifiers.
- The tracer uses MORPH effect configuration/readback, exact duplicate-sensitive HSBK equality and verified restoration.
</success_criteria>

<output>
Create `.planning/phases/08-hardware-fidelity-validation/08-01-SUMMARY.md` when done.
</output>
