---
phase: 14-thread-revalidation-and-docs
plan: 04
type: execute
wave: 4
depends_on: [14-03]
files_modified:
  - scripts/thread_revalidation.py
  - scripts/measurement_support.py
  - tests/test_scripts/test_thread_revalidation.py
autonomous: true
requirements: [THREAD-01, THREAD-02, THREAD-03, THREAD-04, THREAD-05]
estimate:
  tokens: 50000
  raw_tokens: 100000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "THREAD-02 boundary: the frozen 100-trial protocol retains the full acknowledgement RTT distribution around the current 200 ms first-retransmit floor."
    - "THREAD-02 adjacency: retransmissions remain attached to one logical request and duplicate replies never create extra trials."
    - "THREAD-02 empty: timeouts and absent acknowledgements are stored as outcomes with undefined latency rather than removed."
    - "THREAD-02 ordering: trial identity and chronological observations are retained while summary generation is deterministic."
    - "THREAD-02 precision: integer monotonic observations yield ordinary median, empirical nearest-rank p95 and observed maximum independently for completed logical latency and accepted-ACK RTT."
    - "THREAD-02 idempotency: reruns have distinct immutable session identities and revisions."
    - "THREAD-02 concurrency: no unlabelled concurrent load is introduced and environmental activity is recorded as a confounder."
    - "THREAD-03 boundary: the frozen 1, 2 and 5 FPS observation accepts every outcome, including zero successful sends and zero qualified rates."
    - "THREAD-03 adjacency: each selected alias and exact public class has separate animation records."
    - "THREAD-03 empty: zero sends, gates, failures, interruption and unavailable optional ACK information are retained without inventing a ceiling."
    - "THREAD-03 ordering: declared attempt and frame chronology remain auditable."
    - "THREAD-03 precision: integer offered, packets_sent, gated and failed counts remain transport-side observations only."
    - "THREAD-03 idempotency: reruns are separated by session, protocol version and revision."
    - "THREAD-03 concurrency: one selected alias runs synchronously at a time without a WiFi-derived query workload; unrelated activity is a confounder."
    - "THREAD-04 boundary: precondition, first per-leg absence, repeated both-absent confirmation, confirmed advertisement expiry and censorship are distinct outcomes."
    - "THREAD-04 precision: disconnect, first-absence and confirmed-expiry values use integer monotonic nanoseconds with a declared absolute 60-second cadence."
    - "THREAD-04 idempotency: every repeated staleness experiment receives a distinct session identity."
    - "THREAD-04 concurrency: only one selected device is disconnected and restored at a time."
    - "THREAD-05 inventory authority: before collection, init validates and freezes an operator-approved expected alias/class roster that is independent of observed discovery results, covers Light, MultiZoneLight, CeilingLight and two distinct MatrixLight aliases, and reserves named gaps exclusively for InfraredLight and HevLight."
    - "THREAD-05 completeness: every expected alias requires its own evidence disposition; an expected alias missing from discovery or a later stage remains incomplete and can never be converted into a class gap."
  prohibitions:
    - statement: "MUST NOT mutate the fleet broadly, control an unselected device, or report success without recording restoration after a mutating stage."
      status: resolved
      verification: test
    - statement: "MUST NOT label synthetic evidence as physical-fleet evidence or turn poor results from available hardware into a named gap."
      status: resolved
      verification: test
  artifacts:
    - path: scripts/thread_revalidation.py
      provides: "Hermetic discovery, request, animation, staleness, validate and generate mode implementations"
    - path: tests/test_scripts/test_thread_revalidation.py
      provides: "Frozen protocol schedules, exact clocks/statistics, censored-result and restoration safety tests"
  key_links:
    - from: scripts/thread_revalidation.py
      to: src/lifx/api.py
      via: "separate discover() and discover_mdns() observations with source-specific absence semantics"
      pattern: "discover|discover_mdns"
    - from: scripts/thread_revalidation.py
      to: src/lifx/animation/animator.py
      via: "fresh synchronous Animator.send_frame() instances and only currently exposed statistics"
      pattern: "packets_sent|total_time_ms|gated|acks_outstanding"
    - from: scripts/thread_revalidation.py
      to: scripts/measurement_support.py
      via: "immutable journals, captured state, observer events, exact restoration and privacy validation"
      pattern: "capture|append|restore|validate"
---

<objective>
Implement and hermetically prove every physical protocol mode before any operator touches the fleet.

Purpose: Hardware time must exercise a frozen, privacy-safe implementation whose schedules, clock boundaries, animation semantics, staleness interpretation and restoration behaviour are already executable and fully tested.
Output: Production-ready discovery, request, animation, staleness, validation and generation modes with deterministic fake-fleet coverage.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/14-thread-revalidation-and-docs/14-CONTEXT.md
@.planning/phases/14-thread-revalidation-and-docs/14-RESEARCH.md
@.planning/phases/14-thread-revalidation-and-docs/14-PATTERNS.md
@.planning/phases/14-thread-revalidation-and-docs/14-SPEC.md
@.planning/phases/14-thread-revalidation-and-docs/14-REVIEWS.md
@.planning/phases/14-thread-revalidation-and-docs/14-03-SUMMARY.md

<interfaces>
Use the existing `discover()`, `discover_mdns()`, Device request path and synchronous `Animator.send_frame()` exactly as exposed. Animator statistics currently expose `packets_sent`, `total_time_ms`, `gated` and `acks_outstanding`; there is no accepted-ACK count, expiry count or rendering signal. Production scheduling remains based on float `time.monotonic()`; measurement events use integer `time.monotonic_ns()` through the Plan 14-01 observer.
</interfaces>
</context>

## Artifacts this phase produces

- `discovery`, `request`, `animation`, `staleness`, `validate` and `generate` CLI modes in `scripts/thread_revalidation.py`.
- Frozen schedule fields for alternating six-round discovery, 100 request gaps, 1/2/5 FPS animation offers and absolute 60-second staleness polls.
- Exact request fields for logical start/completion, successful transmission timestamps, accepted sequence, accepted-ACK timestamp, retransmission count and separate logical/ACK distributions.
- Animation fields limited to offered frames, successful `packets_sent`, `total_time_ms`, `gated`, `acks_outstanding`, send failures, interruption, pre/post liveness and restoration.
- Staleness fields for per-leg observations, first merged-liveness absence, first advertisement absence, consecutive both-absent pairs, confirmed advertisement expiry, cadence overrun/confounder and censorship.
- Hermetic fake-fleet tests covering every terminal outcome before the hardware checkpoint.
- A staged-index validator that inspects the exact evidence allowlist from Git's index after staging and emits only bounded, identity-free failures.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze paired discovery and 100-trial accepted-ACK request modes</name>
  <files>scripts/thread_revalidation.py, scripts/measurement_support.py, tests/test_scripts/test_thread_revalidation.py</files>
  <read_first>
    - .planning/phases/14-thread-revalidation-and-docs/14-CONTEXT.md (D-01 through D-08, D-17 through D-20)
    - src/lifx/api.py (`discover()` and `discover_mdns()` streaming contracts)
    - src/lifx/network/connection.py (Plan 14-01 private observer event boundary)
    - scripts/measure_merged_discovery.py (Phase 13 alias, deterministic summary and validation patterns)
    - tests/test_scripts/test_measure_merged_discovery.py (proven hermetic CLI selector and privacy assertions)
  </read_first>
  <behavior>
    - Per D-01 and D-02, exactly six paired rounds alternate source order and use one pre-generated, bounded-jitter inter-round schedule frozen in the manifest.
    - Per D-03, D-05 and D-06, each alias in the frozen operator-approved expected roster receives exactly 100 no-op SetPower logical trials at its captured binary power with one pre-generated 0.5-to-1.5-second schedule; every timeout, send failure and retransmission remains a trial outcome.
    - Per D-07, a valid response is timed from logical start and from the successful transmission carrying the accepted sequence; late acknowledgements select the matching earlier transmission.
    - Per D-08, median, nearest-rank p95 at ceil(0.95*N) and maximum are computed independently for each completed distribution; timeout latency stays undefined.
  </behavior>
  <action>Addresses the review findings on accepted-ACK timing, paired discovery, exact statistics and observation-sink ownership. Implement discovery and request CLI modes against the Plan 14-02 manifest/journal contract. Require init to accept an operator-approved expected alias/class roster independently of discovery output, validate privacy-safe unique aliases and exact public class names, and freeze the roster before opening a collection journal. The roster must contain every operator-known available Thread device, including at least one Light, one MultiZoneLight, one CeilingLight and two distinct MatrixLight aliases; it must identify InfraredLight and HevLight as the only classes without available hardware. Pre-generate and persist schedules before I/O; execute serially and append one validated event before continuing. Discovery rows distinguish source, round, order, zero result and failure. Request rows consume the private observer values without changing production scheduling clocks, bind accepted acknowledgements to their actual successful-send sequence, and never derive timing from queue insertion after validation. Run class-shaped preflight capture before mutation and apply the Plan 14-03 `power_out_of_range` stop rule. Derive distributions from immutable terminal rows only. Resume every missing/incomplete expected alias; an expected alias absent from discovery stays incomplete, while an observed alias outside the roster records roster drift and stops the session. Reject roster, seed, revision or protocol drift rather than silently mixing sessions.</action>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_scripts/test_measure_merged_discovery.py tests/test_scripts/test_thread_revalidation.py -q</automated>
    <fails_when>The suite exits non-zero; collection starts without a valid frozen expected roster; either Matrix alias or any required available class is omitted; an expected alias missing from discovery becomes a gap; roster drift is ignored; rounds are unpaired or not alternated; a schedule is generated during execution; fewer/more than 100 terminal trials close an expected alias; failures disappear; accepted RTT uses the wrong sequence or timestamp boundary; p95 uses interpolation; timeout latency is numeric; or resume overwrites history.</fails_when>
  </verify>
  <acceptance_criteria>
    - Six source-paired discovery rounds retain duplicate, empty and failed observations without double-counting alias/source/round outcomes.
    - Every expected alias, including both distinct MatrixLight aliases, has 100 immutable request outcomes unless an explicit preflight/restoration stop leaves it incomplete.
    - Logical and ACK latency statistics use integer nanoseconds and the exact D-07/D-08 definitions.
    - No production clock, retry constant, public API or dependency changes.
  </acceptance_criteria>
  <done>THREAD-01 and THREAD-02 modes are frozen, replayable and hermetically proven with correct source, trial, retransmission and timing semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze current-production animation and source-aware staleness modes</name>
  <files>scripts/thread_revalidation.py, scripts/measurement_support.py, tests/test_scripts/test_thread_revalidation.py</files>
  <read_first>
    - .planning/phases/14-thread-revalidation-and-docs/14-CONTEXT.md (D-04, D-09 through D-16)
    - .planning/phases/14-thread-revalidation-and-docs/14-REVIEWS.md (animation-stat, outstanding-count and staleness/censorship findings)
    - src/lifx/animation/animator.py (`Animator`, `send_frame()` and statistics)
    - src/lifx/animation/flow.py (`acks_outstanding` increment, ACK and expiry reduction paths)
    - src/lifx/api.py (`discover()` merged unicast-liveness path and `discover_mdns()` advertisement path)
    - scripts/ipv6_thread_probe.py (state capture/restoration and operator stop precedent)
  </read_first>
  <behavior>
    - Per D-09 through D-15, a fresh Animator synchronously offers one serial 10-second run at 1, 2 and 5 FPS for every animation-capable alias in the frozen expected roster, with only pre/post liveness and no concurrent-query load or performance gate.
    - Per D-11, the mode adds no WiFi-derived concurrent-query workload; it records only pre/post liveness and treats unrelated activity as a confounder.
    - Per D-13, no production animation instrumentation, flow-control constant, delivery strategy or public API changes solely for THREAD-03, and the observation cannot justify tuning.
    - Only `packets_sent`, `total_time_ms`, `gated` and `acks_outstanding` are read from production statistics; offered, failed, interrupted, liveness and restoration values are script-owned. A decrease in `acks_outstanding` is never labelled or interpreted as ACK delivery because expiry has the same effect.
    - Per D-04, the staleness clock begins at disconnect and polls both APIs on absolute 60-second monotonic deadlines; `discover()` absence is unicast-liveness absence, `discover_mdns()` absence is advertisement absence, and only three consecutive both-absent pairs confirm expiry.
    - A call-window overrun records a confounder and does not overlap the next pair; a target advertised at three hours is censored and does not close THREAD-04.
  </behavior>
  <action>Addresses review findings on synchronous Animator use, exposed-stat names, ambiguous outstanding reductions, staleness semantics and censored-result escalation. Build animation frames through the current class-specific packet path, call synchronous `send_frame()` directly from the async loop, and `await asyncio.sleep()` only between absolute offer deadlines; do not use a thread/executor wrapper. Capture state, run preflight, use a fresh Animator, and exact-restore/read back in every BaseException path per D-14/D-16. Keep D-12 counters observational: no rendering, smoothness, ceiling, ACK-delivery or WiFi comparison language. For staleness, record both leg results and their different meanings on every pair, plus first merged-liveness absence and first advertisement absence separately. Start absolute cadence at disconnect, give each call its configured/default 15-second window, record deadline overruns without overlapping calls, confirm only after three consecutive both-absent pairs, then restore and prove starting availability. At the cap, emit `censored` and require the executor to stop at a blocking decision: either the user explicitly amends D-04 for a new extended-cap session, or the censored record is preserved and THREAD-04/Phase 14 remains open. Do not convert censorship to a named hardware gap or completed expiry.</action>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_scripts/test_thread_revalidation.py -q</automated>
    <fails_when>The suite exits non-zero; Animator is run in an executor or production animation changes; an unavailable field is emitted; outstanding-count reduction is called an ACK; a zero-throughput run fails; discovery legs share false semantics; one missed round confirms expiry; cadence overlaps after overrun; censorship closes THREAD-04; or any exit omits exact restoration evidence.</fails_when>
  </verify>
  <acceptance_criteria>
    - Fake Animator tests prove exact offered/sent/gated/failed/interrupted accounting at all three frozen rates and accept zero success.
    - Assertions forbid ACK-received/expiry-count fields and any delivery interpretation of `acks_outstanding` reductions.
    - Staleness tests cover transient one-leg absence, first absence, reset of the consecutive counter, three-pair confirmation, call overrun, censorship and restored availability.
    - Censored execution exposes only the two source-grounded decisions and cannot produce a passing closure row.
  </acceptance_criteria>
  <done>THREAD-03 and THREAD-04 modes preserve current production behaviour and produce honest, source-aware, restoration-backed outcomes without performance claims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fail closed on roster completeness, staged privacy and unsupported evidence language</name>
  <files>scripts/thread_revalidation.py, scripts/measurement_support.py, tests/test_scripts/test_thread_revalidation.py</files>
  <read_first>
    - .planning/phases/14-thread-revalidation-and-docs/14-SPEC.md (AC-07 through AC-17 and all prohibitions)
    - .planning/phases/14-thread-revalidation-and-docs/14-CONTEXT.md (D-15 through D-20)
    - src/lifx/products/__init__.py (public product/class registry exports)
    - scripts/check_patch_coverage.py (privacy-safe fail-closed validation style)
    - tests/test_scripts/test_thread_revalidation.py (mode-level fake fleet)
  </read_first>
  <behavior>
    - Exact public lighting classes Light, MultiZoneLight, MatrixLight, CeilingLight, InfraredLight and HevLight each receive one closure disposition driven by the frozen operator-approved expected roster, never by the subset observed during initial discovery.
    - Every expected Light, MultiZoneLight, CeilingLight and MatrixLight alias has its own physical evidence; both distinct MatrixLight aliases are required. Named gaps are legal only for InfraredLight and HevLight, and poor/zero results or a missing expected alias never become gaps.
    - Expected-roster changes, unexpected observed aliases, foreign session rows, missing terminal records, mutation without exact restoration, raw-looking identifiers, synthetic provenance, censored expiry and benchmark/tuning claims all fail validation.
    - The staged-evidence command reads the exact allowed evidence paths from Git's index after staging, rejects a missing/extra staged evidence path or worktree-only substitution, applies full schema/privacy/provenance validation to those index blobs, and never prints a matched private value.
    - Validation is deterministic and side-effect free; generation cannot run until it passes.
  </behavior>
  <action>Complete the hermetic validator/generator boundary per D-15, D-18, D-19 and D-20. Derive the six-class ledger from the manifest's immutable operator-approved expected roster and journals rather than caller-supplied closure claims or discovered inventory. Reject init unless the independent roster contains every operator-known available Thread alias, explicitly including Light, MultiZoneLight, CeilingLight and two distinct MatrixLight devices. Require every expected alias to complete all applicable stages, including the bounded animation attempt regardless of outcome. Permit named gaps only for InfraredLight and HevLight; the four available classes can never receive gaps, and a missing expected alias remains an incomplete disposition. If live availability differs from the roster, append a privacy-safe confounder/incomplete disposition and require a new session rather than silently skipping or relabelling. Add mechanical privacy checks for aliases/addresses/hostnames/raw captures, provenance checks that reject emulator/synthetic rows as fleet evidence, and vocabulary checks that reject authoritative benchmark, universal limit, regression gate or tuning claims. Add a `validate-staged` mode for Plan 14-06: require the exact nine evidence paths, read their staged index blobs rather than working-tree files, reject extra/missing evidence paths, and report only bounded path/category failures without echoing matched content. Hermetic tests must create a temporary Git index, stage safe and unsafe synthetic evidence, prove worktree-after-stage changes do not affect the inspected bytes, and assert the private sentinel never appears in stdout, stderr or exceptions. Make generation atomic only after validation and deterministic from sorted keyed data, without modifying journals.</action>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_scripts/test_thread_revalidation.py -q &amp;&amp; uv run --frozen pyright scripts/measurement_support.py scripts/thread_revalidation.py</automated>
    <fails_when>Either command exits non-zero; the expected roster omits a required class or either Matrix alias; initial discovery is treated as roster authority; a missing expected alias or poor physical result becomes a gap; any class other than InfraredLight or HevLight receives a gap; synthetic provenance passes; roster drift is skipped; the staged-index validator accepts missing/extra paths, reads worktree bytes or exposes a private sentinel; a raw-looking value, discarded outcome, censored expiry, unrestored mutation or benchmark/tuning claim validates; generation changes a journal; or repeated generation differs.</fails_when>
  </verify>
  <acceptance_criteria>
    - The fake-fleet matrix covers all six exact public lighting classes, both distinct MatrixLight aliases and every legal/illegal closure disposition.
    - Every expected alias requires individual evidence; only InfraredLight and HevLight receive explicit named gaps; no result-quality threshold exists.
    - Missing expected aliases remain incomplete, unexpected aliases are roster drift, and neither state can close the session.
    - Privacy, exact staged-index contents, provenance, immutability, restoration, censorship and evidence-language failures are mechanical and block generation or commit.
    - Validation and generation are byte-deterministic from the same immutable journals.
  </acceptance_criteria>
  <done>All physical modes and closure rules are hermetically ready for one operator-gated run, with no path from unsafe or overstated evidence to a passing report.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high-severity threat blocks plan completion until its named mitigation test passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live discovery/request/animation APIs -> private observer | Network-controlled timing and values enter a privacy-safe event pipeline. |
| Operator-selected alias -> mutating animation/staleness stage | Only one mapped target may be controlled, and every mutation requires exact restoration. |
| Journals -> validator/generator | Untrusted, interrupted or synthetic rows must not acquire fleet authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-11 | Spoofing / Information disclosure | live identity resolution and staged evidence | high | mitigate | External alias validation precedes I/O; write-boundary schema and leak tests reject raw identifiers and synthetic provenance; the staged-index validator checks the exact nine evidence blobs without echoing matches. |
| T-14-12 | Tampering / Repudiation | immutable journals and closure | high | mitigate | Append-only keyed events, frozen manifest, deterministic validation and no gap conversion for poor results. |
| T-14-13 | Denial of service | staleness poll and animation schedules | medium | mitigate | Absolute bounded schedules, no overlapping calls, explicit interruption/censorship and mandatory restoration. |
| T-14-14 | Elevation of privilege | selected-device mutation | high | mitigate | Exact alias/inventory membership checks, one serial target and fail-closed readback before progression. |
| T-14-SC | Tampering | package installs | low | accept | No package or lockfile change is made. |
</threat_model>

<verification>
- Run all three hermetic task commands without hardware.
- Confirm all 18 applicable resolved edge rows appear as executable test expectations.
- Confirm both prohibition rows are mechanically rejected and all high-severity threats block generation.
</verification>

<success_criteria>
- Every physical mode is frozen and hermetically executable before the hardware checkpoint.
- Accepted-ACK timing, current animation statistics, staleness semantics and restoration are exact and source-grounded.
- Privacy, provenance, inventory and closure validation fail closed without turning poor observations into gaps.
</success_criteria>

<output>
Create `.planning/phases/14-thread-revalidation-and-docs/14-04-SUMMARY.md` when done.
</output>
