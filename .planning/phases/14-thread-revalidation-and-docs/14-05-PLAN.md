---
phase: 14-thread-revalidation-and-docs
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/user-guide/discovery.md
  - docs/user-guide/advanced-usage.md
  - docs/user-guide/troubleshooting.md
  - docs/api/network.md
  - examples/discovery_progressive.py
  - mkdocs.yml
  - AGENTS.md
  - CLAUDE.md
  - tests/test_network/test_mdns/test_phase_contract.py
  - tests/test_repository_guidance.py
autonomous: true
requirements: [DOCS-04, DOCS-05, DOCS-06]
estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - statement: "DOCS-04 adjacency is a backstop: guidance sections are independent prose and have no touching/equal collection-element semantics."
      verification: backstop
    - "DOCS-04 empty: the canonical guide covers discover(), discover_udp() and discover_mdns() with one runnable progressive migration example."
    - statement: "DOCS-04 encoding is a backstop: API names are exact Python identifiers in code spans and no text-normalisation comparison exists."
      verification: backstop
    - statement: "DOCS-04 ordering is a backstop: readable section order does not alter the documented API contract."
      verification: backstop
    - statement: "DOCS-05 adjacency is a backstop: the named limitations are independent facts rather than mergeable collection intervals."
      verification: backstop
    - "DOCS-05 empty: all named discovery limitations and the Phase 14 evidence qualification are present."
    - statement: "DOCS-05 encoding is a backstop: standard Markdown text has no length/equality contract."
      verification: backstop
    - statement: "DOCS-05 ordering is a backstop: limitations may appear in any readable order without changing meaning."
      verification: backstop
    - statement: "DOCS-06 adjacency is a backstop: architecture-guide matches are independently corrected and adjacency has no semantic effect."
      verification: backstop
    - "DOCS-06 empty: zero false TaskGroup/Python-floor/default-discovery claims remain and both guidance files contain their accurate replacement boundary."
    - statement: "DOCS-06 encoding is a backstop: asyncio.TaskGroup is an exact ASCII code identifier and no alternate normalisation is accepted."
      verification: backstop
    - statement: "DOCS-06 ordering is a backstop: guide ordering does not affect whether each factual claim is correct."
      verification: backstop
  prohibitions: []
  artifacts:
    - path: docs/user-guide/discovery.md
      provides: "Canonical consumer-journey discovery guide and Phase 14 evidence qualification"
    - path: examples/discovery_progressive.py
      provides: "Single executable source for merged and explicit discovery migration snippets"
    - path: tests/test_repository_guidance.py
      provides: "AGENTS/CLAUDE import, de-duplication and Python 3.10 concurrency contract tests"
    - path: mkdocs.yml
      provides: "Discovery navigation and strict snippet-path validation"
  key_links:
    - from: docs/user-guide/discovery.md
      to: examples/discovery_progressive.py
      via: "pymdownx.snippets includes from one executable source"
      pattern: "--8<--"
    - from: mkdocs.yml
      to: docs/user-guide/discovery.md
      via: "navigation plus snippets check_paths=true and repository-root base_path"
      pattern: "check_paths|base_path"
    - from: CLAUDE.md
      to: AGENTS.md
      via: "literal @AGENTS.md import; shared architecture guidance remains canonical only in AGENTS.md"
      pattern: "@AGENTS.md"
---

<objective>
Ship the independent consumer discovery journey and correct repository guidance without waiting for physical hardware collection.

Purpose: Consumers need accurate source selection, limitations, fallback and Python 3.10 concurrency guidance even if the hardware-gated evidence session remains open.
Output: A canonical discovery guide, executable progressive snippets, concise linked API/advanced pages, strict docs checks and canonical AGENTS/CLAUDE guidance.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/phases/14-thread-revalidation-and-docs/14-CONTEXT.md
@.planning/phases/14-thread-revalidation-and-docs/14-RESEARCH.md
@.planning/phases/14-thread-revalidation-and-docs/14-PATTERNS.md
@.planning/phases/14-thread-revalidation-and-docs/14-SPEC.md
@.planning/phases/14-thread-revalidation-and-docs/14-REVIEWS.md

<interfaces>
The public API already exposes merged `discover()`, explicit `discover_udp()` and explicit `discover_mdns()`. `discover()` preserves UDP source control and merges active work; `discover_mdns()` is an explicit bounded legacy-unicast DNS-SD alternative whose absence indicates advertisement visibility, not the same liveness fact as merged discovery. The project supports Python 3.10, where `asyncio.TaskGroup` is unavailable; user-facing fan-out examples must use `asyncio.create_task()`/`asyncio.gather()` with explicit ownership and cleanup.
</interfaces>
</context>

## Artifacts this phase produces

- New `docs/user-guide/discovery.md` canonical guide with consumer-journey sections, method-selection table, limitations and troubleshooting.
- New `examples/discovery_progressive.py` executable example regions for unchanged merged discovery and both explicit source APIs.
- `mkdocs.yml` navigation plus `pymdownx.snippets` `check_paths: true` and `base_path: ["."]` configuration.
- Concise linked discovery summaries in `docs/user-guide/advanced-usage.md` and `docs/api/network.md`.
- Corrected `docs/user-guide/troubleshooting.md` Python 3.10 fan-out ownership guidance.
- Canonical shared guidance in `AGENTS.md`, import-only shared handoff in `CLAUDE.md`, and new repository-level contract tests.

<tasks>

<task type="auto">
  <name>Task 1: Create the executable progressive discovery journey</name>
  <files>docs/user-guide/discovery.md, examples/discovery_progressive.py, tests/test_network/test_mdns/test_phase_contract.py</files>
  <read_first>
    - .planning/phases/14-thread-revalidation-and-docs/14-CONTEXT.md (D-21 through D-23)
    - .planning/phases/14-thread-revalidation-and-docs/14-RESEARCH.md (consumer API semantics and documentation architecture)
    - docs/user-guide/advanced-usage.md (existing UDP discovery material to move, not duplicate)
    - docs/api/network.md (current discovery signatures and wording)
    - src/lifx/api.py (`discover`, `discover_udp`, `discover_mdns`, `find_by_serial`, `find_by_ip`)
    - tests/test_network/test_mdns/test_phase_contract.py (current docs/query-model contract, including stale assertions)
  </read_first>
  <action>Implement D-21, D-22 and D-23 as one consumer-facing tracer. Create the canonical guide organised around: unchanged `discover()` migration, explicit `discover_udp()` and `discover_mdns()` caller control, targeted lookup and IPv6, method selection, limitations, and troubleshooting. Move the substantive UDP material rather than copy it. Create one executable synthetic example file with named regions for all three discovery flows; snippets in the guide must include those regions, not hand-copy code. Explain that merged discovery preserves results from both sources, explicit APIs retain caller control, mDNS may be unavailable, and callers can select/fallback without private network examples. Update the phase contract to execute/import the example and assert every public name/section while removing the obsolete Phase 11 claim that default discovery is mDNS-only. Use only documentation and synthetic addresses/serials.</action>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_network/test_mdns/test_phase_contract.py -q</automated>
    <fails_when>The suite exits non-zero; any of the three APIs or migration regions is absent; guide code diverges from the executable file; UDP material is duplicated instead of moved; the stale default-mDNS claim survives; or an example resembles a live identifier/address.</fails_when>
  </verify>
  <acceptance_criteria>
    - One progressive example executes/imports and supplies all guide snippets.
    - The guide follows D-22's full consumer journey and distinguishes merged versus explicit source control accurately.
    - Existing callers can identify an unchanged `discover()` path and explicit fallbacks without reading implementation internals.
    - No raw/live identifier or private-infrastructure example enters documentation.
  </acceptance_criteria>
  <done>DOCS-04 has one executable source of truth and a complete, accurate migration journey.</done>
</task>

<task type="auto">
  <name>Task 2: Link the canonical guide, enforce snippet paths and correct user-facing concurrency advice</name>
  <files>docs/user-guide/advanced-usage.md, docs/api/network.md, docs/user-guide/troubleshooting.md, mkdocs.yml, tests/test_network/test_mdns/test_phase_contract.py</files>
  <read_first>
    - docs/user-guide/discovery.md (Task 1 canonical destination)
    - docs/user-guide/advanced-usage.md (remaining summary/link site)
    - docs/api/network.md (concise factual API reference)
    - docs/user-guide/troubleshooting.md (current concurrency and timeout advice)
    - mkdocs.yml (navigation and `pymdownx.snippets` configuration)
    - AGENTS.md (Python floor and supported fan-out semantics)
  </read_first>
  <action>Finish D-21 and the DOCS-05 consumer boundary. Replace the moved advanced-usage body with a short accurate summary and link, keep the API page signature-led and factual, and add the canonical guide to user-guide navigation. Configure `pymdownx.snippets` with `check_paths: true` and repository-root `base_path: ["."]` so a missing include fails the real docs build. Document AC-11's four required limitations exactly: the query leg is IPv4 multicast, replies are legacy-unicast-only, unsolicited announcements are not received, and fleet-scale mesh behaviour is supported by synthetic rather than current-hardware proof. Also distinguish merged/source-specific visibility, state that bounded timeouts/censorship are not absence proofs, and qualify Phase 14 physical observations as fleet-specific rather than universal benchmarks. Include targeted/IPv6 and fallback troubleshooting. Because troubleshooting is user-facing architecture guidance, replace any Python-3.11-only TaskGroup recommendation with accurate Python 3.10 `asyncio.create_task()`/`asyncio.gather()` ownership, cancellation and result-handling prose; do not leave that correction only in AGENTS.md.</action>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_network/test_mdns/test_phase_contract.py -q &amp;&amp; uv run --frozen zensical build &amp;&amp; uv run --frozen llmstxt-standalone build</automated>
    <fails_when>Any command exits non-zero; navigation/link/include is broken; snippets do not use strict path checking from the repository root; substantive guidance remains duplicated; a required limitation is absent; or troubleshooting recommends TaskGroup/unsupported Python.</fails_when>
  </verify>
  <acceptance_criteria>
    - A deliberately broken snippet path would make `zensical build` fail, and the configured valid paths build successfully.
    - Advanced usage links rather than duplicates; the API page remains concise and signature-accurate.
    - IPv4 multicast queries, legacy-unicast-only replies, no unsolicited announcements, synthetic fleet-scale proof and fleet-specific Phase 14 qualification are mechanically asserted.
    - Troubleshooting gives Python 3.10-compatible fan-out and cleanup advice.
  </acceptance_criteria>
  <done>The canonical guide is reachable, source-checked and supported by accurate API, limitation and troubleshooting boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Resolve the AGENTS/CLAUDE contract conflict in favour of canonical shared guidance</name>
  <files>AGENTS.md, CLAUDE.md, tests/test_repository_guidance.py, tests/test_network/test_mdns/test_phase_contract.py</files>
  <read_first>
    - .planning/phases/14-thread-revalidation-and-docs/14-CONTEXT.md (D-24)
    - .planning/phases/14-thread-revalidation-and-docs/14-REVIEWS.md (CLAUDE duplication conflict and troubleshooting scope findings)
    - AGENTS.md (canonical architecture, Python-floor and concurrency guidance)
    - CLAUDE.md (current duplicated shared query-model text and Claude-specific remainder)
    - tests/test_network/test_mdns/test_phase_contract.py (`_REQUIRED_QUERY_MODEL_PATHS` and prior Phase 11 expectations)
  </read_first>
  <behavior>
    - `AGENTS.md` is the sole shared/GSD architecture source and accurately describes Python 3.10 `create_task()`/`gather()` ownership.
    - `CLAUDE.md` contains a literal `@AGENTS.md` import plus only genuinely Claude-specific instructions; copied query-model/architecture paragraphs are absent.
    - The phase contract no longer demands duplicated shared text in CLAUDE.md, while a separate repository test requires the import and rejects duplicate architecture guidance.
    - Both files reject the false claim that the library uses `asyncio.TaskGroup` for device concurrency.
  </behavior>
  <action>Implement D-24 exactly and resolve the review's direct contract conflict. Correct shared discovery/query-model and Python 3.10 concurrency prose in `AGENTS.md`. Reduce `CLAUDE.md` to `@AGENTS.md` plus genuinely Claude-specific instructions. Deliberately remove `CLAUDE.md` from `_REQUIRED_QUERY_MODEL_PATHS` in `test_phase_contract.py`; the old equality expectation contradicts D-24 and must not be retained. Add `tests/test_repository_guidance.py` to require the import, compare against a bounded list of shared architecture markers to prohibit copied guidance, assert accurate `asyncio.create_task()`/`asyncio.gather()` semantics in AGENTS.md, and reject TaskGroup claims in both files. Keep the phase contract focused on canonical/query-model facts and update any Phase 11-era default-discovery assumption to the current merged `discover()` contract.</action>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_repository_guidance.py tests/test_network/test_mdns/test_phase_contract.py -q &amp;&amp; uv run --frozen pytest</automated>
    <fails_when>Either command exits non-zero; CLAUDE.md remains a required duplicate query-model path; the import is absent; shared architecture paragraphs occur in both files; AGENTS.md lacks accurate Python 3.10 fan-out/cleanup guidance; a TaskGroup claim remains; or a stale mDNS-only default assertion survives.</fails_when>
  </verify>
  <acceptance_criteria>
    - The two formerly conflicting tests now encode complementary canonical-source and import-only contracts.
    - Shared architecture/query-model guidance exists in AGENTS.md only; CLAUDE.md retains only the import and Claude-specific content.
    - Exact tests guard Python 3.10 support and the current merged discovery model.
    - The full suite passes without weakening any earlier phase contract.
  </acceptance_criteria>
  <done>DOCS-06 is corrected at both source and test-contract levels, with no duplicated guidance or unsupported concurrency claim.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high-severity threat blocks plan completion until its named mitigation test passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executable examples -> published documentation | Copied or missing snippets can publish code that no longer runs. |
| Historical architecture prose -> current consumer guidance | Stale Phase 11 and Python 3.11 assumptions can misdirect supported callers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-15 | Tampering | snippet includes and navigation | medium | mitigate | One executable source, strict `check_paths`, repository-root base path, real site builds and phase-contract tests. |
| T-14-16 | Repudiation | duplicated AGENTS/CLAUDE guidance | medium | mitigate | Canonical-source/import-only tests make ownership and factual corrections explicit. |
| T-14-17 | Information disclosure | discovery examples | high | mitigate | Synthetic-only values and repository tests reject live-looking identifiers/addresses before publication. |
| T-14-SC | Tampering | package installs | low | accept | Existing documentation dependencies are used with `--frozen`; no package or lockfile change is made. |
</threat_model>

<verification>
- Run all three task commands without hardware or network access.
- Confirm all 12 DOCS edge rows are present: three resolved empty rows plus nine flat backstop objects.
- Confirm docs work is Wave 1 with no dependency on physical collection.
</verification>

<success_criteria>
- Consumers have one executable, navigable and source-checked discovery guide covering all public paths and limitations.
- Advanced/API/troubleshooting pages link accurately without duplicating the canonical body.
- AGENTS.md is canonical, CLAUDE.md imports it, and tests resolve the former duplication conflict.
</success_criteria>

<output>
Create `.planning/phases/14-thread-revalidation-and-docs/14-05-SUMMARY.md` when done.
</output>
