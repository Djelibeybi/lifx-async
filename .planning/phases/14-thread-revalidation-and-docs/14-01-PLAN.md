---
phase: 14-thread-revalidation-and-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/14-thread-revalidation-and-docs/14-COVERAGE-BASE.txt
  - src/lifx/network/connection.py
  - scripts/measurement_support.py
  - scripts/thread_revalidation.py
  - tests/test_network/test_connection_retry.py
  - tests/test_scripts/test_thread_revalidation.py
autonomous: true
requirements: [THREAD-02]
estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "Per D-07, the production request engine emits enough private, value-only timing information to derive logical completion latency from the initial logical start and acknowledgement RTT from the exact successful transmission whose sequence receives the first valid acknowledgement."
    - "The accepted timestamp is sampled with time.monotonic_ns() immediately after response_queue.get() returns, retained only after source/sequence/serial validation succeeds, and explicitly includes background-receiver queueing plus event-loop wake latency while excluding later validation work."
    - "Observer-only time.monotonic_ns() samples do not replace, convert, or influence the existing time.monotonic() float clock, deadline arithmetic, retransmission schedule, wait boundaries, public signatures, or request results."
    - "Per D-17 and D-19, scripts/measurement_support.py owns the request observation event, sink, and caller-task capture context; connection.py owns only a private callback shape plus explicit internal propagation into _transmit_and_listen(), and no script imports a helper from tests."
    - "A successful send records sent_ns immediately after send_packet() returns; an accepted response uses the stored sent_ns for header.sequence, so a late acknowledgement for an earlier sequence remains valid and measurable."
    - "The accepted-response observation records header.thread_connection (frame address byte 22, bit 3), the device's own report that the reply travelled over a Thread connection. It is a value-only boolean already in scope at the acceptance point, so it needs no signature, queue, or propagation change."
  prohibitions:
    - statement: "MUST NOT place a device serial, address, packet, raw exception, hostname, endpoint, or private alias value in a request observation, its repr, its error path, or the tracer journal."
      status: resolved
      verification: test
  artifacts:
    - path: src/lifx/network/connection.py
      provides: "Private request-observer callback selection and explicit per-request propagation at the retry/correlation owner"
    - path: scripts/measurement_support.py
      provides: "Private _RequestObservation, _RequestObservationSink, and _capture_request_observations ownership"
    - path: scripts/thread_revalidation.py
      provides: "One production-quality request-to-privacy-gated-JSONL tracer path"
    - path: tests/test_network/test_connection_retry.py
      provides: "Exact timestamp, sequence, event-order, no-observer, failure, cancellation, and privacy tests"
    - path: tests/test_scripts/test_thread_revalidation.py
      provides: "End-to-end tracer journal validation and replay test"
    - path: .planning/phases/14-thread-revalidation-and-docs/14-COVERAGE-BASE.txt
      provides: "Full 40-character implementation-start revision for later changed-line and changed-branch coverage gates"
  key_links:
    - from: scripts/measurement_support.py
      to: src/lifx/network/connection.py
      via: "caller task attribute is selected once by the thin wrapper and passed explicitly to _transmit_and_listen()"
      pattern: "_lifx_request_observer|observer"
    - from: src/lifx/network/connection.py
      to: scripts/thread_revalidation.py
      via: "logical-start, transmission-sent, accepted-response, timeout, send-error, cancellation and cleanup events feed the selected script-owned sink"
      pattern: "logical_start_ns|sent_ns|accepted_ns|sequence|thread_connection"
---

<objective>
Trace one normal SetPower acknowledgement through the real production retry/correlation engine into a privacy-safe append-only event record, with measurement semantics frozen before any physical run.

Purpose: THREAD-02 cannot recover the winning retransmission RTT outside the connection engine, and the review found that ambiguous timestamp placement could invalidate every hardware sample.
Output: A private opt-in request observer, a script-owned sink, one end-to-end tracer journal path, exact hermetic tests, and a fixed coverage base.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-thread-revalidation-and-docs/14-CONTEXT.md
@.planning/phases/14-thread-revalidation-and-docs/14-RESEARCH.md
@.planning/phases/14-thread-revalidation-and-docs/14-PATTERNS.md
@.planning/phases/14-thread-revalidation-and-docs/14-SPEC.md
@.planning/phases/14-thread-revalidation-and-docs/14-REVIEWS.md
@.agents/skills/spike-findings-lifx-async/references/retry-schedule.md

<interfaces>
`DeviceConnection._transmit_and_listen()` owns logical start, every successful send, the shared response queue, source/sequence/serial validation, first accepted response, timeout, and cleanup. `_request_ack_stream_impl()` discards the header and yields `True`, so the observer must live below it. Existing scheduling uses `time.monotonic()` floats and must stay unchanged. The new script-owned callback receives categories, integer timestamps, and the accepted response's `thread_connection` boolean only; it receives no identity or exception text. `_transmit_and_listen()` already holds the unpacked `LifxHeader` at the acceptance point, so the Thread observation is read from the header in scope rather than plumbed.
</interfaces>
</context>

## Artifacts this phase produces

- Private `_RequestObservation`, `_RequestObservationSink`, and `_capture_request_observations()` symbols in `scripts/measurement_support.py`.
- A private request-observer callback/task-attribute seam in `src/lifx/network/connection.py`; no public export, flag, result, or constructor changes.
- Tracer-only request event validation/append/reload functions in `scripts/thread_revalidation.py`, ready for the complete schema in Plan 14-02.
- `14-COVERAGE-BASE.txt`, containing the exact implementation-start commit used by later anti-weakening and 100% changed-line/branch checks.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace a retransmitted acknowledgement from production send to validated JSONL</name>
  <reversibility rating="costly">Changing timestamp capture semantics after fleet collection invalidates THREAD-02 evidence and requires a new session.</reversibility>
  <files>.planning/phases/14-thread-revalidation-and-docs/14-COVERAGE-BASE.txt, src/lifx/network/connection.py, scripts/measurement_support.py, scripts/thread_revalidation.py, tests/test_scripts/test_thread_revalidation.py</files>
  <read_first>
    - .planning/phases/14-thread-revalidation-and-docs/14-CONTEXT.md (D-07, D-17, D-19)
    - .planning/phases/14-thread-revalidation-and-docs/14-REVIEWS.md (accepted-ACK clock and observation-sink findings)
    - src/lifx/network/connection.py (`_transmit_and_listen()` and `_request_ack_stream_impl()`)
    - tests/test_network/test_connection_retry.py (patched gaps, queue injection, cleanup conventions)
    - tests/test_discovery_observation.py (value-suppressed sink precedent only; do not import it from the new script)
  </read_first>
  <behavior>
    - RED proves initial and retransmitted sends, a late valid acknowledgement for an earlier sequence, timeout, send error, cancellation/cleanup, no-observer behaviour, and repr/privacy suppression.
    - `logical_start_ns` is sampled before the initial send; `sent_ns` is sampled immediately after each successful `send_packet()` return; `dequeued_ns` is sampled immediately after the queue get returns and becomes `accepted_ns` only after the response passes existing validation.
    - `accepted_ns` includes receiver queueing and event-loop wake latency by design; this application-observed boundary is named in the protocol event and tests.
    - The accepted-response event records `thread_connection` from the accepted `LifxHeader`; RED proves a Thread-flagged and an unflagged acknowledgement are distinguished, and that the flag never alters correlation, validation, or timing.
    - The legacy float scheduler and all public observable behaviour are byte-semantically unchanged when no observer is attached.
  </behavior>
  <action>Addresses review concerns: accepted-ACK timestamp semantics, monotonic clock mismatch, sink ownership, and a stable patch-coverage baseline. Before RED or any executable edit, write the current full `git rev-parse HEAD` value to `14-COVERAGE-BASE.txt`, validate that it resolves as a commit, and never rewrite it during the phase. Start with failing focused tests. Add a private callback type and caller-task selector in `connection.py`; select it once in each thin request wrapper and pass it explicitly into `_transmit_and_listen()` rather than reading ambient state inside the retry loop. Put the event dataclass, repr-suppressed sink, and capture context in `scripts/measurement_support.py`, which attaches only the callback to the current caller task and restores any prior value in `finally`. Sample observer metadata with `time.monotonic_ns()` at the exact points in `<behavior>` while leaving every existing `time.monotonic()` call and retransmission/deadline calculation intact. Emit value-only categories for logical start, successful transmission, first accepted response, timeout, send error, cancellation, and cleanup. Do not emit raw exception text or any connection/device identity. Wire one retransmitted fake SetPower acknowledgement through `scripts/thread_revalidation.py` to validate, append, reload, and derive distinct logical latency and winning-sequence RTT; this is production-quality skeleton code reused by Plan 14-02, not a throwaway path.</action>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_network/test_connection_retry.py tests/test_scripts/test_thread_revalidation.py -q</automated>
    <fails_when>The focused suite exits non-zero; accepted_ns is sampled before dequeue or after validation work; a late acknowledgement uses the newest rather than matching sequence timestamp; any observer value includes identity/packet/exception text; the float scheduling path changes; no-observer results differ; or the validated tracer cannot append and reload.</fails_when>
  </verify>
  <acceptance_criteria>
    - `14-COVERAGE-BASE.txt` is captured before executable edits and contains one resolvable full SHA.
    - A retransmission test proves `logical_latency_ns = accepted_ns - logical_start_ns` and `ack_rtt_ns = accepted_ns - sent_ns[accepted_sequence]` with different expected values.
    - Tests explicitly document that accepted_ns includes background-receiver queueing and caller-loop wake latency but excludes later validation work.
    - The accepted-response observation carries the device-reported `thread_connection` value, making each RTT sample self-describing. The value is expected to be invariant for a given device across a session, because radio mode is fixed until a firmware crossgrade; the observation verifies device selection, it does not measure a per-request transport choice.
    - Timeout, send-error, cancellation, and cleanup each emit bounded categories without exception text, and an absent observer is a no-op.
    - The end-to-end tracer writes only validated identity-free rows and deterministically replays the same derived request result.
  </acceptance_criteria>
  <done>One real request path is observed end to end with frozen application-observed ACK timing semantics, sequence-correct RTT, privacy-safe storage, and no scheduling or public-API change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze the implementation base and close observer regression gates</name>
  <files>src/lifx/network/connection.py, tests/test_network/test_connection_retry.py, tests/test_scripts/test_thread_revalidation.py</files>
  <read_first>
    - .planning/phases/13-merged-discovery/13-01-PLAN.md (proven focused pytest selector style)
    - scripts/check_patch_coverage.py (full-SHA and anti-weakening contract)
    - src/lifx/network/connection.py (post-tracer request engine)
    - tests/test_network/test_connection_retry.py (post-tracer observer matrix)
  </read_first>
  <behavior>
    - The base file contains exactly the full 40-character HEAD that preceded implementation changes and cannot be rewritten after later task commits.
    - Observer coverage includes every event branch and proves no timing or scheduling constant changed.
    - Existing request retry/correlation tests remain green under the exact Phase 13-style focused selector.
  </behavior>
  <action>Verify that Task 1 captured the current implementation-start commit in `14-COVERAGE-BASE.txt` and retain it unchanged thereafter. Expand the focused tests until all added executable observer lines and outgoing branches are exercised without coverage exclusions, skip markers, or coverage pragmas. Add a source-regression assertion that the observer insertion does not alter `REQUEST_RETRANSMIT_GAPS`, existing `time.monotonic()` scheduling calls, public signatures, queue shape, or request return values. This addresses the review's coverage/estimate concern by giving the split plan a fixed measurable patch boundary rather than adding a large unbudgeted module to the global coverage denominator without an explicit gate.</action>
  <verify>
    <automated>uv run --frozen pytest -o addopts='' tests/test_network/test_connection_retry.py tests/test_scripts/test_thread_revalidation.py -q</automated>
    <fails_when>The suite exits non-zero; the base file is not one resolvable full SHA; any new observer branch is untested; an exclusion/skip/pragma weakens coverage; the request queue or scheduling clock changes; or existing retry/correlation behaviour regresses.</fails_when>
  </verify>
  <acceptance_criteria>
    - `14-COVERAGE-BASE.txt` is a single full SHA resolving to the pre-implementation commit.
    - Focused tests cover initial ACK, retransmitted ACK, earlier-sequence late ACK, timeout, send error, cancellation, cleanup, absent observer, and privacy-safe repr.
    - No public API, dependency, Python floor, retry constant, or animation source file changes.
  </acceptance_criteria>
  <done>The observer tracer has a fixed coverage base and complete focused regression matrix suitable for downstream schema and hardware work.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high-severity threat blocks plan completion until its named mitigation test passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted UDP response -> request correlation -> private observer | Network input reaches measurement metadata only after existing validation. |
| Script-owned capture context -> production request engine | Repository tooling temporarily attaches an opt-in callback to one caller task. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Spoofing / Tampering | request response acceptance | high | mitigate | Preserve existing source, sequence and serial validation before accepting the dequeue timestamp; tests inject invalid and late responses. |
| T-14-02 | Information disclosure | observer event/repr/journal | high | mitigate | Events contain categories, sequence and integer timestamps only; repr and failure tests reject identity, packet and exception content before append. |
| T-14-03 | Tampering | retry scheduling | medium | mitigate | Observer uses a separate monotonic_ns metadata clock; regression tests pin float scheduling calls, constants and no-observer results. |
| T-14-SC | Tampering | package installs | low | accept | No package is installed; existing uv lock and empty runtime dependency set are unchanged. |
</threat_model>

<verification>
- Run the two task commands without hardware or ambient discovery.
- Confirm the implementation diff contains no animation source, retry constant, public export, dependency, or Python-floor change.
- Confirm `14-COVERAGE-BASE.txt` resolves and remains unchanged after Task 1 commits.
</verification>

<success_criteria>
- The real retry/correlation path exposes sequence-correct application-observed ACK timing to one private script-owned sink.
- Measurement clocks cannot influence production scheduling.
- Identity-safe tracer rows survive append/reload and every observer branch is hermetically exercised.
</success_criteria>

<output>
Create `.planning/phases/14-thread-revalidation-and-docs/14-01-SUMMARY.md` when done.
</output>
