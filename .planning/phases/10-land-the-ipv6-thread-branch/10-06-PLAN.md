---
phase: 10-land-the-ipv6-thread-branch
plan: 06
type: execute
wave: 5
depends_on: [10-04, 10-05]
files_modified:
  - .planning/phases/10-land-the-ipv6-thread-branch/10-UAT-RESULTS.json
  - .planning/phases/10-land-the-ipv6-thread-branch/10-EXCEPTION-OVERRIDE.json
  - .planning/phases/10-land-the-ipv6-thread-branch/10-06-SUMMARY.md
autonomous: false
requirements: [IPV6-01, IPV6-02, IPV6-03, IPV6-04]
tags: [ipv6, thread, merge, uat, ci, codecov]
user_setup:
  - service: thread-hardware
    why: "SPEC R9 gates the merge on a recorded control-path UAT against a real Thread MatrixLight; CI has no Thread hardware"
    env_vars: []
    dashboard_config:
      - task: "Ensure at least one Thread MatrixLight (Test Candle or Test Tube, ULA prefix fd00:1::) is powered and reachable, and quiesced from other pollers during the run"
        location: "Local network / Home Assistant"

estimate:
  tokens: 50000
  raw_tokens: 100000
  tasks: 4
  confidence: med

must_haves:
  truths:
    - "Every commit the merge adds to main carries a good GPG signature and a Signed-off-by trailer; redding1's original commit 79a5ce0 is present unmodified with its own sign-off, and redding1 is credited by Co-Authored-By on the squashed commit (SPEC AC 17, amended for D-25)"
    - "Codecov reports 100% branch patch coverage on the merged report, zero partial branches, not merely 100% line coverage (SPEC AC 18, R8 boundary edge)"
    - "Either 10-UAT-RESULTS.json records a passed control run against a Thread MatrixLight, carrying that device's serial and a timestamp inside the phase window, or 10-EXCEPTION-OVERRIDE.json records an operator-approved exception with a non-empty hardware-unavailability reason and a THREAD-05 deferral reference (SPEC AC 19)"
    - "Exactly one of 10-UAT-RESULTS.json and 10-EXCEPTION-OVERRIDE.json is present, never both (SPEC AC 20, R9 empty edge: no reachable hardware routes to the recorded exception, not an indefinite block)"
    - "CI green is required in both cases; the exception waives only the hardware gate, never CI (SPEC AC 21)"
    - "Control passing while streaming fails is an explicit, allowed outcome; the streaming run is recorded with its actual result and does not gate (SPEC AC 19/21, R9 adjacency edge)"
    - "The gate artefact validators enforce every field they certify: schema_version and kind, the named device against the known Thread MatrixLight serials, a timezone-aware timestamp inside the phase window, library_head equal to the PR head, and for an exception a non-empty reason plus a THREAD-05 reference (review finding 10)"
    - "Whichever XOR artefact exists is the one staged, committed and merged, alongside 10-06-SUMMARY.md in the same pre-merge commit — no documentation commit lands on main after the merge (review findings 10 and 12)"
    - "This plan VERIFIES that 10-COVERAGE-GAPS.md is closed; it does not discover or improvise coverage work against the merge deadline (review finding 3)"
  artifacts:
    - ".planning/phases/10-land-the-ipv6-thread-branch/10-UAT-RESULTS.json (or 10-EXCEPTION-OVERRIDE.json on the v1.2 Phase 8 schema)"
    - ".planning/phases/10-land-the-ipv6-thread-branch/10-06-SUMMARY.md, committed on the feature branch in the same commit as the gate artefact, before the fast-forward"
    - "A merged PR whose head is the feature branch tip, fast-forwarded onto main locally so signatures survive"
  key_links:
    - "PR head -> UAT run (D-22: the UAT exercises the code actually about to land, after CI green, before merge)"
    - "UAT artefact commit -> merge (D-22: the artefact is the last commit before merge)"
  prohibitions:
    - statement: "MUST NOT retune any WiFi-measured reliability constant (REQUEST_RETRANSMIT_GAPS, ACK_INFLIGHT_LIMIT, ACK_EXPIRY_SECONDS, DISCOVERY_REBROADCAST_GAPS) in this phase"
      status: resolved
      verification: judgment
    - statement: "MUST NOT add a runtime dependency (pyproject.toml dependencies stays [])"
      status: resolved
      verification: judgment
    - statement: "MUST NOT reach the green gate by weakening the checks: no new pragma-no-cover on IPv6 code, no existing test deleted or skipped, no lowering of the Codecov patch target"
      status: resolved
      verification: judgment
    - statement: "MUST NOT record the hardware UAT as passed without an actual run against a Thread MatrixLight — the recorded exception is the sanctioned route when hardware is unavailable, so there is never a reason to fabricate a pass"
      status: resolved
      verification: judgment
---

<objective>
Close the phase (SPEC R8 + R9, D-18/D-22): push the stacked branch, open the single PR, drive CI to green including the 100% branch patch coverage gate, run the recorded Thread hardware UAT against the PR head, commit exactly one gate artefact, and land the series on main by local fast-forward so the commit signatures survive.

D-23 placed the UAT run and the merge outside the four development plans; this plan exists to make that closing gate an explicit, verifiable artefact with `autonomous: false`, as the SPEC's R9 acceptance criteria require. The merge is the phase's one-way decision (D-18).

Purpose: IPV6-01..04 are only delivered when the rebased, fixed branch is on main with CI green and the hardware gate resolved.
Output: merged PR, gate artefact in the phase directory, phase complete.

<review_response>
Revised 2026-08-27 from `10-REVIEWS.md`. Changes landing in this plan:

- **Finding 4 (HIGH) — the `codecov.yml` premise was factually false.** Task 1's read_first said `codecov.yml` has "no flags key on the status". It has a top-level `flags:` block at line 17 declaring five per-Python-version flags, each scoping `paths:` to `src/lifx/` and `scripts/` with `carryforward: true`. What is genuinely absent is a `flags:` key *inside* `coverage.status.patch.default`, which is why the patch status is still computed against the merged report across all five flags rather than per flag. The conclusion held; the stated reason did not. `10-SPEC.md`'s Constraints line carries the same imprecision ("with no `flags:` key"); the SPEC is locked, so this plan records the correction here rather than editing it, and the conclusion the SPEC drew is unaffected. The practical consequence matters for finding 5: because `scripts/` is inside the flag paths but outside pytest's `--cov` scope, probe changes are in Codecov's file scope but absent from the report.
- **Finding 3 (HIGH, all three reviewers) — Task 1 now verifies instead of discovering.** The branch's patch-coverage debt is measured in plan 10-01 Task 2 and written to `10-COVERAGE-GAPS.md`; plans 10-02 and 10-03 close their assigned gaps in their own commits. Task 1's job here is to confirm the list is closed before pushing. A residual gap discovered at this point is an escalation to report, not open-ended work to improvise against the merge deadline — which is exactly where the "never weaken a check" prohibition faces real pressure.
- **Finding 5 follow-through.** Task 1's local inspection previously limited itself to `src/lifx/*` and missed the changed probe script entirely. It now inspects the `scripts/` diff too, and verifies plan 10-05's recorded coverage treatment for the probe rather than reopening the question.
- **Finding 10 (MEDIUM) — the validators did not enforce what they certify.** The UAT validator checked neither the named device, the timestamp window, the schema kind, nor `library_head`; the exception validator required neither a non-empty reason nor a THREAD-05 reference. Both are now enforced in the automated check. Task 4 also previously named and staged only `10-UAT-RESULTS.json` although the XOR permits `10-EXCEPTION-OVERRIDE.json` instead; it now stages whichever exists.
- **Finding 6 (MEDIUM).** Task 2's operator command depends on `--serial`, which does not exist on the branch probe. Plan 10-05 Task 1 now adds it; Task 2 here checks it is present before the operator is asked to run anything.
- **Finding 14 (LOW).** `git log ... | head -3` checked the newest stacked commits, not the three base replay commits. Now `git log --reverse`, which is what the criterion always meant. **Superseded 2026-08-28 by D-25:** the series was squashed to two commits, so there is no longer a three-commit base to read. The check now asserts the oldest commit in `main..HEAD` is `79a5ce0` and that authorship is preserved across both commits.
- **Finding 12 (LOW) — summary sequencing, decided.** `10-06-SUMMARY.md` was required after Task 4 had already merged and pushed, so it could not land with the merge without an extra unreviewed commit on `main`. **Decision: the summary is written and committed on the feature branch in Task 4 step 1, in the same signed commit as the gate artefact, before the fast-forward.** It documents plans 10-01 to 10-06 through Task 3's approved merge decision and states that the merge is the immediately following step. The merge's own outcome is proven by Task 4's acceptance criteria and recorded in `STATE.md` on the planning branch. No post-merge commit is made to `main` for documentation, so the one-PR promise (D-18) holds.
- **Codex LOW, folded in:** commit verification uses `git verify-commit` on the exact SHAs rather than reading `git log --show-signature` output.
</review_response>
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-land-the-ipv6-thread-branch/10-SPEC.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-CONTEXT.md
@.planning/milestones/v1.2-phases/08-hardware-fidelity-validation/08-EXCEPTION-OVERRIDE.json
@codecov.yml
</context>

## Artifacts this phase produces

New symbols created across Phase 10: `lifx.network.address` module with `validate_address()`, `family_for()`, `wildcard_for()` and the `Device.connect()` address guard (plan 10-02); `tests/test_network/test_address.py` (plan 10-02); send-time family assertion and leak-safe, state-clearing `MdnsTransport.open()` (plan 10-03); conftest helpers and fixtures `get_free_port6()`, `_Ipv6EmulatedLifxServer`, `ipv6_available`, `emulator_server_ipv6`, `ipv6_light` (plan 10-04); `tests/test_api/test_ipv6_e2e.py` (plan 10-04); env var `LIFX_REQUIRE_IPV6` (plan 10-04); `scripts/ipv6_thread_probe.py` control/streaming stages, its `--serial`, `--uat-output` and `--stream` flags and its helpers `_select_target()`, `_capture_device_state()`, `_restore_device_state()`, `_stage_result()`, `_build_uat_record()`, `_write_uat_record()` (plan 10-05); test package `tests/test_scripts/` with `tests/test_scripts/test_ipv6_thread_probe.py` (plan 10-05); `10-COVERAGE-GAPS.md` (plan 10-01); `10-UAT-RESULTS.json` / `10-EXCEPTION-OVERRIDE.json` (this plan); rebased branch symbols `_LifxRecordCache`, `_pick_address()`, `build_address_query()` (plan 10-01).

<tasks>

<task type="auto">
  <name>Task 1: Verify the coverage list is closed, push, open the single PR, drive CI to green</name>
  <files>.planning/phases/10-land-the-ipv6-thread-branch/10-06-SUMMARY.md</files>
  <read_first>
    - .planning/phases/10-land-the-ipv6-thread-branch/10-CONTEXT.md D-18 (one PR: the three rebased commits plus the stacked fix/fixture commits, one CI cycle) and D-20 (do not touch the backup ref)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-COVERAGE-GAPS.md — the gap list plan 10-01 Task 2 measured, with each gap assigned to plan 10-02, 10-03 or 10-05. This task verifies it is closed; it does not rediscover it
    - .planning/phases/10-land-the-ipv6-thread-branch/10-05-SUMMARY.md — the recorded coverage treatment for `scripts/ipv6_thread_probe.py` (tested via `tests/test_scripts/test_ipv6_thread_probe.py` with a scoped assertion, deliberately not added to the global `--cov`). Confirm it is recorded; do not reopen the decision here
    - codecov.yml lines 6-42 — read it, do not assume it. `coverage.status.patch.default` sets `target: 100%` and carries NO `flags:` key of its own, so the patch status is computed against the merged report across all five Python flags and branch partials count against it. Separately, a top-level `flags:` block DOES exist at line 17, declaring `python3.10` through `python3.14`, each scoping `paths:` to `src/lifx/` and `scripts/` with `carryforward: true`. An earlier draft of this plan asserted there was no flags key at all; that was wrong (review finding 4). The practical consequence: `scripts/` is inside Codecov's flag paths but outside pytest's `--cov` scope (`pyproject.toml:117-118` covers `lifx` and `generate_theme_data` only), so probe diff lines are in scope for Codecov yet absent from the uploaded report
    - Auto-memory project_codecov_branch_patch (check branch partials, not just diff-cover lines)
    - .github/workflows/ci.yml (the jobs that must pass: quality, generated files, 3-OS x 5-version test matrix on a source-changing PR)
  </read_first>
  <precondition>Plans 10-01..10-05 are complete on feat/ipv6-thread-support; `10-COVERAGE-GAPS.md` exists on the branch; `gh auth status` succeeds; working tree clean.</precondition>
  <action>
    1. **Verify, do not discover (review finding 3).** Run `uv run --frozen pytest` (branch coverage is already configured in pyproject addopts) and check the PR-diff files in BOTH scoped directories: `git diff main --name-only -- 'src/lifx/*' 'scripts/*'` (the old instruction looked only at `src/lifx/*` and missed the probe entirely — review finding 5). Walk `10-COVERAGE-GAPS.md` and confirm every listed gap is either covered now or carries an unreachability annotation in the owning plan's summary, and that the probe's coverage treatment is recorded in `10-05-SUMMARY.md`. If a gap remains open, or a gap appears that the list never named, STOP and report it as an escalation: unbounded reactive test-writing at this point is what the review identified as the phase's biggest schedule risk and the exact moment prohibition 3 gets tested. Never close a gap with a pragma marker, a skip, a deletion or a target change.
    2. `git push --force-with-lease origin feat/ipv6-thread-support` (the remote still holds the pre-rebase history; force-with-lease is expected and safe — reflog and this PR are the recovery path per D-20).
    3. Open the PR with `gh pr create` against main, titled for the IPv6/Thread landing, body summarising: the three replayed commits, the reconciled audit fixes B1/B2/B4/B9, IPV6-04, the address helper, the ::1 fixture and CI gate, and the pending hardware UAT gate. Note in the body that merge will be a local fast-forward to preserve signatures.
    4. Watch CI: `gh pr checks --watch`. Every check must pass, including all codecov statuses. For the patch status, confirm 100% with zero partial branches (`gh api repos/{owner}/{repo}/commits/<head-sha>/status` or the codecov check output). If patch coverage fails despite step 1 passing, that is new information the local measurement missed: identify the uncovered or partially-covered branches from the codecov report, add tests on the branch, push, re-watch, and record in the summary why the local check did not catch it so the gap list mechanism can be trusted next time. Never weaken a check to pass it. Do not simplify the verify's jq gate: statusCheckRollup mixes CheckRun objects (`status`/`conclusion`) and StatusContext objects (`state`), so the filter selects a bad check via OR across both shapes, and SKIPPED conclusions are allowed because this repo's green PRs legitimately carry SKIPPED deploy jobs (validated against merged PR #207 on 2026-08-27).
  </action>
  <verify>
    <automated>gh pr view --json state,mergeable,statusCheckRollup --jq '.statusCheckRollup | map(select(((.state // "SUCCESS") != "SUCCESS") or ((.status // "COMPLETED") != "COMPLETED") or (((.conclusion // "SUCCESS") != "SUCCESS") and ((.conclusion // "") != "NEUTRAL") and ((.conclusion // "") != "SKIPPED")))) | length == 0' | grep -q true && ! gh pr checks | grep -q fail</automated>
  </verify>
  <acceptance_criteria>
    - Every gap in `10-COVERAGE-GAPS.md` is closed or annotated before the push, verified by walking the list against the local coverage run; the summary states the count checked (review finding 3: this task verifies a closed list, it does not open one)
    - The local inspection covered `scripts/` as well as `src/lifx/`, and `10-05-SUMMARY.md` records the probe's coverage treatment (review finding 5)
    - An open PR exists whose head equals `git rev-parse feat/ipv6-thread-support`
    - `gh pr checks` shows every check passing, including the codecov patch status at 100% branch coverage with zero partial branches
    - `git rev-list --reverse main..origin/feat/ipv6-thread-support | head -1` prints `79a5ce09b71ea076d1065953aee72609e154fa0a`, proving redding1's original commit is still at the BASE of the stack and unmodified (amended for D-25: the series was squashed to two commits, so the former three-subject read no longer applies; the check that it reads the OLDEST end, review finding 14, is retained)
    - `git log main..origin/feat/ipv6-thread-support --format='%(trailers:key=Co-authored-by)'` credits `redding1` on the squashed commit
    - `git verify-commit` exits 0 for every SHA in `git rev-list main..origin/feat/ipv6-thread-support`
    - codecov.yml and pyproject.toml are byte-identical to main (`git diff main -- codecov.yml pyproject.toml` empty except for any version bump semantic-release owns), and `dependencies` is still []
  </acceptance_criteria>
  <done>One PR carries the whole series, CI fully green, the branch patch gate genuinely satisfied by tests written in earlier waves rather than improvised here.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Thread hardware UAT against the PR head, artefact recorded, merge approved</name>
  <files>.planning/phases/10-land-the-ipv6-thread-branch/10-UAT-RESULTS.json</files>
  <read_first>
    - .planning/phases/10-land-the-ipv6-thread-branch/10-SPEC.md Requirement 9 and AC 19-22 (what the record must carry; what the exception must carry; exactly one file; CI never waivable)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-CONTEXT.md D-21, D-22 (probe as harness; run against PR head after CI green, before merge; artefact is the last commit before merge)
    - .planning/milestones/v1.2-phases/08-hardware-fidelity-validation/08-EXCEPTION-OVERRIDE.json (the exception schema: schema_version 1, kind operator-approved-exception, scope, decision accepted_exception, authority operator, date, facts)
    - Auto-memory `project_test_fleet` (the fleet inventory holding the two Thread MatrixLight serials the validator checks the record against)
  </read_first>
  <precondition>`uv run python scripts/ipv6_thread_probe.py --help` lists `--serial` and `--uat-output`. The branch probe has neither; plan 10-05 Task 1 adds them, and the operator command below hard-depends on `--serial` (review finding 6: one missing flag turns this blocking checkpoint into a decode-failure loop).</precondition>
  <action>
    Operator gate — the merge is blocked until this checkpoint resolves. Steps for the operator (Claude may run the probe command when asked, but only the operator confirms the result is genuine and approves the merge):

    0. Confirm the harness is present before asking the operator to touch hardware: `uv run python scripts/ipv6_thread_probe.py --help` must list `--serial`, `--uat-output` and `--stream`. If any is missing, stop — plan 10-05 did not land what this gate consumes.
    1. On the PR head (`git switch feat/ipv6-thread-support`, clean tree, same SHA CI validated), run the control UAT against one of the two Thread MatrixLights (Test Candle or Test Tube, ULA prefix fd00:1::), with the device quiesced from other pollers:
       `uv run python scripts/ipv6_thread_probe.py --serial <device-serial> --uat-output .planning/phases/10-land-the-ipv6-thread-branch/10-UAT-RESULTS.json` (add the streaming flag if a streaming artefact is wanted; its result is recorded either way and does not gate).
    2. Inspect 10-UAT-RESULTS.json: device_serial must be one of the two known Thread MatrixLight serials from the fleet inventory, schema_version 1 and kind "thread-hardware-uat", timestamp inside the phase window, library_head equal to the PR head SHA, stages.control "passed", and `restored` true. The streaming value is recorded as produced ("passed", "failed" or "not_run") and gates nothing. If `restored` is false the device was left mid-run: put it right by hand before proceeding, and say so in the checkpoint response.
    3. IF no Thread device is reachable: instead create .planning/phases/10-land-the-ipv6-thread-branch/10-EXCEPTION-OVERRIDE.json on the v1.2 Phase 8 schema with kind "operator-approved-exception", decision "accepted_exception", authority "operator", date, a non-empty reason naming why the hardware was unavailable, and a facts entry deferring the Thread control evidence to THREAD-05 in Phase 14. Exactly one of the two files may exist — never both. CI green is never waivable by this exception.
    4. Confirm the record reflects a genuine run (the SPEC prohibition on fabricated passes routes here for judgment review).

    The automated check below enforces every one of those field requirements. It previously checked only the XOR, `stages.control`, and the presence of a serial and timestamp, which meant it would have passed a record naming the wrong device, carrying a stale timestamp, or built on a different commit; and it would have accepted an exception with an empty reason and no THREAD-05 reference (review finding 10). The two known serials and the phase window are supplied to the check as literals at run time, so the check is self-contained and needs no network. **Note (2026-08-28):** `PHASE_HEAD` is the head at the moment this task runs, i.e. before Task 4's docs-only artefact commit. Re-running this validator after that commit will fail the `library_head` assertion; that is expected and is not a UAT failure, because the artefact commit changes no library code.
  </action>
  <what-built>Plans 10-01..10-05 produced the rebased branch with all fixes, the ::1 CI proof, and the UAT harness; Task 1 drove the PR to full CI green. What remains unproven is the physical Thread control path, which no automated test can reach.</what-built>
  <how-to-verify>
    1. Run the probe command in step 1 above against the Candle or the Tube and watch the control stage report
    2. Open .planning/phases/10-land-the-ipv6-thread-branch/10-UAT-RESULTS.json and check device_serial, timestamp and stages.control == "passed" (streaming is recorded but does not gate)
    3. If hardware is unreachable, write 10-EXCEPTION-OVERRIDE.json per step 3 above instead — exactly one of the two files
  </how-to-verify>
  <resume-signal>Type "uat-verified" when the record (or exception) is genuine and correctly shaped, or describe what failed</resume-signal>
  <verify>
    <human-check>Operator confirms the UAT record (or exception) is genuine, correctly shaped, and that the merge may proceed</human-check>
    <automated>THREAD_SERIALS="${THREAD_SERIALS:?set to the two Thread MatrixLight serials, comma separated, from the fleet inventory}" PHASE_HEAD="$(git rev-parse feat/ipv6-thread-support)" uv run python -c "
import datetime, json, os, pathlib
d = pathlib.Path('.planning/phases/10-land-the-ipv6-thread-branch')
uat, exc = d/'10-UAT-RESULTS.json', d/'10-EXCEPTION-OVERRIDE.json'
assert uat.exists() ^ exc.exists(), 'exactly one gate artefact must exist'
if uat.exists():
    r = json.loads(uat.read_text())
    assert r['schema_version'] == 1, 'unexpected schema_version'
    assert r['kind'] == 'thread-hardware-uat', 'wrong record kind'
    assert r['phase'] == '10', 'wrong phase'
    known = {s.strip().lower() for s in os.environ['THREAD_SERIALS'].split(',') if s.strip()}
    assert r['device_serial'].lower() in known, f\"device_serial {r['device_serial']} is not a known Thread MatrixLight\"
    ts = datetime.datetime.fromisoformat(r['timestamp'])
    assert ts.tzinfo is not None, 'timestamp must carry a timezone'
    age = datetime.datetime.now(datetime.timezone.utc) - ts
    assert datetime.timedelta(0) <= age <= datetime.timedelta(days=7), f'timestamp {ts} is outside the phase window'
    assert r['library_head'] == os.environ['PHASE_HEAD'], 'record was not produced against the PR head'
    assert r['stages']['control'] == 'passed', 'control stage did not pass'
    assert r['stages']['connect'] == 'passed', 'connect stage did not pass'
    assert r['stages']['streaming'] in ('passed', 'failed', 'not_run')
    assert r['restored'] is True, 'device state was not fully restored'
else:
    r = json.loads(exc.read_text())
    assert r['schema_version'] == 1, 'unexpected schema_version'
    assert r['kind'] == 'operator-approved-exception' and r['decision'] == 'accepted_exception'
    assert r['authority'] == 'operator'
    assert isinstance(r.get('reason'), str) and r['reason'].strip(), 'exception needs a non-empty reason'
    assert 'THREAD-05' in json.dumps(r), 'exception must reference the THREAD-05 deferral'
    datetime.date.fromisoformat(str(r['date']))
print('gate artefact ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one of 10-UAT-RESULTS.json / 10-EXCEPTION-OVERRIDE.json exists in the phase directory (XOR asserted by the verify script)
    - If UAT: the verify script's every assertion passes — schema_version 1, kind "thread-hardware-uat", phase "10", device_serial in the two known Thread MatrixLight serials, timezone-aware timestamp inside a seven-day window, library_head equal to the PR head SHA, connect and control both "passed", streaming recorded with its actual value, restored true (review finding 10: none of schema, device, timestamp window or library_head was previously enforced)
    - If exception: schema_version 1, kind/decision/authority as above, a non-empty `reason` string, an ISO date, and a THREAD-05 reference somewhere in the record (previously neither the reason nor the THREAD-05 reference was enforced)
    - The `--help` precondition passed, so `--serial` exists before the operator is asked to run anything (review finding 6)
    - The operator has confirmed the record is genuine in the checkpoint response
  </acceptance_criteria>
  <done>The hardware gate is resolved by evidence or by recorded operator exception, with every field the gate certifies actually checked.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Approve the one-way merge to main</name>
  <read_first>
    - .planning/phases/10-land-the-ipv6-thread-branch/10-CONTEXT.md D-18 (why the merge is rated one-way: once the series is on the published default branch it cannot be reshaped without force-pushing main)
    - The Task 2 checkpoint response and the gate artefact it produced (UAT record or approved exception), plus Task 1's CI-green evidence — the two facts this decision rests on
  </read_first>
  <decision>Merge the Phase 10 series to main now, or hold</decision>
  <context>D-18 rates the merge one-way: once the series is on the published default branch it cannot be reshaped without a force-push. CI is green (Task 1), the hardware gate artefact is recorded (Task 2). This is the last exit before publication.</context>
  <options>
    <option id="merge">
      <name>Merge now via local fast-forward</name>
      <pros>Phase 10 delivers; Phases 11 and 12 unblock (Phase 10 is the milestone's critical path)</pros>
      <cons>The commit series on main is final</cons>
    </option>
    <option id="hold">
      <name>Hold the merge</name>
      <pros>Time to re-examine anything the UAT surfaced</pros>
      <cons>Milestone stays blocked; the PR head may drift from the UAT-validated SHA if anything else lands</cons>
    </option>
  </options>
  <resume-signal>Type "merge" to proceed with Task 4, or "hold" with the reason</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Commit the gate artefact and the summary, then fast-forward main</name>
  <files>.planning/phases/10-land-the-ipv6-thread-branch/10-UAT-RESULTS.json, .planning/phases/10-land-the-ipv6-thread-branch/10-EXCEPTION-OVERRIDE.json, .planning/phases/10-land-the-ipv6-thread-branch/10-06-SUMMARY.md</files>
  <read_first>
    - .planning/phases/10-land-the-ipv6-thread-branch/10-CONTEXT.md D-18 (one-way rationale), D-22 (artefact is the last commit before merge)
    - Auto-memory feedback_github_merge (GitHub's rebase button strips signatures — expected behaviour, which is exactly why the merge here is a local fast-forward, not a web-UI merge)
    - The Finding 10 and Finding 12 entries in this plan's `<review_response>` (stage whichever XOR artefact exists; the summary is committed before the merge, not after)
  </read_first>
  <precondition>Task 2's UAT checkpoint resolved and Task 3's decision checkpoint answered "merge"; the PR's CI is still green on the current head.</precondition>
  <reversibility rating="one-way">Merging publishes the commit series to main; reshaping it afterwards would require force-pushing a published default branch (D-18). Task 3 is the checkpoint:decision gate for this step.</reversibility>
  <action>
    1. Commit the gate artefact and the phase summary together on the feature branch as the last commit before merge (D-22, and review finding 12's resolution). Stage **whichever XOR artefact actually exists** — `10-UAT-RESULTS.json` on the evidence path, `10-EXCEPTION-OVERRIDE.json` on the approved-exception path. Do not name only the UAT file, as an earlier draft did: the exception path would have left its artefact unstaged and unmerged. Write `10-06-SUMMARY.md` in this same commit, covering plans 10-01 to 10-06 through Task 3's approved merge decision and stating that the fast-forward is the immediately following step. Commit with `git commit -s` (sign-off and GPG signing per repository convention), then push. No documentation commit is made to `main` after the merge; the merge outcome is proven by this task's acceptance criteria and recorded in STATE.md on the planning branch, so the one-PR promise (D-18) holds.
    2. Wait for the PR checks to complete on the artefact commit (docs-only, but the head must be green).
    3. Merge by local fast-forward so every signature survives (the web UI's rebase merge would re-create the commits unsigned): `git switch main && git pull --ff-only && git merge --ff-only feat/ipv6-thread-support && git push origin main`. GitHub marks the PR merged automatically when main contains the head. If main moved since the last rebase (it should not have), stop and report rather than improvising a new rebase inside this task.
    4. Confirm delivery with a strict verifier rather than a presentation check: run `git verify-commit` over every SHA the merge added (`git rev-list <pre-merge-main-sha>..main`), which exits non-zero on any bad or missing signature; every SHA in that same `git rev-list` range shows a `Signed-off-by` trailer under `git log -1 --format='%(trailers:key=Signed-off-by)' <sha>` (SPEC AC 17, amended for D-25 — the former fixed `-25` window assumed the pre-squash stack depth and would now read commits the merge did not add).
  </action>
  <verify>
    <automated>git merge-base --is-ancestor feat/ipv6-thread-support main && git merge-base --is-ancestor 79a5ce09b71ea076d1065953aee72609e154fa0a main && gh pr view --json state --jq .state | grep -q MERGED && test 1 -eq "$(ls .planning/phases/10-land-the-ipv6-thread-branch/10-UAT-RESULTS.json .planning/phases/10-land-the-ipv6-thread-branch/10-EXCEPTION-OVERRIDE.json 2>/dev/null | wc -l | tr -d ' ')" && test -f .planning/phases/10-land-the-ipv6-thread-branch/10-06-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - main contains the full series: `git merge-base --is-ancestor feat/ipv6-thread-support main` exits 0
    - `git verify-commit` exits 0 for every SHA the merge added to main, and every one of those SHAs carries a `Signed-off-by` trailer (post-D-25 that is two commits: `79a5ce0` and the squashed commit)
    - The PR is in state MERGED with no web-UI merge commit (`git log main --merges --oneline -1` shows no new merge commit for this PR)
    - Exactly one gate artefact is present on main and it is the one Task 2 produced (UAT or exception, never both, never neither) — the verify command asserts the count is 1
    - `10-06-SUMMARY.md` is on main, having been committed before the merge rather than pushed afterwards (`git log main --format=%H -- .planning/phases/10-land-the-ipv6-thread-branch/10-06-SUMMARY.md` returns a SHA that is an ancestor of the merge point)
    - The gate artefact and the summary share the last non-planning commit before the merge point, per D-22
  </acceptance_criteria>
  <done>The rebased, fixed, tested branch is on main with signatures and DCO intact, exactly one hardware-gate artefact and the phase summary landed with it — Phase 10 delivered (SPEC AC 17-22).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> phase record | the UAT artefact asserts facts about a physical run nothing can cryptographically prove |
| feature branch -> main | publication boundary: the one-way merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-16 | Repudiation | fabricated or mis-attributed UAT pass | high | mitigate | Blocking human checkpoint; XOR artefact check; the validator now also pins schema/kind, the device against the known Thread serials, a timezone-aware timestamp inside the phase window and `library_head` against the PR head, so a record from the wrong device, the wrong day or the wrong commit cannot pass (review finding 10); honest exception route always available; SPEC prohibition routes fabrication to judgment review (Task 2) |
| T-10-17 | Tampering | signature loss at merge (web-UI rebase re-creates commits unsigned) | high | mitigate | Local --ff-only merge and push; AC 17 verified with `git verify-commit` over every SHA the merge added, which fails closed where reading `git log --show-signature` output does not (Task 4) |
| T-10-18 | Elevation of privilege | merging with a red or waived CI, or with the patch gate satisfied by weakening it under deadline pressure | high | mitigate | CI green is never waivable (SPEC AC 21); Task 1 gates on every check including the codecov branch patch status; Task 4 precondition re-checks green on the artefact commit. Plan 10-01 Task 2's up-front coverage measurement and plans 10-02/10-03's scheduled top-ups remove the late-stage pressure that made this threat live: Task 1 now verifies a closed list rather than discovering an open one (review finding 3) |
| T-10-23 | Repudiation | the approved-exception path producing an artefact that is never staged, so main records neither a pass nor a waiver | medium | mitigate | Task 4 stages whichever XOR artefact exists and the verify asserts exactly one is present on main (review finding 10) |
| T-10-SC | Tampering | package installs | low | accept | No package installed anywhere in this phase; dependencies = [] verified in Task 1 |
</threat_model>

<verification>
- SPEC AC 17-22 all satisfied on main
- 10-COVERAGE-GAPS.md verified closed before the push, across `src/lifx/` and `scripts/`
- Exactly one gate artefact in the phase directory, passing every field assertion the validator makes
- PR merged with signatures verified per commit and DCO intact; no check weakened at any point
- 10-06-SUMMARY.md landed with the merge, not after it
</verification>

<success_criteria>
Phase 10's goal is TRUE: the three-commit branch is on main with the reconciled fixes, CI green at 100% branch patch coverage, and the Thread hardware control path proven (or its absence recorded as an operator-approved exception) — a caller can control a device that has only an IPv6 address.
</success_criteria>

<output>
Create `.planning/phases/10-land-the-ipv6-thread-branch/10-06-SUMMARY.md` in Task 4 step 1, committed on the feature branch in the same signed commit as the gate artefact, BEFORE the fast-forward. It therefore lands on main with the merge and needs no follow-up commit (review finding 12: the previous wording required it after Task 4 had already merged and pushed, which could only have been satisfied by an extra unreviewed commit on main). The summary covers plans 10-01 to 10-06 through Task 3's approved merge decision; the merge's own outcome is proven by Task 4's acceptance criteria and recorded in STATE.md on the planning branch.
</output>
