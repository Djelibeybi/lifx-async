---
phase: 10-land-the-ipv6-thread-branch
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/lifx/network/address.py
  - tests/test_network/test_address.py
  - src/lifx/network/transport.py
  - src/lifx/network/connection.py
  - src/lifx/animation/animator.py
  - src/lifx/devices/base.py
  - src/lifx/api.py
  - tests/test_devices/test_base.py
  - tests/test_api/test_api_discovery.py
  - tests/test_network/test_transport.py
  - tests/test_network/test_connection.py
  - tests/test_animation/test_animator.py
autonomous: true
requirements: [IPV6-02, IPV6-03]
tags: [ipv6, address, validation, network]

estimate:
  tokens: 95000
  raw_tokens: 190000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "`Device.connect(ip=...)` with no serial validates the address before constructing a DeviceConnection, so the serial-less path cannot reach the 16-second silent timeout IPV6-02 exists to kill (review finding 9; the fourth entry point named by SPEC AC 3 as amended 2026-08-27)"
    - "Every gap that 10-COVERAGE-GAPS.md assigns to plan 10-02 is closed by a test in the same commit as the change that owns the file, with no pragma-no-cover added and no test skipped (review finding 3)"
    - "`::ffff:192.0.2.1` (IPv4-mapped IPv6) is rejected with `ValueError` by the shared helper (SPEC AC 5, R1 adjacency edge)"
    - "`\"\"` and `None` are rejected with `ValueError` by the shared helper (SPEC AC 6, R1/R3 empty edge)"
    - "`fe80::1` raises `ValueError` naming the missing zone identifier at `Device.__init__`, `Device.from_ip()`, `find_by_ip()` and `Device.connect()`, each in under 100 ms and before any socket exists; `fe80::1%en0` is accepted at all four (SPEC AC 3 as amended 2026-08-27, R2 adjacency edge)"
    - "`fe80::1%` (empty zone) raises `ValueError` (SPEC AC 4, R2 empty edge)"
    - "`FE80::1` and the fully expanded form `fe80:0:0:0:0:0:0:1` raise identically to `fe80::1` (SPEC AC 4, R2/R3 encoding edge — the helper owns normalisation)"
    - "`\"::\"` and `\"0:0:0:0:0:0:0:0\"` resolve to the same family from `family_for()` (SPEC AC 7, R3 adjacency edge)"
    - "A repository grep for the two-character colon-membership pattern under src/lifx/ returns no family-selection use (SPEC AC 8)"
    - "Every branch of the moved validation rules (loopback warning, unspecified raise, non-private warning, zone-less link-local raise, IPv4-mapped raise, empty/None raise, malformed raise) has a unit test exercising both sides, with no pragma-no-cover marker (D-04)"
  artifacts:
    - "src/lifx/network/address.py — new leaf-rule module exposing validate_address(), family_for(), wildcard_for() (D-01, D-02)"
    - "tests/test_network/test_address.py — full branch-coverage unit suite for the helper"
  key_links:
    - "devices/base.py Device.__init__ -> lifx.network.address.validate_address (the single home of address rules, D-03)"
    - "network/transport.py UdpTransport.open() -> family_for(bind address); network/connection.py DeviceConnection._open() -> wildcard_for(target); animation/animator.py send_frame -> family_for(target) (the three B9 sites)"
    - "api.py find_by_ip() first statement -> validate_address (D-07)"
    - "devices/base.py Device.connect() first statement -> validate_address (review finding 9; the serial-less leg constructs DeviceConnection before __init__ ever runs)"
  prohibitions:
    - statement: "MUST NOT retune any WiFi-measured reliability constant (REQUEST_RETRANSMIT_GAPS, ACK_INFLIGHT_LIMIT, ACK_EXPIRY_SECONDS, DISCOVERY_REBROADCAST_GAPS) in this phase"
      status: resolved
      verification: judgment
    - statement: "MUST NOT add a runtime dependency (pyproject.toml dependencies stays [])"
      status: resolved
      verification: judgment
    - statement: "MUST NOT reach the green gate by weakening the checks: no new pragma-no-cover on IPv6 code, no existing test deleted or skipped, no lowering of the Codecov patch target"
      status: resolved
      verification: judgment
---

<objective>
Create `src/lifx/network/address.py` as the single home of address-family selection and address validation (IPV6-03, audit B9), adopt it at the three socket-creation sites, and gate the four public entry points with `validate_address()` so a zone-less link-local address raises `ValueError` immediately instead of costing a silent 16-second timeout (IPV6-02, audit B2).

Implements locked decisions D-01 through D-08. The helper-before-fixes ordering is the part of D-23 that would be expensive to get wrong: plan 10-03's send-time family assertion consumes `family_for()`.

<assumption_delta_decision>
Noun promoted: the socket family is now a value derived from the address by one shared rule (`family_for()`), not a hardcoded `AF_INET` constant nor three inline per-site heuristics. Decision: **promote** — the general representation (derive-from-address) becomes primary at every socket-creation site and the per-site heuristic is deleted, not kept alongside (D-02 rationale; the add-alongside alternative is exactly the drift B9 documents). Invariant encoding: SPEC AC 8's repository grep plus the helper's unit suite.
</assumption_delta_decision>

Purpose: one implementation that cannot drift (the `theme/slug.py` precedent), and fast typed failure for a permanent configuration error.
Output: new `lifx.network.address` module + full-branch test suite, three converted call sites, four validated entry points (`Device.__init__`, `Device.from_ip()`, `find_by_ip()`, `Device.connect()`).

<review_response>
Revised 2026-08-27 from `10-REVIEWS.md`. Changes landing in this plan:

- **Finding 11 (LOW).** `validate_address`'s rule order emitted warnings before rejections, so an address that will be rejected (for example an IPv4-mapped literal that is not RFC1918-private) could emit a spurious non-private warning first. Task 1 now orders every raise ahead of every warning. No acceptance criterion changes: the rejected addresses still raise and the accepted ones still warn.
- **Finding 9 (MEDIUM) — `Device.connect()`, and why this plan guards it rather than deferring it.** On the branch, `Device.connect()` with `serial=None` builds a `DeviceConnection` directly (`devices/base.py:739-750`) and only reaches `__init__` at Step 3, so `Device.connect(ip="fe80::1")` still buys the full 16-second silent timeout that IPV6-02 exists to eliminate. **Resolved 2026-08-27 — the operator kept the guard and amended the SPEC.** `10-SPEC.md` requirement 2, its In-scope list and its Acceptance Criteria now name **four** entry points — `Device.__init__`, `Device.from_ip()`, `find_by_ip()` and `Device.connect()` — carrying an `Amended 2026-08-27` note. The amendment reconciles the enumeration with the intent the SPEC's own Interview Log already recorded ("every public entry point taking an address"); it is not a widening of scope. Guarding `connect()` is therefore now *required* by the SPEC, not an addition beyond it, and the earlier opt-out no longer applies — removing the guard would fail amended AC 3.
- **Finding 3 (HIGH).** Plan 10-01 Task 2 now measures the rebased branch's patch-coverage debt up front and writes `10-COVERAGE-GAPS.md`. Tasks 2 and 3 of this plan close every gap that file assigns to 10-02, in the same commits as the changes that own those files.
- **Codex LOW, folded in:** the `fe80::1%en0` acceptance must-have claimed every entry point but only `Device` construction was actually asserted. Task 3 now requires mocked acceptance tests at `from_ip()` and `find_by_ip()` too.
- **Antigravity LOW, REJECTED:** adding a runtime `isinstance(ip, str)` guard so `validate_address(12345)` raises rather than parsing as `0.0.48.57`. The signature is `str | None`, Pyright runs in standard mode across the repo, and no other public function in this codebase performs defensive runtime type checks on typed parameters. Adding one here would introduce a branch whose only reachable caller is a type-error the type checker already rejects, and would set a precedent the rest of the library does not follow.
</review_response>
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-land-the-ipv6-thread-branch/10-SPEC.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-CONTEXT.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-PATTERNS.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-COVERAGE-GAPS.md
@src/lifx/theme/slug.py
@src/lifx/const.py
</context>

## Artifacts this phase produces

New symbols created across Phase 10: `lifx.network.address` module with `validate_address()`, `family_for()`, `wildcard_for()` (this plan); `tests/test_network/test_address.py` (this plan); the `Device.connect()` address guard (this plan); the send-time family assertion in `UdpTransport.send()` and the leak-safe `MdnsTransport.open()` (plan 10-03); conftest helpers and fixtures `get_free_port6()`, `_Ipv6EmulatedLifxServer`, `ipv6_available`, `emulator_server_ipv6`, `ipv6_light` (plan 10-04); `tests/test_api/test_ipv6_e2e.py` (plan 10-04); env var `LIFX_REQUIRE_IPV6` (plan 10-04); `tests/test_scripts/test_ipv6_thread_probe.py` and the probe helpers `_select_target()`, `_capture_device_state()`, `_restore_device_state()`, `_stage_result()`, `_build_uat_record()`, `_write_uat_record()` (plan 10-05); `scripts/ipv6_thread_probe.py` control/streaming stages and its `--serial`, `--uat-output`, `--stream` flags (plan 10-05); `10-COVERAGE-GAPS.md` (plan 10-01); `10-UAT-RESULTS.json` / `10-EXCEPTION-OVERRIDE.json` (plan 10-06); rebased branch symbols `_LifxRecordCache`, `_pick_address()`, `build_address_query()` (plan 10-01).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lifx.network.address — the one home of the address rules — with full-branch tests</name>
  <files>src/lifx/network/address.py, tests/test_network/test_address.py</files>
  <read_first>
    - src/lifx/theme/slug.py (the leaf-rule module pattern to copy: docstring naming the call sites that must not drift, rationale comment on every constant, `from __future__ import annotations` opener)
    - src/lifx/devices/base.py lines 485-560 (the inline validation block being MOVED — the executor sees the exact current rules, dict-log shapes and pragma markers; on the rebased head these are the *(branch)* lines from 10-PATTERNS.md)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-CONTEXT.md D-01..D-06 (placement, three-function surface, full consolidation, pragma removal, serial checks stay behind, new log shape)
    - tests/test_theme/test_slug.py (unit-test shape for a leaf-rule module)
    - src/lifx/const.py line 13 (DEFAULT_IP_ADDRESS = "0.0.0.0", consumed by wildcard_for)
  </read_first>
  <behavior>
    - validate_address(None) and validate_address("") raise ValueError; message names the empty/missing address
    - validate_address("not-an-ip") raises ValueError with "Invalid IP address format" preserved from the current message
    - validate_address("fe80::1") raises ValueError whose message names the missing zone identifier (e.g. contains "zone"); validate_address("fe80::1%en0") returns None
    - validate_address("fe80::1%") raises ValueError (empty zone fails the parse)
    - validate_address("FE80::1") and validate_address("fe80:0:0:0:0:0:0:1") raise identically to "fe80::1"
    - validate_address("::ffff:192.0.2.1") raises ValueError naming IPv4-mapped rejection (detection: parsed address's ipv4_mapped attribute is not None)
    - validate_address("0.0.0.0") and validate_address("::") raise ValueError (unspecified not allowed — current rule, moved)
    - validate_address("127.0.0.1") and validate_address("::1") pass but emit the loopback warning as a dict with keys module="lifx.network.address", function="validate_address", action="is_loopback", ip=<input> (D-06 shape — class/method keys dropped)
    - validate_address("8.8.8.8") passes but emits the non-private warning with action="non_private_ip" in the same dict shape
    - validate_address("192.168.1.10") passes silently
    - Rejection precedes warning: validate_address("::ffff:8.8.8.8") raises and caplog records NO warning at all, because every raise is evaluated before either warning (review finding 11). The same holds for "0.0.0.0" and "::"
    - family_for("192.168.1.10") == socket.AF_INET; family_for("fd00:1::") == socket.AF_INET6; family_for("::") == family_for("0:0:0:0:0:0:0:0") == socket.AF_INET6; family_for(DEFAULT_IP_ADDRESS) == socket.AF_INET; family_for("fe80::1%en0") == socket.AF_INET6; family_for("bogus") raises ValueError
    - wildcard_for("fdc6::1") == "::"; wildcard_for("192.168.1.10") == DEFAULT_IP_ADDRESS
  </behavior>
  <action>
    Write the failing tests first (tests/test_network/test_address.py, mirroring test_slug.py's plain-class style with caplog assertions for the warning dicts), then implement per D-02:

    - Module: `src/lifx/network/address.py`. Imports (all at top): `from __future__ import annotations`, `import ipaddress`, `import logging`, `import socket`, `from lifx.const import DEFAULT_IP_ADDRESS`. Module-level `_LOGGER = logging.getLogger(__name__)`. Docstring per the slug.py pattern: state this is the one home of address-family selection and address validation, name the call sites (devices/base.py, api.py, network/transport.py, network/connection.py, animation/animator.py), state each rule with its rationale, and note it is a near-leaf module (its one lifx import is `lifx.const.DEFAULT_IP_ADDRESS`, needed by wildcard_for).
    - `validate_address(ip: str | None) -> None` — the entry-point gate. Rules ALL moved from Device.__init__ per D-03 plus the new ones, ordered **every rejection first, both warnings last** (review finding 11: the previous ordering let an address that was about to be rejected emit a warning on its way out): (1) empty/None -> ValueError; (2) parse with `ipaddress.ip_address(ip)`, catching ValueError and re-raising with the existing "Invalid IP address format" message — the pragma marker on this branch comes OFF (D-04); (3) IPv6 with `ipv4_mapped is not None` -> ValueError (new rule, SPEC in-scope item); (4) unspecified -> ValueError (existing message "Unspecified IP address (0.0.0.0) not allowed"); (5) IPv6 link-local with `scope_id` None -> ValueError naming the missing zone identifier (this is B2's WARNING-to-raise flip — IPV6-02); (6) loopback -> warning (new dict shape per D-06); (7) non-private -> warning. The reordering changes no accepted or rejected outcome, only whether a doomed address logs on its way to the raise. NOTE Python's `ipaddress.ip_address` accepts zoned literals like fe80::1%en0 on 3.10+ and exposes `scope_id`; an empty zone (`fe80::1%`) fails the parse and lands in rule (2), which satisfies the AC.
    - `family_for(ip: str) -> socket.AddressFamily` — parses with `ipaddress.ip_address()` (ValueError propagates for malformed input) and returns AF_INET6 for version 6 else AF_INET. It deliberately does NOT apply the entry-point rules: bind literals like "::" and DEFAULT_IP_ADDRESS are legitimate here (transport derives family from the local bind address). Document this validate/derive split in the docstrings — accepted cost per D-02 is that an address is parsed twice when a caller needs both.
    - `wildcard_for(ip: str) -> str` — returns "::" when family_for(ip) is AF_INET6, else DEFAULT_IP_ADDRESS, so DeviceConnection._open() contains no family test at all (D-02).
    - No pragma-no-cover anywhere in the new module (D-04, SPEC prohibition 3). Serial checks do NOT move (D-05).
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_network/test_address.py -q && uv run pytest tests/test_network/test_address.py --cov=lifx.network.address --cov-branch --cov-report=term-missing --cov-fail-under=100 -q</automated>
  </verify>
  <acceptance_criteria>
    - src/lifx/network/address.py contains `def validate_address(`, `def family_for(`, `def wildcard_for(` and no other public functions
    - `grep -c "pragma: no cover" src/lifx/network/address.py` prints 0
    - Coverage run reports 100% line AND branch coverage for lifx/network/address.py
    - `uv run pyright` exits 0; `uv run ruff check .` exits 0
    - caplog-based tests assert the exact warning dict keys module/function/action/ip (and the absence of class/method keys)
    - A caplog test asserts `caplog.records == []` for `validate_address("::ffff:8.8.8.8")` inside `pytest.raises(ValueError)` — proof the raise-before-warn ordering holds (review finding 11)
  </acceptance_criteria>
  <done>The helper module exists with all address rules, every branch tested from both sides, warnings in the D-06 shape, committed with its tests in one commit.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt the helper at the three socket-creation sites (B9) and close their coverage gaps</name>
  <files>src/lifx/network/transport.py, src/lifx/network/connection.py, src/lifx/animation/animator.py, tests/test_network/test_transport.py, tests/test_network/test_connection.py, tests/test_animation/test_animator.py</files>
  <read_first>
    - src/lifx/network/transport.py — UdpTransport.open() around line 295 on the rebased head (the first inline family heuristic; see 10-PATTERNS.md "transport.py" section for the exact line being replaced)
    - src/lifx/network/connection.py — DeviceConnection._open() around line 234 on the rebased head (the second site: derives a bind literal from the target)
    - src/lifx/animation/animator.py — send_frame around line 399 on the rebased head (the third site: cache-on-first-frame frame socket), plus line 147 where _addr is assigned once
    - tests/test_animation/test_animator.py — test_send_frame_uses_ipv4_socket_family and test_send_frame_uses_ipv6_socket_family (the branch's existing family tests that must keep passing unchanged)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-CONTEXT.md D-02, D-08 (which function each site calls; no per-frame check)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-COVERAGE-GAPS.md — the sections for `src/lifx/network/transport.py`, `src/lifx/network/connection.py` and `src/lifx/animation/animator.py` (the gaps plan 10-01 Task 2 assigned to 10-02 for these three files)
  </read_first>
  <action>
    Replace the inline colon-membership family heuristic at each of the three sites named in read_first with a call into `lifx.network.address` (B9, IPV6-03), importing at module top per house style. Per D-02:

    - `UdpTransport.open()`: derive the family from the **local bind** address — `family = family_for(self._ip_address)`. Keep the surrounding create_datagram_endpoint call, reuse_port guard and debug dict log exactly as the branch wrote them.
    - `DeviceConnection._open()`: derive the **bind literal** from the device's target — `local_ip = wildcard_for(self.ip)` — so _open() contains no family test at all. Everything else in the method (receiver task, _is_opening guard, debug log) stays as the branch wrote it.
    - `Animator.send_frame`: derive the frame-socket family from the **target** — `family = family_for(self._addr[0])` — keeping the cache-on-first-frame shape exactly. NO per-frame family check is added (D-08: the Phase 4 hot path stays lean; the send-time safety net is plan 10-03's B1 assertion in UdpTransport, and Animator._addr is assigned once and never reassigned, so its family is fixed for the object's lifetime — SPEC A-03).

    Update each site's inline comment to describe the derivation without restating the deleted heuristic. Do not modify the branch's existing animator family tests — they assert the constructed socket family via mock calls and must pass unchanged, which proves behaviour parity (D-19: this commit carries any test updates the removal requires; none are expected beyond parity).

    Coverage top-up in the same commit (review finding 3): close every gap `10-COVERAGE-GAPS.md` assigns to plan 10-02 for `network/transport.py`, `network/connection.py` and `animation/animator.py`, by writing tests only. If a listed gap is genuinely unreachable, say so in the summary with the reason and leave the line uncovered rather than adding a pragma marker; a pragma is the exact weakening prohibition 3 forbids. Do not touch `pyproject.toml` or `codecov.yml`.
  </action>
  <verify>
    <automated>test "$(grep -rn '":" in ' src/lifx/ | wc -l | tr -d ' ')" = "0" && uv run --frozen pytest tests/test_network/test_transport.py tests/test_network/test_connection.py tests/test_animation/test_animator.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '":" in ' src/lifx/` produces no output at all (SPEC AC 8 — the literal must not appear anywhere under src/lifx/, including comments; note this grep also counts comment text, so no replacement comment may quote the deleted pattern)
    - `grep -n "family_for" src/lifx/network/transport.py src/lifx/animation/animator.py` shows exactly one call site in each; `grep -n "wildcard_for" src/lifx/network/connection.py` shows exactly one call site
    - tests/test_animation/test_animator.py::test_send_frame_uses_ipv4_socket_family and ::test_send_frame_uses_ipv6_socket_family pass without modification
    - Every gap listed under `src/lifx/network/transport.py`, `src/lifx/network/connection.py` and `src/lifx/animation/animator.py` in 10-COVERAGE-GAPS.md is either covered by a new test in this commit or annotated in the summary with an unreachability reason — checked by re-running `uv run --frozen pytest` and confirming term-missing reports no missing line or partial branch inside this plan's diff for those three files
    - `git diff main HEAD --name-only -- pyproject.toml codecov.yml` is empty and `git diff HEAD~1 | grep -c "pragma: no cover"` prints 0 (the gate was satisfied, not weakened)
    - `uv run --frozen pytest` exits 0 (full suite — no regression at any consumer)
  </acceptance_criteria>
  <done>All three socket-creation sites derive family/bind through lifx.network.address, their assigned coverage gaps are closed, the heuristic pattern is absent from src/lifx/, full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gate the four entry points with validate_address (B2 raise included)</name>
  <files>src/lifx/devices/base.py, src/lifx/api.py, tests/test_devices/test_base.py, tests/test_api/test_api_discovery.py</files>
  <read_first>
    - src/lifx/devices/base.py — Device.__init__ validation block (lines ~485-560 on the rebased head: serial checks with pragmas ABOVE the address block, port checks BELOW it), Device.from_ip() around line 596, and Device.connect() around lines 739-750 on the rebased head, where the `if serial is None` leg constructs a DeviceConnection directly and only reaches `cls(...)` at Step 3
    - src/lifx/api.py — find_by_ip() around line 906 (signature and current body shape)
    - tests/test_devices/test_base.py — existing ValueError-on-construction tests (the shape new entry-point tests copy), the existing Device.connect() tests (whose mocking shape the new connect() test copies), and any test asserting the loopback / non-private warning dicts (those move or update to the D-06 shape)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-CONTEXT.md D-03, D-05, D-07 (full consolidation; serial checks stay; find_by_ip validates first, then falls through to today's behaviour)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-COVERAGE-GAPS.md — the sections for `src/lifx/devices/base.py` and `src/lifx/api.py`
    - The `<review_response>` block at the top of this plan, specifically the Finding 9 entry recording why `Device.connect()` is guarded here and how to opt out
  </read_first>
  <behavior>
    - Device(serial=valid, ip="fe80::1") raises ValueError naming the missing zone identifier in under 100 ms (measured with time.perf_counter around pytest.raises); Device(serial=valid, ip="fe80::1%en0") constructs
    - Device.from_ip("fe80::1") raises the same ValueError before any socket or connection object exists; Device.from_ip("fe80::1%en0") returns a Device (the acceptance side, asserted explicitly rather than assumed from the Device case)
    - await find_by_ip("fe80::1") raises the same ValueError immediately; await find_by_ip("fe80::1%en0") does NOT raise and proceeds into the existing lookup path with the network layer mocked (the acceptance side); a syntactically valid IPv6 literal such as "fdc6::1" does NOT raise and falls through to today's broadcast behaviour, returning None (D-07 — the Phase 12 gap, no interim docstring note, no LifxUnsupportedCommandError)
    - await Device.connect(ip="fe80::1") raises ValueError in under 100 ms with no DeviceConnection constructed, asserted with the transport patched so a leaked construction would be observable; await Device.connect(ip="fe80::1%en0", serial=<valid>) reaches the existing mocked path unchanged
    - Device(serial=all-zeros or broadcast ffffffffffff serial) still raises the existing serial ValueErrors (D-05 — untouched, pragmas intact)
    - Device(serial=valid, ip="192.168.1.10", port=1023) still raises the existing port ValueError (untouched)
  </behavior>
  <action>
    Write the entry-point tests first, then:

    - `Device.__init__` (devices/base.py): delete the entire inline address block (parse, loopback warn, unspecified raise, non-private warn, zone-less link-local warn) and replace it with a single `validate_address(ip)` call (D-03 — the fullest reading of IPV6-03's "exactly one implementation"). The serial checks above it and the port checks below it stay byte-identical with their existing pragma markers (D-05). Remove the now-unused `ipaddress` import if nothing else in the file uses it. This commit is B2's WARNING-to-raise flip at the Device level and carries its test updates (D-19): any existing test asserting the old class/method warning dict shape for is_loopback / non_private_ip moves to the new helper-shape or is superseded by tests/test_network/test_address.py assertions — updated in this same commit, never deleted without replacement.
    - `Device.from_ip()`: add `validate_address(ip)` as the first statement, before anything is constructed, so fe80::1 raises with no socket (SPEC AC 3 lists from_ip as its own entry point even though construction routes through __init__).
    - `find_by_ip()` (api.py): add `validate_address(ip)` as the first statement of the body, before any socket exists (D-07). No other change to the function.
    - `Device.connect()` (devices/base.py): add `validate_address(ip)` as the first statement, before Step 1's serial lookup. This closes review finding 9 — the serial-less leg builds a DeviceConnection directly and would otherwise still spend the full 16 seconds that IPV6-02 exists to eliminate. Amended SPEC AC 3 names this as the fourth required entry point, so it is not optional: omitting it fails acceptance.
    - Import `validate_address` at the top of both files.
    - Coverage top-up in the same commit (review finding 3): close every gap `10-COVERAGE-GAPS.md` assigns to plan 10-02 for `devices/base.py` and `api.py`, by writing tests only. No pragma marker, no skip, no deletion.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_devices/test_base.py tests/test_api/ tests/test_network/test_address.py -q && uv run --frozen pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - devices/base.py contains exactly three `validate_address(ip)` calls, one each inside `__init__`, `from_ip` and `connect`; api.py's find_by_ip body begins with `validate_address(ip)`
    - `grep -c "is_loopback\|non_private_ip\|link_local" src/lifx/devices/base.py` prints 0 (the grep is scoped to base.py deliberately: those three tokens legitimately appear in `src/lifx/network/address.py`, which is now their only home, and in this plan's own prose. Nothing may reintroduce them into base.py, including a comment)
    - The four sub-100 ms raise tests (`__init__`, `from_ip`, `find_by_ip`, `connect`) pass with the elapsed-time assertion in each
    - The three acceptance-side tests pass: `Device.from_ip("fe80::1%en0")` returns a Device, `find_by_ip("fe80::1%en0")` does not raise under mocked networking, and `Device.connect(ip="fe80::1%en0", serial=<valid>)` reaches the mocked path (review: the must-have claimed all entry points accept a scoped address but only Device construction was previously asserted)
    - `grep -n "pragma: no cover" src/lifx/devices/base.py` shows only the serial-check and port-check markers that predate this phase (count unchanged from the rebased head minus the removed address-block markers)
    - No missing line or partial branch remains inside this plan's diff for `devices/base.py` and `api.py`, and `git diff HEAD~1 | grep -c "pragma: no cover"` prints 0
    - Full suite exits 0
  </acceptance_criteria>
  <done>All four public IP entry points reject a zone-less link-local address immediately via the shared helper; Device.__init__ contains no address logic of its own; branch-flip tests and the assigned coverage top-up live in the same commit as the behaviour change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> Device/api entry points | caller-supplied address strings cross into socket construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | DoS (self-inflicted) | zone-less link-local address -> 16 s silent timeout | medium | mitigate | validate_address raises ValueError at all four public IP entry points before any socket: `Device.__init__`, `Device.from_ip()`, `find_by_ip()` and `Device.connect()`. The fourth was an unguarded hole the cross-AI review found (finding 9); guarding it is recorded in this plan's `<review_response>` (B2 fix, this plan's Task 3) |
| T-10-05 | Spoofing/confusion | IPv4-mapped IPv6 literals smuggling an IPv4 target through IPv6 code paths | medium | mitigate | validate_address rejects `ipv4_mapped` addresses with ValueError (Task 1, SPEC AC 5) |
| T-10-06 | Information disclosure | loopback/non-private warnings log caller-supplied IPs | low | accept | Operator-facing debug logs on a local library; same exposure as today, shape change only (D-06) |
| T-10-SC | Tampering | package installs | low | accept | No package installed; dependencies = [] unchanged |
</threat_model>

<verification>
- SPEC AC 3-8 all pass (entry-point raises, encoding/empty/mapped edges, family equivalence, repository grep)
- The fourth entry point `Device.connect()` also raises, per SPEC AC 3 as amended 2026-08-27
- Every 10-COVERAGE-GAPS.md gap owned by plan 10-02 is closed by a test, or annotated unreachable with a reason; no pragma marker added
- Full suite, pyright, ruff green
- Helper at 100% line and branch coverage with zero pragmas
</verification>

<success_criteria>
Address-family selection has exactly one implementation consumed by every socket-creation site, and a permanent configuration error fails in milliseconds with a named cause instead of a silent 16-second wait.
</success_criteria>

<output>
Create `.planning/phases/10-land-the-ipv6-thread-branch/10-02-SUMMARY.md` when done (commit on the feature branch).
</output>
