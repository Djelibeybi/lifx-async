---
phase: 11-mdns-hardening
plan: 04
type: execute
wave: 4
depends_on: [11-03]
files_modified:
  - docs/api/devices.md
  - docs/api/network.md
  - docs/api/index.md
  - docs/user-guide/advanced-usage.md
  - examples/discovery_mdns.py
  - src/lifx/api.py
  - src/lifx/network/mdns/transport.py
  - AGENTS.md
  - CLAUDE.md
autonomous: true
requirements: [MDNS-02, MDNS-08]
tags: [mdns, documentation, public-api, limitations]

estimate:
  tokens: 16000
  raw_tokens: 32000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Public device documentation describes `connectivity` as `\"thread\"` only for a positive exact Thread report and `\"wifi\"` for every other construction/discovery outcome, without naming or expanding the private wire key (D-01, D-02, D-04)."
    - "Public API, guide, index, and example pages promote only supported device-level mDNS discovery; raw service records and their generator are absent (D-03)."
    - "Private mDNS docstrings state IPv4-multicast query, ephemeral source port, legacy-unicast reply, no membership, no unsolicited announcements, no legacy-unicast cache-flush semantics, and per-call non-reusable cache state."
    - "Documentation contains no claim that the query socket joins the multicast group and no responder-population comparison probe or live-fleet evidence."
  artifacts:
    - path: "docs/api/devices.md"
      provides: "Supported public `Device.connectivity` contract"
      contains: "connectivity"
    - path: "docs/user-guide/advanced-usage.md"
      provides: "High-level `discover_mdns()` guidance and honest limitations"
    - path: "src/lifx/network/mdns/transport.py"
      provides: "Complete internal legacy-unicast transport limitations"
    - path: "examples/discovery_mdns.py"
      provides: "Device-level discovery example without raw record access"
    - path: "src/lifx/api.py"
      provides: "Supported `discover_mdns()` docstring with the public connectivity and legacy-unicast contract"
  key_links:
    - from: "docs/api/devices.md"
      to: "src/lifx/devices/base.py::Device.connectivity"
      via: "public API documentation generated from the supported property"
      pattern: "connectivity"
    - from: "docs/user-guide/advanced-usage.md"
      to: "src/lifx/api.py::discover_mdns"
      via: "supported high-level async generator example"
      pattern: "discover_mdns"
  prohibitions:
    - statement: "MUST NOT bind the mDNS query socket to port 5353 or join the multicast group, including as a fallback for unsolicited reception."
      status: resolved
      verification: "internal docstrings and documentation review"
    - statement: "MUST NOT expose `LifxServiceRecord`, `discover_lifx_services()`, `TransportMethod`, or the raw connectivity field as public API, documentation, or examples."
      status: resolved
      verification: "documentation build and active-page audit"
    - statement: "MUST NOT assert an expansion of the private connectivity abbreviation; only the exact sentinel may be described as Thread and every other case is WiFi."
      status: resolved
      verification: "wording review against D-02 and D-04"
    - statement: "MUST NOT place live serials, IP addresses, hostnames, or raw discovery output in Phase 11 tests, documentation, or evidence."
      status: resolved
      verification: "documentation fixtures and staged-diff privacy audit"
---

<objective>
Publish the supported Device-level connectivity and mDNS discovery contract while removing low-level record guidance and recording the exact limitations of the existing ephemeral legacy-unicast design.

Purpose: ensure users do not infer multicast membership, unsolicited announcements, reusable DNS cache state, or a supported raw-record API from stale prose.
Output: corrected API pages, user guide, example, internal module documentation, and repository architecture guidance.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mdns-hardening/11-SPEC.md
@.planning/phases/11-mdns-hardening/11-CONTEXT.md
@.planning/phases/11-mdns-hardening/11-RESEARCH.md
@.planning/phases/11-mdns-hardening/11-PATTERNS.md
@.planning/phases/11-mdns-hardening/11-SOURCE-AUDIT.md
@.planning/phases/11-mdns-hardening/11-01-SUMMARY.md
@docs/api/devices.md
@docs/api/network.md
@docs/user-guide/advanced-usage.md
@examples/discovery_mdns.py
@src/lifx/network/mdns/transport.py
</context>

## Artifacts this phase produces

Phase 11 produces `Device.connectivity`, `Device._connectivity`,
`Device._set_connectivity()`, `_LifxServiceRecord` with trailing defaulted
`connectivity` and `addresses` fields, `_CachedResourceRecord.expires_at`,
`_validate_txt_id()`, `_connectivity_from_txt()`,
`_LifxRecordCache._MAX_TXT_RRS_PER_OWNER`,
`_LifxRecordCache._MAX_SRV_RRS_PER_OWNER`,
`_LifxRecordCache._pending_expiries`, `_LifxRecordCache.next_expiry_delay()`,
`_LifxRecordCache.expire()`, and `_discover_lifx_services()`, while preserving the
existing public `create_device_from_record()` name and export because D-03 authorises only
the record/generator removal. It also produces the stable
diagnostic reasons `missing_txt_id`, `invalid_txt_id`, `conflicting_txt_id`,
`missing_product_id`, `invalid_product_id`, `invalid_address`, `malformed_packet`,
`rr_identity_limit`, and `unexpected_cache_flush`, plus the phase-local paths `COVERAGE.md` and
`11-SOURCE-AUDIT.md`. It adds no enum, CLI flag, persistence field, schema, dependency, or
external service integration.

<tasks>

<task type="auto">
  <name>Task 1: Document supported Device connectivity and remove raw-record guidance</name>
  <reversibility rating="one-way">D-03 deliberately removes a documented v1.x low-level API; the user already locked the v2.0 break, so no decision checkpoint is repeated.</reversibility>
  <files>docs/api/devices.md, docs/api/network.md, docs/api/index.md, docs/user-guide/advanced-usage.md, src/lifx/api.py</files>
  <read_first>
    - .planning/phases/11-mdns-hardening/11-SPEC.md (requirement 8; AC5, AC23-AC25)
    - .planning/phases/11-mdns-hardening/11-CONTEXT.md (D-01 through D-04 and public wording boundaries)
    - .planning/phases/11-mdns-hardening/11-PATTERNS.md (exact documentation removal targets)
    - docs/api/devices.md (base Device and property sections)
    - docs/api/network.md (low-level mDNS directives)
    - docs/api/index.md (architecture tree and quick-reference entries)
    - docs/user-guide/advanced-usage.md (high- and low-level mDNS examples)
    - src/lifx/api.py (`discover_mdns()` public docstring)
  </read_first>
  <action>
    Add `Device.connectivity` to the public Device property documentation using only the two exact lowercase values. Describe `"thread"` as the result of an exact positive Thread report and `"wifi"` as every other outcome, including ordinary non-mDNS construction; do not name, expose, or expand the private wire metadata. Remove the low-level service-record/generator API directives, architecture entries, and raw-data guide row. Retain `discover_mdns()` as the supported opt-in high-level path. Update `src/lifx/api.py::discover_mdns` so its rendered public docstring states the same connectivity and legacy-unicast limitations rather than merely saying multicast must be accessible. Use these exact approved phrases once in the public guidance so Plan 11-06 can assert them structurally: `ephemeral source port`, `legacy-unicast replies`, `does not join the multicast group`, `does not receive unsolicited announcements`, and `mesh scale is proven synthetically`. Check the Zensical navigation for orphaned anchors or entries and rely on the strict clean build as the executable gate. Task 2 migrates the example in the same plan. Do not add default merged discovery guidance, targeted IPv6 lookup, responder-population comparison, hardware claims, Thread tuning, or downstream Home Assistant instructions. Use Australian English and synthetic/documentation-range examples only; do not edit generated `docs/changelog.md` or historical planning files. Addresses review concerns: the supported public function owns its contract, approved exact phrases replace a broad negation heuristic, and local verification matches CI's strict build.
  </action>
  <verify>
    <automated>uv run zensical build --clean --strict &amp;&amp; uv run llmstxt-standalone build</automated>
  </verify>
  <acceptance_criteria>
    - Public docs expose only the supported high-level mDNS path and Device property
    - The exact two connectivity values are documented without the raw key or any invented expansion
    - Raw-record API directives, guide prose, index entries, example function, and example invocation are gone
    - The limitations accurately state ephemeral legacy-unicast reception and absence of membership/unsolicited announcements
    - The rendered `discover_mdns()` API reference carries the same supported Device-level contract and no stale low-level wording
    - No Phase 12-14, live-hardware, external-service, or private-identifier material is introduced
    - Both documentation builds pass without warnings caused by removed references
  </acceptance_criteria>
  <done>A caller sees an honest supported Device-level API and cannot mistake private mDNS records or transport details for public contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Align the supported example and private architecture documentation</name>
  <reversibility rating="reversible">Private docstrings and architecture prose can be corrected locally without migration or public API break.</reversibility>
  <files>examples/discovery_mdns.py, src/lifx/network/mdns/transport.py, AGENTS.md, CLAUDE.md</files>
  <read_first>
    - .planning/phases/11-mdns-hardening/11-SPEC.md (requirement 8 and Phase 11 exclusions)
    - .planning/phases/11-mdns-hardening/11-CONTEXT.md (canonical references and exact limitation list)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-08-SUMMARY.md (transport lifecycle invariants to preserve)
    - src/lifx/network/mdns/transport.py (existing accurate overview)
    - examples/discovery_mdns.py (high-level and raw-record functions)
    - AGENTS.md (network-layer inventory and privacy rules)
    - CLAUDE.md (mDNS architecture inventory)
  </read_first>
  <action>
    Remove `discover_raw_records()` and its invocation from `examples/discovery_mdns.py`, retain the supported high-level call, and show `device.connectivity` in its result handling. After Plan 11-03 lands, extend the internal mDNS/transport docstrings to state the implemented phase contract. Use these exact approved phrases once in `src/lifx/network/mdns/transport.py`: `IPv4 multicast query`, `ephemeral source port`, `legacy-unicast replies`, `does not join the multicast group`, `does not receive unsolicited announcements`, `cache-flush semantics do not apply`, and `cache state is scoped to one discovery call`. Update AGENTS.md and CLAUDE.md architecture inventories to private record/generator terminology and the same truthful behaviour, while preserving their coding, privacy, testing, and Phase 10 lifecycle guidance. Do not claim that the socket listens on 5353, receives multicast announcements, caches across calls, authenticates mDNS identities, expands the private abbreviation, or probes the responder population. Change the transport file's docstrings/prose only; preserve every socket operation, branch, and state transition. Plan 11-06 re-checks these approved phrases against the code/docs cutovers from parallel Wave 4 Plans 11-04 and 11-05. Addresses review concerns: removes the docs-before-behaviour window, places documentation and code cutover in the same execution wave, and replaces the reviewed broad regex/negation heuristic with exact phrase assertions.
  </action>
  <verify>
    <automated>uv run ruff check src/lifx/api.py src/lifx/network/mdns/transport.py examples/discovery_mdns.py &amp;&amp; uv run ruff format --check src/lifx/api.py src/lifx/network/mdns/transport.py examples/discovery_mdns.py &amp;&amp; uv run zensical build --clean --strict</automated>
  </verify>
  <acceptance_criteria>
    - Internal docstrings contain all seven required transport/cache limitations
    - AGENTS.md and CLAUDE.md no longer describe the raw record or generator as public
    - The example contains only supported high-level device discovery and displays `device.connectivity`
    - No text claims membership, port-5353 binding, unsolicited reception, cross-call caching, or mDNS authentication
    - Existing transport socket operations, branches, and state transitions are unchanged by this task
    - Australian English and privacy constraints are preserved
  </acceptance_criteria>
  <done>Private implementation and repository guidance now describe the same ephemeral legacy-unicast, per-call-cache design that the tests prove.</done>
</task>

</tasks>

<threat_model>
## Security Profile

- `asvs_level: L1` (opportunistic)
- `block_threshold: high`; any unresolved critical/high threat blocks plan completion

## Trust Boundaries

| Boundary | Description |
|---|---|
| Internal implementation facts -> public documentation | Incorrect wording could expose private fields or cause callers to depend on unsupported transport behaviour |
| Synthetic examples -> published artefacts | Example data could accidentally disclose live infrastructure identifiers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-15 | Information Disclosure | public docs/examples | high | mitigate | Remove raw-record API and private metadata; use synthetic examples only (Task 1) |
| T-11-16 | Spoofing | documentation authenticity claims | high | mitigate | Explicitly state diagnostic connectivity and legacy-unicast limitations without claiming authentication (both tasks) |
| T-11-17 | Repudiation | stale multicast-membership claims | medium | mitigate | Align public and private prose with socket-level tests and source behaviour (Task 2) |
| T-11-SC | Tampering | package supply chain | low | accept | Documentation uses existing locked tools and adds no dependency |
</threat_model>

## Multi-Source Coverage Audit

The complete audit is in `11-SOURCE-AUDIT.md`. This plan covers MDNS-02 documentation,
MDNS-08, D-01 through D-04 public wording, and prohibitions P1-P3 and P6. The SPEC's R8 empty
and encoding edges are dismissed because this is a static factual contract, not runtime input.

<verification>
- `uv run zensical build --clean --strict`
- `uv run llmstxt-standalone build`
- `uv run ruff check src/lifx/api.py src/lifx/network/mdns/transport.py examples/discovery_mdns.py`
- `uv run ruff format --check src/lifx/api.py src/lifx/network/mdns/transport.py examples/discovery_mdns.py`
</verification>

<success_criteria>
Public material documents only supported device-level discovery/connectivity, private material states every legacy-unicast limitation, and no stale low-level API, multicast-membership, metadata-expansion, live-identifier, or responder-probe claim remains.
</success_criteria>

<output>
Create `.planning/phases/11-mdns-hardening/11-04-SUMMARY.md` when done.
</output>
