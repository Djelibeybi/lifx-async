---
phase: 11-mdns-hardening
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - src/lifx/network/mdns/discovery.py
  - tests/test_network/test_mdns/test_discovery.py
autonomous: true
requirements: [MDNS-03, MDNS-04, MDNS-06, MDNS-07]
tags: [mdns, rfc6762, expiry, diagnostics, synthetic-scale]

estimate:
  tokens: 31000
  raw_tokens: 62000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Two simultaneous discovery calls have independent cached records, pending targets, resolved-instance state, expiry state, serial dedupe, and diagnostic counters."
    - "A successfully sent follow-up target is not queried again; failed sends stop after two attempts per hostname and no more than 64 distinct hostnames are admitted per call."
    - "A later A or AAAA response completes an instance whose SRV target was previously missing an address."
    - "TTL-zero TXT, SRV, A, and AAAA records receive one second of goodbye grace; an identical positive reannouncement rescues the record, while expiry removes only the matching identity (D-11)."
    - "An empty packet is a no-op and cannot change resolution, expiry, emission, or deadline state."
    - "Rescue and expiry outcomes derive from current live RR identity state rather than packet arrival winning a historical conflict."
    - "Repeated goodbye, rescue, replay, and unexpected cache-flush inputs are idempotent apart from bounded diagnostic counts."
    - "Every discovery call starts with empty record, expiry, conflict, emission, follow-up, and diagnostic state; nothing survives across calls."
    - "A live TXT-ID, same-ID construction-metadata, or same-instance SRV target/port conflict may recover only after goodbye expiry leaves one consistent value, without re-emitting a serial already yielded (D-12)."
    - "Goodbye scheduling may shorten a receive wait but never extends or resets the caller's overall or idle deadline (D-13)."
    - "An unexpected cache-flush bit is processed as ordinary record data, never replaces cached siblings, and increments `unexpected_cache_flush` once in the aggregate count (D-14)."
    - "A timeout wake processes every due cause in deterministic order: elapsed expiries first, then a due retransmit, before re-evaluating the caller deadline."
    - "A goodbye for an RR identity that is not live is a no-op; delayed loop wake-up expires records when `now >= expires_at`, never by float equality."
    - "A receive timeout with no retransmit and no pending expiry ends collection; a pending expiry keeps the loop alive only within the unchanged idle/overall deadline."
    - "The rejection summary is emitted exactly once from `finally` on normal timeout, early generator close, or unexpected failure, and unexpected implementation exceptions still propagate."
    - "Expiry and diagnostics preserve Plan 11-02's independent first-admitted 16-TXT/16-SRV per-owner ceilings, while admitted A/AAAA addresses remain uncapped per D-05."
    - "Expiry scheduling indexes only identities with a pending TTL-zero grace deadline, so retaining many ordinary A/AAAA records does not make every receive-loop wake scan the full cache."
  artifacts:
    - path: "src/lifx/network/mdns/discovery.py"
      provides: "Per-sweep monotonic goodbye scheduler, aggregate diagnostic counter, and bounded mesh-scale generator loop"
      contains: "unexpected_cache_flush"
    - path: "tests/test_network/test_mdns/test_discovery.py"
      provides: "Deterministic expiry/rescue, diagnostics privacy, concurrency, permutation, and follow-up traffic proofs"
  key_links:
    - from: "src/lifx/network/mdns/discovery.py::_CachedResourceRecord.expires_at"
      to: "src/lifx/network/mdns/discovery.py::discover_lifx_services"
      via: "nearest expiry clamps transport receive timeout inside `IdleDeadline.remaining()`"
      pattern: "next_expiry"
    - from: "src/lifx/network/mdns/discovery.py::_LifxRecordCache.pending_targets"
      to: "src/lifx/network/mdns/discovery.py::build_address_query"
      via: "separate attempt and successful-send ledgers"
      pattern: "query_attempts"
  prohibitions:
    - statement: "MUST NOT apply cache-flush replacement semantics to legacy-unicast responses."
      status: resolved
      verification: "ordinary-processing and aggregate-counter tests"
    - statement: "MUST NOT allow connectivity metadata to influence address selection, routing, connection behaviour, retries, or tuning during Phase 11."
      status: resolved
      verification: "follow-up and deadline tests are invariant across connectivity values"
    - statement: "MUST NOT place live serials, IP addresses, hostnames, or raw discovery output in Phase 11 tests, documentation, or evidence."
      status: resolved
      verification: "aggregate schema assertions and staged-diff privacy audit"
---

<objective>
Complete the per-sweep cache contract with RFC 6762 goodbye grace/rescue, privacy-safe aggregate diagnostics, deadline-safe expiry wake-ups, concurrent-call isolation, packet-order proof, and bounded follow-up address traffic.

Purpose: prove the mDNS leg at synthetic mesh scale without introducing persistent cache state, hardware dependence, or default-discovery changes.
Output: timed live-record semantics, one aggregate diagnostic event, and synthetic generator tests for every remaining MDNS-03/04/06/07 edge.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mdns-hardening/11-SPEC.md
@.planning/phases/11-mdns-hardening/11-CONTEXT.md
@.planning/phases/11-mdns-hardening/11-RESEARCH.md
@.planning/phases/11-mdns-hardening/11-PATTERNS.md
@.planning/phases/11-mdns-hardening/11-SOURCE-AUDIT.md
@.planning/phases/11-mdns-hardening/11-02-SUMMARY.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-07-SUMMARY.md
@src/lifx/network/mdns/discovery.py
@tests/test_network/test_mdns/test_discovery.py
</context>

## Artifacts this phase produces

Phase 11 produces `Device.connectivity`, `Device._connectivity`,
`Device._set_connectivity()`, `_LifxServiceRecord` with trailing defaulted
`connectivity` and `addresses` fields, `_CachedResourceRecord.expires_at`,
`_validate_txt_id()`, `_connectivity_from_txt()`,
`_LifxRecordCache._MAX_TXT_RRS_PER_OWNER`,
`_LifxRecordCache._MAX_SRV_RRS_PER_OWNER`,
`_LifxRecordCache._pending_expiries`, `_LifxRecordCache.next_expiry_delay()`,
`_LifxRecordCache.expire()`, and `_discover_lifx_services()`, while preserving the
existing public `create_device_from_record()` name and export because D-03 authorises only
the record/generator removal. It also produces the stable
diagnostic reasons `missing_txt_id`, `invalid_txt_id`, `conflicting_txt_id`,
`missing_product_id`, `invalid_product_id`, `invalid_address`, `malformed_packet`,
`rr_identity_limit`, and `unexpected_cache_flush`, plus the phase-local paths `COVERAGE.md` and
`11-SOURCE-AUDIT.md`. It adds no enum, CLI flag, persistence field, schema, dependency, or
external service integration.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schedule exact-RR goodbye expiry and rescue inside caller deadlines</name>
  <reversibility rating="reversible">The pending-goodbye index is per-sweep private state and leaves caller deadlines and persisted/public contracts unchanged.</reversibility>
  <files>src/lifx/network/mdns/discovery.py, tests/test_network/test_mdns/test_discovery.py</files>
  <read_first>
    - .planning/phases/11-mdns-hardening/11-SPEC.md (requirement 7; AC17, AC19, AC21-AC22)
    - .planning/phases/11-mdns-hardening/11-CONTEXT.md (D-11 through D-14)
    - .planning/phases/11-mdns-hardening/11-RESEARCH.md (Patterns 1 and 3; Pitfalls 2 and 3; A3 and A8)
    - .planning/phases/11-mdns-hardening/11-PATTERNS.md (deadline and deterministic clock seams)
    - src/lifx/network/mdns/discovery.py (`_CachedResourceRecord`, cache resolve state, receive timeout calculation)
    - src/lifx/network/utils.py (`IdleDeadline` contract)
    - tests/test_network/test_mdns/test_discovery.py (`_fake_deadline`, `_fake_transport`, retransmission tests)
  </read_first>
  <behavior>
    - Test 1: a TTL-zero TXT/SRV/A/AAAA marks only the identical live RR for expiry at monotonic now plus one second
    - Test 2: an identical positive-TTL reannouncement before expiry clears the grace deadline and rescues the RR
    - Test 3: expiry fires without needing an unrelated packet and can recover unresolved TXT-ID, TXT product/firmware/connectivity, and SRV target/port conflicts only after one consistent live value remains
    - Test 4: goodbye/rescue after emission cannot retract or re-emit the immutable yielded record
    - Test 5: receive timeout is the minimum of caller remaining time, retransmit slot, and nearest expiry; expiry never calls `mark_response()`
    - Test 6: repeated goodbye/rescue and empty packets are stable and each new cache starts empty
    - Test 7: a goodbye for an uncached identity is a no-op, while delayed wake-up expires every identity whose deadline is less than or equal to current monotonic time
    - Test 8: when expiry and retransmit are due together, the loop expires all due RRs first, performs the due retransmit second, then re-evaluates the unchanged caller deadline
    - Test 9: a timeout with neither pending expiry nor retransmit ends collection, while a pending expiry can keep collection alive only until the original idle/overall deadline
    - Test 10: a pending-goodbye TXT/SRV identity continues consuming one of its 16 slots until expiry; after expiry the non-evicting first-admitted policy admits the next qualifying identity
    - Test 11: a cache containing a large ordinary A/AAAA set and one goodbye-pending identity computes and processes expiry from the pending-expiry index only; instrumentation proves ordinary non-expiring addresses are not visited by `next_expiry_delay()` or `expire()`
  </behavior>
  <action>
    Write deterministic tests with a module-local fake monotonic clock, mocked `IdleDeadline`, scripted transport barriers, and no real one-second sleep. Advance the fake clock by explicit test-controlled increments before releasing the receive barrier, then yield one event-loop tick so timeout handling observes the intended instant deterministically. Add optional `expires_at` to `_CachedResourceRecord`. Maintain a separate per-cache pending-expiry index keyed by complete RR identity; only TTL-zero-marked live identities enter it, an identical positive reannouncement removes its pending entry, and expiry removes both the due record and index entry. A goodbye for an identity not already cached is a no-op. `next_expiry_delay(now)` and `expire(now)` inspect only the pending-expiry index, never all ordinary cached RRs; use `now >= expires_at` and process every due identity. A direct instrumented scale regression must populate many ordinary A/AAAA records plus one pending goodbye and prove scheduler traversal remains proportional to goodbye-pending identities, eliminating OpenCode's CPU residual while retaining every address per D-05. A TXT/SRV identity awaiting goodbye expiry remains live and counts against its Plan 11-02 per-owner/per-type ceiling until removed; after removal, the next candidate is admitted under the same non-evicting first-admitted rule. Add expiry sequences for a conflicting TXT ID, each construction-relevant same-ID TXT field (product, firmware, connectivity), and each same-instance SRV endpoint field (target, port). Use deliberately lower-sorting conflicting RRs and prove the instance stays unresolved during grace, then resolves only after expiry leaves one consistent construction tuple or endpoint. In the receive loop clamp the existing remaining timeout by retransmission and nearest expiry. On each `LifxTimeoutError`, process all due causes rather than choosing one: expire all elapsed identities first, issue a due retransmit second, then re-evaluate `IdleDeadline`. Preserve the existing clean termination when neither a retransmit nor an expiry remains; a pending expiry may keep the loop alive only inside the original caller deadline. Do not call `mark_response()` for clock-only work, extend either deadline, sleep, create detached callbacks, or implement natural positive-TTL expiry beyond the locked goodbye contract. Preserve `_resolved_instances` and serial dedupe so a yielded record is never retracted or emitted twice. Ensure conflict recovery is a fresh fail-closed derivation from all current live TXT and SRV RRs; ordering may stabilise storage or presentation but cannot choose the effective source. Addresses review concerns: removes the O(total cached RRs) scheduler interaction, makes mocked-clock/event-loop advancement explicit, and pins simultaneous events, uncached goodbyes, delayed wake-ups, cache-bound interactions, non-displacement recovery, and caller-visible termination.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py -k 'goodbye or rescue or expiry or deadline' -q</automated>
  </verify>
  <acceptance_criteria>
    - All four scoped RR types receive exactly one second of grace keyed to complete identity
    - Reannouncement rescues only an identical pending RR and expiry needs no unrelated packet
    - TXT-ID, TXT construction-metadata, and SRV target/port conflict recovery occurs only after live state leaves one consistent value, independent of prior arrival or lexicographic order
    - Emitted records remain exact-once through goodbye, rescue, and expiry
    - Expiry can only shorten receive waiting; overall and idle deadlines are unchanged
    - Every due event on one timeout wake is processed in the specified order, and loop termination is unchanged when no timed work remains
    - Uncached goodbyes are no-ops and delayed wake-up uses an inequality comparison to expire all elapsed identities
    - Goodbye grace cannot bypass or inflate the TXT/SRV ceilings, and expiry releases a non-address slot only when the live identity is actually removed
    - Tests contain no wall-clock sleep, daemon, network, or hardware dependency
    - Scheduler cost depends on pending goodbye identities, not on the number of ordinary retained A/AAAA records
  </acceptance_criteria>
  <done>Goodbye and rescue semantics are exact, deterministic, per-sweep, and subordinate to caller deadlines, including recoverable identity conflicts and exact-once emission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit one bounded privacy-safe rejection summary per sweep</name>
  <reversibility rating="reversible">The stable reason vocabulary and internal DEBUG schema are private diagnostics and require no external migration.</reversibility>
  <files>src/lifx/network/mdns/discovery.py, tests/test_network/test_mdns/test_discovery.py</files>
  <read_first>
    - .planning/phases/11-mdns-hardening/11-SPEC.md (AC18, AC20-AC21, AC25)
    - .planning/phases/11-mdns-hardening/11-CONTEXT.md (D-08, D-09, D-10, D-14)
    - .planning/phases/11-mdns-hardening/11-RESEARCH.md (Pattern 5, Pitfall 6, A5 and the first Resolved Research Question)
    - .planning/phases/11-mdns-hardening/11-PATTERNS.md (privacy-safe `caplog` analogue and logging schema)
    - src/lifx/network/mdns/discovery.py (existing structured logging and recoverable parse-error path)
    - tests/test_network/test_mdns/test_discovery.py (caplog and invalid-record tests)
  </read_first>
  <behavior>
    - Test 1: zero or multiple rejection reasons produce exactly one DEBUG summary at generator close, with deterministic reason-then-type ordering and bounded reason/type/count entries
    - Test 2: event keys and values contain no serial, IP, hostname, TXT value, instance, hash, exception string, or raw packet
    - Test 3: exact locked reason `unexpected_cache_flush` increments for each observed bit while the associated RR remains usable and does not replace siblings
    - Test 4: `ValueError`, `IndexError`, and `struct.error` from recoverable parsing contribute `malformed_packet` with record type `PACKET` rather than an identifier-bearing per-packet event
    - Test 5: missing/malformed/unrecognised/non-2 connectivity metadata contributes no rejection count
    - Test 6: separate and concurrent generator calls emit independent summaries
    - Test 7: breaking from the async generator and calling `aclose()` still emits the summary exactly once
    - Test 8: a `RuntimeError` injected into cache processing reaches the caller after the summary is emitted instead of being relabelled as malformed input
    - Test 9: each replayed observation of the same rejected over-cap TXT/SRV candidate increments `rr_identity_limit` again with type `TXT` or `SRV` only; diagnostic key cardinality stays fixed and no owner, rdata, TXT value, hostname, or rejected identity appears
    - Test 10: a syntactically invalid advertised address, including a packet-source fallback that fails final selected-address validation, contributes `invalid_address` to the same aggregate with no address/serial fields, while a factory-raised `ValueError` representing an implementation defect propagates
    - Test 11: the existing unrelated-query-failure regression positively asserts the aggregate summary schema/counts and usable resolved record rather than passing vacuously because the old `parse_error` action disappeared
  </behavior>
  <action>
    Create one per-generator `Counter[tuple[str, str]]` and thread it through cache validation without retaining rejected values. Freeze the stable reasons `missing_txt_id`, `invalid_txt_id`, `conflicting_txt_id`, `missing_product_id`, `invalid_product_id`, `invalid_address`, `malformed_packet`, `rr_identity_limit`, and locked `unexpected_cache_flush`; use stable DNS record-type names and `PACKET` only for whole-packet parse failures. Do not add a per-record product-consensus or SRV-consensus diagnostic: construction disagreement is live resolution ambiguity, not permission to discard one attacker-controlled candidate. Preserve Plan 11-02's accounting exactly: every observation of an unseen TXT/SRV candidate while its per-owner/per-type set already contains 16 live identities increments the integer `rr_identity_limit` count for `TXT` or `SRV`, including replay of the same rejected candidate; refreshes and all A/AAAA records do not increment it. When any RR carries the high class bit, increment the locked reason and process the RR normally with the bit masked for identity; do not flush, replace, or reject cached siblings. Import `struct` at module top and narrow only the DNS parse boundary to the exact recoverable tuple `except (ValueError, IndexError, struct.error)`, following the existing probe precedent. Count those failures as `malformed_packet`/`PACKET`; let cache, resolution, and construction defects propagate with their original identity. Replace the identifier-bearing `parse_error` log with this aggregate path. Also remove the separate identifier-bearing `invalid_address` event in `discover_devices_mdns`: syntactically invalid A/AAAA input, including a packet-source fallback that fails final selected-address validation, must be rejected and counted inside the cache before record emission, while an unexpected `ValueError` from `create_device_from_record()` propagates as an implementation defect rather than being logged with serial/address fields. Update `test_resolved_record_survives_unrelated_query_failure` (or its current equivalent) to positively assert the usable record plus the exact aggregate action/schema/count, so removal of `parse_error` cannot make it pass vacuously. Wrap the sweep body in `try/finally` and emit exactly one synchronous dictionary-shaped `_LOGGER.debug()` event with no `await` on normal deadline exhaustion, early `aclose()`, cancellation, or unexpected failure, including an empty bounded collection when no rejection occurred. Sort entries by reason then record type. The event contains only a fixed action/class plus reason, type, and count scalars. Do not include source address, record owner, serial, metadata, raw bytes, hash, exception text, or live values. Do not count any connectivity fallback. Addresses review concerns: names the complete parser exception tuple, aggregates both identifier-bearing log paths, makes the former negative assertion non-vacuous, pins replay counting, and keeps finalisation safe during `GeneratorExit`.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py -k 'diagnostic or cache_flush or malformed_packet or privacy' -q</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one summary is emitted per sweep at DEBUG, including normal exhaustion, zero-count, early-close, and failure paths, with deterministic reason-then-type ordering
    - The payload schema is structurally incapable of carrying a live identifier or rejected value
    - Unexpected cache-flush observations neither replace nor reject an otherwise usable RR
    - Repetition affects only bounded counts and does not alter cache semantics
    - Connectivity fallback is absent from all rejection reasons
    - TXT/SRV limit events use only `rr_identity_limit`, `TXT`/`SRV`, and integer counts; A/AAAA retention never contributes this reason
    - Replaying the same rejected over-cap identity increments count magnitude on every observation without increasing diagnostic key cardinality or changing cache membership
    - Invalid advertised addresses join the same reason-only summary; no per-record serial/address event remains, and unexpected factory defects propagate
    - Recoverable packet handling catches exactly `ValueError`, `IndexError`, and `struct.error`; broader implementation failures are not swallowed
    - Unrelated unexpected exceptions still propagate rather than being hidden in the summary
  </acceptance_criteria>
  <done>Operators receive one useful bounded reason/type/count summary per sweep without leaking LAN identity or applying multicast-only cache semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove packet permutations, concurrent isolation, and bounded follow-up completion</name>
  <reversibility rating="reversible">This preserves existing private follow-up bounds and adds synthetic evidence without changing the public discovery entry point.</reversibility>
  <files>src/lifx/network/mdns/discovery.py, tests/test_network/test_mdns/test_discovery.py</files>
  <read_first>
    - .planning/phases/11-mdns-hardening/11-SPEC.md (requirements 3 and 4; AC6-AC11 and AC22)
    - .planning/phases/11-mdns-hardening/11-CONTEXT.md (cross-packet, per-call, and traffic-bound contracts)
    - .planning/phases/11-mdns-hardening/11-PATTERNS.md (scripted transport, follow-up ledger, exact-once analogues)
    - .agents/skills/spike-findings-lifx-async/references/discovery.md (async generator and first-wins dedupe; do not import broadcast scheduling)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-07-SUMMARY.md (successful-send and attempt ledger baseline)
    - src/lifx/network/mdns/discovery.py (`pending_targets`, query ledgers, yield-before-send ordering)
    - tests/test_network/test_mdns/test_discovery.py (`TestMdnsFollowUpAddressQueries`, retransmission and serial dedupe tests)
  </read_first>
  <behavior>
    - Test 1: every selected TXT/SRV/A/AAAA packet permutation, with empty and replay packets interleaved, yields one equal record
    - Test 2: two simultaneous generators with disjoint scripted responses cannot complete one another's instances or share counts/ledgers
    - Test 3: a missing target sends exact combined A/AAAA query bytes and a later A or AAAA response yields the pending instance
    - Test 4: successful send is one-shot, transient failure retries once, persistent failure stops after two attempts
    - Test 5: the 65th distinct unresolved hostname is never sent and failed targets still consume admission capacity
    - Test 6: resolved records are yielded before an unrelated follow-up send failure and remain exact-once
  </behavior>
  <action>
    Expand the scripted generator tests from cache-level cases into complete receive-loop permutations. Instantiate all cache, expiry, serial, pending-target, attempt, successful-send, and diagnostic state inside each `_discover_lifx_services()` invocation. Preserve Phase 10's ordering: update cache, resolve and yield new serials before auxiliary sends. Retain separate `query_attempts` and `queried_targets`; admit at most 64 distinct hostnames, increment before each send, stop after two attempts, and suppress every target after one successful send. Assert `build_address_query(target)` bytes directly, then feed a later address response and observe the record. Run two generators concurrently using separately patched transport instances and event-controlled interleaving to prove no cross-call state. Do not import broadcast rebroadcast scheduling, use `asyncio.TaskGroup`, merge with `discover()`, query IPv6 multicast, run the hardware probe, or change any retry/tuning constant.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py::TestMdnsFollowUpAddressQueries tests/test_network/test_mdns/test_discovery.py::TestMdnsSerialDeduplication -q</automated>
  </verify>
  <acceptance_criteria>
    - Packet boundaries, permutation, empty packets, duplicate replay, and unrelated incomplete instances do not change the resolved identity/address set/class
    - Concurrent calls share no mutable state and each emits only its own diagnostic summary
    - Follow-up A/AAAA bytes and later completion are directly asserted
    - Two attempts and 64 admitted targets are hard caps, with no repeat after success
    - Resolved records survive unrelated auxiliary send failures and emit once
    - No Phase 12, 13, 14, live-hardware, or broadcast-schedule behaviour enters the diff
  </acceptance_criteria>
  <done>The complete mDNS generator is synthetically proven across mesh-shaped packet splits, simultaneous calls, exact-once delivery, and strictly bounded follow-up traffic.</done>
</task>

</tasks>

<threat_model>
## Security Profile

- `asvs_level: L1` (opportunistic)
- `block_threshold: high`; any unresolved critical/high threat blocks plan completion

## Trust Boundaries

| Boundary | Description |
|---|---|
| Untrusted legacy-unicast DNS packets -> timed per-sweep cache | TTL, class bits, duplicates, and malformed data drive cache transitions and diagnostics |
| Cache pending targets -> outbound multicast query | Untrusted hostnames can amplify follow-up traffic |
| Rejection state -> local logs | Diagnostic output could disclose private LAN identifiers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-10 | Tampering | goodbye/cache-flush semantics | high | mitigate | Exact-RR one-second grace/rescue and count-only cache-flush handling (Tasks 1-2) |
| T-11-11 | Denial of Service | expiry scheduler | high | mitigate | Per-call pending-goodbye index, no scan of ordinary retained addresses, 1,024-owner admission, independent first-admitted 16-TXT/16-SRV owner ceilings, nearest-expiry clamp, all-due-event processing, no detached task/sleep, and unchanged caller deadlines; Plan 11-02 T-11-07R records only the remaining D-05 A/AAAA memory exposure (Task 1) |
| T-11-12 | Information Disclosure | aggregate diagnostics | high | mitigate | Fixed reason/type/count-only schema, removal of both `parse_error` and `invalid_address` identifier-bearing events, and explicit forbidden-field tests (Task 2) |
| T-11-13 | Denial of Service | follow-up amplification | high | mitigate | Two attempts per host, 64 admitted hosts, success deduplication, and failure isolation (Task 3) |
| T-11-14 | Repudiation | cross-call state leakage | medium | mitigate | Per-generator ownership and deterministic concurrent isolation tests (Task 3) |
| T-11-SC | Tampering | package supply chain | low | accept | No dependency install or change occurs |
</threat_model>

## Multi-Source Coverage Audit

The complete audit is in `11-SOURCE-AUDIT.md`. This plan closes MDNS-03, MDNS-04,
MDNS-06 conflict recovery, MDNS-07, D-08 through D-14, the R3 concurrency edge, R4
idempotency edge, all five resolved R7 edges, and prohibitions P4-P6.

<verification>
- `uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py -q`
- `uv run --frozen pytest -q`
- `uv run ruff check src/lifx/network/mdns/discovery.py tests/test_network/test_mdns/test_discovery.py`
- `uv run ruff format --check src/lifx/network/mdns/discovery.py tests/test_network/test_mdns/test_discovery.py`
- `uv run pyright`
</verification>

<success_criteria>
The per-call mDNS cache honours one-second goodbye/rescue, ignores legacy-unicast cache-flush semantics while counting them, emits private diagnostics once, resolves across arbitrary packet splits, and bounds every follow-up and deadline interaction.
</success_criteria>

<output>
Create `.planning/phases/11-mdns-hardening/11-03-SUMMARY.md` when done.
</output>
