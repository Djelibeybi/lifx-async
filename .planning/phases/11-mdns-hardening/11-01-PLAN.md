---
phase: 11-mdns-hardening
plan: 01
type: tracer
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-mdns-hardening/11-PHASE-BASE.txt
  - src/lifx/devices/base.py
  - src/lifx/network/mdns/types.py
  - src/lifx/network/mdns/discovery.py
  - src/lifx/network/mdns/transport.py
  - tests/test_devices/test_base.py
  - tests/test_network/test_mdns/test_discovery.py
  - tests/test_network/test_mdns/test_transport.py
  - tests/test_api/test_api_discovery.py
autonomous: true
requirements: [MDNS-01, MDNS-02]
tags: [mdns, tracer, connectivity, legacy-unicast, privacy]

estimate:
  tokens: 26000
  raw_tokens: 52000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "A real `MdnsTransport` binds an operating-system-selected UDP port in 1 through 65535 other than 5353 and receives a synthetic legacy-unicast datagram sent directly to that port."
    - "Only exact ASCII private TXT value `2` maps an mDNS-discovered device to `connectivity == \"thread\"`; every other value maps to `\"wifi\"` (D-01, D-02)."
    - "Absent and empty connectivity metadata produce `\"wifi\"` without raising and without adding a rejection diagnostic (D-02, D-10)."
    - "Whitespace-padded, alternate integer spellings, negative, malformed, and unrecognised metadata remain valid WiFi outcomes because mapping is an exact string comparison (D-02, D-10)."
    - "Every Device constructed outside the mDNS path exposes `connectivity == \"wifi\"`."
    - "The type-strict private connectivity hand-off rejects values outside the two public literals instead of allowing internal state drift, and cached-metadata adoption carries the source Device connectivity."
    - "The new service-record connectivity field defaults to `\"wifi\"`, so every pre-existing positional and keyword construction site remains compatible and the Wave 1 full suite stays green."
    - "The exact pre-implementation Phase 11 base commit is recorded once before any production or test edit and remains unchanged through the final phase audit."
  artifacts:
    - path: ".planning/phases/11-mdns-hardening/11-PHASE-BASE.txt"
      provides: "Immutable 40-character commit boundary for the complete Phase 11 implementation diff"
    - path: "src/lifx/devices/base.py"
      provides: "Public getter-only `Device.connectivity` property, private `_connectivity` storage, and private `_set_connectivity()` hand-off"
      contains: "def connectivity"
    - path: "src/lifx/network/mdns/types.py"
      provides: "Record-level private connectivity value consumed by device construction"
    - path: "src/lifx/network/mdns/discovery.py"
      provides: "Exact TXT mapping and end-to-end propagation through `create_device_from_record()`"
    - path: "tests/test_network/test_mdns/test_transport.py"
      provides: "Real loopback legacy-unicast receipt proof"
  key_links:
    - from: "src/lifx/network/mdns/discovery.py::_LifxRecordCache.resolve"
      to: "src/lifx/network/mdns/types.py::_LifxServiceRecord"
      via: "exact private TXT value mapping stored on the record"
      pattern: "thread.*2.*wifi"
    - from: "src/lifx/network/mdns/discovery.py::create_device_from_record"
      to: "src/lifx/devices/base.py::Device.connectivity"
      via: "private `_set_connectivity()` before the constructed device is returned"
      pattern: "_set_connectivity"
  prohibitions:
    - statement: "MUST NOT bind the mDNS query socket to port 5353 or join the multicast group, including as a fallback for unsolicited reception."
      status: resolved
      verification: "real-socket test plus source assertion"
    - statement: "MUST NOT allow connectivity metadata to influence address selection, routing, connection behaviour, retries, or tuning during Phase 11."
      status: resolved
      verification: "factory/property tests and final scope audit"
    - statement: "MUST NOT place live serials, IP addresses, hostnames, or raw discovery output in Phase 11 tests, documentation, or evidence."
      status: resolved
      verification: "synthetic fixtures and staged-diff privacy audit"
---

<objective>
Prove one production-quality path from a synthetic mDNS TXT response through internal resolution and device construction to the new public connectivity property, then prove the existing ephemeral legacy-unicast socket with a real loopback datagram.

Purpose: fail early if the Phase 11 architecture cannot carry private record metadata into the stable Device API or if the transport proof accidentally measures a host daemon.
Output: the recorded Phase 11 implementation base, end-to-end connectivity tracer, getter-only device surface, exact mapping tests, and real-socket MDNS-01 regression.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mdns-hardening/11-SPEC.md
@.planning/phases/11-mdns-hardening/11-CONTEXT.md
@.planning/phases/11-mdns-hardening/11-RESEARCH.md
@.planning/phases/11-mdns-hardening/11-PATTERNS.md
@.planning/phases/11-mdns-hardening/11-SOURCE-AUDIT.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-09-SUMMARY.md
@src/lifx/network/mdns/discovery.py
@src/lifx/network/mdns/types.py
@src/lifx/devices/base.py
@src/lifx/network/mdns/transport.py
</context>

## Artifacts this phase produces

Phase 11 produces `Device.connectivity`, `Device._connectivity`,
`Device._set_connectivity()`, `_LifxServiceRecord` with trailing defaulted
`connectivity` and `addresses` fields, `_CachedResourceRecord.expires_at`,
`_validate_txt_id()`, `_connectivity_from_txt()`,
`_LifxRecordCache._MAX_TXT_RRS_PER_OWNER`,
`_LifxRecordCache._MAX_SRV_RRS_PER_OWNER`,
`_LifxRecordCache._pending_expiries`, `_LifxRecordCache.next_expiry_delay()`,
`_LifxRecordCache.expire()`, and `_discover_lifx_services()`, while preserving the
existing public `create_device_from_record()` name and export because D-03 authorises only
the record/generator removal. It also produces the stable
diagnostic reasons `missing_txt_id`, `invalid_txt_id`, `conflicting_txt_id`,
`missing_product_id`, `invalid_product_id`, `invalid_address`, `malformed_packet`,
`rr_identity_limit`, and `unexpected_cache_flush`, plus the phase-local paths `COVERAGE.md` and
`11-SOURCE-AUDIT.md`. It adds no enum, CLI flag, persistence field, schema, dependency, or
external service integration.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace exact private connectivity metadata into a public Device</name>
  <reversibility rating="one-way">D-01 publishes two exact string values callers may persist; the user already locked this choice, so record the door without re-asking it.</reversibility>
  <files>.planning/phases/11-mdns-hardening/11-PHASE-BASE.txt, src/lifx/devices/base.py, src/lifx/network/mdns/types.py, src/lifx/network/mdns/discovery.py, tests/test_network/test_mdns/test_discovery.py</files>
  <read_first>
    - .planning/phases/11-mdns-hardening/11-SPEC.md (requirements 2 and 3; AC3-AC4)
    - .planning/phases/11-mdns-hardening/11-CONTEXT.md (D-01, D-02, D-04, D-10 and planner discretion for private storage)
    - .planning/phases/11-mdns-hardening/11-PATTERNS.md (Device base property, TXT helper, factory and public API test analogues)
    - src/lifx/devices/base.py (`Device.__init__` and getter-only property shapes)
    - src/lifx/network/mdns/discovery.py (`_LifxRecordCache.resolve`, `create_device_from_record`, `discover_devices_mdns`)
    - src/lifx/network/mdns/types.py (`LifxServiceRecord` frozen dataclass)
    - tests/test_network/test_mdns/test_discovery.py (`_txt`, `TestCreateDeviceFromRecord`, `TestDiscoverDevicesMdns`)
  </read_first>
  <behavior>
    - Test 1: split synthetic TXT, SRV, and A input carrying exact private value `2` resolves to a record and then a public `Light` whose connectivity is `"thread"`
    - Test 2: exact value `1`, missing, empty, whitespace-padded `2`, `02`, `+2`, `0`, `3`, negative text, and non-numeric text each yield `"wifi"` without raising
    - Test 3: connectivity metadata changes only the property; selected IP, port, retry settings, and device class remain identical
  </behavior>
  <action>
    Before any Phase 11 production or test edit, run `git rev-parse HEAD`, write that exact lowercase 40-character commit ID as the sole newline-terminated line of `.planning/phases/11-mdns-hardening/11-PHASE-BASE.txt`, and never overwrite it on a resumed execution; if the file already exists, validate that it names an ancestor of HEAD and continue with the recorded value. This boundary intentionally includes all planning revisions and excludes every Phase 11 implementation commit, giving Plan 11-06 one stable range even if `main` moves. Then start with one failing end-to-end test that feeds the existing cache complete split records, obtains its service record, passes that record through `create_device_from_record()`, and observes `Device.connectivity`. Extend `Device.__init__` only with private `_connectivity = "wifi"` state, a getter-only `connectivity` property typed as `Literal["wifi", "thread"]`, and a private `_set_connectivity(value: Literal["wifi", "thread"]) -> None` method used solely by the mDNS factory before returning the object. Make the setter validate the two literal runtime values and raise `ValueError` for any other value. This choice is within the user's constructor-plumbing discretion and avoids changing every specialised constructor signature while retaining the public read-only-looking contract. Add the record connectivity field as a trailing field with default `"wifi"`; all existing record construction sites, including positional fixtures, must remain source-compatible. Add an exact-comparison `_connectivity_from_txt()` helper; Plan 11-02 replaces which live TXT RR supplies the value when it introduces multi-value RR state, while retaining this exact mapping. Propagate the mapped value after normal product-based device construction so all device subclasses inherit the same property without changing class selection or network behaviour. Use only clearly synthetic serials, hostnames, and documentation-range addresses. Implement D-01, D-02, D-04, and D-10 exactly; do not expose the raw metadata or add an enum. Addresses review concerns: explicitly defaults the new dataclass field, makes the setter type-strict, and records the final privacy-audit boundary before implementation starts.
  </action>
  <verify>
    <automated>bash -c 'set -eu; IFS= read -r phase_base &lt; .planning/phases/11-mdns-hardening/11-PHASE-BASE.txt; case "$phase_base" in ""|*[!0-9a-f]*) exit 1;; esac; test "${#phase_base}" -eq 40; git cat-file -e "${phase_base}^{commit}"; git merge-base --is-ancestor "$phase_base" HEAD' &amp;&amp; uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py::TestCreateDeviceFromRecord -q</automated>
  </verify>
  <acceptance_criteria>
    - The tracer test fails before production changes because `Device.connectivity` is absent, then passes through cache, record, factory, and public Device layers
    - `11-PHASE-BASE.txt` contains the sole pre-implementation HEAD commit, is an ancestor of the task commit, and is not recalculated from a later moving branch reference
    - The property type and runtime value are limited to lowercase `"wifi"` and `"thread"`
    - Only exact private value `2` selects Thread; every parameterised alternative selects WiFi without a rejection diagnostic
    - The private setter's annotation and runtime gate limit the internal hand-off to the two public literals
    - The record connectivity field is trailing and WiFi-defaulted so existing constructors remain source-compatible
    - Device class, selected address, port, timeout, retry, connection, and animation behaviour are unchanged by connectivity metadata
    - Fixtures contain no live identifier or raw discovery output
  </acceptance_criteria>
  <done>A caller of supported mDNS discovery can read the exact two-value connectivity property, while all non-mDNS and non-sentinel cases honestly report WiFi.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove defaults, cached-metadata adoption, and high-level propagation</name>
  <reversibility rating="reversible">Default/adoption plumbing remains behind the already locked public connectivity property and touches no persisted schema.</reversibility>
  <files>src/lifx/devices/base.py, tests/test_devices/test_base.py, tests/test_api/test_api_discovery.py</files>
  <read_first>
    - .planning/phases/11-mdns-hardening/11-CONTEXT.md (D-01, D-02, D-10 and private storage discretion)
    - src/lifx/devices/base.py (`Device.__init__`, `adopt_cached_metadata`, property patterns)
    - tests/test_devices/test_base.py (direct construction and keyword-only option tests)
    - tests/test_api/test_api_discovery.py (public `discover_mdns()` generator tests)
  </read_first>
  <behavior>
    - Test 1: direct Device construction and existing non-mDNS high-level paths default to `"wifi"`
    - Test 2: `_set_connectivity(value: Literal["wifi", "thread"]) -> None` accepts only the two literals and rejects every other runtime value
    - Test 3: `adopt_cached_metadata()` copies Thread connectivity from the donor and preserves WiFi when both devices are default-constructed
    - Test 4: public `discover_mdns()` preserves the connectivity of each yielded device
    - Test 5: all pre-existing record/device construction sites remain compatible under the full frozen suite
  </behavior>
  <action>
    Put direct-construction, invalid-setter, and adoption regressions in `tests/test_devices/test_base.py`, not only in the mDNS suite. Update `Device.adopt_cached_metadata()` to copy `_connectivity` from the donor alongside existing cached fields so a future serial-deduplicated path cannot silently downgrade a Thread device to WiFi. Add a public API regression showing `discover_mdns()` returns the factory-produced connectivity unchanged. Run the complete frozen suite in Wave 1 to prove the trailing defaulted record field and Device storage did not break any of the existing positional or keyword construction sites. Do not infer connectivity from IP family or let adoption change address, routing, retries, or tuning.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_devices/test_base.py tests/test_api/test_api_discovery.py -k 'connectivity or mdns or adopt_cached_metadata' -q &amp;&amp; uv run --frozen pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Direct and non-mDNS devices are WiFi-defaulted
    - Invalid private hand-off values raise without mutating the Device
    - Cached-metadata adoption carries connectivity and leaves existing adopted fields unchanged
    - Public `discover_mdns()` preserves exact factory connectivity
    - The full frozen suite proves every existing construction site remains compatible before Wave 2
  </acceptance_criteria>
  <done>Connectivity remains correct through direct construction, metadata adoption, and the supported high-level API, with full-suite compatibility proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the ephemeral socket receives a direct legacy-unicast reply</name>
  <reversibility rating="reversible">This adds a regression around the already-landed ephemeral transport and changes no published or persisted contract.</reversibility>
  <files>src/lifx/network/mdns/transport.py, tests/test_network/test_mdns/test_transport.py</files>
  <read_first>
    - .planning/phases/11-mdns-hardening/11-SPEC.md (requirement 1; AC1-AC2)
    - .planning/phases/11-mdns-hardening/11-CONTEXT.md (Phase 10 transport baseline and no-rejoin boundary)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-08-SUMMARY.md (cancellation-safe transport ownership)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-09-SUMMARY.md (atomic endpoint publication)
    - src/lifx/network/mdns/transport.py (`open`, `receive`, `close`, `is_open`)
    - tests/test_network/test_mdns/test_transport.py (real socket lifecycle and async context-manager fixtures)
  </read_first>
  <behavior>
    - Test 1: an opened real transport reports a local UDP port between 1 and 65535 and unequal to 5353
    - Test 2: a second IPv4 UDP socket sends synthetic bytes to loopback at that selected port and `receive()` returns the same payload and sender
    - Test 3: the test never requests port 5353, never joins multicast membership, and closes both sockets on success or assertion failure
    - Test 4: existing Phase 10 cancellation, open/open, and open/close lifecycle tests stay green
  </behavior>
  <action>
    Add a distinct real-loopback regression alongside the mocked bind assertion. Keep the actual `MdnsTransport` open, read its chosen port from its owned socket, create a second IPv4 UDP sender bound only to an OS-selected loopback port, send a synthetic payload directly to `127.0.0.1`, and await `receive()` with the existing bounded timeout. Assert payload and sender address, and use context/finally cleanup for both descriptors. Permit a skip only when the platform cannot create or bind IPv4 loopback; do not fall back to a system daemon, multicast route, external network, or hardware. Do not change the Phase 10 endpoint lifecycle, wildcard bind, socket family, multicast TTL, locking, generation, or cleanup behaviour. Update only the internal transport docstring needed to identify the socket as IPv4 multicast outbound with legacy-unicast replies; the complete limitation wording is owned by Plan 11-04.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_network/test_mdns/test_transport.py -q -W error::ResourceWarning</automated>
  </verify>
  <acceptance_criteria>
    - The real transport's selected port satisfies the complete integer range and excludes 5353
    - The received payload equals the bytes sent directly to that port and the sender tuple matches the loopback sender
    - No socket in the test binds 5353 or requests multicast membership
    - The test has no dependency on Avahi, Bonjour, multicast routing, or LIFX hardware
    - Existing resource, cancellation, concurrency, and reopen tests pass with ResourceWarning promoted to error
  </acceptance_criteria>
  <done>The OS-selected mDNS query socket is proven to receive RFC 6762 legacy-unicast delivery without sharing the daemon port or altering transport lifecycle behaviour.</done>
</task>

</tasks>

<threat_model>
## Security Profile

- `asvs_level: L1` (opportunistic)
- `block_threshold: high`; any unresolved critical/high threat blocks plan completion

## Trust Boundaries

| Boundary | Description |
|---|---|
| Untrusted LAN TXT/SRV/A data -> internal record -> public Device | Private metadata controls a user-visible property and device factory output |
| OS UDP socket -> asyncio receive queue | A synthetic datagram proves ownership and direct delivery without a shared daemon socket |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-01 | Spoofing | connectivity metadata | medium | mitigate | Treat the field as diagnostic only, accept only exact sentinel mapping, default every other case to WiFi, and make no authenticity claim (Task 1) |
| T-11-02 | Tampering | device factory | high | mitigate | Permit only the literal union, propagate after class construction, and regression-test that address/routing/retries remain unchanged (Task 1) |
| T-11-03 | Denial of Service | mDNS socket bind | high | mitigate | Retain ephemeral binding, prohibit membership/5353, and prove direct receipt on the owned socket (Task 3) |
| T-11-04 | Information Disclosure | synthetic tests | medium | mitigate | Use synthetic serials, documentation-range addresses, loopback, and no raw hardware output (all tasks) |
| T-11-SC | Tampering | package supply chain | low | accept | No dependency or package-manager install is planned; the zero-runtime-dependency contract remains unchanged |
</threat_model>

## Multi-Source Coverage Audit

The complete GOAL/REQ/RESEARCH/CONTEXT audit is in `11-SOURCE-AUDIT.md`. This plan covers
MDNS-01, MDNS-02, D-01, D-02, D-04, D-10, the R1 boundary edge, the R2
boundary/empty/encoding edges, and SPEC prohibitions P1, P5, and P6.

<verification>
- `uv run --frozen pytest tests/test_network/test_mdns/test_transport.py tests/test_network/test_mdns/test_discovery.py tests/test_devices/test_base.py tests/test_api/test_api_discovery.py -q -W error::ResourceWarning`
- `uv run --frozen pytest -q`
- `uv run ruff check src/lifx/devices/base.py src/lifx/network/mdns/types.py src/lifx/network/mdns/discovery.py src/lifx/network/mdns/transport.py tests/test_network/test_mdns/test_discovery.py tests/test_network/test_mdns/test_transport.py tests/test_api/test_api_discovery.py`
- `uv run ruff format --check src/lifx/devices/base.py src/lifx/network/mdns/types.py src/lifx/network/mdns/discovery.py src/lifx/network/mdns/transport.py tests/test_network/test_mdns/test_discovery.py tests/test_network/test_mdns/test_transport.py tests/test_api/test_api_discovery.py`
- `uv run pyright`
</verification>

<success_criteria>
One complete synthetic mDNS path yields a correctly classified Device, all alternative metadata remains valid WiFi, and a real ephemeral socket receives a direct loopback reply without port 5353, membership, hardware, or daemon dependence.
</success_criteria>

<output>
Create `.planning/phases/11-mdns-hardening/11-01-SUMMARY.md` when done.
</output>
