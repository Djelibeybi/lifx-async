---
phase: 11-mdns-hardening
plan: 07
type: execute
wave: 6
depends_on: [11-06]
files_modified:
  - .planning/STATE.md
  - .planning/phases/11-mdns-hardening/11-RESEARCH.md
  - .planning/phases/11-mdns-hardening/11-PATTERNS.md
  - .planning/phases/11-mdns-hardening/11-SOURCE-AUDIT.md
  - .planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md
autonomous: false
requirements: [MDNS-08]
gap_closure: true
tags: [mdns, gap-closure, recovery, privacy, evidence]

estimate:
  tokens: 12000
  raw_tokens: 24000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "The signed, DCO-compliant authority amendments in commits cd0c3ac and f7c7c6f remain intact and are not repeated, amended, or represented as newly executed work."
    - "The prior Task 2 drafts in STATE, RESEARCH, PATTERNS, and SOURCE-AUDIT are preserved, completed, and committed without overwriting unrelated executor-owned hunks."
    - "The unresolved operational candidate at `.planning/STATE.md:105` is removed without reading or reproducing its value; only its repository location, `ipv6` category, and sanitised disposition are recorded."
    - "D-09's identifier-free evidence boundary and SPEC Prohibition P7 apply to planning state, diffs, reports, prompts, and memory as well as runtime diagnostics."
    - "Per D-15, address admission stops at exactly 256 live A/AAAA RR identities per owner and 1,024 per sweep; exact duplicates refresh without consuming capacity, and the first unseen identity beyond either ceiling is rejected without eviction."
    - "Per D-15, owner overflow and sweep exhaustion are permanent for the discovery call; an incomplete owner or sweep cannot select an address, resolve or emit a record, or trigger follow-up work, including when no selectable address exists."
    - "Per D-16 and SPEC Prohibition P2, `_LifxServiceRecord`, `_discover_lifx_services`, and `_create_device_from_record` remain private together with no public or compatibility alias, public documentation, or public example."
    - "SPEC Prohibition P6 remains explicit: incomplete D-15 address state cannot select, resolve, emit, or trigger follow-up work."
    - "SPEC Prohibition P7 remains explicit: Phase 11 tests, documentation, evidence, plans, diffs, prompts, reports, and memory contain no live serial, address, hostname, mapping value, or raw discovery output."
    - "Plan 11-07 finishes with a SUMMARY, eliminating the incomplete-plan trap before any history decision or rewrite plan can run."
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Operationally useful Phase 11 state with the private endpoint value replaced by a category-neutral re-derivation note"
    - path: ".planning/phases/11-mdns-hardening/11-RESEARCH.md"
      provides: "Completed D-15/D-16 current research guidance from the preserved prior draft"
    - path: ".planning/phases/11-mdns-hardening/11-PATTERNS.md"
      provides: "Completed D-15/D-16 pattern guidance from the preserved prior draft"
    - path: ".planning/phases/11-mdns-hardening/11-SOURCE-AUDIT.md"
      provides: "Physical-plan coverage for GOAL, MDNS-01..08, RESEARCH, D-01..16, 29 edges, 7 prohibitions, and detector dispositions"
    - path: ".planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md"
      provides: "Value-suppressed current-file finding and sanitisation record"
      contains: "Current-file remediation"
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md"
      via: "location/category/disposition evidence only; the candidate value and external mapping never cross the boundary"
      pattern: "STATE.md.*ipv6.*sanitised"
    - from: ".planning/phases/11-mdns-hardening/11-SOURCE-AUDIT.md"
      to: ".planning/phases/11-mdns-hardening/11-CONTEXT.md"
      via: "D-09, D-15, and D-16 remain explicitly covered while D-05 stays superseded"
      pattern: "D-(09|15|16)"
---

<objective>
Recover the halted Plan 11-07 without replaying completed authority work: sanitise the value-suppressed working-tree finding, preserve and finish the executor's four draft guidance files, and close 11-07 with an honest summary.

Purpose: remove the current-file privacy blocker and the no-SUMMARY partial-plan trap before deciding whether local branch history must be rewritten.
Output: a sanitised STATE entry, committed D-15/D-16 guidance drafts, a value-suppressed remediation record, and `11-07-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
@$HOME/.codex/skills/evidence-privacy-audit/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-mdns-hardening/11-CONTEXT.md
@.planning/phases/11-mdns-hardening/11-SPEC.md
@.planning/phases/11-mdns-hardening/11-VERIFICATION.md
@.planning/phases/11-mdns-hardening/11-06-SUMMARY.md
@.planning/phases/11-mdns-hardening/11-RESEARCH.md
@.planning/phases/11-mdns-hardening/11-PATTERNS.md
@.planning/phases/11-mdns-hardening/11-SOURCE-AUDIT.md

<interfaces>
Recovery invariants:
- `cd0c3ac` and `f7c7c6f` are the completed, signed+DCO authority-amendment commits from the halted run.
- The expected executor-owned dirty set is exactly `.planning/STATE.md`, `11-RESEARCH.md`, `11-PATTERNS.md`, and `11-SOURCE-AUDIT.md`; there must be no staged or untracked file.
- `$HOME/.codex/skills/evidence-privacy-audit/scripts/scan_private_candidates.py` reports only location and category; exit 1 means candidates require contextual classification and exit 2 means the audit is incomplete.
- The private external mapping remains outside the repository and is neither required nor inspected for current-file removal.
</interfaces>
</context>

## Recovery representation

This file is a targeted replacement of the incomplete 11-07 prompt, not a new attempt to execute its old tasks. The previous Task 1 is already complete in `cd0c3ac` and `f7c7c6f`; no `11-07-SUMMARY.md` exists, so adding only a later-numbered plan would cause `$gsd-execute-phase 11 --gaps-only` to redispatch stale work. This replacement carries only the unfinished draft/privacy work. Plans 11-08 and 11-09 are sequential follow-ons for the separately authorised history scope.

## Artifacts this phase produces

This plan produces a value-suppressed current-file remediation record and commits the four preserved authority/guidance drafts. It does not modify source, tests, executed Plans 11-01 through 11-06, their summaries, review reports, the two completed 11-07 authority commits, changelog history, external mapping data, local branch history, remote refs, or hardware state.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Establish cryptographic verification outside a denied signing environment</name>
  <precondition>The commit objects for `cd0c3ac` and `f7c7c6f` exist locally and remain ancestors of HEAD.</precondition>
  <what-built>The executor first distinguishes three states for each preserved authority commit: valid signature, invalid/missing object signature, or cryptographic verification unavailable because the sandbox cannot access the configured verification environment. It checks signature headers and DCO trailers without printing identity values and attempts `git verify-commit` before asking for human input.</what-built>
  <how-to-verify>If `git verify-commit` succeeds for both commits, record that result and continue. If and only if the commit objects contain signature headers and DCO trailers but verification is unavailable because the execution environment cannot access the verifier/keyring, verify the exact two commit IDs in an approved external shell and confirm success. An invalid signature, missing signature header, missing DCO trailer, changed commit ID, or verification failure unrelated to environment access blocks the plan and cannot be overridden here.</how-to-verify>
  <verify>
    <automated>git merge-base --is-ancestor cd0c3ac HEAD &amp;&amp; git merge-base --is-ancestor f7c7c6f HEAD &amp;&amp; uv run --frozen python -c 'import subprocess; commits = ("cd0c3ac", "f7c7c6f"); objects = [subprocess.run(["git", "cat-file", "commit", commit], check=True, capture_output=True, text=True).stdout for commit in commits]; assert all("\ngpgsig " in "\n" + obj and "\nSigned-off-by: " in "\n" + obj for obj in objects)'</automated>
  </verify>
  <acceptance_criteria>
    - The exact preserved commit objects contain signature headers and DCO trailers and are ancestors of HEAD
    - Cryptographic verification succeeds either in the executor or through an approved external verification when the executor environment alone is unavailable
    - Invalid or missing signatures/DCO, rather than environment denial, halt recovery
    - No candidate value, author identity, email, private mapping, or raw commit body is printed or recorded
  </acceptance_criteria>
  <resume-signal>Reply `verified` only after both exact commits verify cryptographically; otherwise reply `stop`. Do not include signer identity or private values.</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: Sanitise the working-tree candidate without exposing or widening it</name>
  <precondition>Task 1 records successful cryptographic verification for commits cd0c3ac and f7c7c6f; the unstaged set is exactly STATE, 11-RESEARCH, 11-PATTERNS, and 11-SOURCE-AUDIT; the index and untracked set are empty.</precondition>
  <files>.planning/STATE.md, .planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md</files>
  <read_first>
    - AGENTS.md (privacy, commit, and evidence rules)
    - $HOME/.codex/skills/evidence-privacy-audit/SKILL.md (value-suppressed audit and external-mapping boundary)
    - .planning/phases/11-mdns-hardening/11-VERIFICATION.md (privacy-provenance limitation and immutable phase base; current SPEC labels that prohibition P7)
    - .planning/STATE.md (read only through location/category-only scanning for the candidate line; do not print or open the value-bearing line)
  </read_first>
  <action>
    First assert the precondition with read-only Git checks and confirm Task 1's cryptographic result applies to the unchanged commit objects. Hash the four existing patches into an ignored temporary directory so later checks can prove that only the intended STATE line changed before Task 3; never copy a value-bearing patch into a tracked artefact.

    Run the evidence privacy scanner against `.planning/STATE.md` and the unstaged STATE diff with `UV_CACHE_DIR=/private/tmp/lifx-async-phase11-privacy-uv-cache`. It must identify the previously reported line by repository location and category only. Replace the complete operational line at the scanner-reported location through a non-echoing in-memory edit with this neutral statement: `Thread hardware endpoint values remain operator-controlled evidence outside the repository and must be re-derived before Phase 14 validation; this planning state intentionally records no serial, address, hostname, or mapping value.` The edit utility must assert that exactly one line in the Phase 11 working-notes region carries the unresolved `ipv6` category before replacement and must print only success/failure, never the removed content. This privacy-specific non-echoing edit is required because opening the line through a normal editor would disclose the value into the prompt or tool log.

    Create `11-PRIVACY-REMEDIATION.md` with repository, branch, finding location, category, `current file: sanitised`, the fact that no external mapping was accessed, and explicit separate sections for current-file, staged/diff, and history scope. Record that history is not remediated or authorised by this plan and routes to 11-08. Do not record a candidate value, hash of the value, raw context line, private mapping, physical identity, raw packet, hostname, or discovery output. This task implements D-09 and SPEC Prohibition P7 at the planning-evidence boundary.

    Before committing, clear the index and stage only `.planning/STATE.md` and `11-PRIVACY-REMEDIATION.md` by exact path. Run the value-suppressed privacy scanner over both staged files and over `git diff --cached --no-ext-diff --binary` through scanner standard input. Exit 2 or any incomplete scan blocks; exit 1 blocks until every location/category hit is classified, and any live or unresolved candidate blocks the commit. Confirm the staged path set equals the task's two declared files, run `git diff --cached --check`, then commit with a Conventional Commit message using `git commit -S -s`. Do not stage the three guidance drafts in this task.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-phase11-privacy-uv-cache uv run $HOME/.codex/skills/evidence-privacy-audit/scripts/scan_private_candidates.py .planning/STATE.md &amp;&amp; git diff --check -- .planning/STATE.md .planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md &amp;&amp; uv run --frozen python -c 'from pathlib import Path; p = Path(".planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md"); text = p.read_text(encoding="utf-8"); assert all(token in text for token in ("STATE.md", "ipv6", "sanitised", "History scope")); assert "operator-controlled evidence" in Path(".planning/STATE.md").read_text(encoding="utf-8")'</automated>
  </verify>
  <acceptance_criteria>
    - The scanner reports no candidate in current STATE after replacement and no removed value appeared in tool output or an artefact
    - The three other executor-owned draft patches remain byte-for-byte unchanged during this task
    - The new record names only repository location, category, scope, and disposition
    - The exact two staged files and their staged diff pass a complete value-suppressed privacy audit; unresolved/live candidates and scanner exit 2 block
    - No pre-existing commit, non-current-branch Git ref, source file, test, external mapping, remote, or worktree is mutated
    - The task commit uses a Conventional Commit message and `git commit -S -s`, and its signature/DCO are verified under the same environment-versus-invalid distinction as Task 1
  </acceptance_criteria>
  <done>The current-file candidate is removed through a value-suppressed path, its safe disposition is recorded, and the prior draft work remains intact for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the preserved guidance drafts and close the partial plan</name>
  <reversibility rating="costly">The coordinated planning guidance is documentation-only, but undo would require reconciling four active authority surfaces again; completed source behaviour and history remain untouched.</reversibility>
  <files>.planning/phases/11-mdns-hardening/11-RESEARCH.md, .planning/phases/11-mdns-hardening/11-PATTERNS.md, .planning/phases/11-mdns-hardening/11-SOURCE-AUDIT.md, .planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md</files>
  <read_first>
    - .planning/phases/11-mdns-hardening/11-CONTEXT.md (D-09, D-15, and D-16 current authority)
    - .planning/phases/11-mdns-hardening/11-SPEC.md (29 Edge Coverage truths, 7 Prohibitions, including current P6 fail-closed state and P7 privacy)
    - .planning/phases/11-mdns-hardening/11-RESEARCH.md (preserved uncommitted D-15/D-16 draft)
    - .planning/phases/11-mdns-hardening/11-PATTERNS.md (preserved uncommitted D-15/D-16 draft)
    - .planning/phases/11-mdns-hardening/11-SOURCE-AUDIT.md (preserved full-source coverage draft)
    - .planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md (Task 2 scope separation)
  </read_first>
  <action>
    Preserve the existing draft hunks and finish only omissions detected by structured checks. RESEARCH and PATTERNS must keep D-15's exact 256-per-owner/1,024-per-sweep, permanent per-call overflow and fail-closed selection/resolution/follow-up guidance, and D-16's private record/generator/converter boundary. SOURCE-AUDIT must cover GOAL, MDNS-01 through MDNS-08, all in-scope RESEARCH constraints, D-01 through D-16, 29/29 applicable SPEC Edge Coverage truths, all 7 Prohibitions, and both detector dispositions; D-05 remains `SUPERSEDED`, not missing. Preserve the `one-second` assumption-delta disposition as a duration with no identity-model change, and preserve the declaration that there is no external API integration matrix.

    Use the privacy scanner for the three files and classify every reported synthetic/reserved example or pseudonym candidate locally. Add only location/category/disposition totals to `11-PRIVACY-REMEDIATION.md`; do not copy values. A live or unresolved finding stops the task and returns for another privacy plan. Re-run the exact D-15/D-16 constant assertion and three focused existing regressions so the new summary contains fresh current-head evidence; the earlier 413-test, 3,871-test, Ruff, format, and Pyright results remain explicitly historical. Do not rerun or relabel the full suite in this plan because 11-09 owns fresh post-history-disposition evidence. Do not edit source/tests if a mismatch appears; stop for a separate replan.

    Before committing, clear the index and stage only the three declared guidance files plus the updated remediation record by exact path. Run the value-suppressed scanner over every staged file and over `git diff --cached --no-ext-diff --binary` through scanner standard input. Exit 2 or any incomplete scan blocks; exit 1 blocks until every location/category hit is classified, and any live or unresolved candidate blocks the commit. Confirm the staged path set is exactly the four declared files and run `git diff --cached --check`. Commit only those files with a signed+DCO Conventional Commit, then verify the new commit under Task 1's environment-versus-invalid distinction. The executor must then create `11-07-SUMMARY.md` stating that the former authority work was preserved in `cd0c3ac`/`f7c7c6f`, this replacement completed only current-file/draft recovery, history remains a separate decision, and no privacy attestation or phase pass has been self-issued.
  </action>
  <verify>
    <automated>uv run --frozen python -c 'from pathlib import Path; paths = [Path(".planning/phases/11-mdns-hardening/11-RESEARCH.md"), Path(".planning/phases/11-mdns-hardening/11-PATTERNS.md"), Path(".planning/phases/11-mdns-hardening/11-SOURCE-AUDIT.md")]; texts = {p: p.read_text(encoding="utf-8") for p in paths}; assert all("D-15" in t and "D-16" in t for t in texts.values()); audit = texts[paths[2]]; assert all(f"MDNS-{i:02d}" in audit for i in range(1, 9)); assert "29/29" in audit; assert all(f"| SPEC PROHIBITION | P{i} |" in audit for i in range(1, 8))' &amp;&amp; uv run --frozen python -c 'from lifx.network.mdns.discovery import _LifxRecordCache; assert (_LifxRecordCache._MAX_ADDRESS_RRS_PER_OWNER, _LifxRecordCache._MAX_ADDRESS_RRS_PER_SWEEP) == (256, 1024)' &amp;&amp; uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCacheBounds::test_address_owner_overflow_fails_closed_without_selecting_a_subset tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCacheBounds::test_sweep_address_budget_cannot_be_bypassed_across_owners tests/test_network/test_mdns/test_phase_contract.py::TestPhase11SurfaceContract::test_record_to_device_factory_is_internal_with_its_record_type -q &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The three preserved draft files express current D-15/D-16 authority with no unqualified unlimited-address or public-converter directive
    - SOURCE-AUDIT retains complete four-source, 29-edge, 7-prohibition, detector, and requirement coverage with no missing row
    - The exact-bound assertion and three focused existing regressions pass at the current head without source/test edits
    - Privacy classifications contain location/category/disposition only and no live or unresolved candidate remains in the current files or diff
    - Historical broad-suite evidence remains labelled historical; fresh post-rewrite evidence is assigned to 11-09
    - `11-07-SUMMARY.md` is created and the stale old Task 1 is not redispatched
  </acceptance_criteria>
  <done>The partial 11-07 execution is closed honestly: current files are sanitised, draft guidance is committed, focused current-head proof is green, and history authority remains pending in Plan 11-08.</done>
</task>

</tasks>

<threat_model>
## Security Profile

- `asvs_level: L1`
- `block_threshold: high`; all critical/high findings must be mitigated before completion

## Trust Boundaries

| Boundary | Description |
|---|---|
| Untrusted operational text -> tracked planning state | A value-bearing working note can disclose a live endpoint when committed or copied into a prompt. |
| Preserved dirty drafts -> executor commit | Existing executor-owned hunks must not be discarded, rewritten wholesale, or misrepresented as planner output. |
| External private mapping -> repository evidence | Mapping/identity attestation remains operator-controlled and outside all repository and agent surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-31 | Information Disclosure | `.planning/STATE.md` current candidate | high | mitigate | Task 2 uses category-only scanning and a non-echoing whole-line replacement; the report records only location/category/disposition per D-09/P7. |
| T-11-32 | Tampering | Four preserved draft patches | high | mitigate | Precondition pins the exact dirty set; per-file patch digests prove Task 2 leaves the other drafts unchanged and Task 3 edits only structured omissions. |
| T-11-33 | Repudiation | Partial 11-07 execution | medium | mitigate | Preserve signed commits `cd0c3ac`/`f7c7c6f`, create a replacement-plan SUMMARY, and distinguish completed, historical, and fresh evidence. |
| T-11-34 | Information Disclosure | External mapping | high | mitigate | Neither task accesses or records the mapping; operator provenance remains a separate external boundary. |
| T-11-SC | Tampering | Package supply chain | low | accept | No package install, dependency, or lockfile change occurs; the existing uv environment and stdlib-only runtime remain unchanged. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Physical coverage | Recovery disposition |
|---|---|---|---|---|
| GOAL | - | Broadcast-grade synthetic mDNS before default promotion | 11-01..11-09 | COVERED; recovery restores trustworthy evidence only. |
| REQ | MDNS-01 | Ephemeral legacy-unicast proof | 11-01, 11-06 | COVERED; unchanged. |
| REQ | MDNS-02 | Connectivity and private record/generator/converter | 11-01, 11-04..11-07 | COVERED; D-16 draft guidance is completed here. |
| REQ | MDNS-03 | Cross-packet exact-once assembly | 11-02, 11-03 | COVERED; unchanged. |
| REQ | MDNS-04 | Bounded follow-up queries | 11-03, 11-07 | COVERED; unchanged source behaviour. |
| REQ | MDNS-05 | D-15 bounded fail-closed address admission | 11-02, 11-03, 11-07 | COVERED; exact focused proof reruns here. |
| REQ | MDNS-06 | Strict TXT identity/conflict recovery | 11-02, 11-03 | COVERED; unchanged. |
| REQ | MDNS-07 | Goodbye/rescue/cache-flush semantics | 11-03 | COVERED; unchanged. |
| REQ | MDNS-08 | Honest supported/private documentation and evidence | 11-04, 11-06..11-09 | COVERED; this recovery owns privacy honesty. |
| RESEARCH | privacy/evidence boundary | Identifier-free diagnostics and artefacts | 11-03, 11-06..11-09 | COVERED; current-file leak is sanitised before commit. |
| RESEARCH | cache/API constraints | D-15 and D-16 current guidance | 11-07 | COVERED; preserved drafts are completed. |
| CONTEXT | D-01, D-02, D-03, D-04 | Connectivity/private API decisions | 11-01, 11-04..11-07 | COVERED; D-03 remains supplemented by D-16. |
| CONTEXT | D-05, D-06, D-07 | Address decisions | 11-02, 11-03, 11-07 | COVERED; D-05 remains SUPERSEDED by D-15. |
| CONTEXT | D-08, D-09, D-10 | Aggregate privacy-safe diagnostics | 11-03, 11-06..11-09 | COVERED; D-09 directly governs recovery. |
| CONTEXT | D-11, D-12, D-13, D-14 | RFC cache/deadline decisions | 11-03 | COVERED; unchanged. |
| CONTEXT | D-15, D-16 | Exact caps/fail-closed state and private converter | `cd0c3ac`, `f7c7c6f`, 11-07 | COVERED; completed commits are preserved, not replayed. |
| SPEC EDGE | R1..R7 | 29 applicable truths | 11-01..11-07 | COVERED 29/29 in SOURCE-AUDIT; no edge is reopened. |
| SPEC PROHIBITION | P1..P6 | Transport/API/tuning/cache/incomplete-state prohibitions | 11-01..11-07 | COVERED; P6 is explicit in this plan's must-haves. |
| SPEC PROHIBITION | P7 | No live identifiers/raw discovery output | 11-06..11-09 | COVERED by current-file sanitisation, history decision, and final audit. |
| DETECTOR | API coverage | No external integration matrix | COVERAGE.md, 11-06, 11-07 | UNCHANGED; no external API exists. |
| DETECTOR | assumption delta | `one-second` is a duration | 11-06, 11-07 | UNCHANGED; no identity-model change. |

No deferred or later-phase item is introduced, and no source item is missing.

<verification>
- Assert the exact dirty-set and completed-commit preconditions before any edit.
- Run all value-bearing current-file/diff material only through the location/category-only scanner.
- Prove STATE is candidate-free after the non-echoing replacement and that the three other draft patches were preserved.
- Run the exact D-15/D-16 assertion and three focused regressions fresh; keep prior broad-suite results historical.
- Require a signed+DCO commit for each committing task and create `11-07-SUMMARY.md`.
</verification>

<success_criteria>
The current working-tree privacy finding is removed without disclosure, the preserved draft guidance is complete and committed, the two already-completed authority commits remain intact, all Phase 11 source/decision/spec coverage remains whole, and gaps-only execution can advance from a real 11-07 summary into the separate history decision.
</success_criteria>

<output>
Create `.planning/phases/11-mdns-hardening/11-07-SUMMARY.md` when done.
</output>
