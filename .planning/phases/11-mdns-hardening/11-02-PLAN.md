---
phase: 11-mdns-hardening
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/lifx/network/mdns/discovery.py
  - src/lifx/network/mdns/types.py
  - scripts/ipv6_thread_probe.py
  - tests/test_network/test_mdns/test_discovery.py
  - tests/test_scripts/test_ipv6_thread_probe.py
autonomous: true
requirements: [MDNS-03, MDNS-05, MDNS-06]
tags: [mdns, cache, addresses, validation, tdd]

estimate:
  tokens: 27000
  raw_tokens: 54000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Duplicate A/AAAA records collapse to one retained address and replayed TXT/SRV/address records do not cause a second service-instance emission."
    - "Empty or incomplete packet sequences yield no record and do not prevent a later complete instance from resolving."
    - "Every tested TXT/SRV/A/AAAA arrival permutation produces the same serial, retained address membership, and selected address class without imposing within-class set iteration order."
    - "Every service record retains each unique syntactically valid advertised A/AAAA address in an unordered internal collection, excluding packet-source fallback (D-05, D-06)."
    - "An admitted hostname retains more than 16 unique live valid A/AAAA addresses without truncation per D-05; only this address cardinality remains an explicitly accepted per-call residual risk."
    - "Each admitted owner retains at most the first 16 distinct live TXT identities and first 16 distinct live SRV identities; an admitted later identity may make resolution ambiguous but can never evict an earlier identity, while every over-cap observation increments only the privacy-safe `rr_identity_limit`/record-type aggregate."
    - "An instance with no selectable address is not emitted, even when an unscoped link-local address is retained internally."
    - "Address selection prefers IPv4, then ULA, GUA, and scoped link-local; within-class selection is first-learned and is not exposed as an ordering contract (D-07)."
    - "Live conflicting valid TXT IDs invalidate the instance, and the outcome is independent of which conflicting value arrived first (D-12)."
    - "An absent or empty TXT ID cannot resolve an internal service record."
    - "A TXT ID accepts exactly 12 hexadecimal characters, normalises valid uppercase to lowercase, and rejects separators, wrong length, non-hex, all-zero, all-ones, and multicast/group identities."
    - "When multiple live TXT RRs carry the sole valid ID, resolution remains unresolved if their complete construction metadata disagree on product, firmware, or connectivity; no RR ordering can select a winner."
    - "When multiple live SRV RRs describe one service instance, resolution remains unresolved if their target or port disagree; no RR ordering can select a winner."
    - "TXT and SRV ambiguity may resolve only after goodbye expiry leaves one consistent construction value; deterministic RR ordering is used only for storage or presentation, never as a trust selector."
    - "The IPv6 Thread probe and its tests consume the revised cache view and address classifier in the same wave, without running hardware or exposing live identifiers."
  artifacts:
    - path: "src/lifx/network/mdns/discovery.py"
      provides: "Distinct-owner-bounded multi-value RR cache with deterministic TXT/SRV identity ceilings, complete admitted-owner address retention, class selection, and strict identity derivation"
      contains: "_CachedResourceRecord"
    - path: "src/lifx/network/mdns/types.py"
      provides: "Frozen unordered advertised-address membership on the internal service record"
      contains: "addresses"
    - path: "tests/test_network/test_mdns/test_discovery.py"
      provides: "Permutation, address-class, duplicate, ID-form, and live-conflict regression matrix"
    - path: "scripts/ipv6_thread_probe.py"
      provides: "Repository-internal cache reporting adapted to the revised live-RR model"
  key_links:
    - from: "src/lifx/network/mdns/dns.py::DnsResourceRecord"
      to: "src/lifx/network/mdns/discovery.py::_CachedResourceRecord"
      via: "lower-case owner, rtype, masked rclass, and raw rdata identity"
      pattern: "rdata"
    - from: "src/lifx/network/mdns/discovery.py::_LifxRecordCache.resolve"
      to: "src/lifx/network/mdns/types.py"
      via: "frozenset address membership plus selected `ip`"
      pattern: "frozenset"
    - from: "src/lifx/network/mdns/discovery.py live TXT/SRV collections"
      to: "src/lifx/network/mdns/discovery.py::_LifxRecordCache.resolve"
      via: "set-consensus over normalised TXT construction tuples and SRV target/port values before record construction"
      pattern: "resolve"
  prohibitions:
    - statement: "MUST NOT allow connectivity metadata to influence address selection, routing, connection behaviour, retries, or tuning during Phase 11."
      status: resolved
      verification: "address-selection tests parameterise connectivity without changing results"
    - statement: "MUST NOT place live serials, IP addresses, hostnames, or raw discovery output in Phase 11 tests, documentation, or evidence."
      status: resolved
      verification: "synthetic fixtures and staged-diff privacy audit"
---

<objective>
Replace last-wins per-name cache slots with a distinct-owner-bounded live-record model that deterministically caps attacker-controlled TXT/SRV identity sets, retains every advertised address for an admitted owner, detects TXT identity conflicts, and resolves by address class across arbitrary packet boundaries.

Purpose: make mesh-scale packet accumulation correct before timed goodbye semantics are layered onto the same exact record identities.
Output: `_CachedResourceRecord`, unordered address membership, strict TXT identity validation, and exhaustive cache-level regression tests.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mdns-hardening/11-SPEC.md
@.planning/phases/11-mdns-hardening/11-CONTEXT.md
@.planning/phases/11-mdns-hardening/11-RESEARCH.md
@.planning/phases/11-mdns-hardening/11-PATTERNS.md
@.planning/phases/11-mdns-hardening/11-SOURCE-AUDIT.md
@.planning/phases/11-mdns-hardening/11-01-SUMMARY.md
@src/lifx/network/mdns/dns.py
@src/lifx/network/mdns/discovery.py
@src/lifx/network/mdns/types.py
@src/lifx/network/address.py
</context>

## Artifacts this phase produces

Phase 11 produces `Device.connectivity`, `Device._connectivity`,
`Device._set_connectivity()`, `_LifxServiceRecord` with trailing defaulted
`connectivity` and `addresses` fields, `_CachedResourceRecord.expires_at`,
`_validate_txt_id()`, `_connectivity_from_txt()`,
`_LifxRecordCache._MAX_TXT_RRS_PER_OWNER`,
`_LifxRecordCache._MAX_SRV_RRS_PER_OWNER`,
`_LifxRecordCache._pending_expiries`, `_LifxRecordCache.next_expiry_delay()`,
`_LifxRecordCache.expire()`, and `_discover_lifx_services()`, while preserving the
existing public `create_device_from_record()` name and export because D-03 authorises only
the record/generator removal. It also produces the stable
diagnostic reasons `missing_txt_id`, `invalid_txt_id`, `conflicting_txt_id`,
`missing_product_id`, `invalid_product_id`, `invalid_address`, `malformed_packet`,
`rr_identity_limit`, and `unexpected_cache_flush`, plus the phase-local paths `COVERAGE.md` and
`11-SOURCE-AUDIT.md`. It adds no enum, CLI flag, persistence field, schema, dependency, or
external service integration.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retain live RR identities and select from unordered advertised addresses</name>
  <reversibility rating="reversible">The cache representation, first-admitted TXT/SRV policy, address collection type, and private tie-break are per-call internals with no persisted state.</reversibility>
  <files>src/lifx/network/mdns/discovery.py, src/lifx/network/mdns/types.py, scripts/ipv6_thread_probe.py, tests/test_network/test_mdns/test_discovery.py, tests/test_scripts/test_ipv6_thread_probe.py</files>
  <read_first>
    - .planning/phases/11-mdns-hardening/11-SPEC.md (requirements 3 and 5; AC6-AC15)
    - .planning/phases/11-mdns-hardening/11-CONTEXT.md (D-05, D-06, D-07)
    - .planning/phases/11-mdns-hardening/11-RESEARCH.md (Patterns 1-3, A1-A3, A6, Pitfalls 4 and 7)
    - .planning/phases/11-mdns-hardening/11-PATTERNS.md (bounded collection, RR identity, address retention versus selection)
    - src/lifx/network/mdns/dns.py (`DnsResourceRecord`, `TxtData`, A/AAAA parsed values)
    - src/lifx/network/mdns/discovery.py (`_pick_address`, `_LifxRecordCache.add_packet`, `resolve`, capacity helpers)
    - src/lifx/network/address.py (`validate_address`; final selected-address gate only)
    - scripts/ipv6_thread_probe.py (`_instance_view`, `_pick_address` import/call sites, cache-reporting output)
    - tests/test_network/test_mdns/test_discovery.py (`TestLifxRecordCache`, bounds tests, `_txt` helper)
    - tests/test_scripts/test_ipv6_thread_probe.py (module-level import and cache/report regression coverage)
  </read_first>
  <behavior>
    - Test 1: permutations of TXT, SRV, A, and AAAA packets resolve to equal address sets and the same selected class
    - Test 2: duplicate/replayed RRs retain one address and one emitted instance
    - Test 3: IPv4 outranks ULA, ULA outranks GUA, GUA outranks scoped link-local
    - Test 4: an unscoped link-local is retained in addresses but cannot become `ip`; it alone leaves the instance unresolved
    - Test 5: packet-source fallback can resolve only where the existing single-instance fallback contract permits, but never appears in advertised addresses
    - Test 6: more than 16 unique valid A/AAAA records for one admitted hostname are all retained and compared by set membership
    - Test 7: the existing 1,024 distinct-owner admission bound remains enforced, but refreshes and new RR identities for an already admitted owner are retained without consuming another owner slot
    - Test 8: for TXT and SRV independently, the first 16 distinct complete identities are retained for one owner; every seventeenth or later identity is rejected without eviction, and a refresh of an already retained identity neither consumes capacity nor reports a limit event
    - Test 9: every observation of an unseen TXT/SRV identity while its per-type ceiling is full contributes one `rr_identity_limit` count keyed only by stable record type, including replay of the same rejected candidate; A/AAAA identities never use this reason and remain lossless
    - Test 10: every old `TestLifxRecordCacheBounds` assertion is replaced by an equivalent or stronger new-model assertion; the former seventeenth-address drop case becomes a more-than-16 retention proof
    - Test 11: importing and exercising the probe's synthetic cache/report helpers succeeds against the revised structures without hardware access
  </behavior>
  <action>
    First write cache-level permutation and selection tests with repeated RRs, a deliberately unscoped link-local address, one admitted hostname advertising more than 16 unique valid A/AAAA records, and adversarial TXT/SRV identity floods. Replace single TXT/SRV/A values with owner-admission-bounded `_CachedResourceRecord` collections keyed by the full DNS RR identity: lower-cased owner name, numeric type, class with the cache-flush bit masked, and raw rdata. Reinterpret the existing 1,024 global admission limit as a cap on distinct owner names admitted during one sweep. Add exact named constants `_MAX_TXT_RRS_PER_OWNER = 16` and `_MAX_SRV_RRS_PER_OWNER = 16`. For each admitted owner and each of those two record types independently, an identical live identity refreshes in place. Admit the first 16 distinct identities and reject every later unseen candidate without eviction, regardless of its lexicographic value; a late attacker must never displace an earlier genuine TXT/SRV identity. Packet-order permutation invariance remains required for all legitimate complete sets within the ceiling, while over-cap adversarial scripts assert the explicit first-admitted security policy rather than inventing order independence under attack. Every observation of an unseen candidate while the set is full increments `rr_identity_limit` for stable record type `TXT` or `SRV`, including repeated observation of the same rejected identity; "bounded" refers to fixed counter-key cardinality, while the caller deadline bounds count magnitude. The diagnostic seam carries only reason/type/count and never the owner, rdata, TXT value, hostname, or rejected identity. Pending-goodbye identities remain live and consume their slot until Plan 11-03 expires them. PTR remains outside these live collections. Apply neither per-owner constant nor any global RR-count truncation to A/AAAA: once an owner is admitted, retain every unique currently live valid address identity exactly as D-05 and AC12 require. Explicitly reject the reviewers' suggested 256/1,024 address caps because either can drop a syntactically valid admitted address and therefore conflicts with the locked lossless contract; retain T-11-07R as the honest per-call memory residual and let Plan 11-03 remove the separate CPU residual by scheduling only goodbye-pending identities. Represent record addresses as `frozenset[str]` per the planner's collection discretion, adding it as a trailing dataclass field with `default_factory=frozenset` so the existing construction sites remain compatible. Parse each address once with `ipaddress.ip_address()` on admission and retain its canonical string, while leaving the existing shared `validate_address()` as the final Device boundary. Keep source fallback in its dedicated map. Refactor `_pick_address` onto the new collection while preserving it as the single private classifier consumed by the library and probe. Classify selectable addresses with IPv4 first, explicit `fc00::/7` ULA second, `is_global` GUA third, and scoped link-local fourth; use first-learned only as an internal same-class tie-break and never test set iteration order. Retain an unscoped link-local in the set but return no selected address when it is the only candidate. Rewrite `TestLifxRecordCacheBounds` in place: preserve its defensive intent with direct 1,024-owner and per-type ceiling assertions instead of treating the owner bound as implicit, and replace the old seventeenth-address drop test with the required more-than-16 retention proof without deleting the test function or file. Port `scripts/ipv6_thread_probe.py::_instance_view` and its `_pick_address` call site to the new cache inspection seam in this introducing wave, update `tests/test_scripts/test_ipv6_thread_probe.py`, and run only synthetic probe helpers. Connectivity metadata must not participate in the classifier. Addresses review concerns: resolves the TXT/SRV eviction disagreement in favour of non-displacement, pins replay counting, keeps new dataclass fields compatible, honours lossless D-05 despite the accepted memory residual, and preserves the independently exercised defensive bounds.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCache tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCacheBounds tests/test_scripts/test_ipv6_thread_probe.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The record address field is immutable and unordered, and duplicate syntactically valid addresses collapse by membership
    - Packet-source fallback is absent from the advertised address set
    - The selected `ip` follows the complete locked class order for every arrival permutation
    - Unscoped link-local is retained but never selected or passed to Device construction
    - Incomplete input produces no record and does not poison later complete resolution
    - The existing capacity bound rejects only new owner names after 1,024 distinct owners are admitted; admitted A/AAAA identities remain lossless under D-05 while TXT/SRV identities obey their independent ceilings
    - A synthetic admitted hostname advertising more than 16 unique valid A/AAAA records retains every address; the 1,024 bound applies only to distinct owner admission
    - TXT and SRV are each capped at the first 16 live identities per admitted owner; no later identity can evict an admitted identity
    - Every seventeenth-or-later unseen TXT/SRV observation is rejected, and replay of the same rejected candidate increments only the integer count for `rr_identity_limit` plus `TXT` or `SRV`
    - Legitimate complete RR sets within the ceiling resolve identically across packet permutations; the security policy for over-cap hostile input is explicitly first-admitted rather than packet-order-independent
    - No A/AAAA identity is rejected, evicted, or diagnosed by the TXT/SRV ceiling policy
    - Every old bounds test is replaced in place by equal-or-stronger coverage of the revised structure; no test function or file is deleted
    - The probe's synthetic cache/report tests pass in the same wave and no hardware path is invoked
  </acceptance_criteria>
  <done>One per-sweep cache retains all useful advertised addresses and resolves only a compatible selectable address, independently of packet order or duplicate replay.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate live TXT/SRV construction identity and preserve recoverable conflicts</name>
  <reversibility rating="reversible">Validation helpers and fail-closed private metadata resolution are internal and can change without a data migration or public contract break.</reversibility>
  <files>src/lifx/network/mdns/discovery.py, tests/test_network/test_mdns/test_discovery.py</files>
  <read_first>
    - .planning/phases/11-mdns-hardening/11-SPEC.md (requirements 6-7; AC16-AC19)
    - .planning/phases/11-mdns-hardening/11-CONTEXT.md (D-08 through D-12)
    - .planning/phases/11-mdns-hardening/11-RESEARCH.md (Pattern 2, Pitfall 1, A7 and A10)
    - src/lifx/network/mdns/dns.py (`TxtData.strings` and last-wins `pairs`)
    - src/lifx/network/discovery.py (broadcast serial validation rules)
    - src/lifx/protocol/models.py (`Serial`; do not use its separator-tolerant parser as the only gate)
    - tests/test_network/test_mdns/test_discovery.py (`_txt` helper and serial normalisation tests)
  </read_first>
  <behavior>
    - Test 1: exactly 12 lower- or uppercase hexadecimal characters resolve to one lowercase serial
    - Test 2: absent, empty, separated, wrong-length, non-hex, all-zero, all-ones, and group/multicast IDs are rejected
    - Test 3: repeated conflicting `id=` strings inside one TXT RR and conflicting IDs across live TXT RRs both invalidate the instance
    - Test 4: conflict outcome is invariant to packet/TXT order and does not select first-wins or last-wins
    - Test 5: an invalid instance does not block a later valid instance in the same cache/sweep
    - Test 6: missing or invalid product candidates are rejected; all remaining same-ID TXT RRs resolve only when their normalised product, firmware (including absence), and derived connectivity values are all consistent
    - Test 7: a later complete TXT RR whose full raw identity sorts below the genuine RR but preserves its serial and changes product, firmware, or connectivity leaves the instance unresolved in each adversarial case; it never becomes the effective metadata source
    - Test 8: multiple live SRV RRs for one instance resolve only when target and port agree; a later lower-sorting RR that changes target or port leaves the instance unresolved instead of becoming the effective endpoint
    - Test 9: identical or construction-equivalent TXT/SRV duplicates remain resolvable across packet permutations, and deterministic ordering is used only for storage or presentation
  </behavior>
  <action>
    Extend the synthetic TXT helper so its raw `strings` can contain repeated keys independently of `pairs`. Add `_validate_txt_id()` using exact length and hexadecimal shape plus the broadcast path's zero, all-ones, and group/unicast identity rules; normalise only after validation. Derive candidate IDs from every live TXT RR string, including repeated `id=` entries inside one RR, rather than trusting either last-wins dictionary. Resolve only when the set of live valid IDs has cardinality exactly one. For all live TXT RRs carrying that sole ID, reject individual RRs whose required product is missing or invalid. From every remaining structurally usable identity-bearing RR derive the construction tuple of normalised serial, product, firmware (with absence represented explicitly), and connectivity, with exact private value `2` mapping to Thread and every other value mapping to WiFi without a diagnostic. Resolve TXT metadata only when all live candidates produce one construction tuple; if product, firmware, or connectivity differs, keep the instance unresolved while those values coexist. Apply the same outcome-level rule to every live SRV RR for the instance: normalise its target for comparison, pair it with port, and resolve the endpoint only when the live set contains one target/port value. Raw/full RR lexicographic ordering may make cache storage, diagnostics, or test presentation reproducible, but must never select a metadata or endpoint winner. Add adversarial tests in which each later RR is deliberately lower-sorting than the genuine RR: same serial with changed product, firmware, or connectivity for TXT, and changed target or port for SRV. Each case must remain unresolved under both packet orders, proving attacker-controlled sort position cannot become the effective source. Include missing-versus-present firmware among the disagreement cases. Add consistent-value duplicate/permutation controls that still resolve. Plan 11-03 then proves that goodbye expiry can restore resolution only after it removes the conflicting live identity and leaves one consistent construction tuple or endpoint. Treat absence, malformed forms, and live identity conflict as per-instance rejection states that leave the remaining sweep productive; retain structured reason codes `missing_txt_id`, `invalid_txt_id`, `conflicting_txt_id`, `missing_product_id`, and `invalid_product_id` for Plan 11-03's aggregate summary. Do not choose an arrival-order or lexicographic first/latest winner, and do not broaden the accepted serial syntax through `Serial.from_string()`.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py -k 'serial or txt_id or conflicting or invalid_instance or construction_metadata or srv_conflict' -q</automated>
  </verify>
  <acceptance_criteria>
    - Accepted IDs are exactly 12 hexadecimal characters and stored lowercase
    - Every forbidden form is rejected before an internal record can be emitted
    - All live raw TXT strings contribute to conflict detection; parser and cache last-wins maps cannot hide a conflict
    - Conflicting valid IDs are unresolved regardless of arrival order
    - Rejection of one instance does not abort resolution of unrelated valid instances
    - Connectivity fallback produces no serial/product rejection reason
    - Complete same-ID TXT candidates resolve only when product, firmware, and connectivity are consistent; any differing live value is fail-closed and unresolved
    - Same-instance SRV candidates resolve only when target and port are consistent; any differing live value is fail-closed and unresolved
    - Later lower-sorting conflicting TXT and SRV RRs cannot become effective metadata or endpoint sources, regardless of arrival order
    - Deterministic ordering is limited to storage or presentation and is never a trust selector
  </acceptance_criteria>
  <done>Internal service records carry only a currently unambiguous broadcast-valid serial, one consistent TXT construction tuple, and one consistent SRV endpoint; malformed or conflicting instances remain isolated and recoverable from live cache state.</done>
</task>

</tasks>

<threat_model>
## Security Profile

- `asvs_level: L1` (opportunistic)
- `block_threshold: high`; any unresolved critical/high threat blocks plan completion

## Trust Boundaries

| Boundary | Description |
|---|---|
| Untrusted DNS resource records -> bounded per-sweep cache | Attacker-controlled names, types, classes, rdata, and addresses consume state and influence resolution |
| Untrusted TXT identity -> device serial | An unauthenticated LAN identity is structurally validated before record emission |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-05 | Tampering | attacker-controlled TXT metadata and SRV endpoint selection | high | mitigate | Preserve complete live RR identities, reject displacement, and keep same-ID TXT metadata or same-instance SRV endpoint conflicts unresolved until expiry leaves exactly one consistent TXT construction tuple and one SRV target/port value; adversarial lower-sorting RR tests prove ordering never selects the effective source (both tasks) |
| T-11-06 | Spoofing | TXT serial | high | mitigate | Exact 12-hex unicast validation and conflict invalidation aligned with broadcast rules (Task 2) |
| T-11-07 | Denial of Service | owner and non-address RR admission | high | mitigate | Bound admitted owners to 1,024; admit only the first 16 TXT and first 16 SRV identities per owner; never evict an admitted identity; exact-deduplicate refreshes; and emit privacy-safe `rr_identity_limit` counts (Task 1) |
| T-11-07R | Denial of Service | per-owner valid A/AAAA cardinality | medium | accept | D-05 and AC12 require lossless retention of every admitted address, so the suggested finite caps are rejected as conflicting with the lock. Exact deduplication, syntactic validation, the unchanged caller deadline, per-call ownership, and complete state discard at call end limit duration but do not claim to bound cumulative admitted-address memory. Plan 11-03 separately prevents expiry work from scanning all addresses. |
| T-11-08 | Information Disclosure | fixtures | high | mitigate | Use synthetic IDs, names, and documentation-range addresses only (both tasks) |
| T-11-09 | Spoofing | structural validation mistaken for authentication | medium | accept | Phase 11 makes no authenticity claim; Phase 13 owns live unicast verification |
| T-11-SC | Tampering | package supply chain | low | accept | No install or dependency change occurs |
</threat_model>

## Multi-Source Coverage Audit

The complete audit is in `11-SOURCE-AUDIT.md`. This plan covers MDNS-03, MDNS-05,
MDNS-06, D-05 through D-07, D-12, the resolved R3 adjacency/empty/ordering/idempotency edges,
all resolved R5 edges, all resolved R6 edges except timed expiry recovery, and prohibition P5/P6.
The finite per-owner cap applies to TXT/SRV identities only and uses non-evicting first admission.
D-05/AC12 separately prohibit address truncation, so T-11-07R records only the admitted A/AAAA
memory residual; Plan 11-03 removes the scheduler CPU interaction without weakening retention.

<verification>
- `uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py tests/test_scripts/test_ipv6_thread_probe.py -q`
- `uv run --frozen pytest -q`
- `uv run ruff check src/lifx/network/mdns/discovery.py src/lifx/network/mdns/types.py scripts/ipv6_thread_probe.py tests/test_network/test_mdns/test_discovery.py tests/test_scripts/test_ipv6_thread_probe.py`
- `uv run ruff format --check src/lifx/network/mdns/discovery.py src/lifx/network/mdns/types.py scripts/ipv6_thread_probe.py tests/test_network/test_mdns/test_discovery.py tests/test_scripts/test_ipv6_thread_probe.py`
- `uv run pyright`
</verification>

<success_criteria>
Packet order and duplicate replay no longer discard live RRs; all advertised addresses are retained internally; selection follows the locked class preference; and an instance resolves only with one live exact unicast-valid TXT identity, one consistent TXT construction tuple, and one consistent SRV target/port value.
</success_criteria>

<output>
Create `.planning/phases/11-mdns-hardening/11-02-SUMMARY.md` when done.
</output>
