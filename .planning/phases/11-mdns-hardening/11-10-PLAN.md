---
phase: 11-mdns-hardening
plan: 10
type: execute
wave: 9
depends_on:
  - 11-09
files_modified:
  - .planning/phases/11-mdns-hardening/11-GAP-BASE.txt
  - src/lifx/network/mdns/types.py
  - src/lifx/network/mdns/discovery.py
  - tests/test_network/test_mdns/test_discovery.py
  - tests/test_api/test_api_discovery.py
autonomous: true
gap_closure: true
requirements:
  - MDNS-03
  - MDNS-06
estimate:
  tokens: 21000
  raw_tokens: 42000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "Only a case-insensitive exact instance of _lifx._udp.local can enter the LIFX construction path, reset its idle deadline, become pending work, resolve to a record, or reach the public device generator (D-02, D-04, D-08, D-16)."
    - "A complete, syntactically valid TXT/SRV/A chain rooted in another service produces no LIFX record and no public device, including when unrelated records share a packet with valid LIFX records."
    - "Repeated TXT product, firmware, and connectivity values are reduced in linear work; a second effective value fails that instance closed before record construction, and at most one construction tuple is attempted (D-03, D-04, D-16)."
    - "The private low-level record/generator/converter boundary and Device.connectivity wifi|thread contract remain intact (D-16)."
  artifacts:
    - path: .planning/phases/11-mdns-hardening/11-GAP-BASE.txt
      provides: "Immutable full pre-gap commit SHA for later changed-line and changed-branch coverage."
    - path: src/lifx/network/mdns/types.py
      provides: "Private LIFX record provenance carried from cache resolution to public construction."
    - path: src/lifx/network/mdns/discovery.py
      provides: "Exact service-instance admission and linear TXT consensus."
    - path: tests/test_network/test_mdns/test_discovery.py
      provides: "Deterministic mixed-service, boundary, and adversarial TXT consensus coverage."
    - path: tests/test_api/test_api_discovery.py
      provides: "Public generator fixtures carrying valid service provenance."
  key_links:
    - from: src/lifx/network/mdns/discovery.py::_LifxRecordCache.add_packet
      to: src/lifx/network/mdns/discovery.py::_LifxRecordCache.resolve
      via: "Only exact LIFX instance owners and SRV-linked address candidates are eligible for resolution."
    - from: src/lifx/network/mdns/discovery.py::_LifxRecordCache.resolve
      to: src/lifx/network/mdns/discovery.py::discover_devices_mdns
      via: "The exact service instance is carried on _LifxServiceRecord and revalidated before public device construction."
    - from: src/lifx/network/mdns/discovery.py::_resolve_txt_metadata
      to: src/lifx/network/mdns/types.py::_LifxServiceRecord
      via: "Single-value consensus emits zero or one metadata tuple rather than a product of alternatives."
---

<objective>
Close the first authoritative Phase 11 gap with a production-quality vertical slice: exact LIFX service provenance from packet admission through public device emission, followed by bounded linear TXT consensus.

Purpose: Prevent unrelated DNS-SD services and repeated valid-looking metadata from entering or amplifying the LIFX construction path while preserving D-16's private/public boundary.
Output: A fixed pre-gap SHA, provenance-bearing private records, exact admission/resolution/public checks, linear consensus, and deterministic regression tests.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-mdns-hardening/11-CONTEXT.md
@.planning/phases/11-mdns-hardening/11-RESEARCH.md
@.planning/phases/11-mdns-hardening/11-PATTERNS.md
@.planning/phases/11-mdns-hardening/11-SPEC.md
@.planning/phases/11-mdns-hardening/11-VERIFICATION.md
@.planning/phases/11-mdns-hardening/11-REVIEW.md
@.planning/phases/11-mdns-hardening/11-09-SUMMARY.md

Assumption delta: no-change. The phrase `one-second` denotes a duration and does not change the singular/plural identity model.
Discovery note: the spike's broadcast schedule is a separate Phase 10 path. This plan changes only deterministic mDNS cache/parser construction; it requires neither broadcast changes nor hardware validation.
</context>

## Artifacts this phase produces

- `.planning/phases/11-mdns-hardening/11-GAP-BASE.txt` — one full lowercase pre-gap commit SHA and a trailing newline.
- `src/lifx/network/mdns/types.py` — private record provenance required by the public construction boundary.
- `src/lifx/network/mdns/discovery.py` — exact service-instance admission and linear metadata consensus.
- `tests/test_network/test_mdns/test_discovery.py` and `tests/test_api/test_api_discovery.py` — synthetic regression coverage using documentation-range addresses and pseudonymous serials only.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry exact LIFX service provenance from admission to public emission</name>
  <files>.planning/phases/11-mdns-hardening/11-GAP-BASE.txt, src/lifx/network/mdns/types.py, src/lifx/network/mdns/discovery.py, tests/test_network/test_mdns/test_discovery.py, tests/test_api/test_api_discovery.py</files>
  <read_first>
    - `.planning/phases/11-mdns-hardening/11-VERIFICATION.md` gaps 1 and 5, and `11-REVIEW.md` CRITICAL-1.
    - `src/lifx/network/mdns/discovery.py`: `LIFX_MDNS_SERVICE`, `_LifxRecordCache.add_packet`, `_LifxRecordCache.resolve`, `_LifxRecordCache.pending_targets`, `_discover_lifx_services`, `_create_device_from_record`, and `discover_devices_mdns`.
    - `src/lifx/network/mdns/types.py`: `_LifxServiceRecord`.
    - `tests/test_network/test_mdns/test_discovery.py`: record helpers, `TestLifxRecordCache`, `TestDiscoverPrivateLifxServices`, and `TestDiscoverDevicesMdns`.
    - `tests/test_api/test_api_discovery.py`: `TestDiscoverMdns` record fixtures.
  </read_first>
  <behavior>
    - Test `test_exact_case_insensitive_lifx_instance_reaches_public_generator`: a mixed-case `Synthetic._LiFx._UdP.LoCaL` TXT/SRV/A chain resolves once and yields the expected public device.
    - Test `test_complete_unrelated_service_chain_is_rejected_at_every_boundary`: valid TXT/SRV/A under `_ipp._tcp.local` is not admitted as LIFX activity, does not reset the idle deadline, is absent from `pending_targets()` and `resolve()`, and yields no public device.
    - Test `test_lifx_lookalike_suffixes_are_rejected`: the bare service owner, prefix-only lookalikes, and names with labels after `_lifx._udp.local` fail exact-instance admission.
    - Test `test_unrelated_records_in_mixed_packet_do_not_gain_lifx_provenance`: an unrelated complete chain cannot borrow provenance from a valid LIFX instance in the same packet.
    - Test `test_public_generator_rejects_record_without_exact_service_provenance`: a directly fabricated private record with absent or unrelated provenance is rejected before device construction.
  </behavior>
  <action>
    First create `11-GAP-BASE.txt` from the current full `git rev-parse HEAD`; validate it against `^[0-9a-f]{40}$` and never overwrite it later. Add a trailing private optional `service_instance` field to `_LifxServiceRecord`, defaulting to absent for existing direct private-converter fixtures, and ensure cache resolution always supplies it. The public generator must reject the absent default. Introduce one canonical name normaliser that removes only the optional terminal root dot and uses `casefold()`, plus an exact LIFX-instance predicate that requires a non-empty instance portion immediately before the canonical `_lifx._udp.local` service suffix. Do not use substring membership.

    Rework `_LifxRecordCache.add_packet` so TXT and SRV construction owners are admitted only when that predicate passes. Preserve cross-packet and A-before-SRV assembly by treating A/AAAA owners as bounded address candidates only: they acquire LIFX construction eligibility solely when a live SRV from an admitted exact LIFX instance references their host target. A valid LIFX instance in a mixed packet must not confer eligibility on unrelated service owners or their targets. Derive `has_lifx`, the idle-deadline reset, `pending_targets()`, `resolve()`, and emitted records from that same provenance relation. At `discover_devices_mdns`, revalidate `record.service_instance` before calling the private converter so hand-fabricated or stale private records cannot cross the public boundary. Preserve D-08/D-09 silent continuation, D-10/D-12 streaming and deduplication, and D-16 public signatures and connectivity values.

    Update only the existing mDNS API test fixtures that legitimately model records entering the public generator so they carry an exact synthetic service instance. Use reserved addresses and synthetic serials. Keep all changes dependency-free and commit with a Conventional Commit message using `git commit -S -s`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-11-10-uv-cache uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCache::test_complete_unrelated_service_chain_is_rejected_at_every_boundary tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCache::test_lifx_lookalike_suffixes_are_rejected tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCache::test_unrelated_records_in_mixed_packet_do_not_gain_lifx_provenance tests/test_network/test_mdns/test_discovery.py::TestDiscoverDevicesMdns::test_exact_case_insensitive_lifx_instance_reaches_public_generator tests/test_network/test_mdns/test_discovery.py::TestDiscoverDevicesMdns::test_public_generator_rejects_record_without_exact_service_provenance tests/test_api/test_api_discovery.py::TestDiscoverMdns -q -W error::ResourceWarning</automated>
  </verify>
  <acceptance_criteria>
    - `11-GAP-BASE.txt` contains exactly the full pre-gap SHA used by Plan 11-14.
    - Exact service provenance is enforced at admission, resolution, idle-deadline, pending-target, record, and public-device boundaries.
    - Complete unrelated and lookalike chains remain ineligible even beside a valid LIFX chain.
    - A-before-SRV packet ordering still resolves when the later SRV belongs to an exact LIFX instance.
    - D-16's private generator/converter and public async API remain unchanged apart from the private provenance field.
  </acceptance_criteria>
  <done>The exact mixed-case LIFX happy path works end to end, while complete unrelated and forged-provenance paths fail closed at every named boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace TXT Cartesian expansion with linear single-value consensus</name>
  <files>src/lifx/network/mdns/discovery.py, tests/test_network/test_mdns/test_discovery.py</files>
  <read_first>
    - `src/lifx/network/mdns/discovery.py`: `_parse_txt`, `_resolve_txt_metadata`, `_LifxRecordCache.resolve`, and the existing 16-TXT-per-owner ceiling.
    - `tests/test_network/test_mdns/test_discovery.py`: existing TXT ambiguity, duplicate, malformed-value, and cache-bound tests.
    - `.planning/phases/11-mdns-hardening/11-SPEC.md`: edge rows for duplicate TXT, conflicting TXT, malformed optional metadata, and unsupported connectivity.
  </read_first>
  <behavior>
    - Test `test_txt_consensus_constructs_at_most_one_tuple_for_repeated_values`: repeated identical product, firmware, and connectivity values invoke `_LifxServiceRecord` construction no more than once.
    - Test `test_txt_consensus_fails_on_second_effective_product_before_later_fields`: the second distinct product value rejects the owner without processing a product-by-firmware-by-connectivity combination space.
    - Test `test_txt_consensus_fails_on_conflicting_firmware_or_connectivity`: a second effective firmware or connectivity value rejects the instance with no record.
    - Test `test_txt_consensus_ignores_exact_duplicates_and_malformed_optional_values`: duplicate effective values remain one value, while existing invalid optional-value handling remains source-backed and deterministic.
  </behavior>
  <action>
    Refactor `_resolve_txt_metadata` into a single pass over the already count-bounded TXT records. Maintain at most one effective product, firmware, and connectivity value plus explicit conflict state. Compare parsed effective values, not raw spelling, so exact duplicates and equivalent normalised connectivity do not create false ambiguity. As soon as a second distinct effective value is observed for any field, mark that instance incomplete and return without constructing a record or scanning combinations. After the pass, construct zero or one metadata tuple and call `_LifxServiceRecord` at most once. Preserve D-03 serial/MAC reconciliation, D-04 model classification, D-08/D-09 rejection semantics, the existing 16-per-owner ceiling, and D-16's public `wifi|thread` values. Do not introduce a Cartesian product, sorting of a candidate product, or a new dependency. Commit with a Conventional Commit message using `git commit -S -s`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-11-10-uv-cache uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCache::test_txt_consensus_constructs_at_most_one_tuple_for_repeated_values tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCache::test_txt_consensus_fails_on_second_effective_product_before_later_fields tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCache::test_txt_consensus_fails_on_conflicting_firmware_or_connectivity tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCache::test_txt_consensus_ignores_exact_duplicates_and_malformed_optional_values -q</automated>
  </verify>
  <acceptance_criteria>
    - Work is linear in the number of retained TXT records and bounded by the unchanged 16-per-owner ceiling.
    - A conflicting product, firmware, or connectivity value fails the affected instance before construction.
    - Identical duplicates produce one effective tuple and `_LifxServiceRecord` is invoked at most once per instance.
    - Existing serial, firmware, connectivity, and unsupported-product validation remains effective.
  </acceptance_criteria>
  <done>TXT metadata consensus has bounded linear behaviour, early conflict rejection, and at most one record-construction attempt per exact LIFX instance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UDP packet -> mDNS cache | Untrusted DNS names and RDATA attempt to enter retained construction state. |
| Cache -> private record | Cross-record provenance and TXT values become a candidate LIFX identity. |
| Private record -> public generator | Internal discovery data can instantiate a user-visible Device. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-10-01 | Spoofing | `_LifxRecordCache.add_packet` | high | mitigate | Require an exact case-insensitive LIFX service instance and explicit SRV-target linkage before any record gains LIFX provenance. |
| T-11-10-02 | Tampering | `_resolve_txt_metadata` | high | mitigate | Reject a second effective value immediately and construct at most one metadata tuple. |
| T-11-10-03 | Elevation of privilege | `discover_devices_mdns` | high | mitigate | Revalidate carried service provenance before public device construction. |
| T-11-10-04 | Denial of service | repeated TXT values | high | mitigate | Use a one-pass consensus algorithm under the unchanged 16-record owner ceiling. |
| T-11-10-05 | Information disclosure | rejection diagnostics | medium | mitigate | Test and retain reason/count-only diagnostics; do not log names, serials, addresses, or TXT values for these rejections. |
| T-11-10-SC | Tampering | package supply chain | low | accept | No package installation or dependency change occurs in this plan. |
</threat_model>

<verification>
- Run `UV_CACHE_DIR=/private/tmp/lifx-async-11-10-uv-cache uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py tests/test_api/test_api_discovery.py -q -W error::ResourceWarning`.
- Run `UV_CACHE_DIR=/private/tmp/lifx-async-11-10-uv-cache uv run --frozen ruff check src/lifx/network/mdns/types.py src/lifx/network/mdns/discovery.py tests/test_network/test_mdns/test_discovery.py tests/test_api/test_api_discovery.py`.
- Run `UV_CACHE_DIR=/private/tmp/lifx-async-11-10-uv-cache uv run --frozen pyright src/lifx/network/mdns/types.py src/lifx/network/mdns/discovery.py`.
- Confirm `git verify-commit HEAD` succeeds after each task and each commit contains a `Signed-off-by:` trailer without printing signer identity into evidence.
</verification>

<success_criteria>
- The verifier's exact-service-provenance and TXT-consensus gaps are closed by executable tests, not historical counts.
- No high or critical threat remains unresolved.
- No broadcast, hardware, generated protocol/product, dependency, or public-API scope is changed.
</success_criteria>

## Source coverage audit

| Source item | Coverage |
|-------------|----------|
| GOAL: correct at mesh scale and safe before default promotion | COVERED by exact provenance and bounded consensus. |
| REQ MDNS-03 | COVERED by provenance-safe TXT/SRV/A assembly. |
| REQ MDNS-06 | COVERED by exact admission and structural single-value validation. |
| RESEARCH constraints: async stdlib, per-call state, private boundary | COVERED; no dependency or public API change. |
| CONTEXT D-02/D-03/D-04/D-08/D-09/D-10/D-12/D-16 | COVERED explicitly in tasks and must-haves. |
| SPEC resolved edges: duplicate/conflicting TXT, mixed packet/order, unsupported metadata | COVERED by named tests. |
| SPEC prohibitions | COVERED: no single-shot truncation, serial fallback, blind IPv6 preference, multicast listener, reused state, packet-address target, or implicit default integration is introduced. |
| Deferred/out-of-phase: Phase 10 broadcast scheduling, default discovery integration, live hardware | EXCLUDED by locked boundary. |

<output>
Create `.planning/phases/11-mdns-hardening/11-10-SUMMARY.md` when done.
</output>
