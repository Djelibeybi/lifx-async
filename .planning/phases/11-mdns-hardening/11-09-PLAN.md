---
phase: 11-mdns-hardening
plan: 09
type: execute
wave: 8
depends_on: [11-08]
files_modified:
  - .planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md
autonomous: false
requirements: [MDNS-08]
gap_closure: true
tags: [mdns, gap-closure, privacy, history-disposition, verification]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "The operator-selected `rewrite` or `no-rewrite` disposition is applied exactly; no shared baseline, other branch, worktree, tag, remote ref, or external state changes."
    - "Commits cd0c3ac and f7c7c6f remain exact and unamended; a rewrite is unavailable and execution stops when either lies in the affected range."
    - "At the history-disposition boundary, `rewrite` compares the tree immediately before and after rebase and preserves ordered authorship/subject metadata, count, DCO, and signatures; `no-rewrite` preserves the complete pre-existing commit-ID sequence through that boundary."
    - "Any later ledger/evidence commit is separately identified as new evidence after the disposition boundary, so it is not misrepresented as unchanged HEAD or byte-identical post-evidence tree state."
    - "No live or unresolved phase-owned candidate remains; inherited baseline findings are reported separately and never silently widened into a shared-history rewrite."
    - "No push, force-push, reflog expiry, garbage collection, mapping access, raw-value output, source edit, test edit, or changelog edit occurs."
    - "Fresh final-head focused tests, the complete frozen suite, Ruff check/format, strict Pyright, and value-suppressed current/diff/history audits pass before independent Phase 11 re-verification."
    - "The final report makes bounded claims about the current branch's pushable range and notes that unreachable local objects may remain until ordinary Git expiry."
  artifacts:
    - path: ".planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md"
      provides: "Authorised history-disposition ledger, invariant results, final value-suppressed privacy audit, and current evidence gates"
      contains: "Final history disposition result"
  key_links:
    - from: ".planning/phases/11-mdns-hardening/11-08-SUMMARY.md"
      to: "current branch history"
      via: "the exact `rewrite` or `no-rewrite` decision and audit are asserted as preconditions before mutation"
      pattern: "(rewrite|no-rewrite)"
    - from: "pre-disposition tree hash"
      to: "post-disposition/pre-evidence tree hash"
      via: "exact boundary-time tree equality proves the authorised history operation preserved source/planning content before the separate ledger commit"
      pattern: "disposition_tree_equal.*true"
    - from: "rewritten STATE history stream"
      to: ".planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md"
      via: "location/category-only scanner proves the affected range clean without reproducing values"
      pattern: "phase_owned_unresolved_count.*0"
---

<objective>
Apply the operator-authorised history disposition, rewriting and re-signing only when the audit requires it, then produce fresh current-head regression and privacy evidence for independent Phase 11 re-verification.

Purpose: remove a confirmed local-only phase-owned candidate when necessary, or preserve commit IDs when the audit proves no rewrite is required, without weakening repository signature/DCO/privacy guarantees.
Output: the exact approved history disposition, a final value-suppressed remediation ledger, fresh gates, and `11-09-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
@$HOME/.codex/skills/evidence-privacy-audit/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md
@.planning/phases/11-mdns-hardening/11-07-SUMMARY.md
@.planning/phases/11-mdns-hardening/11-08-SUMMARY.md
@.planning/phases/11-mdns-hardening/11-CONTEXT.md
@.planning/phases/11-mdns-hardening/11-SPEC.md
@.planning/phases/11-mdns-hardening/11-SOURCE-AUDIT.md

<interfaces>
History-disposition contract:
- Plan 11-08 supplies baseline-versus-phase ownership, `rewrite_required`, exact affected range when applicable, reachability, and local-only status.
- The 11-08 summary must record the exact operator response `rewrite` or `no-rewrite`.
- Repository commits require `git commit -S -s`; the signing key is configured by the repository environment.
- A temporary safety ref lives outside `refs/heads` and is deleted only after all tree/count/authorship/signature/DCO/privacy checks pass.
</interfaces>
</context>

## Artifacts this phase produces

This plan changes commit identities only for an explicitly authorised `rewrite`; `no-rewrite` preserves the graph. Both paths update only the remediation ledger and produce no source, test, API, dependency, schema, external-service, capability-matrix, hardware, mapping, or changelog change.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Establish signature validity and preservation eligibility before history mutation</name>
  <precondition>`11-08-SUMMARY.md` records exactly `rewrite` or `no-rewrite`, and the committed audit still matches the current branch/ref/worktree state.</precondition>
  <what-built>The executor checks signature headers and DCO trailers without printing identity values, attempts cryptographic verification of cd0c3ac, f7c7c6f, both 11-07 recovery commits, and the 11-08 audit commit, and independently recomputes whether the audited affected range contains either preserved authority commit.</what-built>
  <how-to-verify>If in-environment `git verify-commit` succeeds for every named object, record the result and continue. If and only if object signature headers and DCO trailers are present but the sandbox cannot access the configured verifier/keyring, verify the exact commit IDs in an approved external shell and confirm success. Invalid/missing signatures or DCO block both dispositions. If the audited rewrite range contains cd0c3ac or f7c7c6f, `rewrite` is invalid regardless of local-only status: reply `stop`, retain the graph unchanged, and escalate the preservation-versus-sanitisation conflict.</how-to-verify>
  <verify>
    <automated>uv run --frozen python -c 'from pathlib import Path; text = Path(".planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md").read_text(encoding="utf-8"); assert all(token in text for token in ("rewrite_eligible:", "preserved_authority_in_affected_range:", "remote_refresh:", "phase_owned_unresolved_count:"))' &amp;&amp; git merge-base --is-ancestor cd0c3ac HEAD &amp;&amp; git merge-base --is-ancestor f7c7c6f HEAD</automated>
  </verify>
  <acceptance_criteria>
    - Every required existing commit is structurally signed+DCO and cryptographically verifies in-process or through the approved external-verification escalation
    - Environment denial is recorded distinctly from invalid or missing object signatures
    - The exact affected range is recomputed against the current disposition-boundary head
    - Rewrite cannot resume when either preserved authority commit is in the range; the only response is `stop`
    - No signer identity, email, commit body, candidate value, or mapping value is printed or recorded
  </acceptance_criteria>
  <resume-signal>Reply `verified` only when signatures are valid and the selected disposition is eligible; reply `stop` for invalid signatures or a preservation conflict. Do not include private values.</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: Apply the authorised history disposition without widening scope</name>
  <precondition>Task 1 records successful cryptographic verification and eligibility for the exact `rewrite` or `no-rewrite` disposition; branch/ref/worktree checks still match the audit; the worktree/index are clean.</precondition>
  <reversibility rating="costly">The `rewrite` path changes descendant commit IDs but was explicitly approved in 11-08 and remains recoverable from the temporary ref/reflog; the `no-rewrite` path is fully reversible because it changes no commit identity.</reversibility>
  <files>.git current-branch commit graph only; no final worktree file changes</files>
  <read_first>
    - .planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md (exact affected commit, parent, head, counts, and local-only proof)
    - .planning/phases/11-mdns-hardening/11-08-SUMMARY.md (explicit rewrite/no-rewrite authority)
    - AGENTS.md (signed+DCO commits and privacy constraints)
    - $HOME/.codex/skills/evidence-privacy-audit/SKILL.md (history sanitisation and bounded claims)
  </read_first>
  <action>
    Re-run every 11-08 baseline/phase ownership, range, ref, worktree, and remote-containment assertion. If any audited value, classification, or sharing status changed, stop without mutation and return to a new blocking decision. Record the selected disposition and non-sensitive invariants in a `mktemp -d` directory: branch ref, disposition-boundary HEAD, tree hash, the complete ordered pre-existing commit-ID sequence, ordered commit count, and a digest of ordered author identity/date plus subject metadata without printing inputs.

    For `no-rewrite`, require `rewrite_required: false`, zero live/unresolved phase-owned candidates, and the operator's private confirmation of any approved pseudonyms. Do not create a safety ref or run rebase; at the disposition boundary assert the recorded HEAD, tree, count, ref, and complete ordered pre-existing commit-ID sequence are unchanged, then proceed to Task 3. The later ledger commit is new evidence and is not part of this unchanged-identity assertion.

    For `rewrite`, require `rewrite_required: true`, `local_only: true`, `rewrite_eligible: true`, and `preserved_authority_in_affected_range: none`. Independently enumerate the exact earliest-affected-parent through disposition-boundary-HEAD range and stop if it contains cd0c3ac or f7c7c6f; this invariant overrides the selected response because those objects must remain exact. Record the affected commit/parent list and create one temporary safety ref under `refs/codex/phase-11-privacy-rewrite/` pointing to the exact original boundary HEAD; never create or push a branch/tag containing the candidate.

    On the `rewrite` path only, use a non-interactive `git rebase --interactive --rebase-merges` rooted at the recorded parent and configure the rebase itself to GPG-sign each recreated commit. An ephemeral sequence editor must mark every audited phase-owned introduction/modification commit requiring sanitisation for edit and leave removal-only and inherited-baseline commits untouched except for descendant recreation and signing; no sequence-editor or patch file is tracked, and no blanket amend exec step may bypass the per-amend staged-content gate. At each edit stop, run the privacy helper against the checked-out STATE and use a non-echoing in-memory utility to replace exactly the report-listed operational candidate line(s) with the same neutral Phase 14 re-derivation sentence used by 11-07. The utility must assert the expected category/count for that commit and emit no content.

    Before every rewrite-path `git commit --amend -S -s --no-edit`, clear the index, stage only the repository-relative `.planning/STATE.md` path, and assert that the complete staged path set is exactly that one path; an empty or additional path set blocks the amend. Run the value-suppressed scanner on the staged `.planning/STATE.md`, then pipe `git diff --cached --no-ext-diff --binary` to the same scanner with `-` as its input path. Scanner exit 2, a failed or incomplete staged-diff scan, an unexpected staged path, or scanner exit 1 with any live or unresolved candidate blocks before the amend; exit-1 locations may proceed only after value-suppressed classification proves every hit inherited or resolved. Emit and record only category/location/direction/disposition, never raw candidate content. Run `git diff --cached --check`; only after every gate passes may the executor run the signed+DCO amend and continue the rebase. Descendant commits that need no STATE content edit are signed by the rebase's signing option and must not be amended through an empty-index shortcut. If the rebase or signing fails, run `git rebase --abort`, retain the safety ref, and stop; do not improvise with filter-repo, unsigned commit-tree calls, reset-hard, or a package install.

    After a rewrite and before any ledger edit/evidence commit, require equality of the original and post-rebase boundary tree hashes, ordered commit counts, and the non-sensitive ordered author/date/subject digest. Confirm cd0c3ac and f7c7c6f remain the same objects in the new ancestry. Verify every rewritten commit cryptographically with `git verify-commit` and verify its DCO trailer without printing email values. If cryptographic verification is unavailable solely because the executor cannot access the configured verifier/keyring, pause at a blocking external-verification escalation and retain the safety ref until the exact rewritten commit IDs verify; invalid/missing signatures or DCO require `git rebase --abort` when still possible or an explicit stop with the safety ref retained. Re-run value-suppressed snapshot/patch classification and require zero live/unresolved phase-owned candidates; inherited baseline candidates remain separately classified. On `no-rewrite`, the tracked ledger must already record `phase_owned_unresolved_count: 0`. On `rewrite`, leave the tracked ledger's pre-remediation non-zero count unchanged until Task 3; record the immediate zero-result proof only in the value-suppressed temporary result under the existing `mktemp -d` directory, keep it outside the tracked boundary, and remove that temporary directory after the boundary checks succeed. Confirm no local/remote/tag/worktree ref other than the temporary safety ref contains the old affected commit, then delete only that exact temporary ref with `git update-ref -d`. Do not expire reflogs or run garbage collection; record that unreachable old objects may persist locally but are not in the current branch's pushable range. Do not push.
  </action>
  <verify>
    <automated>git diff --quiet &amp;&amp; git diff --cached --quiet &amp;&amp; git merge-base --is-ancestor cd0c3ac HEAD &amp;&amp; git merge-base --is-ancestor f7c7c6f HEAD &amp;&amp; uv run --frozen python -c 'import re; from pathlib import Path; text = Path(".planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md").read_text(encoding="utf-8"); required = ("rewrite_required:", "rewrite_eligible:", "preserved_authority_in_affected_range: none", "phase_owned_unresolved_count:"); assert all(token in text for token in required); rewrite = re.search(r"(?m)^rewrite_required:\s*(true|false)\s*$", text); unresolved = re.search(r"(?m)^phase_owned_unresolved_count:\s*(\d+)\s*$", text); assert rewrite and unresolved; assert rewrite.group(1) == "true" or int(unresolved.group(1)) == 0'</automated>
  </verify>
  <acceptance_criteria>
    - `no-rewrite` leaves the disposition-boundary HEAD and complete pre-existing commit-ID sequence unchanged; `rewrite` changes only the exact eligible current-branch range
    - The immediate pre/post-disposition trees, commit counts, and ordered author/date/subject digests match before any later evidence commit
    - cd0c3ac and f7c7c6f retain their exact identities and are never amended or re-signed
    - Every rewritten commit is cryptographically signed and retains a DCO trailer; the unchanged path re-verifies existing signatures
    - Every rewrite-path amend is preceded by an exact-singleton `.planning/STATE.md` staged-path assertion plus value-suppressed staged-file and staged-binary-diff scans; unexpected paths, scan incompleteness/failure, exit 2, or a live/unresolved exit-1 finding blocks before commit creation
    - Value-suppressed classification records zero live/unresolved phase-owned candidates and keeps inherited baseline findings separate
    - A temporary safety ref exists only on the rewrite path and is deleted after all invariants pass; reflogs/object storage are not expired or collected
    - No push, source/test edit, mapping access, or raw-value output occurs
  </acceptance_criteria>
  <done>The approved disposition is applied: the exact local-only lineage is rewritten and re-signed when required, or commit IDs remain unchanged when not required, with zero live/unresolved phase-owned candidate and no widened scope.</done>
</task>

<task type="auto">
  <name>Task 3: Record bounded final evidence and rerun fresh final-head gates</name>
  <files>.planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md</files>
  <read_first>
    - .planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md (preflight and decision evidence)
    - .planning/phases/11-mdns-hardening/11-SOURCE-AUDIT.md (complete source coverage)
    - .planning/phases/11-mdns-hardening/11-SPEC.md (P6 fail-closed state, P7 privacy, and all resolved acceptance boundaries)
    - .planning/phases/11-mdns-hardening/11-VERIFICATION.md (historical gaps_found result; do not overwrite with a self-issued pass)
  </read_first>
  <action>
    Append `Final history disposition result` to the remediation ledger with the selected option, pre/post-disposition boundary heads, the complete pre-existing identity invariant result for no-rewrite, exact range/count when rewritten, immediate boundary-time tree/count/author-digest/signature/DCO/ref results, baseline-versus-phase classifications, current branch, and merge base. Claim only `zero live or unresolved phase-owned candidate in the audited STATE scope`; inherited baseline findings retain their explicit dispositions. Record only Git object identifiers and location/category/direction/disposition evidence; never record the candidate value, a value hash, author email, external mapping, physical identity, packet, or discovery output. On rewrite, state that unreachable objects may remain in local reflogs/object storage until ordinary expiry; on either path state that no push or garbage collection occurred. Reserve a separate `evidence_commit` field for the new ledger commit so it cannot be confused with a disposition-boundary identity or tree invariant.

    Run the privacy helper over current STATE, the remediation ledger, staged diff, unstaged diff, merge-base/HEAD snapshots, and the complete confirmed unpushed STATE patch stream with commit metadata suppressed. Classify all hits by baseline/phase ownership and record only category/location/direction/disposition; any live/unresolved phase-owned candidate, inherited live/unresolved candidate requiring shared-history coordination, or incomplete scan stops completion. Then run fresh current-head evidence: the exact D-15/D-16 assertion and three focused regressions, the Phase 11 focused mDNS/API/device/probe set with `ResourceWarning` as an error, the complete frozen suite with sugar disabled, Ruff check, Ruff format check, and strict Pyright. These are new final-head results; do not reuse the earlier 413/3,871 counts as current and do not fabricate a count in advance.

    If any source/test gate fails, diagnose and stop for a separate plan without editing undeclared source/tests or weakening authority. Before committing, clear the index and stage only the final remediation ledger by exact path. Run the value-suppressed scanner over the staged ledger and over `git diff --cached --no-ext-diff --binary` through scanner standard input. Exit 2 or any incomplete scan blocks; exit 1 blocks until every location/category hit is classified, and any live or unresolved candidate blocks the commit. Confirm the staged path set contains only the ledger and run `git diff --cached --check`, then commit with `git commit -S -s`. Record that commit's identifier as the separate `evidence_commit`, verify its DCO and cryptographic signature, and create `11-09-SUMMARY.md`. If the verifier is unavailable only because the sandbox cannot access its environment, pause for approved external verification of the exact evidence commit; invalid/missing signature or DCO blocks completion. Leave `11-VERIFICATION.md` at its historical `gaps_found` result; the next action is independent Phase 11 verification, not a self-issued pass.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-phase11-privacy-uv-cache uv run $HOME/.codex/skills/evidence-privacy-audit/scripts/scan_private_candidates.py .planning/STATE.md .planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md &amp;&amp; uv run --frozen python -c 'from pathlib import Path; text = Path(".planning/phases/11-mdns-hardening/11-PRIVACY-REMEDIATION.md").read_text(encoding="utf-8"); assert "phase_owned_unresolved_count: 0" in text and "Final history disposition result" in text' &amp;&amp; uv run --frozen python -c 'from lifx.network.mdns.discovery import _LifxRecordCache; assert (_LifxRecordCache._MAX_ADDRESS_RRS_PER_OWNER, _LifxRecordCache._MAX_ADDRESS_RRS_PER_SWEEP) == (256, 1024)' &amp;&amp; uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCacheBounds::test_address_owner_overflow_fails_closed_without_selecting_a_subset tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCacheBounds::test_sweep_address_budget_cannot_be_bypassed_across_owners tests/test_network/test_mdns/test_phase_contract.py::TestPhase11SurfaceContract::test_record_to_device_factory_is_internal_with_its_record_type -q &amp;&amp; uv run --frozen pytest tests/test_network/test_mdns tests/test_devices/test_base.py tests/test_api/test_api_discovery.py tests/test_scripts/test_ipv6_thread_probe.py -q -W error::ResourceWarning &amp;&amp; uv run --frozen pytest -q -p no:sugar &amp;&amp; uv run --frozen ruff check . &amp;&amp; uv run --frozen ruff format --check . &amp;&amp; uv run --frozen pyright &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Final current/staged/unstaged/snapshot/patch scans are complete, contain no live/unresolved phase-owned candidate, and separately disposition inherited baseline hits
    - The ledger records only bounded scope, Git object identifiers, category/location/disposition, and invariant outcomes
    - The ledger commit is separately identified as new evidence after the disposition boundary; no claim requires unchanged final HEAD or post-evidence tree equality
    - Fresh focused, complete frozen-suite, Ruff, format, and strict Pyright gates pass at the final head
    - No source/test change is made to force a gate green and no historical result is relabelled current
    - The final ledger commit and every rewritten commit are signed+DCO and cryptographically verified through the environment-aware escalation when required
    - Phase 11 remains pending independent re-verification against MDNS-01 through MDNS-08
  </acceptance_criteria>
  <done>The authorised branch disposition and current evidence have zero live/unresolved phase-owned finding within the declared scope, fresh final-head gates pass, and Phase 11 is ready for independent re-verification.</done>
</task>

</tasks>

<threat_model>
## Security Profile

- `asvs_level: L1`
- `block_threshold: high`; all critical/high findings must be mitigated before completion

## Trust Boundaries

| Boundary | Description |
|---|---|
| Operator decision -> Git history mutation | Only the exact audited local-only range may be rewritten. |
| Original branch graph -> rewritten graph | Final content, authorship, DCO, signatures, and commit count must survive changed commit IDs. |
| Unreachable old objects -> pushable refs | Old objects may persist locally but must not remain reachable from refs that can be pushed. |
| Fresh head -> verification claims | Historical green results cannot be presented as evidence for rewritten commit IDs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-39 | Tampering | Rebase/rewrite range | high | mitigate | Exact precondition, preserved-authority exclusion, temporary non-branch safety ref, boundary-tree/count/author-digest equality, and abort-on-drift constrain Task 2. |
| T-11-40 | Repudiation | Rewritten commits | high | mitigate | Every rewritten commit is amended with `-S -s`, then independently checked for a valid signature and DCO trailer. |
| T-11-41 | Information Disclosure | Historical candidate | high | mitigate | Non-echoing replacement plus value-suppressed range scans; temporary ref deleted after proof; no push/GC/mapping access. |
| T-11-42 | Tampering | Other branches/worktrees/remotes | high | mitigate | Ref/worktree/remote containment is rechecked immediately before mutation; only current branch moves and no remote command writes state. |
| T-11-43 | Repudiation | Fresh verification evidence | medium | mitigate | Task 3 reruns focused/full/static gates at the disposition head and records actual results without recycled counts. |
| T-11-SC | Tampering | Package supply chain | low | accept | No dependency or package installation is permitted; existing uv lock/environment only. |
</threat_model>

## Multi-Source Coverage Audit

The physical Phase 11 set continues to cover GOAL, MDNS-01 through MDNS-08, all in-scope RESEARCH constraints, D-01 through D-16, all 29 applicable SPEC Edge Coverage truths, and all 7 Prohibitions. This plan claims only MDNS-08, RESEARCH privacy/history evidence, CONTEXT D-09, and SPEC P7; 11-07 explicitly retains P2, P6, and P7 in its must-haves. Plans 11-01 through 11-07 retain every functional/API/cache truth; 11-08 supplies an explicit rewrite/no-rewrite decision; 11-09 applies only that disposition and fresh evidence. No external API capability matrix applies, `one-second` remains a duration rather than an identity-model change, and no deferred or Phase 12-14 feature is introduced.

<verification>
- Assert the committed 11-08 `rewrite` or `no-rewrite` decision against the audit before mutation.
- At the disposition boundary, compare the immediate pre/post-rewrite tree/count/author metadata and re-sign+DCO every rewritten commit; on no-rewrite, preserve the complete pre-existing commit-ID sequence through that boundary.
- Keep inherited baseline findings separate and prove zero live/unresolved phase-owned candidate while refraining from shared-history rewrite/reflog expiry/GC.
- Run final value-suppressed current/diff/history scans and fresh focused/full/static gates.
- Leave phase pass/failure to an independent verifier.
</verification>

<success_criteria>
The operator-authorised disposition leaves zero live/unresolved phase-owned candidate, preserves or safely rewrites the current branch exactly as approved, keeps shared-baseline findings separate, and supplies fresh green evidence ready for independent Phase 11 verification.
</success_criteria>

<output>
Create `.planning/phases/11-mdns-hardening/11-09-SUMMARY.md` when done.
</output>
