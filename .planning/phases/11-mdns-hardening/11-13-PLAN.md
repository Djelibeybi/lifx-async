---
phase: 11-mdns-hardening
plan: 13
type: execute
wave: 10
depends_on:
  - 11-10
files_modified:
  - AGENTS.md
  - CLAUDE.md
  - docs/getting-started/quickstart.md
  - tests/test_network/test_mdns/test_phase_contract.py
autonomous: true
gap_closure: true
requirements:
  - MDNS-08
estimate:
  tokens: 12000
  raw_tokens: 24000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "AGENTS.md, CLAUDE.md, and the quickstart each document the initial PTR service query, bounded PTR retransmissions at one and three seconds, and conditional A/AAAA follow-ups for SRV targets lacking usable addresses (D-01, D-02, D-08, D-10, D-16)."
    - "The documentation contract scans all three corrected surfaces and fails if any surface omits the initial PTR send, either bounded retransmission time, or the conditional bounded follow-up model, as well as on known false single-total-query formulations."
    - "Public documentation keeps the async streaming, wifi|thread connectivity, fallback, limitation, and no-multicast-listener contracts accurate (D-10, D-14, D-16)."
    - "No private helper/type names, identifiers, packet-address targeting, default-integration claim, or broadcast-schedule claim enters public documentation."
  artifacts:
    - path: AGENTS.md
      provides: "Accurate repository architecture description of mDNS query behaviour."
    - path: CLAUDE.md
      provides: "Accurate agent-facing architecture description of mDNS query behaviour."
    - path: docs/getting-started/quickstart.md
      provides: "Accurate user-facing discovery example and bounded follow-up explanation."
    - path: tests/test_network/test_mdns/test_phase_contract.py
      provides: "Executable anti-drift coverage for the complete initial-PTR, retransmission, and follow-up model on all corrected surfaces."
  key_links:
    - from: tests/test_network/test_mdns/test_phase_contract.py
      to: AGENTS.md
      via: "Repository-instruction surface must state the initial PTR send, one- and three-second retransmissions, and conditional A/AAAA follow-ups."
    - from: tests/test_network/test_mdns/test_phase_contract.py
      to: CLAUDE.md
      via: "Agent-facing architecture text cannot omit either retransmission or drift back to a single-query guarantee."
    - from: tests/test_network/test_mdns/test_phase_contract.py
      to: docs/getting-started/quickstart.md
      via: "Quickstart wording is checked for the same three-stage query model alongside the established Phase 11 public documentation corpus."
---

<objective>
Correct the three independently confirmed false single-query claims and extend the documentation contract so the complete initial-PTR, bounded-retransmission, and conditional-follow-up model cannot silently drift.

Purpose: Close MDNS-08 honestly without exposing private implementation names or overstating default integration.
Output: Correct repository, agent, and quickstart text plus executable three-stage query-model anti-drift tests.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-mdns-hardening/11-CONTEXT.md
@.planning/phases/11-mdns-hardening/11-RESEARCH.md
@.planning/phases/11-mdns-hardening/11-PATTERNS.md
@.planning/phases/11-mdns-hardening/11-SPEC.md
@.planning/phases/11-mdns-hardening/11-VERIFICATION.md
@.planning/phases/11-mdns-hardening/11-REVIEW.md
@.planning/phases/11-mdns-hardening/11-10-SUMMARY.md

There is no external API/SDK integration in this phase, so no API coverage matrix is created. The mDNS implementation is a local protocol path.
</context>

## Artifacts this phase produces

- `AGENTS.md`, `CLAUDE.md`, and `docs/getting-started/quickstart.md` — consistent documentation of the initial PTR send, one- and three-second retransmissions, and conditional A/AAAA follow-ups.
- `tests/test_network/test_mdns/test_phase_contract.py` — semantic anti-drift checks for all three stages across all corrected surfaces.

<tasks>

<task type="tracer">
  <name>Task 1: Document the complete bounded PTR and follow-up query model</name>
  <files>AGENTS.md, CLAUDE.md, docs/getting-started/quickstart.md</files>
  <read_first>
    - `AGENTS.md`: High-Level API `discover_mdns()` description and mDNS transport/discovery architecture.
    - `CLAUDE.md`: the matching High-Level API `discover_mdns()` description.
    - `docs/getting-started/quickstart.md`: mDNS discovery introduction and example.
    - `docs/api/network.md` and `docs/user-guide/advanced-usage.md`: already-approved public wording for the network layer, initial service query, bounded retransmissions and target follow-ups, limitations, and fallback.
    - `src/lifx/network/mdns/discovery.py`: `_discover_lifx_services_sweep` initial `MdnsTransport.send`, one- and three-second `retransmit_delays`, and conditional `build_address_query` sends controlled by `pending_targets()`, `query_attempts`, and successful-send deduplication.
    - `.planning/phases/11-mdns-hardening/11-CONTEXT.md`: D-01, D-02, D-08, D-10, D-14, and D-16.
  </read_first>
  <action>
    Replace each false claim with one consistent, complete model on all three surfaces: `discover_mdns()` sends an initial DNS-SD PTR service query, may retransmit that PTR query once at one second and once at three seconds within the caller's deadline, assembles valid legacy-unicast replies during the quiet window, and conditionally sends bounded A/AAAA follow-ups—one successful send or no more than two failed attempts for each of at most 64 SRV targets—only when a valid target lacks a usable address. State that this remains an explicit alternative to broadcast discovery, returns devices incrementally as an async generator, and may be unavailable on networks or devices that do not expose the required service records. Keep `Device.connectivity` limited to `wifi|thread` where mentioned. Do not name private helpers/types, promise one UDP datagram or one total query, claim default integration, describe multicast subscription, import the Phase 10 broadcast schedule, or add hardware evidence. Use Australian English. Commit with a Conventional Commit message using `git commit -S -s`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-11-13-uv-cache uv run --frozen pytest tests/test_network/test_mdns/test_phase_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All three surfaces distinguish the initial PTR service query, PTR retransmissions at one and three seconds, and conditional address follow-ups.
    - Wording matches production's bounded retransmission schedule, successful-send deduplication, 64-target/two-failed-attempt bounds, and async streaming behaviour.
    - Explicit-alternative, limitations, and broadcast fallback remain accurate.
    - No private implementation name, default-promotion claim, multicast-listener claim, or Phase 10 schedule appears.
  </acceptance_criteria>
  <done>Repository, agent, and quickstart documentation make the same accurate, bounded mDNS query claim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the documentation contract across the corrected surfaces</name>
  <files>tests/test_network/test_mdns/test_phase_contract.py</files>
  <read_first>
    - `tests/test_network/test_mdns/test_phase_contract.py`: `_PUBLIC_PATHS`, approved limitation phrases, forbidden private tokens, private transport phrase checks, and AST multicast-membership check.
    - `AGENTS.md`, `CLAUDE.md`, and `docs/getting-started/quickstart.md`: the corrected passages from Task 1.
    - `.planning/phases/11-mdns-hardening/11-VERIFICATION.md`: gap 5 exact surfaces.
  </read_first>
  <behavior>
    - Test `test_query_model_contract_covers_repository_agent_and_quickstart_surfaces`: the contract corpus explicitly contains all three corrected paths.
    - Test `test_query_model_never_promises_a_single_total_query`: known false formulations fail across the full corpus after comment/header lines are excluded from any count gate.
    - Test `test_query_model_documents_initial_ptr_and_both_retransmissions`: every corrected surface semantically states the initial PTR send and bounded PTR retransmissions at both one and three seconds; deleting either retransmission statement makes the test fail.
    - Test `test_query_model_documents_conditional_bounded_follow_ups`: every corrected surface states that A/AAAA follow-ups are conditional on a valid SRV target lacking a usable address and conveys the 64-target, two-failed-attempt, success-once bounds.
    - Test `test_corrected_surfaces_preserve_public_contract_boundaries`: private names, multicast listener claims, packet-address targeting, implicit default integration, and broadcast-schedule language remain absent.
  </behavior>
  <action>
    Add `AGENTS.md`, `CLAUDE.md`, and `docs/getting-started/quickstart.md` to an explicit query-model documentation corpus without accidentally reclassifying repository instructions as API reference. Add focused per-surface semantic assertions for the known false formulations and for all three required stages: initial PTR transmission, bounded PTR retransmissions at one and three seconds, and conditional bounded A/AAAA follow-ups. Tests must fail when either retransmission is omitted even if the initial-query and follow-up prose remains. Reuse the existing public-contract and private-name checks where their scope is correct; create a separate path tuple where repository/agent files require distinct treatment. Any negative grep/count helper must operate on the intended text region and exclude comments/headings so its own explanatory text cannot self-trigger. Keep tests resilient to prose wrapping by normalising whitespace, but do not weaken them into mere token presence. Commit with a Conventional Commit message using `git commit -S -s`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-11-13-uv-cache uv run --frozen pytest tests/test_network/test_mdns/test_phase_contract.py -q &amp;&amp; UV_CACHE_DIR=/private/tmp/lifx-async-11-13-uv-cache uv run --frozen zensical build &amp;&amp; UV_CACHE_DIR=/private/tmp/lifx-async-11-13-uv-cache uv run --frozen llmstxt-standalone build</automated>
  </verify>
  <acceptance_criteria>
    - The contract explicitly scans AGENTS.md, CLAUDE.md, and the quickstart for query-model drift.
    - Known false single-total-query claims and omission of either the one-second or three-second PTR retransmission make the test fail.
    - Accurate initial-PTR, both-retransmissions, and conditional-bounded-follow-up wording passes on every corrected surface.
    - Existing limitation, fallback, private-name, transport, and no-default-integration contracts remain green.
    - Both documentation build commands succeed from the changed tree.
  </acceptance_criteria>
  <done>The three corrected surfaces are guarded by executable semantic contracts and both documentation builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Implementation behaviour -> documentation | Runtime query limits become developer and user expectations. |
| Private implementation -> public prose | Internal names or unsupported guarantees could become accidental API commitments. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-13-01 | Repudiation | query-model documentation | medium | mitigate | State the initial PTR query, one- and three-second PTR retransmissions, and exact conditional bounded follow-up model consistently on all three surfaces. |
| T-11-13-02 | Information disclosure | public documentation | medium | mitigate | Contract-test absence of private symbols and use no live identifiers or raw probe output. |
| T-11-13-03 | Tampering | documentation drift | medium | mitigate | Add per-surface semantic anti-drift tests that fail on either omitted retransmission as well as known false formulations. |
| T-11-13-04 | Spoofing | default integration claim | low | mitigate | Preserve explicit-alternative and fallback wording; test absence of implicit-default claims. |
| T-11-13-SC | Tampering | package supply chain | low | accept | No package installation or dependency change occurs in this plan. |
</threat_model>

<verification>
- Run `UV_CACHE_DIR=/private/tmp/lifx-async-11-13-uv-cache uv run --frozen pytest tests/test_network/test_mdns/test_phase_contract.py -q`.
- Run both documentation builds from the modified tree.
- Run `UV_CACHE_DIR=/private/tmp/lifx-async-11-13-uv-cache uv run --frozen ruff check tests/test_network/test_mdns/test_phase_contract.py` and `ruff format --check` on that file.
- Confirm both signed task commits verify and contain DCO trailers without copying signer identity into evidence.
</verification>

<success_criteria>
- Verifier gap 5 and the corresponding critical review finding are closed on all named surfaces.
- Documentation describes actual production bounds and preserves private/public boundaries.
- No high or critical threat remains unresolved.
</success_criteria>

## Source coverage audit

| Source item | Coverage |
|-------------|----------|
| GOAL: documented honestly | COVERED by corrected text plus anti-drift tests. |
| REQ MDNS-08 | COVERED by repository, agent, quickstart, contract, and build verification. |
| CONTEXT D-01/D-02/D-08/D-10/D-14/D-16 | COVERED by accurate opt-in, initial/retransmission/follow-up timing, streaming, limitation, and public-boundary wording. |
| SPEC docs/privacy/default-integration edges and prohibitions | COVERED by semantic contract checks. |
| External API/SDK matrix | EXCLUDED because this phase implements a local mDNS protocol and integrates no external API/SDK. |
| Phase 10 broadcast schedule and live hardware | EXCLUDED by locked boundaries. |

<output>
Create `.planning/phases/11-mdns-hardening/11-13-SUMMARY.md` when done.
</output>
