---
phase: 11-mdns-hardening
plan: 11
type: execute
wave: 10
depends_on:
  - 11-10
files_modified:
  - src/lifx/network/mdns/discovery.py
  - tests/test_network/test_mdns/test_discovery.py
autonomous: true
gap_closure: true
requirements:
  - MDNS-03
  - MDNS-05
  - MDNS-06
estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "Every retained record has an exact variable-payload byte cost, one record cannot retain more than 4,096 bytes, and one discovery sweep cannot retain more than 262,144 bytes; a TTL=0 goodbye retains that charge through its one-second grace, rescue retains it, and only `expire(now)` removal releases it (D-11)."
    - "D-15's 256 unique A/AAAA identities per owner and 1,024 across a sweep remain unchanged and independently enforced; byte pressure never weakens count pressure."
    - "Exceeding a byte bound fails the affected owner or sweep closed without eviction, with privacy-safe reason/count diagnostics and no raw DNS, serial, address, or TXT values (D-08, D-09, D-13, D-15)."
    - "Address candidates are filtered for usability before IPv4/IPv6 class priority, so unusable IPv4 cannot suppress a valid ULA and unusable-only records produce no public device (D-05, D-06, D-07, D-16)."
  artifacts:
    - path: src/lifx/network/mdns/discovery.py
      provides: "Exact retained-payload byte accounting and usability-first address selection."
    - path: tests/test_network/test_mdns/test_discovery.py
      provides: "Boundary, goodbye-expiry release/rescue, mixed-address, and public-generator regression tests."
  key_links:
    - from: src/lifx/network/mdns/discovery.py::_LifxRecordCache._add_record
      to: src/lifx/network/mdns/discovery.py::_LifxRecordCache.expire
      via: "The same stored byte-cost field increments admission accounting and decrements on removal."
    - from: src/lifx/network/mdns/discovery.py::_pick_address
      to: src/lifx/network/mdns/discovery.py::discover_devices_mdns
      via: "Only usable candidates participate in family/class ordering, followed by public-boundary validation."
---

<objective>
Bound mDNS cache memory by exact retained payload bytes and correct address selection so hostile but valid-size data cannot exhaust a sweep or make an unusable IPv4 suppress a valid Thread ULA.

Purpose: Close verifier gaps 3 and 4 while preserving every existing count ceiling, fail-closed rule, and D-16 public contract.
Output: Per-record/per-sweep byte budgets, D-11 goodbye-grace accounting, usability-first selection, privacy-safe diagnostics, and deterministic boundary tests.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-mdns-hardening/11-CONTEXT.md
@.planning/phases/11-mdns-hardening/11-RESEARCH.md
@.planning/phases/11-mdns-hardening/11-PATTERNS.md
@.planning/phases/11-mdns-hardening/11-SPEC.md
@.planning/phases/11-mdns-hardening/11-VERIFICATION.md
@.planning/phases/11-mdns-hardening/11-REVIEW.md
@.planning/phases/11-mdns-hardening/11-10-SUMMARY.md

The byte bounds govern attacker-controlled variable payload, while existing entry and address-identity ceilings bound Python object overhead. The source documents supply no smaller required byte values; 4 KiB per record and 256 KiB per sweep preserve normal DNS-SD payloads while creating deterministic limits.
</context>

## Artifacts this phase produces

- `src/lifx/network/mdns/discovery.py` — 4,096-byte record and 262,144-byte sweep budgets with exact D-11 goodbye-grace release, plus usability-first address selection.
- `tests/test_network/test_mdns/test_discovery.py` — below/at/above byte-limit, duplicate-refresh, goodbye expiry/rescue, mixed-address, and public-generator tests.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Enforce exact retained-payload byte budgets through goodbye grace</name>
  <files>src/lifx/network/mdns/discovery.py, tests/test_network/test_mdns/test_discovery.py</files>
  <read_first>
    - `src/lifx/network/mdns/discovery.py`: `_CachedResourceRecord`, `_LifxRecordCache._records_by_owner`, `_add_record` owner/type `by_type` counting and `_address_rr_count` admission paths, `expire`'s matching `by_type` removal and `_address_rr_count` decrement paths, `next_expiry_delay`, and rejection logging.
    - `tests/test_network/test_mdns/test_discovery.py`: `TestLifxRecordCacheBounds`, `TestLifxRecordCacheGoodbyeExpiry`, and `TestMdnsRejectionDiagnostics`.
    - `.planning/phases/11-mdns-hardening/11-SPEC.md`: byte-bounded memory, TTL=0, expiry scheduling, record flood, cache overflow, privacy, and cleanup edge rows.
  </read_first>
  <behavior>
    - Test `test_retained_payload_at_4096_byte_record_limit_is_accepted`: a syntactically valid large TXT or SRV record with an exact computed retained cost of 4,096 is retained and can participate in resolution.
    - Test `test_retained_payload_above_4096_byte_record_limit_fails_owner_closed`: the next byte is rejected before storage, the owner cannot resolve, and the diagnostic contains only a stable reason and numeric counts.
    - Test `test_sweep_retained_payload_at_262144_bytes_is_accepted`: multiple large valid records remain eligible at the exact sweep limit.
    - Test `test_sweep_retained_payload_above_262144_bytes_fails_sweep_closed`: the first over-limit record is not retained and all subsequent construction for that sweep remains disabled without eviction.
    - Test `test_duplicate_refresh_does_not_double_charge_retained_bytes`: refreshing the same canonical record identity replaces/extends it without increasing the total.
    - Test `test_positive_ttl_record_retains_charge_for_the_sweep`: advancing the clock beyond a positive record's advertised TTL neither removes the record nor releases its retained-byte charge.
    - Test `test_goodbye_grace_retains_charge_until_expire_removes_record`: a matching TTL=0 goodbye changes only the record's expiry deadline; the full byte charge remains during the one-second grace and is subtracted exactly once only when `expire(now)` removes the identity.
    - Test `test_positive_reannouncement_rescues_goodbye_without_recharging`: a matching positive-TTL reannouncement during the one-second grace clears the pending goodbye expiry while retaining the existing charge, and a later `expire(now)` does not remove or release it.
    - Test `test_byte_limits_do_not_change_address_identity_limits`: D-15's 256-per-owner and 1,024-per-sweep unique address limits still fail closed at the same boundaries.
  </behavior>
  <action>
    Add named private constants `_MAX_RETAINED_PAYLOAD_BYTES_PER_RECORD = 4096` and `_MAX_RETAINED_PAYLOAD_BYTES_PER_SWEEP = 262144`. Define one deterministic retained-payload cost function over data actually copied into cache state: UTF-8 owner bytes, raw RDATA bytes, four bytes for rtype/rclass, and the canonical parsed payload (TXT string UTF-8 byte lengths; SRV target UTF-8 bytes plus six bytes for priority/weight/port; A/AAAA packed-address bytes). Store that exact cost on `_CachedResourceRecord`; document that existing count ceilings bound fixed Python object overhead.

    Check the per-record limit before cache insertion or replacement. Maintain an exact sweep total and avoid double charging on identical refresh/replacement. Implement D-11 exactly: positive-TTL records remain retained for the discovery sweep; a matching TTL=0 goodbye changes that cached identity to a one-second expiry but leaves its byte charge intact throughout the grace; `expire(now)` subtracts the stored cost exactly once only when it actually removes the identity; and a matching positive-TTL reannouncement during grace clears the pending expiry while retaining the record and its existing charge. A per-record overrun permanently marks that owner/type byte-incomplete for the call; a sweep overrun permanently marks the sweep byte-incomplete. As with D-15, do not evict earlier attacker-chosen entries to recover eligibility. Keep the existing `_MAX_ENTRIES`, 16-record TXT/SRV owner ceilings, 256 address identities per owner, and 1,024 address identities per sweep unchanged and independently tested. Add stable debug reasons such as `record_byte_capacity` and `sweep_byte_capacity`, logging only record type and numeric current/limit counts. Never log owner names, targets, addresses, serials, or TXT values in these rejection messages. Commit with a Conventional Commit message using `git commit -S -s`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-11-11-uv-cache uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCacheByteBounds tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCacheBounds tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCacheGoodbyeExpiry tests/test_network/test_mdns/test_discovery.py::TestMdnsRejectionDiagnostics -q</automated>
  </verify>
  <acceptance_criteria>
    - Per-record and per-sweep costs are deterministic, exact for retained variable payload, and stored once for symmetric release.
    - Valid large TXT and SRV records have explicit at-limit success and one-byte-over failure tests.
    - Positive-TTL retention, duplicate refresh, one-second goodbye grace, `expire(now)` removal, and rescue cannot leak, double charge, prematurely release, or underflow the sweep counter.
    - Byte-bound failures are fail-closed and privacy-safe; D-15 count ceilings are unchanged.
  </acceptance_criteria>
  <done>Untrusted retained payload is byte-bounded per record and per sweep with exact lifetime accounting, fail-closed state, and privacy-safe diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filter unusable addresses before family and class priority</name>
  <files>src/lifx/network/mdns/discovery.py, tests/test_network/test_mdns/test_discovery.py</files>
  <read_first>
    - `src/lifx/network/mdns/discovery.py`: `_pick_address`, `_LifxRecordCache.resolve`, `_create_device_from_record`, and `discover_devices_mdns` address validation.
    - `src/lifx/network/address.py`: `validate_address` and its exact unsupported-address rules.
    - `tests/test_network/test_mdns/test_discovery.py`: existing IPv4/ULA/GUA/scoped-link-local selection and public generator rejection tests.
    - `.planning/phases/11-mdns-hardening/11-CONTEXT.md`: D-05, D-06, D-07, D-09, and D-16.
  </read_first>
  <behavior>
    - Test `test_pick_address_ignores_unspecified_ipv4_before_valid_ula`: `0.0.0.0` plus `fd00::10` selects the ULA.
    - Test `test_pick_address_ignores_ipv4_mapped_ipv6_before_valid_ula`: an IPv4-mapped IPv6 value plus a ULA selects the ULA.
    - Test `test_pick_address_with_only_unusable_candidates_returns_none`: unspecified IPv4/IPv6, mapped IPv6, and unscoped link-local candidates resolve to no record.
    - Test `test_mixed_unusable_ipv4_and_valid_ula_yields_thread_device`: the private cache and public generator together yield one Thread device at the ULA.
    - Test `test_unusable_only_cache_yields_no_public_device`: a complete TXT/SRV chain with only unusable addresses is silently rejected before construction.
    - Test `test_existing_address_priority_is_unchanged_for_usable_candidates`: usable IPv4, then ULA, then GUA, then scoped link-local remain the priority classes; same-class lexical ordering remains deterministic.
  </behavior>
  <action>
    Centralise a side-effect-free mDNS candidate-usability predicate consistent with `validate_address`: reject unspecified IPv4/IPv6, IPv4-mapped IPv6, and link-local IPv6 without a scope before assigning any family/class rank. Keep unscoped link-local records retained in the bounded cache per D-07 so a later scoped answer can recover, but never let them enter selection. Then sort only usable candidates using D-06's established order: IPv4, ULA, GUA, scoped link-local, with deterministic lexical order inside a class. If none remain, return no record. Retain public-boundary validation as defence in depth and test both cache resolution and `discover_devices_mdns`, not `_pick_address` alone. Preserve zone identifiers on scoped link-local addresses and D-16 connectivity values. Commit with a Conventional Commit message using `git commit -S -s`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/lifx-async-11-11-uv-cache uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCache::test_pick_address_ignores_unspecified_ipv4_before_valid_ula tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCache::test_pick_address_ignores_ipv4_mapped_ipv6_before_valid_ula tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCache::test_pick_address_with_only_unusable_candidates_returns_none tests/test_network/test_mdns/test_discovery.py::TestDiscoverDevicesMdns::test_mixed_unusable_ipv4_and_valid_ula_yields_thread_device tests/test_network/test_mdns/test_discovery.py::TestDiscoverDevicesMdns::test_unusable_only_cache_yields_no_public_device tests/test_network/test_mdns/test_discovery.py::TestLifxRecordCache::test_existing_address_priority_is_unchanged_for_usable_candidates -q</automated>
  </verify>
  <acceptance_criteria>
    - Unusable candidates are removed before ranking and cannot suppress a usable lower-priority-family address.
    - Mixed unusable IPv4 plus valid ULA yields the ULA-backed Thread device.
    - Unusable-only cache and public-generator paths produce no device and continue silently.
    - Existing usable-address priority, stable ordering, link-local scope preservation, and connectivity contracts remain unchanged.
  </acceptance_criteria>
  <done>Address selection ranks only usable candidates, preserving existing priority while eliminating unusable-address suppression at cache and public boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DNS RDATA -> retained cache | Attacker-controlled variable-length data consumes process memory. |
| Retained address set -> selected endpoint | An unusable high-priority address can suppress a valid endpoint. |
| Selected endpoint -> public Device | A network address becomes user-visible connection state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-11-01 | Denial of service | `_LifxRecordCache._add_record` | high | mitigate | Enforce 4,096-byte record and 262,144-byte sweep retained-payload budgets before storage. |
| T-11-11-02 | Tampering | byte accounting | high | mitigate | Store one exact cost per cached identity, retain it through TTL=0 grace/rescue, and release it only when `expire(now)` removes the record; test positive-TTL retention and underflow boundaries. |
| T-11-11-03 | Denial of service | D-15 count ceilings | high | mitigate | Preserve independent 256-owner and 1,024-sweep unique address limits and fail-closed state. |
| T-11-11-04 | Spoofing | `_pick_address` | high | mitigate | Exclude unsupported candidates before class ranking and revalidate before public construction. |
| T-11-11-05 | Information disclosure | capacity diagnostics | medium | mitigate | Emit stable reason and numeric counts only; regression-test absence of raw identifiers and values. |
| T-11-11-SC | Tampering | package supply chain | low | accept | No package installation or dependency change occurs in this plan. |
</threat_model>

<verification>
- Run `UV_CACHE_DIR=/private/tmp/lifx-async-11-11-uv-cache uv run --frozen pytest tests/test_network/test_mdns/test_discovery.py -q -W error::ResourceWarning`.
- Run `UV_CACHE_DIR=/private/tmp/lifx-async-11-11-uv-cache uv run --frozen ruff check src/lifx/network/mdns/discovery.py tests/test_network/test_mdns/test_discovery.py`.
- Run `UV_CACHE_DIR=/private/tmp/lifx-async-11-11-uv-cache uv run --frozen pyright src/lifx/network/mdns/discovery.py`.
- Confirm `git verify-commit HEAD` succeeds after each task and the commit contains a `Signed-off-by:` trailer without recording signer identity.
</verification>

<success_criteria>
- Verifier gaps 3 and 4 close with exact limit-boundary, cleanup, mixed-address, and public-generator tests.
- All high threats are mitigated; count ceilings and public contracts do not regress.
- No identifier, raw discovery output, dependency, broadcast path, or generated file enters the change.
</success_criteria>

## Source coverage audit

| Source item | Coverage |
|-------------|----------|
| GOAL: correct at mesh scale | COVERED by independent count/byte bounds and usability-first selection. |
| REQ MDNS-03 | COVERED by bounded cross-packet retained assembly. |
| REQ MDNS-05 | COVERED by cache-to-public IPv4/ULA/unusable-only behaviour. |
| REQ MDNS-06 | COVERED by structural resource limits and invalid-address rejection. |
| RESEARCH: fail closed without eviction; preserve per-call cleanup | COVERED by permanent call-local incomplete flags and D-11 goodbye-expiry accounting. |
| CONTEXT D-05/D-06/D-07/D-08/D-09/D-11/D-13/D-14/D-15/D-16 | COVERED explicitly. |
| SPEC resolved edges: floods, one-second goodbye expiry/rescue, malformed/unusable addresses, cleanup, privacy | COVERED by named tests. |
| SPEC prohibitions | COVERED without changing broadcast, multicast membership, cache lifetime, response targeting, or default discovery. |

<output>
Create `.planning/phases/11-mdns-hardening/11-11-SUMMARY.md` when done.
</output>
