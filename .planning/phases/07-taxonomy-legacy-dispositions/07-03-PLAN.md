---
phase: 07-taxonomy-legacy-dispositions
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - docs/migration/theme-taxonomy-v1.2.md
  - mkdocs.yml
  - docs/api/themes.md
  - docs/getting-started/themes.md
autonomous: true
requirements: [META-04, COMPAT-04]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "docs/migration/theme-taxonomy-v1.2.md exists, is reachable from both mkdocs nav sections, and names all 9 categories with their theme counts, all 6 legacy names, and all 9 deprecated keys with their replacements (SPEC R6 acceptance, D-09)"
    - "The legacy-name table carries one row per legacy name including the 4 that raise, and the deprecated-key table one row per key — no empty sections (SPEC R6 adjacency + empty edges)"
    - "The page is stamped as a point-in-time record — 'As of migration' — whose counts are historical claims, deliberately not resynced and not CI-pinned (D-10)"
    - "No docstring or doc page mentions a 'time' category, and no shipped doc teaches a category name that raises (SPEC R6 acceptance, D-11 sites 3-4)"
    - "docs/getting-started/themes.md examples call get_by_category with app category names, and the stale get_by_category admonition in docs/api/themes.md is gone (D-11 sites 3-4)"
  artifacts:
    - "docs/migration/theme-taxonomy-v1.2.md — new migration page"
    - "mkdocs.yml — the page added to BOTH Migration nav sections (llms nav ~:149-150 and main nav ~:223-224)"
    - "docs/api/themes.md — stale admonition replaced"
    - "docs/getting-started/themes.md — examples re-pointed at app names"
  key_links:
    - "mkdocs.yml nav entry → the new page (an unlisted page is unreachable; both nav trees must list it)"
    - "Every fate and replacement on the page copies the locked SPEC tables — the page and the code implement the same locked source, which is what keeps them consistent without a CI check"
  prohibitions:
    - statement: "MUST NOT present the synthetic Library category as one the LIFX app defines — the page must attribute Library to this library (pre-v1.2 keys with no app counterpart), and count tables must not imply the app publishes it"
      status: resolved
      verification: judgment
    - statement: "MUST NOT update docs/changelog.md — it is auto-generated by the release workflow"
      status: resolved
      verification: test
---

<objective>
Document the taxonomy and the v1.2 migration: a new point-in-time migration page under the
existing Migration nav section carrying the 9 categories with counts, the 6 legacy-name
fates and the 9 deprecated keys with replacements (D-09, D-10), plus the two remaining
stale-doc corrections outside library.py (D-11 sites 3-4). Runs after plan 07-02 so every
code example on the page is verified against the delivered behaviour, not hoped-for
behaviour.

Purpose: SPEC R6 — a human can read what changed and what to call now; the migration
record stays honest by being dated rather than resynced.
Output: one new docs page, two nav entries, two corrected doc files.

**Scope guard:** Phase 9 owns DOCS-03 (broader theme documentation). This plan documents
ONLY the taxonomy and the migration. Do not rewrite user-guide/themes.md, do not add a
full Theme attribute reference, do not touch docs/changelog.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-taxonomy-legacy-dispositions/07-SPEC.md
@.planning/phases/07-taxonomy-legacy-dispositions/07-CONTEXT.md
@.planning/phases/07-taxonomy-legacy-dispositions/07-PATTERNS.md
@.planning/phases/07-taxonomy-legacy-dispositions/07-01-SUMMARY.md
@.planning/phases/07-taxonomy-legacy-dispositions/07-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 03 share)

New files and content created by THIS plan (source-grounding: exclude from drift verification):

- `docs/migration/theme-taxonomy-v1.2.md` — new documentation page (category table, legacy-fate table, deprecated-key table, before/after examples)
- Two new `mkdocs.yml` nav entries referencing `migration/theme-taxonomy-v1.2.md`
- Replacement paragraph in `docs/api/themes.md` (supersedes the removed admonition)
- Corrected example lines in `docs/getting-started/themes.md`

<tasks>

<task type="auto">
  <name>Task 1: The theme-taxonomy migration page and its nav entries</name>
  <files>docs/migration/theme-taxonomy-v1.2.md, mkdocs.yml</files>
  <read_first>
    - docs/migration/effect-api-changes.md (the analog: heading style, Overview section, numbered change list, Before/After fenced code blocks)
    - mkdocs.yml :145-155 and :218-226 (BOTH Migration nav sections the page joins — the llms/full_output nav lists bare paths, the main nav lists "Title: path" pairs)
    - .planning/phases/07-taxonomy-legacy-dispositions/07-SPEC.md (requirement 4's locked disposition table and requirement 3's legacy fates — the page's content source, copied verbatim)
    - .planning/phases/07-taxonomy-legacy-dispositions/07-CONTEXT.md (D-09, D-10 — placement and the point-in-time framing; the Specific Ideas section for the operator's own "As of migration" wording)
  </read_first>
  <reversibility rating="reversible">A docs page and nav entries; deleting or moving them later is trivial.</reversibility>
  <action>
    Implements D-09 (placement) and D-10 (point-in-time contract); delivers SPEC R6
    (META-04 + COMPAT-04 documentation). Australian English throughout.

    Create docs/migration/theme-taxonomy-v1.2.md following the effect-api-changes.md
    shape (H1, intro sentence, Overview, then sections). Required content:

    1. **Stamp, near the top:** a line stating the tables record the library **as of the
       v1.2 migration (2026-08-15)** and are deliberately not updated as the library
       changes — a dated migration record, not a live reference (D-10; use the
       operator's framing from CONTEXT.md Specific Ideas).
    2. **The nine categories** — table of category → theme count: Archives 60,
       Art Series 10, Holidays 15, Library 28, Moods 13, Music 14, Nature 8, Play 7,
       Space 11. Attribution wording (prohibition): eight are the LIFX app's own
       categories; **Library is defined by this library** for the 28 pre-v1.2 keys with
       no app counterpart — never present it as app-defined. Note the app's 3 sport
       categories (AUSSIE RULES, LEAGUE, UNION) are not carried (out of scope since
       Phase 6).
    3. **Legacy category names** — table with one row per pre-v1.2 name (all 6): name,
       fate, and what to call now. holiday → maps to Holidays; mood → maps to Moods;
       seasonal → raises, closest replacement Nature; ambient → raises, closest
       replacement Play; functional → raises, closest replacement Library;
       atmosphere → raises, closest replacement Moods. Mention that three names the old
       lists carried (winter, romantic, dramatic) never resolved to real themes and are
       no longer silently filtered, and that matching is case- and punctuation-
       insensitive (Art Series, art series and art_series all resolve).
    4. **Deprecated keys** — table with one row per key (all 9): focusing → gentle,
       intense → fantasy, shamrock → st_patrick_s_day, love → romance,
       holly → christmas, fire → warm_ember, proud → pride, pumpkin → pumpkin_spice,
       santa → candy_cane. State the contract: a deprecated key still resolves and
       always will within v1.x — the disposition is queryable data
       (Theme.disposition / Theme.replaced_by), never a warning; the remaining 19
       orphans are recorded as library-only and stay.
    5. **Before/After examples** — fenced Python: Before shows
       get_by_category("holiday") under the old hand-made grouping; After shows
       get_categories() and get_by_category("Holidays"), plus reading
       .disposition/.replaced_by from a fetched deprecated theme. Every After snippet
       must actually run against the shipped code (verified below).

    Add the page to BOTH mkdocs.yml Migration sections: `- migration/theme-taxonomy-v1.2.md`
    in the llms nav list, and `- Theme Taxonomy Changes: migration/theme-taxonomy-v1.2.md`
    in the main nav, each alongside the existing effect-api-changes entry.
  </action>
  <verify>
    <automated>uv run zensical build && uv run python -c "
from lifx.theme import ThemeLibrary
cats = ThemeLibrary.get_categories()
page = open('docs/migration/theme-taxonomy-v1.2.md').read()
for c in cats: assert c in page, c
for k in ['focusing','intense','shamrock','love','holly','fire','proud','pumpkin','santa']: assert k in page, k
for n in ['holiday','mood','seasonal','ambient','functional','atmosphere']: assert n in page, n
assert 'As of' in page
t = ThemeLibrary.get('fire')
assert t.disposition == 'deprecated' and t.replaced_by == 'warm_ember'
print('page ok')"</automated>
  </verify>
  <done>
    The page exists with all three tables and the point-in-time stamp, both nav trees
    list it, the docs build passes, and the page's After examples run against the
    shipped code.
  </done>
  <acceptance_criteria>
    - docs/migration/theme-taxonomy-v1.2.md exists; `grep -c "warm_ember" docs/migration/theme-taxonomy-v1.2.md` returns at least 1
    - `grep -c "theme-taxonomy-v1.2.md" mkdocs.yml` returns exactly 2 (both nav sections)
    - The page contains all 9 category names, all 6 legacy names and all 9 deprecated keys (the verify script asserts each)
    - `grep -ci "as of" docs/migration/theme-taxonomy-v1.2.md` returns at least 1 (D-10 stamp)
    - The page never states or implies the app defines the Library category (judgment: the Library row/paragraph attributes it to this library)
    - `uv run zensical build` exits 0
    - `git diff --name-only | grep -c "docs/changelog.md"` returns 0 (changelog untouched)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Correct the two stale doc sites outside library.py</name>
  <files>docs/api/themes.md, docs/getting-started/themes.md</files>
  <read_first>
    - docs/api/themes.md :50-75 (the category-count table that stays, and the now-false admonition about get_by_category using the older grouping that goes)
    - docs/getting-started/themes.md :20-40 (the two example lines being re-pointed)
    - docs/migration/theme-taxonomy-v1.2.md (created in Task 1 — the link target)
  </read_first>
  <action>
    Implements D-11 sites 3-4. Australian English.

    **docs/api/themes.md:** delete the `!!! note` admonition block that says
    get_by_category() predates the generated data and takes the legacy names — it is now
    false. Replace it with a short paragraph: get_by_category() takes the categories in
    the table above (case- and punctuation-insensitively), get_categories() lists them,
    the two pre-v1.2 names holiday and mood still map to Holidays and Moods, and the
    remaining pre-v1.2 names raise ValueError naming their closest replacement — with a
    link to the migration page (relative path ../migration/theme-taxonomy-v1.2.md). One
    sentence noting Theme.disposition and Theme.replaced_by record each theme's v1.2
    fate, linking to the same page, is in scope; a full attribute reference is not
    (Phase 9, DOCS-03).

    **docs/getting-started/themes.md:** re-point the two example calls at the app names —
    get_by_category("Holidays") and get_by_category("Moods") — and add one
    get_categories() line above them so a reader discovers names instead of guessing.
    Keep the surrounding prose shape; do not expand the page otherwise.
  </action>
  <verify>
    <automated>uv run zensical build && uv run python -c "
api = open('docs/api/themes.md').read()
gs = open('docs/getting-started/themes.md').read()
assert 'older grouping' not in api
assert 'theme-taxonomy-v1.2' in api
assert 'get_by_category(\"Holidays\")' in gs and 'get_by_category(\"Moods\")' in gs
assert 'get_by_category(\"holiday\")' not in gs and 'get_by_category(\"mood\")' not in gs
print('corrections ok')"</automated>
  </verify>
  <done>
    Neither corrected page teaches a category name that raises; the false admonition is
    gone; both pages link or point readers at the current API; the docs build passes.
  </done>
  <acceptance_criteria>
    - `grep -c "older grouping" docs/api/themes.md` returns 0 (admonition gone)
    - `grep -c "theme-taxonomy-v1.2" docs/api/themes.md` returns at least 1 (migration link present)
    - `grep -c 'get_by_category("Holidays")' docs/getting-started/themes.md` returns at least 1 and `grep -c 'get_by_category("holiday")' docs/getting-started/themes.md` returns 0
    - `grep -c "get_categories" docs/getting-started/themes.md` returns at least 1
    - `grep -rn "seasonal, mood, holiday, time" docs/ src/lifx/` returns nothing (no shipped text mentions the phantom category list — SPEC R6 acceptance)
    - `uv run zensical build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → reader | Published documentation is trusted by users as a description of API behaviour |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Repudiation / integrity of claims | migration page tables | low | mitigate | Every fate, replacement and count is copied from the locked SPEC tables and the page is dated "As of migration" so its claims are scoped in time (D-10); the After code examples are executed against the shipped library in the verify step |
| T-07-08 | Spoofing (misattribution) | Library category description | low | mitigate | Page wording gate: Library is attributed to this library, never to the LIFX app (SPEC prohibition, checked in acceptance criteria) |
| T-07-09 | Tampering (supply chain) | docs toolchain | low | accept | Zero packages installed by this phase (zensical is an existing dev dependency); no package-legitimacy audit applies |
</threat_model>

<verification>
- `uv run zensical build` exits 0
- Both Task verify scripts pass (page content + behavioural examples + corrections)
- `grep -rn "seasonal, mood, holiday, time" docs/ src/lifx/` returns nothing
- docs/changelog.md untouched
</verification>

<success_criteria>
- SPEC R6 delivered: the page names all 9 categories, all 6 legacy names and all 9 deprecated keys; the stale sites are corrected; nothing shipped teaches a name that raises and nothing mentions a phantom category
- D-10 honoured: the page is a dated point-in-time record with no CI count-pinning
- The Library category is never presented as app-defined
</success_criteria>

<output>
Create `.planning/phases/07-taxonomy-legacy-dispositions/07-03-SUMMARY.md` when done.
</output>
