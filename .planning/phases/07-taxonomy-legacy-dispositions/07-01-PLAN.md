---
phase: 07-taxonomy-legacy-dispositions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/themes.jsonl
  - scripts/generate_theme_data.py
  - src/lifx/theme/data.py
  - src/lifx/theme/theme.py
  - src/lifx/theme/library.py
  - tests/test_theme/test_theme_generator.py
  - tests/test_theme/test_theme.py
  - tests/test_theme/test_library.py
autonomous: true
requirements: [COMPAT-04]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 166 records in data/themes.jsonl carry disposition in {lifx-app, library-only, deprecated}; the 28 Library records split exactly 9 deprecated / 19 library-only per the locked SPEC R4 table (COMPAT-04, D-05)"
    - "Every replaced_by value resolves in THEMES (aliases count); chains are permitted by the schema and zero exist today (SPEC R4 adjacency edge)"
    - "replaced_by is None unless disposition == 'deprecated', and the generator aborts with a controlled error on a deprecated record with no replaced_by (SPEC R4 empty edge, D-06)"
    - "disposition and replaced_by are ASCII; replaced_by passes the existing canonical-key check when present (SPEC R4 encoding edge)"
    - "disposition is present on every record including the 138 app themes as 'lifx-app', and Theme.palette_equals() ignores both new fields (SPEC R5 unclassified edge, D-19a unchanged)"
    - "ThemeLibrary.get('fire').disposition == 'deprecated' and .replaced_by == 'warm_ember'; get('hygge').disposition == 'library-only' and .replaced_by is None; get('christmas').disposition == 'lifx-app' (SPEC R5 acceptance)"
    - "Theme.__eq__ stays identity comparison and hash(theme) still works (D-20a unchanged); Theme([...]) with no keywords still constructs (D-07 additive constraint)"
    - "All 168 names still resolve; the 57-key PRE_V12_KEYS fixture passes; both rename aliases still bind their target's own record object (SPEC R7 adjacency edge)"
    - "Regenerating src/lifx/theme/data.py from data/themes.jsonl a second time produces a byte-identical file (THEME-04 idempotence re-established after the schema change)"
  artifacts:
    - "data/themes.jsonl — every one of the 166 lines carries disposition; the 9 deprecated lines also carry replaced_by"
    - "scripts/generate_theme_data.py — _DISPOSITIONS constant, disposition in _REQUIRED_FIELDS, replaced_by in _OPTIONAL_FIELDS, three new validations, extended emit"
    - "src/lifx/theme/data.py — regenerated; ThemeRecord carries disposition: str and replaced_by: str | None = None"
    - "src/lifx/theme/theme.py — Theme gains keyword-only disposition and replaced_by, both defaulting to None"
    - "src/lifx/theme/library.py — ThemeLibrary.get() threads both new fields onto the returned Theme"
  key_links:
    - "data/themes.jsonl disposition field → validate_records() → emit_data_module() → ThemeRecord → ThemeLibrary.get() → Theme.disposition (the full pipeline; break anywhere and COMPAT-04 is unverifiable)"
    - "THEMES alias emission order (aliases assigned after the dict literal) must be undisturbed or R7 alias identity breaks"
  prohibitions:
    - statement: "MUST NOT assign or withhold a disposition based on what a theme depicts — every one of the 9 deprecation calls copies the locked SPEC R4 table (measured palette distance + name correspondence); no new judgement is exercised during execution"
      status: resolved
      verification: judgment
    - statement: "MUST NOT fetch taxonomy or disposition data from any network source, LIFX API or device — regeneration reads only the committed data/themes.jsonl"
      status: resolved
      verification: test
---

<objective>
Wire the COMPAT-04 disposition schema end-to-end: every one of the 166 theme records in
`data/themes.jsonl` gains a `disposition` field (`lifx-app` / `library-only` / `deprecated`),
the 9 deprecated records gain `replaced_by`, the generator validates and emits both fields,
`ThemeRecord` and `Theme` carry them, and `ThemeLibrary.get()` surfaces them — implementing
D-05, D-06, D-07 and D-08 from 07-CONTEXT.md.

Purpose: each of the 28 orphaned library keys gets a recorded, machine-readable fate
(phase success criterion 3) without any key ceasing to resolve (SPEC R7).
Output: rewritten data file, extended generator, regenerated data module, extended
`Theme`, threading in `get()`, and the D-08 validation tests.

**Surfaced SPEC discrepancy (assumption):** SPEC R4's target text says "The 140 app
records are `lifx-app`" but the measured count is **138** (166 records − 28 Library
records; SPEC's own background table sums 60+10+15+13+14+8+7+11 = 138, and the ROADMAP
amendment says "138 app themes"). Use **138**. Do not hunt for 2 extra records.

**Surfaced SPEC discrepancy (typo):** SPEC Acceptance Criteria bullet 11 ("Two Themes
with equal palettes and differing dispositions compare equal; `Theme.__hash__` is None")
contradicts SPEC R5 and CONTEXT D-19a/D-20a; the authoritative contract is R5 —
`palette_equals()` ignores `disposition` and `replaced_by`, `__eq__` stays identity, and
`Theme` stays hashable (`hash(theme)` works, `__hash__` is NOT None). This plan
implements R5; the checklist bullet is a SPEC typo.

**Deferred (do NOT implement):** issue #201 alias-vs-primary listing (D-12 defers to
Phase 9 — no new public API for enumeration), a public accessor for the legacy map, any
count-pinning invariant test for the 9/19/138 split (declined per D-08/D-23), any
`DeprecationWarning` or log line (SPEC out-of-scope: disposition is queryable data, never
emitted at runtime).

**Plan atomicity (review F17):** Tasks 1–3 are one atomic plan-level change with respect
to CI's 100% branch patch coverage gate. Task 1 adds generator validation and emit
branches whose tests land in Task 3, so intermediate task commits are not expected to
satisfy the coverage gate on their own, and Task 1's verify is not a statement of final
branch coverage — the gate is judged on the plan's complete diff.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-taxonomy-legacy-dispositions/07-SPEC.md
@.planning/phases/07-taxonomy-legacy-dispositions/07-CONTEXT.md
@.planning/phases/07-taxonomy-legacy-dispositions/07-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 01 share)

New symbols and files created or extended by THIS plan (source-grounding: exclude from drift verification):

- `ThemeRecord.disposition` (field, `str`) and `ThemeRecord.replaced_by` (field, `str | None = None`) — generated dataclass in `src/lifx/theme/data.py`
- `Theme.disposition` and `Theme.replaced_by` — keyword-only `__init__` parameters and instance attributes on `src/lifx/theme/theme.py`, both `str | None = None`
- `_DISPOSITIONS` — module-level frozenset constant in `scripts/generate_theme_data.py`
- Three new validation branches in `validate_records()` and extended emit lines in `emit_data_module()` (same file)
- `disposition` key on all 166 `data/themes.jsonl` lines; `replaced_by` key on the 9 deprecated lines
- New test code: three D-08 failing-record tests plus the branch-completing bad-value tests (non-string disposition; empty, non-string and non-canonical replaced_by) in `tests/test_theme/test_theme_generator.py`, two new rows in the existing parametrised emit-time backstop matrix, one emit round-trip test, `TestDispositionSurfacing` class in `tests/test_theme/test_library.py`, disposition-defaults test in `tests/test_theme/test_theme.py`, `"disposition": "lifx-app"` added to the `_record()` helper

## Cross-AI review dispositions (07-REVIEWS.md — this plan's share)

| Finding | Disposition |
|---------|-------------|
| F1 (HIGH — generator branch coverage) | Incorporated — Task 3 action and acceptance criteria: the generator IS coverage-measured (pyproject addopts `--cov=generate_theme_data --cov-branch`); the false "scripts/ is excluded from coverage per D-21" claim is removed; the full bad-value branch matrix is specified; emit-time cases extend the existing parametrised backstop matrix. STATE.md's stale D-21 record is being corrected by the orchestrator — do NOT edit STATE.md in this plan. |
| F3 (MEDIUM — replaced_by permitted on non-deprecated records) | Deferred per locked D-08 — Task 1 step 3 records the deferral, the residual risk, and the accepted emit-time asymmetry. |
| F7 (MEDIUM — stale shuffled()/random() docstring note) | Incorporated — Task 2 action plus a pinned acceptance criterion. |
| F9 (LOW — ensure_ascii on the JSONL rewrite) | Incorporated — Task 1 step 1. |
| F12 (LOW — no-network grep is string-based) | Deferred — one-line rationale on the Task 1 acceptance criterion. |
| F14 (LOW — throwaway rewrite script not persisted) | Incorporated — Task 1 requires the SUMMARY to record the script's logic. |
| F17 (LOW — task-boundary red window) | Incorporated — plan-atomicity note in the objective. |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — one deprecated record wired from JSONL through generator validation and emit into the importable THEMES dict</name>
  <files>data/themes.jsonl, scripts/generate_theme_data.py, tests/test_theme/test_theme_generator.py, src/lifx/theme/data.py</files>
  <read_first>
    - data/themes.jsonl (line shape: slug, name, category, [aliases,] colors — only lines 3 and 50 carry aliases)
    - scripts/generate_theme_data.py (whole file: _REQUIRED_FIELDS :48, _OPTIONAL_FIELDS :51, _fail :161, validate_records :248, the seen_keys collision pass :262/:329-335, emit_data_module :380, the emit-time backstops :451-457, alias emission :469-482)
    - tests/test_theme/test_theme_generator.py :60-90 (the _record()/_pairs()/_exec_module() helpers this task must keep valid)
    - .planning/phases/07-taxonomy-legacy-dispositions/07-SPEC.md (requirement 4 — the locked disposition table; copy it, never re-derive it)
  </read_first>
  <reversibility rating="costly">D-05 makes disposition a required field of the JSONL format that Phase 9's capture→JSONL converter must then emit; walking it back means another 166-line rewrite plus tooling changes.</reversibility>
  <action>
    Implements D-05, D-06, D-08 (validations) — the data half of COMPAT-04.

    **1. JSONL rewrite (scripted, one-off).** Write a throwaway Python script in the
    scratchpad directory (NOT committed, NOT in scripts/) that reads data/themes.jsonl
    line by line, parses each JSON object, and rebuilds it with keys in this exact
    insertion order: slug, name, category, disposition, replaced_by (only when present),
    aliases (only when present), colors. Write each object back with
    json.dumps(obj, separators=(", ", ": "), ensure_ascii=False) matching the existing
    one-line-per-record format, then a trailing newline — ensure_ascii=False is required
    (review F9): without it any surviving non-ASCII character would drift into a \uXXXX
    escape; these separators were independently verified to reproduce existing lines
    byte-for-byte. Record the script's full logic in 07-01-SUMMARY.md — key insertion
    order, json.dumps arguments, the disposition rule and that the 28 Library values come
    from the locked SPEC R4 table — so the rewrite stays reproducible after the
    scratchpad is gone (review F14; the SPEC R4 table remains the authoritative
    fallback). Disposition rule: category != "Library" gets
    "lifx-app" (138 records). The 28 category == "Library" records use the locked SPEC R4
    table — deprecated (9, each also gains replaced_by): focusing→gentle,
    intense→fantasy, shamrock→st_patrick_s_day, love→romance, holly→christmas,
    fire→warm_ember, proud→pride, pumpkin→pumpkin_spice, santa→candy_cane; library-only
    (19, no replaced_by): arctic, autumn, bias_lighting, cherry_blossom, cyberpunk,
    deep_sea, desert, epic, evening, galaxy, hygge, neon, relaxing, serene, spring,
    sports, tropical, vaporwave, water. These 28 values are copied verbatim from the
    locked table — exercise no palette or semantic judgement of your own.

    **2. Generator schema.** In scripts/generate_theme_data.py: add "disposition" to
    _REQUIRED_FIELDS; add "replaced_by" to _OPTIONAL_FIELDS; add a new module constant
    in the same #:-docstring style as _REQUIRED_FIELDS:
    `_DISPOSITIONS = frozenset({"lifx-app", "library-only", "deprecated"})`.

    **3. Three validations (D-08), each aborting via the existing _fail() pattern:**
    (a) In the per-record loop of validate_records(), after the existing name/category
    string checks: disposition must be a str (type(value) is not str check, matching the
    existing style) and a member of _DISPOSITIONS; the abort message names the offending
    value and the allowed set.
    (b) In the same loop: if disposition == "deprecated" and "replaced_by" is not a
    non-empty str in the record, abort with a message stating a deprecated record
    requires replaced_by. When replaced_by IS present, also require validate_key(replaced_by)
    (it must be a canonical key to ever resolve), reusing the existing canonical-key
    abort message shape.
    (c) Cross-record, AFTER the main loop completes (so seen_keys holds every slug and
    alias): iterate the records again and for any record carrying "replaced_by", abort
    via _fail() if the value is not in seen_keys — message naming the unresolvable key.
    Aliases count as resolution targets (SPEC: "resolves in THEMES").
    Exactly these three validations — do not add a fourth rejecting replaced_by on
    non-deprecated records. **Explicit deferral (review F3 — Codex MEDIUM, OpenCode
    LOW):** D-08 locks the generator to these three validations and places the
    replaced_by-only-when-deprecated invariant in the library-side shape sweep (Task 3)
    instead. Residual risk, accepted: a future data edit could pass a replaced_by onto a
    lifx-app record through the generator and into data.py; the sweep catches it at test
    time, not at generation time. **Emit-time asymmetry, accepted (review F3):** the
    emit-time backstop checks replaced_by is canonical but NOT that it resolves, while
    validate_records() checks both. Resolution needs whole-set knowledge (seen_keys)
    that the per-record emit backstops deliberately do not rebuild, and
    emit_data_module() called without validate_records() is a test-harness path, not a
    production path. Record both decisions in the SUMMARY.

    **4. Emit.** In emit_data_module(): ThemeRecord field lines gain `disposition: str`
    immediately after `category: str`, and `replaced_by: str | None = None` after
    `colors: tuple[HSBK, ...]` (default-carrying field must come last). Per-record
    emission gains `disposition={...!r}` after the category line and
    `replaced_by={record.get("replaced_by")!r}` after the colors block, so None prints
    as None on the 157 non-deprecated records. Extend the emit-time backstop block
    (defence in depth, same style as the name/category backstop): disposition must be in
    _DISPOSITIONS, and replaced_by must be None or pass validate_key(). Do NOT touch the
    alias emission pass — aliases must still be assigned after the dict literal.

    **5. Keep the generator test file valid:** add `"disposition": "lifx-app"` to the
    _record() helper in tests/test_theme/test_theme_generator.py so the minimal-valid
    record stays minimal-valid (this one-line change lands atomically with the schema
    change — either alone leaves the generator suite red).

    **6. Regenerate:** `uv run scripts/generate_theme_data.py`. Never hand-edit
    src/lifx/theme/data.py.
  </action>
  <verify>
    <automated>uv run python -c "from lifx.theme.data import THEMES; r=THEMES['fire']; assert r.disposition=='deprecated' and r.replaced_by=='warm_ember', (r.disposition, r.replaced_by); assert THEMES['hygge'].disposition=='library-only' and THEMES['hygge'].replaced_by is None; assert THEMES['christmas'].disposition=='lifx-app'; assert THEMES['forest'] is THEMES['forrest'] and THEMES['aurora_borealis'] is THEMES['aurora']; print('tracer ok')" && uv run pytest tests/test_theme/test_theme_generator.py tests/test_theme/test_library.py -x -q</automated>
  </verify>
  <done>
    The one path is wired end-to-end: a deprecated record's disposition and replaced_by
    travel from data/themes.jsonl through validation and emit into the importable
    generated THEMES dict; regeneration succeeds; alias identity is undisturbed; the
    existing generator and library suites are green.
  </done>
  <acceptance_criteria>
    - `uv run python -c "import json,sys; recs=[json.loads(l) for l in open('data/themes.jsonl') if l.strip()]; assert len(recs)==166; assert all('disposition' in r for r in recs); from collections import Counter; c=Counter(r['disposition'] for r in recs); assert c=={'lifx-app':138,'library-only':19,'deprecated':9}, c; assert all(('replaced_by' in r)==(r['disposition']=='deprecated') for r in recs); print('jsonl ok')"` prints `jsonl ok`
    - `grep -c "disposition" src/lifx/theme/data.py` is at least 167 (166 record kwargs + 1 field declaration)
    - src/lifx/theme/data.py contains `disposition: str` and `replaced_by: str | None = None`
    - scripts/generate_theme_data.py contains `_DISPOSITIONS` and `"disposition"` inside the `_REQUIRED_FIELDS` literal and `"replaced_by"` inside the `_OPTIONAL_FIELDS` literal
    - Running `uv run scripts/generate_theme_data.py` a second time then `git diff --exit-code src/lifx/theme/data.py` exits 0 (THEME-04 byte-idempotence)
    - `uv run pytest tests/test_theme/test_theme_generator.py -q` exits 0
    - `grep -rE "urlopen|urllib|requests|aiohttp|http://|https://" src/lifx/theme/*.py scripts/generate_theme_data.py | grep -v "^.*#.*github.com" | grep -vE "docstring|Djelibeybi/(aiolifx|lifx)"` finds no new network access (the no-network prohibition; pre-existing attribution URLs in docstrings are not network access). Review F12 deferral: an import/AST-based assertion was considered and declined — the generator has zero network imports and this sweep is adequate; revisit only if the attribution exceptions multiply
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface disposition on the public Theme via get()</name>
  <files>src/lifx/theme/theme.py, src/lifx/theme/library.py, tests/test_theme/test_theme.py</files>
  <read_first>
    - src/lifx/theme/theme.py (:16-101 — class docstring Attributes block and the D-16/D-17 keyword-only __init__ pattern to extend; :223-260 palette_equals, which must NOT change)
    - src/lifx/theme/library.py (:59-95 — get(), where the two kwargs thread through)
    - tests/test_theme/test_theme.py (existing constructor/identity tests, for placement and style)
  </read_first>
  <behavior>
    - Test 1: Theme([HSBK(...)]) with no keywords constructs; .disposition is None and .replaced_by is None (D-07 additive guarantee)
    - Test 2: Theme([...], disposition="library-only") sets the attribute; replaced_by stays None
    - Test 3: two Themes with equal palettes and different dispositions satisfy palette_equals() (R5: both new fields ignored)
    - Test 4: hash(Theme([...], disposition="deprecated", replaced_by="x")) works and == stays identity (D-20a unchanged)
  </behavior>
  <action>
    Implements D-06/D-07 — the public-surface half of COMPAT-04 (SPEC R5).

    In src/lifx/theme/theme.py: append `disposition: str | None = None` and
    `replaced_by: str | None = None` to the existing keyword-only block of
    Theme.__init__ (after category), assign both in the body exactly as slug/name/category
    are assigned. Extend the class docstring Attributes block and the __init__ Args block
    with entries for both, using the established "(None for a caller-constructed theme)"
    phrasing; for replaced_by state it is the successor key and None unless
    disposition is "deprecated". Also update the class docstring's copy-semantics note
    (the shuffled()/random() note at theme.py:31-34, which currently lists slug, name
    and category): replace the field list so it reads "slug, name, category, disposition
    and replaced_by do not propagate" — review F7: shuffled() constructs
    Theme(shuffled_colors) with no keywords, so the new fields do not survive either,
    and without this edit the note ships stale. Do not touch palette_equals(), __eq__
    absence, or any other method.

    In src/lifx/theme/library.py get(): add `disposition=record.disposition,
    replaced_by=record.replaced_by,` to the existing Theme(...) construction. No other
    change to library.py in this task (the taxonomy rewrite is plan 07-02's).

    Write the behaviour tests in tests/test_theme/test_theme.py first (they fail against
    the pre-change Theme with TypeError on the unknown keyword), then implement.
  </action>
  <verify>
    <automated>uv run python -c "from lifx.theme import ThemeLibrary as T; t=T.get('fire'); assert t.disposition=='deprecated' and t.replaced_by=='warm_ember', (t.disposition, t.replaced_by); h=T.get('hygge'); assert h.disposition=='library-only' and h.replaced_by is None; c=T.get('christmas'); assert c.disposition=='lifx-app' and c.replaced_by is None; print('surface ok')" && uv run pytest tests/test_theme/test_theme.py tests/test_theme/test_library.py -x -q</automated>
  </verify>
  <done>
    The tracer path now reaches the public API: get() returns Themes carrying disposition
    and replaced_by; caller-constructed Themes default both to None; palette_equals,
    identity equality and hashability are unchanged.
  </done>
  <acceptance_criteria>
    - src/lifx/theme/theme.py contains `disposition: str | None = None` and `replaced_by: str | None = None` inside the keyword-only parameter block of __init__
    - src/lifx/theme/library.py contains `disposition=record.disposition` and `replaced_by=record.replaced_by`
    - `uv run pytest tests/test_theme/ -q` exits 0
    - `uv run pyright src/lifx/theme/theme.py src/lifx/theme/library.py` reports 0 errors
    - `uv run python -c "from lifx.theme import Theme; t=Theme(); assert t.disposition is None and t.replaced_by is None; print('additive ok')"` prints `additive ok`
    - `grep -c "disposition and replaced_by do not propagate" src/lifx/theme/theme.py` returns 1 (the copy-semantics note names the new fields — review F7)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: D-08 validation tests plus disposition surfacing and data-shape sweeps</name>
  <files>tests/test_theme/test_theme_generator.py, tests/test_theme/test_library.py</files>
  <read_first>
    - tests/test_theme/test_theme_generator.py (:270-310 TestValidateRecordsSchema failing-record style to copy; :479+ TestEmitDataModule for the emit assertion style; :558-580 test_emit_time_backstops_reject_unvalidated_records — the EXISTING parametrised (record, expected) backstop matrix the new emit-time cases must extend; the _record()/_pairs()/_exec_module() helpers)
    - tests/test_theme/test_library.py (:432+ TestKeyErrorMessage and :454+ TestLibrarySweeps for class/style placement; :15 PRE_V12_KEYS)
    - .planning/phases/07-taxonomy-legacy-dispositions/07-SPEC.md (requirement 4/5 acceptance — the exact assertions to encode)
  </read_first>
  <action>
    Implements D-08 (exactly one failing-record test per validation) and SPEC R5/R7
    acceptance.

    **Coverage ground truth (review F1 — adjudicated against source):** the generator IS
    coverage-measured. pyproject.toml addopts run `--cov=lifx --cov=generate_theme_data
    --cov-branch`, and the `[tool.coverage.run]` omit list excludes only
    src/lifx/theme/data.py plus the protocol/products generators —
    scripts/generate_theme_data.py is hand-written and stays measured. Every new
    validation branch, every bad-value side of each compound condition, and both emit
    paths (replaced_by None vs set) must therefore execute under test or CI's 100%
    branch patch gate fails. (STATE.md's D-21 record claiming the theme generator is
    coverage-excluded is stale relative to current pyproject.toml; the orchestrator is
    correcting that record separately — do NOT edit STATE.md in this plan.) D-08 locks
    the NUMBER of generator validations at three; it does not license leaving their
    branches untested, so the bad-value tests below are branch coverage, not extra
    validations.

    **In tests/test_theme/test_theme_generator.py** (new class TestValidateRecordsDispositions,
    matching the existing failing-record pattern with pytest.raises(RuntimeError, match=...)):
    1. test_unknown_disposition_aborts — `_record(disposition="retired")` aborts naming
       the record, the line, and the bad value.
    2. test_deprecated_without_replacement_aborts — `_record(disposition="deprecated")`
       (no replaced_by) aborts with the controlled deprecated-requires-replaced_by error
       (the SPEC-pinned acceptance criterion).
    3. test_unresolvable_replaced_by_aborts — `_record(disposition="deprecated",
       replaced_by="no_such_theme")` aborts naming the unresolvable key (canonical form
       but absent from seen_keys — the cross-record branch).
    4. Branch-completing bad-value tests (review F1 — parametrised or named, same
       failing-record pattern; each is a DISTINCT branch from tests 1-3):
       - non-string disposition — `_record(disposition=123)` (the type-check side of the
         disposition condition, distinct from the unknown-string side in test 1);
       - empty replaced_by — `_record(disposition="deprecated", replaced_by="")`;
       - non-string replaced_by — `_record(disposition="deprecated", replaced_by=123)`;
       - present-but-non-canonical replaced_by — `_record(disposition="deprecated",
         replaced_by="Bad-Key")` failing validate_key (a different branch from the
         unresolved-but-canonical case in test 3).
    Emit-time backstops (review F1c): extend the EXISTING parametrised matrix
    test_emit_time_backstops_reject_unvalidated_records (:558-580) with two new
    (record, expected) rows — one record carrying an invalid disposition, one carrying a
    non-canonical replaced_by — rather than creating a disconnected test pattern.
    Also add to TestEmitDataModule: one passing test that a two-record emit (one
    deprecated with replaced_by pointing at the other, one lifx-app) round-trips through
    _exec_module(): the executed THEMES carries disposition values and the deprecated
    record's replaced_by, and the lifx-app record's replaced_by is None. Plus one
    resolution-via-alias test if cheap: replaced_by naming an alias of another record
    validates (aliases count as resolution targets); if it fits the same test, fold it in.

    **In tests/test_theme/test_library.py** (new class TestDispositionSurfacing):
    - the three SPEC R5 pinned lookups: get("fire") → ("deprecated", "warm_ember");
      get("hygge") → ("library-only", None); get("christmas") → ("lifx-app", None)
    - sweep over lifx.theme.data.THEMES: every record's disposition is one of the three
      allowed values; every record with disposition != "deprecated" has replaced_by None;
      every non-None replaced_by resolves as a key of THEMES (shape invariants, NOT
      count pins — do not assert 9/19/138 anywhere, declined per D-08/D-23)
    - alias identity: THEMES["forest"] is THEMES["forrest"] and THEMES["aurora_borealis"]
      is THEMES["aurora"] still hold with the new fields present (R7)
    Existing TestPreV12Compatibility already covers the 57-key fixture — leave it alone.
  </action>
  <verify>
    <automated>uv run pytest tests/test_theme/ -q && uv run pytest tests/test_theme/test_theme_generator.py -q -k "disposition or replaced" </automated>
  </verify>
  <done>
    The three D-08 failing-record tests exist and pass; disposition surfacing and the
    shipped-data shape invariants are asserted; no count-pinning test exists; the whole
    theme suite is green.
  </done>
  <acceptance_criteria>
    - `grep -c "def test_" tests/test_theme/test_theme_generator.py` increased by at least 4 relative to the pre-phase file
    - tests/test_theme/test_theme_generator.py contains test methods covering: unknown disposition value, deprecated without replacement, unresolvable replaced_by (the three D-08 validations, one failing-record test each)
    - tests/test_theme/test_theme_generator.py also exercises the review-F1 branch matrix: non-string disposition, empty replaced_by, non-string replaced_by, and present-but-non-canonical replaced_by (parametrised or named — every branch side runs)
    - test_emit_time_backstops_reject_unvalidated_records carries two new (record, expected) rows — invalid disposition and non-canonical replaced_by — in its existing parametrised matrix (review F1c)
    - tests/test_theme/test_library.py contains a TestDispositionSurfacing class asserting the fire/hygge/christmas triples from SPEC R5
    - No test in tests/test_theme/ asserts the literal counts 9, 19 or 138 of disposition values (D-08/D-23: count pinning declined)
    - `uv run pytest tests/test_theme/ -q` exits 0
    - `uv run pytest tests/test_theme/ --cov=lifx.theme --cov=generate_theme_data --cov-branch --cov-report=term-missing -q 2>/dev/null | grep -E "generate_theme_data|theme/theme|theme/library|TOTAL"` shows no Missing entries for generate_theme_data.py in the new validation/emit line ranges and no uncovered new branches in theme.py or library.py — the generator IS measured (review F1: the previous claim here that scripts/ is coverage-excluded per D-21 was false; pyproject addopts already carry `--cov=generate_theme_data --cov-branch`)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/themes.jsonl → generator | Committed data file is the sole input; a tampered line is the attack surface |
| generator → src/lifx/theme/data.py | Emitted source is imported by the package; injected text would execute at import |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | data/themes.jsonl disposition/replaced_by fields | medium | mitigate | Three controlled validations (allowed set, deprecated↔replaced_by presence, cross-record resolution) plus emit-time backstops re-asserting disposition ∈ _DISPOSITIONS and replaced_by is None-or-canonical-key before any value reaches emitted source text |
| T-07-02 | Tampering (supply chain) | regeneration inputs | medium | mitigate | Regeneration reads only the committed local data file — no network, no device, no LIFX endpoint (SPEC prohibition; same control and evidence as Phase 6 T-06-03/T-06-06). Zero packages are installed by this phase, so no package-legitimacy audit applies |
| T-07-03 | Information disclosure | Theme.replaced_by | low | accept | Field carries only public theme keys already listable via get_available_themes(); nothing sensitive to disclose |
</threat_model>

<verification>
- `uv run pytest tests/test_theme/ -q` exits 0 (full theme suite)
- `uv run pyright` reports 0 errors; `uv run ruff check .` clean
- Idempotence: `uv run scripts/generate_theme_data.py && git diff --exit-code src/lifx/theme/data.py` exits 0
- Phase-acceptance split check (run once, not committed as a test): `uv run python -c "from lifx.theme.data import THEMES; recs={id(r): r for r in THEMES.values()}.values(); from collections import Counter; print(Counter(r.disposition for r in recs))"` prints exactly `{'lifx-app': 138, 'library-only': 19, 'deprecated': 9}`
- All 168 keys resolve: `uv run python -c "from lifx.theme.data import THEMES; assert len(THEMES)==168"`
</verification>

<success_criteria>
- COMPAT-04 data half delivered: 166/166 records carry a disposition; 9 deprecated with the locked replacements; 19 library-only; every replaced_by resolves
- SPEC R5 delivered: Theme.disposition / Theme.replaced_by readable on get() results; palette_equals and hashability unchanged
- SPEC R7 held: 168 names resolve, PRE_V12_KEYS passes, alias identity intact
- THEME-04 held: regeneration byte-idempotent
- D-08 tests exist; no count-pinning test exists
</success_criteria>

<output>
Create `.planning/phases/07-taxonomy-legacy-dispositions/07-01-SUMMARY.md` when done.
</output>
