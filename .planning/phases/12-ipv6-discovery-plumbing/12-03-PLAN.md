---
phase: 12-ipv6-discovery-plumbing
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - tests/test_api/test_ipv6_e2e.py
autonomous: true
requirements: [FIND-06]
tags: [ipv6, discovery, concurrency, cancellation, lifecycle]

estimate:
  tokens: 16000
  raw_tokens: 32000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Two concurrent public IPv6 targeted lookups open two distinct real `AF_INET6` endpoints and both complete independently with the expected synthetic device (D-09; AC5)."
    - "Cancellation happens only after a spy proves the real endpoint is open and a real receive is blocked; cancellation closes that endpoint without a sleep or pre-start race (D-07; AC5)."
    - "After cancellation, the retained endpoint reports closing/closed and a fresh real `find_by_ip(\"::1\")` lookup succeeds against the emulator (D-08; AC5, AC7)."
    - "Concurrency and cancellation remain separate tests so either failure identifies one lifecycle invariant rather than conflating two causes (D-09)."
    - "Edge R1 concurrency is covered: concurrent calls are independent, cancellation releases its endpoint, and a later call can reuse the public path."
  artifacts:
    - path: "tests/test_api/test_ipv6_e2e.py"
      provides: "Emulator-marked lifecycle class plus test-local observation queues/events for deterministic real-endpoint concurrency, cancellation, cleanup, and reuse regressions"
      contains: "class TestIpv6TargetedDiscoveryLifecycle"
  key_links:
    - from: "tests/test_api/test_ipv6_e2e.py::_ObservedDiscoveryTransport.receive"
      to: "src/lifx/network/transport.py::UdpTransport.receive"
      via: "a fresh per-test observation queue exposes each instance record, whose events signal immediately before awaiting the real blocked receive"
      pattern: "receive_started"
    - from: "src/lifx/network/discovery.py::_discover_with_packet"
      to: "src/lifx/network/transport.py::UdpTransport.__aexit__"
      via: "cancellation propagates through the real receive and the innermost async transport context closes the endpoint; Plan 12-01's successful-return tests, not this cancellation assertion, discriminate the two outer `aclosing` owners"
      pattern: "async with UdpTransport"
    - from: "tests/test_api/test_ipv6_e2e.py::TestIpv6TargetedDiscoveryLifecycle"
      to: "src/lifx/api.py::find_by_ip"
      via: "all concurrent, cancelled, and reuse operations enter through the public API"
      pattern: "find_by_ip"
  prohibitions:
    - statement: "MUST NOT place live serials, IP addresses, hostnames, account identifiers, or raw discovery output in Phase 12 tests, documentation, commits, or evidence."
      status: resolved
      verification: "AC11 loopback/synthetic fixtures plus value-suppressed staged-diff privacy inspection"
    - statement: "MUST NOT count a pre-existing Windows skip or allowed-failure state as the Phase 12 Windows attempt; the targeted test must actually run, or the precise external blocker and explicit D-11 operator decision must be recorded."
      status: resolved
      verification: "AC9 required CI step and Plan 12-04 blocking evidence checkpoint"
---

<objective>
Expand the proven IPv6 public lookup into deterministic concurrent-call, cancellation-cleanup, and post-cancellation reuse evidence using actual datagram endpoints.

Purpose: protect the shared discovery generator from socket leaks and cross-call interference when callers cancel or overlap targeted IPv6 lookups.
Output: separate real-endpoint concurrency and cancellation/reuse tests.
</objective>

<execution_context>
@~/.codex/gsd-core/workflows/execute-plan.md
@~/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ipv6-discovery-plumbing/12-CONTEXT.md
@.planning/phases/12-ipv6-discovery-plumbing/12-SPEC.md
@.planning/phases/12-ipv6-discovery-plumbing/12-RESEARCH.md
@.planning/phases/12-ipv6-discovery-plumbing/12-PATTERNS.md
@.planning/phases/12-ipv6-discovery-plumbing/12-SOURCE-AUDIT.md
@.planning/phases/12-ipv6-discovery-plumbing/12-01-SUMMARY.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-08-SUMMARY.md
@.planning/phases/10-land-the-ipv6-thread-branch/10-09-SUMMARY.md
@src/lifx/network/discovery.py
@src/lifx/network/transport.py
@src/lifx/api.py
@tests/conftest.py
@tests/test_api/test_ipv6_e2e.py
</context>

## Artifacts this phase produces

This plan adds only test behaviour: the emulator-marked `TestIpv6TargetedDiscoveryLifecycle` class
and fresh per-test `_DiscoveryObservation` queues/events supplied through
`_make_observed_discovery_transport()`. It adds no production symbol or file and no class-level
mutable observation registry.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove two IPv6 lookups own independent real endpoints</name>
  <reversibility rating="reversible">The task adds deterministic concurrency evidence around per-call state and changes no runtime implementation.</reversibility>
  <files>tests/test_api/test_ipv6_e2e.py</files>
  <read_first>
    - .planning/phases/12-ipv6-discovery-plumbing/12-CONTEXT.md (D-09 distinct concurrency test)
    - .planning/phases/12-ipv6-discovery-plumbing/12-SPEC.md (AC5, AC6, AC7 and Edge R1 concurrency)
    - .planning/phases/12-ipv6-discovery-plumbing/12-01-SUMMARY.md (real tracer and observation helper contract)
    - src/lifx/network/discovery.py (`_discover_with_packet` per-call source, seen-set, deadlines, and transport context)
    - src/lifx/network/transport.py (`UdpTransport` instance ownership and endpoint lifecycle)
    - tests/conftest.py (`emulator_server_ipv6` session fixture and synthetic device)
    - tests/test_api/test_ipv6_e2e.py (`_ObservedDiscoveryTransport` and `TestIpv6TargetedDiscovery`)
  </read_first>
  <behavior>
    - Test 1: two concurrently scheduled `find_by_ip("::1")` calls both return the expected `MatrixLight`
    - Test 2: exactly two tasks are created with `asyncio.create_task()` and awaited together with `asyncio.gather()`, which is available on every supported Python version including 3.10
    - Test 3: observation records show two different transport instances and two distinct non-zero local endpoint ports
    - Test 4: both sockets are `AF_INET6`, bound to `::`, send to the same caller-supplied emulator destination, and close independently
  </behavior>
  <action>
    Add `TestIpv6TargetedDiscoveryLifecycle::test_concurrent_lookups_use_independent_endpoints` and mark the class with `@pytest.mark.emulator` so every emulator-backed lifecycle test receives the repository's 120-second timeout policy. Start exactly two public `find_by_ip("::1", port=fixture_port, timeout=1.0, idle_timeout_multiplier=0.5)` calls with two explicit `asyncio.create_task()` calls, then await both together with `asyncio.gather()` and assert the two results in deterministic creation order. This Python 3.10-compatible pattern is required because `asyncio.TaskGroup` is unavailable at the project's supported floor. Create a fresh `asyncio.Queue[_DiscoveryObservation]` inside this test and pass it to `_make_observed_discovery_transport()` before patching the consumed transport name. Each real transport instance publishes exactly one distinct record into that queue; await two queue reads rather than polling or clearing class state. After both calls complete, assert both product-derived results and the two distinct actual local ports, correct family/wildcard/destination, and independent closed endpoints. Do not add batching, shared sockets, sleeps, cancellation, production locks, or module/class mutable observation state. This isolates D-09's concurrency contract from Task 2's cancellation contract.
  </action>
  <verify>
    <automated>uv run pytest tests/test_api/test_ipv6_e2e.py::TestIpv6TargetedDiscoveryLifecycle::test_concurrent_lookups_use_independent_endpoints -q -W error::ResourceWarning</automated>
  </verify>
  <acceptance_criteria>
    - Exactly two public lookup tasks complete successfully and return the fixture's matrix-capable Device
    - The test uses two explicit `asyncio.create_task()` calls plus `asyncio.gather()` and runs under Python 3.10 without `asyncio.TaskGroup`
    - Exactly two distinct real discovery endpoint instances and local ephemeral ports are observed
    - Each endpoint is `AF_INET6`, wildcard-bound to `::`, targets the fixture port, and closes after its own lookup
    - The test contains no cancellation, sleep, mocked public API, or mutable state leaking into another test
    - `TestIpv6TargetedDiscoveryLifecycle` carries `@pytest.mark.emulator`, and observations arrive through a queue created inside this test rather than a class-level list
    - Per-call discovery source, serial deduplication, and deadline ownership remain production-owned
  </acceptance_criteria>
  <done>Concurrent IPv6 targeted lookups are proven to be independent end-to-end operations with separate real socket ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cancel only after blocked receive, prove close, then reuse the path</name>
  <reversibility rating="reversible">This task observes the existing async context-manager cancellation semantics and introduces no one-way choice.</reversibility>
  <files>tests/test_api/test_ipv6_e2e.py</files>
  <read_first>
    - .planning/phases/12-ipv6-discovery-plumbing/12-CONTEXT.md (D-07 deterministic cancellation, D-08 close and fresh success, D-09 test separation)
    - .planning/phases/12-ipv6-discovery-plumbing/12-SPEC.md (AC5-AC7)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-08-SUMMARY.md (cancellation-safe transport ownership)
    - .planning/phases/10-land-the-ipv6-thread-branch/10-09-SUMMARY.md (atomic endpoint publication and stale-callback safety)
    - src/lifx/network/discovery.py (`async with UdpTransport` cancellation boundary)
    - src/lifx/network/transport.py (`receive`, `close`, `is_open`, endpoint `is_closing()`)
    - tests/conftest.py (`get_free_port6`, real IPv6 emulator fixture)
    - tests/test_api/test_ipv6_e2e.py (observation transport and lifecycle class from Task 1)
  </read_first>
  <behavior>
    - Test 1: a lookup to an unused IPv6 loopback port signals that its real endpoint opened and entered the real blocked receive before cancellation
    - Test 2: cancelling the task raises `CancelledError` to the waiter and closes the retained endpoint/transport
    - Test 3: a subsequent public lookup against the real IPv6 emulator returns the expected synthetic `MatrixLight` through a fresh endpoint
  </behavior>
  <action>
    Add a separate `test_cancellation_closes_endpoint_and_next_lookup_succeeds`. Create a fresh observation queue and transport class through `_make_observed_discovery_transport()` for this test. Each `_DiscoveryObservation` owns its own `opened`, `receive_started`, and `closed` `asyncio.Event` objects; obtain the instance record by awaiting the queue, then await `opened` and `receive_started` without polling. Set `receive_started` immediately before awaiting `super().receive(timeout=...)`; do not replace the receive with a fake wait. Reserve an unused synthetic `::1` UDP port, start one public lookup to that port, await the record and both events, retain the real endpoint/socket observation, cancel the task, and assert `CancelledError`. Then await the record's `closed` event, assert `UdpTransport.is_open` is false, and assert the retained asyncio endpoint reports closing/closed. State the mechanism accurately: cancellation raised inside `UdpTransport.receive()` reaches the innermost `UdpTransport` context before the two outer generator owners finish unwinding, so endpoint closure proves D-07/D-08 transport cleanup but does not discriminate whether Plan 12-01's outer `aclosing` calls exist. The successful-return tracer and `TestDiscoveryGeneratorOwnership` remain the explicit discriminating ownership proofs. Finally invoke a fresh public lookup against `emulator_server_ipv6` and prove the expected matrix-capable Device returns through a different endpoint. Use event handshakes only—no sleep, class state, polling interval, cancellation before start, or elapsed-time assertion. This implements D-07, D-08, and the lifecycle part of AC5.
  </action>
  <verify>
    <automated>uv run pytest tests/test_api/test_ipv6_e2e.py::TestIpv6TargetedDiscoveryLifecycle::test_cancellation_closes_endpoint_and_next_lookup_succeeds -q -W error::ResourceWarning &amp;&amp; uv run pytest tests/test_network/test_transport.py -q -W error::ResourceWarning</automated>
  </verify>
  <acceptance_criteria>
    - Both open and receive-started events are observed before the task is cancelled
    - The receive awaiting cancellation is the real `UdpTransport.receive()`, not an artificial future
    - Cancellation propagates as `CancelledError` and closes the transport plus retained endpoint through the innermost transport context; the plan does not misstate this assertion as proof of the outer `aclosing` owners
    - A fresh per-test observation queue supplies the exact instance events without polling or class-level mutable state
    - A fresh emulator-backed public lookup succeeds immediately afterwards through a different real endpoint
    - There are no sleeps, pre-start cancellation races, hardware calls, or external-network dependencies
    - Existing transport cancellation/reopen/resource tests remain green with ResourceWarning treated as an error
  </acceptance_criteria>
  <done>Cancellation deterministically releases the real IPv6 discovery endpoint and leaves the public targeted-lookup path immediately reusable.</done>
</task>

</tasks>

<threat_model>
## Security Profile

- `asvs_level: L1`
- `block_threshold: high`; unresolved critical/high threats block completion

## Trust Boundaries

| Boundary | Description |
|---|---|
| Concurrent caller tasks -> per-call discovery state | Sources, endpoints, seen sets, and deadlines must not cross-contaminate |
| Task cancellation -> asyncio transport ownership | Cancellation must run context cleanup and release the datagram endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-08 | Tampering | concurrent per-call discovery state | high | mitigate | Prove two transports/endpoints and two successful product-derived results in a dedicated concurrency test (Task 1) |
| T-12-09 | Denial of Service | cancellation cleanup | high | mitigate | Synchronise on real open/blocked-receive events, cancel, assert close, and prove fresh reuse (Task 2) |
| T-12-10 | Repudiation | lifecycle evidence | medium | mitigate | Assert actual endpoint state and ResourceWarning-clean tests rather than relying on elapsed time or intent fields |
| T-12-11 | Information Disclosure | endpoint observations | high | mitigate | Restrict assertions to loopback, family, wildcard, and ephemeral port properties; commit no live network data |
| T-12-SC | Tampering | package supply chain | low | accept | No install or dependency change is planned |
</threat_model>

## Multi-Source Coverage Audit

The complete audit is in `12-SOURCE-AUDIT.md`. This plan covers FIND-06, D-07 through
D-09, AC5-AC7, Edge R1 concurrency, and both phase prohibitions.

<verification>
- `uv run pytest tests/test_api/test_ipv6_e2e.py::TestIpv6TargetedDiscoveryLifecycle -q -W error::ResourceWarning`
- `uv run pytest tests/test_network/test_transport.py -q -W error::ResourceWarning`
- `uv run ruff format --check tests/test_api/test_ipv6_e2e.py`
- `uv run ruff check tests/test_api/test_ipv6_e2e.py`
- `uv run pyright`
</verification>

<success_criteria>
Two real IPv6 lookups coexist without shared endpoint state, cancellation deterministically closes an in-flight endpoint through the inner transport context, the successful-return tests remain the discriminating outer-generator ownership proof, and a fresh public lookup succeeds afterwards.
</success_criteria>

<output>
Create `.planning/phases/12-ipv6-discovery-plumbing/12-03-SUMMARY.md` when done.
</output>
